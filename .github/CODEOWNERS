# CODEOWNERS — required-review gate for security-sensitive files.
#
# GitHub's CODEOWNERS spec: https://docs.github.com/en/repositories/managing-your-repositories-settings-and-features/customizing-your-repository/about-code-owners
# Branch-protection rule "Require review from Code Owners" must also be enabled
# in repository settings for this file to enforce reviews — CODEOWNERS alone
# only suggests reviewers.
#
# Rules below are last-match-wins per GitHub's spec.

# HLS proxy allow-list (HLSProxyAllowedDomains in proxy.go). Any new entry
# adds a host that user clients and the scraper will trust; this gate
# requires the security owner to sign off on additions. Prefer signing at
# the source (catalog streamsign) over adding entries.
/libs/videoutils/proxy.go            @StaticVirtualObserver
/docs/security/                      @StaticVirtualObserver
