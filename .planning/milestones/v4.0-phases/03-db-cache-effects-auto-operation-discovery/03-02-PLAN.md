---
phase: 03-db-cache-effects-auto-operation-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/tempo/tempo.yaml
  - docker/docker-compose.yml
  - docker/prometheus/prometheus.yml
  - docker/grafana/provisioning/datasources/datasources.yml
autonomous: true
requirements: [AR-EFFECT-04]
must_haves:
  truths:
    - "Tempo's metrics_generator (span-metrics + service-graphs) is enabled with remote_write to Prometheus, config-only"
    - "Prometheus accepts remote-write at its /prometheus route-prefix"
    - "The Grafana Tempo datasource links to Prometheus for service map + traces-to-metrics"
    - "Implements D-12 (Tempo metrics_generator span-metrics + service-graphs enabled by config only, no per-span code); carries the D-14 Phase-6 hand-off note that these RED metrics later move to the OTel Collector spanmetrics connector"
  artifacts:
    - path: "infra/tempo/tempo.yaml"
      provides: "metrics_generator block + overrides.defaults.metrics_generator.processors enable list"
      contains: "metrics_generator"
    - path: "docker/docker-compose.yml"
      provides: "Prometheus --web.enable-remote-write-receiver flag"
      contains: "enable-remote-write-receiver"
    - path: "docker/grafana/provisioning/datasources/datasources.yml"
      provides: "Tempo serviceMap.datasourceUid + tracesToMetrics wiring to Prometheus"
      contains: "serviceMap"
  key_links:
    - from: "infra/tempo/tempo.yaml metrics_generator.storage.remote_write"
      to: "prometheus:9090/prometheus/api/v1/write"
      via: "remote-write URL matching the route-prefix"
      pattern: "prometheus.*api/v1/write"
    - from: "infra/tempo/tempo.yaml overrides.defaults.metrics_generator"
      to: "processors [span-metrics, service-graphs]"
      via: "per-tenant enable list (Pitfall 6)"
      pattern: "service-graphs"
---

<objective>
Enable Tempo's span-metrics + service-graphs generator (config only, no per-span code) so per-operation RED metrics and a service graph appear in Prometheus + Grafana. Fully independent of the Go effect work — no `libs/` or service code is touched — so this runs in parallel with the Wave-1 foundation.

Purpose: AR-EFFECT-04 — the free, total-coverage popularity/RED layer (D-12/D-13) that complements the sparse fact-row cost ledger. Note the Phase-6 hand-off (D-14): this generator lives in Tempo, which Phase 6 retires; the RED metrics will later be regenerated by the OTel Collector spanmetrics connector. Do NOT pre-empt Phase 6 — enable in Tempo now.

Output: Tempo `metrics_generator` config, Prometheus remote-write-receiver, Grafana Tempo->Prometheus links.

Effort: `UXΔ = +1 (Better)` (per-operation RED + service graph for admins; indirect user benefit via faster triage) · `CDI = 0.03 * 5` (pure config across 4 infra files; no code-structure disruption) · `MVQ = Sprite 84%/82%` (small surgical config that lights up total-coverage metrics for free; slop-resistant because it leans on a built-in Tempo processor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-CONTEXT.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-RESEARCH.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Tempo metrics_generator block + verify/enable Prometheus remote-write-receiver</name>
  <read_first>
    - infra/tempo/tempo.yaml (the full current file — confirm it has server/distributor/ingester/compactor/storage/usage_report and NO metrics_generator block today)
    - docker/docker-compose.yml (the prometheus + tempo service blocks — find Prometheus command args + route-prefix; confirm whether --web.enable-remote-write-receiver is present)
    - docker/prometheus/prometheus.yml (route-prefix / external_url context — the existing datasource url is http://prometheus:9090/prometheus)
    - .planning/phases/03-db-cache-effects-auto-operation-discovery/03-RESEARCH.md (Code Examples: the exact metrics_generator YAML; Pitfall 6: the overrides enable list is REQUIRED; Open Q1 + A7: remote-write-receiver flag)
  </read_first>
  <action>
    Add to `infra/tempo/tempo.yaml` a top-level `metrics_generator:` block with `registry.external_labels.source: tempo`, `storage.path: /var/tempo/generator/wal`, `storage.remote_write` pointing at `http://prometheus:9090/prometheus/api/v1/write` with `send_exemplars: true`, `traces_storage.path: /var/tempo/generator/traces`, and `processor.span_metrics.dimensions: [service.name, span.name, operation]` (the `operation` dimension surfaces the per-op RED metric) + `processor.service_graphs.dimensions: [service.name]`. CRITICAL (Pitfall 6): also add the per-tenant enable — `overrides.defaults.metrics_generator.processors: [span-metrics, service-graphs]` — or the generator silently no-ops. In `docker/docker-compose.yml`, ensure the Prometheus container command includes `--web.enable-remote-write-receiver` (add it if absent) so `/prometheus/api/v1/write` accepts Tempo's remote_write; keep the existing `--web.route-prefix`/external-url args intact so the `/prometheus` prefix still resolves. Ensure the Tempo container has a writable generator WAL/traces volume path (mount or use the existing tempo data volume). Match the remote_write URL to the actual route-prefix you find in the compose/prometheus config (RESEARCH assumes `/prometheus` — verify, don't assume blindly).
  </action>
  <verify>
    <automated>python3 -c "import yaml;d=yaml.safe_load(open('infra/tempo/tempo.yaml'));assert d['metrics_generator']['processor']['span_metrics'];assert 'span-metrics' in d['overrides']['defaults']['metrics_generator']['processors'];print('tempo metrics_generator OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python yaml assertion prints `tempo metrics_generator OK`.
    - Source assertion: `grep -c 'metrics_generator' infra/tempo/tempo.yaml` returns >=2 (the block + the overrides enable).
    - Source assertion: `grep -c 'span-metrics' infra/tempo/tempo.yaml` returns >=1 (overrides enable list present — Pitfall 6).
    - Source assertion: `grep -c 'enable-remote-write-receiver' docker/docker-compose.yml` returns >=1.
    - Behavior assertion: the remote_write URL in tempo.yaml ends with the SAME route-prefix path used by the existing Grafana Prometheus datasource (`/prometheus/api/v1/write`).
  </acceptance_criteria>
  <done>Tempo metrics_generator + overrides enable list present; Prometheus accepts remote-write at the correct route-prefix; config validates.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Grafana Tempo datasource to Prometheus (serviceMap + tracesToMetrics)</name>
  <read_first>
    - docker/grafana/provisioning/datasources/datasources.yml (the Tempo datasource block — confirm nodeGraph.enabled:true already; find the Prometheus datasource uid, e.g. PBFA97CFB590B2093)
    - .planning/phases/03-db-cache-effects-auto-operation-discovery/03-RESEARCH.md (Code Examples Grafana wiring note)
  </read_first>
  <action>
    In `docker/grafana/provisioning/datasources/datasources.yml`, on the Tempo datasource `jsonData`, add `serviceMap.datasourceUid: <prometheus-uid>` (use the existing Prometheus datasource uid you find in the file) so the service graph resolves `traces_service_graph_*` metrics, and add a `tracesToMetrics` block pointing at the same Prometheus uid so per-operation RED panels resolve against `traces_spanmetrics_*`. Keep `nodeGraph.enabled: true` intact. Do NOT change the Prometheus or ClickHouse datasource entries.
  </action>
  <verify>
    <automated>python3 -c "import yaml;d=yaml.safe_load(open('docker/grafana/provisioning/datasources/datasources.yml'));ts=[x for x in d['datasources'] if x.get('type')=='tempo'][0];jd=ts['jsonData'];assert 'serviceMap' in jd;assert 'tracesToMetrics' in jd;print('grafana tempo->prom wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python yaml assertion prints `grafana tempo->prom wiring OK`.
    - Source assertion: `grep -c 'serviceMap' docker/grafana/provisioning/datasources/datasources.yml` returns >=1.
    - Source assertion: `grep -c 'tracesToMetrics' docker/grafana/provisioning/datasources/datasources.yml` returns >=1.
    - Behavior assertion: the serviceMap/tracesToMetrics datasourceUid equals the uid of the existing Prometheus datasource in the same file (not a placeholder).
  </acceptance_criteria>
  <done>Tempo datasource links to Prometheus for both service map and traces-to-metrics; Prometheus/ClickHouse datasources untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tempo generator → Prometheus remote-write | Internal Docker network; the remote-write receiver accepts pushed metrics |
| Prometheus /prometheus route-prefix | Admin-only metrics surface; not user-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Prometheus remote-write-receiver accepts arbitrary pushed series | accept | Receiver reachable only on the Docker network; Tempo is the sole writer; not gateway-exposed |
| T-03-05 | Denial of Service | high-cardinality span-metrics labels (operation) blow up Prometheus TSDB | mitigate | Dimensions limited to [service.name, span.name, operation]; operation is the fine-but-bounded service-frame label, not raw URLs; revisit cardinality in Phase 5 dashboards |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages); config-only change |
</threat_model>

<verification>
- Tempo config validates with the metrics_generator block + overrides enable present.
- Prometheus compose args include `--web.enable-remote-write-receiver`.
- Grafana Tempo datasource carries serviceMap + tracesToMetrics pointed at Prometheus.
- (Live confirmation of `traces_spanmetrics_*` in Prometheus + service graph in Grafana is the phase-gate checkpoint in plan 06.)
</verification>

<success_criteria>
- AR-EFFECT-04 config in place: span-metrics + service-graphs generator enabled, remote-write target correct, Grafana wired.
- No application code touched — config-only, parallel-safe with the Go work.
</success_criteria>

<output>
Create `.planning/phases/03-db-cache-effects-auto-operation-discovery/03-02-SUMMARY.md` when done.
</output>
