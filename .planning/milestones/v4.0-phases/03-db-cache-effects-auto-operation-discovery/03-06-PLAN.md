---
phase: 03-db-cache-effects-auto-operation-discovery
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - libs/tracing/client.go
  - services/catalog/cmd/catalog-api/main.go
  - services/player/cmd/player-api/main.go
  - services/themes/cmd/themes-api/main.go
  - services/themes/internal/config/config.go
  - services/auth/cmd/auth-api/main.go
  - services/notifications/cmd/notifications-api/main.go
  - services/scheduler/cmd/scheduler-api/main.go
  - services/library/cmd/library-api/main.go
  - services/library/internal/config/config.go
  - services/gateway/cmd/gateway-api/main.go
autonomous: false
requirements: [AR-EFFECT-01, AR-EFFECT-02, AR-EFFECT-03, AR-EFFECT-04]
must_haves:
  truths:
    - "All 7 GORM services register the DB-effect callbacks + a ReadGate + a ThresholdRefresher after InstrumentGORM"
    - "Implements D-15 (DB effect recording is wired across ALL 7 gormtrace services: catalog, player, themes, auth, notifications, scheduler, library)"
    - "player/auth/scheduler/catalog reuse their existing Redis client for the refresher; themes/notifications/library construct one from cfg.Redis (themes+library also gain the cfg.Redis field) so all 7 refreshers have a live Redis handle"
    - "Cache-using services instantiate + Start/Stop the cache aggregator; the aggregator flushes BEFORE the producer drains on shutdown"
    - "catalog is the cache-effect service; gateway is N/A for the cache aggregator because its rate-limit cache uses a raw redis.Client (redis_rate/v10), NOT libs/cache.RedisCache — the aggregator's only hook seam (D-17 useful-signal judgement, planner discretion)"
    - "services/analytics is NEVER wired into effect recording (D-16)"
    - "Live: a DB write, a cached miss-then-hit, and a real catalog.* operation appear in ClickHouse; per-op RED metrics + service graph appear in Prometheus/Grafana"
  artifacts:
    - path: "services/catalog/cmd/catalog-api/main.go"
      provides: "RegisterEffectCallbacks + NewReadGate + ThresholdRefresher.Start + CacheAggregator wired into the existing SetGlobalSink/InstrumentGORM block"
      contains: "RegisterEffectCallbacks"
    - path: "libs/tracing/client.go"
      provides: "GlobalSink() getter so the gorm/cache hooks can reach the process sink (if not already exported)"
      contains: "func GlobalSink"
  key_links:
    - from: "each GORM service main.go"
      to: "gormtrace.RegisterEffectCallbacks(db.DB, tracing.GlobalSink(), gate)"
      via: "boot wiring after InstrumentGORM + SetGlobalSink"
      pattern: "RegisterEffectCallbacks"
    - from: "catalog (cache-effect service) main.go"
      to: "cache.CacheAggregator.Start() / defer Stop()"
      via: "instantiate + graceful flush-before-producer-drain on the libs/cache.RedisCache"
      pattern: "Aggregator"
---

<objective>
Wire the new DB-effect callbacks, ReadGate + ThresholdRefresher, and cache aggregator into every affected service's boot, then prove the whole phase live against the running stack. This is the integration + phase-gate plan: it has a human-verify checkpoint because the four success criteria are end-to-end (real rows in ClickHouse, real metrics in Prometheus, a service graph in Grafana) — the proven Phase-2 Task-3 pattern.

Purpose: AR-EFFECT-01..04 end-to-end. The shared logic shipped in plans 01/03/04/05; this plan instantiates it per service following the exact `catalog/cmd/catalog-api/main.go` (`InstrumentGORM` + `SetGlobalSink`) + `streaming/cmd/streaming-api/main.go` (`Producer.Start`/`defer Stop`, aggregator `Start`/`defer Stop`) anchors. D-16: analytics is NEVER wired. D-15: all 7 GORM services + the useful cache services. D-17 (planner discretion — "if the signal is useful"): the cache aggregator hooks `libs/cache.RedisCache.Get/Set` exclusively; **catalog** is the cache-effect service. **gateway is N/A**: its only Redis usage is the per-user rate limiter, built on a raw `redis.Client` + `redis_rate/v10` (gateway main.go ~lines 59-68, with a comment explicitly rejecting `libs/cache`), so there is no `libs/cache.RedisCache` for the aggregator to hook — wiring it would require rebuilding the limiter on `libs/cache`, out of phase scope. AR-EFFECT-02 is fully satisfied by catalog. rooms/watch-together also skipped (Redis-as-datastore, low signal).

Output: per-service boot wiring + the optional `GlobalSink()` getter + a live verification checkpoint.

Effort: `UXΔ = +1 (Better)` (the register goes live across all data services) · `CDI = 0.07 * 13` (mechanical-but-broad wiring across 7 GORM main.go files + catalog's aggregator + one getter + a Redis client/config add in three services; the spread is the effort, each edit mirrors the catalog/streaming anchor) · `MVQ = Kraken 85%/83%` (tentacles into every data service's boot; slop-resistant because every edit is a copy of the same proven wiring block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-CONTEXT.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-RESEARCH.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-PATTERNS.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-01-SUMMARY.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-03-SUMMARY.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-04-SUMMARY.md
@.planning/phases/03-db-cache-effects-auto-operation-discovery/03-05-SUMMARY.md

<interfaces>
<!-- The boot APIs to wire. Confirm exact names against the SUMMARYs above. -->

libs/tracing/client.go (current): SetGlobalSink(sink EffectSink); globalSink atomic.Pointer[EffectSink].
  → If no exported getter exists, add: func GlobalSink() EffectSink { if sp := globalSink.Load(); sp != nil { return *sp }; return nil }

libs/tracing/gormtrace (post plan-03): RegisterEffectCallbacks(db *gorm.DB, sink tracing.EffectSink, gate ReadGate) error; NewReadGate(staticDefaultMS int) *snapshotGate.
libs/tracing/gormtrace (post plan-05): ThresholdRefresher{...}.Start(ctx); .Stop(). Needs a Redis client (or HGetAll reader) to snapshot the read_thresholds hash.
libs/cache (post plan-04): CacheAggregator{...}; cache.New(cfg) already builds RedisCache — add the aggregator as an optional field/setter; .Start(); .Stop().

Redis-client inventory (verified at planning time):
  - HAS a Redis client / cfg.Redis today: catalog, player, auth, scheduler (reuse it for the refresher).
  - notifications: has cfg.Redis (config.go:18 `Redis cache.Config`) but does NOT construct a client in main.go — construct one.
  - themes, library: NO cfg.Redis field and NO client — add a `Redis cache.Config` config field (mirror notifications/catalog config.go) + construct a client in main.go.
  - gateway: raw redis.Client via redis_rate/v10 (main.go ~59-68); NOT libs/cache — see D-17 N/A note below.

EXISTING per-service anchor (catalog/cmd/catalog-api/main.go):
```go
gormtrace.InstrumentGORM(db.DB)            // line 68
redisCache, _ := cache.New(cfg.Redis)      // line 114
effectProducer := tracing.NewProducer(...) // line 130
effectProducer.Start(); defer Stop()       // 131-132
tracing.SetGlobalSink(effectProducer)      // line 133
```
streaming/cmd/streaming-api/main.go (82-91) shows the aggregator Start()/defer Stop() ordering: flush the aggregator BEFORE the producer drains.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GlobalSink getter + wire DB-effect callbacks, ReadGate, and ThresholdRefresher into the 7 GORM services</name>
  <files>libs/tracing/client.go, services/catalog/cmd/catalog-api/main.go, services/player/cmd/player-api/main.go, services/themes/cmd/themes-api/main.go, services/themes/internal/config/config.go, services/auth/cmd/auth-api/main.go, services/notifications/cmd/notifications-api/main.go, services/scheduler/cmd/scheduler-api/main.go, services/library/cmd/library-api/main.go, services/library/internal/config/config.go</files>
  <read_first>
    - libs/tracing/client.go (SetGlobalSink + globalSink atomic.Pointer — add GlobalSink() getter if absent)
    - services/catalog/cmd/catalog-api/main.go (the InstrumentGORM@68 + SetGlobalSink@133 block — the wiring anchor; the other 6 services mirror it)
    - services/{player,themes,auth,notifications,scheduler,library}/cmd/*/main.go (each has InstrumentGORM — wire after it)
    - services/notifications/internal/config/config.go (config.go:18 + ~94 — the `Redis cache.Config` field + its env block; the template themes/library copy)
    - services/{themes,library}/internal/config/config.go (NO Redis field today — add one mirroring notifications)
    - .planning/phases/03-db-cache-effects-auto-operation-discovery/03-03-SUMMARY.md + 03-05-SUMMARY.md (exact RegisterEffectCallbacks / NewReadGate / ThresholdRefresher signatures + the Redis handle the refresher expects)
    - .planning/phases/03-db-cache-effects-auto-operation-discovery/03-PATTERNS.md (Boot wiring shared pattern + D-16 exclusion)
  </read_first>
  <action>
    If `libs/tracing/client.go` lacks an exported getter, add `func GlobalSink() EffectSink` returning the loaded `globalSink` (or nil). In EACH of the 7 GORM services (`catalog, player, themes, auth, notifications, scheduler, library`), after the existing `gormtrace.InstrumentGORM(db.DB)` AND after the Producer is started + `tracing.SetGlobalSink(...)` is called: construct a `gate := gormtrace.NewReadGate(50)` (50ms static cold-start default — A1), call `gormtrace.RegisterEffectCallbacks(db.DB, tracing.GlobalSink(), gate)` (the sink is the already-started Producer), and start a `gormtrace.ThresholdRefresher` (built with the service's Redis client + the gate) with `Start(ctx)` + `defer refresher.Stop()`. For services that don't yet create a Producer/SetGlobalSink in main (only catalog/scraper/streaming did so in Phase 2 per 02-04-SUMMARY), ADD the Phase-2 Producer boot block (NewProducer with the ANALYTICS_INTERNAL_URL, Start, defer Stop, SetGlobalSink) mirroring catalog lines 130-133 — the DB hook needs a live sink.

    REDIS CLIENT FOR THE REFRESHER (warning fix — the refresher needs a Redis handle to read the `read_thresholds` hash; not all 7 services have one today):
    - `catalog, player, auth, scheduler` already construct a Redis client / have `cfg.Redis` — REUSE the existing client for the refresher.
    - `notifications` already has `cfg.Redis` (`config.go:18 Redis cache.Config`) but does NOT build a client in main.go — construct one (`cache.New(cfg.Redis)` or `redis` client per the refresher's expected handle from 03-05-SUMMARY) and pass it to the refresher.
    - `themes` and `library` have NEITHER a `cfg.Redis` field NOR a client — FIRST add a `Redis cache.Config` field to their `internal/config/config.go` (mirror notifications/catalog: the field at struct top + the `Redis: cache.Config{ Host: getEnv("REDIS_HOST","redis"), Port: getEnvInt("REDIS_PORT",6379), Password: getEnv("REDIS_PASSWORD",""), DB: getEnvInt("REDIS_DB",0) }` block in the loader), THEN construct the client in main.go and pass it to the refresher.
    The shared `REDIS_HOST`/`REDIS_PORT`/`JWT_SECRET` trio is already provided to every service via docker-compose (per CLAUDE.md), so no compose change is needed — only the Go config field + client construction.

    CRITICAL (D-16): do NOT touch `services/analytics` — it must never register effect callbacks. Keep edits minimal and identical in shape across services.
  </action>
  <verify>
    <automated>for s in catalog player themes auth notifications scheduler library; do (cd services/$s && go build ./... ) || exit 1; done; echo ALL_GORM_SERVICES_BUILD; ! grep -rn "RegisterEffectCallbacks" services/analytics/ </automated>
  </verify>
  <acceptance_criteria>
    - All 7 GORM services compile: the loop prints `ALL_GORM_SERVICES_BUILD`.
    - Source assertion: `grep -rl "RegisterEffectCallbacks" services/*/cmd/*/main.go | wc -l` returns 7 (all GORM services wired).
    - Source assertion (D-16): `grep -rn "RegisterEffectCallbacks" services/analytics/` returns NOTHING (the `!` guard passes).
    - Source assertion: each wired service also starts a ThresholdRefresher (`grep -rl "ThresholdRefresher" services/*/cmd/*/main.go | wc -l` returns 7).
    - Source assertion (Redis-client fix): themes + library now have a `Redis cache.Config` field — `grep -l "Redis .*cache.Config" services/themes/internal/config/config.go services/library/internal/config/config.go | wc -l` returns 2; notifications/themes/library each construct a Redis client in main.go before wiring the refresher (reviewer-verifiable: the refresher is built with a non-nil client in all 7 services).
    - Behavior assertion: `tracing.GlobalSink()` getter exists and returns the Producer set by SetGlobalSink (the DB hook receives a non-nil sink at boot).
  </acceptance_criteria>
  <done>7 GORM services register DB-effect callbacks + ReadGate + ThresholdRefresher after InstrumentGORM/SetGlobalSink, each with a live Redis client (themes/library gain the cfg.Redis field + client; notifications constructs its client); analytics untouched; all build.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the cache aggregator into the cache-effect service (catalog) with flush-before-drain shutdown</name>
  <files>services/catalog/cmd/catalog-api/main.go</files>
  <read_first>
    - services/streaming/cmd/streaming-api/main.go (82-91 — the aggregator Start()/defer Stop() ordering: flush aggregator BEFORE producer drains)
    - services/catalog/cmd/catalog-api/main.go (cache.New@114 + Producer@130 — attach the aggregator to the RedisCache)
    - services/gateway/cmd/gateway-api/main.go (~59-89 — CONFIRM the limiter uses a raw `redis.Client` + redis_rate/v10, NOT libs/cache.RedisCache; this is why gateway is N/A, see action)
    - libs/cache/cache.go (post plan-04 — the optional aggregator field/setter on RedisCache + CacheAggregator.Start/Stop)
    - .planning/phases/03-db-cache-effects-auto-operation-discovery/03-04-SUMMARY.md (exact CacheAggregator constructor + attach API)
  </read_first>
  <action>
    In `services/catalog/cmd/catalog-api/main.go` (the heaviest cache user, and the cache-effect service for this phase): after `cache.New(cfg.Redis)` and after the Producer is started + `SetGlobalSink`, construct a `cache.NewCacheAggregator(tracing.GlobalSink())` (or the constructor name from 03-04-SUMMARY), attach it to the `redisCache` via the optional setter/field, call `agg.Start()`, and `defer agg.Stop()` ORDERED so the aggregator flushes BEFORE `effectProducer.Stop()` drains (mirror streaming's shutdown ordering: register the aggregator Stop to run first / before the producer defer).

    GATEWAY IS N/A (D-17 planner-discretion fix): do NOT wire the aggregator into `services/gateway`. The plan-04 cache aggregator hooks `libs/cache.RedisCache.Get/Set` exclusively, but gateway has no `libs/cache.RedisCache` — its only Redis use is the per-user rate limiter, built on a raw `redis.Client` + `redis_rate/v10` (gateway main.go ~59-68, with a comment explicitly rejecting `libs/cache`). The aggregator has no seam to hook there; wiring it would mean rebuilding the limiter on `libs/cache`, which is out of phase scope and would alter the GCRA limiter's behavior. D-17 is explicit planner discretion ("only if the signal is useful"), and AR-EFFECT-02 is fully satisfied by catalog — so gateway's cache effects are intentionally NOT recorded this phase. ALSO SKIP `services/rooms` and `services/watch-together` (D-17 — Redis-as-datastore, low signal). Keep the nil-guard discipline: a service without an aggregator attached is unaffected. Do NOT attach the aggregator in analytics (D-16).
  </action>
  <verify>
    <automated>cd services/catalog && go build ./... && echo CACHE_WIRED_BUILDS</automated>
  </verify>
  <acceptance_criteria>
    - catalog compiles: prints `CACHE_WIRED_BUILDS`.
    - Source assertion: `grep -c "CacheAggregator\|NewCacheAggregator" services/catalog/cmd/catalog-api/main.go` returns >=1.
    - Source assertion: the aggregator `Stop()` is ordered to run BEFORE the producer `Stop()` at shutdown (reviewer-verifiable from defer ordering / explicit Stop sequence).
    - Source assertion (D-17 N/A): no aggregator wiring added to gateway, rooms, or watch-together (`grep -rn "CacheAggregator" services/gateway services/rooms services/watch-together` returns nothing) — gateway's rate-limit cache bypasses libs/cache.RedisCache, the aggregator's only seam.
  </acceptance_criteria>
  <done>catalog instantiates, attaches, and gracefully stops the cache aggregator (flush before producer drain); gateway is N/A (rate-limit cache bypasses libs/cache.RedisCache); rooms/watch-together skipped per D-17; analytics untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live phase-gate verification — real effect rows + span-metrics + service graph</name>
  <files>(no code change — live verification against the running stack)</files>
  <action>
    Redeploy the affected services + observability config, then verify the four phase success criteria end-to-end against the live stack (see how-to-verify). This is a verification-only checkpoint: no code is written; it confirms the wiring from Tasks 1-2 + plans 01-05 produces real db_write/db_read/cache rows with fine operations in ClickHouse and per-operation RED metrics + a service graph in Prometheus/Grafana. Pause for human confirmation before marking the phase complete.
  </action>
  <what-built>
    DB-effect callbacks, cache aggregator (catalog), the unified stack-frame operation resolver, the daily-P95 read gate, and the Tempo span-metrics generator are now wired across the 7 GORM services + catalog cache. After redeploying the affected services + Tempo/Prometheus/Grafana config, the full activity-register DB/cache half should be live. (Gateway cache effects are intentionally out of scope — its rate-limit cache uses a raw redis.Client, not libs/cache.RedisCache.)
  </what-built>
  <how-to-verify>
    1. Redeploy the affected services + observability config: `make redeploy-catalog player themes auth notifications scheduler library` (or per-service), then `make restart-tempo prometheus grafana` (config-only reload) and `make health`.
    2. AR-EFFECT-01 (DB write rows): trigger a known write (e.g. resolve/upsert an anime via a search or the watch page so `catalog.UpdateAnimeInfo`-class code runs). Query ClickHouse: `SELECT operation, target, effect_kind, row_count FROM events WHERE effect_kind='db_write' ORDER BY timestamp DESC LIMIT 10` — expect rows with a real `table`, `op`, and non-zero `row_count`. Then exercise a high-volume trivial read and confirm it produces NO `db_read` row (sparse cost ledger): `SELECT count() FROM events WHERE effect_kind='db_read' AND target='<that table>' AND timestamp > now()-INTERVAL 5 MINUTE` stays ~0 for trivial fast reads.
    3. AR-EFFECT-02 (cache rows): hit a cached catalog endpoint twice (first call miss, second hit). After ~10-15s (flush), query `SELECT operation, target AS key_class, effect_kind, requests FROM events WHERE effect_kind='cache' ORDER BY timestamp DESC LIMIT 20` — expect summed rows for the key_class with both a miss-classified and a hit-classified entry.
    4. AR-EFFECT-03 (auto operation): confirm an effect row carries a real `catalog.*` (or `<svc>.*`) operation with no code naming it — visible in the same db_write/cache queries above (the `operation` column should read like `catalog.UpdateAnimeInfo`, not a coarse `catalog GET /api/...`).
    5. AR-EFFECT-04 (span-metrics + service graph): query Prometheus `curl 'http://localhost:9090/prometheus/api/v1/query?query=traces_spanmetrics_calls_total'` — expect a non-empty result with per-operation series. Open Grafana → the Tempo service-graph / node-graph panel and confirm a service graph renders.
    6. Confirm `tracing_effects_dropped_total` in `/metrics` is not climbing pathologically (drop-on-full healthy, not flooding).
  </how-to-verify>
  <resume-signal>Type "approved" once db_write rows (non-zero row_count), miss+hit cache rows, a fine `catalog.*` operation, per-op `traces_spanmetrics_*` in Prometheus, and the Grafana service graph are all confirmed — or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-service boot → global sink/producer | Internal; the producer posts to analytics /internal/effects over the Docker network only |
| services/analytics | MUST remain unwired (D-16) — the sink, never a recorder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | analytics accidentally wired into effect recording (self-referential amplification) | mitigate | Task 1/2 verify steps assert `grep RegisterEffectCallbacks services/analytics/` is empty (D-16); checkpoint watches effects_dropped for runaway amplification |
| T-03-19 | Denial of Service | aggregator not flushed before producer drain → lost cache rows OR producer blocked at shutdown | mitigate | Shutdown ordering flushes the aggregator BEFORE the producer drains (cloned from streaming main); verified by defer ordering |
| T-03-20 | Information Disclosure | a service newly given a Producer leaks user_id onto outbound wire baggage | mitigate | The Phase-2 stripWireBaggagePII guard + the plan-01 no-user_id-in-baggage path apply to every Producer-bearing service; no new baggage member is seeded |
| T-03-21 | Denial of Service | span-metrics high-cardinality operation labels overload Prometheus after the broad rollout | accept | Operation is the bounded service-frame label; the checkpoint watches Prometheus health; cardinality tuning is a Phase-5 dashboard concern |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; wiring-only across existing modules (RESEARCH Package Legitimacy Audit: zero new) |
</threat_model>

<verification>
- All 7 GORM services + catalog build; analytics has zero effect-callback wiring (D-16); gateway gets NO cache aggregator (rate-limit cache bypasses libs/cache.RedisCache — D-17 N/A).
- themes + library gained a `Redis cache.Config` field; notifications/themes/library construct a Redis client for the refresher.
- Live checkpoint confirms the 4 success criteria end-to-end (ClickHouse rows + Prometheus span-metrics + Grafana service graph).
- effects_dropped healthy; aggregator flush-before-drain ordering correct.
</verification>

<success_criteria>
- AR-EFFECT-01: db_write rows with non-zero row_count live; trivial reads produce none.
- AR-EFFECT-02: miss-then-hit cache rows live (from catalog), classified by key-class.
- AR-EFFECT-03: a real `catalog.*` operation appears with no naming code.
- AR-EFFECT-04: per-operation RED metrics in Prometheus + a service graph in Grafana.
</success_criteria>

<output>
Create `.planning/phases/03-db-cache-effects-auto-operation-discovery/03-06-SUMMARY.md` when done.
</output>
</output>
