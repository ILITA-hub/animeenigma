---
phase: 05-reports-dashboards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/grafana/dashboards/activity-register-overview.json
  - docker/grafana/provisioning/alerting/rules.yml
autonomous: true
requirements: [AR-REPORT-03, AR-REPORT-04]
must_haves:
  truths:
    - "The overview dashboard surfaces, in one view, current top operations + top external dependencies + active anomalies (AR-REPORT-04)"
    - "A query-driven, explainable avg+Nσ baseline panel flags an operation/provider whose recent volume is far above baseline — no ML (AR-REPORT-03)"
    - "The σ multiplier and window are dashboard custom vars ($sigma default 3, $window default 24) so the admin tunes noise without editing SQL"
    - "The top-external-deps region filters effect_kind = 'egress' AND source = 'be' (authoritative bytes only, AR-FE-03)"
    - "A provisioned unified-alert rule on datasource uid aenigma-clickhouse fires when the anomaly count > 0, mirroring the existing rules.yml 3-refId A=query/B=reduce/C=threshold shape"
    - "All dropdowns self-discover via SELECT DISTINCT; no hardcoded effect_kind/target enum in panel SQL"
  artifacts:
    - path: "infra/grafana/dashboards/activity-register-overview.json"
      provides: "AR-REPORT-04 awareness overview (top ops + top deps + active anomalies) + the AR-REPORT-03 anomaly panel with $sigma/$window tunables"
      contains: "is_anomaly"
    - path: "docker/grafana/provisioning/alerting/rules.yml"
      provides: "AR-REPORT-03 provisioned volume-anomaly alert on aenigma-clickhouse (uid activity-register-volume-anomaly)"
      contains: "activity-register-volume-anomaly"
  key_links:
    - from: "activity-register-overview.json anomaly panel"
      to: "ClickHouse windowed baseline (avg + $sigma * stddevPop)"
      via: "rawSql WITH hourly/stats CTE computing is_anomaly"
      pattern: "is_anomaly"
    - from: "rules.yml activity-register-volume-anomaly refId A"
      to: "datasourceUid aenigma-clickhouse"
      via: "rawSql counting is_anomaly rows -> reduce -> threshold gt 0"
      pattern: "datasourceUid: aenigma-clickhouse"
---

<objective>
Author the awareness OVERVIEW dashboard (AR-REPORT-04 — current top operations + top external dependencies + active anomalies in one view) carrying the explainable query-driven volume-ANOMALY panel (AR-REPORT-03), and append the matching provisioned alert rule to `docker/grafana/provisioning/alerting/rules.yml` pointed at the ClickHouse datasource. Pure declarative Grafana JSON + YAML over `aenigma-clickhouse`.

Purpose: AR-REPORT-04 (the at-a-glance "what is the platform doing now/today" view) + AR-REPORT-03 (anomalies visibly surfaced as a panel AND a firing alert). The anomaly panel lives in the overview so the awareness view shows active anomalies directly; the alert rule additionally pages.

Output: `infra/grafana/dashboards/activity-register-overview.json` (valid JSON) + a new rule appended under the existing alerting group in `rules.yml`.

Effort: `UXΔ = +2 (Better)` (this is where awareness becomes usable — the admin sees top ops + deps + anomalies at a glance and gets paged on a spike) · `CDI = 0.05 * 13` (one multi-region dashboard + one alert rule appended to an existing provisioned file; the effort is the explainable baseline SQL + mirroring the 3-refId rule shape onto the CH datasource) · `MVQ = Dragon 88%/86%` (the high-impact awareness centerpiece; slop-resistant because the anomaly is explainable SQL — last vs threshold — not ML black-box panel-spam).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reports-dashboards/05-RESEARCH.md
@.planning/phases/05-reports-dashboards/05-VALIDATION.md

<interfaces>
<!-- Schema, datasource, analog panel, analog alert-rule shape. Executor uses directly — no exploration. -->

Datasource: { "type": "grafana-clickhouse-datasource", "uid": "aenigma-clickhouse" }, db analytics, table events.
Measure note: for egress request volume use sum(requests) (one row may carry requests>1 for HLS aggregation).
Byte measures MUST filter source = 'be'. Measure col is row_count (NOT `rows`).

Analog dashboard panel/target shape: infra/grafana/dashboards/product-analytics.json:50-62
  (datasource uid, rawQuery:true, format "table"/0, queryType "sql", refId "A", rawSql).

Analog alert-rule shape: docker/grafana/provisioning/alerting/rules.yml — every rule is a 3-refId block:
  refId A = the query (datasourceUid = the data source, model.{rawSql|expr, format, queryType, refId, instant}),
  refId B = { datasourceUid: __expr__, model: { type: reduce, expression: A, reducer: last, refId: B } },
  refId C = { datasourceUid: __expr__, model: { type: threshold, expression: B, conditions:[{evaluator:{type:gt,params:[N]}}], refId: C } },
  plus condition: C, for, labels, annotations. Existing rules sit under group "AnimeEnigma Alerts" (folder Alerts).
  NB: the Prometheus rules use datasourceUid PBFA97CFB590B2093 — the anomaly rule MUST use aenigma-clickhouse instead.

Explainable anomaly query (Pattern 3) — per-operation hourly counts, last $window h, flag latest hour vs trailing avg+$sigma*stddevPop:
  WITH hourly AS (SELECT operation, toStartOfHour(timestamp) AS h, sum(requests) AS req FROM events
    WHERE timestamp >= now() - INTERVAL $window HOUR AND effect_kind='egress' AND source='be' GROUP BY operation, h),
  stats AS (SELECT operation, avgIf(req, h < toStartOfHour(now())) AS baseline_avg,
    stddevPopIf(req, h < toStartOfHour(now())) AS baseline_sd,
    anyIf(req, h = toStartOfHour(now() - INTERVAL 1 HOUR)) AS last_req FROM hourly GROUP BY operation)
  SELECT operation, last_req, round(baseline_avg,1) AS baseline_avg,
    round(baseline_avg + $sigma * baseline_sd, 1) AS threshold,
    last_req > baseline_avg + $sigma * baseline_sd AS is_anomaly
  FROM stats WHERE baseline_avg > 0 ORDER BY (last_req - baseline_avg)/nullIf(baseline_sd,0) DESC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the awareness overview dashboard with the anomaly panel (AR-REPORT-04 + AR-REPORT-03 panel)</name>
  <files>infra/grafana/dashboards/activity-register-overview.json</files>
  <read_first>
    - infra/grafana/dashboards/product-analytics.json (panel/target JSON shape; stat + table panels; format 0 vs "table")
    - .planning/phases/05-reports-dashboards/05-RESEARCH.md Pattern 3 (anomaly baseline) + Pattern 4 (awareness overview) + the Code Examples "Top external dependencies" SQL
    - infra/grafana/dashboards/activity-register-overview.json (the file being written, on re-runs)
  </read_first>
  <action>
    Create the dashboard with uid `activity-register-overview`, title "Activity Register — Awareness Overview", tags ["activity-register","awareness"], schemaVersion 39, time { from: "now-24h", to: "now" }, refresh "30s". Add two custom tuning vars bound to nothing (plain custom): `sigma` (custom, value "3") and `window` (custom, value "24") so the admin tunes the anomaly sensitivity without editing SQL.
    Three regions of panels:
    (1) "Top operations (now/today)" — table panel: SELECT operation, sum(requests) AS requests, round(avg(duration_ms),1) AS avg_ms FROM events WHERE $__timeFilter(timestamp) AND operation != '' GROUP BY operation ORDER BY requests DESC LIMIT 10.
    (2) "Top external dependencies" — table panel: SELECT target AS dependency, sum(requests) AS requests, sum(bytes_out + bytes_in) AS bytes FROM events WHERE $__timeFilter(timestamp) AND effect_kind = 'egress' AND source = 'be' GROUP BY target ORDER BY bytes DESC LIMIT 10. Set the `bytes` field unit to "bytes".
    (3) "Active volume anomalies" — table panel using the Pattern-3 anomaly query from <interfaces>, interpolating `$window` into the INTERVAL and `$sigma` into the threshold. Add a fieldConfig override coloring the `is_anomaly` field (threshold/value-mapping: 1 = red/anomaly, 0 = green). Optionally add a stat panel "Active anomaly count" reading `SELECT count() AS anomalies FROM ( <the Pattern-3 query> ) WHERE is_anomaly` for the at-a-glance number.
    All panels bind datasource uid aenigma-clickhouse. Do NOT hardcode effect_kind/target enums. Do NOT reuse an existing uid. Lay panels out with gridPos so all three regions are visible in one view.
  </action>
  <acceptance_criteria>
    - `jq empty infra/grafana/dashboards/activity-register-overview.json` exits 0.
    - `jq -r '.uid' infra/grafana/dashboards/activity-register-overview.json` == "activity-register-overview".
    - At least 3 table panels exist: `jq '[.panels[] | select(.type=="table")] | length' ...` >= 3 (top ops + top deps + anomalies).
    - The top-deps region rawSql contains `effect_kind = 'egress'` AND `source = 'be'` (AR-FE-03).
    - The anomaly panel rawSql contains `is_anomaly` and interpolates both `$window` and `$sigma`: `grep -q 'is_anomaly' ...` && `grep -q 'INTERVAL $window HOUR' ...` && `grep -q '$sigma' ...`.
    - `jq -e '.templating.list[] | select(.name=="sigma")' ...` and `... select(.name=="window")` are non-empty (tunables present — anomaly-noise mitigation).
    - `grep -q '"uid": "aenigma-clickhouse"' ...`.
  </acceptance_criteria>
  <verify>
    <automated>jq empty infra/grafana/dashboards/activity-register-overview.json && [ "$(jq '[.panels[] | select(.type=="table")] | length' infra/grafana/dashboards/activity-register-overview.json)" -ge 3 ] && grep -q "effect_kind = 'egress'" infra/grafana/dashboards/activity-register-overview.json && grep -q "source = 'be'" infra/grafana/dashboards/activity-register-overview.json && grep -q 'is_anomaly' infra/grafana/dashboards/activity-register-overview.json && grep -q 'INTERVAL $window HOUR' infra/grafana/dashboards/activity-register-overview.json && grep -q '\$sigma' infra/grafana/dashboards/activity-register-overview.json</automated>
  </verify>
  <done>Valid overview dashboard JSON with >=3 table panels (top ops, top deps filtered egress+be, active anomalies), $sigma/$window tunable custom vars, an is_anomaly explainable baseline panel, all bound to aenigma-clickhouse.</done>
</task>

<task type="auto">
  <name>Task 2: Append the provisioned volume-anomaly alert rule (AR-REPORT-03)</name>
  <files>docker/grafana/provisioning/alerting/rules.yml</files>
  <read_first>
    - docker/grafana/provisioning/alerting/rules.yml (the exact 3-refId rule shape to mirror — e.g. the `high-error-rate` rule, lines ~63-109 — and where the "AnimeEnigma Alerts" group's rules: list ends; the file being edited)
    - .planning/phases/05-reports-dashboards/05-RESEARCH.md Pattern 3 "Provisioned alert rule" block (the CH-datasource rule template)
  </read_first>
  <action>
    Append a new rule to the existing `rules:` list under the "AnimeEnigma Alerts" group (folder Alerts), mirroring the existing 3-refId A/B/C shape but pointing refId A at the ClickHouse datasource:
    - uid `activity-register-volume-anomaly`, title "Activity Register Volume Anomaly", condition C, noDataState OK, execErrState Error.
    - refId A: relativeTimeRange { from: 86400, to: 0 }, datasourceUid `aenigma-clickhouse` (NOT the Prometheus uid PBFA97CFB590B2093), model with rawSql = `SELECT count() AS anomalies FROM ( <the Pattern-3 anomaly query with a fixed 3σ / 24h since alert rules have no dashboard vars> ) WHERE is_anomaly`, format table, queryType sql, refId A. (Alert rules cannot read dashboard template vars — hardcode 3 and 24h here; the dashboard panel keeps the tunable $sigma/$window.)
    - refId B: datasourceUid __expr__, model { type: reduce, expression: A, reducer: last, refId: B }.
    - refId C: datasourceUid __expr__, model { type: threshold, expression: B, conditions: [{ evaluator: { type: gt, params: [0] } }], refId: C }.
    - for: 5m, labels { severity: warning, component: activity-register }, annotations { summary: "Volume anomaly: an operation is far above baseline", description: "An operation's last-hour request volume exceeded its 24h trailing avg+3σ baseline. Open the Activity Register — Awareness Overview dashboard." }.
    Keep YAML indentation consistent with the surrounding rules (2-space under `rules:` list items). Do NOT touch the existing rules or the second "Scraper Self-Healing" group.
  </action>
  <acceptance_criteria>
    - `python3 -c "import yaml,sys; yaml.safe_load(open('docker/grafana/provisioning/alerting/rules.yml'))"` exits 0 (valid YAML after edit).
    - `grep -c 'uid: activity-register-volume-anomaly' docker/grafana/provisioning/alerting/rules.yml` == 1 (rule added exactly once).
    - The rule references the ClickHouse datasource, not Prometheus: the anomaly rule block contains `datasourceUid: aenigma-clickhouse` and does NOT introduce a new `PBFA97CFB590B2093` reference for refId A.
    - The rule's rawSql contains `is_anomaly` (it counts anomaly rows) and the byte/volume baseline carries `effect_kind = 'egress'` AND `source = 'be'`.
    - Existing rule count is preserved + 1: `grep -c '^      - uid:' docker/grafana/provisioning/alerting/rules.yml` increases by exactly 1 (14 existing -> 15). Do NOT hardcode the absolute number — assert the delta; existing rules must be untouched.
    - The alert rawSql preserves the byte discipline: `grep -q "source = 'be'"` AND `grep -q "effect_kind = 'egress'"` over rules.yml hold (the anomaly baseline never sums approximate fe_rum rows).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('docker/grafana/provisioning/alerting/rules.yml'))" && [ "$(grep -c 'uid: activity-register-volume-anomaly' docker/grafana/provisioning/alerting/rules.yml)" -eq 1 ] && grep -q 'datasourceUid: aenigma-clickhouse' docker/grafana/provisioning/alerting/rules.yml && grep -q 'is_anomaly' docker/grafana/provisioning/alerting/rules.yml && grep -q "source = 'be'" docker/grafana/provisioning/alerting/rules.yml && grep -q "effect_kind = 'egress'" docker/grafana/provisioning/alerting/rules.yml</automated>
  </verify>
  <done>rules.yml parses as YAML and contains exactly one new `activity-register-volume-anomaly` rule on datasourceUid aenigma-clickhouse with the A=query(is_anomaly count)/B=reduce/C=threshold-gt-0 shape; existing rules untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Grafana | Admin lands as Org Admin via gateway auth-proxy (already solved). Tuning vars $sigma/$window are admin-selected and flow into the anomaly rawSql. |
| Grafana → ClickHouse | overview panels + the alert rule query aenigma-clickhouse read-only. |
| Grafana alerting → notifier | the new rule fires a notification on anomaly count > 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Integrity | top-external-deps byte measure | mitigate | Region 2 filters effect_kind='egress' AND source='be' (asserted in Task 1 acceptance) — fe_rum approximate bytes never enter the dependency totals (AR-FE-03). |
| T-05-05 | Denial of Service | anomaly alert false-positive storm (noisy σ) | mitigate | σ multiplier + window are dashboard custom vars ($sigma default 3, $window default 24) so the admin tunes without SQL edits; the rule uses `for: 5m` so a single transient bucket won't page; baseline_avg > 0 guard drops empty operations. |
| T-05-06 | Spoofing / Misrouting | anomaly alert pointed at the wrong datasource | mitigate | refId A explicitly uses datasourceUid aenigma-clickhouse (NOT PBFA97CFB590B2093); acceptance asserts it — reusing/renaming uids historically orphaned 14 rules. |
| T-05-07 | Tampering (SQL injection) | $sigma/$window interpolation into anomaly rawSql | accept | Both are numeric custom vars on a fixed default; the dashboard does not expose a free-text entry for them by default. Residual risk low (admin-only surface, numeric tunables). |
</threat_model>

<verification>
- `jq empty` passes on activity-register-overview.json; it has >=3 table panels, $sigma/$window vars, an is_anomaly panel, top-deps filtered egress+be.
- rules.yml parses as YAML; exactly one new `activity-register-volume-anomaly` rule on aenigma-clickhouse with the A/B/C shape; existing rules untouched.
- (Live: synthetic-spike flagging + alert transition is the plan 05-03 non-autonomous gate.)
</verification>

<success_criteria>
`activity-register-overview.json` exists and parses, surfaces top operations + top external dependencies (egress+be) + active anomalies in one view with tunable $sigma/$window, and `rules.yml` carries a valid provisioned volume-anomaly alert on the ClickHouse datasource mirroring the existing 3-refId shape.
</success_criteria>

<output>
Create `.planning/phases/05-reports-dashboards/05-02-SUMMARY.md` when done.
</output>
