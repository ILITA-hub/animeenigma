---
phase: 01-clickhouse-foundation-eventstore-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.yml
  - docker/clickhouse/config.d/prometheus.xml
  - docker/prometheus/prometheus.yml
  - docker/clickhouse/BACKUP-RESTORE.md
  - docker/.env.example
autonomous: false
requirements: [AR-STORE-01]
user_setup: []

must_haves:
  truths:
    - "docker compose ps shows a healthy ClickHouse container (8123/ping passes)"
    - "Prometheus scrapes ClickHouse self-metrics on :9363"
    - "A backup is created and a restore is dry-run successfully at least once"
    - "ClickHouse ports are bound to 127.0.0.1 only"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "clickhouse + clickhouse-backup services (pinned image, named volume, healthcheck)"
      contains: "clickhouse/clickhouse-server"
    - path: "docker/clickhouse/config.d/prometheus.xml"
      provides: "native <prometheus> endpoint on :9363"
      contains: "<prometheus>"
    - path: "docker/prometheus/prometheus.yml"
      provides: "clickhouse scrape job"
      contains: "job_name: 'clickhouse'"
    - path: "docker/clickhouse/BACKUP-RESTORE.md"
      provides: "documented backup/restore runbook with the dry-run result recorded"
      min_lines: 25
  key_links:
    - from: "docker/docker-compose.yml clickhouse service"
      to: "docker/clickhouse/config.d/prometheus.xml"
      via: "config.d bind mount"
      pattern: "config.d"
    - from: "docker/prometheus/prometheus.yml"
      to: "clickhouse:9363"
      via: "static scrape target"
      pattern: "clickhouse:9363"
---

<objective>
Stand up ClickHouse as a single-host docker-compose service with native Prometheus self-metrics, a backup/restore sidecar, and a documented + dry-run-verified backup procedure. This is the infrastructure half of Phase 1; it shares NO files with the Go store impl (plan 01-02) so the two run in parallel.

Purpose: Deliver AR-STORE-01 — a healthy, scraped, backup-able ClickHouse instance the Go store (01-02) and the migration (01-03) build on.
Output: A running `clickhouse` container, a `clickhouse-backup` sidecar, a Prometheus scrape job, and a backup/restore runbook with a recorded dry-run.
</objective>

**Metrics:** `UXΔ = 0 (Neutral)` (pure infra; no user-facing change) · `CDI = 0.08 * 13` (new stateful service + backup sidecar across docker/ + prometheus.yml; mirrors the tempo/postgres compose shape) · `MVQ = Dragon 82%/85%` (sturdy foundational infrastructure the rest of v4 stands on)

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-clickhouse-foundation-eventstore-swap/CONTEXT.md
@.planning/phases/01-clickhouse-foundation-eventstore-swap/RESEARCH.md
@.planning/phases/01-clickhouse-foundation-eventstore-swap/01-PATTERNS.md

<interfaces>
<!-- Exact compose analogs to mirror (do NOT explore the codebase). -->

docker/docker-compose.yml — postgres block (lines 6-22): pinned image `postgres:16-alpine`, `container_name: animeenigma-<svc>`, `restart: unless-stopped`, `ports: ["127.0.0.1:HOST:CONTAINER"]`, named volume, healthcheck.

docker/docker-compose.yml — tempo block (lines 274-291): 3rd-party data store mounting a `:ro` config file + named data volume + wget-spider healthcheck. Best structural analog for ClickHouse.
  healthcheck: ["CMD","wget","--quiet","--tries=1","--spider","http://localhost:PORT/ready"], interval 15s / timeout 5s / retries 5.

Top-level `volumes:` list ends ~line 868 (postgres_data, redis_data, …, tempo_data). Add `clickhouse_data:` and `clickhouse_backups:` here.

Shared network: implicit `default` → `animeenigma-network` (lines ~879-881). No explicit network block needed.

Port collisions to avoid: MinIO already binds `9000`. Bind CH native as `127.0.0.1:9100:9000` (Tempo set the remap-on-collision precedent with `3201:3200`). CH HTTP `8123` is free → `127.0.0.1:8123:8123`. CH prometheus `9363` is free → `127.0.0.1:9363:9363`.

docker/prometheus/prometheus.yml — analytics job is the exact scrape shape:
  - job_name: 'analytics'
    static_configs:
      - targets: ['analytics:8092']
    metrics_path: /metrics
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm + pin the current ClickHouse LTS image tag</name>
  <what-built>
    Nothing is built yet — this gate exists because RESEARCH.md flagged the ClickHouse
    CalVer image tag as the #1 riskiest unknown (the WebSearch index reported a likely
    future-dated `26.x`; the assumed stable LTS line is `25.3`). We must pin an EXACT
    patch tag, never `latest`/`lts`, so redeploys are reproducible.

    Claude has run, before pausing:
      docker run --rm clickhouse/clickhouse-server:lts clickhouse-server --version
    and checked https://github.com/ClickHouse/ClickHouse/releases for the current LTS line,
    and reports the exact tag it intends to pin (e.g. `clickhouse/clickhouse-server:25.3.x.y`)
    plus the matching `altinity/clickhouse-backup` tag for that CH major.
  </what-built>
  <how-to-verify>
    1. Read the version Claude reported from `clickhouse-server --version` and the
       GitHub releases LTS line.
    2. Confirm the exact patch tag to pin (must be a concrete `YY.M.x.y`, NOT `latest` or bare `lts`).
    3. Confirm the `altinity/clickhouse-backup` tag (pin to one matching the CH major, NOT `latest`).
  </how-to-verify>
  <resume-signal>Reply with the exact tags to pin, e.g. "pin clickhouse-server:25.3.x.y and clickhouse-backup:2.x" — or "approved" to accept Claude's reported tags.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add clickhouse + clickhouse-backup services, config.d, and Prometheus scrape</name>
  <files>docker/docker-compose.yml, docker/clickhouse/config.d/prometheus.xml, docker/prometheus/prometheus.yml, docker/.env.example</files>
  <action>
    Mirror the postgres (lines 6-22) and tempo (lines 274-291) compose blocks to add two services. Use the EXACT image tags confirmed in Task 1 — pin concrete patch tags, never `latest`/`lts` (anti-pattern per RESEARCH §Anti-Patterns).

    1. Add `clickhouse:` service: confirmed pinned image; `container_name: animeenigma-clickhouse`; `restart: unless-stopped`; `ulimits: { nofile: { soft: 262144, hard: 262144 } }` (CH requirement). Environment: `CLICKHOUSE_DB: analytics`, `CLICKHOUSE_USER: ${CLICKHOUSE_USER:-analytics}`, `CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-changeme}`, `CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"`. Ports bound to 127.0.0.1 ONLY (V4 access control, RESEARCH §Security): `127.0.0.1:8123:8123` (http), `127.0.0.1:9100:9000` (native — remapped because MinIO owns host 9000), `127.0.0.1:9363:9363` (prometheus). Volumes: `clickhouse_data:/var/lib/clickhouse` and `./clickhouse/config.d:/etc/clickhouse-server/config.d:ro`. Healthcheck mirroring tempo's wget-spider: `["CMD","wget","--quiet","--tries=1","--spider","http://localhost:8123/ping"]`, interval 15s / timeout 5s / retries 5. Do NOT add a `deploy.resources` limits block — no stateful service in this repo uses one (PATTERNS §4); CH tuning belongs in config.d if ever needed.
    2. Add `clickhouse-backup:` sidecar: confirmed `altinity/clickhouse-backup` tag; `container_name: animeenigma-clickhouse-backup`; `restart: unless-stopped`; `entrypoint: ["/bin/sh","-c","sleep infinity"]` (driven via `docker exec`); environment `CLICKHOUSE_HOST: clickhouse`, `CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-changeme}`; volumes MUST share the CH data volume: `clickhouse_data:/var/lib/clickhouse` and `clickhouse_backups:/var/lib/clickhouse/backup`; `depends_on: { clickhouse: { condition: service_healthy } }`.
    3. Register `clickhouse_data:` and `clickhouse_backups:` under the top-level `volumes:` list (after `tempo_data:`).
    4. Create `docker/clickhouse/config.d/prometheus.xml` enabling the native `<prometheus>` endpoint on port 9363 with `<endpoint>/metrics</endpoint>`, `<metrics>true</metrics>`, `<events>true</events>`, `<asynchronous_metrics>true</asynchronous_metrics>` (RESEARCH §Compose Prometheus endpoint).
    5. Append a `clickhouse` scrape job to `docker/prometheus/prometheus.yml` mirroring the `analytics` job: `job_name: 'clickhouse'`, `static_configs.targets: ['clickhouse:9363']`, `metrics_path: /metrics`.
    6. Add `CLICKHOUSE_USER` and `CLICKHOUSE_PASSWORD` (with safe placeholder values) to `docker/.env.example` so the prod host sets a non-empty CH password (do NOT leave CH on the empty-password `default` user — RESEARCH §Security).
    Do NOT add `depends_on: clickhouse` to the analytics service in this plan — that wiring lives in 01-03 (the analytics container does not yet know about CH; adding the dependency before the Go code is wired would block analytics boot).
  </action>
  <verify>
    <automated>cd /data/animeenigma && docker compose -f docker/docker-compose.yml config >/dev/null && echo COMPOSE_OK && grep -q "job_name: 'clickhouse'" docker/prometheus/prometheus.yml && grep -q "<prometheus>" docker/clickhouse/config.d/prometheus.xml && grep -Eq "clickhouse/clickhouse-server:[0-9]" docker/docker-compose.yml && echo CONFIG_OK</automated>
  </verify>
  <done>`docker compose config` validates; compose contains pinned `clickhouse` + `clickhouse-backup` services with 127.0.0.1-bound ports and a wget healthcheck; `prometheus.xml` enables :9363; `prometheus.yml` has the clickhouse scrape job; `.env.example` carries CH creds.</done>
</task>

<task type="auto">
  <name>Task 3: Bring ClickHouse up, verify scrape, and dry-run backup/restore (AR-STORE-01)</name>
  <files>docker/clickhouse/BACKUP-RESTORE.md</files>
  <action>
    Deploy and prove the operational surface. This server IS production — `docker compose up` deploys live; bringing up only the new CH + backup containers (and reloading prometheus) is non-disruptive to existing services.

    1. Start the new containers: `docker compose -f docker/docker-compose.yml up -d clickhouse clickhouse-backup` and wait for the CH healthcheck to report healthy (`docker compose ps`).
    2. Confirm the healthcheck + ping: `curl -s http://127.0.0.1:8123/ping` returns `Ok.`.
    3. Confirm self-metrics: `curl -s http://127.0.0.1:9363/metrics | head` returns Prometheus exposition text; reload Prometheus (`docker compose restart prometheus` or hot-reload) and confirm the `clickhouse` target is UP at `http://127.0.0.1:9090/prometheus/targets` (or via `curl` of the Prometheus targets API).
    4. Create a backup: `docker exec animeenigma-clickhouse-backup clickhouse-backup create dryrun-$(date +%F)`; list it: `... clickhouse-backup list`.
    5. DRY-RUN a restore to prove the procedure: `docker exec animeenigma-clickhouse-backup clickhouse-backup restore --schema dryrun-YYYY-MM-DD` (schema-only restore is safe against an empty/fresh CH; capture the command output).
    6. Write `docker/clickhouse/BACKUP-RESTORE.md`: the create/list/restore commands, where backups live (`clickhouse_backups` volume), the restore-to-scratch dry-run recipe, AND a recorded result line ("Dry-run restore verified YYYY-MM-DD: <outcome>"). This recorded dry-run is the AR-STORE-01 acceptance artifact.
  </action>
  <verify>
    <automated>cd /data/animeenigma && curl -s http://127.0.0.1:8123/ping | grep -q "Ok." && curl -s http://127.0.0.1:9363/metrics | grep -q "ClickHouse" && docker exec animeenigma-clickhouse-backup clickhouse-backup list 2>/dev/null | grep -q dryrun && grep -qi "Dry-run restore verified" docker/clickhouse/BACKUP-RESTORE.md && echo AR_STORE_01_OK</automated>
  </verify>
  <done>AR-STORE-01 satisfied: `docker compose ps` shows healthy ClickHouse; `:8123/ping` returns Ok; Prometheus scrapes `:9363`; a backup exists and a `restore --schema` dry-run ran; `BACKUP-RESTORE.md` records the dry-run result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → CH ports | CH HTTP/native/prometheus ports must not be reachable off-host |
| analytics svc → CH | internal Docker-network auth (CH user/password) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | exposed CH ports (8123/9000/9363) | mitigate | bind all three to `127.0.0.1` only (mirrors postgres/redis pattern) |
| T-01-02 | Spoofing | empty-password `default` CH user | mitigate | run as `CLICKHOUSE_USER`/`CLICKHOUSE_PASSWORD`; `.env.example` carries non-empty creds; warn if placeholder |
| T-01-03 | Tampering | non-reproducible image tag | mitigate | pin exact CalVer patch tag via Task-1 human-verify; never `latest`/`lts` |
| T-01-SC | Tampering | container image pulls (clickhouse-server, clickhouse-backup) | mitigate | both images verified Approved in RESEARCH §Package Legitimacy Audit (official ClickHouse + Altinity orgs); pinned tags confirmed at human-verify gate |
</threat_model>

<verification>
- `docker compose -f docker/docker-compose.yml config` validates.
- `docker compose ps` shows `animeenigma-clickhouse` healthy.
- `curl http://127.0.0.1:8123/ping` → `Ok.`; `curl http://127.0.0.1:9363/metrics` → CH metrics.
- Prometheus `clickhouse` target is UP.
- `clickhouse-backup list` shows the dry-run backup; `BACKUP-RESTORE.md` records the restore dry-run.
</verification>

<success_criteria>
AR-STORE-01 is fully met: a healthy, host-bound, scraped ClickHouse with a documented + dry-run-verified backup/restore procedure. No existing service is disturbed (analytics still writes to Postgres; CH dependency wiring is deferred to 01-03).
</success_criteria>

<output>
Create `.planning/phases/01-clickhouse-foundation-eventstore-swap/01-01-SUMMARY.md` when done.
Record the exact pinned image tags (from Task 1) in the summary for 01-03 and future phases.
</output>
