---
phase: 01-instrumentation-baseline
plan: 07
type: execute
wave: 6
depends_on: [03, 05, 06]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - frontend/web/public/changelog.json
autonomous: false
requirements: [M-01, M-02]
threats: []

must_haves:
  truths:
    - "PROJECT.md documents Loki retention as 7 days (168h), correcting the 31d figure in early CONTEXT D-06"
    - "make redeploy-player completes successfully"
    - "make redeploy-gateway completes successfully"
    - "make health reports all services green"
    - "Player /metrics endpoint exposes both combo_override_total AND combo_resolve_total counter families"
    - "A live curl smoke test (POST /api/preferences/override with X-Anon-ID, valid body) returns 204 and increments the metric"
    - "A live curl on /api/preferences/resolve as anon returns 200"
    - "Invalid dimension (POST /api/preferences/override with dimension=quality) returns 400 — T-01-02 enforced live"
    - "Both-missing identity (POST /api/preferences/override with no JWT and no X-Anon-ID) returns 400 — T-01-01 enforced live"
    - "Grafana panel 'Auto-Pick Override Rate (Phase 1 Baseline)' renders at https://admin.animeenigma.ru/grafana/d/preference-resolution-v1 (manual visual confirmation in checkpoint)"
    - "STATE.md contains a 'Phase 1 follow-up' marker reminding that the 24h baseline must be captured to PROJECT.md before Phase 6 starts"
    - "frontend/web/public/changelog.json has a new entry describing Phase 1 instrumentation"
    - "git commit + push completed via animeenigma-after-update skill with the three required co-authors"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Loki 7d retention constraint subsection under Key Decisions"
      contains: "Loki retention"
    - path: ".planning/STATE.md"
      provides: "Phase 1 follow-up marker for the 24h baseline capture"
      contains: "Phase 1 follow-up"
    - path: "frontend/web/public/changelog.json"
      provides: "User-facing changelog entry"
      contains: "override\\|baseline\\|instrumentation"
  key_links:
    - from: "make redeploy-player"
      to: "http://localhost:8083/metrics"
      via: "Prometheus scrape exposes new counter families after restart"
      pattern: "combo_(override|resolve)_total"
    - from: "git commit"
      to: "remote main branch"
      via: "git push"
      pattern: "ahead 0"
---

<objective>
Document the Loki retention constraint in PROJECT.md (BLOCKER #2 from plan-checker), deploy Phase 1 to production, smoke-test live with verified status codes, append the Phase 1 follow-up marker to STATE.md, then run the `animeenigma-after-update` skill which handles lint/build/redeploy/changelog/commit/push.

This is the docs + deploy + verify + ship plan. After this plan: PROJECT.md correctly states Loki retention is 7 days (NOT 31d); the override-rate metric is producing data on production; the Grafana dashboard tile is live; STATE.md carries forward a reminder that the 24h baseline snapshot must be captured to PROJECT.md before Phase 6 opens. Success criterion 4 from ROADMAP ("deployed via `make redeploy-player` and verified live on production") is met. Success criterion 3 ("≥ 24h baseline snapshot recorded in PROJECT.md") is a phase-gate verification done AFTER this plan completes — see VALIDATION.md "Manual-Only Verifications" and the STATE.md follow-up marker we add in Task 4.

This plan has a checkpoint:human-verify task because Grafana visual confirmation of the rendered panel cannot be automated cleanly via CLI. All other verifications are automated.

Output:
- `.planning/PROJECT.md` updated with the Loki 7d retention subsection
- Player + gateway services running new code on production
- `combo_override_total` and `combo_resolve_total` visible at `/metrics`
- 4 live smoke tests pass (anon resolve, anon override, invalid dimension, both missing)
- Grafana panel renders (human-verified)
- `.planning/STATE.md` carries the Phase 1 follow-up marker
- Changelog updated, commit pushed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-instrumentation-baseline/01-CONTEXT.md
@.planning/phases/01-instrumentation-baseline/01-RESEARCH.md
@.planning/phases/01-instrumentation-baseline/01-PATTERNS.md
@.planning/phases/01-instrumentation-baseline/01-VALIDATION.md
@CLAUDE.md
@.planning/phases/01-instrumentation-baseline/01-03-SUMMARY.md
@.planning/phases/01-instrumentation-baseline/01-05-SUMMARY.md
@.planning/phases/01-instrumentation-baseline/01-06-SUMMARY.md

<interfaces>
The `animeenigma-after-update` skill is in `.claude/skills/animeenigma-after-update`. Per CLAUDE.md it:
1. Lints and builds the affected code
2. Redeploys changed services via `make redeploy-<service>`
3. Runs health checks
4. Updates `frontend/web/public/changelog.json` with user-facing changelog entries (informative + enthusiastic tone with emojis)
5. Commits all changes with co-authors and pushes to remote

`frontend/web/public/changelog.json` schema (verified at edit time): top-level is an array of date-grouped objects, each with shape `{ "date": "YYYY-MM-DD", "entries": [ { "type": "fix|feature|...", "message": "..." }, ... ] }`. Entries have a `message` field — there is NO `title` field and NO top-level `entries` field. Verification commands MUST use `jq -r '.[0].entries[].message'` (NOT `.entries[0].title`).

Co-authors per MEMORY.md:
- Claude Opus 4.6 <noreply@anthropic.com>
- 0neymik0 <0neymik0@gmail.com>
- NANDIorg <super.egor.mamonov@yandex.ru>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Document Loki 7d retention constraint in PROJECT.md</name>
  <read_first>
    - .planning/PROJECT.md (full file — locate the `## Key Decisions` section)
    - docker/loki/loki-config.yml (verify line 27-28 still reads `retention_period: 168h  # 7 days`)
    - .planning/phases/01-instrumentation-baseline/01-CONTEXT.md (D-06 — the documentation error this task corrects)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    The plan-checker identified that CONTEXT D-06 mentions Loki retention as ~31 days, but the actual configuration in `docker/loki/loki-config.yml` line 27-28 sets `retention_period: 168h  # 7 days`. Plans 02 and 07 passively claim "Loki retention is 7 days (documented in PROJECT.md per CONTEXT Critical Finding 2)" but no task actually writes this documentation. This task corrects that.

    **A. Verify the Loki config still says 7 days:**
    ```bash
    grep -E "retention_period|168h|7 days" /data/animeenigma/docker/loki/loki-config.yml
    ```
    Expected: at least one match showing `168h  # 7 days`. If the config changed, STOP and ask before continuing — the documentation must match the config, and a config drift means a different decision needs to be recorded.

    **B. Add a "Loki retention constraint" subsection to `.planning/PROJECT.md`** under the existing `## Key Decisions` section, immediately after the Key Decisions table (NOT inside the table — the table is for one-line decision rows; this is a multi-paragraph constraint that needs prose).

    Locate the closing line of the Key Decisions table (the row ending with `Phase C makes the *next* project (recs) cheaper. | — Pending |`) and the next blank line, then the `## Evolution` heading. Insert the new subsection BETWEEN the table and `## Evolution`.

    The exact content to insert:

    ```markdown

    ### Loki retention constraint

    **Loki retention is 168h / 7 days**, NOT the 31d figure mentioned in early Phase 1 CONTEXT D-06. Verified at `docker/loki/loki-config.yml` line 27-28 (`limits_config.retention_period: 168h  # 7 days`). The 31d figure was a documentation error; the actual provisioned retention is 7 days.

    What this means for the project:

    - **Prometheus retention (15d)** covers the `combo_override_total` / `combo_resolve_total` rate metric, which is the Phase 7 baseline-comparison signal. The Phase 1 success criterion 3 (24h baseline) and the Phase 7 before/after override-rate comparison both rely on Prometheus rate counters, NOT on Loki log lines, so the 7d Loki window is sufficient for those use cases.
    - **Loki (7d)** is for retroactive per-event inspection within a 7-day window. Useful for debugging individual override events ("why did this user override this anime"), per-event PII audit (T-01-03 verification), and short-window investigations.
    - **If Phase 6 needs > 7d per-event analysis** (e.g., to validate Tier 2 time-decay using individual override events spanning weeks), that becomes a Phase 5 schema-add (per-event DB table), which is already on the Deferred list in Phase 1 CONTEXT.md and PROJECT.md "Out of Scope" framing. Do NOT modify Loki retention to work around this — the per-event DB table is the correct lever.

    This subsection is intentionally placed in Key Decisions so that any future phase opening a per-event analytics question must read past it before assuming Loki has 31d of history.

    ```

    Note: this revision-mode plan has ALREADY pre-applied the PROJECT.md edit (the planner did it as part of revision setup). The executor's job here is to verify the section exists AND to leave it alone if it does. If for any reason the section is missing (e.g., the file was reverted between planning and execution), the executor must restore it using the exact content above.

    Per BLOCKER #2 from plan-checker (revision context).
  </action>
  <verify>
    <automated>grep -ic "loki retention" /data/animeenigma/.planning/PROJECT.md | tr -d '\n' && echo " loki-retention-mentions" && grep -cE "168h|7 days|7d" /data/animeenigma/.planning/PROJECT.md | tr -d '\n' && echo " seven-day-mentions" && test "$(grep -ic 'loki retention' /data/animeenigma/.planning/PROJECT.md)" -ge 1 && test "$(grep -cE '168h|7 days|7d' /data/animeenigma/.planning/PROJECT.md)" -ge 1 && echo OK</automated>
    <!-- Expected output: grep counts >= 1 each, then "OK" on the last line. -->
  </verify>
  <acceptance_criteria>
    - `grep -ic "loki retention" .planning/PROJECT.md` returns >= 1.
    - `grep -cE "168h|7 days|7d" .planning/PROJECT.md` returns >= 1.
    - The subsection mentions the 31d documentation error correction: `grep -c "31d\|31 days" .planning/PROJECT.md` returns >= 1.
    - The subsection mentions the Phase 5 schema-add escape hatch: `grep -c "Phase 5\|per-event DB table" .planning/PROJECT.md` returns >= 1.
    - The file is well-formed Markdown (no broken table; the subsection lives outside the table): `grep -A 1 "Recommendations engine out of scope" .planning/PROJECT.md | grep -c "| —"` returns 1 (the table row above the new subsection still parses).
  </acceptance_criteria>
  <done>PROJECT.md correctly documents the Loki 7d retention constraint with the rationale and the Phase 5 escape hatch. Plans 02 and 07 (and any future phase) can now reference this as the authoritative source.</done>
</task>

<task type="auto">
  <name>Task 1: Deploy backend + gateway + frontend; live smoke test (4 status-code-asserted curls)</name>
  <read_first>
    - Makefile (existing — verify `redeploy-player` and `redeploy-gateway` targets)
    - docker/docker-compose.yml (verify the service names match)
    - .planning/phases/01-instrumentation-baseline/01-RESEARCH.md (Pitfall 4 — Grafana dashboard reload requires either 10s wait or `restart grafana`; Pitfall 7 — backend before frontend deploy order)
    - services/player/internal/handler/override.go (canonical OverrideRequest body shape — `anime_id`, `load_session_id`, `dimension`, `original_combo`, `new_combo`, `ms_since_load`, `tier`, `tier_number`, `player`)
    - services/player/internal/handler/preference.go (existing ResolveRequest body shape)
  </read_first>
  <files></files>
  <action>
    Execute the deploy sequence in this exact order to honor RESEARCH Pitfall 7 (backend before frontend, so anon resolve doesn't 401 mid-deploy). The 4 smoke tests are now part of the `<verify>` clause below — this prose section covers the redeploy steps and the rationale for each smoke test. The status-code assertions live in `<verify>` so the executor cannot mark the task done with smoke tests failing silently (BLOCKER #4 from plan-checker).

    **Step 1 — Backend redeploys (sequential, NOT parallel — gateway proxies to player):**
    ```bash
    cd /data/animeenigma
    make redeploy-player
    make redeploy-gateway
    ```

    **Step 2 — Frontend redeploy (after backend confirmed):**
    ```bash
    make redeploy-web
    ```
    If `make redeploy-web` doesn't exist (frontend may use different target), check with `make help` and use whichever target rebuilds the nginx-served Vue bundle.

    **Step 3 — Health check.** All services must be reported healthy. If any are red, investigate logs (`make logs-player`, `make logs-gateway`) before proceeding.
    ```bash
    make health
    ```

    **Step 4 — 4 live smoke tests (canonical bodies match `services/player/internal/handler/override.go`):**

    The `<verify><automated>` clause below executes all 4. Their intent:
    - 4a (`combo_(override|resolve)_total` HELP lines): proves both metric families self-registered after restart.
    - 4b (anon resolve `POST /api/preferences/resolve` → 200): proves Critical Finding 3 frontend wiring works — anon callers can resolve.
    - 4c (anon override `POST /api/preferences/override` → 204): proves Critical Finding 1 routing works — gateway forwards `/api/preferences/override` outside the JWT group, player service accepts X-Anon-ID.
    - 4d (invalid dimension `dimension=INVALID` → 400): T-01-02 enforced live (cardinality protection, dimension whitelist).
    - 4e (no JWT, no X-Anon-ID → 401 OR 400): T-01-01 enforced. The expected status depends on where the rejection lands:
      - If the gateway has its own X-Anon-ID-or-JWT enforcement (defense in depth, plan 03 task 4): expect 401.
      - If only the player service rejects (current plan 03): expect 400 with body "X-Anon-ID required for unauthenticated requests".
      The verify clause accepts EITHER 400 OR 401 (with grep -E '^(400|401)$') so the executor doesn't have to predict which layer fires first; both are acceptable defenses against T-01-01.

    Save the smoke test outputs to `/tmp/plan07-smoke.log` for the SUMMARY (the verify clause writes them via tee).

    **Step 5 — Grafana dashboard reload (Pitfall 4):**
    After confirming smoke tests pass, restart Grafana to pick up the new dashboard JSON immediately (the file provider polls every ~10s but explicit restart guarantees pickup):
    ```bash
    docker compose -f docker/docker-compose.yml restart grafana
    sleep 10
    docker compose -f docker/docker-compose.yml logs grafana --tail 20 | grep -i "provision\|reload"
    ```

    If ANY smoke test fails (the verify clause exits non-zero), STOP. Do not proceed to the human-verify checkpoint or the changelog. Diagnose via `make logs-<service>`, fix, redeploy, retry.

    **Body shape used in the smoke curls** (matches `OverrideRequest` in `override.go` exactly):
    ```json
    {
      "anime_id": "smoke-test",
      "load_session_id": "<uuid>",
      "dimension": "language",
      "original_combo": null,
      "new_combo": {"language": "en"},
      "ms_since_load": 2500,
      "tier": "default",
      "tier_number": 5,
      "player": "kodik"
    }
    ```

    Per CONTEXT D-15, D-16, D-17, RESEARCH Pitfall 4, Pitfall 7. Per BLOCKER #4 from plan-checker.
  </action>
  <verify>
    <automated>set -e; cd /data/animeenigma; make redeploy-player && make redeploy-gateway && make redeploy-web && make health > /tmp/plan07-health.log 2>&1 && grep -ic "unhealthy\|red\|down" /tmp/plan07-health.log | tee -a /tmp/plan07-smoke.log | grep -q "^0$" && curl -s http://localhost:8083/metrics 2>/dev/null | grep -cE "^# HELP combo_(override|resolve)_total" | tee -a /tmp/plan07-smoke.log | grep -q "^2$" && curl -fsSo /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/preferences/resolve -H "Content-Type: application/json" -H "X-Anon-ID: smoke-anon-$(date +%s)" -d '{"anime_id":"smoke-test","available":[{"player":"kodik","language":"ru","watch_type":"sub","translation_title":"AniLibria"}]}' | tee -a /tmp/plan07-smoke.log | grep -q "^200$" && SMOKE_ID="smoke-$(date +%s)-aaaa-bbbb-cccc-dddddddddddd" && curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/preferences/override -H "Content-Type: application/json" -H "X-Anon-ID: $SMOKE_ID" -d '{"anime_id":"smoke-test","load_session_id":"'"$SMOKE_ID"'","dimension":"language","original_combo":null,"new_combo":{"language":"en"},"ms_since_load":2500,"tier":"default","tier_number":5,"player":"kodik"}' | tee -a /tmp/plan07-smoke.log | grep -q "^204$" && curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/preferences/override -H "Content-Type: application/json" -H "X-Anon-ID: $SMOKE_ID-2" -d '{"anime_id":"smoke-test","load_session_id":"x","dimension":"INVALID","original_combo":null,"new_combo":{},"ms_since_load":1000,"tier":"default","tier_number":5,"player":"kodik"}' | tee -a /tmp/plan07-smoke.log | grep -q "^400$" && curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8000/api/preferences/override -H "Content-Type: application/json" -d '{"anime_id":"smoke-test","load_session_id":"y","dimension":"language","original_combo":null,"new_combo":{},"ms_since_load":1000,"tier":"default","tier_number":5,"player":"kodik"}' | tee -a /tmp/plan07-smoke.log | grep -qE "^(400|401)$" && docker compose -f docker/docker-compose.yml restart grafana && sleep 10 && docker compose -f docker/docker-compose.yml logs grafana --tail 30 2>&1 | grep -ci "provision\|reload" | grep -qE "^[1-9]" && echo "SMOKE_OK"</automated>
    <!-- Expected: every chained command exits 0; final line is "SMOKE_OK". Each curl's status code is captured to /tmp/plan07-smoke.log via tee for SUMMARY. -->
  </verify>
  <acceptance_criteria>
    - `make redeploy-player`, `make redeploy-gateway`, `make redeploy-web` each exit 0 (chained with `&&` in verify).
    - `make health` reports zero unhealthy services: `grep -ic "unhealthy\|red\|down" /tmp/plan07-health.log` returns 0.
    - Both metric families exposed: `curl -s http://localhost:8083/metrics | grep -cE "^# HELP combo_(override|resolve)_total"` returns 2.
    - Smoke 4b (anon resolve `POST /api/preferences/resolve`): returns HTTP 200.
    - Smoke 4c (anon override `POST /api/preferences/override` with valid body): returns HTTP 204.
    - Smoke 4d (invalid dimension `INVALID`): returns HTTP 400 (T-01-02 enforced).
    - Smoke 4e (no JWT and no X-Anon-ID): returns HTTP 400 OR 401 (T-01-01 enforced; either layer is acceptable).
    - Grafana logs show provisioning reload after restart: `docker compose -f docker/docker-compose.yml logs grafana --tail 30 | grep -ci "provision\|reload"` returns >= 1.
    - Final verify line is `SMOKE_OK` (proves all chained commands passed).
  </acceptance_criteria>
  <done>Backend + gateway + frontend deployed. Both counter families ingressing. All 4 smoke tests pass with explicit status-code assertions in `<verify>` (BLOCKER #4 closed). Grafana picked up the new dashboard JSON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Grafana panel renders</name>
  <files>(none — verification only)</files>
  <action>
    Pause execution and wait for human visual confirmation that the new Grafana panel
    renders correctly. The executor performs no file edits in this task; it presents the
    verification instructions below to the human and waits for an "approved" or "issues: ..."
    response on the resume-signal.
  </action>
  <what-built>
    The "Auto-Pick Override Rate (Phase 1 Baseline)" panel was added to the existing
    preference-resolution dashboard and Grafana was restarted to pick up the change.
    Backend smoke tests (Task 1) confirmed both counter families are ingressing and that
    T-01-01 / T-01-02 reject invalid requests with the expected status codes.
  </what-built>
  <how-to-verify>
    1. Open `https://admin.animeenigma.ru/grafana/d/preference-resolution-v1` in a browser.
       (If the URL slug differs — find it via Grafana home → "Dashboards" → "Preference Resolution".)
    2. Confirm the new collapsible row "Auto-Pick Override Rate (Phase 1 Baseline)" is visible
       at the bottom of the dashboard.
    3. Expand the row if collapsed. Verify all 5 panels render:
       a. "Override Rate (last 5m)" — a stat panel; expected to show "No data" or a small
          value (the smoke tests fired only 1 override).
       b. "Override Rate by Tier" — a timeseries; may be empty if no traffic in last 5m.
       c. "Override Rate by Player" — a timeseries.
       d. "Override Rate by Language and Auth State" — a timeseries.
       e. "Overrides by Dimension (24h count)" — a barchart; should show "language: 1" from
          the smoke test (or similar).
    4. Hover over the stat panel: confirm the description tooltip mentions "Override rate is
       computed against fresh resolves only..." (the Pitfall 4 documentation).
    5. Click "Edit" on any of the new panels and confirm the PromQL `expr` references
       `combo_override_total` and `combo_resolve_total` (NOT `users_*` or older metric names).
    6. Click an alternate language tab on a real anime detail page (logged out) within 30s
       of player load. Wait 1 minute. Refresh the dashboard. The "Overrides by Dimension"
       barchart should now show a count for that dimension.

    Optional bonus: in Grafana Explore, run LogQL `{service="player"} |= "combo_override" | json`
    over the last 1h. The smoke test override should appear as a structured log line with
    fields anime_id, load_session_id, dimension, anon_id, tier, etc. Verify NO `username`
    or `Authorization` field is present (T-01-03 manual confirmation). Note: per the Loki
    retention constraint documented in PROJECT.md, this view is bounded to the last 7 days.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 panels render and the smoke override appears in the dimension
    barchart. Type "issues: <description>" if the panel doesn't appear, renders broken, or
    the PromQL targets the wrong metrics. If broken, the most likely causes are:
      - Grafana didn't pick up the new dashboard (re-run `docker compose restart grafana`).
      - Datasource UID mismatch (the new panels reference `${DS_PROMETHEUS}` but existing
        panels use a literal name — re-edit the JSON to mirror exactly).
      - The override row was added below the visible scroll area — scroll to bottom.
  </resume-signal>
  <verify>
    <automated>echo "Manual verification gate — awaiting human resume-signal"</automated>
  </verify>
  <done>Human types "approved" after seeing all 5 panels render with the smoke override visible in the dimension barchart.</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 1 follow-up marker to STATE.md</name>
  <read_first>
    - .planning/STATE.md (full file — append the marker at the end, but BEFORE any closing footer if one exists)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    Append a single follow-up marker line so future Claude / human is reminded to capture the
    24h baseline override-rate snapshot to PROJECT.md before Phase 6 starts. WARNING #9 from
    plan-checker: "No mechanism reminds future Claude / human to capture the 24h baseline
    before Phase 6."

    Read the current STATE.md to find the right insertion point. If STATE.md has a "follow-ups"
    or "open items" section, append the marker there. If not, append it at the end of the file
    (before any final `---` or footer line).

    Append exactly this content (one blank line above, the heading + marker, no trailing
    whitespace):

    ```
    
    ## Phase 1 Follow-ups
    
    - **Phase 1 follow-up:** Capture ≥ 24h baseline override-rate snapshot to .planning/PROJECT.md before Phase 6 starts. Computed via PromQL: rate(combo_override_total[24h]) / rate(combo_resolve_total[24h]), segmented by tier/language/anon/player/dimension. This is ROADMAP success criterion 3 — a phase-gate, not a Phase 1 task. Do not open Phase 6 work until this snapshot is recorded under PROJECT.md § "Baseline override rate".
    ```

    If STATE.md already has a `## Phase 1 Follow-ups` heading from a prior revision pass,
    DO NOT duplicate the heading — append only the bullet line under the existing heading.

    Implementation pattern:
    ```bash
    # Use a heredoc into a single append, no echo with newline tricks.
    # First check if the heading already exists.
    if grep -q "^## Phase 1 Follow-ups" /data/animeenigma/.planning/STATE.md; then
      # Heading exists — append just the bullet (idempotent on multiple runs would
      # add duplicates, so check for the marker text first).
      if ! grep -q "Phase 1 follow-up:" /data/animeenigma/.planning/STATE.md; then
        printf '\n- **Phase 1 follow-up:** Capture ≥ 24h baseline override-rate snapshot to .planning/PROJECT.md before Phase 6 starts. Computed via PromQL: rate(combo_override_total[24h]) / rate(combo_resolve_total[24h]), segmented by tier/language/anon/player/dimension. This is ROADMAP success criterion 3 — a phase-gate, not a Phase 1 task. Do not open Phase 6 work until this snapshot is recorded under PROJECT.md § "Baseline override rate".\n' >> /data/animeenigma/.planning/STATE.md
      fi
    else
      # Heading doesn't exist — append heading + bullet.
      printf '\n## Phase 1 Follow-ups\n\n- **Phase 1 follow-up:** Capture ≥ 24h baseline override-rate snapshot to .planning/PROJECT.md before Phase 6 starts. Computed via PromQL: rate(combo_override_total[24h]) / rate(combo_resolve_total[24h]), segmented by tier/language/anon/player/dimension. This is ROADMAP success criterion 3 — a phase-gate, not a Phase 1 task. Do not open Phase 6 work until this snapshot is recorded under PROJECT.md § "Baseline override rate".\n' >> /data/animeenigma/.planning/STATE.md
    fi
    ```

    Per WARNING #9 from plan-checker (revision context).
  </action>
  <verify>
    <automated>grep -c "Phase 1 follow-up" /data/animeenigma/.planning/STATE.md | tee /tmp/plan07-state-grep.log | grep -qE "^[1-9]"</automated>
    <!-- Expected: grep returns >= 1 (acceptance criterion from WARNING #9). -->
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 1 follow-up" .planning/STATE.md` returns >= 1.
    - `grep -c "rate(combo_override_total" .planning/STATE.md` returns >= 1 (PromQL hint preserved).
    - `grep -c "Phase 6" .planning/STATE.md` returns >= 1 (the gating phase is named).
    - The marker is on a single bullet line (no orphan paragraph or broken Markdown): `grep -B 1 "Phase 1 follow-up:" .planning/STATE.md | head -1` shows either an empty line or another bullet (NOT a stray heading mid-paragraph).
    - The append is idempotent on re-run: running the heredoc twice does NOT produce two markers (gated by `grep -q "Phase 1 follow-up:"`).
  </acceptance_criteria>
  <done>STATE.md carries forward a single durable reminder that the 24h baseline must be captured to PROJECT.md before Phase 6. WARNING #9 closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run animeenigma-after-update skill (changelog + commit + push)</name>
  <files>frontend/web/public/changelog.json (modified by the skill, not by the executor)</files>
  <action>
    Pause and request the user to invoke the `animeenigma-after-update` skill which performs
    the standard end-of-implementation flow per CLAUDE.md: lint, build, redeploy, update
    `frontend/web/public/changelog.json`, commit with the three required co-authors, and
    push to origin/main. The executor verifies completion by reading the criteria below and
    waits for the resume-signal.
  </action>
  <what-built>
    Phase 1 backend, frontend, and dashboard JSON are deployed and live. PROJECT.md now
    documents the Loki 7d retention constraint. STATE.md carries the Phase 1 follow-up
    marker. The codebase has uncommitted changes across libs/metrics, services/player,
    services/gateway, frontend/web, docker/grafana/dashboards, .planning/PROJECT.md, and
    .planning/STATE.md. The `animeenigma-after-update` skill handles lint, build, redeploy
    verification, changelog update, and the final commit + push.
  </what-built>
  <how-to-verify>
    1. The user invokes the `animeenigma-after-update` skill (e.g. by running
       `/animeenigma-after-update` or another agreed trigger).
    2. The skill performs (per CLAUDE.md "After-Update Skill"):
       a. Lints the affected code (`bunx eslint src/`, `bunx tsc --noEmit`, `go vet ./...`).
       b. Builds (`bunx vite build` for the frontend if not already done by redeploy-web; `go build ./...` for affected services).
       c. Redeploys via `make redeploy-player` / `make redeploy-gateway` / `make redeploy-web`.
          (Plan 07 Task 1 already redeployed; the skill will be a fast no-op if state is clean.)
       d. Runs `make health` to confirm.
       e. Updates `frontend/web/public/changelog.json` with a new entry describing the Phase 1
          instrumentation (auto-pick override rate metric + Grafana tile). The entry should be
          informative + enthusiastic with emojis (per CLAUDE.md), and include user-visible
          implications: "we now know when the auto-pick gets it wrong" — even if the user-
          visible behavior didn't change. **Schema reminder:** changelog.json is a top-level
          ARRAY of date-grouped objects, each with `entries[]` whose items have `type` and
          `message` (NO `title` field). The skill must follow this shape; verification below
          uses `jq -r '.[0].entries[].message'`.
       f. Commits all changed files with the three required co-authors:
          ```
          Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
          Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
          Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>
          ```
       g. Pushes to the remote `main` branch.

    3. After the skill completes, verify:
       - `git log -1 --pretty=fuller` shows the three co-authors.
       - `git status` is clean (no uncommitted files).
       - `git rev-list HEAD..origin/main --count` returns 0 (or `git status -sb` shows
         "ahead 0, behind 0" relative to origin/main).
       - `jq -r '.[0].entries[].message' frontend/web/public/changelog.json | grep -iE "override|baseline|instrumentation"` non-empty
         (BLOCKER #3 from plan-checker: the schema is array-of-date-groups with `message`
         fields, NOT `entries[0].title`. The verify command MUST use the array-indexed path.)
  </how-to-verify>
  <resume-signal>
    Type "approved" once the skill has completed and the verifications above all pass.
    Type "issues: <description>" if anything failed (e.g., push rejected, lint error).
    The skill is the standard end-of-implementation flow per CLAUDE.md "Don't skip this step."
  </resume-signal>
  <verify>
    <automated>git log -1 --pretty=fuller | grep -c "Co-Authored-By: 0neymik0\|Co-Authored-By: NANDIorg\|Co-Authored-By: Claude Opus" ; git status --porcelain | wc -l ; git rev-list HEAD..@{u} --count 2>/dev/null || echo 0 ; jq -r '.[0].entries[].message' frontend/web/public/changelog.json 2>/dev/null | grep -ciE "override|baseline|instrumentation"</automated>
    <!-- Expected: 3 (co-authors) ; 0 (clean) ; 0 (not behind) ; >=1 (changelog entry mentions phase 1). -->
  </verify>
  <done>HEAD commit has all 3 required co-authors; `git status` is clean; local main is not behind origin; `jq -r '.[0].entries[].message' frontend/web/public/changelog.json | grep -iE "override|baseline|instrumentation"` returns at least one matching entry. BLOCKER #3 closed (correct jq path used).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production server (this server IS production per CLAUDE.md) → live users | `make redeploy` deploys live; smoke tests run against the actual production stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | DoS | live `/api/preferences/override` endpoint exposed publicly after deploy | mitigate | Smoke test 4e in `<verify>` confirms the both-missing-identity guard fires (400 OR 401) on production. Existing gateway rate-limiter applies to /api/* paths. Grafana panel "Override Rate by Player" surfaces anomalous rates within 5 minutes. |
| T-01-02 | Tampering | invalid `dimension` label causing Prometheus cardinality explosion | mitigate | Smoke test 4d in `<verify>` confirms the dimension whitelist rejects `INVALID` with HTTP 400 on production. Static enforcement is in `services/player/internal/handler/override.go` (`validDimensions` map). |
| T-01-03 | Information Disclosure | structured `combo_override` log lines in production Loki | mitigate | Manual verification in Task 2 step 6 (Grafana Explore) confirms NO username, Authorization, or token in the structured fields. Loki retention is 7 days as documented in PROJECT.md § "Loki retention constraint" (Task 0 of this plan). |
</threat_model>

<verification>
- All `make redeploy-*` targets succeed; `make health` reports healthy
- `/metrics` exposes `combo_override_total` and `combo_resolve_total`
- 4 smoke tests in `<verify>` (anon resolve, anon override, invalid dimension, both missing) return expected status codes — chained as a single `set -e` shell pipeline so any failure aborts the verify
- PROJECT.md documents Loki 7d retention (Task 0)
- STATE.md carries Phase 1 follow-up marker (Task 3)
- Grafana renders the new panel (human-verified in Task 2)
- changelog.json updated; git committed with 3 co-authors; pushed to origin (with correct jq schema path)
</verification>

<success_criteria>
1. ROADMAP success criterion 1: `combo_override` events emitted across all 4 players (frontend wiring deployed; smoke test confirms backend ingest path).
2. ROADMAP success criterion 2: Grafana tile shows override rate segmented by tier, language, anon-vs-auth, and player, refreshing within 1 minute (manually verified in Task 2).
3. ROADMAP success criterion 4: Instrumentation deployed via `make redeploy-player` (and gateway and web) AND verified live (smoke tests + manual Grafana check).
4. M-01 + M-02 functional contracts met.
5. Phase commit + push complete.
6. PROJECT.md and STATE.md correctly carry the Phase 6 prerequisites (Loki retention truth + 24h-baseline reminder).

NOTE: ROADMAP success criterion 3 (≥ 24h baseline snapshot recorded in PROJECT.md before Phase 6 starts) is a **phase-gate** done AFTER 24h of real traffic. It is recorded in VALIDATION.md "Manual-Only Verifications" AND in STATE.md (via the Task 3 follow-up marker). It is the responsibility of whoever opens Phase 6 — not this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrumentation-baseline/01-07-SUMMARY.md` with:
- Output of all 4 smoke tests (status codes + counter delta) from `/tmp/plan07-smoke.log`
- Confirmation of Grafana visual approval
- Hash of the final commit + confirmation it pushed
- Diff (or section reference) of the new PROJECT.md "Loki retention constraint" subsection
- Diff (or line reference) of the new STATE.md "Phase 1 follow-up" marker
- Reminder block in BIG letters: "Phase 6 BLOCKER: capture 24h baseline override rate from
  Grafana on `<DATE+24h>` and record under '.planning/PROJECT.md § Baseline override rate'
  before opening Phase 6 work. PromQL one-liner:
  `rate(combo_override_total[24h]) / rate(combo_resolve_total[24h])`"
</output>
