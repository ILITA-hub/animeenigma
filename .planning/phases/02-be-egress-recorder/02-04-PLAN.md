---
phase: 02-be-egress-recorder
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - libs/tracing/client.go
  - libs/tracing/client_test.go
  - services/catalog/cmd/catalog-api/main.go
  - services/scraper/cmd/scraper-api/main.go
  - services/streaming/cmd/streaming-api/main.go
  - services/analytics/cmd/analytics-api/main.go
  - docker/docker-compose.yml
  - CLAUDE.md
autonomous: false
requirements: [AR-EGRESS-01, AR-EGRESS-02, AR-EGRESS-03, AR-EGRESS-04, AR-EGRESS-05]
must_haves:
  truths:
    - "user_id NEVER leaves the process in propagated baggage on a 3rd-party-bound outbound request (only origin/operation ride the wire)"
    - "An inbound request → outbound effect carries the SAME origin/operation baggage end-to-end (AR-EGRESS-02)"
    - "The seed middleware + producer are wired into catalog, scraper, streaming with ANALYTICS_INTERNAL_URL; analytics serves /internal/effects"
    - "After redeploy, real egress rows (host/provider/bytes) and a single aggregated HLS row per watch session appear in ClickHouse"
    - "make health is green for catalog, scraper, streaming, analytics post-deploy"
  artifacts:
    - path: "libs/tracing/client.go"
      provides: "baggage-PII strip on outbound 3rd-party requests"
      contains: "user_id"
    - path: "docker/docker-compose.yml"
      provides: "ANALYTICS_INTERNAL_URL env on catalog/scraper/streaming"
      contains: "ANALYTICS_INTERNAL_URL"
  key_links:
    - from: "inbound SeedMiddleware"
      to: "outbound recording RoundTripper"
      via: "origin/operation baggage end-to-end (user_id private ctx only)"
      pattern: "operation"
    - from: "catalog/scraper/streaming producer"
      to: "analytics /internal/effects"
      via: "ANALYTICS_INTERNAL_URL"
      pattern: "ANALYTICS_INTERNAL_URL"
---

<objective>
Close the phase: harden the baggage-PII boundary (the sharpest security finding), prove baggage rides end-to-end inbound→outbound, wire the seed middleware + producer into catalog/scraper/streaming with the analytics internal URL, then redeploy the four services and verify live egress + single-HLS rows in ClickHouse.

Purpose: AR-EGRESS-02 (baggage end-to-end), the security mitigation T-02-PII (user_id must not ride outbound wire baggage to Kodik/OpenSubtitles/ARM/AniList/CDNs — RESEARCH §Security "sharpest finding"), and the live production verification of AR-EGRESS-01/03/04/05.
Output: A hardened, wired, deployed BE egress recorder with live ClickHouse effect rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-be-egress-recorder/02-CONTEXT.md
@.planning/phases/02-be-egress-recorder/02-RESEARCH.md
@.planning/phases/02-be-egress-recorder/02-01-SUMMARY.md
@.planning/phases/02-be-egress-recorder/02-02-SUMMARY.md
@.planning/phases/02-be-egress-recorder/02-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From Plan 02-01 (libs/tracing): -->
```go
func SeedMiddleware(service string) func(http.Handler) http.Handler // seeds origin/operation baggage + private user_id ctx
func WrapTransport(base http.RoundTripper) http.RoundTripper          // composes recording when global sink set
type Producer // Record(Effect) + Start()/Stop(); POSTs to ANALYTICS_INTERNAL_URL + /internal/effects
```

From RESEARCH §Security Domain (the mitigation to implement):
- otelhttp injects baggage on OUTBOUND requests → user_id in W3C baggage would be sent to every external host.
- Mitigation chosen (02-01): user_id is carried via a PRIVATE non-propagated ctx value, NOT baggage. This plan ADDS the defense-in-depth strip + the proving test, and asserts no user_id baggage member is ever set.

From RESEARCH §Runtime State Inventory:
- New env: ANALYTICS_INTERNAL_URL=http://analytics:8092 on catalog/scraper/streaming compose blocks + CLAUDE.md env docs.
- /internal/effects must NOT be gateway-proxied (assert no gateway route).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baggage-PII strip + end-to-end baggage proof (libs/tracing)</name>
  <read_first>
    libs/tracing/client.go (recordingTransport from 02-01 — add the outbound user_id-strip guard);
    libs/tracing/baggage.go (WithUserID/UserIDFromContext private ctx value — user_id must stay here, never in baggage);
    libs/tracing/middleware.go (SeedMiddleware from 02-01);
    libs/tracing/client_test.go (extend);
    RESEARCH §Security Domain "sharpest finding" + VALIDATION row (security) TestNoUserIDOnOutboundWire.
  </read_first>
  <files>libs/tracing/client.go, libs/tracing/client_test.go</files>
  <behavior>
    - Test 1 (TestNoUserIDOnOutboundWire): seed user_id (private ctx) + origin/operation (baggage), issue an outbound request to an httptest server that inspects the incoming `baggage:` header → the header contains origin/operation but NO user_id member; the recorder's in-process Effect still has UserID populated (read from the private ctx value).
    - Test 2 (TestBaggageE2E): an httptest inbound handler wrapped in SeedMiddleware makes an outbound call on the recording transport → the emitted Effect carries the same origin/operation as seeded inbound.
  </behavior>
  <action>
    In `libs/tracing/client.go` recordingTransport.RoundTrip, add a defense-in-depth guard ensuring no `user_id` baggage member is ever present on the outbound request (the design already keeps user_id out of baggage via the private ctx value from 02-01; this asserts/strips it so a future caller can't accidentally add it). The recorder reads UserID from `UserIDFromContext(req.Context())` (private ctx), NOT from baggage. Add `TestNoUserIDOnOutboundWire` (inspect the wire `baggage:` header on an httptest server) and `TestBaggageE2E` (SeedMiddleware inbound → recording outbound carries operation). Confirm only origin/operation ride the wire.
    Implements AR-EGRESS-02 (end-to-end) + security T-02-PII.
  </action>
  <verify>
    <automated>cd libs/tracing && go test ./ -run 'TestNoUserIDOnOutboundWire|TestBaggageE2E' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cd libs/tracing && go test ./ -run 'TestNoUserIDOnOutboundWire|TestBaggageE2E' -count=1` passes.
    - `grep -nv '^//' libs/tracing/client.go | grep -c 'user_id'` — any user_id reference is the strip guard, never a baggage.NewMemberRaw("user_id", ...) add.
    - `cd libs/tracing && go test ./ -count=1` (whole package) passes; `go vet ./` clean.
  </acceptance_criteria>
  <done>user_id never rides outbound wire baggage (proven by TestNoUserIDOnOutboundWire); inbound→outbound origin/operation carry end-to-end (TestBaggageE2E); the recorder reads user_id from the private ctx value.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SeedMiddleware + producer into catalog/scraper/streaming/analytics + ANALYTICS_INTERNAL_URL</name>
  <read_first>
    services/catalog/cmd/catalog-api/main.go, services/scraper/cmd/scraper-api/main.go, services/streaming/cmd/streaming-api/main.go (where tracing.HTTPMiddleware is applied + where the recording sink/producer is constructed);
    services/analytics/cmd/analytics-api/main.go (effects handler wiring from 02-01 — confirm /internal/effects served);
    docker/docker-compose.yml (catalog/scraper/streaming env blocks — add ANALYTICS_INTERNAL_URL);
    CLAUDE.md (Environment Variables section — document the new var);
    RESEARCH §Runtime State Inventory (env + Docker-only route).
  </read_first>
  <files>services/catalog/cmd/catalog-api/main.go, services/scraper/cmd/scraper-api/main.go, services/streaming/cmd/streaming-api/main.go, services/analytics/cmd/analytics-api/main.go, docker/docker-compose.yml, CLAUDE.md</files>
  <action>
    In catalog/scraper/streaming main: construct the `tracing.Producer` from `ANALYTICS_INTERNAL_URL` (default `http://analytics:8092`), set it as the process-global EffectSink (so `WrapTransport`/`WrapRecording` record), `Start()` it and register `Stop()` on graceful shutdown; add `tracing.SeedMiddleware(service)` AFTER chi routing so the route pattern is available (alongside the existing `tracing.HTTPMiddleware`). Confirm analytics main wires the effects handler (from 02-01) so `/internal/effects` is served. Add `ANALYTICS_INTERNAL_URL=http://analytics:8092` to the catalog/scraper/streaming env blocks in `docker/docker-compose.yml`; document it in CLAUDE.md's Environment Variables section. Do NOT add any gateway route for `/internal/effects`.
    Implements the wiring half of AR-EGRESS-01/02/03/04/05.
  </action>
  <verify>
    <automated>cd services/catalog && go build ./... && cd /data/animeenigma/services/scraper && go build ./... && cd /data/animeenigma/services/streaming && go build ./... && cd /data/animeenigma/services/analytics && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - All four `go build ./...` succeed.
    - `grep -c 'ANALYTICS_INTERNAL_URL' docker/docker-compose.yml` ≥ 3 (catalog/scraper/streaming).
    - `grep -c 'SeedMiddleware' services/catalog/cmd/catalog-api/main.go services/scraper/cmd/scraper-api/main.go services/streaming/cmd/streaming-api/main.go` ≥ 1 each.
    - `grep -v '^#' docker/docker-compose.yml | grep -c 'ANALYTICS_INTERNAL_URL'` ≥ 3; no gateway route for /internal/effects (assert absence in gateway router).
    - `grep -c 'ANALYTICS_INTERNAL_URL' CLAUDE.md` ≥ 1.
  </acceptance_criteria>
  <done>Seed middleware + producer wired into the three BE services with ANALYTICS_INTERNAL_URL; analytics serves /internal/effects; the new env var is documented; gateway exposes no /internal/effects route.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Redeploy + live ClickHouse egress verification</name>
  <action>Run the four redeploys + make health, trigger a real scraper resolve + an HLS watch session, then query ClickHouse to confirm live egress rows and a single aggregated per-(session,host) HLS row before requesting human sign-off. Steps detailed in how-to-verify below.</action>
  <what-built>The full BE egress recorder: effect ingestion path, recording transport, baggage propagation, 4 client retrofits, HLS session aggregation + dual byte counting, all wired into catalog/scraper/streaming/analytics. Claude will run the redeploy + automated full-suite + health checks BEFORE this checkpoint.</what-built>
  <how-to-verify>
    1. Claude runs: `cd /data/animeenigma && make redeploy-analytics && make redeploy-catalog && make redeploy-scraper && make redeploy-streaming && make health` — confirm all four report healthy.
    2. Trigger a real EN-player resolve (scraper egress) and play an episode through the HLS proxy, then stop and wait ~idle window (~45s).
    3. Query ClickHouse for live egress rows:
       `docker compose -f docker/docker-compose.yml exec -T clickhouse clickhouse-client -q "SELECT origin, operation, target, host, bytes_in, bytes_out, requests FROM analytics.events WHERE effect_kind='egress' ORDER BY timestamp DESC LIMIT 20"`
       — confirm rows with non-zero host/bytes, and at least one row whose target carries a stream-provider.
    4. Confirm the HLS session produced ONE aggregated row per (session,host) with summed bytes + a segment count (requests > 1), NOT one row per segment.
    5. Confirm no user_id leaked to a 3rd-party host (the automated TestNoUserIDOnOutboundWire already proves this in CI; spot-check is optional).
  </how-to-verify>
  <resume-signal>Type "approved" once live egress rows + a single aggregated HLS row are confirmed, or describe what's missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorder ctx → outbound 3rd-party wire | OTel baggage injected on outbound; the PII boundary to harden |
| BE service → analytics /internal/effects | Docker-network-only producer→sink |
| inbound seed → downstream services | baggage carries origin/operation cross-service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-PII | Information Disclosure | recording RoundTripper / outbound baggage | mitigate | user_id carried via private non-propagated ctx value; defense-in-depth strip ensures no user_id baggage member rides the wire; proven by TestNoUserIDOnOutboundWire (the blocking security finding). |
| T-02-INT | Elevation/Tampering | /internal/effects exposure | mitigate | ANALYTICS_INTERNAL_URL hits analytics directly on the Docker network; assert NO gateway route is added for /internal/effects. |
| T-02-ENV | Tampering | ANALYTICS_INTERNAL_URL | accept | Non-secret service-discovery URL; no credential. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd libs/tracing && go test ./ -count=1`
- `cd services/{catalog,scraper,streaming,analytics} && go build ./...`
- `make redeploy-{analytics,catalog,scraper,streaming} && make health`
- Live ClickHouse query: egress rows with host/provider/bytes + a single aggregated HLS row per session.
- Assert no gateway route for /internal/effects.
</verification>

<success_criteria>
- user_id never rides outbound wire baggage (TestNoUserIDOnOutboundWire green); origin/operation carry end-to-end (TestBaggageE2E green) — AR-EGRESS-02.
- Producer + seed middleware wired into catalog/scraper/streaming; analytics serves /internal/effects Docker-network-only.
- After redeploy, live egress rows (host/provider/bytes) and ONE aggregated HLS row per watch session appear in ClickHouse — AR-EGRESS-01/03/04/05 confirmed live.
- make health green for the four services.
</success_criteria>

<output>
Create `.planning/phases/02-be-egress-recorder/02-04-SUMMARY.md` when done
</output>
