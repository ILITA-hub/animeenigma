---
phase: 04-fe-causation-rum
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/analytics/types.ts
  - frontend/web/src/analytics/rum.ts
  - frontend/web/src/analytics/index.ts
  - frontend/web/src/analytics/__tests__/rum.spec.ts
autonomous: true
requirements: [AR-FE-03]
must_haves:
  truths:
    - "A PerformanceObserver emits one beacon row per (flush-window, 3rd-party host) flagged source='fe_rum', accuracy=approx"
    - "RUM rows carry requests (count) + duration_ms (timing) + host-only target; never bytes; never the full signed URL"
    - "Self-host (location.host) resource entries are dropped — only browser→3rd-party timings are beaconed"
  artifacts:
    - path: "frontend/web/src/analytics/rum.ts"
      provides: "PerformanceObserver('resource') → per-host aggregated fe_rum rows via the existing Transport"
      min_lines: 30
    - path: "frontend/web/src/analytics/types.ts"
      provides: "AnalyticsEvent extended with source/route/action/operation/target/target_kind/requests/duration_ms"
      contains: "duration_ms"
    - path: "frontend/web/src/analytics/__tests__/rum.spec.ts"
      provides: "host aggregation + self-host drop + byte-poverty + host-only-target assertions"
      contains: "fe_rum"
  key_links:
    - from: "frontend/web/src/analytics/rum.ts"
      to: "frontend/web/src/analytics/index.ts"
      via: "analytics.track('rum.resource', ...) routed through the existing Transport"
      pattern: "track\\(.*rum"
    - from: "frontend/web/src/analytics/index.ts"
      to: "frontend/web/src/analytics/rum.ts"
      via: "RUM observer init wired into Analytics.init()"
      pattern: "initRum|startRum|rum"
---

<objective>
Add the one new instrumentation primitive this phase needs: a `PerformanceObserver('resource')` module (`rum.ts`) that aggregates browser→3rd-party resource timings per host per flush window and beacons them through the EXISTING `Transport`, flagged `source='fe_rum'` with host-only `target` and NO byte fields. Extend the `AnalyticsEvent` wire type with the register fields (`source`, `route`, `action`, `operation`, `target`, `target_kind`, `requests`, `duration_ms`) so Plan 03's interceptor and this RUM module can emit register rows that Plan 01's collect handler maps.

Purpose: AR-FE-03 — cross-origin resource timings are opaque (`transferSize=0` without `Timing-Allow-Origin`), so RUM rows must be structurally byte-poor and clearly flagged `accuracy=approx`, making contamination of authoritative BE bytes impossible (RESEARCH Pattern 3 / Pitfall 1).
Output: new `rum.ts` + co-located `rum.spec.ts`; extended `types.ts`; RUM observer wired into `index.ts` `init()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fe-causation-rum/04-RESEARCH.md
@.planning/phases/04-fe-causation-rum/04-PATTERNS.md

<interfaces>
<!-- Existing AnalyticsEvent (types.ts:5-20) to EXTEND (keep all new fields optional): -->
interface AnalyticsEvent { event_type; event_name?; timestamp; url?; path?; ...el_*; active_ms?; trace_id?; properties? }

<!-- Existing emit seam (index.ts:71-73): -->
track(name, props?) -> enqueue({ event_type:'custom', event_name:name, timestamp, path, properties:props })

<!-- The observer/feature-detect analog (utils/diagnostics.ts:140-159): -->
if (typeof PerformanceObserver !== 'undefined') { try { new PerformanceObserver((list)=>{ for (const e of list.getEntries()) {/* PerformanceResourceTiming */} }).observe({type:'resource', buffered:false}) } catch {} }

<!-- Init lifecycle to copy (index.ts:19-21 idempotent guard, :36/:50/:53 listener registration). -->
<!-- snake_case wire keys MUST equal the Go json tags Plan 01 adds: source, route, action, operation, target, target_kind, requests, duration_ms. NEVER 'rows'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AnalyticsEvent wire type with register fields</name>
  <files>frontend/web/src/analytics/types.ts</files>
  <read_first>
    - frontend/web/src/analytics/types.ts (the AnalyticsEvent interface at :5-20 — the edit target)
    - .planning/phases/04-fe-causation-rum/04-PATTERNS.md (section "types.ts (MODIFY)" — the exact field list + the "keep optional" rationale)
  </read_first>
  <action>
    Add EIGHT optional fields to the `AnalyticsEvent` interface, named EXACTLY as the Go `json:` tags Plan 01 adds (snake_case): `source?: string`, `route?: string`, `action?: string`, `operation?: string`, `target?: string`, `target_kind?: string`, `requests?: number`, `duration_ms?: number`. Keep every field optional so existing clickstream rows (pageview/click/heartbeat) that omit them are unaffected — this mirrors how `domain.Event` register fields default empty for clickstream rows. Do NOT add any byte field (`bytes_in`/`bytes_out`) — RUM rows are structurally byte-poor. Do NOT rename or remove any existing field. Add a one-line comment above the new block noting these mirror the collect.go register dimensions (Phase 4).
  </action>
  <verify>
    <automated>cd frontend/web && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'duration_ms' frontend/web/src/analytics/types.ts` returns the new field.
    - `grep -nE 'bytes_(in|out)' frontend/web/src/analytics/types.ts` returns nothing.
    - `bunx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>AnalyticsEvent carries the 8 new optional register fields (snake_case, no byte fields); typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create rum.ts PerformanceObserver + co-located spec, wire into init()</name>
  <files>frontend/web/src/analytics/rum.ts, frontend/web/src/analytics/index.ts, frontend/web/src/analytics/__tests__/rum.spec.ts</files>
  <read_first>
    - frontend/web/src/utils/diagnostics.ts:139-159 (the existing PerformanceObserver('resource') shape + feature-detect — the observer analog)
    - frontend/web/src/analytics/index.ts (the track emit at :71-73 and the idempotent init lifecycle at :19-57 — the emit + init analogs)
    - frontend/web/src/analytics/__tests__/transport.spec.ts (the vitest+jsdom mocking + vi.fn/vi.stubGlobal style — the test analog)
    - .planning/phases/04-fe-causation-rum/04-PATTERNS.md (sections "rum.ts (NEW)" and "rum.spec.ts (NEW)" — the aggregation + flag + host-only + byte-poverty requirements)
  </read_first>
  <behavior>
    - Two synthetic resource entries to the same 3rd-party host aggregate into ONE emitted row with requests==2 and duration_ms == sum of their durations (rounded).
    - A resource entry whose host === location.host is dropped (not emitted).
    - The emitted props carry source:'fe_rum', target_kind:'host', target equal to the host ONLY (no path, no query), requests, duration_ms; and carry NO bytes_in/bytes_out key.
    - Feature-absence (no PerformanceObserver) is a silent no-op (init does not throw).
  </behavior>
  <action>
    Create `frontend/web/src/analytics/rum.ts` exporting an `initRum()` function (idempotent — guard a module-level `started` flag mirroring index.ts:20). Inside, feature-detect `typeof PerformanceObserver !== 'undefined'` (silently return if absent, like diagnostics.ts). Construct a `PerformanceObserver` whose callback iterates `list.getEntries()` cast to `PerformanceResourceTiming`; for each, parse the host via `new URL(entry.name).host` inside a try/catch (skip on parse failure); SKIP entries where `host === location.host` (only browser→3rd-party). Aggregate into a `Map<string, { count: number; dur: number }>` keyed by host, incrementing `count` and adding `entry.duration` (the ONE reliable cross-origin field — never read `transferSize`/`encodedBodySize`). After the loop, for each host emit via the existing `analytics.track('rum.resource', { source: 'fe_rum', target_kind: 'host', target: host, requests: count, duration_ms: Math.round(dur) })`. Call `observer.observe({ type: 'resource', buffered: true })` (buffered catches pre-init entries). NEVER include a byte field in the emitted props. NEVER ship the full URL — host only (signed CDN URLs carry `tham/h` auth tokens, RESEARCH Security: Information Disclosure).

    Import `analytics` from `./index` (or pass a thin emit callback to keep rum.ts testable without the singleton — prefer dependency-injecting the emit fn with a default of `analytics.track.bind(analytics)` so the spec can assert on a `vi.fn()` directly). Wire `initRum()` into `Analytics.init()` in index.ts alongside the existing click-listener registration (after `this.page()` or near the listener block) so RUM starts at analytics bootstrap; respect the existing idempotency guard.

    Create `frontend/web/src/analytics/__tests__/rum.spec.ts` mirroring transport.spec.ts: `vi.resetModules()` in beforeEach; stub `PerformanceObserver` via `vi.stubGlobal` so the test can synchronously invoke the registered callback with a fake `{ getEntries: () => [...] }` list of `PerformanceResourceTiming`-shaped objects (give each a `name` full URL with path+query, a `duration`, and one entry with `name` on `location.host`). Inject a `vi.fn()` emit and assert (≥5): (1) the self-host entry produced no emit; (2) two same-3rd-party-host entries produce exactly one emit with `requests===2`; (3) `duration_ms` equals the rounded sum of the two durations; (4) emitted `target` equals the host only (no `/path` or `?query`); (5) the emitted props object has NO `bytes_in`/`bytes_out` key and carries `source==='fe_rum'`.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/analytics/__tests__/rum.spec.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run src/analytics/__tests__/rum.spec.ts` passes with ≥5 assertions.
    - `grep -n "source: 'fe_rum'" frontend/web/src/analytics/rum.ts` (or equivalent) present.
    - `grep -nE "transferSize|encodedBodySize|bytes_(in|out)" frontend/web/src/analytics/rum.ts` returns nothing.
    - `grep -n "initRum" frontend/web/src/analytics/index.ts` shows it wired into init().
  </acceptance_criteria>
  <done>rum.ts aggregates per 3rd-party host, emits byte-poor host-only fe_rum rows via the existing Transport, is wired into init(), and the spec proves aggregation + self-host drop + byte-poverty + host-only target; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser RUM observer → analytics beacon | The observed resource URLs include signed 3rd-party CDN URLs with auth tokens; only the host should leave the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | `rum.ts` resource `entry.name` | mitigate | Reduce `entry.name` to host-only via `new URL().host` before beaconing — signed CDN segment URLs carry `tham/h` auth windows; never ship the full URL. |
| T-04-05 | Information Disclosure | RUM byte fields | mitigate | Never read `transferSize`/`encodedBodySize`; emit no byte field. Cross-origin sizes are 0 anyway (no TAO) — any byte would be fiction. |
| T-04-06 | Denial of Service | per-resource row explosion during HLS playback | mitigate | Aggregate per (flush-window, host) — one row per host, not per segment (mirrors Phase 2 per-(session,host) aggregation). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Gate is a no-op). |
</threat_model>

<verification>
- `cd frontend/web && bunx vitest run src/analytics/ && bunx tsc --noEmit` green.
- `grep -nE "transferSize|bytes_(in|out)" frontend/web/src/analytics/rum.ts` empty (byte-poverty).
- RUM observer wired into `index.ts` init().
</verification>

<success_criteria>
- rum.ts beacons per-host approximate fe_rum rows through the existing Transport with host-only targets and zero byte fields.
- types.ts carries the register wire fields (snake_case, optional, no bytes).
- rum.spec.ts proves aggregation, self-host drop, host-only target, and byte-poverty.
</success_criteria>

<output>
Create `.planning/phases/04-fe-causation-rum/04-02-SUMMARY.md` when done.

**Plan metrics (no time units):** `UXΔ = 0 (Ambiguous)` (silent instrumentation, no rendered surface) · `CDI = 0.04 * 8` (one new module + a wire-type extension + a co-located spec, contained to the FE analytics package, reuses Transport) · `MVQ = Griffin 85%/85%` (the host-only + byte-poverty + per-host aggregation discipline is the crafted, slop-resistant core of AR-FE-03).
</output>
