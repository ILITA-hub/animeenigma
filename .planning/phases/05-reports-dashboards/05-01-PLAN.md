---
phase: 05-reports-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/grafana/dashboards/activity-register-pivot.json
  - infra/grafana/dashboards/activity-register-flow.json
autonomous: true
requirements: [AR-REPORT-01, AR-REPORT-02]
must_haves:
  truths:
    - "An admin can switch the $group_by template var and the pivot table regroups live by ANY dimension (origin, operation, effect_kind, target_kind, target, source)"
    - "$effect_kind / $origin / $target dropdowns self-discover live values via SELECT DISTINCT (no hardcoded enum lists)"
    - "The pivot's byte measure uses sumIf(..., source = 'be') so fe_rum approximate rows never contaminate authoritative byte totals (AR-FE-03)"
    - "The flow report renders origin -> operation -> per-target requests + bytes for a real origin"
    - "The flow report's byte aggregation filters source = 'be' AND effect_kind = 'egress'"
    - "Both dashboards bind every panel + var to datasource uid aenigma-clickhouse and parse with jq"
  artifacts:
    - path: "infra/grafana/dashboards/activity-register-pivot.json"
      provides: "AR-REPORT-01 wide-event pivot: $group_by custom var + SELECT DISTINCT filter vars driving a GROUP BY ${group_by} table"
      contains: "${group_by}"
    - path: "infra/grafana/dashboards/activity-register-flow.json"
      provides: "AR-REPORT-02 from->choke-point->effects table: GROUP BY origin, operation, target with requests + bytes"
      contains: "source = 'be'"
  key_links:
    - from: "activity-register-pivot.json $group_by custom var"
      to: "the table panel rawSql GROUP BY ${group_by}"
      via: "raw-column interpolation of a fixed-allowlist custom var"
      pattern: "GROUP BY ${group_by}"
    - from: "activity-register-flow.json table panel"
      to: "ClickHouse events GROUP BY origin, operation, target"
      via: "rawSql with effect_kind = 'egress' AND source = 'be'"
      pattern: "GROUP BY origin, operation, target"
---

<objective>
Author the two table-driven Activity Register reports: the wide-event PIVOT dashboard (AR-REPORT-01) whose template vars regroup the table by any dimension live, and the FROM -> CHOKE-POINT -> EFFECTS flow report (AR-REPORT-02) showing origin -> operation -> per-target requests + bytes. Pure declarative Grafana JSON over the already-provisioned `aenigma-clickhouse` datasource — no Go/Vue, no design-system machinery.

Purpose: AR-REPORT-01 (admin can pivot by ANY dimension) + AR-REPORT-02 (the choke-point breakdown). These are the two "read the data" reports; the anomaly + awareness dashboard is plan 02; the live render gate is plan 03.

Output: `infra/grafana/dashboards/activity-register-pivot.json` and `infra/grafana/dashboards/activity-register-flow.json`, both valid JSON, both file-provisioner-loadable on `make restart-grafana`.

Effort: `UXΔ = +2 (Better)` (the register becomes pivotable — the admin can finally slice "what is happening" by any axis) · `CDI = 0.04 * 13` (two new dashboard JSON files on an existing Grafana, zero code-structure disruption; the effort is getting the template-var interpolation + conditionalAll macros right) · `MVQ = Dragon 86%/84%` (the showy pivot centerpiece; slop-resistant because the SQL does the real grouping and the dropdowns self-discover rather than being panel-spam).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reports-dashboards/05-RESEARCH.md
@.planning/phases/05-reports-dashboards/05-VALIDATION.md

<interfaces>
<!-- ClickHouse events schema (services/analytics/internal/repo/clickhouse_schema.go), datasource, and the analog panel JSON. Executor uses these directly — no codebase exploration needed. -->

Datasource: { "type": "grafana-clickhouse-datasource", "uid": "aenigma-clickhouse" }, db `analytics`, table `events`.

events dimension columns (LowCardinality String unless noted):
  origin (DEFAULT 'api'), operation, effect_kind (egress|db_write|db_read|cache_hit|cache_miss),
  target_kind (host|table|key_class|provider), source (be|fe|fe_rum), target (String, HIGH cardinality).
events measure columns:
  requests (UInt32 — prefer sum(requests) for egress volume), bytes_in (UInt64), bytes_out (UInt64),
  duration_ms (UInt32), row_count (UInt32 — NOT `rows`).

Analog panel target shape (infra/grafana/dashboards/product-analytics.json:50-62):
  a target object has keys: datasource (uid aenigma-clickhouse), rawQuery:true, format ("table" for tables /
  0 for time_series), queryType "sql", refId "A", rawSql "<the SQL>".
  Dashboard top-level keys observed: annotations, editable, graphTooltip, id:null, uid, title, description,
  tags, time{from,to}, timezone, schemaVersion:39, version, panels[], (+ templating.list[] for vars).

CH plugin macros (drive every dropdown + filter — never string-concat raw input):
  $__timeFilter(timestamp) — dashboard time-range predicate.
  $__conditionalAll(dim IN (${dim:singlequote}), $dim) — 1st arg uses :singlequote, 2nd arg is a BARE $dim.
  SELECT DISTINCT <dim> FROM events WHERE <dim> != '' AND $__timeFilter(timestamp) ORDER BY <dim> — var query.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the wide-event pivot dashboard (AR-REPORT-01)</name>
  <files>infra/grafana/dashboards/activity-register-pivot.json</files>
  <read_first>
    - infra/grafana/dashboards/product-analytics.json (canonical CH panel/target JSON shape to copy: datasource uid, rawQuery/format/queryType/refId/rawSql)
    - .planning/phases/05-reports-dashboards/05-RESEARCH.md Pattern 1 (template-var pivot) + the macro gotchas
    - infra/grafana/dashboards/activity-register-pivot.json (the file being written, on re-runs)
  </read_first>
  <action>
    Create the dashboard with uid `activity-register-pivot`, title "Activity Register — Pivot", tags ["activity-register","pivot"], schemaVersion 39, time { from: "now-24h", to: "now" }, refresh "30s", and a templating.list with these vars, all bound to datasource uid aenigma-clickhouse:
    (1) `group_by` — type custom, query "origin, operation, effect_kind, target_kind, target, source", current value "operation". This is a FIXED-ALLOWLIST custom var (never a query/text var) because it is interpolated as a raw unquoted SQL column fragment.
    (2) `effect_kind` — type query, includeAll true, multi true, refresh 2, query "SELECT DISTINCT effect_kind FROM events WHERE effect_kind != '' AND $__timeFilter(timestamp) ORDER BY effect_kind".
    (3) `origin` — type query, includeAll true, multi true, refresh 2, query "SELECT DISTINCT origin FROM events WHERE origin != '' AND $__timeFilter(timestamp) ORDER BY origin".
    (4) `target` — type query, includeAll true, multi true, refresh 2, query "SELECT DISTINCT target FROM events WHERE target != '' AND $__conditionalAll(effect_kind IN (${effect_kind:singlequote}), $effect_kind) AND $__timeFilter(timestamp) ORDER BY target LIMIT 200" — scoped by $effect_kind + time-bounded so this high-cardinality dropdown stays usable.
    One table panel (type "table", full width) whose target rawSql selects `${group_by} AS dimension, sum(requests) AS requests, sumIf(bytes_out + bytes_in, source = 'be') AS bytes_be, round(avg(duration_ms), 1) AS avg_ms` FROM events WHERE $__timeFilter(timestamp) with three `$__conditionalAll(... IN (${var:singlequote}), $var)` guards for effect_kind, origin, and target, GROUP BY dimension ORDER BY requests DESC LIMIT 100. Set the bytes_be field unit to "bytes" via a fieldConfig override. Add a second stat/timeseries panel "Requests over time" with format 0, a `time` column first (toStartOfHour(timestamp)), sum(requests), same conditionalAll guards, GROUP BY time ORDER BY time — so the pivot also shows volume trend. Use the exact conditionalAll arg convention: 1st arg `dim IN (${dim:singlequote})`, 2nd arg a BARE `$dim` (no :singlequote).
    Do NOT hardcode any effect_kind/target enum in panel SQL — the dropdowns self-discover. Do NOT reuse the `product-analytics` uid.
  </action>
  <acceptance_criteria>
    - `jq empty infra/grafana/dashboards/activity-register-pivot.json` exits 0 (valid JSON).
    - `jq -r '.uid' infra/grafana/dashboards/activity-register-pivot.json` == "activity-register-pivot" (no collision with product-analytics).
    - `grep -q 'GROUP BY dimension' infra/grafana/dashboards/activity-register-pivot.json` and the table rawSql contains `${group_by}` (raw-column pivot wired).
    - `grep -q "sumIf(bytes_out + bytes_in, source = 'be')" infra/grafana/dashboards/activity-register-pivot.json` (byte measure is source='be'-filtered — AR-FE-03).
    - `grep -c 'SELECT DISTINCT' infra/grafana/dashboards/activity-register-pivot.json` >= 3 (effect_kind/origin/target dropdowns self-discover).
    - `grep -q '"uid": "aenigma-clickhouse"' infra/grafana/dashboards/activity-register-pivot.json` (CH datasource bound).
    - `jq -e '.templating.list[] | select(.name=="group_by" and .type=="custom")' infra/grafana/dashboards/activity-register-pivot.json` is non-empty (group_by is a custom allowlist var, not a query/text var — SQL-injection guard).
  </acceptance_criteria>
  <verify>
    <automated>jq empty infra/grafana/dashboards/activity-register-pivot.json && grep -q "${group_by}" infra/grafana/dashboards/activity-register-pivot.json && grep -q "sumIf(bytes_out + bytes_in, source = 'be')" infra/grafana/dashboards/activity-register-pivot.json && [ "$(grep -c 'SELECT DISTINCT' infra/grafana/dashboards/activity-register-pivot.json)" -ge 3 ]</automated>
  </verify>
  <done>Valid pivot dashboard JSON exists with a custom $group_by allowlist var, >=3 SELECT-DISTINCT dropdowns, a GROUP BY ${group_by} table whose byte measure filters source='be', all bound to aenigma-clickhouse.</done>
</task>

<task type="auto">
  <name>Task 2: Author the from->choke-point->effects flow report (AR-REPORT-02)</name>
  <files>infra/grafana/dashboards/activity-register-flow.json</files>
  <read_first>
    - infra/grafana/dashboards/product-analytics.json (table panel + target JSON shape)
    - .planning/phases/05-reports-dashboards/05-RESEARCH.md Pattern 2 (from->choke-point->effects) + the Sankey deferral note in Open Question 1
    - infra/grafana/dashboards/activity-register-flow.json (the file being written, on re-runs)
  </read_first>
  <action>
    Create the dashboard with uid `activity-register-flow`, title "Activity Register — Flow (from → choke-point → effects)", tags ["activity-register","flow"], schemaVersion 39, time { from: "now-24h", to: "now" }, refresh "30s". Add an $origin query var (SELECT DISTINCT origin ... includeAll, multi) so the admin can scope to one origin.
    One table panel (type "table", full width) whose target rawSql selects `origin, operation, target AS dependency, sum(requests) AS requests, sum(bytes_out + bytes_in) AS bytes` FROM events WHERE $__timeFilter(timestamp) AND effect_kind = 'egress' AND source = 'be' AND $__conditionalAll(origin IN (${origin:singlequote}), $origin) GROUP BY origin, operation, target ORDER BY bytes DESC LIMIT 200. Set the `bytes` field unit to "bytes" (fieldConfig override).
    Apply Grafana's "Group by" / row-grouping transformation on `origin` then `operation` (transformation id "groupBy" with origin+operation as grouping fields and requests/bytes as aggregated/sum fields, OR an "organize"/nested-table presentation) so the table renders the from -> choke-point -> per-target DRILL shape rather than a flat list.
    DEFERRED (note explicitly in the dashboard description AND in the SUMMARY): a literal Sankey/node-graph visual flow is intentionally NOT shipped — it needs a community panel plugin (a GF_INSTALL_PLUGINS addition + container recreate = a real Wave-0 cost). The table + row-grouping transform fully satisfies AR-REPORT-02; Sankey is a noted future enhancement (research Open Question 1), not a silent scope reduction.
    Do NOT reuse an existing uid.
  </action>
  <acceptance_criteria>
    - `jq empty infra/grafana/dashboards/activity-register-flow.json` exits 0.
    - `jq -r '.uid' infra/grafana/dashboards/activity-register-flow.json` == "activity-register-flow".
    - The flow table rawSql contains `GROUP BY origin, operation, target` (the from->choke-point->effects shape).
    - The flow table rawSql contains both `effect_kind = 'egress'` and `source = 'be'` (authoritative egress bytes only — AR-FE-03).
    - `jq -e '.panels[].transformations[]? | select(.id=="groupBy")' infra/grafana/dashboards/activity-register-flow.json` is non-empty (row-grouping drill present).
    - The dashboard `.description` mentions the Sankey deferral (explicit, not silent): `grep -iq sankey infra/grafana/dashboards/activity-register-flow.json`.
    - `grep -q '"uid": "aenigma-clickhouse"' infra/grafana/dashboards/activity-register-flow.json`.
  </acceptance_criteria>
  <verify>
    <automated>jq empty infra/grafana/dashboards/activity-register-flow.json && grep -q "GROUP BY origin, operation, target" infra/grafana/dashboards/activity-register-flow.json && grep -q "effect_kind = 'egress'" infra/grafana/dashboards/activity-register-flow.json && grep -q "source = 'be'" infra/grafana/dashboards/activity-register-flow.json && grep -iq sankey infra/grafana/dashboards/activity-register-flow.json</automated>
  </verify>
  <done>Valid flow dashboard JSON exists: origin->operation->target table with requests+bytes, bytes filtered effect_kind='egress' AND source='be', a groupBy row-grouping transform for the drill shape, Sankey deferral noted in the description, bound to aenigma-clickhouse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Grafana | Admin lands as Org Admin via the gateway `/admin/grafana` auth-proxy (X-WEBAUTH-*) — already solved, no phase work. The trust crossing inside scope is the template-var values an admin selects flowing into panel rawSql. |
| Grafana → ClickHouse | Panel + var rawSql run against `aenigma-clickhouse` (db analytics, read-only query workload). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Integrity | pivot bytes_be + flow bytes measures | mitigate | Both byte aggregations explicitly carry source='be' (pivot uses sumIf(..., source='be'); flow uses WHERE source='be'); asserted in both tasks' acceptance_criteria so fe_rum approximate rows can never contaminate authoritative byte totals (AR-FE-03). |
| T-05-02 | Tampering (SQL injection) | $group_by raw-column interpolation + the filter vars | mitigate | $group_by is a FIXED-ALLOWLIST custom var (acceptance asserts type=="custom"); filter vars use the plugin's ${var:singlequote} + $__conditionalAll macros + SELECT DISTINCT enum dropdowns — never string-concatenation of raw free-text input. |
| T-05-03 | Denial of Service | high-cardinality $target dropdown + unbounded GROUP BY ${group_by} | accept | $target var query is scoped by $effect_kind + $__timeFilter + LIMIT 200; pivot/flow tables LIMIT 100/200. Read-only query workload on a columnar store with bounded result sets — residual risk low. |
</threat_model>

<verification>
- `jq empty` passes on both new dashboard JSON files.
- Pivot: $group_by is a custom allowlist var; >=3 SELECT DISTINCT dropdowns; GROUP BY ${group_by}; bytes_be uses sumIf(..., source='be').
- Flow: GROUP BY origin, operation, target; effect_kind='egress' AND source='be'; groupBy transform present; Sankey deferral noted.
- Both bound to datasource uid aenigma-clickhouse; neither reuses an existing dashboard uid.
- (Live render — switching $group_by regroups the table, real origin breakdown renders — is the plan 05-03 non-autonomous gate.)
</verification>

<success_criteria>
Both `activity-register-pivot.json` and `activity-register-flow.json` exist in `infra/grafana/dashboards/`, parse with jq, bind every panel/var to aenigma-clickhouse, drive all dropdowns from SELECT DISTINCT, filter byte measures by source='be', and (flow) render the origin->operation->target drill with a row-grouping transform + a noted Sankey deferral.
</success_criteria>

<output>
Create `.planning/phases/05-reports-dashboards/05-01-SUMMARY.md` when done. Note the Sankey deferral explicitly in the summary.
</output>
