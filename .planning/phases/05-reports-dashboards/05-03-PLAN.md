---
phase: 05-reports-dashboards
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [AR-REPORT-01, AR-REPORT-02, AR-REPORT-03, AR-REPORT-04]
must_haves:
  truths:
    - "After make restart-grafana, the file provisioner loads all three new dashboards + the new alert rule with no provisioning errors in the Grafana logs"
    - "AR-REPORT-01: switching the $group_by template var on the pivot dashboard regroups the table live by the chosen dimension"
    - "AR-REPORT-02: the flow dashboard renders a real origin's origin->operation->per-target requests+bytes breakdown"
    - "AR-REPORT-03: a synthetic volume spike injected into ClickHouse is flagged is_anomaly in the overview panel AND transitions the activity-register-volume-anomaly alert"
    - "AR-REPORT-04: the awareness overview shows current top operations + top external dependencies + active anomalies in one view"
    - "Every panel's rawSql dry-runs against ClickHouse without error (no column-name typos, no fe_rum byte contamination)"
  artifacts:
    - path: ".planning/phases/05-reports-dashboards/05-03-SUMMARY.md"
      provides: "Live-gate verification record for the four reports + the anomaly alert transition"
      contains: "AR-REPORT-03"
  key_links:
    - from: "make restart-grafana"
      to: "Grafana file provisioner loading dashboards-infra + the alerting provider"
      via: "config reload (no rebuild)"
      pattern: "restart-grafana"
    - from: "synthetic ClickHouse spike (synthetic.SpikeOp)"
      to: "the overview anomaly panel is_anomaly flag + the provisioned alert"
      via: "INSERT INTO analytics.events ... egress rows above the operation's baseline"
      pattern: "synthetic.SpikeOp"
---

<objective>
Prove the four Activity Register reports end-to-end against the live stack: reload Grafana config, dry-run every panel's SQL against ClickHouse, open all four reports, switch the pivot's $group_by, read a real flow breakdown, inject a synthetic volume spike, and confirm the anomaly panel flags it + the provisioned alert transitions. This is the phase-gate plan — declarative JSON has no unit-test surface, so the proof is a non-autonomous live render gate (mirroring 02-04 / 03-06 / 04-04). No new files are authored; this plan instantiates and verifies plans 05-01 + 05-02.

Purpose: AR-REPORT-01..04 end-to-end. The dashboards + alert shipped in 05-01/05-02; this plan confirms they provision cleanly, query real register data, and that the anomaly path actually fires on a controlled spike.

Output: a verification record `05-03-SUMMARY.md` confirming the four success criteria + the alert transition.

Effort: `UXΔ = +1 (Better)` (the reports go live and the awareness loop is proven) · `CDI = 0.03 * 8` (no code authored — restart, dry-run SQL, inject a spike, eyeball four dashboards; the effort is the disciplined end-to-end verification) · `MVQ = Griffin 84%/82%` (the proving flight — guards the whole phase; slop-resistant because it injects a real spike and watches a real alert transition rather than trusting the JSON).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reports-dashboards/05-RESEARCH.md
@.planning/phases/05-reports-dashboards/05-VALIDATION.md
@.planning/phases/05-reports-dashboards/05-01-PLAN.md
@.planning/phases/05-reports-dashboards/05-02-PLAN.md

<interfaces>
<!-- Reload + dry-run + spike-injection recipes. -->

Reload (config-only, no rebuild): make restart-grafana
Grafana local container: 127.0.0.1:3004 ; prod path-routed at /admin/grafana.
Provisioning-log check: docker logs animeenigma-grafana 2>&1 | grep -iE 'provisioning|error' | tail
ClickHouse client (dry-run a panel's rawSql, substituting $__timeFilter(timestamp) with a literal e.g. timestamp >= now()-INTERVAL 24 HOUR):
  docker compose -f docker/docker-compose.yml exec -T clickhouse clickhouse-client -d analytics -q "<SQL>"
Synthetic spike (AR-REPORT-03 gate — direct CH insert, isolated):
  INSERT INTO analytics.events (timestamp, origin, operation, effect_kind, target, target_kind, source, requests)
  SELECT now(), 'api', 'synthetic.SpikeOp', 'egress', 'spike.example.com', 'host', 'be', 1 FROM numbers(5000)
  -> 'synthetic.SpikeOp' has a zero trailing baseline, so last_req(5000) > avg+3σ ⇒ is_anomaly = 1.
New dashboards to open (uids): activity-register-pivot, activity-register-flow, activity-register-overview.
New alert uid: activity-register-volume-anomaly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reload Grafana + dry-run every panel SQL against ClickHouse</name>
  <files><!-- read-only inputs, no writes: infra/grafana/dashboards/activity-register-pivot.json, infra/grafana/dashboards/activity-register-flow.json, infra/grafana/dashboards/activity-register-overview.json, docker/grafana/provisioning/alerting/rules.yml --></files>
  <read_first>
    - .planning/phases/05-reports-dashboards/05-VALIDATION.md (the jq + provision-load + SQL-dry-run proof model)
    - infra/grafana/dashboards/activity-register-pivot.json, activity-register-flow.json, activity-register-overview.json (extract each panel's rawSql to dry-run)
  </read_first>
  <action>
    First `jq empty` all three new dashboard JSON files and `python3 -c "import yaml; yaml.safe_load(...)"` the edited rules.yml (cheap pre-flight). Then `make restart-grafana` and scan `docker logs animeenigma-grafana` for provisioning errors / confirm a "finished to provision dashboards" (or equivalent) and no datasource/uid-collision warnings. Then extract each panel's rawSql from the three dashboards and dry-run them against ClickHouse via `clickhouse-client -d analytics`, substituting the `$__timeFilter(timestamp)` macro with a literal `timestamp >= now() - INTERVAL 24 HOUR` and the `$__conditionalAll(...)` guards with `1` (the "All" case) and `${group_by}` with a concrete column like `operation`. Confirm every query returns without a column-name/type error — this catches `rows` vs `row_count` typos, missing columns, and macro mistakes before the human gate.
  </action>
  <acceptance_criteria>
    - All three dashboards pass `jq empty`; rules.yml passes YAML safe_load.
    - `docker logs animeenigma-grafana 2>&1 | grep -iE 'error|fail' | grep -i provision` returns no NEW errors referencing activity-register-* (clean provision load).
    - Each panel's rawSql (macros substituted) runs against `clickhouse-client -d analytics` and returns a result set with no `Unknown identifier`/`Missing column` error — in particular every byte panel's query succeeds with its `source = 'be'` filter and no query references a bare `rows` column.
    - The anomaly query returns rows with an `is_anomaly` column.
  </acceptance_criteria>
  <verify>
    <automated>for f in infra/grafana/dashboards/activity-register-pivot.json infra/grafana/dashboards/activity-register-flow.json infra/grafana/dashboards/activity-register-overview.json; do jq empty "$f" || exit 1; done && python3 -c "import yaml; yaml.safe_load(open('docker/grafana/provisioning/alerting/rules.yml'))"</automated>
  </verify>
  <done>Grafana reloaded with no provisioning errors for the activity-register-* dashboards/rule; every panel's rawSql dry-runs cleanly against ClickHouse (no bad-column/macro errors), confirming the JSON queries real register data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live phase gate — open all four reports + inject a synthetic spike</name>
  <what-built>
    Three new Grafana dashboards (Activity Register — Pivot / Flow / Awareness Overview) over the ClickHouse `events` register plus a provisioned volume-anomaly alert. Task 1 already reloaded Grafana and confirmed every panel's SQL dry-runs against ClickHouse. This checkpoint confirms the four phase success criteria end-to-end against live data, including a controlled anomaly.
  </what-built>
  <how-to-verify>
    Open Grafana (local 127.0.0.1:3004 or prod /admin/grafana) and:
    1. AR-REPORT-01 — Open "Activity Register — Pivot". Switch the $group_by dropdown between e.g. `operation`, `origin`, `effect_kind`, `target` and confirm the table REGROUPS live each time. Confirm the $effect_kind/$origin/$target dropdowns show real self-discovered values (not a hardcoded list). Confirm the bytes column shows authoritative (source='be') totals.
    2. AR-REPORT-02 — Open "Activity Register — Flow". Confirm it renders a real origin's origin->operation->per-target requests+bytes drill (row-grouped). Confirm bytes are egress + source='be'.
    3. AR-REPORT-04 — Open "Activity Register — Awareness Overview". Confirm all three regions populate: top operations, top external dependencies, active anomalies.
    4. AR-REPORT-03 — Inject the synthetic spike:
       `docker compose -f docker/docker-compose.yml exec -T clickhouse clickhouse-client -d analytics -q "INSERT INTO analytics.events (timestamp, origin, operation, effect_kind, target, target_kind, source, requests) SELECT now(), 'api', 'synthetic.SpikeOp', 'egress', 'spike.example.com', 'host', 'be', 1 FROM numbers(5000)"`
       Refresh the overview's "Active volume anomalies" panel — confirm `synthetic.SpikeOp` appears flagged is_anomaly. Then check Alerting → confirm the `Activity Register Volume Anomaly` rule transitions to Pending/Firing within ~5m (its `for:`). (Cleanup is optional — the synthetic row ages out of the 24h window; mention if you delete it.)
  </how-to-verify>
  <resume-signal>Type "approved" once: $group_by regroups the pivot live, the flow drill renders a real origin breakdown, the overview shows all three regions, AND the injected synthetic.SpikeOp is flagged is_anomaly + the volume-anomaly alert transitions — or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ClickHouse | The synthetic-spike INSERT writes 5000 controlled `be`/egress rows into analytics.events for the gate. |
| Grafana → ClickHouse | All panels + the alert query read-only. |
| Grafana alerting → notifier | The volume-anomaly rule fires on the injected spike. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering / Integrity | the synthetic spike pollutes real register data | accept | The spike is a clearly-named `synthetic.SpikeOp` / `spike.example.com` operation that ages out of the 24h anomaly window; it is `source='be'` egress so it is filterable/identifiable and does not corrupt other operations' baselines. Optional explicit cleanup noted in the gate. |
| T-05-09 | Integrity | a byte panel silently summed fe_rum (caught only live) | mitigate | Task 1 dry-runs every byte panel's rawSql and the human gate reads the authoritative totals; the source='be' filter was asserted structurally in 05-01/05-02 acceptance criteria. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | None — this phase installs no packages (plugin pre-installed in Phase 1); nothing to slopcheck. |
</threat_model>

<verification>
- Grafana reloads cleanly (no activity-register-* provisioning errors); every panel rawSql dry-runs against ClickHouse.
- Human gate confirms all four success criteria live: $group_by regroups (01), real origin flow drill (02), synthetic spike flagged + alert transitions (03), overview three-region awareness (04).
- After approval, set `nyquist_compliant: true` in 05-VALIDATION.md.
</verification>

<success_criteria>
All four AR-REPORT requirements verified live in one Grafana session: the pivot regroups on $group_by, the flow report shows a real origin->operation->target breakdown, the injected synthetic.SpikeOp is flagged is_anomaly and the provisioned alert transitions, and the awareness overview shows top ops + top deps + active anomalies in one view.
</success_criteria>

<output>
Create `.planning/phases/05-reports-dashboards/05-03-SUMMARY.md` when done, recording the live-gate result + the anomaly alert transition. Then run `/animeenigma-after-update` (changelog entry is user-facing Russian Trump-mode even though the feature is admin-only observability).
</output>
