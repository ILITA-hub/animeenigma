---
phase: 06-consolidation-topology-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/otel/collector-config.yaml
  - docker/docker-compose.yml
autonomous: true
requirements: [AR-CONS-01, AR-CONS-02, AR-CONS-03]
user_setup: []

must_haves:
  truths:
    - "OTLP traces land in ClickHouse analytics.otel_traces alongside the still-live Tempo path"
    - "Container logs land in ClickHouse analytics.otel_logs via the OTel filelog receiver (message body unwrapped, trace_id attribute present)"
    - "Per-operation span-metrics + service-graph metrics reach Prometheus from the OTel Collector connectors (independent of Tempo)"
    - "The collector config passes otelcol validate before recreate"
    - "Tempo/Loki/Promtail remain running and unchanged during this additive wave (gated cutover)"
  artifacts:
    - path: "infra/otel/collector-config.yaml"
      provides: "ClickHouse traces+logs exporters, filelog receiver, spanmetrics+servicegraph connectors, prometheusremotewrite exporter, dual traces pipeline (clickhouse + tempo)"
      contains: "clickhouse"
    - path: "docker/docker-compose.yml"
      provides: "otel-collector CH creds env + docker containers log mount + depends_on clickhouse/prometheus"
      contains: "CLICKHOUSE_USER"
  key_links:
    - from: "infra/otel/collector-config.yaml"
      to: "clickhouse:9000 analytics.otel_traces / analytics.otel_logs"
      via: "clickhouse exporter (two named instances for distinct TTLs)"
      pattern: "tcp://clickhouse:9000"
    - from: "infra/otel/collector-config.yaml"
      to: "http://prometheus:9090/prometheus/api/v1/write"
      via: "prometheusremotewrite exporter fed by spanmetrics+servicegraph connectors"
      pattern: "prometheus:9090/prometheus/api/v1/write"
---

<objective>
Stand up the NEW ClickHouse trace + log export path and the OTel-Collector-native span-metrics generation ALONGSIDE the existing Tempo/Loki/Promtail stack — purely additive, nothing removed. This is the reversible front half of the gated cutover: prove traces+logs flow into ClickHouse and span-metrics still reach Prometheus before any destructive change (which happens in 06-03).

Purpose: De-risk the consolidation. By exporting to ClickHouse in parallel with Tempo, and moving span-metrics/service-graph generation off Tempo onto the collector's connectors, the destructive removal wave can be a pure deletion with the new path already proven live.

Output: A reconfigured OTel Collector (mounted config + compose env/mounts/depends_on) that dual-exports traces (clickhouse + tempo), ships container logs to ClickHouse via filelog, and remote-writes spanmetrics+servicegraph to Prometheus. No app code, no datasource/dashboard changes (those land in 06-02).

Effort: `UXΔ = 0 (Ambiguous)` (purely additive backend plumbing — no user-visible change this wave; the benefit lands at cutover) · `CDI = 0.03 * 8` (one mounted collector config + compose env/mounts on an existing service, fully reversible since the old path stays live) · `MVQ = Sprite 84%/82%` (small, quiet, exactly-scoped de-risking move — slop-resistant because it dual-exports in parallel rather than a risky big-bang swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-consolidation-topology-a/06-RESEARCH.md
@.planning/phases/06-consolidation-topology-a/06-CONTEXT.md

<interfaces>
<!-- Exact current state of the files being modified. Executor edits these in place. -->

Current OTel Collector config (infra/otel/collector-config.yaml):
- receivers.otlp.protocols.grpc :4317 / http :4318
- processors: memory_limiter, tail_sampling (errors+slow+20% probabilistic), batch
- exporters: otlp/tempo -> tempo:4317 (insecure)
- service.pipelines.traces: receivers [otlp] processors [memory_limiter, tail_sampling, batch] exporters [otlp/tempo]
- service.telemetry.logs.level: warn

Current otel-collector compose service (docker/docker-compose.yml:359-371):
- image otel/opentelemetry-collector-contrib:0.103.1
- command ["--config=/etc/otel/collector-config.yaml"]
- volumes: ../infra/otel/collector-config.yaml -> /etc/otel/collector-config.yaml:ro
- ports 127.0.0.1:4317, 127.0.0.1:4318
- depends_on: tempo (service_healthy)
- HAS NO ClickHouse creds in env today

ClickHouse service (compose:312-336): db `analytics`, user ${CLICKHOUSE_USER:-analytics}, password ${CLICKHOUSE_PASSWORD:-changeme}, native port reachable in-network at clickhouse:9000 (host-published 127.0.0.1:9100->9000), HTTP 127.0.0.1:8123.

Prometheus (compose:215-239): --web.enable-remote-write-receiver ON, --web.route-prefix=/prometheus. Remote-write endpoint = http://prometheus:9090/prometheus/api/v1/write.

Tempo metrics_generator today (infra/tempo/tempo.yaml:46-68): span_metrics dimensions [service.name, span.name, operation]; service_graphs dimensions [service.name]; remote_write http://prometheus:9090/prometheus/api/v1/write with send_exemplars:true; external_labels source: tempo. DO NOT edit tempo.yaml in this plan — Tempo keeps running; its generator and the new collector connector co-emit during the gate. 06-03 deletes tempo.yaml.

Promtail source today (docker/promtail/config.yml): docker_sd via /var/run/docker.sock, host mount /var/lib/docker/containers:ro, pushes to loki:3100. The filelog receiver must mirror this source (json-file logs under /var/lib/docker/containers). Logger emits trace_id (libs/logger/logger.go:92).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ClickHouse traces+logs exporters, filelog receiver, and spanmetrics/servicegraph connectors to the collector config (additive — keep Tempo)</name>
  <files>infra/otel/collector-config.yaml</files>
  <read_first>
    - infra/otel/collector-config.yaml (the file being edited — current single traces pipeline to otlp/tempo)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Pattern 1 config keys + Pitfall 2/3/4/5 + Assumptions A1/A5)
    - infra/tempo/tempo.yaml (span_metrics/service_graphs dimensions to mirror in the connectors; remote_write endpoint format)
    - docker/promtail/config.yml (the log source filelog must mirror)
  </read_first>
  <action>
    Edit infra/otel/collector-config.yaml in place (config-only, no app code):
    1. Add a `filelog` receiver with include glob /var/lib/docker/containers/*/*-json.log and operators: a `json_parser` then a `move` from attributes.log to body (unwrap docker json-file envelope per Pitfall 2). Set `start_at: end` so it does not replay old logs on first start.
    2. Add a `connectors:` section with `spanmetrics` (dimensions service.name, span.name, operation — preserve the operation Phase-3 RED dimension per tempo.yaml:59) and `servicegraph` (dimensions service.name).
    3. Add TWO named ClickHouse exporter instances under exporters for distinct TTLs (Pitfall 3): `clickhouse/traces` (endpoint tcp://clickhouse:9000?dial_timeout=10s, database analytics, username ${env:CLICKHOUSE_USER}, password ${env:CLICKHOUSE_PASSWORD}, create_schema true, traces_table_name otel_traces, ttl 336h) and `clickhouse/logs` (same conn, logs_table_name otel_logs, ttl 168h). Use the collector ${env:VAR} syntax (NOT bare ${VAR}).
    4. Add a `prometheusremotewrite` exporter with endpoint http://prometheus:9090/prometheus/api/v1/write (MUST carry the /prometheus route-prefix per Pitfall 4) and external_labels source: otelcol so its series are distinguishable from Tempo's source: tempo during the co-emit gate.
    5. Wire pipelines under service.pipelines: traces receivers [otlp] processors [memory_limiter, tail_sampling, batch] exporters [otlp/tempo, clickhouse/traces, spanmetrics, servicegraph] (Tempo STAYS in the list during the gate); add logs pipeline receivers [filelog] processors [memory_limiter, batch] exporters [clickhouse/logs]; add metrics/spanmetrics pipeline receivers [spanmetrics, servicegraph] exporters [prometheusremotewrite].
    Do NOT remove otlp/tempo. Do NOT touch tempo.yaml. Exact 0.103.1 key spellings are validated by otelcol validate in Task 3 (Assumption A1) before recreate.
  </action>
  <verify>
    <automated>grep -q 'clickhouse/traces' infra/otel/collector-config.yaml && grep -q 'clickhouse/logs' infra/otel/collector-config.yaml && grep -q 'filelog' infra/otel/collector-config.yaml && grep -q 'spanmetrics' infra/otel/collector-config.yaml && grep -q 'servicegraph' infra/otel/collector-config.yaml && grep -q 'prometheus:9090/prometheus/api/v1/write' infra/otel/collector-config.yaml && grep -q 'otlp/tempo' infra/otel/collector-config.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - clickhouse/traces and clickhouse/logs exporters both present, with ttl 336h and 168h respectively, using ${env:CLICKHOUSE_USER}/${env:CLICKHOUSE_PASSWORD}.
    - filelog receiver present with include glob /var/lib/docker/containers/*/*-json.log and a json_parser + move(attributes.log -> body) operator chain.
    - spanmetrics connector dimensions include operation; servicegraph connector present.
    - prometheusremotewrite.endpoint contains the /prometheus/api/v1/write route-prefix.
    - traces pipeline exporters list still contains otlp/tempo (Tempo not removed).
    - New logs and metrics/spanmetrics pipelines present.
  </acceptance_criteria>
  <done>Collector config dual-exports traces (tempo + clickhouse), ships filelog->clickhouse logs, and remote-writes connector spanmetrics+servicegraph to Prometheus; Tempo path intact.</done>
</task>

<task type="auto">
  <name>Task 2: Inject ClickHouse creds + docker-log mount + depends_on into the otel-collector compose service</name>
  <files>docker/docker-compose.yml</files>
  <read_first>
    - docker/docker-compose.yml (otel-collector service block lines ~359-371; clickhouse block ~312-336 for cred names; grafana env ~431-432 shows the established CLICKHOUSE_USER/PASSWORD compose-env pattern with :- defaults)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Runtime State Inventory — Secrets/env vars row: collector has no CH creds today; Pattern 1 gotcha re ${env:VAR})
    - docker/promtail/config.yml (the /var/lib/docker/containers mount to mirror)
  </read_first>
  <action>
    Edit the otel-collector service in docker/docker-compose.yml in place:
    1. Add an environment block exporting CLICKHOUSE_USER: ${CLICKHOUSE_USER:-analytics} and CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-changeme} — mirror the grafana service pattern (compose:431-432). The collector ${env:VAR} reads these from container env.
    2. Add a read-only host mount /var/lib/docker/containers:/var/lib/docker/containers:ro to the collector volumes so the filelog receiver can read docker json-file logs (mirrors promtail's mount). Do NOT add the docker.sock mount — filelog reads files, not the socket.
    3. Update depends_on to add clickhouse (condition service_healthy) and prometheus (condition service_healthy). KEEP tempo (condition service_healthy) during the gate (06-03 removes it).
    Do NOT publish any new host port. Do NOT change the ClickHouse 127.0.0.1 port bindings.
  </action>
  <verify>
    <automated>docker compose -f docker/docker-compose.yml config >/dev/null && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 (valid compose).
    - otel-collector service environment contains CLICKHOUSE_USER and CLICKHOUSE_PASSWORD with :- defaults.
    - otel-collector volumes include /var/lib/docker/containers:/var/lib/docker/containers:ro.
    - otel-collector depends_on lists clickhouse, prometheus, AND tempo (all service_healthy).
    - No new host-published port added to otel-collector.
  </acceptance_criteria>
  <done>otel-collector has CH creds in env, the docker-log mount for filelog, and depends_on clickhouse+prometheus+tempo; compose validates.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the new config, recreate the collector, and prove traces+logs+span-metrics flow live</name>
  <files>infra/otel/collector-config.yaml, docker/docker-compose.yml</files>
  <read_first>
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Pitfall 5 otelcol validate; Code Examples — verify traces/logs land in ClickHouse + verify span-metrics survive; Pitfall 4 route-prefix; A5 filelog path)
    - infra/otel/collector-config.yaml (the validated config)
    - docker/docker-compose.yml (recreate target)
  </read_first>
  <action>
    1. PRE-RECREATE GUARD: validate the config WITHOUT touching the running collector — `docker run --rm -v "$PWD/infra/otel/collector-config.yaml":/c/collector-config.yaml otel/opentelemetry-collector-contrib:0.103.1 validate --config /c/collector-config.yaml`. It MUST exit 0. If it fails, fix key spellings (Assumption A1) before proceeding — do NOT recreate a broken config (Pitfall 5 = all traces stop).
    2. Recreate ONLY the collector (config + compose env/mount/depends change need a recreate, not restart): `docker compose -f docker/docker-compose.yml up -d --no-deps --force-recreate otel-collector`. Then confirm it is not restart-looping: `docker compose -f docker/docker-compose.yml ps otel-collector` shows running/healthy and `docker logs animeenigma-otel-collector --tail 50` shows no fatal config error.
    3. Generate live signal: hit a real endpoint that produces a backend trace (e.g. `curl -s http://localhost:8000/api/anime/_/scraper/health` or any catalog route) so OTLP traces flow; wait > batch timeout (~10s).
    4. Verify traces in ClickHouse: `curl -s 'http://localhost:8123/?user=analytics&password=changeme' --data 'SELECT count() FROM analytics.otel_traces'` returns a count > 0.
    5. Verify logs in ClickHouse (and that the body is the message, NOT the wrapped {"log":...} envelope per Pitfall 2): `curl -s 'http://localhost:8123/?user=analytics&password=changeme' --data "SELECT count(), any(Body) FROM analytics.otel_logs WHERE Timestamp > now() - INTERVAL 5 MINUTE"` returns count > 0 with a plain message body.
    6. Verify span-metrics reach Prometheus from the collector connector: `curl -s 'http://localhost:9090/prometheus/api/v1/query?query=traces_spanmetrics_calls_total' | jq '.data.result | length'` returns > 0 (Pitfall 4 — confirms the route-prefix is correct).
    Do NOT remove Tempo/Loki/Promtail — they keep running. If any check fails, the config is reversible (revert collector-config.yaml + recreate) since the old Tempo path is untouched.
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD/infra/otel/collector-config.yaml":/c/collector-config.yaml otel/opentelemetry-collector-contrib:0.103.1 validate --config /c/collector-config.yaml && echo VALIDATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - otelcol validate exits 0 BEFORE any recreate.
    - `docker compose ps otel-collector` shows the collector running (not restart-looping); logs show no fatal config error.
    - analytics.otel_traces count > 0 after generating live traffic.
    - analytics.otel_logs has recent rows (last 5 min) with a plain-message Body (not the docker json envelope).
    - traces_spanmetrics_calls_total returns >= 1 result via the /prometheus route-prefixed query.
    - Tempo/Loki/Promtail containers still running (gate intact).
  </acceptance_criteria>
  <done>New ClickHouse trace+log path and collector span-metrics are proven live alongside the still-running Tempo/Loki stack; the front half of the gated cutover is reversible and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services -> otel-collector (OTLP :4317/:4318) | Internal Docker-network only; unchanged |
| otel-collector -> clickhouse:9000 | NEW internal edge; CH creds from container env (from docker/.env), no plaintext in committed files |
| otel-collector -> prometheus /prometheus/api/v1/write | NEW remote-write edge; Docker-network only, receiver already enabled |
| filelog -> /var/lib/docker/containers (host mount, ro) | Same data Promtail already reads; no new exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | otel-collector config typo halts ALL traces | mitigate | `otelcol validate` exits 0 BEFORE recreate (Task 3 step 1); gate keeps Tempo path so the change is revertible |
| T-06-02 | Information Disclosure | CH creds added to collector env | mitigate | Use ${CLICKHOUSE_USER}/${CLICKHOUSE_PASSWORD} from docker/.env via compose :- defaults — no plaintext secret committed; mirrors existing grafana/clickhouse pattern |
| T-06-03 | Information Disclosure | new exposed ClickHouse/collector port | accept | No new host port published; CH stays 127.0.0.1-bound; collector keeps existing 4317/4318 only |
| T-06-04 | Information Disclosure | filelog ingests container stdout into a now-queryable store | accept | Same data Promtail already shipped to Loki — no NEW PII captured; existing posture (RESEARCH V7) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — config/provisioning only (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- otelcol validate exits 0 on the new config before recreate.
- After collector recreate: analytics.otel_traces and analytics.otel_logs both receive rows; traces_spanmetrics_calls_total present in Prometheus via the /prometheus prefix.
- Tempo/Loki/Promtail still running (additive wave — nothing removed).
- `docker compose config` valid; no new published host ports.
</verification>

<success_criteria>
- ClickHouse trace+log export and collector-native span-metrics are live ALONGSIDE Tempo/Loki/Promtail.
- The new path is proven (live CH queries + Prometheus span-metric query) and fully reversible.
- AR-CONS-01/02 export half satisfied (storage proven); AR-CONS-03 span-metrics survival path proven independent of Tempo.
</success_criteria>

<output>
Create `.planning/phases/06-consolidation-topology-a/06-01-SUMMARY.md` when done
</output>
