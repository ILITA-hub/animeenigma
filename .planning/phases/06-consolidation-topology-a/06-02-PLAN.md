---
phase: 06-consolidation-topology-a
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docker/grafana/provisioning/datasources/datasources.yml
  - infra/grafana/dashboards/backend-tracing.json
autonomous: true
requirements: [AR-CONS-01, AR-CONS-02]
user_setup: []

must_haves:
  truths:
    - "Grafana has a ClickHouse trace datasource (OTel mode) and a ClickHouse log datasource (OTel mode) reachable and healthy"
    - "The backend-tracing dashboard is repointed from the Tempo datasource to the ClickHouse trace datasource and renders recent slow traces"
    - "Logs↔traces correlation is configured natively in the ClickHouse datasource (joined on trace_id), replacing the Loki derivedField→Tempo link"
    - "Tempo and Loki datasources/containers remain present during this repoint wave (gated cutover — removed only in 06-03)"
  artifacts:
    - path: "docker/grafana/provisioning/datasources/datasources.yml"
      provides: "ClickHouse trace+log datasource config (OTel table mappings) on the existing aenigma-clickhouse datasource or a fresh-uid datasource"
      contains: "otel"
    - path: "infra/grafana/dashboards/backend-tracing.json"
      provides: "trace panel repointed to the ClickHouse trace datasource with a CH-plugin trace query (no traceqlSearch)"
      contains: "grafana-clickhouse-datasource"
  key_links:
    - from: "infra/grafana/dashboards/backend-tracing.json"
      to: "ClickHouse trace datasource (analytics.otel_traces)"
      via: "panel datasource + CH plugin trace query"
      pattern: "grafana-clickhouse-datasource"
    - from: "datasources.yml ClickHouse trace/log datasource"
      to: "analytics.otel_traces / analytics.otel_logs"
      via: "OTel trace/log builder (otelEnabled)"
      pattern: "otel"
---

<objective>
Repoint Grafana's trace + log views from Tempo/Loki to ClickHouse, and PROVE they render — while Tempo and Loki containers/datasources are still present (gated cutover back half setup; destructive removal is 06-03). This makes the new ClickHouse-backed trace search, trace view, and log view the live surface BEFORE the old stores are deleted.

Purpose: Satisfy the "repointed dashboard/log views still render" half of AR-CONS-01/02 and prove it operationally + visually BEFORE 06-03 removes the safety net.

Output: ClickHouse trace+log datasource config (OTel mode) with native logs↔traces linking, and a repointed backend-tracing.json whose trace panel queries ClickHouse via the CH plugin (no TraceQL). Parity gaps documented honestly (no TraceQL/LogQL — SQL/builder equivalents).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-consolidation-topology-a/06-RESEARCH.md
@.planning/phases/06-consolidation-topology-a/06-01-SUMMARY.md

<interfaces>
<!-- Exact current state of the files being modified. -->

datasources.yml today (docker/grafana/provisioning/datasources/datasources.yml):
- Prometheus uid PBFA97CFB590B2093 (MUST stay — orphaned 14 alert rules when renamed)
- Loki uid aenigma-loki, derivedFields TraceID -> datasourceUid aenigma-tempo (the Loki->Tempo correlation being replaced)
- PostgreSQL uid aenigma-postgres
- Tempo uid aenigma-tempo, tracesToLogsV2 -> aenigma-loki, serviceMap/tracesToMetrics -> PBFA97CFB590B2093
- ClickHouse uid aenigma-clickhouse, type grafana-clickhouse-datasource, host clickhouse port 9000 protocol native, defaultDatabase analytics, username ${CLICKHOUSE_USER}, password ${CLICKHOUSE_PASSWORD} (creds supplied to grafana container env). This is the EVENTS datasource the product-analytics + activity-register dashboards bind to — extend it in place (preferred, lowest churn) OR add a fresh-uid trace/log datasource. NEVER reuse aenigma-postgres / aenigma-tempo / aenigma-loki / PBFA97CFB590B2093.

backend-tracing.json today (infra/grafana/dashboards/backend-tracing.json):
- uid backend-tracing; single panel type "traces", title "Recent slow traces (>1s)"
- datasource { type: tempo, uid: aenigma-tempo }
- target { queryType: traceqlSearch, query: "{ duration > 1s }", limit: 50 }  <- MUST be rewritten to the CH plugin trace query (no TraceQL)

Grafana CH plugin: grafana-clickhouse-datasource installed via GF_INSTALL_PLUGINS; Grafana 10.3.3 ships plugin v4.x with the OTel trace builder + trace-ID search + logs↔traces linking. ClickHouse tables analytics.otel_traces / analytics.otel_logs created by the exporter in 06-01.

Production Grafana URL form: https://animeenigma.ru/admin/grafana (GF_SERVER_SERVE_FROM_SUB_PATH=true). Local host port 127.0.0.1:3004.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ClickHouse trace + log datasource config (OTel mode) with native logs↔traces linking</name>
  <files>docker/grafana/provisioning/datasources/datasources.yml</files>
  <read_first>
    - docker/grafana/provisioning/datasources/datasources.yml (the file — note the uid-reuse warning header and the existing aenigma-clickhouse block)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Pattern 2 datasource config + Anti-Patterns "Reusing an existing datasource uid" + Feature-Parity Gaps "trace->logs correlation")
    - .planning/phases/06-consolidation-topology-a/06-01-SUMMARY.md (confirm otel_traces/otel_logs tables exist + their column layout)
  </read_first>
  <action>
    Edit docker/grafana/provisioning/datasources/datasources.yml in place. Preferred (lowest-churn): extend the EXISTING aenigma-clickhouse datasource by adding a `traces` block (defaultDatabase analytics, defaultTable otel_traces, otelEnabled true, otelVersion latest) and a `logs` block (defaultDatabase analytics, defaultTable otel_logs, otelEnabled true) under its jsonData — so the one CH datasource serves events + traces + logs. If a single datasource cannot carry all three table mappings cleanly with the v4.x plugin, instead add a SEPARATE datasource with a FRESH uid `aenigma-clickhouse-traces` (and/or `aenigma-clickhouse-logs`) of type grafana-clickhouse-datasource — NEVER reuse PBFA97CFB590B2093 / aenigma-postgres / aenigma-tempo / aenigma-loki (the orphaned-14-alerts hazard). Keep username ${CLICKHOUSE_USER} / secureJsonData.password ${CLICKHOUSE_PASSWORD} (bare ${VAR} — Grafana provisioning does NOT support :- defaults; the creds come from the grafana container env). Set editable: false. Configure the CH datasource's logs↔traces linking (the plugin's trace-to-logs / logs-to-trace correlation on trace_id) so it replaces the Loki derivedField→Tempo correlation. DO NOT remove the Loki or Tempo datasource blocks yet — they stay through the gate (06-03 removes them).
  </action>
  <verify>
    <automated>docker compose -f docker/docker-compose.yml restart grafana >/dev/null 2>&1 || true; sleep 8; grep -q 'otel' docker/grafana/provisioning/datasources/datasources.yml && grep -q 'otel_traces' docker/grafana/provisioning/datasources/datasources.yml && grep -q 'otel_logs' docker/grafana/provisioning/datasources/datasources.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - datasources.yml contains otel-mode trace mapping (defaultTable otel_traces, otelEnabled true) and log mapping (defaultTable otel_logs, otelEnabled true).
    - No reuse of PBFA97CFB590B2093 / aenigma-postgres / aenigma-tempo / aenigma-loki for the new trace/log source.
    - Loki and Tempo datasource blocks STILL present (gate intact).
    - CH datasource credentials use bare ${CLICKHOUSE_USER}/${CLICKHOUSE_PASSWORD}; editable false.
    - Grafana datasource health: query the Grafana datasource health API (admin SSO/form login) for the CH trace/log datasource returns 200 / "OK" — capture in the summary. (Manual visual confirm is in Task 3.)
  </acceptance_criteria>
  <done>Grafana provisions a ClickHouse trace + log datasource in OTel mode with native trace_id correlation; Tempo/Loki datasources untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint backend-tracing.json to the ClickHouse trace datasource and rewrite the TraceQL query to the CH plugin trace query</name>
  <files>infra/grafana/dashboards/backend-tracing.json</files>
  <read_first>
    - infra/grafana/dashboards/backend-tracing.json (the single traces panel bound to {type:tempo, uid:aenigma-tempo} with queryType traceqlSearch)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Component Responsibilities row for backend-tracing.json + Feature-Parity Gaps "TraceQL" row + Assumption A2)
    - docker/grafana/provisioning/datasources/datasources.yml (the CH trace datasource uid/name created in Task 1 — bind to that exact uid)
  </read_first>
  <action>
    Edit infra/grafana/dashboards/backend-tracing.json in place:
    1. Change the panel `datasource` from { type: tempo, uid: aenigma-tempo } to the ClickHouse trace datasource of type grafana-clickhouse-datasource with the exact uid configured in Task 1 (aenigma-clickhouse or aenigma-clickhouse-traces).
    2. Replace the target: drop queryType traceqlSearch / query "{ duration > 1s }". Express the "recent slow traces (>1s)" use case via the CH plugin's trace query (OTel trace builder / queryType=traces) — duration filter equivalent is Duration > 1e9 (1s in ns) over analytics.otel_traces ordered by Timestamp desc, limit 50 (Feature-Parity Gaps: no TraceQL; SQL/builder equivalent per Assumption A2 — verify it renders in Task 3).
    3. Update the dashboard `description` to note the datastore moved from Tempo to ClickHouse and that TraceQL is replaced by the CH builder/SQL (honest parity note). Keep uid backend-tracing.
    Do NOT delete the dashboard or change its uid (it is provisioned by uid).
  </action>
  <verify>
    <automated>grep -q 'grafana-clickhouse-datasource' infra/grafana/dashboards/backend-tracing.json && ! grep -q 'traceqlSearch' infra/grafana/dashboards/backend-tracing.json && ! grep -q '"type": "tempo"' infra/grafana/dashboards/backend-tracing.json && python3 -c "import json;json.load(open('infra/grafana/dashboards/backend-tracing.json'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Panel datasource type is grafana-clickhouse-datasource bound to the Task-1 CH trace uid; no `"type": "tempo"` / `aenigma-tempo` remains in the file.
    - No `traceqlSearch` / TraceQL query remains; the trace query uses the CH plugin trace builder/SQL with a >1s duration filter.
    - File is valid JSON; uid stays backend-tracing.
    - Description honestly notes the Tempo→ClickHouse move and TraceQL→builder/SQL parity.
  </acceptance_criteria>
  <done>backend-tracing.json queries ClickHouse via the CH plugin; the slow-trace use case is preserved without TraceQL.</done>
</task>

<task type="auto">
  <name>Task 3: Reload Grafana and prove the repointed trace + log views render from ClickHouse (operational + visual)</name>
  <files>docker/grafana/provisioning/datasources/datasources.yml, infra/grafana/dashboards/backend-tracing.json</files>
  <read_first>
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Validation Architecture map AR-CONS-01/02 rows; Code Examples; Feature-Parity Gaps)
    - .planning/phases/06-consolidation-topology-a/06-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    1. Reload Grafana provisioning so the new datasource + repointed dashboard load: `docker compose -f docker/docker-compose.yml restart grafana` (mounted-file provisioning change = restart is sufficient, no recreate). Wait for healthy: `docker compose ps grafana`.
    2. Operational — datasource health: call the Grafana datasource health endpoint for the CH trace/log datasource (via admin form login admin/${GRAFANA_ADMIN_PASSWORD} at http://localhost:3004 or the auth-proxy SSO) and confirm it returns "OK"/200.
    3. Operational — data present: re-confirm analytics.otel_traces and analytics.otel_logs have recent rows (reuse the 06-01 CH HTTP queries) so the panels have something to render.
    4. Operational — dashboard datasource binding: confirm via Grafana API that dashboard uid backend-tracing resolves its panel datasource to the CH trace uid (GET /admin/grafana/api/dashboards/uid/backend-tracing).
    5. VISUAL (DS-NF-06 standing rule, manual but performed by the executor in a browser): open https://animeenigma.ru/admin/grafana dashboard uid backend-tracing — run the slow-trace query, confirm spans/trace rows display from ClickHouse; open a log view bound to the CH log datasource and confirm recent logs display with trace_id present and the logs↔traces link works. Record the visual outcome (pass / what rendered) in the summary.
    Do NOT remove Tempo/Loki yet — they stay running so this is fully reversible.
  </action>
  <verify>
    <automated>curl -s 'http://localhost:8123/?user=analytics&password=changeme' --data 'SELECT count() FROM analytics.otel_traces' | awk '{exit ($1>0)?0:1}' && curl -s 'http://localhost:8123/?user=analytics&password=changeme' --data "SELECT count() FROM analytics.otel_logs WHERE Timestamp > now() - INTERVAL 10 MINUTE" | awk '{exit ($1>0)?0:1}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Grafana restarts healthy and loads the new provisioning without error (check `docker logs animeenigma-grafana` for provisioning errors).
    - CH trace + log datasource health returns OK/200.
    - analytics.otel_traces > 0 and recent analytics.otel_logs > 0 (data to render).
    - backend-tracing dashboard's panel datasource resolves to the CH trace uid via the Grafana API.
    - VISUAL: backend-tracing renders slow traces from ClickHouse AND a CH log view renders recent logs with trace_id linking — recorded in the summary (manual per DS-NF-06).
    - Tempo/Loki containers still running (gate intact).
  </acceptance_criteria>
  <done>The repointed trace + log views render from ClickHouse and are verified operationally + visually, while Tempo/Loki remain as the still-removable safety net.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana -> ClickHouse (trace/log datasource) | Internal Docker-network; creds from grafana container env (docker/.env), provisioned read-only |
| admin browser -> Grafana (/admin/grafana) | Auth-proxy SSO (X-WEBAUTH from gateway) + form-login fallback; unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | New datasource as a X-WEBAUTH bypass vector | accept | No change to Grafana auth-proxy/whitelist; datasource is provisioned editable:false (read-only) |
| T-06-06 | Tampering | Reusing an orphaned datasource uid breaks 14 alert rules | mitigate | Extend aenigma-clickhouse in place OR use a fresh uid; NEVER reuse PBFA97CFB590B2093/aenigma-postgres/tempo/loki (RESEARCH Anti-Pattern) |
| T-06-07 | Information Disclosure | CH creds in datasources.yml | mitigate | Bare ${CLICKHOUSE_USER}/${CLICKHOUSE_PASSWORD} from grafana container env — no plaintext secret committed (existing aenigma-clickhouse pattern) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — provisioning/dashboard JSON only (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- CH trace + log datasource provisioned (OTel mode), health OK; no reused/orphaned uid.
- backend-tracing.json bound to the CH datasource, no TraceQL; valid JSON.
- Live: otel_traces + recent otel_logs have rows; dashboard + log view render from ClickHouse (visual, DS-NF-06).
- Tempo/Loki datasources + containers still present (gated — removed in 06-03).
</verification>

<success_criteria>
- AR-CONS-01 repoint half: backend-tracing dashboard renders traces from ClickHouse.
- AR-CONS-02 repoint half: log views render from ClickHouse with trace_id correlation.
- The repointed surface is proven BEFORE any destructive removal; parity gaps (no TraceQL/LogQL) documented honestly.
</success_criteria>

<output>
Create `.planning/phases/06-consolidation-topology-a/06-02-SUMMARY.md` when done
</output>
