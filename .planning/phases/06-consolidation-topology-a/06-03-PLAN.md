---
phase: 06-consolidation-topology-a
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - infra/otel/collector-config.yaml
  - docker/docker-compose.yml
  - docker/grafana/provisioning/datasources/datasources.yml
  - infra/tempo/tempo.yaml
  - docker/loki/loki-config.yml
  - docker/promtail/config.yml
autonomous: false
requirements: [AR-CONS-01, AR-CONS-02, AR-CONS-03]
user_setup: []

must_haves:
  truths:
    - "Tempo, tempo-init, Loki, and Promtail are removed from docker-compose and no longer run"
    - "The Tempo and Loki Grafana datasources (and the Loki derivedField correlation) are removed; no datasource binds to aenigma-tempo or aenigma-loki"
    - "The collector traces pipeline no longer exports to otlp/tempo; span-metrics/service-graph still reach Prometheus from the collector connectors (sole writer now)"
    - "Grafana starts cleanly with no depends_on: loki and no dangling Tempo/Loki references"
    - "Prometheus alerts still fire (no error state) and existing metric dashboards still render after cutover (AR-CONS-03)"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "tempo/tempo-init/loki/promtail services deleted; grafana depends_on: loki removed; otel-collector tempo dep removed"
      contains: "otel-collector"
    - path: "infra/otel/collector-config.yaml"
      provides: "traces pipeline with otlp/tempo exporter removed (clickhouse + connectors only)"
      contains: "clickhouse/traces"
    - path: "docker/grafana/provisioning/datasources/datasources.yml"
      provides: "Tempo + Loki datasource blocks removed; CH trace/log datasource remains"
      contains: "ClickHouse"
  key_links:
    - from: "docker/docker-compose.yml grafana service"
      to: "(removed) loki dependency"
      via: "depends_on edge deleted so compose starts post-cutover"
      pattern: "grafana"
    - from: "infra/otel/collector-config.yaml traces pipeline"
      to: "clickhouse/traces only (no otlp/tempo)"
      via: "exporter list edit"
      pattern: "clickhouse/traces"
---

<objective>
Execute the DESTRUCTIVE back half of the gated cutover: remove Tempo, tempo-init, Loki, and Promtail (containers, configs, datasources, depends_on edges), drop the collector's otlp/tempo export so the collector connectors become the sole span-metrics writer, and run the full final verification proving AR-CONS-01/02/03 hold on the consolidated topology. This wave runs ONLY after 06-02 proved the ClickHouse trace/log views render — it is the irreversible step, gated behind a human render-confirmation checkpoint.

Purpose: Complete Topology A — ClickHouse is the single event/trace/log plane; Prometheus + Grafana remain the metrics/alerting/rendering layer, untouched and verified.

Output: A consolidated compose stack (Tempo/Loki/Promtail gone), a Tempo-free collector traces pipeline, Tempo/Loki-free datasources, and a green final verification (live CH traces/logs, span-metrics in Prometheus, alerts firing, metric dashboards rendering).

Effort: `UXΔ = +1 (Better)` (lower ops surface + a single query plane; user benefit is indirect via reliability) · `CDI = 0.10 * 13` (the irreversible deletion of three stateful services + their datasources/configs/depends_on edges — concentrated risk, mitigated by the human render gate as Task 1 and the proven-live new path from 06-01/02) · `MVQ = Phoenix 84%/86%` (rises-from-ashes consolidation — three stores collapse into one; slop-resistant because deletion is gated behind a blocking human confirmation and a full AR-CONS-01/02/03 re-verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-consolidation-topology-a/06-RESEARCH.md
@.planning/phases/06-consolidation-topology-a/06-01-SUMMARY.md
@.planning/phases/06-consolidation-topology-a/06-02-SUMMARY.md
@.planning/phases/06-consolidation-topology-a/06-VALIDATION.md

<interfaces>
<!-- Exact current state of the targets. -->

docker/docker-compose.yml services to DELETE (with their depends_on edges):
- loki (compose:241-255), promtail (compose:257-268, depends_on loki), tempo-init (compose:270-283), tempo (compose:285-302, depends_on tempo-init)
- grafana depends_on: loki (compose:452-453) MUST be removed or compose won't start (RESEARCH Pitfall 6)
- otel-collector depends_on: tempo (compose:369-371) MUST be removed (Tempo gone)

infra/otel/collector-config.yaml (post-06-01): traces pipeline exporters = [otlp/tempo, clickhouse/traces, spanmetrics, servicegraph]. REMOVE otlp/tempo from the list AND delete the otlp/tempo exporter definition. The clickhouse/traces + connectors stay; the prometheusremotewrite connector path becomes the SOLE span-metrics writer (Tempo's generator is gone).

datasources.yml: REMOVE the Tempo block (uid aenigma-tempo, compose:46-74) and the Loki block (uid aenigma-loki, including its derivedFields -> aenigma-tempo correlation, lines 17-29). KEEP Prometheus (PBFA97CFB590B2093), PostgreSQL (aenigma-postgres), and the ClickHouse datasource(s) from 06-02.

Files to DELETE (no longer mounted after the services are gone): infra/tempo/tempo.yaml, docker/loki/loki-config.yml, docker/promtail/config.yml.

Verification commands (RESEARCH Code Examples + Validation Architecture):
- traces: SELECT count() FROM analytics.otel_traces  (CH HTTP 127.0.0.1:8123, user analytics)
- logs: SELECT count() FROM analytics.otel_logs WHERE Timestamp > now() - INTERVAL 5 MINUTE
- span-metrics: GET http://localhost:9090/prometheus/api/v1/query?query=traces_spanmetrics_calls_total
- alerts: GET http://localhost:9090/prometheus/api/v1/rules  (rule states must be inactive/firing, NOT error)
- production admin forms: https://animeenigma.ru/admin/prometheus/... , https://animeenigma.ru/admin/grafana/...

AR-CONS-03 NOTE (RESEARCH Pitfall 1, VERIFIED): no alert rule and no dashboard panel queries traces_spanmetrics_* / traces_service_graph_* — they appear ONLY in the (now-removed) Tempo datasource convenience config. Alerts query app /metrics (up, scheduler_*, provider_health_up, playability_canary_*, http_request_duration_seconds, ...). DO NOT touch rules.yml. Retiring Tempo cannot break a firing alert; verify cheaply.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: GATE — confirm the ClickHouse trace + log views render BEFORE deleting Tempo/Loki</name>
  <action>BLOCKING human-verify checkpoint — pause execution and do NOT proceed to any destructive task (2/3/4) until the human approves. Present the render-confirmation steps below and wait for the resume signal. This is the gated/reversible cutover boundary: everything before this is additive and revertible; everything after deletes Tempo/Loki/Promtail.</action>
  <what-built>06-01 stood up ClickHouse trace+log export + collector span-metrics; 06-02 repointed the backend-tracing dashboard + log views to ClickHouse and verified they render. This checkpoint is the point-of-no-return gate: once Tempo/Loki/Promtail are deleted (next tasks), trace/log history in those stores is gone (short-TTL debugging data, acceptable per RESEARCH Runtime State Inventory).</what-built>
  <how-to-verify>
    1. Open https://animeenigma.ru/admin/grafana dashboard uid `backend-tracing` — confirm "Recent slow traces (>1s)" renders trace rows/spans FROM ClickHouse (not Tempo).
    2. Open the ClickHouse-backed log view — confirm recent logs display with `trace_id` present and the logs↔traces link works.
    3. Confirm span-metrics are live in Prometheus: https://animeenigma.ru/admin/prometheus/api/v1/query?query=traces_spanmetrics_calls_total returns results.
    4. Confirm you accept losing Tempo/Loki historical trace/log blocks (short-TTL debug data).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms backend-tracing renders from ClickHouse, the CH log view renders with trace_id linking, and span-metrics are present in Prometheus.
    - Human explicitly approves proceeding with the irreversible Tempo/Loki/Promtail removal.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with Tempo/Loki/Promtail removal, or describe what failed to render.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Drop otlp/tempo from the collector + remove Tempo/Loki/Promtail/tempo-init services and depends_on edges from compose</name>
  <files>infra/otel/collector-config.yaml, docker/docker-compose.yml</files>
  <read_first>
    - infra/otel/collector-config.yaml (traces pipeline exporters list + otlp/tempo exporter definition to remove)
    - docker/docker-compose.yml (loki/promtail/tempo-init/tempo service blocks + grafana depends_on:loki + otel-collector depends_on:tempo)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Component Responsibilities table + Pitfall 6 grafana depends_on)
  </read_first>
  <action>
    1. Edit infra/otel/collector-config.yaml: remove `otlp/tempo` from the traces pipeline exporters list (leaving [clickhouse/traces, spanmetrics, servicegraph]) AND delete the `otlp/tempo` exporter definition under `exporters:`. The clickhouse/traces + spanmetrics + servicegraph + prometheusremotewrite stay (collector is now the SOLE span-metrics writer).
    2. Edit docker/docker-compose.yml: DELETE the entire `loki:`, `promtail:`, `tempo-init:`, and `tempo:` service blocks. Remove the `depends_on: loki` edge from the grafana service (Pitfall 6 — else compose won't start). Remove the `depends_on: tempo` edge from the otel-collector service (keep clickhouse + prometheus deps from 06-01).
    Do NOT touch the prometheus service, rules.yml, or the metric dashboards (AR-CONS-03 — unchanged). Do NOT delete docker volumes yet (handled in Task 4 cleanup, optional).
  </action>
  <verify>
    <automated>! grep -q 'otlp/tempo' infra/otel/collector-config.yaml && ! grep -qE '^\s*(loki|promtail|tempo|tempo-init):' docker/docker-compose.yml && docker compose -f docker/docker-compose.yml config >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - otlp/tempo removed from collector traces pipeline AND its exporter definition deleted; clickhouse/traces + connectors remain.
    - loki, promtail, tempo, tempo-init service blocks deleted from compose.
    - grafana service has no depends_on: loki; otel-collector has no depends_on: tempo.
    - `docker compose config` exits 0 (no dangling depends_on references).
  </acceptance_criteria>
  <done>Compose + collector reference no Tempo/Loki/Promtail; the collector is the sole span-metrics writer; compose validates.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Tempo + Loki datasources (and the Loki→Tempo derivedField) and delete the orphaned tempo/loki/promtail config files</name>
  <files>docker/grafana/provisioning/datasources/datasources.yml, infra/tempo/tempo.yaml, docker/loki/loki-config.yml, docker/promtail/config.yml</files>
  <read_first>
    - docker/grafana/provisioning/datasources/datasources.yml (Tempo block uid aenigma-tempo + Loki block uid aenigma-loki with derivedFields)
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Component Responsibilities — datasources.yml row; files-to-delete-at-cutover rows)
  </read_first>
  <action>
    1. Edit docker/grafana/provisioning/datasources/datasources.yml: DELETE the Tempo datasource block (uid aenigma-tempo) and the Loki datasource block (uid aenigma-loki, including its derivedFields → aenigma-tempo correlation). KEEP Prometheus (PBFA97CFB590B2093), PostgreSQL (aenigma-postgres), and the ClickHouse trace/log datasource(s) from 06-02.
    2. Delete the now-unmounted config files: `git rm infra/tempo/tempo.yaml docker/loki/loki-config.yml docker/promtail/config.yml` (these are no longer referenced by any service).
    Confirm no remaining reference to aenigma-tempo / aenigma-loki anywhere in provisioning or dashboards (grep).
  </action>
  <verify>
    <automated>! grep -q 'aenigma-tempo' docker/grafana/provisioning/datasources/datasources.yml && ! grep -q 'aenigma-loki' docker/grafana/provisioning/datasources/datasources.yml && ! grep -rq 'aenigma-tempo\|aenigma-loki' infra/grafana/dashboards/ && [ ! -f infra/tempo/tempo.yaml ] && [ ! -f docker/loki/loki-config.yml ] && [ ! -f docker/promtail/config.yml ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Tempo + Loki datasource blocks removed; no aenigma-tempo / aenigma-loki reference remains in datasources.yml or any dashboard JSON.
    - Prometheus + PostgreSQL + ClickHouse datasources retained.
    - infra/tempo/tempo.yaml, docker/loki/loki-config.yml, docker/promtail/config.yml deleted.
  </acceptance_criteria>
  <done>Grafana provisioning is Tempo/Loki-free; orphaned tempo/loki/promtail configs removed.</done>
</task>

<task type="auto">
  <name>Task 4: Apply the cutover (recreate collector, remove containers, restart grafana) and run final AR-CONS-01/02/03 verification</name>
  <files>docker/docker-compose.yml, infra/otel/collector-config.yaml</files>
  <read_first>
    - .planning/phases/06-consolidation-topology-a/06-RESEARCH.md (Pitfall 5 validate-before-recreate; Code Examples; Validation Architecture map; Pitfall 1 alert survival)
    - .planning/phases/06-consolidation-topology-a/06-VALIDATION.md (Per-Task Verification Map Wave 3 rows; Manual-Only Verifications)
  </read_first>
  <action>
    1. PRE-RECREATE GUARD: `docker run --rm -v "$PWD/infra/otel/collector-config.yaml":/c/collector-config.yaml otel/opentelemetry-collector-contrib:0.103.1 validate --config /c/collector-config.yaml` MUST exit 0 (Tempo-free config).
    2. Recreate the collector with the Tempo-free config: `docker compose -f docker/docker-compose.yml up -d --no-deps --force-recreate otel-collector`; confirm running/not restart-looping.
    3. Tear down the retired services: `docker compose -f docker/docker-compose.yml up -d --remove-orphans` (removes the deleted loki/promtail/tempo/tempo-init containers) and restart grafana to drop the Tempo/Loki datasources: `docker compose -f docker/docker-compose.yml restart grafana`. Confirm `docker compose ps` shows NO tempo, tempo-init, loki, or promtail.
    4. AR-CONS-01: analytics.otel_traces count > 0 after live traffic; `docker compose ps` has no tempo; datasources.yml has no aenigma-tempo; backend-tracing dashboard still renders (re-confirm via API / quick visual).
    5. AR-CONS-02: analytics.otel_logs has rows in the last 5 min; no loki/promtail containers; CH log view renders.
    6. AR-CONS-03 (the critical regression gate): `GET http://localhost:9090/prometheus/api/v1/rules` — every rule group state is inactive/firing, NONE in `error`; `traces_spanmetrics_calls_total` still present (now from the collector connector); open the metric dashboards (animeenigma-services "Services / Overview", playback-health, product-analytics, activity-register-*) and confirm panels render. DO NOT edit rules.yml.
    7. Optional cleanup (only after all above green, not on the critical path): `docker volume rm animeenigma_tempo_data animeenigma_loki_data` and `mc rb local/tempo` to reclaim space (verify volume names with `docker volume ls` first).
    Record every check result in the summary.
  </action>
  <verify>
    <automated>docker compose -f docker/docker-compose.yml ps --services 2>/dev/null | grep -vqE '^(tempo|tempo-init|loki|promtail)$'; docker compose -f docker/docker-compose.yml ps 2>/dev/null | grep -qE 'tempo|loki|promtail' && { echo "RETIRED SERVICE STILL RUNNING"; exit 1; }; curl -s 'http://localhost:9090/prometheus/api/v1/rules' | grep -q '"state":"error"' && { echo "ALERT RULE IN ERROR"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - otelcol validate exits 0 on the Tempo-free config before recreate; collector recreated and not restart-looping.
    - `docker compose ps` shows NO tempo, tempo-init, loki, or promtail container.
    - AR-CONS-01: otel_traces > 0; no aenigma-tempo datasource; backend-tracing renders from ClickHouse.
    - AR-CONS-02: recent otel_logs > 0; no loki/promtail; CH log view renders.
    - AR-CONS-03: /prometheus/api/v1/rules has NO rule in `error` state (all inactive/firing); traces_spanmetrics_calls_total present; metric dashboards (animeenigma-services, playback-health, product-analytics, activity-register-*) render. rules.yml untouched.
  </acceptance_criteria>
  <done>Topology A is live: ClickHouse is the single event/trace/log plane, Tempo/Loki/Promtail are gone, and Prometheus + Grafana metrics/alerting/rendering are verified intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services -> otel-collector -> clickhouse / prometheus | Internal Docker-network; Tempo edge removed |
| Grafana -> ClickHouse + Prometheus | Only remaining query datastores; Tempo/Loki datasources removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service | Tempo-free collector config typo halts all traces at cutover | mitigate | `otelcol validate` exits 0 before recreate (Task 4 step 1); render gate (Task 1) already proved the CH path |
| T-06-09 | Denial of Service | grafana fails to start on dangling depends_on: loki | mitigate | depends_on:loki removed in the SAME change as the Loki service deletion (Task 2); compose config validated |
| T-06-10 | Availability | ClickHouse becomes the single SPOF for events+traces+logs | accept | ROADMAP CDI-noted accepted tradeoff; clickhouse-backup sidecar exists (Phase 1); Prometheus alerting independent of CH |
| T-06-11 | Information Disclosure | removing Loki/Tempo opens a CH query port externally | mitigate | No port changes — CH stays 127.0.0.1-bound; service deletions remove ports, never add (RESEARCH V4) |
| T-06-12 | Tampering | accidental edit to rules.yml / metric dashboards breaks alerting (AR-CONS-03) | mitigate | Explicit constraint: DO NOT touch rules.yml or metric dashboards; final check asserts no rule in `error` state |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — config/provisioning removal only (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Human render gate passed BEFORE any deletion (Task 1).
- otelcol validate 0 on Tempo-free config; collector recreated cleanly.
- `docker compose ps`: no tempo/tempo-init/loki/promtail.
- AR-CONS-01: otel_traces > 0, no aenigma-tempo, backend-tracing renders.
- AR-CONS-02: recent otel_logs > 0, no loki/promtail, CH log view renders.
- AR-CONS-03: no alert rule in error; span-metrics present; metric dashboards render; rules.yml untouched.
</verification>

<success_criteria>
- AR-CONS-01: traces in ClickHouse; Tempo container + datasource removed; backend-tracing renders from CH.
- AR-CONS-02: logs in ClickHouse; Loki + Promtail removed; log views render from CH.
- AR-CONS-03: Prometheus + Grafana unchanged — alerts still firing, metric dashboards still rendering.
- Topology A consolidation complete and verified on the live production stack.
</success_criteria>

<output>
Create `.planning/phases/06-consolidation-topology-a/06-03-SUMMARY.md` when done
</output>
