---
phase: 07-pool-foundation-config-migration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - services/library/migrations/006_autocache_config.sql
  - services/library/migrations/migrations.go
  - services/library/cmd/library-api/main.go
  - services/library/internal/domain/autocache_config.go
  - services/library/internal/repo/autocache_config.go
  - services/library/internal/handler/autocache_config.go
  - services/library/internal/transport/router.go
autonomous: true
requirements: [POOL-04, POOL-05]
must_haves:
  truths:
    - "An admin can GET the autocache config and receive all nine tunables plus enabled, enveloped as {success,data}"
    - "An admin can PATCH any single field and the new value persists with no redeploy"
    - "The master enabled switch is stored and readable through a typed accessor for later phases to consume"
    - "The singleton config row is seeded with the spec §3.5 defaults at startup"
  artifacts:
    - path: "services/library/migrations/006_autocache_config.sql"
      provides: "autocache_config singleton table + seeded defaults"
      contains: "CREATE TABLE IF NOT EXISTS autocache_config"
    - path: "services/library/internal/domain/autocache_config.go"
      provides: "AutocacheConfig GORM model (singleton, id=1)"
      contains: "func (AutocacheConfig) TableName"
    - path: "services/library/internal/repo/autocache_config.go"
      provides: "Get + Patch typed accessor over the singleton row"
      contains: "func NewAutocacheConfigRepository"
    - path: "services/library/internal/handler/autocache_config.go"
      provides: "GET/PATCH admin handler over the config store interface"
      contains: "AutocacheConfigStore"
  key_links:
    - from: "services/library/internal/transport/router.go"
      to: "services/library/internal/handler/autocache_config.go"
      via: "GET/PATCH /api/library/autocache/config routes"
      pattern: "autocache/config"
    - from: "services/library/cmd/library-api/main.go"
      to: "services/library/migrations/migrations.go"
      via: "db.DB.Exec(migrations.AutocacheConfigSQL)"
      pattern: "AutocacheConfigSQL"
---

<objective>
Add the DB-backed, admin-editable autocache config: a singleton `autocache_config` table
(migration 006) seeded with the spec §3.5 defaults, a mirrored GORM model, a typed
Get/Patch repository accessor, and a gateway-routed GET/PATCH admin handler at
`/api/library/autocache/config`. The master `enabled` field is plumbed through the
accessor so the future downloader/evictor (Phases 8-10) reads it — but this phase
implements NO downloading or eviction behavior.

Purpose: POOL-04 (live-editable config, no redeploy) + POOL-05 (master enabled switch
stored + readable). Output: migration 006, AutocacheConfig model/repo/handler, router wiring.

Route decision (resolves PATTERNS.md path discrepancy, lines 377-383): mount under
`/api/library/autocache/config`. The gateway already proxies ALL `/api/library/*` to the
library service behind JWT + AdminRoleMiddleware via a `/*` wildcard
(gateway router.go:563-571) — so NO gateway change is needed and NO in-service auth is added.
The spec's `/api/admin/library/...` is treated as nominal (CONTEXT lists exact path as
Claude's discretion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pool-foundation-config-migration/07-PATTERNS.md
@docs/superpowers/specs/2026-06-17-auto-torrent-population-design.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Config field defaults (spec §3.5, CONTEXT lines 44-48):
  enabled=true, budget_bytes=107374182400 (100 GiB), auto_fresh_download_days=10,
  auto_fresh_fetch_days=3, admin_fresh_days=30, active_watcher_days=30,
  quality_cap=1080, min_seeders=3, sweep_interval_min=20.

Repo error-wrap convention (services/library/internal/repo/episode.go):
  liberrors "github.com/ILITA-hub/animeenigma/libs/errors"
  gorm.ErrRecordNotFound → liberrors.NotFound("...")
  else → liberrors.Wrap(err, liberrors.CodeInternal, "...")
  struct{ db *gorm.DB }; New…Repository(db) ctor; r.db.WithContext(ctx)

Read-handler shape (services/library/internal/handler/episodes.go):
  interface seam (e.g. EpisodeStoreReader) so tests inject a stub without Postgres;
  New…Handler(...) ctor; httputil.OK(w, payload) / httputil.Error(w, err) / httputil.BadRequest(w, msg).
  httputil.OK wraps {success,data} — the catalog library/client.go envelope decodes exactly this.

Router group (services/library/internal/transport/router.go:57-85):
  r.Route("/api/library", func(r chi.Router) { … nil-guarded handler wiring … })
  Admin gate is at the gateway; NO server-side auth here.

main.go DI + migration apply (services/library/cmd/library-api/main.go):
  migration applies at :116-131 (flat db.DB.Exec sequence);
  repo construction e.g. episodeRepo := repo.NewEpisodeRepository(db.DB) at :181;
  handlers passed into transport.NewRouter(...).

Gateway (services/gateway/internal/transport/router.go:563-571):
  r.Route("/library", …) → r.HandleFunc("/*", proxyHandler.ProxyToLibrary) behind
  JWTValidationMiddleware + AdminRoleMiddleware. The /* wildcard already covers
  /api/library/autocache/config — confirmed, no gateway edit required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 006 — autocache_config singleton table + seeded defaults</name>
  <read_first>
    - services/library/migrations/001_library_jobs.sql (CREATE TABLE IF NOT EXISTS shape)
    - services/library/migrations/003_library_filename_patterns.sql (idempotent seed via INSERT … ON CONFLICT DO NOTHING)
    - docs/superpowers/specs/2026-06-17-auto-torrent-population-design.md §3.5 (defaults)
  </read_first>
  <files>services/library/migrations/006_autocache_config.sql, services/library/migrations/migrations.go, services/library/cmd/library-api/main.go</files>
  <action>
    Create `006_autocache_config.sql`. Use `CREATE TABLE IF NOT EXISTS autocache_config` with a
    singleton-guard PK: `id INT PRIMARY KEY DEFAULT 1` plus `CONSTRAINT autocache_config_singleton CHECK (id = 1)`.
    Columns with NOT NULL DEFAULT mapping the §3.5 defaults: `enabled BOOLEAN NOT NULL DEFAULT true`,
    `budget_bytes BIGINT NOT NULL DEFAULT 107374182400`, `auto_fresh_download_days INT NOT NULL DEFAULT 10`,
    `auto_fresh_fetch_days INT NOT NULL DEFAULT 3`, `admin_fresh_days INT NOT NULL DEFAULT 30`,
    `active_watcher_days INT NOT NULL DEFAULT 30`, `quality_cap INT NOT NULL DEFAULT 1080`,
    `min_seeders INT NOT NULL DEFAULT 3`, `sweep_interval_min INT NOT NULL DEFAULT 20`,
    `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Seed the singleton idempotently:
    `INSERT INTO autocache_config (id) VALUES (1) ON CONFLICT (id) DO NOTHING;`.
    Register it in migrations.go: `//go:embed 006_autocache_config.sql` + `var AutocacheConfigSQL string`
    after AutocachePoolSQL, and extend the apply-order doc with entry 5 (006).
    Apply it in main.go AFTER `migrations.AutocachePoolSQL` (Plan 01), with `log.Fatalw` message
    "failed to apply autocache_config migration".
  </action>
  <acceptance_criteria>
    - File `006_autocache_config.sql` contains `CREATE TABLE IF NOT EXISTS autocache_config` with the singleton CHECK constraint.
    - All nine tunable columns plus `enabled` and `updated_at` are present with the exact §3.5 defaults (budget_bytes default = 107374182400).
    - Contains `INSERT INTO autocache_config (id) VALUES (1) ON CONFLICT (id) DO NOTHING`.
    - `migrations.go` declares `AutocacheConfigSQL`; `main.go` execs it after `migrations.AutocachePoolSQL`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'CREATE TABLE IF NOT EXISTS autocache_config' services/library/migrations/006_autocache_config.sql && grep -q '107374182400' services/library/migrations/006_autocache_config.sql && grep -q 'ON CONFLICT (id) DO NOTHING' services/library/migrations/006_autocache_config.sql && grep -q 'AutocacheConfigSQL' services/library/migrations/migrations.go && grep -q 'migrations.AutocacheConfigSQL' services/library/cmd/library-api/main.go</automated>
  </verify>
  <done>Migration 006 creates and idempotently seeds the singleton config table with §3.5 defaults; registered and applied after 005.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AutocacheConfig model + Get/Patch repo accessor</name>
  <read_first>
    - services/library/internal/domain/job.go (struct + TableName() pattern)
    - services/library/internal/repo/episode.go (db field, ctor, WithContext, liberrors wrapping)
    - services/library/migrations/006_autocache_config.sql (mirror columns 1:1)
  </read_first>
  <files>services/library/internal/domain/autocache_config.go, services/library/internal/repo/autocache_config.go, services/library/internal/repo/autocache_config_test.go</files>
  <behavior>
    - AutocacheConfig.TableName() == "autocache_config".
    - Get returns the singleton row (id=1); a missing row is treated as an internal error (it is seeded by migration), not NotFound.
    - Patch with {"min_seeders": 5} updates only that column, bumps updated_at, and returns the full updated row with all other fields unchanged.
    - Patch rejects an empty field map (no-op or InvalidInput) rather than issuing an empty UPDATE.
  </behavior>
  <action>
    Create `domain/autocache_config.go` mirroring migration 006: `AutocacheConfig` struct with
    `ID int gorm:"primaryKey;column:id;default:1" json:"-"`, `Enabled bool`, `BudgetBytes int64`,
    `AutoFreshDownloadDays int`, `AutoFreshFetchDays int`, `AdminFreshDays int`, `ActiveWatcherDays int`,
    `QualityCap int`, `MinSeeders int`, `SweepIntervalMin int`, `UpdatedAt time.Time` — each with the
    snake_case `column:` tag and `not null` where the SQL has NOT NULL; JSON tags matching the snake_case
    field names. Add `func (AutocacheConfig) TableName() string { return "autocache_config" }`.
    Create `repo/autocache_config.go`: `AutocacheConfigRepository struct{ db *gorm.DB }`,
    `NewAutocacheConfigRepository(db)` ctor, `Get(ctx)` loading `Where("id = 1").First(&c)` (wrap errors
    via liberrors.CodeInternal — a missing seeded row is Internal, not NotFound), and
    `Patch(ctx, fields map[string]any) (*domain.AutocacheConfig, error)` that validates the map is non-empty,
    sets `updated_at = now()` in the same Updates() call, runs `Updates(fields)` scoped to `id = 1`,
    then re-reads and returns the row. Use the GORM partial-write Updates() pattern.
    Use a sqlmock-based or in-memory test (mirror existing library repo tests) for the test file;
    if the repo package has no existing DB-backed test harness, assert Patch's empty-map rejection and
    TableName via a lightweight unit test without a live DB.
  </action>
  <acceptance_criteria>
    - `AutocacheConfig` struct has all ten config fields + UpdatedAt with column tags mirroring 006; `json:"-"` on ID.
    - `TableName()` returns `autocache_config`.
    - `Get` scopes to `id = 1`; `Patch` rejects an empty field map and bumps `updated_at`.
    - `go build ./...` and `go test ./internal/repo/... ./internal/domain/...` pass from `services/library`.
  </acceptance_criteria>
  <verify>
    <automated>cd services/library && go build ./... && go test ./internal/repo/... ./internal/domain/... -count=1 && grep -q 'func (AutocacheConfig) TableName' internal/domain/autocache_config.go && grep -q 'func NewAutocacheConfigRepository' internal/repo/autocache_config.go && grep -q 'updated_at' internal/repo/autocache_config.go</automated>
  </verify>
  <done>Singleton model + Get/Patch accessor exist, mirror migration 006, reject empty patches, bump updated_at; tests + build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET/PATCH admin handler + router wiring + main.go DI</name>
  <read_first>
    - services/library/internal/handler/episodes.go (read-handler interface seam + httputil helpers + ctor)
    - services/library/internal/handler/jobs.go:339-343 (linkJobRequest body decode + httputil.BadRequest on error)
    - services/library/internal/transport/router.go:25-88 (NewRouter signature + /api/library route group + nil-guard wiring)
    - services/library/cmd/library-api/main.go:180-183 (repo construction) and the transport.NewRouter call site
  </read_first>
  <files>services/library/internal/handler/autocache_config.go, services/library/internal/handler/autocache_config_test.go, services/library/internal/transport/router.go, services/library/cmd/library-api/main.go</files>
  <behavior>
    - GET /api/library/autocache/config returns 200 with the {success,data} envelope wrapping all config fields.
    - PATCH /api/library/autocache/config with {"min_seeders": 5} returns 200 and the updated config; the store receives only the min_seeders key.
    - PATCH with an out-of-range value (budget_bytes <= 0, or quality_cap not a positive int) returns 400 BadRequest and writes nothing.
    - PATCH with a malformed JSON body returns 400.
  </behavior>
  <action>
    Create `handler/autocache_config.go`. Define the store interface seam
    `AutocacheConfigStore { Get(ctx) (*domain.AutocacheConfig, error); Patch(ctx, fields map[string]any) (*domain.AutocacheConfig, error) }`,
    a handler struct `{ repo AutocacheConfigStore; log *logger.Logger }`, and `NewAutocacheConfigHandler(repo, log)`.
    `Get(w,r)`: call repo.Get, on error `httputil.Error(w, err)`, else `httputil.OK(w, cfg)`.
    `Patch(w,r)`: decode the JSON body into a struct of pointer fields (each tunable as `*int`/`*int64`/`*bool`)
    so absent keys stay nil; on decode error `httputil.BadRequest`. Build a `map[string]any` from the
    non-nil pointers using the DB column names. Validate ranges before writing: reject `budget_bytes <= 0`,
    `quality_cap <= 0`, `min_seeders < 0`, and any of the *_days / sweep_interval_min `< 1` with
    `httputil.BadRequest`. Reject an empty field map (no writable keys) with BadRequest. Then call repo.Patch
    and return the updated row via `httputil.OK`.
    Wire the handler into `transport.NewRouter`: add an `autocacheConfigHandler *handler.AutocacheConfigHandler`
    parameter, and inside the `/api/library` group add, behind a nil-guard,
    `r.Get("/autocache/config", autocacheConfigHandler.Get)` and `r.Patch("/autocache/config", autocacheConfigHandler.Patch)`.
    In main.go: construct `autocacheConfigRepo := repo.NewAutocacheConfigRepository(db.DB)` and
    `autocacheConfigHandler := handler.NewAutocacheConfigHandler(autocacheConfigRepo, log)`, and pass it into
    the `transport.NewRouter(...)` call. Add a co-located handler test using a stub store (no Postgres) covering
    the four behavior cases.
    Do NOT implement any downloading/eviction reaction to `enabled` here — only persist + serve it.
  </action>
  <acceptance_criteria>
    - `handler/autocache_config.go` declares `AutocacheConfigStore`, the handler struct, `NewAutocacheConfigHandler`, and `Get`/`Patch` methods.
    - PATCH validates ranges and rejects empty/malformed bodies with `httputil.BadRequest`; only non-nil fields reach the store.
    - `transport.NewRouter` takes the new handler param and registers `GET`+`PATCH /autocache/config` under `/api/library` behind a nil-guard.
    - `main.go` constructs the repo + handler and passes the handler into `transport.NewRouter`.
    - `go build ./...` passes and `go test ./internal/handler/...` passes from `services/library`.
  </acceptance_criteria>
  <verify>
    <automated>cd services/library && go build ./... && go test ./internal/handler/... -count=1 && grep -q 'AutocacheConfigStore' internal/handler/autocache_config.go && grep -q '/autocache/config' internal/transport/router.go && grep -q 'NewAutocacheConfigHandler' cmd/library-api/main.go</automated>
  </verify>
  <done>Admin GET/PATCH config endpoint is wired end-to-end through the gateway-admin-gated /api/library group, validates input, returns the {success,data} envelope, and persists changes with no redeploy.</done>
</task>

</tasks>

<verification>
- `cd services/library && go build ./... && go test ./internal/repo/... ./internal/domain/... ./internal/handler/... -count=1` passes.
- Migration 006 creates + idempotently seeds the singleton with §3.5 defaults; registered + applied after 005.
- GET/PATCH `/api/library/autocache/config` routes exist behind the gateway admin gate (no gateway edit needed — `/*` wildcard already covers it).
- PATCH range-validates and writes only provided fields; `enabled` is persisted and readable through the typed accessor for later phases.
</verification>

<success_criteria>
- POOL-04: An admin can GET the config and PATCH any field (budget, freshness windows, active-watcher window, quality cap, min seeders, sweep interval) live, no redeploy, enveloped as {success,data}.
- POOL-05: The master `enabled` switch is stored in the singleton row and exposed through the Get/Patch accessor that Phases 8-10 will consume; this phase implements no download/eviction behavior.
</success_criteria>

<output>
Create `.planning/phases/07-pool-foundation-config-migration/07-02-SUMMARY.md` when done.
</output>
