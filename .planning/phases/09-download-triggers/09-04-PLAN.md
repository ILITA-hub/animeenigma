---
phase: 09-download-triggers
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - services/scheduler/internal/jobs/autocache_logic_a.go
  - services/scheduler/internal/jobs/autocache_logic_a_test.go
  - services/scheduler/internal/config/config.go
  - services/scheduler/internal/service/job.go
  - services/scheduler/internal/service/job_test.go
  - services/scheduler/cmd/scheduler-api/main.go
autonomous: true
requirements: [TRIG-01]

must_haves:
  truths:
    - "A periodic scheduler cron job enumerates ongoing anime with at least one active JP-audio-combo watcher (status=watching AND watch progress within active_watcher_days) and fires an ongoing demand for the latest-aired episode of each"
    - "The enumeration runs the hotcombos-style DISTINCT join (animeenigma shared DB) plus the D8 recency predicate, JP-audio filter, and episodes_aired join"
    - "The demand carries mal_id (shikimori_id), episode=episodes_aired (latest aired), reason=ongoing"
    - "A non-JP-audio ongoing anime (only kodik/ru watchers) fires no demand"
    - "The cron job is registered via the existing robfig/cron AddFunc harness with metrics + last-run tracking"
  artifacts:
    - path: "services/scheduler/internal/jobs/autocache_logic_a.go"
      provides: "Logic A job — join + per-ongoing latest-ep demand POST to library"
      min_lines: 70
      contains: "func (j *AutocacheLogicAJob) Run"
    - path: "services/scheduler/internal/service/job.go"
      provides: "cron registration of the Logic A job"
      contains: "autocache_logic_a"
    - path: "services/scheduler/internal/config/config.go"
      provides: "AutocacheLogicACron + LibraryInternalURL config"
      contains: "AutocacheLogicACron"
  key_links:
    - from: "services/scheduler/internal/jobs/autocache_logic_a.go"
      to: "watch_history x anime_list x animes DISTINCT join"
      via: "ongoing + watching + JP-audio + D8 recency + episodes_aired"
      pattern: "episodes_aired"
    - from: "services/scheduler/internal/jobs/autocache_logic_a.go"
      to: "library POST /internal/library/autocache/demand"
      via: "per-ongoing demand {mal_id, episode, reason=ongoing}"
      pattern: "autocache/demand"
    - from: "services/scheduler/internal/service/job.go"
      to: "cron.AddFunc"
      via: "register the Logic A job alongside the existing daily jobs"
      pattern: "AddFunc"
---

<objective>
Build Logic A (TRIG-01): a periodic scheduler cron job that pushes newly-aired episodes of watched
ongoings into the autocache. Each sweep it runs the proven hotcombos-style DISTINCT join over
`watch_history × anime_list × animes` (all in the shared `animeenigma` DB the scheduler reads) — adding
a JP-audio filter, the D8 active-watcher recency predicate (`active_watcher_days`, default 30), and the
`episodes_aired` join — to find every ongoing anime with ≥1 active JP-audio-combo watcher, then fires an
`ongoing`-reason demand for that anime's latest-aired episode to the library internal endpoint.

The scheduler owns the `robfig/cron` harness and already POSTs library/analytics internal endpoints from
cron jobs (read_threshold.go is the exact mirror — a cron job that just does a Docker-network HTTP POST,
no cross-service import). "As soon as on torrents" is satisfied by the library Planner (Plan 09-02)
retrying the search each loop; this producer simply re-asserts demand every sweep, and the library
PK-dedup + Planner backoff bound the churn.

Purpose: keep the pool ahead of newly-aired episodes for every watched ongoing.
Output: `jobs/autocache_logic_a.go` (join + demand POST) + cron registration + config + DI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-download-triggers/09-CONTEXT.md
@.planning/phases/09-download-triggers/09-RESEARCH.md

<interfaces>
<!-- The join to adapt + the cron/POST patterns to mirror. Use directly. -->

ADAPT THIS join (services/notifications/internal/job/hotcombos.go:46-60) — DISTINCT over
  watch_history wh JOIN anime_list al ON al.user_id=wh.user_id AND al.anime_id=wh.anime_id
  JOIN animes a ON a.id=wh.anime_id
  WHERE al.status='watching' AND a.status='ongoing'.
  ADD for Logic A (RESEARCH Pattern 2):
    AND (wh.player IN ('ae','raw') OR wh.language='ja')          -- JP-audio (CONTEXT D1 / A1)
    AND al.updated_at > now() - (interval '1 day' * ?)           -- D8 active_watcher_days recency
  SELECT DISTINCT a.shikimori_id, a.episodes_aired                -- latest-aired ep target (A3)
  (the tables are all in the animeenigma DB — scheduler reads them with no HTTP, db.DB is a *gorm.DB).

MIRROR THIS cron job (services/scheduler/internal/jobs/read_threshold.go):
  ReadThresholdJob{ client *http.Client; config *config.JobsConfig; log }
  NewReadThresholdJob(cfg, log); Run(ctx) error — builds a URL from config, POSTs it (Docker-network),
  returns non-2xx/transport errors so the JobService metrics wrap records failure. ~70s client timeout.
  (Logic A's per-demand POSTs are tiny — use a short timeout, e.g. 5s, and don't fail the whole Run on a
  single demand POST error; aggregate-and-log.)

CRON REGISTRATION (services/scheduler/internal/service/job.go):
  JobService holds each *jobs.XxxJob + a lastXxxRun time.Time; NewJobService takes them as params; Start(...)
  takes the cron exprs and calls s.cron.AddFunc(expr, func(){ ...Run(ctx)...; metrics.SchedulerJobExecutionsTotal
  .WithLabelValues("name", "success|error").Inc(); ...SchedulerJobDuration...; ...SchedulerJobLastSuccess... }).
  The readThresholdJob/providerRankingJob are nil-guarded (`if s.readThresholdJob != nil`) — mirror that
  optional-registration shape so a missing library URL disables the job cleanly.

CONFIG (services/scheduler/internal/config/config.go) — JobsConfig holds cron exprs + service URLs via
  getEnv/getEnvInt. Add AutocacheLogicACron + LibraryInternalURL (default http://library:8089). The
  active_watcher_days value: the AUTHORITATIVE source is library autocache_config (live-editable, default 30)
  — but the scheduler is on a different DB and does NOT read library's DB. Use a scheduler env mirror
  AUTOCACHE_ACTIVE_WATCHER_DAYS (default 30) for the recency predicate; document the mirror in the SUMMARY.

MAIN.GO DI (services/scheduler/cmd/scheduler-api/main.go:114-142) — construct the job, pass into
  NewJobService, pass its cron expr into jobService.Start(...). db.DB is the *gorm.DB to give the job.

From Plan 09-01: the library autocache_demand_reason enum accepts 'ongoing', AND Plan 09-01 Task 4 fixed
  the /internal/library/autocache/demand handler to VALIDATE-AND-HONOR the wire reason against
  {backfill,next_ep,ongoing} (default backfill only on absent/invalid). The demand POST body is
  {mal_id, episode, reason:"ongoing"} and is recorded as ongoing on this Docker-network-only path — no
  server-side override, no SUMMARY follow-up needed. Send the true reason and rely on it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AutocacheLogicAJob — join + per-ongoing demand POST</name>
  <files>services/scheduler/internal/jobs/autocache_logic_a.go, services/scheduler/internal/jobs/autocache_logic_a_test.go</files>
  <read_first>
    - services/notifications/internal/job/hotcombos.go (the DISTINCT join to adapt — copy the SQL, add the JP-audio + D8 recency + episodes_aired select)
    - services/scheduler/internal/jobs/read_threshold.go (the cron-job struct + Run(ctx) + Docker-network POST + http.Client to mirror)
    - 09-RESEARCH.md "Pattern 2" (the literal Logic A SQL) + A3 (episodes_aired authoritative) + Pitfall 4 (re-asserting demand every sweep is bounded by library-side dedup/backoff)
  </read_first>
  <behavior>
    - Run(ctx) executes the DISTINCT join and, for each (shikimori_id, episodes_aired) row with episodes_aired > 0 and a non-empty shikimori_id, POSTs {mal_id: shikimori_id, episode: episodes_aired, reason: "ongoing"} to the library demand endpoint.
    - Only ongoing anime with a watching + JP-audio (player in ae/raw OR language=ja) + recent (al.updated_at within active_watcher_days) watcher appear in the result set.
    - An ongoing anime watched only via kodik/ru or english/en produces no row -> no demand.
    - A row with episodes_aired=0 or empty shikimori_id is skipped (no valid target).
    - A single demand POST failure is logged and counted but does not abort the rest of the sweep; Run returns an error only on the join query failure (so the JobService wrap records a real failure).
  </behavior>
  <action>
    Create jobs/autocache_logic_a.go with an `AutocacheLogicAJob` struct holding `db *gorm.DB`,
    `client *http.Client` (short timeout, e.g. 5s), `libraryURL string`, `activeWatcherDays int`, and `log`.
    `NewAutocacheLogicAJob(db, libraryURL string, activeWatcherDays int, log)`.

    `Run(ctx)`: execute the adapted hotcombos DISTINCT join (Raw + Scan into a small row struct
    {ShikimoriID string; EpisodesAired int}), binding the active_watcher_days param. On query error return it
    wrapped. Then iterate rows: skip episodes_aired<=0 or shikimoriID==""; build the demand body
    {mal_id, episode: episodesAired, reason:"ongoing"}; POST it to `libraryURL + "/internal/library/autocache/demand"`
    with the short-timeout client; on a non-2xx/transport error log Warnw + count a local failure but CONTINUE
    the loop (re-asserting demand is idempotent via library PK-dedup — a transient blip self-heals next sweep).
    Log a summary Infow (rows, demands_fired, demands_failed). Return nil unless the JOIN failed.

    Reason-handling note: send reason="ongoing" on the wire. Plan 09-01 Task 4 already fixed the library
    /internal demand handler to honor a validated wire reason on this Docker-network-only path, so an
    ongoing demand is recorded as ongoing (no server-side override). The seam is correct by construction —
    no SUMMARY follow-up is needed. The test asserts the OUTBOUND body carries reason="ongoing".

    Add a unit test using a fake *gorm.DB over SQLite (seed watch_history/anime_list/animes minimal tables —
    follow the existing scheduler/notifications test seeding style) OR, if SQLite seeding is heavy, factor the
    join behind a tiny `enumerator` seam and unit-test the POST-iteration + an httptest server capturing the
    body asserting {mal_id, episode, reason:"ongoing"}; plus: episodes_aired=0 skipped; one POST failure does
    not abort the rest. Pick the lighter seam and note it in the SUMMARY.
  </action>
  <verify>
    <automated>cd services/scheduler && go build ./internal/jobs/... && go vet ./internal/jobs/... && go test ./internal/jobs/... -run LogicA -count=1 && grep -q "episodes_aired" internal/jobs/autocache_logic_a.go && grep -q "autocache/demand" internal/jobs/autocache_logic_a.go && grep -qi "ongoing" internal/jobs/autocache_logic_a.go && grep -q "ae.*raw\|'ae'.*'raw'" internal/jobs/autocache_logic_a.go</automated>
  </verify>
  <acceptance_criteria>
    - The join filters ongoing + watching + JP-audio + D8 recency and selects shikimori_id + episodes_aired.
    - Each valid row fires {mal_id, episode=episodes_aired, reason="ongoing"} to the library demand endpoint.
    - episodes_aired=0 / empty shikimori_id are skipped; one POST failure does not abort the sweep; JOIN failure returns an error.
    - Tests cover the fire + skip + partial-failure behavior and pass.
  </acceptance_criteria>
  <done>Logic A job enumerates JP-audio ongoings and re-asserts ongoing demand per sweep; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Config + cron registration + main.go DI</name>
  <files>services/scheduler/internal/config/config.go, services/scheduler/internal/service/job.go, services/scheduler/internal/service/job_test.go, services/scheduler/cmd/scheduler-api/main.go</files>
  <read_first>
    - services/scheduler/internal/config/config.go (JobsConfig + the ReadThresholdCron/AnalyticsInternalURL block + getEnv/getEnvInt to mirror)
    - services/scheduler/internal/service/job.go (JobService fields + NewJobService params + Start(...) AddFunc + the nil-guarded `if s.readThresholdJob != nil` optional-registration shape)
    - services/scheduler/cmd/scheduler-api/main.go:114-142 (job construction + NewJobService + jobService.Start(...) call site)
  </read_first>
  <action>
    Config: add to JobsConfig `AutocacheLogicACron` (default a frequent-but-sane cron, e.g. every 20 min:
    `*/20 * * * *`, mirroring sweep_interval_min default 20), `LibraryInternalURL`
    (getEnv("LIBRARY_INTERNAL_URL", getEnv("LIBRARY_SERVICE_URL","http://library:8089"))), and
    `AutocacheActiveWatcherDays` (getEnvInt("AUTOCACHE_ACTIVE_WATCHER_DAYS", 30)).

    JobService: add an `autocacheLogicAJob *jobs.AutocacheLogicAJob` field + `lastAutocacheLogicARun time.Time`;
    add it as a NewJobService param; in Start(...) add an `autocacheLogicACron string` param and register the
    job via cron.AddFunc with the SchedulerJobExecutionsTotal/Duration/LastSuccess metrics wrap (label
    "autocache_logic_a"), nil-guarded (`if s.autocacheLogicAJob != nil`) like readThresholdJob so a missing
    library URL disables it cleanly. Add the job to GetStatus(). Update job_test.go if it asserts the
    NewJobService/Start arity.

    main.go: construct `autocacheLogicAJob := jobs.NewAutocacheLogicAJob(db.DB, cfg.Jobs.LibraryInternalURL,
    cfg.Jobs.AutocacheActiveWatcherDays, log)`, pass it into NewJobService, and pass cfg.Jobs.AutocacheLogicACron
    into jobService.Start(...). Keep the existing param order; append the new ones at the end of each signature
    to minimize churn.
  </action>
  <verify>
    <automated>cd services/scheduler && go build ./... && go vet ./... && go test ./internal/service/... -count=1 && grep -q "AutocacheLogicACron" internal/config/config.go && grep -q "AUTOCACHE_ACTIVE_WATCHER_DAYS" internal/config/config.go && grep -q "autocache_logic_a" internal/service/job.go && grep -q "NewAutocacheLogicAJob" cmd/scheduler-api/main.go</automated>
  </verify>
  <acceptance_criteria>
    - Config exposes AutocacheLogicACron (default ~20min), LibraryInternalURL, AutocacheActiveWatcherDays (default 30).
    - The job is registered via cron.AddFunc with the standard metrics wrap, nil-guarded like the other optional jobs.
    - main.go constructs + wires the job + cron expr; `go build ./... && go vet ./...` clean.
  </acceptance_criteria>
  <done>Logic A cron registered + configured + wired into scheduler boot; scheduler builds + tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watch_history/anime_list/animes -> Logic A | shared-DB read of who-wants-what (the scheduler is on the animeenigma DB) |
| scheduler -> library /internal/* | Docker-network-only demand POST; no gateway proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Denial of Service | re-asserting demand every sweep (thundering herd) | mitigate | The producer only re-asserts; the library PK-dedup collapses repeats to one row and the Planner backoff (Plan 09-02) prevents re-searching un-findable episodes — bounded by design (RESEARCH Pitfall 4) |
| T-09-12 | Tampering | wrong-audience demand (non-JP watchers) | mitigate | The join's `wh.player IN ('ae','raw') OR wh.language='ja'` + status/recency predicates ensure only active JP-audio ongoing watchers produce a demand (CONTEXT D1) |
| T-09-13 | Information disclosure | demand POST over the network | accept | Docker-network-only `/internal/*` (no gateway proxy, Phase-8 precedent); body carries only mal_id/episode/reason |
| T-09-14 | Denial of Service | a single library blip aborting the whole sweep | mitigate | Per-demand POST failures are logged + counted but do not abort the loop; Run returns an error only on the JOIN failure |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (stdlib net/http + existing gorm/robfig-cron); RESEARCH audit confirms zero new deps |
</threat_model>

<verification>
- `cd services/scheduler && go build ./... && go vet ./...` clean.
- `go test ./internal/jobs/... ./internal/service/... -count=1` green.
- Logic A enumerates JP-audio ongoings (status=watching + D8 recency), fires ongoing demand per latest-aired ep, and is registered on the cron harness with metrics.
</verification>

<success_criteria>
- TRIG-01: an ongoing anime with ≥1 active JP-audio-combo watcher gets its newly-aired episode demanded each sweep, so the Planner downloads it once a qualifying release appears.
- Non-JP-audio ongoings produce no demand; episodes_aired=0 / empty mal_id are skipped.
- The cron job is registered, metric-wrapped, last-run-tracked, and nil-guarded like the other optional scheduler jobs.
</success_criteria>

<output>
Create `.planning/phases/09-download-triggers/09-04-SUMMARY.md` when done.
</output>
