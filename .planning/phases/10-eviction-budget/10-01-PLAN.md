---
phase: 10-eviction-budget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/library/internal/repo/episode.go
  - services/library/internal/repo/episode_test.go
  - services/library/internal/minio/writer.go
  - services/library/internal/minio/writer_test.go
  - services/library/internal/metrics/library_metrics.go
  - services/library/internal/metrics/library_metrics_test.go
autonomous: true
requirements: [EVICT-01, EVICT-02, EVICT-03, EVICT-04]

must_haves:
  truths:
    - "A bytes-sum query returns Σ size_bytes over the aeProvider/ pool (admin + auto), 0 for an empty pool"
    - "A single SQL query returns only Stale rows, ordered in the locked 4-tier eviction order, oldest-first within tier"
    - "Deleting all MinIO objects under a prefix is one call that hard-fails on the first RemoveObject error and is a nil no-op for an empty prefix"
    - "library_autocache_evicted_total{source}, library_autocache_rejected_total{reason}, library_autocache_bytes_used{source,freshness}, library_autocache_budget_bytes, library_autocache_episodes{source,freshness} exist and are settable/incrementable"
  artifacts:
    - path: "services/library/internal/repo/episode.go"
      provides: "SumPoolBytes, ListStaleEvictionCandidates, DeleteByID"
      contains: "func (r *EpisodeRepository) SumPoolBytes"
    - path: "services/library/internal/minio/writer.go"
      provides: "DeletePrefix"
      contains: "func (w *Writer) DeletePrefix"
    - path: "services/library/internal/metrics/library_metrics.go"
      provides: "evicted/rejected counters + bytes_used/budget_bytes/episodes gauges"
      contains: "library_autocache_evicted_total"
  key_links:
    - from: "internal/repo/episode.go ListStaleEvictionCandidates"
      to: "autocache_config day-windows"
      via: "bound ? params for freshness windows"
      pattern: "auto_fresh_download_days|admin_fresh_days"
    - from: "internal/metrics/library_metrics.go"
      to: "prometheus default registry"
      via: "promauto.With(reg) factory"
      pattern: "library_autocache_bytes_used"
---

<objective>
Build the three Phase-10 primitive layers the Evictor (Plan 02) and pre-admit gates (Plan 03) consume: the repo aggregate/candidate/delete queries, the MinIO recursive prefix delete, and the eviction/budget metrics (2 counters + 3 gauges). No behavior wiring yet — this plan creates the building blocks only.

Purpose: Isolate the data-access + metric surface so Plan 02's Evictor composes against finished, tested primitives (interface-first ordering — contracts before consumers).
Output: `SumPoolBytes`/`ListStaleEvictionCandidates`/`DeleteByID` on EpisodeRepository, `DeletePrefix` on the MinIO Writer, and the five new collectors on LibraryMetrics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-eviction-budget/10-CONTEXT.md
@.planning/phases/10-eviction-budget/10-PATTERNS.md

<interfaces>
<!-- Live config the candidate query reads (Plan 02/03 pass it in). From internal/domain/autocache_config.go -->
AutocacheConfig fields: BudgetBytes int64, AutoFreshDownloadDays int, AutoFreshFetchDays int, AdminFreshDays int (all live-read).

<!-- Episode row shape. From internal/domain/episode.go -->
Episode: ID string, ShikimoriID string, EpisodeNumber int, MinioPath string (prefix ending '/'), SizeBytes *int64, Source EpisodeSource ("admin"|"autocache"), DownloadedAt *time.Time, LastFetchAt *time.Time, FetchCount int64, CreatedAt time.Time.
EpisodeSource consts: domain.EpisodeSourceAdmin="admin", domain.EpisodeSourceAutocache="autocache".

<!-- MinIO Uploader seam already has these (writer.go:48-57) — no new SDK surface -->
ListObjectsByPrefix(ctx, prefix) ([]string, error); uploader.RemoveObject(ctx, bucket, object, minio.RemoveObjectOptions{}) error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EpisodeRepository — SumPoolBytes + ListStaleEvictionCandidates + DeleteByID</name>
  <files>services/library/internal/repo/episode.go, services/library/internal/repo/episode_test.go</files>
  <read_first>
    - services/library/internal/repo/episode.go — extend this file. Copy the GORM idiom from `List` (lines 65-74), `UpdateMinioPath` (81-89), `BumpFetch` (100-111, `gorm.Expr`), and `ListAdminLegacyPath` (120-129, the `minio_path NOT LIKE 'aeProvider/%'` literal — invert it to `LIKE`). All wrap via `liberrors.Wrap(err, liberrors.CodeInternal, "...")`.
    - services/library/internal/domain/episode.go — Episode field/column tags (Source, DownloadedAt, LastFetchAt, SizeBytes, MinioPath, CreatedAt) lines 48-62.
    - services/library/internal/domain/autocache_config.go — AutocacheConfig day-window fields lines 24-30 (the bound `?` params for the Stale predicate).
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 135-200 — the SQL sketch for all three methods (verbatim guidance, including the 4-tier CASE order + COALESCE within-tier sort).
  </read_first>
  <behavior>
    - SumPoolBytes returns Σ size_bytes over `minio_path LIKE 'aeProvider/%'`; an empty pool returns 0 (COALESCE), not an error.
    - ListStaleEvictionCandidates returns ONLY Stale rows (excludes Fresh per the source-branched windows). Fresh autocache: downloaded_at > now - auto_fresh_download_days OR last_fetch_at > now - auto_fresh_fetch_days. Fresh admin: downloaded_at > now - admin_fresh_days OR last_fetch_at > now - admin_fresh_days. NULL downloaded_at = "very old" (classify by last_fetch_at only); NULL last_fetch_at = never-fetched. Day-windows are bound `?` params, NOT string-interpolated.
    - Ordering tiers: (1) autocache·last_fetch_at IS NULL, (2) autocache·fetched, (3) admin·last_fetch_at IS NULL, (4) admin·fetched — via a CASE; within tier, COALESCE(last_fetch_at, downloaded_at, created_at) ASC.
    - DeleteByID deletes the single row by id; deleting a non-existent id is a nil no-op (no NotFound).
    - A pure-Go helper is NOT added here (it lives in evictor.go, Plan 02) — but the test asserts the SQL Stale predicate + tier ordering against a fake/stubbed result OR a build-tag-gated integration path consistent with this package's convention (DB-backed repo tests are `//go:build integration`; the non-integration unit covers the LIKE-prefix scoping + COALESCE-zero + nil-id no-op shape, matching `episode_test`'s existing no-DB style).
  </behavior>
  <action>
    Add three methods to EpisodeRepository. `SumPoolBytes(ctx) (int64, error)`: `Model(&domain.Episode{}).Where("minio_path LIKE 'aeProvider/%'").Select("COALESCE(SUM(size_bytes), 0)").Scan(&total)`. `ListStaleEvictionCandidates(ctx, cfg *domain.AutocacheConfig, now time.Time) ([]domain.Episode, error)`: scope to `minio_path LIKE 'aeProvider/%'`, then a NOT-Fresh `Where` with the source-branched predicate using bound params computed as `now.AddDate(0,0,-cfg.AutoFreshDownloadDays)` etc. (pass the cutoff timestamps as `?` args, never interpolate). Order by the 4-tier `CASE WHEN source='autocache' AND last_fetch_at IS NULL THEN 1 WHEN source='autocache' THEN 2 WHEN source='admin' AND last_fetch_at IS NULL THEN 3 ELSE 4 END ASC`, then `COALESCE(last_fetch_at, downloaded_at, created_at) ASC`. `DeleteByID(ctx, id string) error`: `Where("id = ?", id).Delete(&domain.Episode{})`, wrap errors. Mirror PATTERNS.md lines 147-199 exactly. Add the matching no-DB unit cases to episode_test.go in the existing style (TableName / construction / nil-id shape — DB-backed assertions stay `//go:build integration` per the package convention noted in 07-02-SUMMARY).
  </action>
  <verify>
    <automated>cd services/library && go build ./internal/repo/... && go vet ./internal/repo/... && go test ./internal/repo/... -count=1 && grep -q "func (r \*EpisodeRepository) SumPoolBytes" internal/repo/episode.go && grep -q "func (r \*EpisodeRepository) ListStaleEvictionCandidates" internal/repo/episode.go && grep -q "func (r \*EpisodeRepository) DeleteByID" internal/repo/episode.go && grep -q "COALESCE(SUM(size_bytes), 0)" internal/repo/episode.go</automated>
  </verify>
  <acceptance_criteria>
    - `SumPoolBytes`, `ListStaleEvictionCandidates`, `DeleteByID` all defined on `*EpisodeRepository`.
    - Candidate query uses bound `?` params for day-windows (grep shows no `fmt.Sprintf`/string-concat of `cfg.Auto*`/`cfg.Admin*` into the WHERE clause).
    - The 4-tier CASE ordering literal is present; within-tier sort uses `COALESCE(last_fetch_at, downloaded_at, created_at)`.
    - `go build`, `go vet`, and `go test ./internal/repo/...` (non-integration) all pass.
  </acceptance_criteria>
  <done>Three new repo methods compile, vet clean, pass the non-integration unit, and the SQL shape matches the locked 4-tier rule with bound params.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MinIO Writer.DeletePrefix</name>
  <files>services/library/internal/minio/writer.go, services/library/internal/minio/writer_test.go</files>
  <read_first>
    - services/library/internal/minio/writer.go lines 297-369 — `ListObjectsByPrefix` (302-317) + `Move` (331-369). DeletePrefix is the inverse of Move's remove-loop; mirror the `strings.HasSuffix(prefix,"/")` normalization (332-337) but HARD-fail on the first RemoveObject error (Move soft-fails; DeletePrefix must not, to avoid an orphaned serving pointer).
    - services/library/internal/minio/writer.go lines 48-57 — the `Uploader` seam (ListObjects/RemoveObject already present; no new SDK method).
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 204-230 — the DeletePrefix sketch + the empty-prefix=nil decision.
    - services/library/internal/minio/writer_test.go — existing fake-Uploader test style to mirror (find the fake that implements ListObjects/RemoveObject).
  </read_first>
  <behavior>
    - DeletePrefix normalizes prefix to end with `/`, lists every key under it via ListObjectsByPrefix, and RemoveObjects each.
    - On the FIRST RemoveObject error it returns that error immediately (caller leaves the row for retry).
    - An empty prefix (0 keys) returns nil (idempotent — objects already gone), NOT an error (unlike Move).
    - A list error returns a wrapped error.
  </behavior>
  <action>
    Add `DeletePrefix(ctx context.Context, prefix string) error` to `*Writer`. Normalize trailing slash, call `w.ListObjectsByPrefix(ctx, prefix)` (wrap list error as `fmt.Errorf("delete list: %w", err)`), and loop `w.uploader.RemoveObject(ctx, w.cfg.Bucket, k, minio.RemoveObjectOptions{})` returning `fmt.Errorf("delete %s: %w", k, rmErr)` on the first failure. Return nil when keys is empty. Mirror PATTERNS.md lines 216-226. Add a writer_test.go case using a fake Uploader: multi-key delete success, empty-prefix→nil, remove-error→propagates (assert which keys were removed before the error).
  </action>
  <verify>
    <automated>cd services/library && go build ./internal/minio/... && go vet ./internal/minio/... && go test ./internal/minio/... -count=1 && grep -q "func (w \*Writer) DeletePrefix" internal/minio/writer.go</automated>
  </verify>
  <acceptance_criteria>
    - `DeletePrefix` defined on `*Writer`; uses the existing `Uploader.RemoveObject` (no new SDK surface).
    - Hard-fails on first RemoveObject error (test asserts propagation); empty prefix returns nil.
    - `go build`/`go vet`/`go test ./internal/minio/...` pass.
  </acceptance_criteria>
  <done>DeletePrefix compiles, hard-fails on first remove error, no-ops on empty prefix, and the writer_test fake-Uploader cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LibraryMetrics — 2 eviction/rejection counters + 3 byte/episode gauges</name>
  <files>services/library/internal/metrics/library_metrics.go, services/library/internal/metrics/library_metrics_test.go</files>
  <read_first>
    - services/library/internal/metrics/library_metrics.go — struct fields (29-62), the `promauto.With(reg)` factory block (75-172), the nil-guarded `Inc`/`Set` methods + `GetXForTest` seams (175-352). Clone `enqueueRejectedTotal` (103-109 / IncEnqueueRejected 208-210) for evictedTotal; clone `autocacheServeTotal` (156-162 / IncServeTotal 322-327) for rejectedTotal; clone `diskFreeBytes` plain Gauge (97-102 / SetDiskFreeBytes 200-202) for budgetBytes. There is NO GaugeVec yet — use `factory.NewGaugeVec` for bytes_used + episodes.
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 279-295 — exact metric names + label sets + method names (locked by spec §7).
  </read_first>
  <behavior>
    - `library_autocache_evicted_total{source}` counter; method IncEvictedTotal(source) + GetEvictedTotalForTest.
    - `library_autocache_rejected_total{reason}` counter (distinct from the existing `library_enqueue_rejected_total{disk_full}`); method IncRejectedTotal(reason) + GetRejectedTotalForTest. Used with reason="budget_full".
    - `library_autocache_bytes_used{source,freshness}` GaugeVec; method SetBytesUsed(source, freshness, n int64).
    - `library_autocache_budget_bytes` plain Gauge; method SetBudgetBytes(n int64).
    - `library_autocache_episodes{source,freshness}` GaugeVec; method SetEpisodes(source, freshness, n int64).
    - All Inc/Set methods nil-guarded (`if m == nil { return }`).
  </behavior>
  <action>
    Add a "Phase 10 (eviction/budget)" comment block to the struct (mirror the dated Phase-08/09 blocks at lines 49-61) with the five new fields: `evictedTotal *prometheus.CounterVec`, `rejectedTotal *prometheus.CounterVec`, `bytesUsed *prometheus.GaugeVec`, `budgetBytes prometheus.Gauge`, `episodes *prometheus.GaugeVec`. Register all five in the `NewLibraryMetricsWithRegisterer` factory block with the exact spec §7 names + Help text. Add the nil-guarded methods IncEvictedTotal/IncRejectedTotal/SetBytesUsed/SetBudgetBytes/SetEpisodes plus GetEvictedTotalForTest/GetRejectedTotalForTest test seams. Mirror PATTERNS.md lines 285-293 exactly. Add library_metrics_test.go cases registering against a fresh `prometheus.NewRegistry()` asserting each Inc/Set moves the right collector via testutil.ToFloat64.
  </action>
  <verify>
    <automated>cd services/library && go build ./internal/metrics/... && go vet ./internal/metrics/... && go test ./internal/metrics/... -count=1 && grep -q "library_autocache_evicted_total" internal/metrics/library_metrics.go && grep -q "library_autocache_rejected_total" internal/metrics/library_metrics.go && grep -q "library_autocache_bytes_used" internal/metrics/library_metrics.go && grep -q "library_autocache_budget_bytes" internal/metrics/library_metrics.go && grep -q "library_autocache_episodes" internal/metrics/library_metrics.go</automated>
  </verify>
  <acceptance_criteria>
    - All five collector names present with the locked label sets (`{source}`, `{reason}`, `{source,freshness}`, none, `{source,freshness}`).
    - Methods IncEvictedTotal/IncRejectedTotal/SetBytesUsed/SetBudgetBytes/SetEpisodes + the two GetXForTest seams exist and are nil-guarded.
    - `go build`/`go vet`/`go test ./internal/metrics/...` pass.
  </acceptance_criteria>
  <done>Five new collectors register cleanly against a fresh registry, the Inc/Set methods move them, and the names match spec §7 exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → Postgres | the Stale-candidate day-windows must not be string-interpolated into SQL (injection-shaped, though values are server-derived ints) |
| Writer → MinIO | object deletion is destructive; a partial delete + premature row delete (Plan 02) would orphan a serving pointer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | ListStaleEvictionCandidates SQL | mitigate | freshness day-windows passed as bound `?` params (computed from cfg ints), never string-interpolated — verified by grep in acceptance |
| T-10-02 | Denial of Service | DeletePrefix | mitigate | hard-fail on first RemoveObject error so a half-deleted prefix never reports success; caller (Plan 02) leaves the row to retry |
| T-10-03 | Information Disclosure | new metrics | accept | labels are source/freshness/reason only — no mal_id/episode, so /metrics leaks no per-title viewing data (matches the established Phase-08/09 low-cardinality rule) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | no package installs — stdlib + existing gorm/minio-go/prometheus only (RESEARCH confirmed zero new deps for the library autocache subsystem) |
</threat_model>

<verification>
- `cd services/library && go build ./... && go vet ./...` — clean.
- `go test ./internal/repo/... ./internal/minio/... ./internal/metrics/... -count=1` — all pass.
- No new go.mod dependency (`git diff services/library/go.mod` empty).
</verification>

<success_criteria>
- SumPoolBytes/ListStaleEvictionCandidates/DeleteByID, DeletePrefix, and the five collectors all exist, compile, vet clean, and pass their unit tests.
- The candidate SQL honors the locked 4-tier order with bound day-window params.
- Metric names/labels match spec §7 verbatim.
</success_criteria>

<output>
Create `.planning/phases/10-eviction-budget/10-01-SUMMARY.md` when done.
</output>
