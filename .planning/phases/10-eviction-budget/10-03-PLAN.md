---
phase: 10-eviction-budget
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - services/library/internal/autocache/planner.go
  - services/library/internal/autocache/planner_test.go
  - services/library/internal/handler/jobs.go
  - services/library/internal/handler/jobs_test.go
  - services/library/cmd/library-api/main.go
autonomous: true
requirements: [EVICT-04, EVICT-05]

must_haves:
  truths:
    - "A Planner enqueue proceeds only when Evictor.EnsureRoom(estimate) admits AND the demand isn't budget-rejected; a budget-rejected demand increments rejected_total{budget_full}, is LEFT in the demand table, and arms the existing searchBackoff (no hot-loop)"
    - "An admin POST /api/library/jobs proceeds only when BOTH DiskGuard.Allow passes AND Evictor.EnsureRoom admits; a budget-rejected upload returns HTTP 507 and increments rejected_total{budget_full}"
    - "The Planner's pre-admit estimate is the selected release size, falling back to an avg_raw_ep_size const (~1.2 GB) when the release size is 0"
    - "The Evictor is constructed in main.go from the already-built episodeRepo/minioWriter/autocacheConfigRepo/libMetrics, Started for its sweep, and injected into BOTH the Planner and the JobsHandler; Stop is wired into graceful shutdown"
  artifacts:
    - path: "services/library/internal/autocache/planner.go"
      provides: "pre-admit EnsureRoom gate before jobs.Create + budgetEvictor seam on Planner"
      contains: "EnsureRoom"
    - path: "services/library/internal/handler/jobs.go"
      provides: "EvictorAPI seam + second pre-admit gate after DiskGuard in Create"
      contains: "EvictorAPI"
    - path: "services/library/cmd/library-api/main.go"
      provides: "NewEvictor construct + Start + inject into Planner & JobsHandler + Stop"
      contains: "NewEvictor"
  key_links:
    - from: "planner.go plan()"
      to: "Evictor.EnsureRoom (Plan 02)"
      via: "budgetEvictor seam, before p.jobs.Create"
      pattern: "EnsureRoom"
    - from: "handler/jobs.go Create"
      to: "Evictor.EnsureRoom (Plan 02)"
      via: "EvictorAPI seam, after DiskGuard.Allow"
      pattern: "EnsureRoom"
    - from: "main.go"
      to: "autocache.NewEvictor"
      via: "construct from episodeRepo/writer/autocacheConfigRepo/libMetrics + Start + inject"
      pattern: "NewEvictor"
---

<objective>
Wire the Plan-02 Evictor into the two pre-admit gates and the boot lifecycle: the Planner's enqueue path (`planner.go plan()`), the admin upload handler (`jobs.go Create`), and `main.go` (construct + Start the sweep + inject the Evictor into both consumers + Stop on shutdown). This is the EVICT-05 layering — the logical budget sits ON TOP of the existing physical DiskGuard; both must pass.

Purpose: Every download (autocache Planner enqueue AND admin manual upload) gates the SAME `Evictor.EnsureRoom`, so an unfittable download is rejected rather than blowing the budget. EVICT-04 (reject + metric), EVICT-05 (both gates).
Output: Planner pre-admit gate + budgetEvictor seam; JobsHandler EvictorAPI seam + second gate; main.go Evictor construction/Start/injection/Stop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-eviction-budget/10-CONTEXT.md
@.planning/phases/10-eviction-budget/10-PATTERNS.md
@.planning/phases/10-eviction-budget/10-02-SUMMARY.md

<interfaces>
<!-- The Evictor method both gates call. From Plan 02 internal/autocache/evictor.go -->
Evictor.EnsureRoom(ctx context.Context, estBytes int64) (admitted bool, err error)
autocache.NewEvictor(config, pool, objects, libMetrics, log) *Evictor   // exact arg order/types per Plan 02's constructor
Evictor.Start(ctx); Evictor.Stop()

<!-- Planner enqueue site. From internal/autocache/planner.go -->
plan() builds `job := &domain.Job{... SizeBytes: rel.SizeBytes ...}` then `p.jobs.Create(ctx, job)` (lines 290-308). rel.SizeBytes is the selected release size. Add the gate BEFORE Create. Planner struct fields lines 95-111; NewPlanner lines 115-136. searchBackoff machinery: markSearched(k) / inBackoff(k) / demandKey(malID, episode) (lines 352-369) — reuse to suppress hot-loop re-search of a budget-rejected demand.
p.metrics is *metrics.LibraryMetrics (Plan-01 IncRejectedTotal available).

<!-- Admin handler. From internal/handler/jobs.go -->
DiskGuardAPI seam lines 48-51; JobsHandler struct 68-77; Create disk-guard block 190-209; createJobRequest.SizeBytes line 132; writeInsufficientStorage (HTTP 507) lines 337-339; NewJobsHandlerWithLink 102-122.

<!-- main.go wiring anchors. From cmd/library-api/main.go -->
episodeRepo line 239 (now carries SumPoolBytes/ListStaleEvictionCandidates/DeleteByID from Plan 01); writer (minio) constructed ~line 300 (carries DeletePrefix); autocacheConfigRepo line 443; libMetrics line 244; NewJobsHandlerWithLink call 424-433; NewPlanner call 470-478; planner.Stop() 533. DiskGuard construct+Run 266-267 is the sweep-loop analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Planner pre-admit budget gate (EVICT-04/05) + budgetEvictor seam</name>
  <files>services/library/internal/autocache/planner.go, services/library/internal/autocache/planner_test.go</files>
  <read_first>
    - services/library/internal/autocache/planner.go lines 95-136 (struct + NewPlanner — add the new seam field + constructor param), 277-316 (the selectRAW winner → job build → `p.jobs.Create` enqueue site — insert the gate before Create), 352-369 (markSearched/inBackoff/demandKey reuse for backoff-on-reject).
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 266-276 (Planner pre-admit guidance) + 388-399 (reject policy: LEAVE the demand + arm searchBackoff; estimate = rel.SizeBytes with avg_raw_ep_size fallback).
    - .planning/phases/10-eviction-budget/10-CONTEXT.md lines 44-50, 112-113 (pre-admit gate + estimate + reject policy).
    - services/library/internal/autocache/evictor.go (Plan 02) — the EnsureRoom signature the seam wraps.
  </read_first>
  <behavior>
    - A `budgetEvictor` local seam (EnsureRoom only) is added to the Planner; nil-allowed (Planner with no evictor skips the gate, like the nil-guarded metrics/log).
    - After `selectRAW` returns a winning release, BEFORE `p.jobs.Create`: compute estBytes = rel.SizeBytes, or `avgRawEpSize` const (~1.2 GB = 1288490188 or a clearly-named const) when rel.SizeBytes <= 0. Call evictor.EnsureRoom(ctx, estBytes).
    - On EnsureRoom error: log warn, treat as fail-open (proceed to enqueue — a budget-read blip must not silently lose a download; mirror the handler/disk-guard fail-open).
    - On admitted=false: increment p.metrics.IncRejectedTotal("budget_full"), LEAVE the demand row (do NOT Delete), arm the backoff via markSearched(demandKey(...)) so the next sweeps don't re-search/re-reject every tick, record the OBS-04 download result "rejected" via IncDownloadsTotal(trigger, "rejected") (a new result label — keep it consistent), and return without enqueueing.
    - On admitted=true: proceed to the existing Create path unchanged.
  </behavior>
  <action>
    Add a `budgetEvictor interface { EnsureRoom(ctx context.Context, estBytes int64) (admitted bool, err error) }` seam in planner.go (beside the other seams ~line 60). Add an `evictor budgetEvictor` field to the Planner struct and a constructor param to NewPlanner (append it, nil-allowed). Define `const avgRawEpSize int64 = 1288490188 // ~1.2 GiB pre-admit fallback (Phase 10 const, not a config column per CONTEXT)`. In `plan()`, after the `selectRAW` success branch and before building/Creating the job, insert the gate: if `p.evictor != nil`, compute estBytes (rel.SizeBytes or avgRawEpSize fallback), call EnsureRoom; on error log+proceed; on !admitted IncRejectedTotal("budget_full") + IncDownloadsTotal(trigger,"rejected") + markSearched(k) + return true (searched). Mirror PATTERNS.md 266-276 + 388-399. Update planner_test.go: add a fakeEvictor (scriptable admitted/err) and pass it (or nil) through NewPlanner in ALL existing cases (nil = gate skipped, preserving current assertions); add new cases: budget-admitted→enqueues, budget-rejected→no enqueue + rejected_total moved + demand LEFT + backoff armed, evictor-error→fail-open enqueues.
  </action>
  <verify>
    <automated>cd services/library && go build ./internal/autocache/... && go vet ./internal/autocache/... && go test ./internal/autocache/... -count=1 && grep -q "budgetEvictor" internal/autocache/planner.go && grep -q "EnsureRoom" internal/autocache/planner.go && grep -q "IncRejectedTotal" internal/autocache/planner.go && grep -q "avgRawEpSize" internal/autocache/planner.go</automated>
  </verify>
  <acceptance_criteria>
    - `budgetEvictor` seam + `evictor` field + NewPlanner param exist; the seam is nil-guarded (existing nil-evictor tests still pass).
    - The gate sits BEFORE `p.jobs.Create`; estimate uses rel.SizeBytes with the `avgRawEpSize` fallback.
    - Budget-rejected path: IncRejectedTotal("budget_full"), demand LEFT (no Delete), backoff armed (markSearched) — asserted by a new test.
    - Evictor-error path fails open (enqueues) — asserted.
    - `go build`/`go vet`/`go test ./internal/autocache/...` pass.
  </acceptance_criteria>
  <done>The Planner gates every enqueue through EnsureRoom, rejects-and-backs-off when the pool can't fit (metric + LEFT demand, no hot-loop), fails open on a read error, and all existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin upload pre-admit gate (EVICT-05) — EvictorAPI seam in JobsHandler.Create</name>
  <files>services/library/internal/handler/jobs.go, services/library/internal/handler/jobs_test.go</files>
  <read_first>
    - services/library/internal/handler/jobs.go lines 48-51 (DiskGuardAPI seam to mirror), 68-122 (JobsHandler struct + both constructors — add the evictor field + param), 159-209 (Create — insert the second gate AFTER the disk-guard block at 209), 132 (createJobRequest.SizeBytes = the estimate), 337-339 (writeInsufficientStorage → HTTP 507).
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 233-263 (the EvictorAPI seam + the hook code, ANDed with DiskGuard, fail-open on read error, 507 on reject).
    - services/library/internal/handler/jobs_test.go — the existing stub-DiskGuard test style to mirror for a stub Evictor.
  </read_first>
  <behavior>
    - An `EvictorAPI` seam (EnsureRoom only) added to JobsHandler; nil-allowed (handler with no evictor skips the second gate).
    - In Create, immediately AFTER the existing DiskGuard block: if evictor != nil, call EnsureRoom(ctx, body.SizeBytes). On error: log warn, fail-open (don't 507 on a budget-read blip — mirror disk-guard fail-open). On admitted=false: IncRejectedTotal("budget_full") + writeInsufficientStorage(w) (HTTP 507) + return. On admitted=true: proceed.
    - Both gates must pass (EVICT-05): DiskGuard first (existing), then the budget gate.
  </behavior>
  <action>
    Add `type EvictorAPI interface { EnsureRoom(ctx context.Context, estBytes int64) (admitted bool, err error) }` to jobs.go (mirror DiskGuardAPI). Add an `evictor EvictorAPI` field to JobsHandler and thread it through `NewJobsHandlerWithLink` (append the param; the legacy `NewJobsHandler` may pass nil or also gain the param — prefer adding to NewJobsHandlerWithLink which main.go uses, and leave evictor nil in NewJobsHandler). Insert the second gate after line 209 per PATTERNS.md 248-259: nil-guard, EnsureRoom(r.Context(), body.SizeBytes), fail-open on err, on !admitted `h.metrics.IncRejectedTotal("budget_full")` + `writeInsufficientStorage(w)` + return. Update jobs_test.go: add a stub Evictor; pass it through the WithLink constructor in the relevant cases; add cases: budget-admitted→201 Created, budget-rejected→507 + rejected_total moved, evictor-error→fail-open proceeds (201), and a both-gates case (DiskGuard pass + budget pass → 201). Keep the existing disk-full→507 case (pass a nil or always-admit evictor so the DiskGuard rejection still fires first).
  </action>
  <verify>
    <automated>cd services/library && go build ./internal/handler/... && go vet ./internal/handler/... && go test ./internal/handler/... -count=1 && grep -q "EvictorAPI" internal/handler/jobs.go && grep -q "EnsureRoom" internal/handler/jobs.go && grep -q "IncRejectedTotal(\"budget_full\")" internal/handler/jobs.go</automated>
  </verify>
  <acceptance_criteria>
    - `EvictorAPI` seam + `evictor` field threaded through `NewJobsHandlerWithLink`; nil-guarded.
    - The budget gate sits AFTER the DiskGuard block in Create; estimate = body.SizeBytes.
    - Budget-rejected → HTTP 507 + IncRejectedTotal("budget_full"); evictor-error fails open (201) — both asserted.
    - `go build`/`go vet`/`go test ./internal/handler/...` pass.
  </acceptance_criteria>
  <done>The admin upload path gates on BOTH DiskGuard and the budget Evictor, returns 507 + the budget_full metric when the pool can't fit, fails open on a budget-read error, and all handler tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: main.go — construct + Start Evictor + inject into Planner & JobsHandler + Stop</name>
  <files>services/library/cmd/library-api/main.go</files>
  <read_first>
    - services/library/cmd/library-api/main.go lines 239 (episodeRepo), ~300 (writer), 443 (autocacheConfigRepo), 244 (libMetrics), 266-267 (diskGuard construct + `go diskGuard.Run` — the sweep-loop analog), 424-433 (NewJobsHandlerWithLink call — add the evictor arg), 470-480 (NewPlanner call — add the evictor arg), 524-533 (rootCancel + planner.Stop() shutdown — add evictor.Stop()).
    - .planning/phases/10-eviction-budget/10-PATTERNS.md lines 299-310 (main.go wiring: construct from already-built repos, Start the sweep, inject into both consumers, mirror planner.Stop).
    - services/library/internal/autocache/evictor.go (Plan 02) — NewEvictor signature + Start/Stop.
  </read_first>
  <action>
    After episodeRepo/writer/autocacheConfigRepo/libMetrics are all constructed (so place the Evictor construction AFTER autocacheConfigRepo at ~line 444, before the Planner at 470), add a "Phase 10 (EVICT-05): the autocache Evictor" comment block and: `evictor := autocache.NewEvictor(autocacheConfigRepo, episodeRepo, writer, libMetrics, log)` (match Plan 02's exact constructor arg order — config, pool, objects, metrics, log). Call `evictor.Start(rootCtx)` for the periodic sweep (mirror the planner.Start pattern, NOT a bare `go evictor.Run`). Pass `evictor` into the `NewJobsHandlerWithLink` call (the new last/appropriate param) and into the `NewPlanner` call (the new appended param). In graceful shutdown, after `planner.Stop()` (line 533), add `evictor.Stop()` so the sweep goroutine exits before MinIO teardown. Add a one-line `log.Infow("autocache evictor started")`. Do NOT add a migration (avg_raw_ep_size is a const per CONTEXT — no schema change this phase).
  </action>
  <verify>
    <automated>cd services/library && go build ./... && go vet ./... && go test ./... -count=1 && grep -q "autocache.NewEvictor" cmd/library-api/main.go && grep -q "evictor.Start(rootCtx)" cmd/library-api/main.go && grep -q "evictor.Stop()" cmd/library-api/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `autocache.NewEvictor(...)` constructed from the already-built episodeRepo/writer/autocacheConfigRepo/libMetrics (no reconstruction).
    - `evictor.Start(rootCtx)` started; `evictor` passed into BOTH NewJobsHandlerWithLink and NewPlanner.
    - `evictor.Stop()` wired into graceful shutdown after planner.Stop().
    - Whole service `go build ./... && go vet ./... && go test ./...` pass.
  </acceptance_criteria>
  <done>main.go constructs the Evictor once, starts its sweep, injects it into both pre-admit gates, and stops it cleanly on shutdown; the full library service builds, vets, and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → POST /api/library/jobs | an admin upload must also be budget-gated (D7) — an oversized upload can't bypass the logical quota |
| Planner → jobs.Create | a budget-rejected demand must not hot-loop the searcher/indexers |
| logical budget ↔ physical DiskGuard | both must pass (EVICT-05) — the budget layers ON TOP, does not replace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Denial of Service | budget-rejected demand re-search | mitigate | on reject the Planner arms the existing per-(mal,ep) searchBackoff (markSearched) so a permanently-too-big pool isn't re-searched/re-rejected every tick (CONTEXT line 112, PATTERNS flag 5) |
| T-10-09 | Elevation of Privilege | DiskGuard bypass | mitigate | the budget gate is ANDed AFTER DiskGuard.Allow in Create — both must pass; the budget does not replace DiskGuard (EVICT-05) |
| T-10-10 | Denial of Service | budget-read blip 500s the upload | mitigate | both gates fail-open on an EnsureRoom error (log warn, proceed) — a transient config/DB read failure never 500s an admin upload or silently drops an autocache download |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | no package installs — pure wiring against existing types |
</threat_model>

<verification>
- `cd services/library && go build ./... && go vet ./...` — clean (whole service).
- `go test ./... -count=1` — autocache (Planner+Evictor) + handler + repo + minio + metrics all green.
- `git diff services/library/go.mod` empty (no new dep, no migration).
- Behavioral: a Planner enqueue and an admin POST both call EnsureRoom; a rejected one fires `library_autocache_rejected_total{reason="budget_full"}` and (admin) returns HTTP 507.
</verification>

<success_criteria>
- EVICT-05: both DiskGuard AND the logical budget gate every download (Planner + admin upload); the budget layers on top of DiskGuard.
- EVICT-04: an unfittable download is rejected (admin → 507; Planner → demand LEFT + backoff) and `library_autocache_rejected_total{reason="budget_full"}` increments.
- main.go owns the single Evictor instance: constructed once, swept on a ticker, injected into both gates, stopped on shutdown.
</success_criteria>

<output>
Create `.planning/phases/10-eviction-budget/10-03-SUMMARY.md` when done.
</output>
