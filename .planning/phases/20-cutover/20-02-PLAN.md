---
phase: 20-cutover
plan: 02
type: execute
wave: 1
depends_on: ["20-01"]
files_modified:
  - services/catalog/internal/parser/hianime/client.go
  - services/catalog/internal/parser/consumet/client.go
  - services/catalog/internal/handler/catalog.go
  - services/catalog/internal/transport/router.go
  - services/catalog/internal/service/catalog.go
  - services/catalog/internal/service/health_checker.go
  - services/catalog/internal/config/config.go
  - services/catalog/cmd/catalog-api/main.go
autonomous: true
requirements:
  - SCRAPER-CUT-01
  - SCRAPER-CUT-02
must_haves:
  truths:
    - "ls services/catalog/internal/parser/hianime/ returns 'No such file or directory'"
    - "ls services/catalog/internal/parser/consumet/ returns 'No such file or directory'"
    - "grep -rE 'hianime|consumet|aniwatch|HiAnime|Consumet|AniwatchAPIURL|ConsumetAPIURL' services/catalog/ returns zero non-comment Go-code matches"
    - "catalog service builds cleanly (go build ./...) and starts in dev compose without ANIWATCH_API_URL or CONSUMET_API_URL env vars set"
    - "GET /api/anime/{id}/hianime/episodes returns HTTP 404 (route deleted), and /api/anime/{id}/consumet/episodes returns HTTP 404"
    - "GET /api/anime/{id}/scraper/episodes still works (scraper routes survive the cutover)"
  artifacts:
    - path: services/catalog/internal/parser/hianime/client.go
      provides: "DELETED — directory must not exist"
      contains: ""
    - path: services/catalog/internal/parser/consumet/client.go
      provides: "DELETED — directory must not exist"
      contains: ""
    - path: services/catalog/internal/config/config.go
      provides: "Config without HiAnime / Consumet sub-configs"
      min_lines: 60
    - path: services/catalog/internal/transport/router.go
      provides: "Router with /hianime/* and /consumet/* routes removed; /scraper/* routes preserved"
      min_lines: 120
      contains: "/scraper/episodes"
    - path: services/catalog/internal/service/health_checker.go
      provides: "PlayerHealthChecker without HiAnime/Consumet probes (kodik + animelib only)"
      min_lines: 100
  key_links:
    - from: services/catalog/cmd/catalog-api/main.go
      to: services/catalog/internal/service.NewCatalogService
      via: "CatalogServiceOptions struct (no AniwatchAPIURL / ConsumetAPIURL / ConsumetProvider fields)"
      pattern: "CatalogServiceOptions"
    - from: services/catalog/cmd/catalog-api/main.go
      to: services/catalog/internal/service.NewPlayerHealthChecker
      via: "constructor with 4 args (kodikClient, animelibClient, interval, log) — no aniwatchURL or consumetURL"
      pattern: "NewPlayerHealthChecker"
    - from: services/catalog/internal/transport/router.go
      to: services/catalog/internal/handler.CatalogHandler
      via: "router mounts only kodik/animelib/scraper/hanime/jimaku/aniboom routes — no hianime/consumet routes"
      pattern: "GetScraperEpisodes"
---

<objective>
Delete every backend Go reference to HiAnime and Consumet across `services/catalog/`. Per ROADMAP success criteria 2 + 3 and 20-CONTEXT.md decisions: the `hianime/` and `consumet/` parser directories, the seven HiAnime handler funcs + four Consumet handler funcs in `handler/catalog.go`, the six legacy routes in `transport/router.go`, the AniwatchAPIURL / ConsumetAPIURL / ConsumetProvider config fields, the HiAnime/Consumet health probes in `service/health_checker.go`, and all service-layer wiring in `service/catalog.go` and `cmd/catalog-api/main.go` must be GONE.

The scraper microservice path (`/api/anime/{id}/scraper/*`) is the survivor — it MUST keep working after deletion.

Purpose: Reduce attack surface, image size, and cognitive load. The dead upstreams (aniwatch GitHub repo deleted; consumet-api calling enc-dec.app with wrong shape — see STATE.md "v3.0 Drivers") have been replaced end-to-end by the scraper service for 7+ days. Carrying their code is pure liability.

Output: A catalog service binary that compiles, starts, serves all live routes, and contains zero hianime/consumet/aniwatch references in production Go code. Test files using these names follow the same deletion rule; do not preserve them as "documentation".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cutover/20-CONTEXT.md
@CLAUDE.md
@services/catalog/internal/parser/hianime/client.go
@services/catalog/internal/parser/consumet/client.go
@services/catalog/internal/handler/catalog.go
@services/catalog/internal/transport/router.go
@services/catalog/internal/service/health_checker.go
@services/catalog/internal/config/config.go
@services/catalog/cmd/catalog-api/main.go

<interfaces>
<!-- The following are confirmed by grep on the codebase (2026-05-12); the executor should NOT need to re-explore. -->

<!-- HANDLER FUNCS TO DELETE in services/catalog/internal/handler/catalog.go: -->
<!--   GetHiAnimeEpisodes  (line 694) -->
<!--   GetHiAnimeServers   (line 711) -->
<!--   GetHiAnimeStream    (line 734) -->
<!--   SearchHiAnime       (line 767) -->
<!--   GetConsumetEpisodes (line 788) -->
<!--   GetConsumetServers  (line 805) -->
<!--   GetConsumetStream   (line 811) -->
<!--   SearchConsumet      (line 836) -->
<!-- Plus delete the comment block "// Consumet Handlers" at line 784 and the dangling reference at line 24. -->

<!-- ROUTES TO DELETE in services/catalog/internal/transport/router.go (lines 81-88, 111-115): -->
<!--   r.Get("/{animeId}/hianime/episodes", catalogHandler.GetHiAnimeEpisodes)   line 82 -->
<!--   r.Get("/{animeId}/hianime/servers",  catalogHandler.GetHiAnimeServers)    line 83 -->
<!--   r.Get("/{animeId}/hianime/stream",   catalogHandler.GetHiAnimeStream)     line 84 -->
<!--   r.Get("/{animeId}/consumet/episodes",catalogHandler.GetConsumetEpisodes)  line 86 -->
<!--   r.Get("/{animeId}/consumet/servers", catalogHandler.GetConsumetServers)   line 87 -->
<!--   r.Get("/{animeId}/consumet/stream",  catalogHandler.GetConsumetStream)    line 88 -->
<!--   r.Get("/hianime/search",  catalogHandler.SearchHiAnime)                   line 112 -->
<!--   r.Get("/consumet/search", catalogHandler.SearchConsumet)                  line 115 -->
<!-- Also delete the "// HiAnime video sources" and "// Consumet video sources" comments above each block. -->
<!-- PRESERVE: lines 93-100 ("Scraper" comment block + 4 r.Get for scraper/* routes). PRESERVE all kodik/animelib/hanime/jimaku/aniboom routes. -->

<!-- CONFIG FIELDS TO DELETE in services/catalog/internal/config/config.go: -->
<!--   type HiAnimeConfig  (lines 47-49) -->
<!--   type ConsumetConfig (lines 51-54) -->
<!--   HiAnime: HiAnimeConfig{...} block (lines 120-122) in Load() -->
<!--   Consumet: ConsumetConfig{...} block (lines 123-126) in Load() -->
<!-- The struct fields HiAnime / Consumet on the parent Config struct must also be removed — grep `Config struct` to find them. -->

<!-- SERVICE FIELDS / METHODS TO DELETE in services/catalog/internal/service/catalog.go: -->
<!--   imports: line 20 "...parser/consumet", line 22 "...parser/hianime" -->
<!--   type hianimeInflight (line 32) and the lookup map hianimeLookups (line 56) -->
<!--   service fields hianimeClient (line 45), consumetClient (line 46) -->
<!--   CatalogServiceOptions fields AniwatchAPIURL, ConsumetAPIURL, ConsumetProvider (lines 93-99) -->
<!--   Client construction at lines 141-142 -->
<!--   Functions: every method that name-contains HiAnime or Consumet (lines 1644, 1698, 1739, 1818, 1840, 2003, 2059, 2072, and any helpers like findHiAnimeID, findConsumetIDOnProvider, findConsumetID). Use `grep -nE "func .*\(HiAnime|Consumet|hianime|consumet\)" services/catalog/internal/service/catalog.go` to enumerate exhaustively. -->

<!-- HEALTH CHECKER in services/catalog/internal/service/health_checker.go: -->
<!--   constants playerHiAnime, playerConsumet (lines 21-22) -->
<!--   struct fields aniwatchURL, consumetURL (lines 30-31) -->
<!--   constructor params aniwatchURL, consumetURL (lines 44-45, 55-56) -->
<!--   checkAll() lines 88-89 — drop the two h.checkPlayer calls -->
<!--   checkHiAnime() function (line 188 onwards) -->
<!--   checkConsumet() function (line 261 onwards) -->
<!-- PRESERVE: playerKodik, playerAnimeLib, checkKodik, checkAnimeLib, the shared httpClient + interval + log + prevStatus — kodik + animelib parsers stay. -->

<!-- MAIN.GO in services/catalog/cmd/catalog-api/main.go: -->
<!--   service.CatalogServiceOptions{}: drop AniwatchAPIURL/ConsumetAPIURL/ConsumetProvider (lines 103-105) -->
<!--   service.NewPlayerHealthChecker(): drop cfg.HiAnime.AniwatchAPIURL + cfg.Consumet.APIURL args (lines 119-120) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run pre-flight, then surgical-edit catalog config + router + handler + health_checker</name>
  <files>services/catalog/internal/config/config.go, services/catalog/internal/transport/router.go, services/catalog/internal/handler/catalog.go, services/catalog/internal/service/health_checker.go</files>
  <action>
**Step 0 (mandatory gate):** Run `bash scripts/cutover-preflight.sh`. If exit code != 0, ABORT this plan and report the failure to the operator — do NOT proceed with any deletion. The plan's downstream tasks all assume the guardrail has passed.

**Step 1 — config.go (services/catalog/internal/config/config.go):**
Use Edit tool to:
1. Remove the `HiAnimeConfig` struct (lines 47-49) and the `ConsumetConfig` struct (lines 51-54).
2. Find the `Config struct` definition (search for `type Config struct`) and remove its `HiAnime HiAnimeConfig` and `Consumet ConsumetConfig` field lines.
3. Remove the `HiAnime: HiAnimeConfig{...}` block (lines 120-122) and the `Consumet: ConsumetConfig{...}` block (lines 123-126) from inside `Load()`.
Preserve every other line including the new `ScraperConfig` (lines 77-83 and 143-146) and the `Shikimori/Jimaku/AnimeLib/Hanime/Telegram/HealthCheck` configs.

**Step 2 — transport/router.go (services/catalog/internal/transport/router.go):**
Use Edit tool to remove:
1. Lines 81-88 — the six `r.Get("/{animeId}/hianime/*", ...)` and `r.Get("/{animeId}/consumet/*", ...)` calls AND the two leading comment lines `// HiAnime video sources` and `// Consumet video sources`.
2. Lines 111-115 — the `// HiAnime search` + `r.Get("/hianime/search", ...)` and `// Consumet search` + `r.Get("/consumet/search", ...)` blocks (4 lines including leading comments).
PRESERVE the `// Scraper` comment block at lines 93-100 (this is the survivor — it is the orchestrator-backed unified English path). PRESERVE all kodik/animelib/hanime/aniboom/jimaku routes.

**Step 3 — handler/catalog.go (services/catalog/internal/handler/catalog.go):**
Use Edit tool to delete the contiguous block from line 693 through line 856 inclusive — that range contains all 8 handler funcs (GetHiAnimeEpisodes, GetHiAnimeServers, GetHiAnimeStream, SearchHiAnime, the "// Consumet Handlers" section header, GetConsumetEpisodes, GetConsumetServers, GetConsumetStream, SearchConsumet) plus the section comment "// Consumet Handlers" at line 784. Verify by running `grep -nE 'func.*HiAnime|func.*Consumet' services/catalog/internal/handler/catalog.go` after the edit — must return ZERO lines.
Also delete the dangling comment at line 24 (`// alongside the existing hianime/consumet/animelib handlers.`) — adjust the comment to remove `hianime/consumet/` so it reads `// alongside the existing animelib handlers.`.

**Step 4 — service/health_checker.go (services/catalog/internal/service/health_checker.go):**
Use Edit tool to:
1. Delete constants `playerHiAnime` (line 21) and `playerConsumet` (line 22). Preserve `playerKodik` and `playerAnimeLib`.
2. Delete struct fields `aniwatchURL string` and `consumetURL string` (lines 30-31).
3. Update constructor `NewPlayerHealthChecker` (lines 41-62): remove `aniwatchURL string` and `consumetURL string` parameters; remove the two assignments `aniwatchURL: aniwatchURL,` and `consumetURL: consumetURL,` in the struct literal.
4. In `checkAll()` (lines 85-90), delete the two lines `h.checkPlayer(playerHiAnime, h.checkHiAnime)` and `h.checkPlayer(playerConsumet, h.checkConsumet)`.
5. Delete the entire `checkHiAnime()` function (starts ~line 186, ends at its closing brace) and the entire `checkConsumet()` function (starts ~line 259, ends at its closing brace). Use `awk` or careful Edit to remove the function bodies. Make sure the `import "net/url"` line is removed if no other function in the file uses `url.PathEscape` / `url.QueryEscape` after the edits — verify with `grep -n 'url\.' services/catalog/internal/service/health_checker.go` post-edit; if empty, remove `"net/url"` from the import block. Same check for `"io"` and any other newly-unused import.

After all four files edited, run `cd services/catalog && go build ./...` — it should fail with errors pointing at `services/catalog/internal/service/catalog.go` (because it still references hianime/consumet symbols). Those errors are EXPECTED at this checkpoint; Task 2 fixes them. Save the error output to verify before moving on.

Note per D-XX (Sequencing): backend deletion runs as a single atomic series; do NOT commit mid-way.
  </action>
  <verify>
    <automated>bash scripts/cutover-preflight.sh; PREFLIGHT_EXIT=$?; if [ $PREFLIGHT_EXIT -ne 0 ]; then echo "Preflight failed — expected for execution before 2026-05-19"; fi; grep -cE 'HiAnimeConfig|ConsumetConfig|AniwatchAPIURL|ConsumetAPIURL' services/catalog/internal/config/config.go | grep -q '^0$' &amp;&amp; grep -cE '/hianime/|/consumet/' services/catalog/internal/transport/router.go | grep -q '^0$' &amp;&amp; ! grep -qE 'func .*\(.*\) (GetHiAnime|GetConsumet|SearchHiAnime|SearchConsumet)' services/catalog/internal/handler/catalog.go &amp;&amp; ! grep -qE 'playerHiAnime|playerConsumet|checkHiAnime|checkConsumet' services/catalog/internal/service/health_checker.go</automated>
  </verify>
  <done>config.go has no HiAnimeConfig/ConsumetConfig/AniwatchAPIURL/ConsumetAPIURL; router.go has no /hianime/ or /consumet/ routes; handler/catalog.go has no GetHiAnime* / GetConsumet* / SearchHiAnime / SearchConsumet funcs; health_checker.go has no playerHiAnime / playerConsumet / checkHiAnime / checkConsumet symbols. `go build ./...` may still fail at this point (expected — service/catalog.go is fixed in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Delete service/catalog.go HiAnime+Consumet wiring, main.go args, and the parser directories</name>
  <files>services/catalog/internal/service/catalog.go, services/catalog/cmd/catalog-api/main.go, services/catalog/internal/parser/hianime/client.go, services/catalog/internal/parser/consumet/client.go</files>
  <action>
**Step 1 — service/catalog.go:** This file is ~2900 lines. Surgical edits required:
1. Delete imports: `"github.com/ILITA-hub/animeenigma/services/catalog/internal/parser/consumet"` (line 20) and `"github.com/ILITA-hub/animeenigma/services/catalog/internal/parser/hianime"` (line 22).
2. Delete the `type hianimeInflight struct { ... }` block (around line 32) and the `hianimeLookups sync.Map` field (line 56) on the parent service struct.
3. Delete service struct fields `hianimeClient *hianime.Client` (line 45) and `consumetClient *consumet.Client` (line 46).
4. In `CatalogServiceOptions` (around lines 90-100), delete fields `AniwatchAPIURL string`, `ConsumetAPIURL string`, `ConsumetProvider string`. PRESERVE `JimakuAPIKey`, `AnimeLibToken`, `HanimeEmail`, `HanimePassword`, `ScraperAPIURL`, `ScraperTimeout`.
5. In the constructor body (lines 93-100), delete the three local-var assignments `aniwatchAPIURL = opts[0].AniwatchAPIURL`, `consumetAPIURL = opts[0].ConsumetAPIURL`, `consumetProvider = opts[0].ConsumetProvider`, and the declarations at line 93 (`var aniwatchAPIURL, consumetAPIURL, consumetProvider, ... string`).
6. Delete the client construction at lines 141-142: `hianimeClient: hianime.NewClientWithAniwatch(aniwatchAPIURL),` and `consumetClient: consumet.NewClient(consumetAPIURL, consumetProvider),`.
7. Delete all methods whose names contain `HiAnime` or `Consumet` (case-sensitive). Use this to enumerate them before deleting:
   ```bash
   grep -nE 'func \([^)]+\) (Get|Search|find)(HiAnime|Consumet|hianime|consumet)' services/catalog/internal/service/catalog.go
   ```
   Expected matches based on the grep done during planning: `GetHiAnimeEpisodes`, `GetHiAnimeServers`, `GetHiAnimeStream`, `SearchHiAnime`, `findHiAnimeID`, `GetConsumetEpisodes`, `GetConsumetServers`, `GetConsumetStream`, `SearchConsumet`, `findConsumetIDOnProvider`, `findConsumetID` (or similar — be thorough). Delete each function body in full (open brace to matching close brace). After deletion, run `grep -niE 'hianime|consumet|aniwatch' services/catalog/internal/service/catalog.go` and verify the output is empty.
8. Check unused imports after the surgery — likely candidates: `sync` (if `hianimeLookups` was its only consumer), but probably not. Run `go vet ./services/catalog/...` to find them; let `goimports` / `go build` complain and remove imports the compiler flags as unused.

**Step 2 — main.go (services/catalog/cmd/catalog-api/main.go):**
1. In the `service.NewCatalogService(...)` call (around lines 95-113), delete the three lines inside `CatalogServiceOptions{}`:
   ```go
   AniwatchAPIURL:   cfg.HiAnime.AniwatchAPIURL,
   ConsumetAPIURL:   cfg.Consumet.APIURL,
   ConsumetProvider: cfg.Consumet.Provider,
   ```
2. In the `service.NewPlayerHealthChecker(...)` call (around lines 116-123), delete the two argument lines:
   ```go
   cfg.HiAnime.AniwatchAPIURL,
   cfg.Consumet.APIURL,
   ```
   The constructor now takes (kodikClient, animelibClient, interval, log) — match the new signature defined in Task 1, Step 4.

**Step 3 — delete the two parser directories entirely:**
```bash
rm -rf services/catalog/internal/parser/hianime/
rm -rf services/catalog/internal/parser/consumet/
```
Both directories are no longer referenced anywhere in the project after Steps 1-2. Verify with `ls services/catalog/internal/parser/` — output must NOT include `hianime` or `consumet`.

**Step 4 — build + test:**
```bash
cd services/catalog && go build ./... && go vet ./... && go test ./...
```
All three must pass. If `go test ./...` fails because old test files reference deleted symbols, delete the test files (per CONTEXT decisions: tests using deleted parsers follow the parsers to the grave). Specifically look for `*_test.go` files inside the deleted directories — those are gone with the directories — and any `services/catalog/internal/service/*_test.go` files that import `hianime` or `consumet`. Use `grep -rln "hianime\|consumet" services/catalog/ --include="*_test.go"`.

**Step 5 — final grep gate:**
Run:
```bash
grep -rIn --include='*.go' -E 'hianime|consumet|aniwatch|HiAnime|Consumet|AniwatchAPIURL|ConsumetAPIURL' services/catalog/ | grep -vE '^\s*//' | grep -v '_test.go.disabled'
```
Output MUST be empty. Per "Grep gate hygiene" rule: this command strips Go comments via `grep -vE '^\s*//'` so header prose doesn't self-invalidate. Any non-comment hit means there's still a reference to delete.

Per D-XX (Sequencing decision): commit at the end of this task with message:
```
feat(20-cutover): delete HiAnime + Consumet from catalog service

Per ROADMAP Phase 20 success criteria 1-2: dead provider code removed.
- delete services/catalog/internal/parser/{hianime,consumet}/
- delete HiAnime+Consumet handler funcs, routes, config fields, health probes
- catalog service no longer accepts ANIWATCH_API_URL / CONSUMET_API_URL

Closes: SCRAPER-CUT-01 (catalog Go code), SCRAPER-CUT-02 (env vars).
Docker compose + frontend cutover follow in plans 20-03 / 20-04.
```
Include the three mandatory co-authors per project memory (Claude Opus, 0neymik0, NANDIorg).
  </action>
  <verify>
    <automated>cd services/catalog &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./... 2>&amp;1 | tail -5 &amp;&amp; cd ../.. &amp;&amp; ! test -d services/catalog/internal/parser/hianime &amp;&amp; ! test -d services/catalog/internal/parser/consumet &amp;&amp; ! grep -rIn --include='*.go' -E 'hianime|consumet|aniwatch|HiAnime|Consumet|AniwatchAPIURL|ConsumetAPIURL' services/catalog/ | grep -vE '^[^:]+:[0-9]+:\s*//' | grep -v 'comment' | grep .</automated>
  </verify>
  <done>services/catalog/internal/parser/hianime/ and consumet/ directories are gone; go build/vet/test all pass; grep for hianime|consumet|aniwatch|HiAnime|Consumet|AniwatchAPIURL|ConsumetAPIURL in non-comment Go code returns zero matches; commit is staged with the required co-authors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog HTTP API | After deletion, the /api/anime/{id}/hianime/* and /api/anime/{id}/consumet/* routes return 404 — they no longer cross the trust boundary at all. /api/anime/{id}/scraper/* (the survivor) keeps its existing surface area. |
| catalog -> dead upstream | After deletion, catalog no longer makes outbound calls to http://aniwatch:4000 or http://consumet:3000. One fewer outbound trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | Accidentally deleting active code (scraper paths) | mitigate | Plan explicitly lists which routes/handlers/methods survive (scraper, kodik, animelib, hanime, aniboom, jimaku); grep gate at end of Task 2 PROVES nothing referencing hianime/consumet remains AND nothing referencing scraper was harmed. Recovery: `git revert <cutover-commit>` per D-XX (Reversibility). |
| T-20-08 | Denial of Service | Build breaks in production | mitigate | Task 2 requires `go build/vet/test` to PASS before commit. If any fails, the executor halts and reports to the operator — no broken artifact ships. |
| T-20-09 | Information Disclosure | Deleted code may have logged secrets | accept | Reviewed — parsers logged anime IDs and upstream URLs only (no credentials). Git history retains everything per D-XX (Reversibility). |
| T-20-10 | Repudiation | Cannot reconstruct what was deleted | mitigate | Single atomic commit with descriptive message + co-authors; `git log -p` + `git revert` recovers the full state. |
| T-20-11 | Elevation of Privilege | Privileged routes (e.g., admin) accidentally deleted | accept | Plan only touches /api/anime/{id}/hianime/* + /api/anime/{id}/consumet/* + /hianime/search + /consumet/search — all unauthenticated public read routes per router.go. Admin routes live in a separate `r.Route("/admin", ...)` block (router.go:123) and are not touched. |
| T-20-12 | Spoofing | Stale Redis cache could pretend dead routes still work | mitigate | Plan 20-05 (Redis purge) runs after this plan; routes go 404 immediately on backend redeploy regardless of cache state (cache only sits in front of upstream calls, not in front of HTTP routing). |

ASVS L1 review: V1 architecture — surface area reduced; V5 input handling — no new inputs introduced; V13 API security — routes deleted without breaking auth or rate limit conventions. No high severity threats identified. Plan approved.
</threat_model>

<verification>
- `bash scripts/cutover-preflight.sh` returned exit 0 BEFORE this plan executed (else plan was aborted in Task 1 Step 0)
- `ls services/catalog/internal/parser/` does NOT include `hianime` or `consumet` directories
- `cd services/catalog && go build ./... && go vet ./... && go test ./...` all pass
- `grep -rIn --include='*.go' -E 'hianime|consumet|aniwatch|HiAnime|Consumet|AniwatchAPIURL|ConsumetAPIURL' services/catalog/ | grep -vE '^\s*//' | grep -v 'comment' | wc -l` returns `0`
- The new `make redeploy-catalog` (after this plan's commit + plan 20-03's docker edits) starts the catalog without ANIWATCH_API_URL / CONSUMET_API_URL in the environment
- `curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8081/api/anime/<any-uuid>/scraper/episodes` returns 200 or 503 (whatever the scraper currently returns) — proving the scraper route survived the surgery
</verification>

<success_criteria>
- Two parser directories (hianime, consumet) deleted
- Eight handler funcs deleted from handler/catalog.go (GetHiAnimeEpisodes, GetHiAnimeServers, GetHiAnimeStream, SearchHiAnime, GetConsumetEpisodes, GetConsumetServers, GetConsumetStream, SearchConsumet)
- Eight routes deleted from transport/router.go (six per-anime + two search)
- HiAnimeConfig + ConsumetConfig structs and their Load() usages deleted from config/config.go
- HiAnime + Consumet probes deleted from service/health_checker.go (kodik + animelib probes preserved)
- All wiring deleted from service/catalog.go and cmd/catalog-api/main.go
- Catalog service compiles, vets, and passes its existing test suite
- Zero non-comment Go-code references to hianime/consumet/aniwatch remain in services/catalog/
- Commit staged with required co-authors
</success_criteria>

<output>
After completion, create `.planning/phases/20-cutover/20-02-SUMMARY.md` documenting:
- Exact list of files modified and what was deleted from each
- Final line counts (config.go, router.go, handler/catalog.go, health_checker.go, service/catalog.go, main.go) — expect noticeable shrinkage
- Confirmation that `go build/vet/test` all passed
- Confirmation that the grep gate returned zero
- Note that the catalog must NOT be redeployed yet — docker compose still references the dead aniwatch + consumet containers (plan 20-03 fixes that)
</output>
