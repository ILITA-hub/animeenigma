---
phase: 20-cutover
plan: 04
type: execute
wave: 2
depends_on: ["20-02", "20-03"]
files_modified:
  - frontend/web/src/components/player/HiAnimePlayer.vue
  - frontend/web/src/components/player/ConsumetPlayer.vue
  - frontend/web/src/api/client.ts
  - frontend/web/src/views/Anime.vue
  - frontend/web/src/types/preference.ts
  - frontend/web/src/composables/useOverrideTracker.ts
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/ja.json
autonomous: true
requirements:
  - SCRAPER-CUT-05
  - SCRAPER-CUT-07
must_haves:
  truths:
    - "HiAnimePlayer.vue and ConsumetPlayer.vue do not exist on disk"
    - "grep -rE 'HiAnimePlayer|ConsumetPlayer|hiAnimeApi|consumetApi|legacy=1' frontend/web/src/ returns zero matches"
    - "grep -rE \"'hianime'|'consumet'\" frontend/web/src/ returns zero matches in TS code (union types and discriminator literals)"
    - "frontend/web/src/locales/{en,ru,ja}.json contain no tabDebugSuffix key (legacy '(debug)' label removed)"
    - "bun run build succeeds without TypeScript errors"
    - "bunx tsc --noEmit succeeds without errors"
    - "Visiting an anime detail page in dev with ?legacy=1 no longer toggles legacy tabs — only the unified English tab renders"
  artifacts:
    - path: frontend/web/src/components/player/HiAnimePlayer.vue
      provides: "DELETED — must not exist"
      contains: ""
    - path: frontend/web/src/components/player/ConsumetPlayer.vue
      provides: "DELETED — must not exist"
      contains: ""
    - path: frontend/web/src/api/client.ts
      provides: "API client without hiAnimeApi or consumetApi exports; scraperApi preserved"
      min_lines: 400
      contains: "export const scraperApi"
    - path: frontend/web/src/views/Anime.vue
      provides: "Anime detail view without legacy tabs, defineAsyncComponent imports for the two dead players, or ?legacy=1 conditional rendering; EnglishPlayer is the sole English-source player"
      min_lines: 800
      contains: "EnglishPlayer"
    - path: frontend/web/src/types/preference.ts
      provides: "Preference type with player union containing only live values (kodik | animelib | hanime | english)"
      min_lines: 5
    - path: frontend/web/src/composables/useOverrideTracker.ts
      provides: "PlayerName type without 'hianime' or 'consumet'"
      min_lines: 20
  key_links:
    - from: frontend/web/src/views/Anime.vue
      to: frontend/web/src/components/player/EnglishPlayer.vue
      via: "defineAsyncComponent import + single EnglishPlayer tag for the English source"
      pattern: "EnglishPlayer"
    - from: frontend/web/src/views/Anime.vue
      to: frontend/web/src/api/client.ts
      via: "imports only scraperApi for English-source data — no hiAnimeApi or consumetApi"
      pattern: "import.*scraperApi"
    - from: frontend/web/src/types/preference.ts
      to: frontend/web/src/composables/useOverrideTracker.ts
      via: "PlayerName + WatchPreference.player share the same closed union"
      pattern: "PlayerName"
---

<objective>
Delete every frontend reference to HiAnimePlayer.vue, ConsumetPlayer.vue, hiAnimeApi, consumetApi, and the ?legacy=1 debug flag. Per ROADMAP success criteria 4 + 5 and 20-CONTEXT.md decisions: the two legacy player components, their API client exports, the union-type values 'hianime' / 'consumet' carried for legacy/debug, the tabDebugSuffix i18n key (it only labeled the now-deleted legacy tabs), and the ?legacy=1 conditional rendering in Anime.vue must be GONE.

The new unified EnglishPlayer.vue is the survivor — it handles AnimePahe + Gogoanime (the live providers) via scraperApi.

Purpose: One English tab, one component, one API client. After this plan, a user visiting any anime detail page sees a single English source button — no debug toggle, no hidden legacy paths. The TS type system enforces the closure: any future code that types a 'hianime' literal fails compilation.

Output: A frontend bundle (bun run build) that compiles without errors, has no references to the deleted components/APIs, and renders identically for the user except that the ?legacy=1 URL parameter is now a no-op (rather than swapping in dead players).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cutover/20-CONTEXT.md
@CLAUDE.md
@frontend/web/src/api/client.ts
@frontend/web/src/views/Anime.vue
@frontend/web/src/types/preference.ts
@frontend/web/src/composables/useOverrideTracker.ts

<interfaces>
All line references confirmed by grep on 2026-05-12 — executor should not need to re-explore.

frontend/web/src/api/client.ts:
- export const hiAnimeApi block: lines 392-404 (search/getEpisodes/getServers/getStream over /hianime/* routes)
- export const consumetApi block: lines 449-459
- PRESERVE: export const scraperApi block (lines 416-440) — the unified survivor
- Comment to clean at line 408: "Replaces hiAnimeApi + consumetApi end-to-end; those two remain in place (and reachable via ?legacy=1) until Phase 20 cutover." Update to drop the "(and reachable via ?legacy=1) until Phase 20 cutover" clause; the cutover IS this plan.
- Line 284 inside an interface/function: `player: 'kodik' | 'animelib' | 'hianime' | 'consumet' | 'english'` — narrow to `'kodik' | 'animelib' | 'english'` (drop the two legacy literals).

frontend/web/src/views/Anime.vue (1300+ lines):
- defineAsyncComponent imports lines 781-782:
  - `const HiAnimePlayer = defineAsyncComponent(() => import('@/components/player/HiAnimePlayer.vue'))`
  - `const ConsumetPlayer = defineAsyncComponent(() => import('@/components/player/ConsumetPlayer.vue'))`
  - DELETE both lines. PRESERVE the EnglishPlayer defineAsyncComponent at line 786.
- Legacy debug tabs block lines 366-385: a comment block `<!-- Legacy debug tabs — only when ?legacy=1 (SCRAPER-UI-04) -->` followed by two `<button>` elements binding `@click="onUserPickedProvider('hianime')"` and `@click="onUserPickedProvider('consumet')"`. DELETE THE ENTIRE COMMENT-DELIMITED BLOCK including the outer wrapper that gates on `?legacy=1` (search for a `v-if` or `legacyMode` ref nearby).
- Legacy player render branches lines 459-473:
  - `<HiAnimePlayer v-else-if="videoProvider === 'hianime'" .../>`
  - `<ConsumetPlayer v-else-if="videoProvider === 'consumet'" .../>`
  - DELETE both. The EnglishPlayer at line 450 already handles `videoProvider === 'english'`. Verify the v-if/v-else-if chain still works after deletion (per project memory "Vue Template Rules": never place non-conditional elements between v-if and v-else-if). The chain should be: KodikPlayer (v-if 'kodik') -> AnimeLibPlayer (v-else-if 'animelib') -> EnglishPlayer (v-else-if 'english') -> Hanime (etc.).
- videoProvider ref type lines 861-862:
  - `const videoProvider = ref<'kodik' | 'animelib' | 'hianime' | 'consumet' | 'hanime' | 'english'>(...)`
  - Narrow to `'kodik' | 'animelib' | 'hanime' | 'english'`. Also narrow the localStorage cast on the same line.
- onUserPickedProvider signature line 991: narrow `newProvider` arg type to drop 'hianime' and 'consumet'.
- localStorage migration lines 1318-1332:
  - Line 1319: `const savedEn = localStorage.getItem('preferred_en_provider') as 'english' | 'hianime' | 'consumet' | null`
  - Narrow to `'english' | null`. AND add a one-time migration so existing users don't get stuck on a deleted value:
    ```ts
    let savedEnRaw = localStorage.getItem('preferred_en_provider')
    if (savedEnRaw === 'hianime' || savedEnRaw === 'consumet') {
      localStorage.setItem('preferred_en_provider', 'english')
      savedEnRaw = 'english'
    }
    const savedEn = savedEnRaw as 'english' | null
    ```
- All other comments mentioning 'hianime' / 'consumet' / 'legacy' (lines 449, 459, 469, 785, 859, 1318, 1332) — delete or rewrite to remove dead references.
- ?legacy=1 query-string check: search Anime.vue for `legacy=1` and `legacyMode`. Each usage gates the legacy tabs/render branches — delete the conditions and the surrounding markup that depends on them. The simplest safe form: replace any `legacyMode.value` reference with `false` then run the build; TypeScript dead-code analysis identifies the now-unreachable branches; remove the dead branches and the ref declaration.

frontend/web/src/types/preference.ts:
- Line 5: narrow `player` union from `'kodik' | 'animelib' | 'hianime' | 'consumet' | 'hanime' | 'english'` to `'kodik' | 'animelib' | 'hanime' | 'english'`.
- Lines 3-4: rewrite comment to reflect Phase 20 — "The closed union mirrors what's actually rendered: Kodik (RU iframe), AnimeLib (RU MP4), Hanime (adult), English (unified scraper orchestrator). Legacy 'hianime' / 'consumet' values from pre-Phase-20 localStorage are migrated to 'english' on Anime.vue mount."

frontend/web/src/composables/useOverrideTracker.ts:
- Line 6: update comment to drop "HiAnimePlayer, ConsumetPlayer" — replace with "EnglishPlayer" (the live English-source player).
- Line 28: narrow `PlayerName` union from `'kodik' | 'animelib' | 'hianime' | 'consumet' | 'english'` to `'kodik' | 'animelib' | 'english'`.

frontend/web/src/locales/{en,ru,ja}.json:
- grep on 2026-05-12 confirmed: en.json line 147: `"tabDebugSuffix": "(debug)"`
- PRESERVE: `"noEpisodes": "No episodes found on {source}"` (line 107) — EnglishPlayer uses this template with `{source: 'AnimePahe'/'Anitaku'}`.
- PRESERVE: `"tabEnglish": "English"` (line 146) — the survivor label.
- PRESERVE: `"englishNotAvailable": {...}` (lines 152-154) — EnglishPlayer empty-state copy.
- DELETE the `tabDebugSuffix` key from all THREE locale files (en.json, ru.json, ja.json) — its only consumer was the now-deleted legacy debug tabs.
- No "hianime"/"consumet"/"HiAnime"/"Consumet" strings exist in any locale file (verified by grep) — only the `tabDebugSuffix` removal is needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy components, narrow type unions, clean api/client.ts, remove tabDebugSuffix from locales</name>
  <files>frontend/web/src/components/player/HiAnimePlayer.vue, frontend/web/src/components/player/ConsumetPlayer.vue, frontend/web/src/api/client.ts, frontend/web/src/types/preference.ts, frontend/web/src/composables/useOverrideTracker.ts, frontend/web/src/locales/en.json, frontend/web/src/locales/ru.json, frontend/web/src/locales/ja.json</files>
  <action>
**Step 0 (mandatory gate):** Run `bash scripts/cutover-preflight.sh`. If exit code is non-zero, ABORT this plan and report the failure. Do NOT proceed with any deletion.

**Step 1 — Delete the two component files:**
```
git rm frontend/web/src/components/player/HiAnimePlayer.vue
git rm frontend/web/src/components/player/ConsumetPlayer.vue
```

**Step 2 — frontend/web/src/api/client.ts:**
1. Delete the `export const hiAnimeApi = { ... }` block (lines 392-404, including the trailing blank line).
2. Delete the `export const consumetApi = { ... }` block (lines 449-459, including the trailing blank line).
3. Update the scraperApi doc comment at lines 406-415: remove the "(and reachable via ?legacy=1) until Phase 20 cutover" clause; rewrite to "Replaces hiAnimeApi + consumetApi (deleted in Phase 20 cutover, 2026-05)."
4. Find line 284 (search for `'kodik' | 'animelib' | 'hianime' | 'consumet' | 'english'`): narrow to `'kodik' | 'animelib' | 'english'`.

**Step 3 — frontend/web/src/types/preference.ts:**
1. Line 5: narrow the player union from `'kodik' | 'animelib' | 'hianime' | 'consumet' | 'hanime' | 'english'` to `'kodik' | 'animelib' | 'hanime' | 'english'`.
2. Lines 3-4: rewrite the comment block per interfaces section above.

**Step 4 — frontend/web/src/composables/useOverrideTracker.ts:**
1. Line 6: rewrite comment to mention EnglishPlayer instead of HiAnimePlayer + ConsumetPlayer.
2. Line 28: narrow the PlayerName union from `'kodik' | 'animelib' | 'hianime' | 'consumet' | 'english'` to `'kodik' | 'animelib' | 'english'`.

**Step 5 — frontend/web/src/locales/{en,ru,ja}.json:**
For each of the three files, delete the line containing `"tabDebugSuffix": "..."`. JSON formatting rule: if the deleted line is the last key in its object, the preceding line's trailing comma must be removed. Use the Edit tool with surrounding context to avoid breaking the JSON.

Verify each file parses cleanly via Node:
```
node -e "for(const f of ['en','ru','ja']){JSON.parse(require('fs').readFileSync('frontend/web/src/locales/'+f+'.json','utf8'));console.log(f,'OK')}"
```
All three must print OK.

**Step 6 — verification grep:**
```
grep -rIn "hiAnimeApi\|consumetApi" frontend/web/src/ 2>/dev/null   # must be empty
grep -rIn "tabDebugSuffix" frontend/web/src/ 2>/dev/null              # must be empty
test ! -f frontend/web/src/components/player/HiAnimePlayer.vue
test ! -f frontend/web/src/components/player/ConsumetPlayer.vue
```
All checks must pass. Anime.vue still references the deleted symbols at this point — that is expected; Task 2 fixes Anime.vue. Do NOT run `bun run build` yet.
  </action>
  <verify>
    <automated>bash scripts/cutover-preflight.sh || true; ! test -f frontend/web/src/components/player/HiAnimePlayer.vue &amp;&amp; ! test -f frontend/web/src/components/player/ConsumetPlayer.vue &amp;&amp; ! grep -qE "hiAnimeApi|consumetApi" frontend/web/src/api/client.ts &amp;&amp; ! grep -q "tabDebugSuffix" frontend/web/src/locales/en.json &amp;&amp; ! grep -q "tabDebugSuffix" frontend/web/src/locales/ru.json &amp;&amp; ! grep -q "tabDebugSuffix" frontend/web/src/locales/ja.json &amp;&amp; ! grep -qE "'hianime'|'consumet'" frontend/web/src/types/preference.ts &amp;&amp; ! grep -qE "'hianime'|'consumet'" frontend/web/src/composables/useOverrideTracker.ts &amp;&amp; node -e "for(const f of ['en','ru','ja']){JSON.parse(require('fs').readFileSync('frontend/web/src/locales/'+f+'.json','utf8'))}"</automated>
  </verify>
  <done>Two .vue component files deleted; api/client.ts has no hiAnimeApi or consumetApi exports; preference.ts and useOverrideTracker.ts player unions narrowed; tabDebugSuffix removed from all three locale JSON files; all three locales still parse as valid JSON. Anime.vue is intentionally still broken at this checkpoint — Task 2 fixes it.</done>
</task>

<task type="auto">
  <name>Task 2: Surgically rewrite Anime.vue to remove legacy tabs/imports/render branches + ?legacy=1 path, then build/typecheck</name>
  <files>frontend/web/src/views/Anime.vue</files>
  <action>
**Step 1 — Delete defineAsyncComponent imports:**
Edit `frontend/web/src/views/Anime.vue` to remove lines 781-782:
```
const HiAnimePlayer = defineAsyncComponent(() => import('@/components/player/HiAnimePlayer.vue'))
const ConsumetPlayer = defineAsyncComponent(() => import('@/components/player/ConsumetPlayer.vue'))
```
PRESERVE line 786 (`const EnglishPlayer = defineAsyncComponent(...)`).

**Step 2 — Delete legacy debug tabs markup:**
Search for the comment `<!-- Legacy debug tabs — only when ?legacy=1 (SCRAPER-UI-04) -->` (line 366). Delete from that comment line through the closing `</div>` or end of the gated `v-if` block (line 385 approximate). The block contains:
- A wrapping element gated on `?legacy=1` / `legacyMode`
- Two `<button @click="onUserPickedProvider('hianime')">` and `<button @click="onUserPickedProvider('consumet')">` elements
- References to `$t('player.tabDebugSuffix')`

Delete the entire wrapper. If the wrapper was the only consumer of `legacyMode.value` (or whatever ref/computed gates `?legacy=1`), Task Step 5 will catch the now-unused ref.

**Step 3 — Delete legacy player render branches:**
Search for `<HiAnimePlayer` (line ~460) and `<ConsumetPlayer` (line ~470). Delete both tags including their attributes and any closing `</HiAnimePlayer>` / `</ConsumetPlayer>`. The v-if/v-else-if chain MUST remain syntactically valid after these deletions. Per project memory "Vue Template Rules": never place non-conditional elements between v-if and v-else-if. After deletion, visually inspect the chain (KodikPlayer v-if -> AnimeLibPlayer v-else-if -> EnglishPlayer v-else-if -> Hanime v-else-if -> etc.) and confirm no non-conditional element interrupts it. Also delete the surrounding comment lines (`<!-- HiAnime Player (legacy — visible only via ?legacy=1) -->` and `<!-- Consumet Player -->`).

**Step 4 — Narrow videoProvider ref type + localStorage cast:**
Lines 861-862:
```
const videoProvider = ref<'kodik' | 'animelib' | 'hianime' | 'consumet' | 'hanime' | 'english'>(
  (localStorage.getItem('preferred_video_provider') as 'kodik' | 'animelib' | 'hianime' | 'consumet' | 'hanime' | 'english') || 'kodik'
)
```
Replace with:
```
const videoProvider = ref<'kodik' | 'animelib' | 'hanime' | 'english'>(
  (() => {
    const raw = localStorage.getItem('preferred_video_provider')
    // Phase 20 migration: legacy 'hianime'/'consumet' map forward to 'english'
    if (raw === 'hianime' || raw === 'consumet') {
      localStorage.setItem('preferred_video_provider', 'english')
      return 'english'
    }
    return (raw as 'kodik' | 'animelib' | 'hanime' | 'english') || 'kodik'
  })()
)
```

**Step 5 — Narrow onUserPickedProvider:**
Line 991: change the function signature from
```
function onUserPickedProvider(newProvider: 'kodik' | 'animelib' | 'hianime' | 'consumet' | 'english') {
```
to
```
function onUserPickedProvider(newProvider: 'kodik' | 'animelib' | 'english') {
```
(Drop 'hianime' + 'consumet'; ensure 'hanime' presence matches the other narrowed unions — keep it in if it was there originally; the inventory shows it was not in this signature.)

**Step 6 — Narrow + migrate localStorage at lines 1318-1332:**
Replace the `const savedEn = localStorage.getItem(...) as 'english' | 'hianime' | 'consumet' | null` block with the migration sketch from the interfaces section:
```ts
let savedEnRaw = localStorage.getItem('preferred_en_provider')
if (savedEnRaw === 'hianime' || savedEnRaw === 'consumet') {
  localStorage.setItem('preferred_en_provider', 'english')
  savedEnRaw = 'english'
}
const savedEn = savedEnRaw as 'english' | null
```
Then update line 1332's comment `// Phase 16: include 'english' alongside legacy 'hianime'/'consumet' in the saved values.` to `// Phase 20: legacy 'hianime'/'consumet' migrated to 'english' on read (see above).`

**Step 7 — Delete ?legacy=1 plumbing:**
Search Anime.vue for `legacy=1`, `legacyMode`, and any `useRoute().query.legacy` / `route.query.legacy` reads:
```
grep -nE "legacy=1|legacyMode|query\.legacy" frontend/web/src/views/Anime.vue
```
For each match, delete the surrounding ref declaration, computed, or v-if/v-show. Be conservative — if the line is only a comment, also delete the comment. After this step, no `legacy` token should remain in Anime.vue (except possibly in a comment explaining the migration; verify with the final grep gate in Step 8).

**Step 8 — Final grep gate on Anime.vue:**
```
grep -nE "HiAnimePlayer|ConsumetPlayer|hiAnimeApi|consumetApi|legacy=1|legacyMode|'hianime'|'consumet'" frontend/web/src/views/Anime.vue
```
Output must be EMPTY. Per "Grep gate hygiene" — comments in the file are typically prefixed with `//` or `<!--`; the grep pattern is restrictive enough that occasional matches inside `<!-- migration note -->` text won't false-positive unless the comment quotes a code literal. If a match appears inside a doc/migration comment, rewrite the comment to remove the quoted literal (use indirect prose: "the legacy English players" instead of "'hianime'/'consumet'").

**Step 9 — Build + typecheck (the moment of truth):**
```
cd frontend/web && bunx tsc --noEmit
cd frontend/web && bun run build
```
Both must succeed. Common failures:
- "Property 'hianime' does not exist on type union" — means a usage of `'hianime'` literal survived; find and fix.
- "Cannot find module '@/components/player/HiAnimePlayer.vue'" — means a stray import survived; find and fix.
- JSON parse error in locale — means Task 1 Step 5 broke a comma; revisit the locale file.

**Step 10 — Commit:**
Stage all modified frontend files. The two `git rm`s from Task 1 are already staged. Commit with:
```
feat(20-cutover): delete legacy English players + ?legacy=1 path from frontend

Per ROADMAP Phase 20 success criteria 4-5: dead frontend code removed.
- delete frontend/web/src/components/player/HiAnimePlayer.vue
- delete frontend/web/src/components/player/ConsumetPlayer.vue
- delete hiAnimeApi + consumetApi exports from api/client.ts
- narrow player + PlayerName type unions to {kodik,animelib,hanime,english}
- delete legacy debug tabs + ?legacy=1 conditional rendering from Anime.vue
- migrate any pre-existing localStorage 'hianime'/'consumet' values to 'english'
- remove unused tabDebugSuffix i18n key from en/ru/ja locales

Closes: SCRAPER-CUT-05, SCRAPER-CUT-07.
Redis cache purge + deploy + Telegram announcement follow in plan 20-05.
```
Include all three mandatory co-authors per project memory.
  </action>
  <verify>
    <automated>cd frontend/web &amp;&amp; bunx tsc --noEmit 2>&amp;1 | tail -5 &amp;&amp; bun run build 2>&amp;1 | tail -10 &amp;&amp; cd ../.. &amp;&amp; ! grep -qE "HiAnimePlayer|ConsumetPlayer|hiAnimeApi|consumetApi|legacy=1|legacyMode|'hianime'|'consumet'" frontend/web/src/views/Anime.vue</automated>
  </verify>
  <done>Anime.vue compiles cleanly; bun run build produces a production bundle; grep gate on Anime.vue returns empty; no defineAsyncComponent imports for the deleted components remain; localStorage migration handles legacy stored values; commit staged with required co-authors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> frontend bundle | Bundle no longer ships the two legacy player components; bundle size drops. No new untrusted input crossings. |
| user -> URL query string | `?legacy=1` is now ignored (the conditional code reading it is deleted). A malicious link with `?legacy=1` no longer routes the user to a dead code path. |
| frontend -> backend | Frontend no longer issues GET /api/anime/{id}/hianime/* or /api/anime/{id}/consumet/* requests. The backend will return 404 for those routes anyway (per Plan 20-02), so the legacy frontend code would have failed; deleting it eliminates the dead network requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-19 | Tampering | localStorage carries 'hianime' / 'consumet' values that crash post-cutover client | mitigate | Steps 4 and 6 explicitly add localStorage migrations that map legacy values to 'english' on read and rewrite the stored value. No user is stranded on a dead provider after upgrade. |
| T-20-20 | Denial of Service | TypeScript build fails after partial edits; deploy is blocked | mitigate | Task 2 Step 9 gates the commit on `bunx tsc --noEmit && bun run build`. Plan does not commit a broken bundle. |
| T-20-21 | Information Disclosure | Deleted code may have leaked anime IDs in console logs | accept | The two deleted components logged anime IDs (their MAL IDs) on errors — public information already. No PII or auth tokens were exposed. Git history retains the code for forensic review. |
| T-20-22 | Tampering | v-if/v-else-if chain breaks after deletion | mitigate | Project memory Vue Template Rules explicitly cited in Task 2 Step 3. Build step (Step 9) catches any chain breakage as a Vue template compilation error. |
| T-20-23 | Repudiation | Cannot reconstruct the deleted Vue components | mitigate | Single commit; git revert restores them. The .planning/phases/16-* SUMMARYs already document why these components existed in the first place. |
| T-20-24 | Spoofing | A page-level XSS attacker could previously force `?legacy=1` to render an attacker-controlled flow | accept | Pre-existing risk; the legacy components are now gone so the surface is gone. No active exploitation observed. |

ASVS L1 review: V13 API & client security — bundle surface reduced; V1 architecture — fewer distinct render paths; V11 cryptography — N/A (no crypto changes). No high severity threats. Plan approved.
</threat_model>

<verification>
- bash scripts/cutover-preflight.sh exit 0 BEFORE this plan executed (else aborted at Task 1 Step 0)
- ls frontend/web/src/components/player/ contains neither HiAnimePlayer.vue nor ConsumetPlayer.vue
- grep -rIn -E 'HiAnimePlayer|ConsumetPlayer|hiAnimeApi|consumetApi|legacy=1' frontend/web/src/ returns zero lines
- bunx tsc --noEmit succeeds with no errors
- bun run build succeeds and emits a production bundle
- All three locale JSON files (en/ru/ja) parse cleanly via JSON.parse
- A manual smoke test of an anime detail page (loaded via dev server post-deploy) shows the English tab renders EnglishPlayer; visiting the same URL with ?legacy=1 appended has no visible effect
</verification>

<success_criteria>
- HiAnimePlayer.vue and ConsumetPlayer.vue files deleted
- hiAnimeApi + consumetApi exports deleted from api/client.ts; scraperApi preserved intact
- Player + PlayerName + WatchPreference.player type unions narrowed to live values only
- Legacy debug tabs markup deleted from Anime.vue
- Legacy player render branches deleted from Anime.vue
- ?legacy=1 plumbing (refs, computed, conditions) deleted from Anime.vue
- localStorage migration maps 'hianime' and 'consumet' values to 'english' on read for both `preferred_video_provider` and `preferred_en_provider`
- tabDebugSuffix key deleted from en.json, ru.json, ja.json
- bunx tsc --noEmit passes
- bun run build passes
- All three locale JSON files parse cleanly
- Commit staged with required co-authors
</success_criteria>

<output>
After completion, create `.planning/phases/20-cutover/20-04-SUMMARY.md` documenting:
- Exact deletions from Anime.vue (line ranges + before/after line counts)
- The localStorage migration code (both for preferred_video_provider and preferred_en_provider)
- That the bundle builds cleanly and is ready for deploy (plan 20-05 handles the actual redeploy + Redis purge + announcement)
- A diff of the player union types (before/after) so future code-search hits land on this commit
</output>
