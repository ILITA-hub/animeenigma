---
phase: 24-en-reconnect
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/issues/scraper-provider-verification-2026-05-19.md
autonomous: false
requirements:
  - SCRAPER-HEAL-20
must_haves:
  truths:
    - "gogoanime end-to-end pipeline against Frieren (MAL 52991) returns episodes, servers, and a fetchable stream URL"
    - "animepahe end-to-end pipeline against Frieren returns episodes, servers, and a fetchable stream URL (Referer-gated playback acceptable; in-browser proxy handles Referer)"
    - "animekai is either green end-to-end OR formally disabled via SCRAPER_DEGRADED_PROVIDERS env var so the orchestrator fall-through is exercised cleanly"
    - "Verdict log at docs/issues/scraper-provider-verification-2026-05-19.md exists with a verdict matrix, dated 2026-05-19+, and is committed to origin/main"
    - "If any provider fails AND is not formally disabled, Wave 1+2+3 work pauses until recovered (hard gate per D2)"
  artifacts:
    - path: "docs/issues/scraper-provider-verification-2026-05-19.md"
      provides: "Provider verification log with curl pipeline, raw responses, and verdict matrix"
      contains: "## Verdict Matrix"
  key_links:
    - from: "docs/issues/scraper-provider-verification-2026-05-19.md"
      to: "services/scraper (orchestrator + provider clients)"
      via: "curl against http://localhost:8087/scraper/* endpoints (or the gateway path)"
      pattern: "curl -s .*/scraper/.*frieren"
---

<objective>
Execute a hard pre-flight gate that verifies every registered English scraper provider (gogoanime, animepahe, animekai) end-to-end against Frieren (MAL 52991) BEFORE any frontend or backend file is touched. Result is a committed verdict log at `docs/issues/scraper-provider-verification-2026-05-19.md`.

Purpose: Per D2 (CONTEXT.md), restoring the EN tab in front of broken providers ships a clickable EN tab to nothing. The operator demanded "test each provider" before any other work — this plan is that gate. If gogoanime or animepahe fails, all downstream Phase 24 work pauses until recovered or formally disabled via `SCRAPER_DEGRADED_PROVIDERS`. animekai is allowed to fail-as-disabled (the orchestrator's `ErrProviderDown` fall-through covers it).

Output: A markdown file under `docs/issues/` (per D7 — verification log lives in `docs/issues/`, not `.planning/`) documenting the curl pipeline, raw responses (or excerpts), and a Verdict Matrix table showing per-provider pass/fail with a stream URL or fail reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-en-reconnect/24-CONTEXT.md
@.planning/milestones/v3.1-REQUIREMENTS.md
@docs/superpowers/specs/2026-05-19-english-scraper-reconnect-design.md
@docs/superpowers/plans/2026-05-19-english-scraper-reconnect.md
@services/scraper/cmd/scraper-api/main.go
@services/scraper/internal/config/config.go

<interfaces>
<!-- Scraper service HTTP contract (current, verified 2026-05-19) -->
<!-- The scraper service runs on port 8087 inside docker-compose; gateway proxies
     `/api/anime/{id}/scraper/*` to it. For verification, prefer hitting the
     gateway path (production-equivalent) on http://localhost:8000 or the
     scraper service directly on http://scraper:8087 from within the docker
     network (or expose via `make ps`). Provider selection is via the
     `?provider=` query param or auto-orchestrated when omitted. -->

Endpoints (per services/scraper):
- GET /scraper/anime/{id}/episodes[?provider=<name>]      -> [{episode_id, number, title}]
- GET /scraper/anime/{id}/servers/{episode_id}[?provider] -> [{server_id, name, embed_url}]
- GET /scraper/anime/{id}/stream/{episode_id}/{server}[?provider] -> {stream_url, type, headers}
- GET /scraper/health                                     -> {providers:[{name, status, last_check}]}

Provider names: gogoanime, animepahe, animekai

Env var (services/scraper/internal/config/config.go:32):
  SCRAPER_DEGRADED_PROVIDERS=comma,separated,lowercase  // skips listed providers from orchestrator candidates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the verdict-log markdown file with curl pipeline template</name>
  <files>docs/issues/scraper-provider-verification-2026-05-19.md</files>
  <action>Create the verdict-log markdown file with the following sections (per D7 — file lives in docs/issues/, not .planning/): (1) Header with date 2026-05-19, target anime "Frieren: Beyond Journey's End — MAL 52991", and link back to 24-CONTEXT.md SCRAPER-HEAL-20 requirement. (2) "Test Environment" subsection capturing the docker-compose stack version (`make ps` output excerpt) and the current value of SCRAPER_DEGRADED_PROVIDERS env var (read from docker/.env or `make logs-scraper | head -20`). (3) "Curl Pipeline" subsection — three shell blocks (one per provider) of the form `curl -s "$BASE/api/anime/52991/scraper/episodes?provider=gogoanime"`, `.../scraper/servers/{episode_id}?provider=gogoanime`, `.../scraper/stream/{episode_id}/{server}?provider=gogoanime`. Use Frieren's Shikimori ID 52991 (Shikimori IDs = MAL IDs, per MEMORY.md). For the BASE URL, use `http://localhost:8000` (gateway → scraper) so the verification matches the production-equivalent path users will hit. (4) Empty "## Verdict Matrix" table with columns: Provider | Episodes? | Servers? | Stream URL Returned? | Stream URL Fetchable? | Disposition. (5) Empty "## Raw Responses (Excerpts)" subsection. Do NOT fill the matrix or raw responses in this task — that's Task 2.</action>
  <verify>test -f docs/issues/scraper-provider-verification-2026-05-19.md &amp;&amp; grep -q "## Verdict Matrix" docs/issues/scraper-provider-verification-2026-05-19.md &amp;&amp; grep -q "MAL 52991" docs/issues/scraper-provider-verification-2026-05-19.md</verify>
  <done>Scaffold file exists at docs/issues/scraper-provider-verification-2026-05-19.md with the five sections, but the verdict matrix and raw-responses sections are still empty (to be filled by Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Execute the curl pipeline against gogoanime + animepahe + animekai and record results</name>
  <files>docs/issues/scraper-provider-verification-2026-05-19.md</files>
  <action>Run the three curl pipelines authored in Task 1, in order: gogoanime → animepahe → animekai. For each provider: (a) Hit `/api/anime/52991/scraper/episodes?provider=&lt;name&gt;` via the gateway on localhost:8000. (b) Pick episode 1 from the response. Hit `/api/anime/52991/scraper/servers/{episode_id}?provider=&lt;name&gt;`. (c) Pick the first server. Hit `/api/anime/52991/scraper/stream/{episode_id}/{server}?provider=&lt;name&gt;`. (d) `curl -I` the returned `stream_url` (passing the `headers` map from the stream response — particularly `Referer` for animepahe — and the in-browser proxy is irrelevant for the verification, but document Referer-gating in the disposition column). Record HTTP status, content-type, and content-length. (e) Fill the "## Verdict Matrix" row for that provider with PASS/FAIL/N/A in each column. (f) Append a short excerpt (first ~30 lines, redact tokens) of each response under "## Raw Responses (Excerpts)" with a heading like `### gogoanime — episodes`. (g) After all three are done, add a "## Disposition" subsection at the end stating: gogoanime PASS/FAIL, animepahe PASS/FAIL, animekai PASS/FAIL/FALL-THROUGH-OK. Per D2: if gogoanime or animepahe is FAIL, also add an "## Action Required" subsection naming the failing provider, the failure mode, and the operator action (recover the provider, or add it to SCRAPER_DEGRADED_PROVIDERS in docker/.env and `make restart-scraper`). animekai is allowed to fail-as-disabled — note "fall-through verified" if its absence triggers gogoanime/animepahe selection by the orchestrator. The verification must use the gateway path (production-equivalent), NOT bypass to the scraper container directly.</action>
  <verify>grep -E "^\| (gogoanime|animepahe|animekai) \|" docs/issues/scraper-provider-verification-2026-05-19.md | wc -l | grep -q "^3$" &amp;&amp; grep -qE "(PASS|FAIL|FALL-THROUGH-OK)" docs/issues/scraper-provider-verification-2026-05-19.md</verify>
  <done>Verdict Matrix has one row per provider (gogoanime, animepahe, animekai) with a PASS/FAIL verdict per column; raw response excerpts are appended; Disposition subsection summarizes overall pass/fail per D2 rules; Action Required subsection exists if any P0 provider (gogoanime/animepahe) failed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HARD GATE — human review of verdict matrix before Wave 1/2/3 unblocks</name>
  <what-built>Verdict matrix and disposition for gogoanime + animepahe + animekai against Frieren (MAL 52991), written to docs/issues/scraper-provider-verification-2026-05-19.md.</what-built>
  <how-to-verify>
    1. Read docs/issues/scraper-provider-verification-2026-05-19.md end-to-end.
    2. Confirm the Verdict Matrix has 3 rows (one per provider) with realistic PASS/FAIL data — not blank, not "N/A everywhere".
    3. Confirm gogoanime row shows PASS for Episodes + Servers + Stream URL Returned + Stream URL Fetchable. If FAIL: investigate before unblocking.
    4. Confirm animepahe row shows PASS for Episodes + Servers + Stream URL Returned + Stream URL Fetchable (Referer-gated playback is acceptable — disposition column should note "Referer-gated, in-browser proxy handles"). If FAIL: investigate before unblocking.
    5. Confirm animekai row shows EITHER PASS for all columns OR documented "FALL-THROUGH-OK" with evidence that orchestrator routes around it (e.g., omitting `?provider=animekai` and letting orchestrator pick gogoanime/animepahe still returns a working stream).
    6. If any P0 provider (gogoanime or animepahe) is FAIL: do NOT type "approved". Either (a) recover the provider, re-run Task 2, and re-review, OR (b) add the failing provider to `SCRAPER_DEGRADED_PROVIDERS` in `docker/.env`, `make restart-scraper`, re-run Task 2 to confirm orchestrator fall-through still produces a working stream from the surviving provider, and re-review.
    7. Verify the file is staged (`git status` shows it) — Task 4 will commit it.
  </how-to-verify>
  <resume-signal>Type "approved" (gate clears, Wave 1/2/3 unblock) or describe failures (Wave 1/2/3 stay paused).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit the verdict log to origin/main</name>
  <files>docs/issues/scraper-provider-verification-2026-05-19.md</files>
  <action>After Task 3 approval, stage docs/issues/scraper-provider-verification-2026-05-19.md and commit it with message `docs(24): provider verification gate — gogoanime + animepahe + animekai vs Frieren (SCRAPER-HEAL-20)`. Include the three co-authors per MEMORY.md (Claude Opus 4.6, 0neymik0, NANDIorg). Push to origin/main so the verdict log is visible to subsequent Phase 24 plans before they start. Do NOT amend an existing commit; create a new one per CLAUDE.md commit safety rules.</action>
  <verify>git log --oneline -1 -- docs/issues/scraper-provider-verification-2026-05-19.md | grep -qE "SCRAPER-HEAL-20|provider verification"</verify>
  <done>Verdict log committed to origin/main as a new commit with the three co-authors; subsequent Phase 24 plans can git-pull and start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → gateway → scraper service | The verification curls cross the same boundary real user traffic does. Verifying via gateway path (not direct scraper container) catches gateway-side regressions. |
| Scraper service → upstream providers (gogoanime/animepahe/animekai) | Upstream-side outages are the primary risk this plan exists to detect. |
| docs/issues/ markdown log → git → origin/main | The verdict log is committed; it must not contain leaked tokens or PII from raw responses. Task 2 explicitly directs redacting tokens in excerpts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-00-01 | Information Disclosure | Raw response excerpts in verdict log | mitigate | Task 2 instructs redacting tokens (Authorization headers, signed URL params) in excerpts; only first ~30 lines preserved. |
| T-24-00-02 | Repudiation | Verdict log claims a provider is green when it's not | mitigate | Task 3 is a blocking human gate; Task 2 records HTTP status + content-length so the claim is auditable. |
| T-24-00-03 | Denial of Service | Curl pipeline hits upstream providers ~9 times (3 calls × 3 providers) | accept | Negligible upstream traffic; bounded one-time pre-flight, not recurring. |
</threat_model>

<verification>
- Verdict log exists at `docs/issues/scraper-provider-verification-2026-05-19.md` with 5 sections (header, environment, curl pipeline, verdict matrix, raw responses, disposition).
- Verdict Matrix has exactly 3 rows (gogoanime, animepahe, animekai), each with PASS/FAIL data in every column.
- Disposition subsection is consistent with the matrix.
- If any P0 provider (gogoanime/animepahe) failed: Action Required subsection names the failure mode and operator action.
- File is committed to origin/main with SCRAPER-HEAL-20 reference in the commit message.
- Task 3 human gate was cleared with "approved" before Task 4 ran.
</verification>

<success_criteria>
- [ ] `docs/issues/scraper-provider-verification-2026-05-19.md` exists and is committed to origin/main.
- [ ] Verdict Matrix shows gogoanime PASS for all 4 columns OR is formally disabled with documented orchestrator fall-through.
- [ ] Verdict Matrix shows animepahe PASS for all 4 columns OR is formally disabled with documented orchestrator fall-through.
- [ ] Verdict Matrix shows animekai PASS OR FALL-THROUGH-OK.
- [ ] If any P0 provider failed AND is not formally disabled, Wave 1/2/3 work is paused (operator decision in Task 3).
- [ ] SCRAPER-HEAL-20 requirement is verifiably addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-en-reconnect/24-00-SUMMARY.md` documenting: per-provider verdict, any operator interventions (e.g., SCRAPER_DEGRADED_PROVIDERS additions), and the commit SHA of the verdict log.
</output>
