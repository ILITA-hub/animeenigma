---
phase: 24-en-reconnect
plan: 02
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - frontend/web/src/components/player/EnglishPlayer.vue
autonomous: true
requirements:
  - SCRAPER-HEAL-17
must_haves:
  truths:
    - "frontend/web/src/components/player/EnglishPlayer.vue exists with content sourced from git commit 8424e99"
    - "EnglishPlayer.vue's three-phase loader (loadingServers / loadingStream / validatingStream refs) is intact"
    - "All imports resolve against the current codebase: scraperApi/jimakuApi/userApi from @/api/client, useAuthStore, useOverrideTracker, useWatchSession, useWatchPreferences, recsAnalytics utils, SubtitleOverlay, ReportButton, WatchCombo type"
    - "bunx tsc --noEmit reports zero errors in EnglishPlayer.vue"
    - "bunx eslint frontend/web/src/components/player/EnglishPlayer.vue reports zero errors"
    - "Per D6: do NOT swap the HLS library; whatever the restored snapshot uses (video.js + hls.js per git ref) stays canonical"
    - "Per D3: single-tab + in-player Source dropdown design is preserved (NOT multi-tab); dropdown stays single-option (Phase 26 lights it up)"
  artifacts:
    - path: "frontend/web/src/components/player/EnglishPlayer.vue"
      provides: "Single-file Vue component rendering English-source playback for an anime, with three-phase loader, multi-source dropdown infrastructure, ReportButton, SubtitleOverlay, OtherSubsPanel integration"
      contains: "scraperApi.getEpisodes"
      min_lines: 1500
  key_links:
    - from: "frontend/web/src/components/player/EnglishPlayer.vue"
      to: "frontend/web/src/api/client.ts::scraperApi"
      via: "import { scraperApi, jimakuApi, userApi } from '@/api/client'"
      pattern: "scraperApi\\.get(Episodes|Servers|Stream|Health)"
    - from: "frontend/web/src/components/player/EnglishPlayer.vue"
      to: "frontend/web/src/composables/useWatchPreferences.ts"
      via: "import { useWatchPreferences } from '@/composables/useWatchPreferences'"
      pattern: "useWatchPreferences\\("
    - from: "frontend/web/src/components/player/EnglishPlayer.vue"
      to: "frontend/web/src/types/preference.ts::WatchCombo"
      via: "import type { WatchCombo } from '@/types/preference'"
      pattern: "WatchCombo"
---

<objective>
Restore `frontend/web/src/components/player/EnglishPlayer.vue` from git commit `8424e99` (1973 lines — the last known-good state before the 2026-05-18 cleanup deleted it) and reconcile any contract drift inline against the current `scraperApi`, `useWatchPreferences`, `ReportButton`, `SubtitleOverlay`, `OtherSubsPanel`, vue-i18n, and TypeScript-strict contracts. Per D1 (CONTEXT.md), restore-not-rewrite: the 1973-line file is recoverable from git and contains all the polish accumulated through 16-06 + 18-04 + 21-04. Per D6, do NOT re-pick the HLS library — trust what the restored snapshot uses.

Purpose: SCRAPER-HEAL-17 requires the component file to exist with three-phase-loader behavior intact and compile cleanly under post-2026-05-18 type unions. The component is the single largest deliverable in Phase 24 and the foundation Wave 2 plans 03 (i18n) and 04 (Anime.vue rewire) depend on.

Output: A single restored file at `frontend/web/src/components/player/EnglishPlayer.vue` (~1973 lines, drift-reconciled), passing `bunx tsc --noEmit` and `bunx eslint`. No new files. No swap of video player tech. No "v1/v2/simplified" rewrites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-en-reconnect/24-CONTEXT.md
@frontend/web/src/api/client.ts
@frontend/web/src/composables/useWatchPreferences.ts
@frontend/web/src/types/preference.ts
@frontend/web/src/components/player/ReportButton.vue
@frontend/web/src/components/player/SubtitleOverlay.vue
@frontend/web/src/components/player/OtherSubsPanel.vue

<interfaces>
<!-- The restored file (git ref 8424e99) declares these imports (verified 2026-05-19): -->
import { ref, computed, watch, toRef, onMounted, onBeforeUnmount, nextTick } from 'vue'
import { useI18n } from 'vue-i18n'
import videojs from 'video.js'
import 'video.js/dist/video-js.css'
import Hls from 'hls.js'
import { scraperApi, jimakuApi, userApi } from '@/api/client'
import { useAuthStore } from '@/stores/auth'
import { useOverrideTracker } from '@/composables/useOverrideTracker'
import { useWatchSession } from '@/composables/useWatchSession'
import { useWatchPreferences } from '@/composables/useWatchPreferences'
import { findRecentClick, emitRecWatched } from '@/utils/recsAnalytics'
import SubtitleOverlay from './SubtitleOverlay.vue'
import ReportButton from './ReportButton.vue'
import type { WatchCombo } from '@/types/preference'

<!-- The restored file uses these scraperApi methods (extracted from git ref 8424e99): -->
scraperApi.getEpisodes(animeId, provider?)         // returns { data: [{episode_id, number, title}] }
scraperApi.getServers(animeId, episodeId, provider?)  // returns { data: [{server_id, name, ...}] }
scraperApi.getStream(animeId, episodeId, server, provider?)  // returns stream URL + headers
scraperApi.getHealth()                              // returns { data: { providers: [...] } }

<!-- Current scraperApi shape (from frontend/web/src/api/client.ts line 593+) — MUST verify
     these four method signatures match what the restored file calls. If they have drifted
     (e.g., renamed args, dropped getHealth, changed return shape), reconcile INLINE in
     EnglishPlayer.vue per D1 — do NOT change scraperApi (other consumers depend on it). -->

<!-- useWatchPreferences current shape (frontend/web/src/composables/useWatchPreferences.ts):
     The restored file calls useWatchPreferences({ animeId, defaultPlayer, ... }) and uses
     pref.resolvedCombo, pref.preferredScraperProvider, etc. — verify these properties still
     exist in the current composable; if renamed, reconcile EnglishPlayer call sites. -->

<!-- The OtherSubsPanel component name in the restored file may or may not be imported —
     git show 8424e99 line 519-521 shows only SubtitleOverlay + ReportButton. If
     OtherSubsPanel is referenced via <OtherSubsPanel> in the template, add an import
     for it. If not, do NOT add (CONTEXT.md says "OtherSubsPanel" is in the drift surface,
     not that the component must use it). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore EnglishPlayer.vue from git ref 8424e99 verbatim</name>
  <files>frontend/web/src/components/player/EnglishPlayer.vue</files>
  <action>Run `git show 8424e99:frontend/web/src/components/player/EnglishPlayer.vue > frontend/web/src/components/player/EnglishPlayer.vue` to write the 1973-line restored snapshot verbatim to disk. Do not edit it yet. Verify line count is ~1973: `wc -l frontend/web/src/components/player/EnglishPlayer.vue`. This task is intentionally pure-restore with zero edits — Task 2 handles drift reconciliation as a single focused pass.</action>
  <verify>test -f frontend/web/src/components/player/EnglishPlayer.vue &amp;&amp; wc -l &lt; frontend/web/src/components/player/EnglishPlayer.vue | awk '{print $1}' | awk '$1 &gt; 1500 &amp;&amp; $1 &lt; 2100'</verify>
  <done>File exists at the target path with ~1973 lines, content identical to `git show 8424e99:frontend/web/src/components/player/EnglishPlayer.vue`. No edits applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: Diff restored file against current contracts and reconcile drift inline</name>
  <files>frontend/web/src/components/player/EnglishPlayer.vue</files>
  <action>Reconcile any drift between the restored snapshot (from Task 1) and the CURRENT contracts of the 13 imports declared at lines 508-521 of the restored file. For each import, grep the current source-of-truth file and verify the symbol still exists with the expected shape:

1. `vue` (ref, computed, watch, toRef, onMounted, onBeforeUnmount, nextTick) — Vue 3 stable, no drift expected.
2. `vue-i18n` (useI18n) — verify the current package version's `useI18n()` return shape (most likely `{ t, locale }` — verify).
3. `video.js`, `video.js/dist/video-js.css`, `hls.js` — verify they're in package.json (do NOT install if absent; flag a blocker — D6 says trust the snapshot's library choice but if the snapshot picks libs no longer installed, that's drift).
4. `@/api/client::scraperApi` — verify the four methods called by EnglishPlayer (`getEpisodes`, `getServers`, `getStream`, `getHealth`) still exist with the expected arg shapes. If renamed (e.g., `getEpisodes` → `fetchEpisodes`), update the call sites in EnglishPlayer.vue. If a method was dropped (e.g., `getHealth`), the restored file's "health check" block needs to be stubbed to a no-op (returning a hardcoded `{ providers: [] }`) and a `// TODO(Phase 25): re-wire health` comment added. Do NOT modify scraperApi itself.
5. `@/api/client::jimakuApi, userApi` — verify these still exist; same rules as scraperApi.
6. `@/stores/auth::useAuthStore` — verify shape; the restored file uses `auth.user`, `auth.isAuthenticated` — if renamed, update.
7. `@/composables/useOverrideTracker`, `@/composables/useWatchSession`, `@/composables/useWatchPreferences` — verify return shape (especially `useWatchPreferences` which exports `resolvedCombo`, `preferredScraperProvider`, etc. per the restored file's usage at lines 1332-1342 — update call sites if anything changed).
8. `@/utils/recsAnalytics::findRecentClick, emitRecWatched` — verify both functions exist with expected signatures.
9. `./SubtitleOverlay.vue`, `./ReportButton.vue` — verify both components exist (they do per ls). Verify the props passed in the restored file's template still match the current component contracts (esp. ReportButton's `:player-type` and SubtitleOverlay's cue + display props).
10. `@/types/preference::WatchCombo` — verify the type's fields still match what the restored file destructures.

Additionally:
- Per CONTEXT.md (point 4) and D3: the cleanup-removed multi-source-switcher i18n keys (`tabEnglish`, `tabDebugSuffix`, `source`, `sourceUnhealthy`, `sourceSwitchFailed`, `sourceSwitchedAnnouncement`) stay removed. If the restored file references any of these via `t('tabEnglish')` etc., REPLACE the call with a hardcoded fallback string `'English'` or similar reasonable English text and add a `// TODO(Phase 24): replace fallback when i18n key added` comment. The single tab pattern means we don't need a tabEnglish key — the EN tab label comes from `videoTab.english` (added in plan 03). The dropdown's per-server labels can use `player.serverPicker` etc. (also added in plan 03). For now, leave those `t()` calls as-is — plan 03 fills the i18n side; if `t('player.serverPicker')` returns the key literal in dev, the user sees the key string until plan 03 runs (acceptable per Wave 2 parallelism).
- The restored file's `defineProps` block should expose `animeId`, `episodeId` (or whatever current convention is) — verify the prop names match what Anime.vue will pass in plan 04.
- Per D6: do NOT swap video.js for hls.js-only or hls.js-only for Safari-native. Whatever the restored file uses (video.js + hls.js per the import list) is canonical.
- After all reconciliation: run `bunx tsc --noEmit` from `frontend/web/` and fix any TypeScript errors that surface in EnglishPlayer.vue (TypeScript-strict was tightened post-2026-05-18 per CONTEXT.md). Fixes should be MINIMAL — add explicit type annotations, narrow `any` to known shapes, fix nullability — not architecture rewrites. Run `bunx eslint frontend/web/src/components/player/EnglishPlayer.vue` and fix any errors with `--fix` first; only manually fix issues `--fix` cannot resolve.

Do NOT replace any large blocks of the restored file with new logic — per D1, the goal is "restore + reconcile drift", not rewrite. If a drift fix would require >50 lines changed in one section, STOP and surface the discrepancy as a blocker (e.g., scraperApi entirely refactored) rather than rewriting.</action>
  <verify>cd frontend/web &amp;&amp; bunx tsc --noEmit 2>&amp;1 | grep -c "src/components/player/EnglishPlayer.vue" | grep -q "^0$" &amp;&amp; bunx eslint src/components/player/EnglishPlayer.vue --max-warnings 0</verify>
  <done>EnglishPlayer.vue passes `bunx tsc --noEmit` with zero errors mentioning the file, and `bunx eslint` with zero warnings/errors. All 13 imports resolve. Three-phase loader (loadingServers/loadingStream/validatingStream refs) is preserved. Single-tab + in-player dropdown design preserved per D3. Video.js + hls.js library choice preserved per D6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → scraperApi (frontend HTTP client) → gateway → scraper service | EnglishPlayer.vue is the consumer; it makes authenticated requests through scraperApi. No new trust boundary; same as the pre-2026-05-18 state. |
| Browser → user-provided text (e.g., report description) → ReportButton → player service | ReportButton's existing input sanitization is reused via the restored ReportButton.vue contract — no change here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | Restored file may execute stale logic that no longer matches current backend (e.g., calls a dropped scraperApi method) | mitigate | Task 2 explicitly verifies each import against current source-of-truth files and reconciles call sites inline. tsc + eslint gates catch mismatch. |
| T-24-02-02 | Information Disclosure | EnglishPlayer.vue calls jimakuApi (subtitle URLs) — if the restored file logs full responses to console, it could leak signed URLs | accept | The restored file is the same one production ran for ~5 days in May 2026 with no leak reports. No change in logging surface. |
| T-24-02-03 | Denial of Service | Restored file's three-phase loader retries scraperApi calls on failure — could amplify upstream load if scraper is partially down | accept | Behavior is unchanged from the pre-2026-05-18 state; Wave 0's verification gate ensures providers are healthy before this ships. |
</threat_model>

<verification>
- `frontend/web/src/components/player/EnglishPlayer.vue` exists with line count between 1500 and 2100 (allowing reasonable drift reconciliation).
- `bunx tsc --noEmit` from `frontend/web/` reports zero errors in EnglishPlayer.vue.
- `bunx eslint frontend/web/src/components/player/EnglishPlayer.vue --max-warnings 0` is green.
- File contains `scraperApi.getEpisodes` (proof the scraperApi wiring survived restoration).
- File contains `loadingServers`, `loadingStream`, `validatingStream` refs (proof the three-phase loader survived).
- File does NOT import a new HLS library (e.g., `shaka-player`, `dashjs`) — D6 preserved.
- File does NOT instantiate a multi-tab UI — D3 preserved (search for absence of `<div class="tabs">` or multi-tab markers that the cleanup correctly removed).
</verification>

<success_criteria>
- [ ] EnglishPlayer.vue restored from git ref 8424e99 with ~1973 lines.
- [ ] All 13 imports resolve against the current codebase (verified by tsc).
- [ ] Three-phase loader behavior preserved (refs intact).
- [ ] Video.js + hls.js library choice preserved (D6).
- [ ] Single-tab + in-player dropdown design preserved (D3).
- [ ] No scope reduction ("v1/v2/simplified") introduced during reconciliation (D1).
- [ ] SCRAPER-HEAL-17 is verifiably addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-en-reconnect/24-02-SUMMARY.md` documenting: per-import drift findings (which imports needed reconciliation and what changed), final line count, tsc output before/after reconciliation, eslint output, and any TODO markers added for plan 03 (i18n keys) or future phases.
</output>
