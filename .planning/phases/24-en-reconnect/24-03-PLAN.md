---
phase: 24-en-reconnect
plan: 03
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/ja.json
autonomous: true
requirements:
  - SCRAPER-HEAL-19
must_haves:
  truths:
    - "All three locale files contain identical key sets — `bun run lint:i18n` shows `Missing keys: 0`"
    - "The 6 new keys per locale exist: videoTab.english, player.englishEmpty, player.englishUnavailable, player.serverPicker, player.categorySub, player.categoryDub"
    - "Removed-by-cleanup keys (tabEnglish, tabDebugSuffix, source, sourceUnhealthy, sourceSwitchFailed, sourceSwitchedAnnouncement) stay absent in all three locales — D3 design constraint"
    - "Existing keys (in 'player', 'nav', etc. namespaces) are untouched — this plan only ADDS keys, never edits or removes"
  artifacts:
    - path: "frontend/web/src/locales/en.json"
      provides: "English UI strings consumed by vue-i18n's t() helper"
      contains: "\"english\""
    - path: "frontend/web/src/locales/ru.json"
      provides: "Russian UI strings"
      contains: "\"english\""
    - path: "frontend/web/src/locales/ja.json"
      provides: "Japanese UI strings"
      contains: "\"english\""
  key_links:
    - from: "frontend/web/src/views/Anime.vue (plan 04)"
      to: "frontend/web/src/locales/{en,ru,ja}.json::videoTab.english"
      via: "t('videoTab.english') call in the EN tab button label"
      pattern: "videoTab\\.english"
    - from: "frontend/web/src/components/player/EnglishPlayer.vue (plan 02)"
      to: "frontend/web/src/locales/{en,ru,ja}.json::player.englishEmpty / player.englishUnavailable / player.serverPicker / player.categorySub / player.categoryDub"
      via: "t('player.englishEmpty') etc. in the empty-state markup and Source dropdown"
      pattern: "player\\.(englishEmpty|englishUnavailable|serverPicker|categorySub|categoryDub)"
---

<objective>
Re-add 6 i18n keys to each of the 3 locale files (`en.json`, `ru.json`, `ja.json`) consumed by the restored EnglishPlayer (plan 02) and the re-mounted EN tab in Anime.vue (plan 04). All three locales must end with parity (zero missing keys per `bun run lint:i18n`).

Purpose: Per SCRAPER-HEAL-19 acceptance, the 6 keys `videoTab.english`, `player.englishEmpty`, `player.englishUnavailable`, `player.serverPicker`, `player.categorySub`, `player.categoryDub` are re-added in all three locales. Per D3 and CONTEXT.md point 4, the cleanup-removed multi-source-switcher keys (tabEnglish, tabDebugSuffix, source, sourceUnhealthy, sourceSwitchFailed, sourceSwitchedAnnouncement) stay removed — the restored EnglishPlayer uses a single-tab + in-player dropdown, NOT a multi-tab UI.

Output: Three JSON files with 6 new keys each (one new top-level namespace `videoTab` introduced with one key `english`; five keys added inside the existing `player` namespace). No edits to existing keys; no key removals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-en-reconnect/24-CONTEXT.md
@frontend/web/src/locales/en.json
@frontend/web/src/locales/ru.json
@frontend/web/src/locales/ja.json

<interfaces>
<!-- Current i18n layout (verified 2026-05-19 against en.json):
     - Top-level "player" namespace exists (line ~165) with keys like player.quality, player.subtitles,
       player.dub, player.sub, player.servers, player.raw.tab, player.subtitlePicker.label,
       player.otherSubs.title, etc. ADD 5 new keys at this top-level "player" level (NOT nested
       inside player.raw or player.otherSubs):
         player.englishEmpty
         player.englishUnavailable
         player.serverPicker
         player.categorySub
         player.categoryDub
     - Top-level "videoTab" namespace does NOT exist. CREATE it with one key:
         videoTab.english
       Place it adjacent to existing top-level UI-section namespaces ("nav", "hero", "home", "player", etc.).
       Order does not affect runtime — pick a position alphabetically near "videoTab" if alphabetical
       ordering is observed (it is NOT strictly observed in current en.json; just keep it readable).
     - The cleanup-removed keys MUST NOT be added back:
         tabEnglish, tabDebugSuffix, source, sourceUnhealthy, sourceSwitchFailed, sourceSwitchedAnnouncement
       (These belonged to the multi-tab UI deleted on 2026-05-18; the restored single-tab design
       does not need them.)
     - lint:i18n is the npm script defined in frontend/web/package.json. After edits, run
       `bun run lint:i18n` and expect `Missing keys: 0`. -->

<!-- Suggested translations (English-first, then RU and JA): -->
Key                          | EN value                                              | RU value                                          | JA value
videoTab.english             | "English"                                             | "Английский"                                       | "英語"
player.englishEmpty          | "No English episodes available yet. Try Kodik or AnimeLib." | "Английские эпизоды пока недоступны. Попробуйте Kodik или AnimeLib." | "英語のエピソードはまだありません。Kodik または AnimeLib をお試しください。"
player.englishUnavailable    | "English playback is temporarily unavailable. Try another player." | "Английское воспроизведение временно недоступно. Попробуйте другой плеер." | "英語再生は一時的に利用できません。他のプレイヤーをお試しください。"
player.serverPicker          | "Source"                                              | "Источник"                                         | "ソース"
player.categorySub           | "Sub"                                                 | "Субтитры"                                         | "字幕"
player.categoryDub           | "Dub"                                                 | "Озвучка"                                          | "吹替"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 player.* keys to all three locale files</name>
  <files>frontend/web/src/locales/en.json, frontend/web/src/locales/ru.json, frontend/web/src/locales/ja.json</files>
  <action>For each of en.json, ru.json, ja.json, locate the top-level "player" namespace (it exists in all three; verified for en.json near line 165). Inside that namespace's object (NOT nested under player.raw, player.subtitlePicker, or player.otherSubs — those are existing sub-objects), add five new key-value pairs near the existing top-level player.* entries (e.g., near player.sub / player.dub / player.servers): `englishEmpty`, `englishUnavailable`, `serverPicker`, `categorySub`, `categoryDub`. Use the per-locale string values from the table in &lt;interfaces&gt; above. Preserve existing JSON formatting: 2-space indent (matches current file), no trailing commas (JSON spec), strings double-quoted. After editing each file, run `bunx --bun python3 -c 'import json,sys; json.load(open(sys.argv[1]))' frontend/web/src/locales/&lt;file&gt;.json` (or any JSON-validity command available) to confirm the file still parses as valid JSON. Do NOT touch any other keys in any of the three files. Do NOT add the deleted multi-source-switcher keys (tabEnglish etc.) — per D3 they stay removed.</action>
  <verify>jq -r '.player.englishEmpty // empty, .player.englishUnavailable // empty, .player.serverPicker // empty, .player.categorySub // empty, .player.categoryDub // empty' frontend/web/src/locales/en.json | grep -c "." | grep -q "^5$" &amp;&amp; jq -r '.player.englishEmpty // empty, .player.englishUnavailable // empty, .player.serverPicker // empty, .player.categorySub // empty, .player.categoryDub // empty' frontend/web/src/locales/ru.json | grep -c "." | grep -q "^5$" &amp;&amp; jq -r '.player.englishEmpty // empty, .player.englishUnavailable // empty, .player.serverPicker // empty, .player.categorySub // empty, .player.categoryDub // empty' frontend/web/src/locales/ja.json | grep -c "." | grep -q "^5$"</verify>
  <done>All three locale files parse as valid JSON. Each contains the five new keys inside the `player` namespace. No existing keys removed or modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add videoTab.english to all three locale files (creating the new top-level namespace)</name>
  <files>frontend/web/src/locales/en.json, frontend/web/src/locales/ru.json, frontend/web/src/locales/ja.json</files>
  <action>For each of en.json, ru.json, ja.json, ADD a new top-level namespace object `"videoTab": { "english": "&lt;value&gt;" }` (per the per-locale values in &lt;interfaces&gt;). Place it adjacent to other top-level UI-section namespaces (e.g., between "player" and the next top-level namespace) for readability. The namespace did NOT exist before — this is a CREATE, not an EDIT. After each edit, run JSON parse validation again. Verify with `jq '.videoTab.english'` on each file.</action>
  <verify>jq -r '.videoTab.english' frontend/web/src/locales/en.json | grep -qE "^(English)$" &amp;&amp; jq -r '.videoTab.english' frontend/web/src/locales/ru.json | grep -qE "Английский" &amp;&amp; jq -r '.videoTab.english' frontend/web/src/locales/ja.json | grep -qE "英語"</verify>
  <done>All three locale files have a top-level `videoTab.english` key with the per-locale value. JSON files parse cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Run lint:i18n to verify parity across all three locales</name>
  <files>(no file modifications — verification only)</files>
  <action>From `frontend/web/`, run `bun run lint:i18n` (the i18n parity linter defined in package.json). The expected output is `Missing keys: 0` (or equivalent zero-missing-keys signal). If any keys report missing, the discrepancy is either: (a) typo in one locale's new key (fix the typo and re-run), (b) a pre-existing parity gap that pre-dates this plan (acceptable — flag in the SUMMARY but do not fix in this plan unless trivial), or (c) an existing reference in EnglishPlayer.vue / Anime.vue to one of the removed keys (e.g., `t('tabEnglish')` survived from plan 02). For case (c): grep the codebase for the missing key string, REMOVE the t() reference (replace with hardcoded fallback or delete the surrounding code path per D3). Re-run lint:i18n until `Missing keys: 0`. Do NOT add the deleted multi-source-switcher keys back to the locales (that would un-do D3). If a removed key is needed by code (case c), the FIX is to remove the t() reference, not the missing key entry.</action>
  <verify>cd frontend/web &amp;&amp; bun run lint:i18n 2>&amp;1 | grep -qE "Missing keys: 0|missing: 0|All locales in sync"</verify>
  <done>`bun run lint:i18n` reports `Missing keys: 0`. All three locale files in parity. No t() references to removed multi-source-switcher keys survive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Vue template via t() → locale JSON | The locale strings are read-only static content delivered with the bundle. No trust boundary crossed for user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Spoofing | A malicious translation in ru.json or ja.json that says something different (e.g., directs users to phishing) | mitigate | All three locales are written by Claude with consistent semantic intent per the table in &lt;interfaces&gt;. Translations are committed via the same review path as code; no untrusted-translator surface. |
| T-24-03-02 | Tampering | JSON validity could be broken by trailing commas or unescaped quotes | mitigate | Task 1 + Task 2 verify JSON parses after each edit; Task 3's lint:i18n script depends on JSON parse succeeding. |
</threat_model>

<verification>
- All three locale files (`en.json`, `ru.json`, `ja.json`) contain the 6 new keys (1 in `videoTab`, 5 in `player`).
- All three files parse as valid JSON.
- `bun run lint:i18n` reports `Missing keys: 0`.
- The 6 cleanup-removed keys (tabEnglish, tabDebugSuffix, source, sourceUnhealthy, sourceSwitchFailed, sourceSwitchedAnnouncement) do NOT appear in any of the three locale files.
- No existing keys modified.
</verification>

<success_criteria>
- [ ] 6 keys × 3 locales = 18 new key-value pairs added.
- [ ] `bun run lint:i18n` passes with zero missing keys.
- [ ] Cleanup-removed keys stay absent (D3 preserved).
- [ ] SCRAPER-HEAL-19 is verifiably addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-en-reconnect/24-03-SUMMARY.md` documenting: per-locale diff (6 keys × 3 locales), lint:i18n output, and any pre-existing parity gaps noted but deferred to a future phase.
</output>
