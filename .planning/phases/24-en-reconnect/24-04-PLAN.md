---
phase: 24-en-reconnect
plan: 04
type: execute
wave: 2
depends_on:
  - "02"
  - "03"
files_modified:
  - frontend/web/src/views/Anime.vue
autonomous: true
requirements:
  - SCRAPER-HEAL-18
must_haves:
  truths:
    - "Anime.vue's VALID_LANGUAGES const includes 'en' (currently ['ru', '18+', 'raw'] per line 1120)"
    - "Anime.vue's VALID_PROVIDERS const includes 'english' (currently ['kodik', 'animelib', 'hanime', 'raw'] per line 1127)"
    - "switchLanguage('en') is a valid call path; the function defaults to preferred_en_provider from localStorage, falling back to 'english' when no preference exists"
    - "The videoProvider save watcher persists 'preferred_en_provider' to localStorage when videoLanguage === 'en' (mirrors the existing per-language watchers)"
    - "applyResolvedCombo no longer strips 'en' or 'english' — Wave 2 plan 02 restored EnglishPlayer.vue, so the filter that hid the EN combo path is removed"
    - "Anime.vue mounts EnglishPlayer.vue via v-else-if=\"videoProvider === 'english'\" between the existing 'animelib' and 'hanime' branches"
    - "The EN tab button is rendered in the language tab row between the RU tab (line ~357) and the 18+ tab (line ~368)"
    - "Stale-localStorage sanitization fix from commit ee4ed56 stays in place — users with 'hianime'/'consumet' in localStorage continue to fall back to 'kodik' cleanly"
    - "bunx tsc --noEmit and bunx eslint both green"
  artifacts:
    - path: "frontend/web/src/views/Anime.vue"
      provides: "Top-level anime detail view; hosts the language tab row, provider selector, and player components"
      contains: "videoProvider === 'english'"
  key_links:
    - from: "frontend/web/src/views/Anime.vue (EN tab button)"
      to: "frontend/web/src/views/Anime.vue::switchLanguage('en')"
      via: "@click handler on the EN tab button"
      pattern: "switchLanguage\\('en'\\)"
    - from: "frontend/web/src/views/Anime.vue::switchLanguage('en')"
      to: "frontend/web/src/views/Anime.vue::videoProvider (ref<VideoProvider>)"
      via: "switchLanguage reads localStorage preferred_en_provider, defaults to 'english', sets videoProvider.value"
      pattern: "videoProvider\\.value = "
    - from: "frontend/web/src/views/Anime.vue (template v-else-if branch)"
      to: "frontend/web/src/components/player/EnglishPlayer.vue"
      via: "<EnglishPlayer :anime-id=\"animeId\" ... /> mounted under v-else-if=\"videoProvider === 'english'\""
      pattern: "<EnglishPlayer"
---

<objective>
Re-mount `EnglishPlayer.vue` (restored by plan 02) inside `frontend/web/src/views/Anime.vue` behind a new "English" tab that sits between the existing RU and 18+ tabs. Widen the type-union whitelists (`VALID_LANGUAGES`, `VALID_PROVIDERS`), teach `switchLanguage` and the `videoProvider` save watcher about `'en'`, and remove the `applyResolvedCombo` filter that strips `'en'`/`'english'` (it was correct while EnglishPlayer was missing; it's wrong once plan 02 lands).

Purpose: Per SCRAPER-HEAL-18 acceptance, the EN tab is the user-facing surface point. Without this plan, plan 02 + 03 ship a working component and i18n keys nobody can reach. Per D5 (CONTEXT.md), the EN tab is shown UNCONDITIONALLY — no health-aware tab hiding. EnglishPlayer's own empty-state covers "no providers responding". Per CONTEXT.md point 2, the stale-localStorage sanitization from commit ee4ed56 stays in place — it handles users with `'hianime'`/`'consumet'` stuck in localStorage.

Output: ~30 lines of edits to a single file (`frontend/web/src/views/Anime.vue`): two type-union widenings, two template additions (EN tab button + v-else-if mount), three function/watcher updates (switchLanguage, videoProvider save watcher, applyResolvedCombo), one import line, and the cleanup-removed `applyResolvedCombo` filter for `'en'`/`'english'` deleted. All edits compile under TypeScript-strict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-en-reconnect/24-CONTEXT.md
@frontend/web/src/views/Anime.vue
@frontend/web/src/components/player/EnglishPlayer.vue

<interfaces>
<!-- Current state of frontend/web/src/views/Anime.vue (verified 2026-05-19 via grep): -->

Line ~357: <button @click="switchLanguage('ru')">  // RU tab
Line ~368: <button @click="switchLanguage('18+')"> // 18+ tab
Line ~381: <button @click="switchLanguage('raw')"> // RAW JP tab
// ADD: An "English" tab button between line 357 and line 368, calling switchLanguage('en').

Line 1120: const VALID_LANGUAGES = ['ru', '18+', 'raw'] as const
Line 1121: type VideoLanguage = (typeof VALID_LANGUAGES)[number]
// EDIT line 1120: const VALID_LANGUAGES = ['ru', 'en', '18+', 'raw'] as const

Line 1127: const VALID_PROVIDERS = ['kodik', 'animelib', 'hanime', 'raw'] as const
Line 1128: type VideoProvider = (typeof VALID_PROVIDERS)[number]
// EDIT line 1127: const VALID_PROVIDERS = ['kodik', 'animelib', 'english', 'hanime', 'raw'] as const
// Position 'english' immediately after 'animelib' to keep the visual order RU → EN → 18+ aligned
// with the tab row order.

Line ~477-516: Template provider mount block:
  <KodikPlayer v-if="videoProvider === 'kodik'" ... />
  <AnimeLibPlayer v-else-if="videoProvider === 'animelib'" ... />
  <HanimePlayer v-else-if="videoProvider === 'hanime'" ... />
  <RawPlayer v-else-if="videoProvider === 'raw' && rawProviderEnabled" ... />
// ADD: <EnglishPlayer v-else-if="videoProvider === 'english'" :anime-id="animeId" :episode-id="..." />
// Place it BETWEEN the animelib branch (line ~487) and the hanime branch (line ~497).
// The exact props to pass match plan 02's EnglishPlayer.vue defineProps shape (most likely
// :anime-id, but verify the restored component's prop names and pass them verbatim).

Line ~1302: const combo = { player: videoProvider.value === 'hanime' || videoProvider.value === 'raw' ? 'kodik' : videoProvider.value, ... }
// EDIT: extend the fallback ternary to keep returning 'kodik' for 'hanime' / 'raw' (these are
// experimental/edge-case providers that don't have player-service entries yet) AND ensure
// 'english' is passed through verbatim (do NOT fold 'english' into the 'kodik' fallback —
// plan 01 adds 'english' to ValidPlayers so the backend accepts it directly).
// Updated form: player: videoProvider.value === 'hanime' || videoProvider.value === 'raw' ? 'kodik' : videoProvider.value
// (no change needed if videoProvider.value === 'english' falls through to videoProvider.value
// since 'english' is not in the ternary's hanime|raw set — verify and confirm).

Line 1310 onwards: switchLanguage(lang: VideoLanguage):
  if (lang === '18+') { ... videoProvider.value = 'kodik' ... }
  if (lang === 'raw') { ... videoProvider.value = 'raw' ... }
  if (lang === 'ru') { ... read preferred_ru_provider from localStorage ... }
// ADD: an 'en' branch that reads `preferred_en_provider` from localStorage,
// defaults to 'english' if absent or invalid (e.g., legacy 'hianime'/'consumet'
// from pre-cleanup state — the existing sanitization layer from commit ee4ed56
// in the savedProv/savedLang init block already coerces those — but switchLanguage
// should also be defensive). Then set videoProvider.value = resolvedProvider.

videoProvider save watcher (the watch() that persists user's selection to localStorage when
  videoProvider changes):
  // The watcher currently writes 'preferred_ru_provider' when videoLanguage === 'ru' etc.
  // ADD a branch: when videoLanguage === 'en', write to 'preferred_en_provider'.
  // Use the same shape as the existing 'ru' branch.

Line 1342: function applyResolvedCombo(combo: WatchCombo) { ... }
// The current implementation may include a filter like:
//   if (combo.language === 'en' || combo.player === 'english') return  // strip
// REMOVE that filter line(s). Per CONTEXT.md point 2 — the filter was correct while
// EnglishPlayer was missing; once plan 02 restores the component, the filter must go.
// Verify by grep: `grep -n "language === 'en'\|player === 'english'" frontend/web/src/views/Anime.vue`
// — any conditional that strips/early-returns on 'en'/'english' is the target.

<!-- Stale-localStorage sanitization (commit ee4ed56, lines ~1123-1124, 1130-1131): -->
const videoLanguage = ref<VideoLanguage>(
  (VALID_LANGUAGES as readonly string[]).includes(_savedLang ?? '') ? (_savedLang as VideoLanguage) : 'ru'
)
const videoProvider = ref<VideoProvider>(
  (VALID_PROVIDERS as readonly string[]).includes(_savedProv ?? '') ? (_savedProv as VideoProvider) : 'kodik'
)
// These two ternaries handle legacy stale strings cleanly — DO NOT modify them.
// Once VALID_LANGUAGES grows 'en' and VALID_PROVIDERS grows 'english', users whose
// localStorage already contained 'en' or 'english' (from the pre-2026-05-18 era) will
// see the EN tab/provider come back as their default — correct behavior per CONTEXT.md.
// Users with 'hianime'/'consumet' continue to fall back to 'kodik' cleanly — those strings
// are not in either whitelist.

<!-- Import section near the top of <script setup>: -->
// Add: import EnglishPlayer from '@/components/player/EnglishPlayer.vue'
// Place adjacent to the other player imports (KodikPlayer, AnimeLibPlayer, HanimePlayer, RawPlayer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen type unions and add EnglishPlayer import</name>
  <files>frontend/web/src/views/Anime.vue</files>
  <action>Make three edits to frontend/web/src/views/Anime.vue: (a) Edit line 1120: change `const VALID_LANGUAGES = ['ru', '18+', 'raw'] as const` to `const VALID_LANGUAGES = ['ru', 'en', '18+', 'raw'] as const`. Position 'en' between 'ru' and '18+' to match the visual tab order. (b) Edit line 1127: change `const VALID_PROVIDERS = ['kodik', 'animelib', 'hanime', 'raw'] as const` to `const VALID_PROVIDERS = ['kodik', 'animelib', 'english', 'hanime', 'raw'] as const`. Position 'english' immediately after 'animelib'. (c) Add `import EnglishPlayer from '@/components/player/EnglishPlayer.vue'` to the import list near the top of `&lt;script setup&gt;`, adjacent to other player component imports (KodikPlayer, AnimeLibPlayer, HanimePlayer, RawPlayer). Do NOT touch the videoLanguage / videoProvider ref initializers (lines 1123-1124, 1130-1131) — the sanitization layer from commit ee4ed56 stays in place and starts accepting 'en' / 'english' automatically once the whitelists widen. Verify each edit with `grep`.</action>
  <verify>grep -E "'ru', 'en', '18\+', 'raw'" frontend/web/src/views/Anime.vue &amp;&amp; grep -E "'kodik', 'animelib', 'english', 'hanime', 'raw'" frontend/web/src/views/Anime.vue &amp;&amp; grep -E "import EnglishPlayer from '@/components/player/EnglishPlayer\.vue'" frontend/web/src/views/Anime.vue</verify>
  <done>VALID_LANGUAGES contains 'en'; VALID_PROVIDERS contains 'english'; EnglishPlayer import added. Stale-localStorage sanitization untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add EN tab button to the language tab row + EnglishPlayer mount under v-else-if</name>
  <files>frontend/web/src/views/Anime.vue</files>
  <action>Two template edits: (a) Between the RU tab button (currently at line ~357 with `@click="switchLanguage('ru')"`) and the 18+ tab button (line ~368 with `@click="switchLanguage('18+')"`, ADD an "English" tab button with the same markup shape as the RU button. The button's label should use `{{ t('videoTab.english') }}` (the key added by plan 03). The button's @click should be `switchLanguage('en')`. The button's active-state binding should mirror the RU button's pattern (e.g., `:class="videoLanguage === 'en' ? 'active-class' : 'inactive-class'"`). Match all surrounding ARIA / role / aria-pressed attributes the RU button has — accessibility parity per the v3.1 Phase 21 polish that the cleanup preserved. (b) Between the AnimeLibPlayer mount (line ~487 with `v-else-if="videoProvider === 'animelib'"`) and the HanimePlayer mount (line ~497 with `v-else-if="videoProvider === 'hanime'"`), ADD `&lt;EnglishPlayer v-else-if="videoProvider === 'english'" :anime-id="animeId" /&gt;` (or whatever props the restored EnglishPlayer.vue's defineProps actually declares — read that file from plan 02's output and pass each declared prop verbatim. If the restored file expects `:anime-id`, pass that; if it expects additional props like `:initial-episode-id` or `:translations`, pass those too. Do NOT guess — read the defineProps block and mirror it). The `v-else-if` chain order MUST remain: kodik → animelib → english → hanime → raw, with no intermediate non-conditional sibling elements (per MEMORY.md "Vue Template Rules" — non-conditional elements between v-if/v-else-if break the chain).</action>
  <verify>grep -E "switchLanguage\('en'\)" frontend/web/src/views/Anime.vue &amp;&amp; grep -E "v-else-if=\"videoProvider === 'english'\"" frontend/web/src/views/Anime.vue &amp;&amp; grep -E "&lt;EnglishPlayer" frontend/web/src/views/Anime.vue</verify>
  <done>EN tab button present in the language tab row between RU and 18+; EnglishPlayer mounted via v-else-if between animelib and hanime; v-if/v-else-if chain intact (no non-conditional siblings inserted between branches per MEMORY.md Vue Template Rules).</done>
</task>

<task type="auto">
  <name>Task 3: Update switchLanguage, videoProvider save watcher, and remove applyResolvedCombo filter</name>
  <files>frontend/web/src/views/Anime.vue</files>
  <action>Three logic edits: (a) In `switchLanguage(lang)` (around line 1310), add an `if (lang === 'en')` branch following the same pattern as the existing `'ru'` branch: read `localStorage.getItem('preferred_en_provider')`, validate it against VALID_PROVIDERS (defensively against legacy 'hianime'/'consumet'), default to `'english'` if missing or invalid, then set `videoProvider.value = resolvedProvider`. Mirror the surrounding setup (e.g., setting videoLanguage.value = lang, any analytics emit, any preference-save trigger) verbatim from the 'ru' branch — the EN tab should behave functionally identical to RU from the tab-switching perspective. (b) Locate the `watch(videoProvider, ...)` save watcher (the one that writes `localStorage.setItem('preferred_ru_provider', ...)` when videoLanguage === 'ru'). ADD a parallel branch: when `videoLanguage.value === 'en'`, `localStorage.setItem('preferred_en_provider', newValue)`. The watcher already covers 'ru' and possibly '18+'/'raw' — match whatever conditional structure exists and add the 'en' branch alongside. (c) Locate `applyResolvedCombo` (around line 1342) and grep for any filter like `if (combo.language === 'en') return` or `if (combo.player === 'english') return` or `if (['en', 'english'].includes(...)) return`. REMOVE that filter line(s). Per CONTEXT.md point 2, the filter was added during the 2026-05-18 cleanup to prevent the resolved combo from setting an 'en' state that had no UI; with EnglishPlayer restored, the filter is wrong. If no such filter exists (e.g., the cleanup never added one and just let 'en' / 'english' get rejected by the type union), no edit needed for (c) — but confirm via grep. (d) After all edits: `bunx tsc --noEmit` from `frontend/web/` and `bunx eslint frontend/web/src/views/Anime.vue --max-warnings 0` must be green.</action>
  <verify>grep -E "if \(lang === 'en'\)|case 'en':" frontend/web/src/views/Anime.vue &amp;&amp; grep -E "preferred_en_provider" frontend/web/src/views/Anime.vue &amp;&amp; cd frontend/web &amp;&amp; bunx tsc --noEmit 2>&amp;1 | grep -c "src/views/Anime.vue" | grep -q "^0$" &amp;&amp; bunx eslint src/views/Anime.vue --max-warnings 0</verify>
  <done>switchLanguage handles 'en' (defaults to 'english' fallback, reads preferred_en_provider); videoProvider save watcher persists preferred_en_provider when videoLanguage === 'en'; applyResolvedCombo no longer strips 'en'/'english' (or never had the filter — verified); tsc and eslint green on Anime.vue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Anime.vue init block | localStorage values are user-controlled (anyone with access to the device or a malicious extension can write); existing sanitization (commit ee4ed56) gates these via VALID_LANGUAGES.includes() / VALID_PROVIDERS.includes() — widening the whitelists adds two more accepted values but keeps the sanitization gate intact. |
| User → EN tab button → switchLanguage → localStorage write | The user can trigger localStorage writes by clicking the tab. Value written is from a closed set (VALID_LANGUAGES / VALID_PROVIDERS). No injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | Malicious localStorage write of 'en' could redirect users to a broken state if EnglishPlayer.vue is missing | mitigate | Plan 02 ships EnglishPlayer.vue; deployment order (plan 02 + 04 ship together in Wave 2 + Wave 3's redeploy) ensures the v-else-if branch's referenced component always exists. The sanitization gate falls back to 'kodik' / 'ru' on a missing component (because VALID_PROVIDERS check is the only gate — if the import resolves at build time, the runtime path works). |
| T-24-04-02 | Information Disclosure | The EN tab being visible unconditionally (D5) means every user — including anonymous — sees it. Could surface a confusing "click me but nothing happens" if providers are all down. | accept | EnglishPlayer's empty-state covers this ("No English episodes available..."). D5 explicitly defers health-aware hiding to future polish; the empty-state is functionally equivalent. |
| T-24-04-03 | Denial of Service | If switchLanguage('en') fires a scraperApi.getHealth() prefetch on each click and providers are slow, repeated tab clicks could amplify backend load | accept | EnglishPlayer's internal debouncing (preserved from git ref 8424e99 — the restored file already had this) gates back-to-back getHealth calls. No new amplification surface from this plan. |
</threat_model>

<verification>
- `VALID_LANGUAGES` contains `'en'` and `VALID_PROVIDERS` contains `'english'`.
- `import EnglishPlayer from '@/components/player/EnglishPlayer.vue'` is present.
- EN tab button exists in the language tab row between RU and 18+, with `@click="switchLanguage('en')"` and `{{ t('videoTab.english') }}` label.
- `<EnglishPlayer v-else-if="videoProvider === 'english'" ... />` mounted between animelib and hanime branches.
- `switchLanguage('en')` reads `preferred_en_provider` from localStorage with a defensive fallback to `'english'`.
- videoProvider save watcher persists `preferred_en_provider` when videoLanguage === 'en'.
- `applyResolvedCombo` does not strip `'en'`/`'english'` (filter removed or never existed).
- Stale-localStorage sanitization from commit ee4ed56 (the videoLanguage/videoProvider ref initializers' ternary `includes()` checks) is untouched and unchanged.
- `bunx tsc --noEmit` and `bunx eslint` both green on Anime.vue.
</verification>

<success_criteria>
- [ ] EN tab visible between RU and 18+; clicking it switches to videoLanguage='en'.
- [ ] videoProvider defaults to 'english' on first EN-tab click (no localStorage entry yet) and to the persisted value on subsequent clicks.
- [ ] EnglishPlayer.vue mounts under v-else-if when videoProvider === 'english'.
- [ ] Type unions widened; tsc + eslint green.
- [ ] Stale-localStorage sanitization preserved.
- [ ] SCRAPER-HEAL-18 is verifiably addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-en-reconnect/24-04-SUMMARY.md` documenting: exact diff lines for each of the 3 tasks, tsc/eslint output, and any drift between the restored EnglishPlayer's defineProps and the props passed by the v-else-if mount.
</output>
