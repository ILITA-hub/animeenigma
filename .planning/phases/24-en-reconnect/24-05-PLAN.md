---
phase: 24-en-reconnect
plan: 05
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
files_modified:
  - frontend/web/tests/e2e/english-player.spec.ts
  - frontend/web/public/changelog.json
autonomous: false
requirements:
  - SCRAPER-HEAL-17
  - SCRAPER-HEAL-18
  - SCRAPER-HEAL-19
  - SCRAPER-HEAL-20
must_haves:
  truths:
    - "make redeploy-web rebuilds and restarts the web container with the new EnglishPlayer.vue, Anime.vue edits, and locale files baked into the production bundle"
    - "Manual smoke (logged-out user opens https://animeenigma.ru/anime/frieren-beyond-journey-s-end, clicks EN tab, clicks episode 1, sees video play within 20s) succeeds"
    - "Manual smoke (ui_audit_bot logged in via API key) confirms preference save/restore across reloads"
    - "frontend/web/tests/e2e/english-player.spec.ts exists, exercises the logged-out happy path against the production-equivalent deployment, and passes"
    - "/animeenigma-after-update has been invoked — services rebuilt, changelog updated, commits pushed to origin/main"
    - "All four phase requirements (SCRAPER-HEAL-17/18/19/20) are mapped to PASS in this plan's summary"
  artifacts:
    - path: "frontend/web/tests/e2e/english-player.spec.ts"
      provides: "Playwright e2e spec exercising the EN tab happy path against a production-equivalent deployment"
      contains: "page.locator"
    - path: "frontend/web/public/changelog.json"
      provides: "User-facing changelog entry for v3.1 Phase 24 EN-tab restoration"
      contains: "English"
  key_links:
    - from: "frontend/web/tests/e2e/english-player.spec.ts"
      to: "https://animeenigma.ru/anime/frieren-beyond-journey-s-end"
      via: "page.goto in a Playwright test"
      pattern: "frieren"
---

<objective>
Ship Phase 24: redeploy the web container so plan 02/03/04 changes hit production, run two manual smoke flows (logged-out + ui_audit_bot), author a Playwright e2e spec at `frontend/web/tests/e2e/english-player.spec.ts` for regression coverage, update the user-facing changelog, and invoke `/animeenigma-after-update` to commit + push.

Purpose: The four upstream plans (00, 01, 02, 03, 04) leave Phase 24 functionally complete but not yet deployed and not yet covered by an automated regression. This Wave 3 plan bridges to production and creates the safety net so plan 02's restored 1973-line file doesn't silently regress in a future cleanup.

Output: Production deploy + verified user-facing surface + new Playwright spec + changelog entry + commit on origin/main. SCRAPER-HEAL-17/18/19/20 all mapped to PASS verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-en-reconnect/24-CONTEXT.md
@frontend/web/public/changelog.json
@frontend/web/playwright.config.ts
@frontend/web/tests/e2e

<interfaces>
<!-- changelog.json shape (current, verified via CLAUDE.md): the file is loaded by
     LastUpdates.vue in the Changelog tab. Each entry is informative + enthusiastic
     with emojis. Per /animeenigma-after-update skill description. -->

<!-- Playwright config (frontend/web/playwright.config.ts) — uses bunx playwright per
     CLAUDE.md "Frontend Note". The test will hit a production-equivalent baseURL —
     either localhost:80 (web container) or animeenigma.ru (true prod) depending on
     CI environment. For this plan, target the local docker stack (baseURL =
     http://localhost or http://web inside docker network) to keep the test
     self-contained and runnable in CI. -->

<!-- ui_audit_bot account (per CLAUDE.md and project_test_user_pattern.md):
     - Username: ui_audit_bot
     - API Key: UI_AUDIT_API_KEY from docker/.env
     - Password: audit_bot_test_password_2026 (set 2026-04-07; usable via
       /api/auth/login + refresh-cookie semantics)
     - Profile: https://animeenigma.ru/user/ui-audit-bot -->

<!-- Frieren slug: 'frieren-beyond-journey-s-end' on animeenigma.ru
     Frieren MAL/Shikimori ID: 52991 (same — Shikimori IDs = MAL IDs per MEMORY.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: make redeploy-web and verify deployment</name>
  <files>(no file modifications — deployment + smoke verification only)</files>
  <action>Run `make redeploy-web` from the repo root to rebuild the web container with the new EnglishPlayer.vue, Anime.vue edits, and locale files. The build runs `bun run build` inside the container per the existing Dockerfile. After the rebuild completes, wait for `make health` to show web as healthy. Then verify deployment by:
(a) `curl -s http://localhost/anime/frieren-beyond-journey-s-end | grep -qi 'english\|videoTab'` — sanity check that some EN-related string is in the HTML/JS bundle.
(b) `curl -s http://localhost/assets/index-*.js | grep -c "english" | awk '$1 &gt; 0 {exit 0} {exit 1}'` — confirm the bundled JS contains references to 'english' (more reliable than HTML check since most strings are bundled).
(c) Open https://animeenigma.ru/anime/frieren-beyond-journey-s-end in a browser (use the chrome MCP tools if available — first load mcp__claude-in-chrome__tabs_context_mcp via ToolSearch select: per the MCP server instructions) and verify the EN tab is visible between RU and 18+ without logging in. The HTML 5 seconds after page load should have the EN tab button rendered. If MCP browser is unavailable, fall back to a curl on the live production URL and grep for `videoTab.english` or `English` in the response/bundle.
If the redeploy did not pick up the changes (no 'english' in bundle), investigate: most likely cause is the docker build cache. Re-run with `make redeploy-web` after `docker compose -f docker/docker-compose.yml build --no-cache web`. Do NOT proceed to Task 2 until the deployed bundle contains the new code.</action>
  <verify>make health 2>&amp;1 | grep -E "web.*healthy|web.*Up" &amp;&amp; curl -s http://localhost/ -o /dev/null -w "%{http_code}" | grep -q "^200$"</verify>
  <done>Web container redeployed; `make health` reports web healthy; production HTML/JS bundle contains references to the new 'english' / 'videoTab' code paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — logged-out user happy path</name>
  <what-built>Production deployment with EN tab + EnglishPlayer + i18n + backend allow-list. The full Phase 24 surface as observed by an anonymous browser visitor.</what-built>
  <how-to-verify>
    1. Open an INCOGNITO/private browser window (so localStorage is empty — first-time-visitor state).
    2. Navigate to https://animeenigma.ru/anime/frieren-beyond-journey-s-end.
    3. Wait for the page to fully load (poster, title, episode list visible).
    4. Confirm the language tab row shows: RU | English | 18+ | RAW JP (in that order). The "English" label should match the locale (English in EN browser locale; "Английский" in RU locale; "英語" in JA locale).
    5. Click the "English" tab. The provider should switch to 'english' (the in-player Source dropdown should appear; it may be single-option per D3 — that's correct).
    6. Wait for the three-phase loader (loadingServers → loadingStream → validatingStream) to complete. The loader text should localize correctly to whatever browser locale you set.
    7. Click episode 1 (the first item in the episode list). Video should start playing within 20 seconds (per the phase success criterion).
    8. Verify: no console errors (open DevTools), no 422s in Network tab on /api/anime/{id}/scraper/* or /api/users/preferences/{id} calls.
    9. Verify: if you reload the page (F5), the EN tab stays selected (preference persisted via the new switchLanguage('en') save path).
    10. Click the "RU" tab to confirm switching away from EN works (the existing 'kodik' / 'animelib' providers should appear; no broken state).
    11. If ALL of the above pass: type "approved". If ANY fail: describe which step failed, and the test is BLOCKED until the regression is fixed. Common failures and their plan: (a) EN tab missing — re-check plan 04 task 2's template edit; (b) 422 on preferences — re-check plan 01's redeploy of player service; (c) video doesn't play — re-check Wave 0's provider verification (one of gogoanime/animepahe may have gone down since); (d) i18n key shown as literal — re-check plan 03's lint:i18n output.
  </how-to-verify>
  <resume-signal>Type "approved" (Task 3 unblocks) or describe failures (Task 3 stays paused).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke — ui_audit_bot logged-in preference round-trip</name>
  <what-built>Backend allow-list (plan 01) accepts player="english" for preference save; the frontend persists and reloads the EN preference across page refreshes via the player service (not just localStorage).</what-built>
  <how-to-verify>
    1. In the same incognito window (or a fresh one), log in as ui_audit_bot using `audit_bot_test_password_2026` via the normal /api/auth/login flow (refresh cookie path). Username = ui_audit_bot.
    2. Navigate to https://animeenigma.ru/anime/frieren-beyond-journey-s-end.
    3. Click the "English" tab. Pick episode 1. Let video start playing.
    4. Open DevTools → Network. Filter for "preferences". Verify that a PUT to /api/users/preferences/{anime_id} fires within ~5 seconds of the EN tab click, with `player: "english"` in the request body and a 200 response (NOT 422).
    5. Reload the page (F5). Wait for the page to fully load.
    6. Verify: the EN tab is auto-selected on reload (because the player service returned the saved preference). The same episode (ep. 1) should be auto-resumed if watch_history was also persisted (existing behavior, unrelated to Phase 24 but should not regress).
    7. Open DevTools → Application → Local Storage. Verify `preferred_en_provider` is present with value `"english"` (or whatever was selected in the in-player Source dropdown).
    8. Click "Report a Problem" (ReportButton) on the player. Submit a dummy report with description "Phase 24 smoke test". Verify: POST to /api/users/report returns 200 (NOT 422) with `player_type: "english"` in the payload.
    9. If ALL of the above pass: type "approved". If ANY fail: describe the failure. Common failures: (a) 422 on preferences — plan 01 redeploy didn't take effect; (b) preference doesn't restore on reload — useWatchPreferences contract drift in plan 02 reconciliation; (c) report submit 422 — same as (a) but for the report.go map.
  </how-to-verify>
  <resume-signal>Type "approved" (Task 4 unblocks) or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Author Playwright e2e spec at frontend/web/tests/e2e/english-player.spec.ts</name>
  <files>frontend/web/tests/e2e/english-player.spec.ts</files>
  <action>Create a new Playwright spec at frontend/web/tests/e2e/english-player.spec.ts that exercises the logged-out happy path verified manually in Task 2. The spec should: (a) Use the existing test infrastructure pattern from other specs in frontend/web/tests/e2e/ (read at least one neighbor spec, e.g., `player.spec.ts` or similar, to mirror the imports, baseURL handling, and helper conventions). (b) Test 1: "anonymous user can click English tab and play episode 1 of Frieren". Steps: page.goto('/anime/frieren-beyond-journey-s-end'); await page.locator('button:has-text("English")').click(); await expect(page.locator('[data-testid="english-player"], .english-player')).toBeVisible(); await page.locator('button:has-text("Episode 1"), [data-episode="1"]').first().click(); await expect(page.locator('video')).toBeVisible({ timeout: 20000 }); await page.waitForFunction(() =&gt; { const v = document.querySelector('video'); return v && !v.paused && v.currentTime &gt; 0; }, { timeout: 20000 }); — adjust selectors to match the actual EnglishPlayer.vue and Anime.vue DOM structure (after plan 02 + 04 ship, run a quick read on the live deployment to grab the actual class/data-testid attributes). (c) Test 2: "EN tab is visible without authentication". Just navigates to the anime page and asserts the tab is present. (d) Test 3 (optional, if time permits): "switching from RU to EN updates the active provider". Use `await expect(...).toHaveClass(/active|selected/)` patterns. (e) Run `bunx playwright test tests/e2e/english-player.spec.ts --reporter=list` and confirm all tests pass against the local docker stack (baseURL configured in playwright.config.ts). (f) If any test fails on selector specificity but the manual flow worked, adjust selectors only — do NOT loosen assertions to make tests pass falsely. The spec must be a real regression net.</action>
  <verify>test -f frontend/web/tests/e2e/english-player.spec.ts &amp;&amp; cd frontend/web &amp;&amp; bunx playwright test tests/e2e/english-player.spec.ts --reporter=list 2>&amp;1 | grep -qE "passed|✓"</verify>
  <done>Spec file exists; `bunx playwright test english-player.spec.ts` reports all defined tests passing against the local docker stack.</done>
</task>

<task type="auto">
  <name>Task 5: Update changelog.json with the user-facing Phase 24 entry</name>
  <files>frontend/web/public/changelog.json</files>
  <action>Read frontend/web/public/changelog.json (current shape — array of entries with date/title/description, per the LastUpdates.vue contract). Add a new entry at the top of the array (most recent first) with: date = 2026-05-19 (or today's date if later), version = "v3.1 Phase 24" (or whatever versioning the existing file uses — match it), and a description in the project's enthusiastic, emoji-friendly tone per the /animeenigma-after-update skill description and CLAUDE.md. Suggested copy (adjust to match neighbors): "The English-language tab is back! Click the EN tab on any anime page to stream from gogoanime + animepahe with subtitles, source switching, and the three-phase loader you remember. 🎬✨ This restores the surface that the May 2026 backend cleanup accidentally removed — same player you had, freshly reconnected to the orchestrator." Keep total entry length similar to neighbors (don't write an essay). Validate JSON parses with `jq '.' frontend/web/public/changelog.json &gt; /dev/null`.</action>
  <verify>jq -r '.[0] // .entries[0] // .changelog[0]' frontend/web/public/changelog.json | grep -qiE "english|EN tab" &amp;&amp; jq '.' frontend/web/public/changelog.json &gt; /dev/null</verify>
  <done>changelog.json has a new top entry referencing the EN tab restoration; file parses as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 6: Invoke /animeenigma-after-update skill to commit + push</name>
  <files>(no direct file modifications — skill invocation orchestrates lint/build/commit/push)</files>
  <action>Run the `/animeenigma-after-update` skill. Per CLAUDE.md, this skill: (1) lints + builds affected code, (2) redeploys any changed services (already done in Task 1 for web + plan 01 for player; the skill may re-run these — that's fine, idempotent), (3) runs `make health`, (4) updates `frontend/web/public/changelog.json` (already done in Task 5 — the skill may detect the existing entry and skip, or may append; either is acceptable), (5) commits all Phase 24 changes with the three co-authors per MEMORY.md and pushes to origin/main. If the skill fails at the commit step (e.g., pre-commit hook flags an issue), follow CLAUDE.md commit safety rules: do NOT amend; fix the issue, re-stage, and create a NEW commit. The skill should produce a single Phase 24 commit (or a small number of related commits) on origin/main. After the skill returns, run `git log --oneline -5 origin/main` and confirm Phase 24's commit(s) are present.</action>
  <verify>git log --oneline -5 origin/main 2>&amp;1 | grep -iqE "phase 24|EN tab|english.*reconnect|SCRAPER-HEAL-1[789]|SCRAPER-HEAL-20"</verify>
  <done>/animeenigma-after-update completed successfully; Phase 24 changes are committed and pushed to origin/main; co-authors per MEMORY.md are in the commit trailer; changelog entry is live; production deployment matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local docker stack ↔ production traffic | Task 1's `make redeploy-web` only touches the local stack — the v3.0 deployment model treats the local docker stack as production (per project_deployment.md). No separate "staging vs prod" boundary; redeploy IS deploy. |
| Anonymous user → EN tab → scraperApi → gateway → scraper service | Verified end-to-end by Task 2 manual smoke. |
| Logged-in user → preference save → gateway → player service | Verified by Task 3 manual smoke. |
| Playwright spec (Task 4) → local docker stack | Test traffic only; uses the same baseURL as developer e2e runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Tampering | A flaky Playwright spec could be ignored or worked around in future cleanups, reducing the regression net | mitigate | Task 4 explicitly says do NOT loosen assertions to make tests pass. The spec is a real net for the user-facing surface. If a future cleanup deletes EnglishPlayer.vue again (the original 2026-05-18 regression's exact shape), this spec catches it. |
| T-24-05-02 | Information Disclosure | changelog.json is public; entry should not name internal service paths, IDs, or operational details | mitigate | Task 5's suggested copy keeps the entry user-facing — no internal IDs, no provider names beyond "gogoanime + animepahe" which are already public knowledge from the milestones/REQUIREMENTS docs visible on the repo. |
| T-24-05-03 | Repudiation | The commit pushed by /animeenigma-after-update should be traceable to Phase 24 for future audits | mitigate | Task 6 verifies the commit message references Phase 24 / EN tab / SCRAPER-HEAL IDs. Co-authors per MEMORY.md provide attribution. |
</threat_model>

<verification>
- `make redeploy-web` completed; `make health` shows web healthy.
- Manual smoke (Task 2 logged-out + Task 3 ui_audit_bot) both approved.
- `frontend/web/tests/e2e/english-player.spec.ts` exists and `bunx playwright test` passes.
- `frontend/web/public/changelog.json` has a new Phase 24 entry.
- /animeenigma-after-update invoked; Phase 24 commit pushed to origin/main with the three co-authors.
- All four phase requirements (SCRAPER-HEAL-17, SCRAPER-HEAL-18, SCRAPER-HEAL-19, SCRAPER-HEAL-20) are verifiable via the changes shipped across plans 00-05.
</verification>

<success_criteria>
- [ ] Production deployment shows EN tab visible on Frieren detail page for anonymous users.
- [ ] Episode 1 of Frieren plays via the EN tab within 20 seconds.
- [ ] Preference save round-trips through the player service (200 not 422).
- [ ] Playwright spec passes against the deployed stack.
- [ ] Changelog updated; commit pushed; co-authors per MEMORY.md.
- [ ] All four SCRAPER-HEAL-17/18/19/20 requirements have evidence of completion.
</success_criteria>

<output>
After completion, create `.planning/phases/24-en-reconnect/24-05-SUMMARY.md` documenting: deployment timestamp, Task 2 + Task 3 manual smoke outcomes (verbatim "approved" or failure description), Playwright spec test names + pass count, changelog entry text shipped, commit SHA pushed to origin/main, and the Phase 24 requirements PASS matrix (SCRAPER-HEAL-17, -18, -19, -20 each mapped to a plan + verification anchor).
</output>
