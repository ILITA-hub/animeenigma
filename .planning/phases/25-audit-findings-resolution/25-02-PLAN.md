---
phase: 25-audit-findings-resolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/maintenance-prompt.md
autonomous: true
requirements:
  - SCRAPER-HEAL-23
must_haves:
  truths:
    - "`.claude/maintenance-prompt.md` Pattern 7 / Scraper Playability Regression block no longer references the non-existent `cacheStream` symbol (verified via `grep -c 'cacheStream' .claude/maintenance-prompt.md` returning 0)"
    - "The remaining symbol reference is `computeStreamTTL` — the actually-existing function at `services/scraper/internal/providers/gogoanime/cache.go` and `services/scraper/internal/providers/animepahe/cache.go`"
    - "`go test ./internal/classifier/... -count=1` in services/maintenance/ stays GREEN — symbol-stability tests (TestMaintenancePrompt_*) continue to pass under the new content"
    - "No section heading text is changed (Pattern 6, Pattern 7, Scraper Playability Regression headings remain byte-identical) so symbol-stability heading tests don't regress"
    - "Reason-enum coverage (TestMaintenancePrompt_AllReasonsCovered) continues to find all libs/streamprobe.Reason values in the prompt body"
  artifacts:
    - path: ".claude/maintenance-prompt.md"
      provides: "Pattern 7 + Scraper Playability Regression troubleshooting guidance that references only existing scraper-side function names"
      contains: "computeStreamTTL"
  key_links:
    - from: ".claude/maintenance-prompt.md::Scraper Playability Regression / signed_url_expired branch (line ~256)"
      to: "services/scraper/internal/providers/{gogoanime,animepahe}/cache.go::computeStreamTTL"
      via: "Operator guidance directs human/bot reader to the symbol whose body computes signed-URL cache TTL"
      pattern: "computeStreamTTL"
---

<objective>
One-line text fix in `.claude/maintenance-prompt.md` Pattern 7 troubleshooting block (currently line ~256, the `signed_url_expired` branch of the Scraper Playability Regression triage list): replace the stale `cacheStream` symbol reference with the actually-existing scraper-side function name `computeStreamTTL`. The audit's W-INT-02 finding observed the prompt directs operator/bot readers to "search for `cacheStream` / `computeStreamTTL`" — but `cacheStream` does not exist in any non-test scraper source file (verified by grep on 2026-05-19). The symbol-stability test (`services/maintenance/internal/classifier/maintenance_prompt_symbols_test.go`) passes today via slash-alternative OR semantics (computeStreamTTL is found), so CI is green and never alarmed — but human readers following the prompt's guidance hit a dead reference.

Purpose: Per SCRAPER-HEAL-23 acceptance, the human-facing guidance text must reference only symbols that actually exist in the codebase. Per CONTEXT.md, this is a one-line fix; no symbol renames, no behavior changes, no test changes.

Output: A single-line edit to `.claude/maintenance-prompt.md` that removes the dead `cacheStream` reference while preserving `computeStreamTTL`. Verification that all symbol-stability tests in `services/maintenance/internal/classifier/` remain green under the new prompt body.

This plan is intentionally tiny: the audit observation is precise, the fix is surgical, and the test gate is already in place. Wave 1 runs it in parallel with 25-01 (test fix) and 25-03 (handler fix) — no file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-audit-findings-resolution/25-CONTEXT.md
@.planning/milestones/v3.1-MILESTONE-AUDIT.md
@.claude/maintenance-prompt.md
@services/maintenance/internal/classifier/maintenance_prompt_symbols_test.go

<interfaces>
<!-- Current state of .claude/maintenance-prompt.md line ~256 (verified 2026-05-19) -->
// 4. `signed_url_expired` → find the stream-cache TTL constant in
//    `services/scraper/internal/providers/<name>/client.go` (search for
//    `cacheStream` / `computeStreamTTL`) and shorten if the upstream
//    signed-URL TTL is now shorter than ours. Tier: `button_fix`.

<!-- Symbols that actually exist (verified 2026-05-19 via grep -rn) -->
// services/scraper/internal/providers/animepahe/cache.go:39: func computeStreamTTL(...)
// services/scraper/internal/providers/gogoanime/cache.go:46: func computeStreamTTL(...)
// NO file in services/scraper/ defines a `cacheStream` symbol.

<!-- Symbol-stability tests at services/maintenance/internal/classifier/maintenance_prompt_symbols_test.go assert: -->
// 1. .claude/maintenance-prompt.md exists and is > 1KB
// 2. Section headings "### Pattern 6:", "### Pattern 7:", "### Scraper Playability Regression" all present
// 3. Every libs/streamprobe.Reason value (or its documented alias) appears textually in the prompt body
// They do NOT assert the literal "cacheStream" string. Removing it is safe.

<!-- Note: The signed_url_expired branch text also lives in `services/scraper/internal/providers/<name>/client.go` —
     verify the path itself remains valid. computeStreamTTL is in cache.go (sibling file), but the operator search
     `grep -rn computeStreamTTL services/scraper/internal/providers/<name>/` still works regardless of which file
     the symbol lives in. Update the path reference if more precision aids the operator. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit .claude/maintenance-prompt.md to remove the cacheStream reference</name>
  <files>.claude/maintenance-prompt.md</files>
  <action>
Locate the `signed_url_expired` bullet in the Scraper Playability Regression section (currently line ~256 — exact line drifts with prompt edits; anchor by searching for the literal substring `cacheStream`).

Apply this edit:

**Before:**
```
4. `signed_url_expired` → find the stream-cache TTL constant in `services/scraper/internal/providers/<name>/client.go` (search for `cacheStream` / `computeStreamTTL`) and shorten if the upstream signed-URL TTL is now shorter than ours. Tier: `button_fix`.
```

**After:**
```
4. `signed_url_expired` → find the stream-cache TTL helper in `services/scraper/internal/providers/<name>/cache.go` (search for `computeStreamTTL`) and shorten if the upstream signed-URL TTL is now shorter than ours. Tier: `button_fix`.
```

Three substantive changes vs the original:
1. Drop `cacheStream / ` (the dead symbol). The slash-alternative now becomes a single canonical reference.
2. Change `client.go` to `cache.go` because `computeStreamTTL` lives in `cache.go` in both gogoanime and animepahe providers (verified 2026-05-19). The operator's grep should land on the right file directly.
3. Change "constant" to "helper" because `computeStreamTTL` is a function (not a const). Small precision win.

Do NOT modify ANY other text in the prompt — no rewording of other bullets, no heading changes, no new sections. The symbol-stability test asserts heading presence and Reason-enum coverage; touching those triggers a false-positive review concern. Surgical edit only.

After the edit, verify the file still parses cleanly (it's markdown — no parser, but check the rendered Pattern 7 section in any markdown previewer doesn't show ragged formatting).
  </action>
  <verify>! grep -q "cacheStream" .claude/maintenance-prompt.md &amp;&amp; grep -q "computeStreamTTL" .claude/maintenance-prompt.md &amp;&amp; grep -q "### Pattern 6:" .claude/maintenance-prompt.md &amp;&amp; grep -q "### Pattern 7:" .claude/maintenance-prompt.md &amp;&amp; grep -q "### Scraper Playability Regression" .claude/maintenance-prompt.md</verify>
  <done>`cacheStream` is no longer in the prompt; `computeStreamTTL` is; all three required section headings remain byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Run symbol-stability tests to confirm the prompt body still validates</name>
  <files>(no file modifications — verification only)</files>
  <action>
From `services/maintenance/`, run:

```
go test ./internal/classifier/... -run "TestMaintenancePrompt_" -count=1 -v
```

All `TestMaintenancePrompt_*` tests must pass:
- `TestMaintenancePrompt_FilePresentInWorkingDir` (file exists + >1KB)
- `TestMaintenancePrompt_ContainsPatterns6And7` (heading presence)
- `TestMaintenancePrompt_AllReasonsCovered` (every libs/streamprobe.Reason in prompt body)

If any test FAILS:
- File-present test failing → unrelated to this edit (file move?). Investigate before continuing.
- Pattern6And7 test failing → an unintentional edit removed a heading. Revert and redo Task 1 more surgically.
- AllReasonsCovered failing → an unintentional edit removed a reason keyword. Revert and redo Task 1.

If all three pass, also run the full package to catch any neighbor regression:

```
go test ./... -count=1
```

Full `services/maintenance/` package stays green.
  </action>
  <verify>cd services/maintenance &amp;&amp; go test ./internal/classifier/... -run "TestMaintenancePrompt_" -count=1 2&gt;&amp;1 | tail -10 | grep -q "ok" &amp;&amp; cd services/maintenance &amp;&amp; go test ./... -count=1 2&gt;&amp;1 | tail -5 | grep -qv "FAIL"</verify>
  <done>All TestMaintenancePrompt_* tests green; full services/maintenance package green; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt text → maintenance-bot dispatcher | The dispatcher classifies alerts based on reason labels, NOT prompt text. The prompt is human/LLM-facing guidance for `button_fix` triage — text content affects what the bot's Telegram message says, not what code path runs. No behavior change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | Accidentally weakening or removing a Pattern 7 heading the symbol-stability test asserts | mitigate | Task 1 is explicitly surgical (substring-anchored on `cacheStream`); Task 2 verifies all three required headings still present via the existing test suite. |
| T-25-02-02 | Information Disclosure | Pointing operators at the wrong file path | mitigate | The path change `client.go` → `cache.go` is verified by grep evidence in this plan's interfaces section; the operator will land on the actually-correct file. |
| T-25-02-03 | Repudiation | A future refactor renaming `computeStreamTTL` silently breaks the prompt | accept | The symbol-stability test does not currently lock `computeStreamTTL` by name (it locks Reason-enum coverage + headings). Strengthening the test to assert `computeStreamTTL` presence is OUT OF SCOPE for Phase 25 (see CONTEXT.md "Out of scope"). Could be a v3.2 hardening — file as a follow-up if desired. |
</threat_model>

<verification>
- `grep -c cacheStream .claude/maintenance-prompt.md` returns 0.
- `grep -c computeStreamTTL .claude/maintenance-prompt.md` returns at least 1.
- `grep -E "^### (Pattern 6|Pattern 7|Scraper Playability Regression)" .claude/maintenance-prompt.md` returns 3 lines.
- `go test ./internal/classifier/... -run "TestMaintenancePrompt_" -count=1` in `services/maintenance/`: all green.
- `go test ./... -count=1` in `services/maintenance/`: all green.
- `git diff --stat .claude/maintenance-prompt.md` shows changes confined to a single line (or at most 2 if formatting wrapping shifts).
</verification>

<success_criteria>
- [ ] `cacheStream` is no longer in `.claude/maintenance-prompt.md`.
- [ ] `computeStreamTTL` remains.
- [ ] All three required section headings byte-identical.
- [ ] `services/maintenance` test suite green.
- [ ] SCRAPER-HEAL-23 verifiably addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-findings-resolution/25-02-SUMMARY.md` documenting: the exact one-line diff (before/after), the go-test output (last ~10 lines), and a one-sentence anchor to the W-INT-02 audit finding.
</output>
