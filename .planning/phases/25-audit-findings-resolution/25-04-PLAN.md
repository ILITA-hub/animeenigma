---
phase: 25-audit-findings-resolution
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - libs/videoutils/proxy.go
  - docs/issues/README.md
  - docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md
autonomous: false
requirements:
  - SCRAPER-HEAL-21
must_haves:
  truths:
    - "The maintenance-bot end-to-end pipeline (canary → Prometheus alert → Grafana → maintenance webhook → Pattern 7 classifier → Telegram diagnosis) is exercised at least once with a live observable Telegram message emitted to the configured TELEGRAM_ADMIN_CHAT_ID"
    - "The Telegram diagnosis correctly identifies known_pattern=7 (Scraper Provider Schema Drift / cdn_unreachable) AND tier=button_fix AND lists at least one of {libs/videoutils/proxy.go, HLSProxyAllowedDomains} in affected_files"
    - "The rotated hls3 hosts captured at plan-time (specifically cdn-centaurus.com + meadowlarkdesignstudio.cfd as of 2026-05-19; operator MUST re-capture live values at ship-time per CONTEXT.md risk #2) are added to libs/videoutils/proxy.go::HLSProxyAllowedDomains via the maintenance-bot proposal flow OR (if the bot's proposal flow is incomplete) by a human-confirmed manual edit that follows the same review path the bot would have used — never as a silent direct commit"
    - "`git log` shows the commit landing the new allowlist entries was authored by the maintenance-bot (or, if the bot's auto-commit branch is incomplete, the commit message explicitly references the maintenance-bot's Telegram diagnosis as its proximate cause — making the lineage traceable)"
    - "A runbook entry at docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md documents the exact operator steps for future hls3 rotations: trigger canary, observe alert, accept maintenance-bot proposal, verify allowlist commit, redeploy streaming + scraper"
    - "docs/issues/README.md ISS-011 (or a new ISS-012) is updated to point at the runbook"
  artifacts:
    - path: "libs/videoutils/proxy.go"
      provides: "HLSProxyAllowedDomains list with at least 2 new entries (cdn-centaurus.com, meadowlarkdesignstudio.cfd OR the live equivalents at ship time)"
      contains: "cdn-centaurus.com"
    - path: "docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md"
      provides: "Operator runbook for triggering and accepting future hls3 rotation self-heals"
      contains: "scraper_playability_canary"
    - path: "docs/issues/README.md"
      provides: "Issue index updated with ISS-011 closure note (or ISS-012 new entry) referencing the runbook"
      contains: "self-heal-runbook"
  key_links:
    - from: "services/scheduler/internal/transport/router.go::POST /api/v1/jobs/scraper_playability_canary"
      to: "Prometheus rule ScraperPlayabilityRegression → Grafana contact point maintenance-webhook → services/maintenance/internal/transport/webhookHandler"
      via: "Plan 23-03 infrastructure (deferred Task 4 manual smoke now being executed)"
      pattern: "scraper_playability_canary"
    - from: "services/maintenance/internal/classifier::Pattern 7 dispatch"
      to: "Telegram bot message → operator → manual or bot-driven edit to libs/videoutils/proxy.go::HLSProxyAllowedDomains"
      via: "tier=button_fix proposal in the maintenance webhook's classifier output"
      pattern: "button_fix"
---

<objective>
Close BLK-INT-01 (SCRAPER-HEAL-21) by exercising the v3.1 self-heal pipeline end-to-end and using its output to land the rotated hls3 hosts into the allowlist. Per CONTEXT.md D1, the operator does NOT short-circuit with a 1-line direct edit; the whole point of v3.1 is that this rotation must demonstrably flow through canary→alert→classifier→Telegram→accept→commit. This plan runs Plan 23-03's deferred Task 4 manual smoke for real, captures the Telegram diagnosis as evidence of pipeline health, and then either accepts the bot's auto-commit (if Plan 23-03's auto-commit path turned out to be wired) or performs a human-confirmed manual edit that follows the bot's recommendation literally.

Purpose: Per SCRAPER-HEAL-21 acceptance, two things must be true after this plan:
1. **Pipeline proven.** The deferred Plan 23-03 Task 4 smoke runs. Operator observes `ScraperPlayabilityRegression` transition Firing in Grafana. Operator confirms the maintenance webhook receives the alert and the classifier emits a Pattern 7 / button_fix diagnosis to Telegram with the right `affected_files` hint. If any link in the chain breaks, the gap is logged and the broken link becomes an in-scope fix (CONTEXT.md risk #1: "what if the loop is broken" branch — debug + fix becomes part of the phase).
2. **Allowlist updated.** The rotated hls3 hosts (cdn-centaurus.com + meadowlarkdesignstudio.cfd captured at plan-time, OR whatever the live rotation is at ship-time per CONTEXT.md risk #2 — operator re-captures via the canary scan output) land in `libs/videoutils/proxy.go::HLSProxyAllowedDomains`. The commit's authorship/message preserves the lineage so future audits can see "this entry was added via the self-heal loop, not a stealth edit".

Output: A merged commit (auto by maintenance-bot OR human-confirmed manual) adding the rotated hosts to HLSProxyAllowedDomains; a Telegram-message screenshot or transcript embedded in the SUMMARY as evidence; a runbook at `docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md` capturing the steps so the next operator (human or future maintenance bot iteration) doesn't have to rediscover the pipeline; and a `docs/issues/README.md` update tying ISS-011 to the runbook.

**This plan is wave 2 and `autonomous: false`** — it requires operator interaction (manual canary trigger, Telegram observation, accept/reject decision). The execute-phase orchestrator must pause for a human-verify checkpoint at Task 3 (Telegram review) and Task 4 (commit acceptance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-audit-findings-resolution/25-CONTEXT.md
@.planning/milestones/v3.1-MILESTONE-AUDIT.md
@.planning/milestones/v3.1-phases/23-self-maintenance-loop/23-03-alerts-and-maintenance-verify-PLAN.md
@.planning/milestones/v3.1-phases/23-self-maintenance-loop/23-03-SUMMARY.md
@.claude/maintenance-prompt.md
@libs/videoutils/proxy.go
@services/scheduler/internal/transport/router.go
@services/maintenance/internal/transport
@services/maintenance/internal/classifier

<interfaces>
<!-- Pipeline endpoints (verified 2026-05-19) -->
// Scheduler canary trigger:
//   POST http://localhost:8085/api/v1/jobs/scraper_playability_canary
//   Handler: services/scheduler/internal/transport/router.go:50

// Prometheus alert rule (provisioned in Plan 23-03):
//   infra/grafana/alerts/scraper.yaml::ScraperPlayabilityRegression
//   Fires on playability_canary_failures_total{provider,server,anime} OR
//          parser_unplayable_total{reason="cdn_unreachable"} > threshold

// Grafana contact point (provisioned in Plan 23-03):
//   infra/grafana/provisioning/contactpoints.yml::maintenance-webhook
//   → http://host-gateway:8087/api/grafana-webhook

// Maintenance daemon webhook:
//   services/maintenance/internal/transport/webhookHandler
//   BasicAuth required; protected by TestWebhook_AuthRejected_NoSubmit guard
//   Forwards classified payload to Telegram via services/maintenance/internal/telegram

// Telegram destination:
//   TELEGRAM_ADMIN_CHAT_ID env var (set in docker/.env)

// Classifier Pattern 7 detection:
//   services/maintenance/internal/classifier (per .claude/maintenance-prompt.md Pattern 7)
//   Matches reason=cdn_unreachable → tier=button_fix → affected_files includes libs/videoutils/proxy.go

<!-- Allowlist target (verified 2026-05-19) -->
// libs/videoutils/proxy.go:230 var HLSProxyAllowedDomains = []string{...}
// Current entries (per Phase 22 baseline + any subsequent edits):
//   managementadvisory.sbs, exoplanethunting.space, plus default CDNs
// Missing (per audit BLK-INT-01 + DEF-22-01):
//   cdn-centaurus.com, meadowlarkdesignstudio.cfd
//   (operator re-verifies at ship-time; rotation may have advanced)

<!-- Acceptance evidence -->
// 1. Grafana alerting list shows ScraperPlayabilityRegression Firing after canary POST
// 2. Telegram message in the configured chat with Pattern 7 diagnosis
// 3. git log shows allowlist commit; if maintenance-bot auto-commit lane is wired,
//    commit author/co-author includes maintenance-bot identity
// 4. make redeploy-streaming + make redeploy-scraper completes; subsequent canary
//    re-trigger shows playability passing for previously-failing host families
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm pipeline infrastructure is up and capture current state</name>
  <files>(no file modifications — diagnostic capture only)</files>
  <action>
Run these diagnostics from the repo root and record the output. The goal is to establish baseline state BEFORE triggering the canary, so any state transition is observable.

1. **Scheduler reachable, canary route registered:**
   ```
   curl -s http://localhost:8085/health
   curl -sX OPTIONS http://localhost:8085/api/v1/jobs/scraper_playability_canary -w "%{http_code}\n"
   ```
   Expectation: scheduler is healthy, the canary route responds (any non-404 for OPTIONS is fine).

2. **Prometheus alert rule loaded:**
   ```
   curl -s http://localhost:9090/api/v1/rules | jq '.data.groups[].rules[] | select(.name=="ScraperPlayabilityRegression") | {name, state, health}'
   ```
   Expectation: rule exists, state is `inactive` or `pending` (NOT `nodata`).

3. **Grafana contact point linked:**
   ```
   curl -s http://localhost:3004/api/v1/provisioning/contact-points -u admin:$GRAFANA_ADMIN_PASSWORD | jq '.[] | select(.name=="maintenance-webhook")'
   ```
   Expectation: contact point exists with the right URL `http://host-gateway:8087/api/grafana-webhook`.

4. **Maintenance daemon up:**
   ```
   curl -s http://localhost:8087/health
   ```
   Expectation: 200/ok.

5. **Telegram chat ID set:**
   ```
   grep -E "^TELEGRAM_ADMIN_CHAT_ID=" docker/.env | head -1
   ```
   Expectation: a non-empty value (do NOT echo the chat ID itself into committed artifacts — note it as "set" or redact).

6. **Current allowlist contents:**
   ```
   sed -n '/HLSProxyAllowedDomains = \[\]string{/,/^}/p' libs/videoutils/proxy.go
   ```
   Record the current list verbatim in a scratch file (not committed) so the diff after the bot's proposal is unambiguous.

7. **Live canary state — what's failing right now?**
   ```
   curl -s 'http://localhost:9090/api/v1/query?query=playability_canary_failures_total' | jq '.data.result'
   curl -s 'http://localhost:9090/api/v1/query?query=parser_unplayable_total{reason="cdn_unreachable"}' | jq '.data.result'
   ```
   Capture which provider/server/anime tuples are failing and which hosts (if any) show up in scrape logs as cdn_unreachable. This is the live ground truth for which hosts need to be added.

If ANY of steps 1-5 fails, do NOT proceed to Task 2 — the pipeline is broken. Stop and triage:
- Scheduler down → `make logs-scheduler`, `make restart-scheduler`.
- Prometheus rule missing → `make logs-prometheus`, verify `infra/grafana/alerts/scraper.yaml` is mounted.
- Contact point missing → re-run `make restart-grafana` to reload provisioning.
- Maintenance daemon down → `make logs-maintenance`, restart.
- TELEGRAM_ADMIN_CHAT_ID missing → set in docker/.env, restart maintenance daemon.

Record all diagnostic output in `/tmp/25-04-preflight.log` (not committed) and reference the file from the eventual SUMMARY.
  </action>
  <verify>curl -s http://localhost:8085/health 2&gt;&amp;1 | grep -q "ok" &amp;&amp; curl -s http://localhost:8087/health 2&gt;&amp;1 | grep -q "ok" &amp;&amp; curl -s 'http://localhost:9090/api/v1/rules' 2&gt;&amp;1 | grep -q "ScraperPlayabilityRegression"</verify>
  <done>All five infrastructure dependencies are healthy; current allowlist + canary failure state captured in /tmp/25-04-preflight.log.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger canary, observe alert transition, capture failing host(s)</name>
  <files>(no file modifications — observation only)</files>
  <action>
1. **Trigger the canary job manually:**
   ```
   curl -sX POST http://localhost:8085/api/v1/jobs/scraper_playability_canary -w "\nHTTP_CODE=%{http_code}\n"
   ```
   Expect HTTP_CODE=200 (or 202 for async). The job runs against the live provider stack.

2. **Wait for evaluation window (~90-120s):** Prometheus evaluation interval + Grafana alert evaluation interval combined is typically ~90s. Use the bounded poll:
   ```
   for i in {1..60}; do
       state=$(curl -s 'http://localhost:9090/api/v1/query?query=ALERTS{alertname="ScraperPlayabilityRegression"}' | jq -r '.data.result[0].metric.alertstate // "absent"')
       echo "[$(date +%H:%M:%S)] alert state: $state"
       [[ "$state" == "firing" ]] && break
       sleep 5
   done
   ```
   If state never transitions to `firing` within 5 minutes, the canary returned playability=pass on this stack (live providers are unexpectedly healthy — operator may need to *force* a failure by temporarily allowlisting a deliberately broken host or setting `SCRAPER_DEGRADED_PROVIDERS=gogoanime,animepahe,animekai` to force a zero-episode return). This is the CONTEXT.md risk #1 branch — diagnose before continuing.

3. **Confirm Grafana alerts UI shows the alert firing:**
   - Open `http://localhost:3004/alerting/list` (or the production Grafana URL).
   - Filter for `ScraperPlayabilityRegression`. Expect state `Firing`. Capture a screenshot to `/tmp/25-04-grafana-firing.png` (not committed; reference path in SUMMARY).

4. **Capture the failing host(s) from canary output:** Re-query Prometheus:
   ```
   curl -s 'http://localhost:9090/api/v1/query?query=parser_unplayable_total{reason="cdn_unreachable"}' | jq '.data.result[] | {server: .metric.server, provider: .metric.provider, value: .value[1]}'
   ```
   Cross-reference with the scraper service logs to extract the actual hostnames:
   ```
   make logs-scraper 2>&1 | grep -E "cdn_unreachable|domain not allowed|host=" | tail -30
   ```
   Build the authoritative list of hosts to add. Compare to CONTEXT.md's plan-time capture (cdn-centaurus.com, meadowlarkdesignstudio.cfd). If the live rotation has advanced (CONTEXT.md risk #2), USE THE LIVE HOSTS, not the plan-time stale list. Document any divergence in the SUMMARY.
  </action>
  <verify>curl -s 'http://localhost:9090/api/v1/query?query=ALERTS{alertname="ScraperPlayabilityRegression",alertstate="firing"}' 2&gt;&amp;1 | jq -e '.data.result | length &gt; 0' &gt;/dev/null 2&gt;&amp;1</verify>
  <done>Canary triggered, ScraperPlayabilityRegression in Firing state in Grafana, failing hosts captured to scratch notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HARD GATE — operator confirms Telegram diagnosis arrived and is correctly classified</name>
  <what-built>
The maintenance bot pipeline (canary → Prometheus rule → Grafana → maintenance-webhook → classifier → Telegram) was exercised. A diagnosis message should now appear in the configured Telegram chat (TELEGRAM_ADMIN_CHAT_ID).
  </what-built>
  <how-to-verify>
1. Open the Telegram chat for TELEGRAM_ADMIN_CHAT_ID.
2. Confirm a new bot message arrived within the last ~5 minutes referencing `ScraperPlayabilityRegression` or `playability_canary_failures_total` or `parser_unplayable_total{reason=cdn_unreachable}`.
3. In the message body, confirm the classifier output contains:
   - `known_pattern`: must say `Pattern 7` (or `pattern_7` / `7` / `Scraper Provider Schema Drift` / similar identifier — the canonical pattern reference per .claude/maintenance-prompt.md).
   - `tier`: must say `button_fix` (cdn_unreachable maps to button_fix per the prompt's dispatch table item 3a).
   - `affected_files`: must include at least one of `libs/videoutils/proxy.go` OR `HLSProxyAllowedDomains` OR a reasonable functional equivalent (the prompt's Pattern 7 item 5 says "check `libs/videoutils/proxy.go` `HLSProxyAllowedDomains`" — the classifier should echo that).
4. If `known_pattern` is anything OTHER than Pattern 7 (e.g. Pattern 6 / ad_decoy, or "unknown"), the classifier mis-categorized. Triage:
   - Check `services/maintenance/internal/classifier/` for the rule that maps `reason=cdn_unreachable` to Pattern 7.
   - If the rule is correct but the alert payload didn't carry `reason=cdn_unreachable`, check the Prometheus alert rule annotations — they should embed the canary's reason label.
   - DO NOT proceed to Task 4 with a wrong classification. Fix the classifier/rule, redeploy, re-run Task 2.
5. If `tier` is `auto_edit_selectors` instead of `button_fix`, the maintenance bot will attempt an autonomous edit. Per CONTEXT.md "Out of scope" (D5 sibling — autonomous allowlist updates are NOT in Phase 25's trust model), HALT the bot before it commits. cdn_unreachable should always be button_fix per the maintenance-prompt; if it's auto_edit_selectors, that's a misclassification — file as a Phase 25 follow-up before continuing.
6. If `affected_files` does NOT mention `libs/videoutils/proxy.go` / `HLSProxyAllowedDomains`, the classifier's hint is wrong but the operator can still proceed manually. Note the gap in the SUMMARY as a Phase 25 hardening follow-up.

Capture the Telegram message text (redact any private identifiers like chat IDs / user names) into `/tmp/25-04-telegram-diagnosis.txt` (not committed); reference the path from the eventual SUMMARY. If the chat won't allow text copy, take a screenshot to `/tmp/25-04-telegram-diagnosis.png`.

**ABORT CRITERIA — if any of these are true, STOP and re-plan:**
- No Telegram message arrived within 5 minutes after Task 2 alert firing → the maintenance daemon's webhook handler or Telegram client is broken. Triage `make logs-maintenance`. File as a Phase 25 follow-up; the BLK-INT-01 closure cannot proceed via pipeline.
- Telegram message arrived but classified as Pattern 6 (ad_decoy) → reason-label routing is broken. Fix the rule/classifier, re-run.
- Telegram message classified `tier: escalate` and recommends a `SCRAPER_DEGRADED_PROVIDERS` env update instead of an allowlist update → the upstream is dead, not rotated. This isn't BLK-INT-01; it's a different failure mode. Note the divergence in the SUMMARY; the allowlist update is no longer the right fix; consult operator on next steps.

Type `approved-pattern7-buttonfix` to unblock Task 4. Any other input pauses the plan.
  </how-to-verify>
  <output>
After approval, capture the Telegram message text/screenshot to /tmp/25-04-telegram-diagnosis.{txt,png} (not committed) for SUMMARY reference.
  </output>
</task>

<task type="auto">
  <name>Task 4: Apply the bot's proposed allowlist edit (or human-confirmed equivalent)</name>
  <files>libs/videoutils/proxy.go</files>
  <action>
Two paths, depending on whether Plan 23-03's auto-commit branch was wired:

**Path A (preferred — bot auto-commit wired):**
1. Wait ~2 minutes after Task 3 approval for the maintenance bot to push a commit to a feature branch (per Plan 23-03 / maintenance daemon's commit lane).
2. `git fetch --all` and check for a new branch matching `maintenance-bot/heal-blk-int-01-*` or similar pattern (consult `services/maintenance/internal/dispatcher` for the exact branch-naming convention).
3. Review the diff: `git diff main..maintenance-bot/heal-blk-int-01-XXX -- libs/videoutils/proxy.go`. Expect ONLY additions to the `HLSProxyAllowedDomains` slice — no other changes. The new hosts must match the live capture from Task 2.
4. If diff looks correct, fast-forward merge to main: `git checkout main && git merge maintenance-bot/heal-blk-int-01-XXX --ff-only`. Push: `git push origin main`.
5. If the bot's commit author is a generic `Anthropic Maintenance Bot <bot@animeenigma.local>` or similar, preserve that authorship in the merge (no rebase to flatten). The audit trail must show the bot as the agent of the fix.

**Path B (bot auto-commit NOT wired — manual following the bot's recommendation):**
1. Open `libs/videoutils/proxy.go` and locate `HLSProxyAllowedDomains = []string{...}` (currently line ~230).
2. Add the live-captured hosts (from Task 2 scraper log + Task 3 Telegram diagnosis) as string entries, alphabetically ordered to match the existing list's pattern. At plan-time these are `cdn-centaurus.com` and `meadowlarkdesignstudio.cfd`; at ship-time they may be different — USE THE LIVE LIST.
3. Edit example (illustrative — actual host list comes from Task 2/3 capture):
   ```go
   var HLSProxyAllowedDomains = []string{
       // ... existing entries ...
       "managementadvisory.sbs",       // Phase 22 hls3 family
       "exoplanethunting.space",       // Phase 22 hls3 family
       "cdn-centaurus.com",            // Phase 25 BLK-INT-01 hls3 rotation, captured 2026-MM-DD via self-heal pipeline
       "meadowlarkdesignstudio.cfd",   // Phase 25 BLK-INT-01 hls3 rotation, captured 2026-MM-DD via self-heal pipeline
   }
   ```
4. Update the existing companion comment or ISS-011 inline doc reference to point at the new entries' provenance.
5. Commit message MUST reference the maintenance bot's Telegram diagnosis as the proximate cause:
   ```
   fix(allowlist): add hls3 rotation hosts surfaced by self-heal pipeline (BLK-INT-01)

   Live canary trigger on 2026-MM-DD HH:MM produced ScraperPlayabilityRegression
   alert; maintenance-bot Telegram diagnosis identified known_pattern=7,
   tier=button_fix, affected_files=[libs/videoutils/proxy.go]. Applying the
   recommended fix manually because the bot's auto-commit lane is not yet
   wired (Phase 23-03 follow-up).

   Hosts added: cdn-centaurus.com, meadowlarkdesignstudio.cfd

   Refs: SCRAPER-HEAL-21, BLK-INT-01, ISS-011

   Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
   Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
   Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>
   ```

After commit (either path), run:
```
go -C libs/videoutils build ./...
go -C libs/videoutils test ./...
```
Both green. The existing `TestHLSProxyAllowedDomains_HasAnimePaheHosts` lockfile test continues to pass (it asserts the AnimePahe hosts are present; new hosts are additive, no removals).

Then redeploy:
```
make redeploy-streaming
make redeploy-scraper
```
Poll for health on both services within 60s each.

Re-trigger the canary (`curl -X POST http://localhost:8085/api/v1/jobs/scraper_playability_canary`) and wait ~90-120s. Confirm:
- `parser_unplayable_total{reason="cdn_unreachable"}` is no longer incrementing for the newly-allowlisted hosts (run the same Prometheus query as Task 2).
- The Grafana alert transitions back to `Resolved`.

If the alert does NOT resolve, the host names are wrong (typo? rotated again?) or there's a deeper issue. Triage and re-run.
  </action>
  <verify>grep -q "cdn-centaurus.com" libs/videoutils/proxy.go &amp;&amp; cd libs/videoutils &amp;&amp; go build ./... &amp;&amp; go test ./... 2&gt;&amp;1 | tail -5 | grep -q "ok" &amp;&amp; git log -1 --pretty=format:"%s" | grep -qi "BLK-INT-01\|allowlist\|hls3"</verify>
  <done>Allowlist updated with live-captured rotated hosts; commit lineage references the self-heal pipeline; libs/videoutils tests green; streaming + scraper redeployed; canary re-trigger shows the alert resolving.</done>
</task>

<task type="auto">
  <name>Task 5: Write the operator runbook for future hls3 rotations</name>
  <files>docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md, docs/issues/README.md</files>
  <action>
Create `docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md` with these sections:

1. **Title + date:** "hls3 Rotation Self-Heal Runbook — first established 2026-05-19, BLK-INT-01 closure"
2. **When to use:** Every time a future canary alerts with `ScraperPlayabilityRegression` + `reason=cdn_unreachable`, OR every time user reports surface "video plays nothing on EN tab" + scraper logs show "domain not allowed for HLS proxy" entries.
3. **The pipeline (one-paragraph diagram):** scheduler canary cron → parser_unplayable_total / playability_canary_failures_total → Prometheus rule ScraperPlayabilityRegression → Grafana contact point maintenance-webhook → maintenance daemon /api/grafana-webhook → classifier (Pattern 7 / cdn_unreachable / button_fix) → Telegram bot message → operator acks → either auto-commit (if wired) OR manual commit following the bot's recommendation literally.
4. **Operator step-by-step (copy from Tasks 1-4 of THIS plan, condensed):** preflight checks, canary trigger curl, alert observation, Telegram review checklist (known_pattern=7, tier=button_fix, affected_files), commit-or-merge, redeploy streaming + scraper, re-trigger canary, confirm alert resolves.
5. **Abort criteria (copy from Task 3 abort list):** when to STOP and re-plan instead of proceeding.
6. **Provenance check for any future audit:** how to verify a given allowlist entry was added via the self-heal loop (commit author = maintenance-bot OR commit message references Telegram diagnosis). New entries added by direct human edit are policy violations after this runbook exists.
7. **Known limitations:** the maintenance bot's auto-commit lane may or may not be wired at the time the runbook is read; operator falls back to Path B (manual-following-bot-recommendation) if auto-commit is incomplete.
8. **First execution log:** a "Verified 2026-MM-DD by [operator]" line with the actual ship-date and operator name once the runbook's first end-to-end run completes (this plan IS that first run; the line gets filled in by Task 5).

Update `docs/issues/README.md`:
- Locate the ISS-011 entry (VibePlayer Ad-Decoy Poisoning).
- Append a "Closure note" subsection or update its existing status section: "BLK-INT-01 (hls3 host rotation) closed 2026-MM-DD via Phase 25 SCRAPER-HEAL-21. Self-heal runbook established at `docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md`. Future rotations follow the runbook, not direct edits."
- OR (operator's choice) create a new ISS-012 entry "hls3 Host Rotation Self-Heal — Established Runbook" pointing at the runbook file as the canonical source.

Either choice is fine — what matters is that `README.md`'s issue index links to the runbook.

Verify the runbook is self-contained — a NEW operator with no prior Phase 25 context should be able to read it and execute the loop. Test this by asking yourself: "If I had never read 25-04-PLAN.md, would the runbook still tell me everything I need?" If no, expand the runbook.
  </action>
  <verify>test -f docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md &amp;&amp; grep -q "scraper_playability_canary" docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md &amp;&amp; grep -q "Pattern 7" docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md &amp;&amp; grep -q "self-heal-runbook\|BLK-INT-01" docs/issues/README.md</verify>
  <done>Runbook exists at docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md with all 8 sections; docs/issues/README.md links to it; future operators can execute the loop standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintenance bot → production allowlist | The bot proposes; operator accepts. The trust model in Phase 25 is "human-acknowledge-then-apply" (CONTEXT.md "Out of scope" #3 — fully autonomous edits are a separate future spec). Even Path A (auto-commit) requires operator merge-approval; the bot does NOT push directly to main. |
| Canary → live providers | The canary scrapes live upstream sites once per manual trigger (and on cron). No new external trust dependency; the pipeline already exists from Phase 21+23. |
| Operator → Telegram chat | Operator confirms diagnosis via Telegram. The chat is private (TELEGRAM_ADMIN_CHAT_ID is admin-only); message content does not leak user data — only host names and metric labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Tampering | Maintenance bot proposes a malicious allowlist entry (e.g. attacker-controlled CDN) | mitigate | Task 4 mandates operator review of the bot's diff before merge. Path B (manual) is the explicit fallback; Path A still requires `git merge` by a human. No automatic main-branch push. |
| T-25-04-02 | Information Disclosure | Telegram message body could embed canary URL parameters or token fragments | mitigate | Maintenance-bot classifier output is limited to {known_pattern, tier, affected_files, anime_id, server, reason} — no auth tokens. Per Plan 23-03, webhook BasicAuth is also enforced (TestWebhook_AuthRejected_NoSubmit). Telegram chat is admin-only. |
| T-25-04-03 | Denial of Service | Manual canary trigger spam → upstream provider rate-limiting | mitigate | One manual trigger per Task 2/4 run. Existing per-host rate.Limiter at 1 RPS bounds the canary's egress. Operator triggers ≤ 3 times per execution per the plan. |
| T-25-04-04 | Repudiation | A direct human edit to HLSProxyAllowedDomains without going through the pipeline | mitigate | Task 5's runbook explicitly forbids direct edits post-runbook-publication; future audits can grep `git log -p libs/videoutils/proxy.go` for the audit trail to detect violations. |
| T-25-04-05 | Elevation of Privilege | Maintenance bot's BasicAuth credential leaked → attacker submits synthetic alert to land arbitrary allowlist edits | mitigate | Task 4 requires operator review before merge; bot proposes but does not push to main. Even if synthetic alerts trick the classifier, the merge gate is the security boundary. The webhook BasicAuth is also rotated per the standard secret rotation policy (out of scope here; tracked separately). |
| T-25-04-06 | Spoofing | Adversary observes the canary pattern and tries to inject hosts via a spoofed alert payload | mitigate | TestWebhook_AuthRejected_NoSubmit from Plan 23-03 already locks BasicAuth on /api/grafana-webhook; without credentials, the dispatch never fires the Telegram message. |
</threat_model>

<verification>
- Task 1 preflight: scheduler /health=ok, maintenance /health=ok, Prometheus rule exists, contact point linked, TELEGRAM_ADMIN_CHAT_ID set.
- Task 2: canary trigger returned 200/202, alert state transitioned to Firing within ~5min, failing hosts captured.
- Task 3 (human gate): Telegram message arrived, classified Pattern 7 + button_fix + libs/videoutils/proxy.go in affected_files, operator typed approval.
- Task 4: libs/videoutils/proxy.go HLSProxyAllowedDomains has new entries matching the live capture; libs/videoutils tests green; streaming + scraper redeployed; canary re-trigger shows alert resolving.
- Task 5: docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md exists with all 8 sections; docs/issues/README.md links to it.
- `git log libs/videoutils/proxy.go` shows the BLK-INT-01 closure commit with proper attribution (bot author OR commit message references Telegram diagnosis).
- A future operator reading ONLY the runbook can execute the same loop without re-discovering the pipeline.
</verification>

<success_criteria>
- [ ] Self-heal pipeline exercised end-to-end (canary → Grafana → maintenance webhook → Telegram).
- [ ] Telegram diagnosis correctly classified Pattern 7 + button_fix + libs/videoutils/proxy.go.
- [ ] Live-captured rotated hosts added to HLSProxyAllowedDomains via the pipeline-following workflow (Path A bot-auto or Path B manual-following-bot).
- [ ] Commit lineage preserves the audit trail (maintenance-bot author OR commit message references Telegram diagnosis).
- [ ] libs/videoutils tests green; streaming + scraper redeployed.
- [ ] Canary re-trigger shows alert resolving for the newly-allowlisted hosts.
- [ ] Operator runbook published at docs/issues/2026-05-19-hls3-rotation-self-heal-runbook.md with all 8 sections.
- [ ] docs/issues/README.md ISS-011 (or new ISS-012) links to the runbook.
- [ ] SCRAPER-HEAL-21 verifiably addressed.
- [ ] BLK-INT-01 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-findings-resolution/25-04-SUMMARY.md` documenting: the preflight diagnostic excerpt, the canary-trigger timestamp + Prometheus alert state-transition log, the Telegram diagnosis text/screenshot path, the chosen path (A vs B), the exact allowlist diff (before/after), the redeploy timestamps, the resolved-alert evidence, the runbook file link, and a sentence anchoring to BLK-INT-01 / SCRAPER-HEAL-21. Reference the CONTEXT.md risk #1 / risk #2 outcomes (did the pipeline work? Did the rotation advance between plan-time and ship-time?).
</output>
