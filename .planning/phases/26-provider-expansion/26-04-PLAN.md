---
phase: 26-provider-expansion
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - services/scraper/internal/providers/SURVIVOR1/client.go
  - services/scraper/internal/providers/SURVIVOR1/dto.go
  - services/scraper/internal/providers/SURVIVOR1/cache.go
  - services/scraper/internal/providers/SURVIVOR1/doc.go
  - services/scraper/internal/providers/SURVIVOR1/client_test.go
  - services/scraper/internal/providers/SURVIVOR1/testdata/episodes_frieren.json
  - services/scraper/internal/providers/SURVIVOR1/testdata/servers_frieren_ep1.json
  - services/scraper/cmd/scraper-api/main.go
  - services/scraper/internal/config/config.go
autonomous: false
requirements:
  - SCRAPER-HEAL-26
must_haves:
  truths:
    - "**CONDITIONAL PLAN — this plan executes ONLY IF the operator's Decision Gate in `.planning/research/2026-05-19-en-source-survival.md` named a Survivor #1**"
    - "If no Survivor #1 was picked (operator chose 0 survivors in 26-03-PLAN.md Task 4), this plan is SKIPPED and the requirement SCRAPER-HEAL-26 is satisfied by 26-03's research-only outcome — no code changes ship"
    - "The placeholder token `SURVIVOR1` in files_modified is REPLACED with the operator-chosen candidate's lowercase identifier (e.g., `miruro` or `animeowl`) before this plan executes; the executor reads the Decision Gate to resolve the token"
    - "services/scraper/internal/providers/<survivor1>/ exists as a self-contained package implementing domain.Provider (Name, FindID, ListEpisodes, ListServers, GetStream, HealthCheck) — same package shape as 26-01-PLAN.md established for allanime"
    - "var _ domain.Provider = (*<survivor1>.Provider)(nil) compiles in client_test.go (compile-time interface assertion)"
    - "go test ./services/scraper/internal/providers/<survivor1>/... -race -count=2 is fully green; all four Provider methods covered against captured testdata goldens"
    - "main.go registers the survivor1 provider AFTER allanime and BEFORE the animekai gate (resulting order: gogoanime, animepahe, allanime, <survivor1>, [animekai])"
    - "make redeploy-scraper exits 0; docker compose ps shows scraper as healthy; curl http://localhost:8087/scraper/health returns 200 with <survivor1> in the providers array"
    - "End-to-end smoke against Frieren (MAL 52991): curl 'http://localhost:8000/api/anime/52991/scraper/episodes?provider=<survivor1>' returns a non-empty episodes array OR a clean ErrProviderDown fall-through (no panic)"
    - "Re-verification step at task start: the candidate is STILL live as of execution day — same probe pattern from 26-03 Task 2. If the candidate went dark between 26-03 sweep and 26-04 execution, this plan halts and reports the regression. Operator decides: retry later, skip survivor, or replace with Survivor #2."
    - "All upstream HTTP traffic goes through services/scraper/internal/domain.BaseHTTPClient (cross-cutting NF inheritance)"
    - "No anti-bot dependency added: go.mod for services/scraper does NOT gain chromedp, go-rod, utls, tls-client, cloudscraper_go, or flaresolverr"
  artifacts:
    - path: "services/scraper/internal/providers/<survivor1>/client.go"
      provides: "Provider struct implementing domain.Provider for Survivor #1"
      contains: "func New(deps Deps)"
    - path: "services/scraper/internal/providers/<survivor1>/client_test.go"
      provides: "Compile-time interface assertion + table-driven tests against captured testdata fixtures"
      contains: "var _ domain.Provider = (*Provider)(nil)"
    - path: "services/scraper/cmd/scraper-api/main.go"
      provides: "Registration of <survivor1> in candidateProviders between allanime and the animekai gate"
      contains: "<survivor1>.New("
  key_links:
    - from: "services/scraper/internal/providers/<survivor1>/client.go"
      to: "26-03 Decision Gate: .planning/research/2026-05-19-en-source-survival.md::Operator selection"
      via: "The operator's Survivor #1 pick is the only source-of-truth for which candidate this plan implements; the executor reads the Decision Gate to resolve the SURVIVOR1 token in this plan"
      pattern: "Operator selection"
---

<objective>
**CONDITIONAL EXECUTION.** This plan implements Survey Candidate #1 as a third-party `domain.Provider` in the scraper failover pool. It executes ONLY if the operator's Decision Gate at the end of `.planning/research/2026-05-19-en-source-survival.md` named a Survivor #1. If the operator picked zero survivors, this plan is SKIPPED — `gsd-execute-phase` reads the Decision Gate and routes around this plan without error. The placeholder token `SURVIVOR1` in this plan resolves to whichever candidate (e.g., `miruro`, `animeowl`) the operator picked.

Purpose: SCRAPER-HEAL-26's "decision gate operator chose a survivor → implement it" outcome path. The pattern mirrors 26-01-PLAN.md exactly (package layout, Provider interface, BaseHTTPClient, captured testdata goldens, registration block, redeploy + smoke). The only varying ingredients are (a) the candidate's identifier, (b) the candidate's API/HTML scraping shape (lifted from the PoC sketch in 26-03 Task 3), and (c) the orchestrator slot (between allanime and the animekai gate).

**Re-verification requirement (per CONTEXT.md Risks):** Survey candidates can rotate scoping rules or add JS challenges between the 26-03 sweep and this plan's execution. Task 1 below re-runs the 26-03 liveness probe against the survivor; if it now fails, halt and surface to operator instead of pushing forward with stale assumptions.

Output: A new Go package under `services/scraper/internal/providers/<survivor1>/` mirroring the allanime package layout from 26-01, a registration block in main.go that places <survivor1> between allanime and the animekai gate, captured testdata goldens, and verification that redeploy + end-to-end Frieren smoke both pass (OR cleanly fall through if the candidate is upstream-degraded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-provider-expansion/26-CONTEXT.md
@.planning/phases/26-provider-expansion/26-03-PLAN.md
@.planning/research/2026-05-19-en-source-survival.md (REQUIRED — read the Decision Gate to resolve SURVIVOR1, and the PoC sketch for the candidate's FindID / Episodes / Servers / Stream shape)
@.planning/phases/26-provider-expansion/26-01-PLAN.md (REQUIRED — reuse the allanime package shape as the template)
@services/scraper/internal/domain/provider.go
@services/scraper/internal/providers/gogoanime/client.go (for the New/Deps wiring pattern)
@services/scraper/cmd/scraper-api/main.go

<interfaces>
<!-- Same Provider interface as 26-01 -->
type Provider interface {
    Name() string
    FindID(ctx context.Context, ref AnimeRef) (string, error)
    ListEpisodes(ctx context.Context, providerID string) ([]Episode, error)
    ListServers(ctx context.Context, providerID, episodeID string) ([]Server, error)
    GetStream(ctx context.Context, providerID, episodeID, serverID string, category Category) (*Stream, error)
    HealthCheck(ctx context.Context) Health
}

<!-- TARGET candidateProviders order after this plan executes -->
// candidateProviders = ["gogoanime", "animepahe", "allanime", "<survivor1>"]
// with animekai still gated and appended last when cfg.AnimeKai.Enabled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Resolve the SURVIVOR1 placeholder by reading the Decision Gate</name>
  <read_first>
    - .planning/research/2026-05-19-en-source-survival.md (the Decision Gate section)
  </read_first>
  <action>
Read the "Operator selection" line from the Decision Gate. Identify the Survivor #1 name (e.g., "miruro", "animeowl"). Lowercase + alphanumeric-only — this becomes the value of SURVIVOR1 used throughout this plan.

**Halt conditions:**
- If the Decision Gate's Operator selection is empty or still contains the placeholder text, halt with the message "26-03 Decision Gate not filled; execute 26-03 first."
- If the Operator selection is "research-only" (zero survivors picked), halt with the message "Operator picked zero survivors; skipping 26-04. SCRAPER-HEAL-26 satisfied by research-only outcome." This is a clean skip, NOT a failure.
- If the Operator selection has only Survivor #2 named (no #1), proceed but treat the named survivor as the SURVIVOR1 binding for this plan — 26-05 stays unconditional or also skipped.

Write the resolved SURVIVOR1 value to `.planning/phases/26-provider-expansion/26-04-RESOLVED.md` so subsequent tasks have a stable reference. Format:
```
SURVIVOR1=<lowercase-identifier>
Source: .planning/research/2026-05-19-en-source-survival.md Operator selection
Resolved: <ISO 8601 timestamp>
```
  </action>
  <verify>test -f .planning/phases/26-provider-expansion/26-04-RESOLVED.md &amp;&amp; grep -E "^SURVIVOR1=[a-z0-9]+$" .planning/phases/26-provider-expansion/26-04-RESOLVED.md</verify>
  <acceptance_criteria>
    - 26-04-RESOLVED.md exists with a SURVIVOR1=<value> line where <value> is a lowercase alphanumeric identifier
    - If the Decision Gate said "research-only", the execution halts with a clean skip message and 26-04-RESOLVED.md is NOT created
    - The resolved value matches the operator's choice in the Decision Gate (cross-verifiable by re-reading the research artifact)
  </acceptance_criteria>
  <done>SURVIVOR1 placeholder is bound to a concrete candidate identifier, or this plan cleanly skips because the operator picked zero survivors.</done>
</task>

<task type="auto">
  <name>Task 1: Re-verify the survivor is still live + unprotected (anti-stale-PoC gate)</name>
  <read_first>
    - .planning/research/2026-05-19-en-source-survival.md (Task 2 probe results from 26-03 for SURVIVOR1)
    - .planning/phases/26-provider-expansion/26-04-RESOLVED.md (the bound identifier from Task 0)
  </read_first>
  <action>
Re-run the exact curl probes from 26-03 Task 2 for SURVIVOR1. Confirm:
1. HTTP liveness probe still returns 200 (or the same status documented at sweep time).
2. No Cloudflare challenge body keywords have appeared since the sweep (`cf-mitigated`, `Challenge`, `<title>Just a moment...</title>`).
3. The candidate's public listing endpoint (the one called by FindID per the PoC sketch) still returns a parseable response in the shape documented at sweep time.

**If any of the three checks fail**, halt this plan with the message:
```
SURVIVOR1=<name> has regressed since 2026-05-19 sweep. Specific failure: <one-line reason>.
Operator: decide:
  (a) retry later (re-probe in N days),
  (b) skip this survivor for v3.1,
  (c) replace with Survivor #2 (move 26-05 ahead of 26-04).
```
Append the regression report to `.planning/phases/26-provider-expansion/26-04-VERIFICATION.md` for operator review. Do NOT proceed to Task 2.

**If all three checks pass**, append the re-verification confirmation (timestamps + matching status codes) to `.planning/phases/26-provider-expansion/26-04-VERIFICATION.md` and proceed.
  </action>
  <verify>grep -E "Re-verification" .planning/phases/26-provider-expansion/26-04-VERIFICATION.md</verify>
  <acceptance_criteria>
    - .planning/phases/26-provider-expansion/26-04-VERIFICATION.md contains a "Re-verification" subsection with the curl invocations, timestamps, and per-check pass/fail outcomes
    - If all three checks pass, the file logs "GREEN — proceed to Task 2" and Task 2 runs
    - If any check fails, the file logs "RED — halt for operator decision" with the specific failure cited, and Task 2 does NOT run
  </acceptance_criteria>
  <done>The survivor's liveness is re-confirmed against fresh probes; the verification log captures the outcome; the plan either proceeds or halts cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Author the survivor package (mirrors 26-01 Task 2 for allanime)</name>
  <read_first>
    - .planning/research/2026-05-19-en-source-survival.md (PoC sketch for SURVIVOR1 from 26-03 Task 3)
    - services/scraper/internal/providers/allanime/client.go (REFERENCE — the just-shipped allanime package is the template to mirror)
    - services/scraper/internal/providers/allanime/cache.go
    - services/scraper/internal/providers/allanime/dto.go
    - services/scraper/internal/domain/provider.go
    - services/scraper/internal/health/stages.go
  </read_first>
  <action>
Create the package files under `services/scraper/internal/providers/<survivor1>/` mirroring the allanime layout from 26-01-PLAN.md Task 2:

- **doc.go** — package comment block stating purpose (survey candidate per SCRAPER-HEAL-26 operator pick, lifted from 2026-05-19 sweep), the upstream contract, and the persisted-probe shape captured.
- **dto.go** — DTOs for the candidate's API responses, mapped to `domain.Episode`, `domain.Server`, `domain.Stream`.
- **cache.go** — Mirror `services/scraper/internal/providers/allanime/cache.go`. Cache keys: `scraper:<survivor1>:show:<malID>` (24h), `scraper:<survivor1>:episodes:<showID>` (6h), `scraper:<survivor1>:servers:<showID>:<ep>` (15min), `scraper:<survivor1>:stream:...` (≤ min(parsed-expiry-30s, 5min)).
- **client.go** — `Deps`, `Provider`, `New(deps)`, plus the six `domain.Provider` methods. The FindID / ListEpisodes / ListServers / GetStream bodies match the PoC sketch from 26-03 Task 3 for this survivor — endpoint paths, query params, response field mappings are all specified there. Use `domain.BaseHTTPClient` for every upstream call. No anti-bot deps.

If the PoC sketch flagged a needed embed extractor that doesn't exist in `services/scraper/internal/embeds/`, scaffold a new extractor file in `services/scraper/internal/embeds/<extractor>.go` following the existing `streamhg.go` / `earnvids.go` pattern. Add the extractor to the registry (the file that maps server names to extractor functions — `grep -n 'Registry' services/scraper/internal/embeds/`).

DO NOT lift code from the catalog. The scraper package is fully self-contained.
  </action>
  <verify>cd services/scraper &amp;&amp; SURVIVOR=$(grep '^SURVIVOR1=' /data/animeenigma/.planning/phases/26-provider-expansion/26-04-RESOLVED.md | cut -d= -f2) &amp;&amp; go build ./internal/providers/$SURVIVOR/... &amp;&amp; go vet ./internal/providers/$SURVIVOR/...</verify>
  <acceptance_criteria>
    - Files exist at services/scraper/internal/providers/<survivor1>/{doc,dto,cache,client}.go where <survivor1> is the resolved SURVIVOR1 value
    - `go build ./internal/providers/<survivor1>/...` from services/scraper/ exits 0
    - `go vet ./internal/providers/<survivor1>/...` from services/scraper/ exits 0
    - client.go contains exactly one `func New(deps Deps) (*Provider, error)` and all six Provider methods (Name, FindID, ListEpisodes, ListServers, GetStream, HealthCheck)
    - No file imports `services/catalog/...`
    - If a new embed extractor was needed, it lives in services/scraper/internal/embeds/<name>.go and is registered in the embed registry
    - `grep -r 'chromedp\|flaresolverr\|utls\|tls-client\|cloudscraper_go' services/scraper/internal/providers/<survivor1>/` returns nothing
  </acceptance_criteria>
  <done>Survivor package compiles standalone, passes vet, has no catalog imports, no anti-bot deps, and exposes all six Provider methods.</done>
</task>

<task type="auto">
  <name>Task 3: Capture goldens + author client_test.go with the compile-time interface assertion</name>
  <read_first>
    - services/scraper/internal/providers/allanime/client_test.go (the template from 26-01 Task 3)
    - services/scraper/internal/providers/<survivor1>/client.go (just authored)
  </read_first>
  <action>
Capture upstream goldens against Frieren (MAL 52991) via the same curl pattern as 26-01 Task 3, adapted to SURVIVOR1's endpoints. Save under `services/scraper/internal/providers/<survivor1>/testdata/`:
- `show_frieren.json` (or equivalent for the candidate's FindID response shape)
- `episodes_frieren.json`
- `servers_frieren_ep1.json`

Author `client_test.go` mirroring the allanime test shape:
1. Compile-time `var _ domain.Provider = (*Provider)(nil)` assertion at file scope.
2. Six test functions: `TestFindID_Frieren`, `TestFindID_NotFound`, `TestListEpisodes_Frieren`, `TestListServers_Frieren_Ep1`, `TestGetStream_HappyPath`, `TestGetStream_NoSources`.
3. Each test stands up `httptest.NewServer` and serves the captured goldens — no real network calls.

Run `go test ./internal/providers/<survivor1>/... -race -count=2`. All tests must pass.
  </action>
  <verify>cd services/scraper &amp;&amp; SURVIVOR=$(grep '^SURVIVOR1=' /data/animeenigma/.planning/phases/26-provider-expansion/26-04-RESOLVED.md | cut -d= -f2) &amp;&amp; go test ./internal/providers/$SURVIVOR/... -race -count=2 -v 2>&amp;1 | tail -30</verify>
  <acceptance_criteria>
    - testdata/ contains at minimum show_frieren.json, episodes_frieren.json, servers_frieren_ep1.json (each with valid JSON, redacted of tracking IDs)
    - client_test.go contains exactly one `var _ domain.Provider = (*Provider)(nil)` at file scope
    - The six required test functions are present
    - `go test ./internal/providers/<survivor1>/... -race -count=2` exits 0 with all tests PASS
    - No test calls a real network endpoint
  </acceptance_criteria>
  <done>Unit test suite passes 2/2 race iterations against captured goldens; compile-time interface assertion holds.</done>
</task>

<task type="auto">
  <name>Task 4: Register <survivor1> in main.go between allanime and the animekai gate</name>
  <read_first>
    - services/scraper/cmd/scraper-api/main.go (the allanime registration from 26-01 Task 4 as the model)
    - services/scraper/internal/config/config.go (for the AllAnime config sub-struct shape)
  </read_first>
  <action>
1. In `services/scraper/internal/config/config.go`, add a `<Survivor1>` sub-struct on Config (PascalCase identifier, e.g., `Miruro` or `AnimeOwl`) only if the candidate needs a runtime-tunable base URL:
   ```go
   <Survivor1> struct {
       BaseURL string `envconfig:"SCRAPER_<SURVIVOR1>_BASE_URL" default:"https://<candidate-domain>"`
   }
   ```
   No `Enabled` flag — survey-selected candidates ship unconditional (operator-confirmed `worth-implementing` in 26-03). If operator needs to disable, they use `SCRAPER_DEGRADED_PROVIDERS=<survivor1>`.

2. In `services/scraper/cmd/scraper-api/main.go`, after the `allanime.New(...)` block from 26-01 Task 4 and before the `if cfg.AnimeKai.Enabled` gate, add:
   ```go
   <survivor1>Provider, err := <survivor1>.New(<survivor1>.Deps{
       HTTP:       baseHTTP,
       Cache:      redisCache,
       Log:        log.With("provider", "<survivor1>"),
       HealthSink: healthSink,
   })
   if err != nil { log.Fatalw("<survivor1>.New", "error", err) }
   providersList = append(providersList, <survivor1>Provider)
   candidateProviders = append(candidateProviders, "<survivor1>")
   ```

3. Add the import `"github.com/ILITA-hub/animeenigma/services/scraper/internal/providers/<survivor1>"`.

Resulting candidateProviders order: `["gogoanime", "animepahe", "allanime", "<survivor1>"]` (+ "animekai" if enabled).
  </action>
  <verify>cd services/scraper &amp;&amp; go build ./cmd/scraper-api/ &amp;&amp; go vet ./... &amp;&amp; SURVIVOR=$(grep '^SURVIVOR1=' /data/animeenigma/.planning/phases/26-provider-expansion/26-04-RESOLVED.md | cut -d= -f2) &amp;&amp; grep -c "\"$SURVIVOR\"" services/scraper/cmd/scraper-api/main.go</verify>
  <acceptance_criteria>
    - main.go imports services/scraper/internal/providers/<survivor1>
    - `grep -c '<survivor1>.New(' services/scraper/cmd/scraper-api/main.go` returns at least 1
    - `grep '"<survivor1>"' services/scraper/cmd/scraper-api/main.go` returns at least one match (in the candidateProviders append)
    - `go build ./cmd/scraper-api/` exits 0
    - `go vet ./...` exits 0
    - The registration block sits BETWEEN the allanime block and the `if cfg.AnimeKai.Enabled` gate (verified by reading the surrounding lines)
  </acceptance_criteria>
  <done>main.go registers the survivor in the unconditional block between allanime and the animekai gate; build + vet clean.</done>
</task>

<task type="auto">
  <name>Task 5: make redeploy-scraper + health smoke + Frieren end-to-end smoke</name>
  <read_first>
    - .planning/phases/26-provider-expansion/26-01-PLAN.md Task 5 (the exact smoke pattern to reuse)
    - .planning/phases/26-provider-expansion/26-04-RESOLVED.md (for SURVIVOR1)
  </read_first>
  <action>
Run the same five-step smoke sequence as 26-01 Task 5, with `<survivor1>` swapped for `allanime`:

1. `make redeploy-scraper`.
2. `curl -sf http://localhost:8087/scraper/health | jq '.providers[] | .name'` includes `<survivor1>`.
3. `curl -sf 'http://localhost:8000/api/anime/52991/scraper/episodes?provider=<survivor1>'` — assert HTTP 200 and non-empty episodes.
4. Servers + Stream smoke for ep 1.
5. Append the five outcomes to `.planning/phases/26-provider-expansion/26-04-VERIFICATION.md`.

Acceptable failure mode: upstream degraded → clean `ErrProviderDown` fall-through, no panic. Document the failure in the verification log.
  </action>
  <verify>grep -q "/scraper/episodes?provider=" .planning/phases/26-provider-expansion/26-04-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `make redeploy-scraper` exits 0
    - `curl http://localhost:8087/scraper/health` returns 200 and includes <survivor1> in the providers array
    - The end-to-end Frieren curl chain either returns 200 at each step OR logs a clean ErrProviderDown fall-through
    - 26-04-VERIFICATION.md contains the five smoke outcomes with timestamps and redacted excerpts
  </acceptance_criteria>
  <done>Scraper redeployed; survivor live in /scraper/health; smoke either passes or fails cleanly; verification log committed.</done>
</task>

</tasks>

<verification>
- 26-04-RESOLVED.md binds SURVIVOR1 to the operator-chosen candidate (or this plan cleanly skipped)
- Survivor re-verification confirms it's still live as of execution day
- Survivor package compiles, vets, tests green
- main.go registration order is correct (allanime, <survivor1>, [animekai])
- Smoke evidence captured in 26-04-VERIFICATION.md
- SCRAPER-HEAL-26 implementation half satisfied (research half satisfied by 26-03)
- No anti-bot deps; no catalog imports
</verification>
