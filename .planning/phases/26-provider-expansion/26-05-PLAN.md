---
phase: 26-provider-expansion
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - services/scraper/internal/providers/SURVIVOR2/client.go
  - services/scraper/internal/providers/SURVIVOR2/dto.go
  - services/scraper/internal/providers/SURVIVOR2/cache.go
  - services/scraper/internal/providers/SURVIVOR2/doc.go
  - services/scraper/internal/providers/SURVIVOR2/client_test.go
  - services/scraper/internal/providers/SURVIVOR2/testdata/episodes_frieren.json
  - services/scraper/internal/providers/SURVIVOR2/testdata/servers_frieren_ep1.json
  - services/scraper/cmd/scraper-api/main.go
  - services/scraper/internal/config/config.go
autonomous: false
requirements:
  - SCRAPER-HEAL-26
must_haves:
  truths:
    - "**CONDITIONAL PLAN — this plan executes ONLY IF the operator's Decision Gate in `.planning/research/2026-05-19-en-source-survival.md` named TWO survivors (Survivor #1 AND Survivor #2)**"
    - "If only Survivor #1 was picked, this plan is SKIPPED. If zero survivors were picked, this plan is SKIPPED (and 26-04 also skipped). The skip is a clean route-around in gsd-execute-phase, not a failure."
    - "The placeholder token `SURVIVOR2` in files_modified is REPLACED with the operator-chosen candidate's lowercase identifier; the executor reads the Decision Gate to resolve the token"
    - "Pattern follows 26-04 exactly — same package shape, same compile-time interface assertion, same testdata fixture approach against Frieren MAL 52991, same re-verification gate"
    - "main.go registers the survivor2 provider AFTER survivor1 (so order is: gogoanime, animepahe, allanime, <survivor1>, <survivor2>, [animekai])"
    - "go test ./services/scraper/internal/providers/<survivor2>/... -race -count=2 is fully green"
    - "make redeploy-scraper exits 0; /scraper/health includes both <survivor1> AND <survivor2>"
    - "End-to-end Frieren smoke against <survivor2> passes OR cleanly falls through via ErrProviderDown"
    - "No anti-bot dependency added; no catalog import"
  artifacts:
    - path: "services/scraper/internal/providers/<survivor2>/client.go"
      provides: "Second-survey-candidate Provider implementation"
      contains: "func New(deps Deps)"
    - path: "services/scraper/internal/providers/<survivor2>/client_test.go"
      provides: "Compile-time interface assertion + table-driven tests"
      contains: "var _ domain.Provider = (*Provider)(nil)"
  key_links:
    - from: "services/scraper/internal/providers/<survivor2>/client.go"
      to: "26-03 Decision Gate Survivor #2 + 26-04 reference implementation"
      via: "Same package shape as 26-04; Survivor #2 binding from Decision Gate"
      pattern: "Operator selection"
---

<objective>
**CONDITIONAL EXECUTION.** Survey Candidate #2 implementation. Mirrors 26-04-PLAN.md exactly with `<survivor2>` swapped for `<survivor1>`. Executes ONLY if the operator's Decision Gate named two survivors. Otherwise SKIPPED.

Purpose: SCRAPER-HEAL-26 second-survivor outcome path. Adds a fifth provider to the failover pool (gogoanime, animepahe, allanime, <survivor1>, <survivor2>). Per CONTEXT.md Risks, this still fits inside the 8s failover budget (max ≤ 4 providers × first-server-only ≤ 2s = ≤ 8s; 5 providers may flirt with the budget — the smoke step validates).

Output: A second new Go provider package, registration in main.go after Survivor #1, captured testdata goldens, verification of redeploy + end-to-end smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-provider-expansion/26-CONTEXT.md
@.planning/phases/26-provider-expansion/26-03-PLAN.md
@.planning/phases/26-provider-expansion/26-04-PLAN.md (REQUIRED — this plan is structurally a parameterised re-execution of 26-04)
@.planning/research/2026-05-19-en-source-survival.md (REQUIRED — Decision Gate + PoC sketch for SURVIVOR2)
@services/scraper/internal/providers/allanime/client.go (template — same as in 26-04)
@services/scraper/cmd/scraper-api/main.go

<interfaces>
<!-- TARGET candidateProviders order after this plan executes -->
// candidateProviders = ["gogoanime", "animepahe", "allanime", "<survivor1>", "<survivor2>"]
// (+ "animekai" if cfg.AnimeKai.Enabled)
//
// 5-provider failover budget check (per CONTEXT.md Risks):
//   max 5 × first-server-only probe (~2s each) = 10s WORST CASE.
//   Hard ≤ 8s budget asserted by orchestrator means the 5th provider only
//   runs if the first 4 ALL fail their first-server probe AND their
//   sequential remainder. Real-world this is extremely rare; if it
//   manifests, Phase 26's smoke catches it and operator reorders
//   SCRAPER_SERVER_PRIORITY to push slow providers later.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Resolve SURVIVOR2 from the Decision Gate, halt if only one survivor</name>
  <read_first>
    - .planning/research/2026-05-19-en-source-survival.md (Decision Gate)
    - .planning/phases/26-provider-expansion/26-04-RESOLVED.md (verify SURVIVOR1 already resolved)
  </read_first>
  <action>
Read the Decision Gate's Operator selection. Extract Survivor #2's lowercase identifier.

**Halt conditions:**
- If only Survivor #1 was named (no #2), halt with the clean-skip message: "Operator picked one survivor; 26-05 is SKIPPED. SCRAPER-HEAL-26 satisfied by 26-03 + 26-04."
- If zero survivors named, halt with "SCRAPER-HEAL-26 satisfied by 26-03 research-only outcome; 26-05 SKIPPED." (This branch is normally caught at 26-04 Task 0 — 26-05 only runs if 26-04 ran.)

If Survivor #2 is named, write `.planning/phases/26-provider-expansion/26-05-RESOLVED.md` with `SURVIVOR2=<lowercase-identifier>` and proceed.
  </action>
  <verify>test -f .planning/phases/26-provider-expansion/26-05-RESOLVED.md &amp;&amp; grep -E "^SURVIVOR2=[a-z0-9]+$" .planning/phases/26-provider-expansion/26-05-RESOLVED.md</verify>
  <acceptance_criteria>
    - 26-05-RESOLVED.md exists with SURVIVOR2=<value> OR the plan cleanly halts because the operator picked < 2 survivors
    - The resolved value matches the Decision Gate's Survivor #2 selection
  </acceptance_criteria>
  <done>SURVIVOR2 bound, or plan cleanly skipped.</done>
</task>

<task type="auto">
  <name>Task 1-5: Execute the same 5-task sequence as 26-04 with SURVIVOR2 substitution</name>
  <read_first>
    - .planning/phases/26-provider-expansion/26-04-PLAN.md (Tasks 1-5 verbatim — re-execute with SURVIVOR2)
    - .planning/research/2026-05-19-en-source-survival.md (PoC sketch for SURVIVOR2)
    - services/scraper/internal/providers/<survivor1>/ (reference for the registration pattern from 26-04)
  </read_first>
  <action>
Execute 26-04-PLAN.md Tasks 1 through 5 verbatim, with `<survivor2>` substituted for `<survivor1>` everywhere:

- **Task 1 (re-verification)** — Re-run the 26-03 probe against SURVIVOR2; halt if it has gone dark.
- **Task 2 (package authoring)** — Create services/scraper/internal/providers/<survivor2>/ with the same 4 files (doc, dto, cache, client) following the PoC sketch from 26-03 Task 3 for SURVIVOR2.
- **Task 3 (testdata + tests)** — Capture goldens against Frieren MAL 52991, author client_test.go with the compile-time interface assertion and six table-driven tests.
- **Task 4 (main.go registration)** — Append `<survivor2>` to candidateProviders AFTER `<survivor1>` so the order is `[gogoanime, animepahe, allanime, <survivor1>, <survivor2>]`. Add the `<Survivor2>` config sub-struct only if a runtime base-URL knob is needed.
- **Task 5 (redeploy + smoke)** — `make redeploy-scraper`, verify both <survivor1> AND <survivor2> are in /scraper/health, run end-to-end Frieren smoke against <survivor2>, append outcomes to `.planning/phases/26-provider-expansion/26-05-VERIFICATION.md`.

**Failover-budget check:** During the smoke, run a 4-provider-failure simulation by setting `SCRAPER_SERVER_PRIORITY=nonexistent` on gogoanime + animepahe + allanime + <survivor1> (forcing all four to fail their first-server probe) and confirm the scraper still returns a stream from <survivor2> in ≤ 8s total wall-clock time. If the budget is blown, document the wall-clock value in 26-05-VERIFICATION.md and surface to operator — operator decides whether to drop one provider or accept the budget.
  </action>
  <verify>cd services/scraper &amp;&amp; SURVIVOR2=$(grep '^SURVIVOR2=' /data/animeenigma/.planning/phases/26-provider-expansion/26-05-RESOLVED.md | cut -d= -f2) &amp;&amp; go test ./internal/providers/$SURVIVOR2/... -race -count=2 -v 2>&amp;1 | tail -20</verify>
  <acceptance_criteria>
    - services/scraper/internal/providers/<survivor2>/ package exists with doc/dto/cache/client + testdata + client_test.go
    - `var _ domain.Provider = (*Provider)(nil)` assertion present in client_test.go
    - `go test ./internal/providers/<survivor2>/... -race -count=2` exits 0
    - main.go appends <survivor2> after <survivor1>; order is verified by reading the surrounding lines
    - `make redeploy-scraper` exits 0; /scraper/health includes both survivors
    - Frieren end-to-end smoke against <survivor2> passes OR logs a clean ErrProviderDown
    - 5-provider failover budget check: total scraper wall-clock from request to first successful stream is ≤ 8s (or operator-documented variance)
    - 26-05-VERIFICATION.md contains the smoke + failover-budget evidence
  </acceptance_criteria>
  <done>Survivor #2 package shipped following the 26-04 template; failover budget validated; verification log committed.</done>
</task>

</tasks>

<verification>
- 26-05-RESOLVED.md binds SURVIVOR2 (or plan cleanly skipped)
- Survivor #2 package compiles, vets, tests green
- main.go registration order correct
- 5-provider failover budget validated against the ≤ 8s assertion
- 26-05-VERIFICATION.md captures evidence
- SCRAPER-HEAL-26 fully satisfied (research from 26-03 + two implementations from 26-04 + 26-05)
</verification>
