---
phase: 26-provider-expansion
plan: 06
type: execute
wave: 4
depends_on: [01]
files_modified:
  - services/scraper/internal/providers/animekai/client.go
  - services/scraper/internal/providers/animekai/find_id.go
  - services/scraper/internal/providers/animekai/episodes.go
  - services/scraper/internal/providers/animekai/servers.go
  - services/scraper/internal/providers/animekai/stream.go
  - services/scraper/internal/providers/animekai/decrypt.go
  - services/scraper/internal/providers/animekai/client_test.go
  - services/scraper/internal/providers/animekai/testdata/episodes_frieren.json
  - services/scraper/internal/providers/animekai/testdata/servers_frieren_ep1.json
  - docker/megacloud-extractor/server.js
  - docker/megacloud-extractor/animekai_token.js
  - services/scraper/cmd/scraper-api/main.go
  - docker/.env.example
autonomous: false
requirements:
  - SCRAPER-HEAL-27
must_haves:
  truths:
    - "**TIME-BOXED R&D PLAN — 7-day effort budget per CONTEXT.md D3.** If R&D does NOT converge within 7 days of focused effort, this plan halts and SCRAPER-HEAL-27 stays OPEN. Phase 26 ships without AnimeKai. The escape-hatch stub state (ErrProviderDown on every method, 501 from sidecar) remains in place — this is the documented contingency from v3.0 Phase 19 RESEARCH.md §Convergence Probability Assessment."
    - "Time-tracking is operator-driven. Operator logs effort days in .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md after each focused work session. When cumulative effort reaches 7 days without convergence, the operator invokes the documented halt branch."
    - "Convergence definition: SCRAPER_ANIMEKAI_ENABLED=true at runtime, the orchestrator routes a request through animekai successfully, end-to-end Frieren smoke returns a fetchable stream URL from anikai.to. ALL FOUR Provider methods (FindID, ListEpisodes, ListServers, GetStream) work — partial convergence (e.g., FindID + ListEpisodes work but GetStream still 501s) does NOT count as converged."
    - "The MegaUp token generator in docker/megacloud-extractor/server.js's /animekai-token handler goes from HTTP 501 (the Phase 19 escape-hatch stub at line 251-260) to a working token-producing implementation. Token-generation algorithm is reverse-engineered from anikai.to's client-side JS as a separate animekai_token.js module."
    - "The animekai provider methods (FindID/ListEpisodes/ListServers/GetStream) go from `domain.WrapProviderDown(errAnimeKaiStub, ...)` returns to real implementations. The errAnimeKaiStub sentinel and its var declaration are REMOVED (or moved to a removed-from-production-but-kept-for-grep historical comment block)."
    - "go test ./services/scraper/internal/providers/animekai/... -race -count=2 is fully green; testdata goldens captured fresh against anikai.to as of execution day"
    - "main.go's animekai-enabled gate remains intact (cfg.AnimeKai.Enabled), but with SCRAPER_ANIMEKAI_ENABLED=true in docker/.env, animekai joins candidateProviders after <survivor1>/<survivor2>/allanime"
    - "Orchestrator failover chain order (with SCRAPER_ANIMEKAI_ENABLED=true): gogoanime → animepahe → allanime → [<survivor1>] → [<survivor2>] → animekai"
    - "Anti-bot guard: if anikai.to has added Turnstile / Cloudflare Challenge / utls-equivalent protection during R&D, the in-house token generator cannot bypass it without anti-bot deps (CI rejected). Operator decides: live with escape hatch, OR escalate to WARP egress sidecar (v3.2 scope). NO anti-bot deps land in this plan regardless of how tempting."
    - "make redeploy-scraper exits 0; make redeploy-megacloud-extractor exits 0 (or equivalent sidecar redeploy target); /scraper/health includes animekai with non-stub stage states"
    - "End-to-end Frieren smoke against animekai returns a fetchable stream URL OR (if R&D halted) the smoke is documented as deferred and SCRAPER-HEAL-27 logged as OPEN in v3.1-REQUIREMENTS.md"
  artifacts:
    - path: "services/scraper/internal/providers/animekai/client.go"
      provides: "Animekai Provider with real (non-stub) method bodies"
      contains: "func (p *Provider) FindID"
    - path: "services/scraper/internal/providers/animekai/find_id.go"
      provides: "FindID implementation against anikai.to's search endpoint"
      contains: "anikai"
    - path: "docker/megacloud-extractor/animekai_token.js"
      provides: "MegaUp token generation logic extracted from anikai.to client JS"
      contains: "generateAnimeKaiToken"
    - path: "docker/megacloud-extractor/server.js"
      provides: "/animekai-token handler returning a real token (not 501)"
      contains: "/animekai-token"
    - path: ".planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md"
      provides: "Operator-maintained log of focused R&D effort against the 7-day budget"
      contains: "## Effort Log"
  key_links:
    - from: "services/scraper/internal/providers/animekai/stream.go::GetStream"
      to: "docker/megacloud-extractor sidecar HTTP /animekai-token endpoint"
      via: "Scraper POSTs encrypted payload → sidecar JS decrypts/generates MegaUp token → scraper resolves to final stream URL"
      pattern: "/animekai-token"
    - from: "docker/megacloud-extractor/server.js::/animekai-token handler"
      to: "docker/megacloud-extractor/animekai_token.js::generateAnimeKaiToken"
      via: "Token-generation module called from the HTTP handler; algorithm extracted from anikai.to client-side JS"
      pattern: "generateAnimeKaiToken"
---

<objective>
Fill in the v3.0 Phase 19 escape-hatch carryover (SCRAPER-KAI-01..04 + KAI-07): turn the AnimeKai provider stub at `services/scraper/internal/providers/animekai/client.go` (currently returning `domain.WrapProviderDown(errAnimeKaiStub, ...)` on every method) into a working scraper against `anikai.to`, and turn the sidecar `/animekai-token` handler at `docker/megacloud-extractor/server.js:251-260` (currently returning HTTP 501) into a real MegaUp-token generator. End-state: `SCRAPER_ANIMEKAI_ENABLED=true` in docker/.env, animekai joins the orchestrator failover chain after allanime + any survey survivors, end-to-end Frieren smoke against anikai.to produces a fetchable stream URL.

**Time-boxed R&D with documented contingency (per CONTEXT.md D3):** If 7 days of focused effort don't produce convergence (all four Provider methods working + sidecar token endpoint returning real tokens), this plan halts and SCRAPER-HEAL-27 stays OPEN. Phase 26 ships without AnimeKai — the same outcome v3.0 Phase 19 documented as the acceptable contingency. The escape-hatch stub remains in place; nothing regresses. The traceability table in `.planning/milestones/v3.1-REQUIREMENTS.md` gets a status update reflecting OPEN-AS-OF-PHASE-26.

Purpose: SCRAPER-HEAL-27 — bring AnimeKai's full implementation in if R&D can converge inside the budget, OR formally accept the second escape-hatch outcome if not. Either path is a valid completion of the requirement per CONTEXT.md D3.

Output: Either (a) shipped AnimeKai provider + sidecar token endpoint + Frieren smoke evidence in 26-06-VERIFICATION.md, OR (b) a documented halt with the 7-day effort log + a status update on SCRAPER-HEAL-27 in the milestone requirements table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-provider-expansion/26-CONTEXT.md (D3, Open Question on AnimeKai R&D budget)
@.planning/phases/19-animekai-gated/19-RESEARCH.md (the 2026-05-12 escape-hatch rationale + convergence-probability assessment)
@.planning/milestones/v3.1-REQUIREMENTS.md (SCRAPER-HEAL-27 row in the traceability table)
@services/scraper/internal/providers/animekai/client.go (the current stub state — ALL methods return ErrProviderDown)
@docker/megacloud-extractor/server.js (the /animekai-token handler at lines 240-265 — currently returns HTTP 501)
@services/scraper/internal/providers/allanime/client.go (the just-shipped reference shape from 26-01)
@services/scraper/internal/providers/gogoanime/client.go (the gold-standard production provider shape)

<interfaces>
<!-- Current animekai stub state (services/scraper/internal/providers/animekai/client.go:200-225) -->
// All four methods return:
//   err := domain.WrapProviderDown(errAnimeKaiStub, "animekai: <method> not implemented")
//   return ..., err
// TARGET: replace the stub bodies with real implementations against anikai.to.

<!-- Current sidecar stub state (docker/megacloud-extractor/server.js:251-260) -->
// if (parsed.pathname === "/animekai-token" && req.method === "POST") {
//     console.warn(`/animekai-token called — escape-hatch stub returning 501`);
//     // drain body
//     res.writeHead(501, ...);
// }
// TARGET: replace with a real handler that generates the MegaUp token via animekai_token.js.

<!-- Config gate (services/scraper/internal/config/config.go) -->
// AnimeKai struct {
//     Enabled bool   `envconfig:"SCRAPER_ANIMEKAI_ENABLED" default:"false"`
//     BaseURL string `envconfig:"SCRAPER_ANIMEKAI_BASE_URL" default:"https://anikai.to"`
// }
// This struct stays; the Enabled gate flips to true in docker/.env upon convergence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Scaffold the effort log + halt-branch decision template</name>
  <read_first>
    - .planning/phases/26-provider-expansion/26-CONTEXT.md (D3 budget definition)
    - .planning/phases/19-animekai-gated/19-RESEARCH.md (the convergence-probability assessment)
  </read_first>
  <action>
Create `.planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md` with:

1. **Header** — SCRAPER-HEAL-27, 7-day effort budget per CONTEXT.md D3, halt branch on day 7 if not converged.
2. **Convergence definition** (copied from must_haves above): all four Provider methods + sidecar token endpoint working against anikai.to.
3. **Effort Log** table:
   ```
   | Day | Date       | Focused Hours | Subtask | Outcome     | Cumulative |
   |---|---|---|---|---|---|
   | 1   | YYYY-MM-DD | _h_           | _      | _          | _h_        |
   ```
4. **Halt-Branch Decision Template** — what to do at day 7 if not converged:
   ```
   ## Halt Branch (invoked if day 7 reached without convergence)
   
   **Trigger:** Cumulative effort > 7 days; SCRAPER_ANIMEKAI_ENABLED still default-false.
   **Action:**
   1. Leave services/scraper/internal/providers/animekai/ in escape-hatch stub state (no production-code changes shipped).
   2. Update .planning/milestones/v3.1-REQUIREMENTS.md SCRAPER-HEAL-27 row: status="OPEN (carried to v3.2)".
   3. Document the carry-over in 26-06-VERIFICATION.md: which subtasks landed (FindID? token endpoint?), which didn't, what blocked R&D.
   4. Commit + push.
   5. Phase 26 ships WITHOUT AnimeKai; v3.1 milestone re-audit proceeds.
   ```

Operator updates this log after each focused work session. The halt branch is invoked manually by the operator (NOT auto-triggered) when cumulative effort reaches 7 days and convergence still seems unlikely.
  </action>
  <verify>test -f .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md &amp;&amp; grep -c "## Halt Branch\|## Effort Log" .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md</verify>
  <acceptance_criteria>
    - .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md exists
    - File has: header, convergence definition, Effort Log table (with header row + at least one empty data row), Halt-Branch Decision Template
    - The Halt-Branch template names the four explicit actions: leave stub, update requirements row, document in verification log, commit + push
  </acceptance_criteria>
  <done>Effort log scaffold ready; operator can begin logging focused R&D sessions; halt branch documented for the day-7 contingency.</done>
</task>

<task type="auto">
  <name>Task 1: R&D — reverse-engineer the MegaUp token-generation algorithm + author animekai_token.js</name>
  <read_first>
    - .planning/phases/19-animekai-gated/19-RESEARCH.md (any token-format notes captured during the original Phase 19 spike)
    - docker/megacloud-extractor/server.js (the existing handler structure to integrate the new module)
  </read_first>
  <action>
**This task is the R&D-heavy half of the plan. Budget: ~3-5 of the 7 days.**

1. Identify the token-generation algorithm. Pull the client-side JS from `https://anikai.to/` (the chunk that builds the `token` parameter sent to `/api/embed?token=...` for MegaUp embeds). Pretty-print, identify the key XOR/hash/cipher routine. Common patterns: AES-CBC with a hardcoded key, base64-then-XOR, multi-pass DJB2 hash. The algorithm extracted in Phase 19 (if any) lives in 19-RESEARCH.md as a starting point.

2. Author `docker/megacloud-extractor/animekai_token.js`:
   ```js
   // generateAnimeKaiToken(episodeID, timestamp, ...) returns a base64/hex token
   // string matching anikai.to's runtime-generated tokens.
   //
   // Algorithm extracted from anikai.to client-side JS as of <YYYY-MM-DD>.
   // Persist the extraction date as a comment block; anikai.to rotates the
   // algorithm periodically and the maintenance bot's Pattern 6/7 dispatch
   // will surface the regression when this module breaks.
   module.exports = { generateAnimeKaiToken };
   ```

3. Update `docker/megacloud-extractor/server.js`:
   - Replace lines 251-260 (the HTTP 501 stub) with a real handler:
     ```js
     if (parsed.pathname === "/animekai-token" && req.method === "POST") {
         let body = "";
         req.on("data", chunk => { body += chunk; });
         req.on("end", () => {
             try {
                 const { episodeID, timestamp } = JSON.parse(body);
                 const token = require("./animekai_token").generateAnimeKaiToken(episodeID, timestamp);
                 res.writeHead(200, { "Content-Type": "application/json" });
                 res.end(JSON.stringify({ token }));
             } catch (err) {
                 res.writeHead(500, { "Content-Type": "application/json" });
                 res.end(JSON.stringify({ error: err.message }));
             }
         });
         return;
     }
     ```

4. Add a unit test (or sanity test) for the token generator. Capture a known-good `(episodeID, timestamp) → token` triple from a live anikai.to network capture; assert the generator reproduces it bit-for-bit.

5. If the token algorithm can't be extracted in 5 days, halt and surface to operator. Common blockers: heavy WebAssembly obfuscation, runtime-rotated algorithm, Cloudflare Turnstile gating the API call before the token is even used. None of these are bypassable without anti-bot deps (CI rejected). Halt and document.
  </action>
  <verify>test -f docker/megacloud-extractor/animekai_token.js &amp;&amp; node -e "require('./docker/megacloud-extractor/animekai_token').generateAnimeKaiToken('test', 1234567890)" 2>&amp;1 | head -5</verify>
  <acceptance_criteria>
    - docker/megacloud-extractor/animekai_token.js exists and exports a `generateAnimeKaiToken` function
    - docker/megacloud-extractor/server.js's `/animekai-token` handler no longer returns HTTP 501; it calls generateAnimeKaiToken and returns the result in JSON
    - A captured-from-live golden `(episodeID, timestamp, expected_token)` triple is asserted by a test (sanity check OR a node --test invocation), and reproduces bit-for-bit
    - If R&D blocked, .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md documents the blocker and the operator's halt decision; this task ends WITHOUT modifying server.js (the 501 stub remains)
  </acceptance_criteria>
  <done>Token generator reproducing live captured tokens, OR a documented R&D halt with the blocker named.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the four animekai Provider methods against anikai.to</name>
  <read_first>
    - services/scraper/internal/providers/animekai/client.go (current stub bodies)
    - services/scraper/internal/providers/allanime/client.go (reference Provider implementation from 26-01)
    - services/scraper/internal/providers/gogoanime/client.go (gold-standard production provider)
    - Captured network traces of anikai.to (operator-supplied during R&D)
  </read_first>
  <action>
**Budget: ~2-3 of the 7 days.**

Replace the stub bodies in `services/scraper/internal/providers/animekai/client.go` with real implementations. Move each method body to its own file for readability (mirror the gogoanime layout):

- `find_id.go` — `FindID` resolves AnimeRef → anikai.to anime slug. Strategy from 19-RESEARCH.md: malsync lookup first (24h cache), fallback to title-search via anikai.to's `/search?q=` HTML scrape. Returns `domain.ErrNotFound` on zero matches.
- `episodes.go` — `ListEpisodes` scrapes the episode listing from anikai.to's anime page. Maps to `[]domain.Episode{Number, Title, EpisodeID}`. 6h cache TTL.
- `servers.go` — `ListServers` scrapes the server selector from the watch page. Returns `[]domain.Server{ServerID, Name, EmbedURL}`. 15min cache TTL.
- `stream.go` — `GetStream` calls the sidecar's `/animekai-token` endpoint (now working from Task 1) to mint the token, then POSTs to the MegaUp embed-resolution endpoint with the token, parses out the m3u8 master URL. Returns `*domain.Stream` with Headers `{Referer: "https://anikai.to/"}`. 5min cache TTL.
- `decrypt.go` — Any AES/XOR decryption helpers needed to unwrap embed-side encrypted payloads.

Remove the `errAnimeKaiStub` var (or move it to a removed-from-runtime comment block flagged "Phase 19 stub — removed 2026-05-19 upon SCRAPER-HEAL-27 convergence"). The `domain.WrapProviderDown(errAnimeKaiStub, ...)` calls are replaced by real error returns: `domain.ErrNotFound` for missing anime, `domain.ErrExtractFailed` for unresolved streams, generic wrapped errors for transient failures.

`HealthCheck` is already wired (the stub already returns a Health with all-degraded stages); update the stage seeding so a successful FindID flips `find_id` stage to `healthy`, etc.

All upstream HTTP calls go through `p.deps.HTTP` (BaseHTTPClient). No anti-bot deps regardless of how tempting (e.g., if Cloudflare Challenge appears, document the blocker — do NOT add `cloudscraper_go` or `flaresolverr`).
  </action>
  <verify>cd services/scraper &amp;&amp; go build ./internal/providers/animekai/... &amp;&amp; ! grep -E "errAnimeKaiStub|WrapProviderDown.*animekai.*not implemented" services/scraper/internal/providers/animekai/*.go</verify>
  <acceptance_criteria>
    - services/scraper/internal/providers/animekai/{find_id,episodes,servers,stream,decrypt}.go all exist (or the methods live in client.go and find_id.go/episodes.go/servers.go/stream.go are absent — either layout is acceptable as long as the bodies are real)
    - `grep 'errAnimeKaiStub' services/scraper/internal/providers/animekai/*.go` returns nothing (or only inside a removed-from-runtime comment block, NOT in active code paths)
    - `grep 'animekai: <method> not implemented' services/scraper/internal/providers/animekai/*.go` returns nothing
    - `go build ./internal/providers/animekai/...` from services/scraper/ exits 0
    - `go vet ./internal/providers/animekai/...` exits 0
    - No new imports of chromedp, flaresolverr, utls, tls-client, cloudscraper_go
    - If R&D blocked, the files remain in stub state and 26-06-EFFORT-LOG.md documents the halt
  </acceptance_criteria>
  <done>All four Provider methods have real bodies, OR the plan halts at this task with the blocker documented.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + redeploy + end-to-end Frieren smoke against anikai.to</name>
  <read_first>
    - services/scraper/internal/providers/animekai/client.go (the just-implemented bodies)
    - services/scraper/internal/providers/allanime/client_test.go (template for the test layout)
    - docker/.env (for SCRAPER_ANIMEKAI_ENABLED flag)
  </read_first>
  <action>
1. **Capture fresh testdata goldens** against anikai.to for Frieren (MAL 52991):
   - `testdata/search_frieren.json` (FindID response)
   - `testdata/episodes_frieren.json`
   - `testdata/servers_frieren_ep1.json`
   - `testdata/megaup_token_response_frieren_ep1.json` (the sidecar's response to a known token-mint request)

2. **Author client_test.go** mirroring 26-01 Task 3's allanime test shape:
   - `var _ domain.Provider = (*Provider)(nil)` compile-time assertion at file scope
   - TestFindID_Frieren, TestFindID_NotFound, TestListEpisodes_Frieren, TestListServers_Frieren_Ep1, TestGetStream_HappyPath, TestGetStream_NoSources
   - Use httptest.NewServer to stand in for anikai.to AND a second httptest.NewServer for the sidecar's `/animekai-token` endpoint

3. **Run tests:** `go test ./internal/providers/animekai/... -race -count=2`. All pass.

4. **Flip the feature flag:** Add `SCRAPER_ANIMEKAI_ENABLED=true` to `docker/.env` (and add the same line to `docker/.env.example` if missing). NO other config changes needed — the config struct already exists from Phase 19.

5. **Redeploy both services:**
   - `make redeploy-scraper`
   - `make redeploy-megacloud-extractor` (or the equivalent target — check `make help` if the name varies; if no dedicated target exists, the docker-compose recreate is `docker compose -f docker/docker-compose.yml up -d --build megacloud-extractor`)

6. **Smoke:**
   - `curl -sf http://localhost:8087/scraper/health | jq '.providers[] | select(.name=="animekai")'` returns a non-stub entry with at least one stage marked `healthy`.
   - `curl -sf 'http://localhost:8000/api/anime/52991/scraper/episodes?provider=animekai'` returns HTTP 200 with non-empty episodes.
   - `curl -sf 'http://localhost:8000/api/anime/52991/scraper/servers/<ep>?provider=animekai'` returns servers.
   - `curl -sf 'http://localhost:8000/api/anime/52991/scraper/stream/<ep>/<server>?provider=animekai'` returns a stream_url. `curl -I` against the stream_url returns HTTP 200 + a media content-type.

7. **Append outcomes** to `.planning/phases/26-provider-expansion/26-06-VERIFICATION.md`.
  </action>
  <verify>grep -E "animekai.*HTTP 200|animekai.*PASS" .planning/phases/26-provider-expansion/26-06-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `go test ./internal/providers/animekai/... -race -count=2` exits 0 with PASS on all six required test functions
    - docker/.env contains `SCRAPER_ANIMEKAI_ENABLED=true`
    - `make redeploy-scraper` and the megacloud-extractor redeploy both exit 0
    - `/scraper/health` lists animekai with at least one stage `healthy` (not all `degraded` or `unknown`)
    - End-to-end Frieren curl chain returns HTTP 200 at each step; the final stream URL is fetchable via `curl -I`
    - 26-06-VERIFICATION.md captures all six outcomes with timestamps + redacted excerpts
  </acceptance_criteria>
  <done>AnimeKai converged: full Provider, working sidecar token endpoint, end-to-end Frieren smoke passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: GATE — Operator reviews convergence status; either confirms ship OR invokes the halt branch</name>
  <read_first>
    - .planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md (cumulative R&D effort logged so far)
    - .planning/phases/26-provider-expansion/26-06-VERIFICATION.md (convergence evidence from Task 3, if Task 3 reached completion)
    - services/scraper/internal/providers/animekai/client.go (current state — stub vs real)
    - docker/megacloud-extractor/server.js (current state of the /animekai-token handler)
    - .planning/milestones/v3.1-REQUIREMENTS.md (SCRAPER-HEAL-27 row, to update if halt branch invoked)
  </read_first>
  <what-built>Either (a) a converged AnimeKai implementation with passing end-to-end Frieren smoke, OR (b) an effort-budget halt with documented blockers.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/26-provider-expansion/26-06-EFFORT-LOG.md` — total cumulative effort.
    2. Read `.planning/phases/26-provider-expansion/26-06-VERIFICATION.md` — convergence evidence (or its absence).
    3. **If converged**: confirm the Frieren smoke evidence is real (HTTP 200 + fetchable stream URL), confirm no anti-bot deps were sneaked in (`git diff services/scraper/go.mod` shows no chromedp/flaresolverr/utls additions), and approve.
    4. **If not converged within 7 days**: invoke the halt branch from 26-06-EFFORT-LOG.md:
       - Confirm the animekai provider files are back to stub state (or never left)
       - Confirm the sidecar /animekai-token still returns 501
       - Update .planning/milestones/v3.1-REQUIREMENTS.md SCRAPER-HEAL-27 row: status="OPEN (carried to v3.2)"
       - 26-06-VERIFICATION.md states: "R&D halted at day 7 per CONTEXT.md D3 contingency. SCRAPER-HEAL-27 carried to v3.2. Phase 26 ships without AnimeKai."
    5. Either path is a valid completion of SCRAPER-HEAL-27 per CONTEXT.md D3.
  </how-to-verify>
  <action>
Block here. Wait for operator confirmation. Type "shipped" in chat if converged + smoke passing; type "halt" if invoking the contingency.
  </action>
  <verify>test -f .planning/phases/26-provider-expansion/26-06-VERIFICATION.md</verify>
  <acceptance_criteria>
    - 26-06-VERIFICATION.md contains a clear verdict: "SHIPPED — AnimeKai converged" OR "HALTED — carried to v3.2"
    - If shipped: docker/.env has SCRAPER_ANIMEKAI_ENABLED=true; /scraper/health shows animekai stages healthy; Frieren end-to-end smoke documented
    - If halted: .planning/milestones/v3.1-REQUIREMENTS.md SCRAPER-HEAL-27 row updated to OPEN status with carry-over note
    - Either path: operator chat message confirms the verdict ("shipped" or "halt")
    - go.mod for services/scraper has no anti-bot deps regardless of verdict
  </acceptance_criteria>
  <done>Operator has reviewed convergence status and chosen the ship or halt branch; either path closes SCRAPER-HEAL-27 per D3 contingency.</done>
</task>

</tasks>

<verification>
- 26-06-EFFORT-LOG.md tracks the 7-day budget with operator updates
- Convergence path: all four animekai Provider methods working, sidecar token endpoint live, end-to-end Frieren smoke passing
- OR halt path: stub state preserved, requirements row updated to OPEN, carry-over documented
- 26-06-VERIFICATION.md states the verdict
- No anti-bot deps under any verdict
- SCRAPER-HEAL-27 closed (shipped) OR carried (halt) per CONTEXT.md D3
</verification>
