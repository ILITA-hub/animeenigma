---
phase: 26-provider-expansion
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - frontend/web/src/components/player/EnglishPlayer.vue
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/ja.json
autonomous: false
requirements:
  - SCRAPER-HEAL-28
must_haves:
  truths:
    - "**HARD DEPENDENCY ON PHASE 24** — this plan requires `frontend/web/src/components/player/EnglishPlayer.vue` to EXIST. As of 2026-05-19 it does NOT exist (verified: `ls frontend/web/src/components/player/` shows no EnglishPlayer.vue). Phase 24 is HARD-GATE-BLOCKED per `.planning/phases/24-en-reconnect/24-00-SUMMARY.md` — all three EN providers failed Wave 0 verification (gogoanime broken, animepahe IP-blocked, animekai stub). Phase 24 cannot complete until at least one provider is recovered; Phase 26 Wave 1 (AllAnime lift in 26-01) provides that recovery."
    - "**EXECUTION GATE**: Task 0 below verifies `frontend/web/src/components/player/EnglishPlayer.vue` exists. If the file is missing, this plan SKIPS — SCRAPER-HEAL-28 stays open with the documented dependency: 'BLOCKED on Phase 24 EnglishPlayer.vue restoration. Once Phase 24 ships, run gsd-execute-phase against 26-07 only.' This is NOT a Phase 26 failure; it's the cross-phase dependency documented in CONTEXT.md Dependencies."
    - "When EnglishPlayer.vue does exist (post-Phase-24-completion), the dropdown infrastructure already lights up automatically: the existing `v-if=\"providers.length > 1\"` gate on the `<select>` element (per CONTEXT.md D4) flips visible the moment candidateProviders has 2+ entries in /scraper/health"
    - "What this plan DOES add: (a) `capitalizeProvider()` branches for every Phase 26-added provider (allanime, <survivor1>, <survivor2>, animekai if shipped), (b) per-provider i18n labels in `browse.filters.provider.*` AND a new `player.scraperProvider.*` group for the dropdown's display text, (c) verification that the dropdown shows the right human-readable label for each provider when multiple are healthy"
    - "useWatchPreferences.preferredScraperProvider persists per anime across page reloads (this is already implemented at frontend/web/src/composables/useWatchPreferences.ts:84-175 — verified via grep — no code changes; only verification that the existing wiring still works post-26-01/26-04/26-06)"
    - "bun run build, bunx tsc --noEmit, bunx eslint src/ — all three exit 0 in frontend/web/"
    - "Playwright e2e test against `ui_audit_bot` user on a logged-in session: navigate to /anime/<id-with-multiple-providers>, switch to EN tab, confirm the dropdown is visible (NOT hidden by `v-if`), confirm dropdown has 2+ options (allanime + survivor1, etc.), confirm clicking each option triggers a `/api/anime/.../scraper/episodes?provider=<name>` request"
    - "After /animeenigma-after-update closes the milestone, the changelog.json entry reflects: 'AnimeKai is back / new EN sources added / dropdown is live' — written in the informative-with-emojis tone CLAUDE.md mandates"
  artifacts:
    - path: "frontend/web/src/components/player/EnglishPlayer.vue"
      provides: "capitalizeProvider branches for every Phase 26-added provider"
      contains: "capitalizeProvider"
    - path: "frontend/web/src/locales/en.json"
      provides: "player.scraperProvider.* keys for the in-player dropdown labels"
      contains: "scraperProvider"
    - path: "frontend/web/public/changelog.json"
      provides: "User-facing changelog entry for Phase 26 — 'AnimeKai back / new providers / dropdown live'"
      contains: "Phase 26"
  key_links:
    - from: "frontend/web/src/components/player/EnglishPlayer.vue::capitalizeProvider"
      to: "frontend/web/src/locales/{en,ru,ja}.json::player.scraperProvider.*"
      via: "Each provider key gets a human-readable label resolved via t('player.scraperProvider.<name>')"
      pattern: "player.scraperProvider"
    - from: ".planning/phases/24-en-reconnect/24-CONTEXT.md SCRAPER-HEAL-17 (EnglishPlayer.vue restoration)"
      to: ".planning/phases/26-provider-expansion/26-07-PLAN.md SCRAPER-HEAL-28 (dropdown polish)"
      via: "26-07 is logically downstream of 24's SCRAPER-HEAL-17 — restoring EnglishPlayer.vue is the prerequisite that 26-07 lights up"
      pattern: "EnglishPlayer.vue"
---

<objective>
**HARD-GATED ON PHASE 24.** Polish the EnglishPlayer in-player source dropdown so it lights up cleanly with 2-4 providers post-Phase-26 expansion. The dropdown's *infrastructure* is supposed to come from Phase 24's SCRAPER-HEAL-17 (EnglishPlayer.vue restoration at commit 8424e99). The `<select>` element with the `v-if="providers.length > 1"` gate already exists in that snapshot per CONTEXT.md D4. This plan adds the small finishing touches: a `capitalizeProvider()` branch per added provider, per-provider i18n labels in a new `player.scraperProvider.*` key group, a Playwright e2e verification that the dropdown renders correctly with multiple providers, and the milestone-closing changelog entry.

**Execution gate**: this plan SKIPS if `frontend/web/src/components/player/EnglishPlayer.vue` doesn't exist on disk. The file is currently missing (verified 2026-05-19); Phase 24 is HARD-GATE-BLOCKED on broken upstream providers; 26-01's AllAnime lift is the recovery path that unblocks Phase 24. Logically: 26-01 ships → Phase 24 unblocks → Phase 24 restores EnglishPlayer.vue → THEN 26-07 can run. If 26-01 doesn't unblock Phase 24 (e.g., AllAnime also fails upstream), 26-07 stays SKIPPED and SCRAPER-HEAL-28 is logged in v3.1-REQUIREMENTS.md as BLOCKED-ON-PHASE-24.

Purpose: SCRAPER-HEAL-28 — make the dropdown observable with the new providers from Phase 26 once EnglishPlayer.vue is back. CONTEXT.md D4 is explicit that this wave is mostly trivial since the infrastructure ships from Phase 24; this plan's value is i18n + capitalizeProvider branches + the user-visible polish in changelog.json.

Output: Either (a) updates to EnglishPlayer.vue's capitalizeProvider + new i18n keys + Playwright e2e + changelog entry, OR (b) a documented skip because EnglishPlayer.vue is still missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-provider-expansion/26-CONTEXT.md (D4 — single-option hide / 2+ show; D6 Wave 5)
@.planning/phases/24-en-reconnect/24-CONTEXT.md (the EnglishPlayer.vue restoration spec)
@.planning/phases/24-en-reconnect/24-00-SUMMARY.md (the current HARD-GATE-BLOCKED status)
@.planning/milestones/v3.1-REQUIREMENTS.md (SCRAPER-HEAL-28 row)
@frontend/web/src/composables/useWatchPreferences.ts (preferredScraperProvider persistence, lines 84-175 — existing, verified)
@frontend/web/public/changelog.json (existing user-facing changelog — Phase 26 entry appends here)

<interfaces>
<!-- Expected post-Phase-24 EnglishPlayer.vue structure (per 24-CONTEXT.md SCRAPER-HEAL-17) -->
// The restored EnglishPlayer.vue has a <select> element gated on
// `providers.length > 1` (per CONTEXT.md D4). The capitalizeProvider helper
// transforms backend slugs to display labels:
//
//   function capitalizeProvider(name: string) {
//     switch (name) {
//       case 'gogoanime': return 'Gogoanime'
//       case 'animepahe': return 'AnimePahe'
//       // TARGET (this plan): add allanime, <survivor1>, <survivor2>, animekai
//       case 'allanime': return 'AllAnime'
//       case 'animekai': return 'AnimeKai'
//       default: return name
//     }
//   }

<!-- New i18n key group (this plan adds) -->
// In frontend/web/src/locales/en.json:
//   "player": {
//     ...
//     "scraperProvider": {
//       "gogoanime": "Gogoanime",
//       "animepahe": "AnimePahe",
//       "allanime":  "AllAnime",
//       "<survivor1>": "<Survivor1 Display Name>",
//       "<survivor2>": "<Survivor2 Display Name>",
//       "animekai":  "AnimeKai"
//     }
//   }
// Mirror in ru.json and ja.json with localized names where appropriate (most
// product names stay verbatim in non-English locales — "AnimeKai" is "AnimeKai"
// in Russian and Japanese).

<!-- Existing preference persistence (DO NOT change) -->
// frontend/web/src/composables/useWatchPreferences.ts:84-175 already
// handles preferredScraperProvider — verified by grep. This plan does not
// touch the composable; it only verifies the existing wiring still works
// after Phase 26's new providers land.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Execution gate — verify EnglishPlayer.vue exists, OR cleanly skip</name>
  <read_first>
    - frontend/web/src/components/player/ (directory listing)
    - .planning/phases/24-en-reconnect/24-00-SUMMARY.md (current blocked status)
  </read_first>
  <action>
Check if `frontend/web/src/components/player/EnglishPlayer.vue` exists on disk.

**If MISSING:**
1. Write `.planning/phases/26-provider-expansion/26-07-VERIFICATION.md` with:
   ```
   # 26-07 — SKIPPED (Phase 24 dependency unmet)
   
   **Date:** <ISO timestamp>
   **Status:** SKIPPED
   **Reason:** frontend/web/src/components/player/EnglishPlayer.vue does not exist.
   Phase 24 (EN Reconnect) is HARD-GATE-BLOCKED per
   .planning/phases/24-en-reconnect/24-00-SUMMARY.md — all three EN providers
   failed Wave 0 verification. 26-01's AllAnime lift may unblock Phase 24, but
   that depends on AllAnime being healthy upstream on the deploy day.
   
   SCRAPER-HEAL-28 stays OPEN with status "BLOCKED-ON-PHASE-24".
   ```
2. Update `.planning/milestones/v3.1-REQUIREMENTS.md` SCRAPER-HEAL-28 row to:
   `| SCRAPER-HEAL-28 | 26 | OPEN — blocked on Phase 24 EnglishPlayer.vue restoration | Once Phase 24 ships, re-run gsd-execute-phase against 26-07 only |`
3. Halt this plan with the clean-skip message. No code changes ship; no frontend files modified.

**If PRESENT:**
1. Append to `.planning/phases/26-provider-expansion/26-07-VERIFICATION.md`:
   ```
   # 26-07 — Active
   
   **EnglishPlayer.vue present:** yes (path: frontend/web/src/components/player/EnglishPlayer.vue)
   **Proceeding to Task 1.**
   ```
2. Proceed to Task 1.
  </action>
  <verify>test -f .planning/phases/26-provider-expansion/26-07-VERIFICATION.md</verify>
  <acceptance_criteria>
    - 26-07-VERIFICATION.md exists with a clear "SKIPPED" or "Active" verdict
    - If SKIPPED: v3.1-REQUIREMENTS.md's SCRAPER-HEAL-28 row reflects the BLOCKED-ON-PHASE-24 status; no other files are modified by this plan
    - If Active: subsequent tasks (Task 1+) run; otherwise the plan halts cleanly
  </acceptance_criteria>
  <done>The dependency on Phase 24's EnglishPlayer.vue restoration is checked; this plan either proceeds or cleanly skips.</done>
</task>

<task type="auto">
  <name>Task 1: Add capitalizeProvider branches for every Phase 26-added provider</name>
  <read_first>
    - frontend/web/src/components/player/EnglishPlayer.vue (the post-Phase-24 restored version — locate the capitalizeProvider function)
    - .planning/phases/26-provider-expansion/26-04-RESOLVED.md (for SURVIVOR1, if it exists)
    - .planning/phases/26-provider-expansion/26-05-RESOLVED.md (for SURVIVOR2, if it exists)
    - .planning/phases/26-provider-expansion/26-06-VERIFICATION.md (for the AnimeKai ship-vs-halt verdict)
  </read_first>
  <action>
Locate `function capitalizeProvider(name: string)` in EnglishPlayer.vue (it's part of the restored 8424e99 snapshot per Phase 24's SCRAPER-HEAL-17). Add case branches for every Phase 26-added provider that's actually live in `candidateProviders`:

- Always add `case 'allanime': return 'AllAnime'` (26-01 ships unconditionally)
- Conditionally add `case '<survivor1>': return t('player.scraperProvider.<survivor1>')` if 26-04-RESOLVED.md exists
- Conditionally add `case '<survivor2>': return t('player.scraperProvider.<survivor2>')` if 26-05-RESOLVED.md exists
- Conditionally add `case 'animekai': return 'AnimeKai'` only if 26-06-VERIFICATION.md verdict is "SHIPPED" (NOT "HALTED")

Replace the existing return with `t('player.scraperProvider.<name>')` lookups where i18n labels diverge from the simple PascalCase transform (e.g., if a survivor has a complex/non-Latin display name).

Don't touch any other code in EnglishPlayer.vue — the dropdown's `v-if="providers.length > 1"` gate stays exactly as Phase 24 shipped it. capitalizeProvider is the only thing this task changes.
  </action>
  <verify>cd frontend/web &amp;&amp; bunx tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; grep -A1 "capitalizeProvider" src/components/player/EnglishPlayer.vue | head -30</verify>
  <acceptance_criteria>
    - capitalizeProvider in EnglishPlayer.vue has a case branch for 'allanime' returning 'AllAnime'
    - If 26-04-RESOLVED.md exists, capitalizeProvider has a case branch for the resolved survivor1 identifier
    - If 26-05-RESOLVED.md exists, capitalizeProvider has a case branch for the resolved survivor2 identifier
    - If 26-06-VERIFICATION.md verdict is SHIPPED, capitalizeProvider has a case branch for 'animekai' returning 'AnimeKai'
    - `bunx tsc --noEmit` in frontend/web/ exits 0
    - The dropdown's v-if gate is UNCHANGED (verified by `git diff src/components/player/EnglishPlayer.vue` showing only the capitalizeProvider hunk)
  </acceptance_criteria>
  <done>capitalizeProvider returns human-readable labels for every Phase 26 provider that's actually live; no other EnglishPlayer.vue code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add player.scraperProvider.* i18n keys to en.json + ru.json + ja.json</name>
  <read_first>
    - frontend/web/src/locales/en.json (locate the `player` object)
    - frontend/web/src/locales/ru.json
    - frontend/web/src/locales/ja.json
    - .planning/phases/26-provider-expansion/26-04-RESOLVED.md / 26-05-RESOLVED.md / 26-06-VERIFICATION.md (provider identifiers + ship/halt status)
  </read_first>
  <action>
In each of the three locale files, add a `scraperProvider` object under `player`:

- `frontend/web/src/locales/en.json`:
  ```json
  "scraperProvider": {
    "gogoanime": "Gogoanime",
    "animepahe": "AnimePahe",
    "allanime": "AllAnime"
  }
  ```
  Add `"<survivor1>": "<Display Name>"` if 26-04 shipped; `"<survivor2>": "<Display Name>"` if 26-05 shipped; `"animekai": "AnimeKai"` if 26-06 verdict is SHIPPED.

- `frontend/web/src/locales/ru.json` — same structure. Product names like "Gogoanime", "AnimePahe", "AllAnime", "AnimeKai" stay verbatim in Russian (operator can override for any survivor that has a localized Russian name).

- `frontend/web/src/locales/ja.json` — same structure. Product names stay verbatim in Japanese (operator can override for any survivor with a katakana name).

Run `bun run lint:i18n` (or the equivalent i18n-key-parity linter) to confirm all three locales have the same keys.
  </action>
  <verify>cd frontend/web &amp;&amp; jq '.player.scraperProvider | keys' src/locales/en.json &amp;&amp; jq '.player.scraperProvider | keys' src/locales/ru.json &amp;&amp; jq '.player.scraperProvider | keys' src/locales/ja.json</verify>
  <acceptance_criteria>
    - All three locale files have a `player.scraperProvider` object with the same set of keys (at minimum: gogoanime, animepahe, allanime; plus survivor1/survivor2/animekai per conditional shipping)
    - `jq . frontend/web/src/locales/en.json > /dev/null` exits 0 (and same for ru.json + ja.json — valid JSON)
    - `bun run lint:i18n` (or equivalent) reports zero key-parity diffs between the three locales for the new keys
  </acceptance_criteria>
  <done>i18n keys added in parity across all three locales; lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e smoke — dropdown visible with 2+ providers, click cycles through</name>
  <read_first>
    - frontend/web/playwright.config.ts or e2e/ directory (locate the existing test pattern)
    - CLAUDE.md UI Audit Test User section (ui_audit_bot credentials in docker/.env)
    - frontend/web/src/components/player/EnglishPlayer.vue (the source dropdown markup)
  </read_first>
  <action>
Author an e2e spec at `frontend/web/e2e/scraper-provider-dropdown.spec.ts` (or whatever the project's e2e path is — `ls frontend/web/e2e/` to confirm):

```ts
import { test, expect } from '@playwright/test';

test('English source dropdown is visible with 2+ providers and persists selection', async ({ page }) => {
  // Log in as ui_audit_bot via /api/auth/login (refresh-cookie semantics).
  await page.request.post('/api/auth/login', {
    data: { username: 'ui_audit_bot', password: 'audit_bot_test_password_2026' },
  });
  // Navigate to an anime page with multiple live providers (use Frieren or whichever
  // anime the 26-01/26-04/26-06 smokes confirmed has 2+ healthy providers).
  await page.goto('/anime/52991');
  // Click the English tab.
  await page.getByRole('tab', { name: /english/i }).click();
  // The source dropdown SHOULD be visible (providers.length > 1 per CONTEXT.md D4).
  const dropdown = page.locator('select[data-testid="scraper-provider-dropdown"]');
  await expect(dropdown).toBeVisible();
  // Dropdown should have at least 2 options.
  const optionCount = await dropdown.locator('option').count();
  expect(optionCount).toBeGreaterThanOrEqual(2);
  // Each option label uses capitalizeProvider — assert at least 'AllAnime' is present.
  const labels = await dropdown.locator('option').allTextContents();
  expect(labels).toContain('AllAnime');
  // Click each option; preference persists.
  await dropdown.selectOption({ index: 1 });
  await page.reload();
  await page.getByRole('tab', { name: /english/i }).click();
  // After reload, the dropdown still has the selected option as the value.
  const selected = await dropdown.inputValue();
  expect(selected).not.toBe('');
});
```

If the EnglishPlayer.vue source markup doesn't have `data-testid="scraper-provider-dropdown"` (it may not — Phase 24 restored from 8424e99), add the data-testid to the `<select>` in a one-line touch-up to EnglishPlayer.vue. This is the only additional code change beyond capitalizeProvider.

Run `bunx playwright test scraper-provider-dropdown`. If the test fails because the test environment doesn't have 2+ healthy providers (e.g., AllAnime upstream is degraded during the test run), capture the failure mode and either:
- Adjust the test to assert that the dropdown is visible IF >= 2 providers respond, gracefully skipping otherwise.
- Or use the test-stub mode (mock /scraper/health response with 2+ providers).

The test must NOT be flaky in CI — choose the mock approach if upstream uncertainty is high.
  </action>
  <verify>cd frontend/web &amp;&amp; bunx playwright test scraper-provider-dropdown 2>&amp;1 | tail -10</verify>
  <acceptance_criteria>
    - frontend/web/e2e/scraper-provider-dropdown.spec.ts exists (or the spec lives at whatever the project's e2e path is)
    - `bunx playwright test scraper-provider-dropdown` exits 0
    - The test asserts: dropdown visible, option count >= 2, AllAnime label present, selection persists across reload
    - If the test uses `data-testid="scraper-provider-dropdown"`, that attribute exists on the `<select>` in EnglishPlayer.vue
  </acceptance_criteria>
  <done>Playwright e2e proves the dropdown lights up correctly with 2+ providers; selection persists across reload.</done>
</task>

<task type="auto">
  <name>Task 4: Update changelog.json + invoke /animeenigma-after-update for the milestone close</name>
  <read_first>
    - frontend/web/public/changelog.json (the user-facing changelog)
    - .claude/commands/animeenigma-after-update.md (the milestone-close skill)
  </read_first>
  <action>
1. Add a Phase 26 entry to `frontend/web/public/changelog.json` in the informative-with-emojis tone CLAUDE.md mandates. Entry covers:
   - AllAnime is now an English source (smile/sparkle emoji)
   - Any shipped survivors (named)
   - AnimeKai is back (if 26-06 SHIPPED) OR omit (if 26-06 HALTED)
   - The browse filter "English sources" is live (a checkbox emoji works)
   - The in-player source dropdown is back with multiple options (a list emoji works)
   - SCRAPER-HEAL-28 closed (or carried to next milestone if Phase 24 still blocked at Task 0)

2. Per CLAUDE.md's "After-Update Skill (MUST USE)" section, this is the milestone-closing wave — invoke `/animeenigma-after-update` to lint, build, redeploy any changed services (web only, for this plan), run health checks, and commit + push with the canonical co-authors.

DO NOT auto-invoke `/animeenigma-after-update` if Task 0 SKIPPED. In that case, this whole plan ended at Task 0; no changelog entry; no after-update invocation.
  </action>
  <verify>grep -E "Phase 26|allanime|AllAnime|dropdown" frontend/web/public/changelog.json | head -5</verify>
  <acceptance_criteria>
    - frontend/web/public/changelog.json has a new Phase 26 entry with the user-facing summary in CLAUDE.md's mandated tone
    - The entry mentions AllAnime, the browse filter, the dropdown, and (if shipped) any survivors / AnimeKai
    - `/animeenigma-after-update` was invoked (verified by git log showing the commit with co-authors); web service redeployed
    - If Task 0 SKIPPED, this task is also SKIPPED — no changelog edit, no after-update invocation
  </acceptance_criteria>
  <done>User-facing changelog updated; milestone-close redeploy + commit + push completed (or cleanly skipped if Phase 24 still blocking).</done>
</task>

</tasks>

<verification>
- Either Task 0 SKIPPED (Phase 24 dependency unmet, SCRAPER-HEAL-28 logged as BLOCKED-ON-PHASE-24 in requirements) OR the plan completed:
- capitalizeProvider branches added for every shipped Phase 26 provider
- player.scraperProvider.* keys added in en.json + ru.json + ja.json
- Playwright e2e proves dropdown visibility + selection persistence
- Changelog.json reflects the user-facing changes
- /animeenigma-after-update closed the milestone (or this plan cleanly skipped without invoking it)
- SCRAPER-HEAL-28 closed (or logged as BLOCKED-ON-PHASE-24 if the dependency is still unmet)
</verification>
