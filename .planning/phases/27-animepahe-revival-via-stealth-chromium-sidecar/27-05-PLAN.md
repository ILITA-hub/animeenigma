---
phase: 27
plan: 05
type: execute
wave: 3
depends_on:
  - 27-04
files_modified:
  - docker/docker-compose.yml
  - frontend/web/public/changelog.json
autonomous: false
requirements:
  - SCRAPER-HEAL-33
user_setup: []

must_haves:
  truths:
    - "`docker/docker-compose.yml` scraper service's `SCRAPER_DEGRADED_PROVIDERS` default flips from `gogoanime,animepahe` to `gogoanime`"
    - "`git diff docker/docker-compose.yml` shows exactly that single value-flip plus a comment update; no other lines change"
    - "Env-override escape hatch is preserved (`${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}` still reads `docker/.env` when set, so operator can re-disable on outage)"
    - "`make redeploy-scraper` brings up scraper with `animepahe` REGISTERED (not SKIPPED) — verified by `docker compose logs scraper | grep \"registered provider\"` listing animepahe"
    - "D7 gate (b): in the first 10 minutes after the redeploy, `make logs-scraper | head -200 | grep -E '403|context deadline' | wc -l` is ≤ 1 — no continuous 403/timeout pattern targeting animepahe"
    - "`/animeenigma-after-update` skill runs: lint + build + redeploy + `frontend/web/public/changelog.json` gets a user-facing entry + commit with co-authors + push"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "Compose default flip + scraper redeployed with animepahe re-registered"
      contains: "${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}"
    - path: "frontend/web/public/changelog.json"
      provides: "User-facing changelog entry (Russian Trump-mode + emoji per CLAUDE.md)"
      contains: "animepahe"
  key_links:
    - from: "docker/docker-compose.yml SCRAPER_DEGRADED_PROVIDERS default"
      to: "services/scraper/internal/config/config.go::parseDegradedProviders"
      via: "comma-split provider names; animepahe removed from default → not SKIPPED at boot"
      pattern: "gogoanime"
---

# Plan 27-05-PLAN.md: Compose Default Cleanup + Post-Ship Redeploy + After-Update

**Plan ID:** 27-05-PLAN
**Wave:** 3
**Plan depends on:** 27-04 (gate-clear verdict log PASS row exists)
**Requirements:** SCRAPER-HEAL-33

## Goal

After the Plan 27-04 gate-clear verdict log shows `animepahe FAIL → PASS`, the `SCRAPER_DEGRADED_PROVIDERS` compose default in `docker/docker-compose.yml` flips from `gogoanime,animepahe` to `gogoanime` (D7 LAST step). `make redeploy-scraper` brings up the stack with animepahe permanently registered. A 10-minute post-redeploy log tail confirms no continuous 403/timeout pattern (D7 gate b). The `/animeenigma-after-update` skill runs: changelog entry committed (informative Russian Trump-mode tone with emoji per CLAUDE.md), all changes pushed with co-authors per `MEMORY.md`.

## Files to Read

- `/data/animeenigma/.planning/phases/27-animepahe-revival-via-stealth-chromium-sidecar/27-CONTEXT.md` — D7 (TWO gates: (a) curl pipeline green from 27-04, (b) 10-minute no-flood log scan from this plan)
- `/data/animeenigma/.planning/phases/27-animepahe-revival-via-stealth-chromium-sidecar/27-VALIDATION.md` — task rows 27-05-01..27-05-03
- `/data/animeenigma/docs/issues/scraper-provider-verification-2026-05-19.md` — precondition check (must contain the Phase 27 post-ship section with `FAIL → PASS` for animepahe)
- `/data/animeenigma/docker/docker-compose.yml` lines 132–139 — the exact `SCRAPER_DEGRADED_PROVIDERS` env block + its leading comment
- `/data/animeenigma/.claude/commands/animeenigma-after-update.md` — the after-update skill the final task invokes
- `/data/animeenigma/CLAUDE.md` "After-Update Skill (MUST USE)" — the mandatory invocation rule

## Files to Touch / Create / Delete

Modify:
- `/data/animeenigma/docker/docker-compose.yml` — flip the env default from `${SCRAPER_DEGRADED_PROVIDERS:-gogoanime,animepahe}` to `${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}` (single value-flip); update the preceding comment block to reflect the new operating posture
- `/data/animeenigma/frontend/web/public/changelog.json` — add user-facing entry for the EN-tab animepahe revival (handled by `/animeenigma-after-update` skill in Task 3, but listed here as a Plan-touched file for the gsd-verify-work audit)

Create: none.
Delete: none.

## Threat Model

Operational only.

`<threat_model>None — operational; sidecar+parser threats are owned by 27-01 + 27-02 per phase-level threat model. The escape-hatch env-override path (`${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}`) is preserved so the operator can re-disable animepahe via `docker/.env` on future outage without a compose-file edit.</threat_model>`

## Tasks

### Task 1: Precondition gate (D7 part a) + flip `SCRAPER_DEGRADED_PROVIDERS` compose default

<action>
First, verify the precondition from Plan 27-04 actually landed — D7 gate (a) is the curl-pipeline pass already recorded in the verdict log:
```bash
grep -q "Post-ship verification — Phase 27" /data/animeenigma/docs/issues/scraper-provider-verification-2026-05-19.md && grep -q "FAIL → PASS" /data/animeenigma/docs/issues/scraper-provider-verification-2026-05-19.md
```
If this check fails, STOP and surface `## PLAN BLOCKED: SCRAPER-HEAL-33 precondition — 27-04 PASS verdict missing from verification log` — do NOT flip the default before the verification evidence exists.

Then edit `/data/animeenigma/docker/docker-compose.yml`. Locate the `SCRAPER_DEGRADED_PROVIDERS` env block (currently at lines 132–139 of the scraper service block, comment + value spanning multiple lines). Change EXACTLY one value: replace `${SCRAPER_DEGRADED_PROVIDERS:-gogoanime,animepahe}` with `${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}`. Update the surrounding comment block (currently describing why both gogoanime and animepahe are degraded) to reflect the new posture — keep gogoanime in the comment as a still-degraded provider (anitaku.to → anineko.to migration; separate phase), remove animepahe from the comment, add a sentence noting "Phase 27 (2026-MM-DD) revived animepahe via the stealth-Chromium sidecar at `services/animepahe-resolver/`; the env-override escape hatch is preserved — set `SCRAPER_DEGRADED_PROVIDERS=gogoanime,animepahe` in `docker/.env` to re-disable on outage." Do NOT touch any other line. Run `docker compose -f docker/docker-compose.yml config scraper | grep SCRAPER_DEGRADED_PROVIDERS` and confirm the resolved value (with no override in docker/.env) is now `gogoanime` (not `gogoanime,animepahe`).
</action>

<verify>
  <automated>grep -q "SCRAPER_DEGRADED_PROVIDERS:-gogoanime}" /data/animeenigma/docker/docker-compose.yml && ! grep -q "SCRAPER_DEGRADED_PROVIDERS:-gogoanime,animepahe}" /data/animeenigma/docker/docker-compose.yml && cd /data/animeenigma && docker compose -f docker/docker-compose.yml config scraper 2>/dev/null | grep -E "SCRAPER_DEGRADED_PROVIDERS:" | grep -v "animepahe"</automated>
</verify>

<done>
The compose default is `${SCRAPER_DEGRADED_PROVIDERS:-gogoanime}`; `animepahe` is no longer in the literal default; the env-override escape hatch is intact.
</done>

**Commit:** `feat(compose): remove animepahe from SCRAPER_DEGRADED_PROVIDERS default — sidecar revival complete (27-05, SCRAPER-HEAL-33)`

---

### Task 2: Redeploy scraper + D7 gate (b) — 10-minute no-flood log scan

<action>
Run `make redeploy-scraper` from `/data/animeenigma`. Wait for the scraper container to flip `(healthy)`: `until docker compose -f docker/docker-compose.yml ps scraper | grep -q '(healthy)'; do sleep 5; done`. Confirm animepahe is now REGISTERED (not SKIPPED): `docker compose -f docker/docker-compose.yml logs --tail=80 scraper | grep -E "registered provider|SKIPPED"`. PASS condition: a `registered provider name=animepahe` log line appears AND NO `SKIPPED.*animepahe` line appears. If the SKIPPED line still appears, the override in `docker/.env` left over from 27-04 was not removed — fix `docker/.env` (remove any `SCRAPER_DEGRADED_PROVIDERS=` line) and re-run `make redeploy-scraper`.

Now run the D7 gate (b) — 10-minute no-flood log scan. Wait 10 minutes from the moment scraper flipped healthy (set a watch: `SCRAPER_HEALTHY_AT=$(date +%s)`; then check `if [ $(($(date +%s) - SCRAPER_HEALTHY_AT)) -lt 600 ]; then sleep 60; fi` in a loop until 10 minutes elapse — operator can split this across other work). After 10 minutes, run the gate:
```bash
make logs-scraper > /tmp/27-05-logs.txt 2>&1 &
LOGS_PID=$!
sleep 3
kill $LOGS_PID 2>/dev/null
head -200 /tmp/27-05-logs.txt | grep -E "403|context deadline" | tee /tmp/27-05-flood.txt | wc -l
```
PASS gate: line count ≤ 1 (transient warmup is allowed; a flood signals the sidecar is silently failing). If the line count is > 1, capture the full log + the resolver `/metrics` (`docker exec animeenigma-animepahe-resolver wget -qO- http://localhost:3000/metrics`) and STOP — do NOT mark the gate green. If the flood is present, the most likely cause is stealth-plugin defeat — check the `stealth_challenge_failures_total` counter; if it's high, apply `STEALTH-PINS.md` "Refresh procedure" before re-running the gate. Last-resort fallback: REVERT the Task 1 compose edit (`git checkout HEAD~1 -- docker/docker-compose.yml`) AND set `docker/.env` `SCRAPER_DEGRADED_PROVIDERS=gogoanime,animepahe`; the operator escape hatch is now active again pending pin refresh. This is the documented fallback per CONTEXT.md "Failure-mode catalog → animepahe.pw itself goes dark".

Also verify the `/scraper/health` gauge for animepahe stays green throughout the 10-minute window — sample at 5 minutes and 10 minutes: `curl -s http://localhost:8088/scraper/health | jq '.providers.animepahe.stages'` should report `up:true` for all four stages at both samples. If any stage flips to `up:false`, capture it as part of the failure-mode evidence.
</action>

<verify>
  <manual>(a) `docker compose logs scraper` shows `registered provider name=animepahe` and NO `SKIPPED.*animepahe`; (b) 10 minutes after redeploy, `make logs-scraper | head -200 | grep -E '403|context deadline' | wc -l` is ≤ 1; (c) `/scraper/health` reports `up:true` for all four animepahe stages at both the 5-minute and 10-minute mark.</manual>
</verify>

<done>
Scraper boots with animepahe registered + no SKIPPED line; D7 gate (b) green — no continuous 403/timeout flood in first 10 minutes; health gauge stable across the window.
</done>

**Commit:** No code-edit commit here (Task 1's compose flip is the only source change; Task 2 is observation-only). Capture the gate-pass evidence in the changelog entry that Task 3 produces.

---

### Task 3: Invoke `/animeenigma-after-update` skill (changelog + commit + push)

<action>
Per CLAUDE.md "After-Update Skill (MUST USE)" this is a mandatory final step. Invoke the `/animeenigma-after-update` skill — see `/data/animeenigma/.claude/commands/animeenigma-after-update.md` for the full workflow. The skill will:
(1) Lint + build affected code (will exercise the sidecar build + scraper build — both should pass; if not, fall back to fixing them before continuing).
(2) Redeploy changed services via `make redeploy-<service>` — for Phase 27 the relevant ones are `make redeploy-animepahe-resolver` AND `make redeploy-scraper`. The skill is allowed to run both.
(3) Run health checks (`make health`) — all services should report healthy including `animepahe-resolver` and `scraper`.
(4) Update `frontend/web/public/changelog.json` with a user-facing entry. Per CLAUDE.md the changelog tone is "informative + enthusiastic with emojis"; per MEMORY.md the changelog Russian Trump-mode shape is in use. The entry MUST be one of (or multiple, ≤ 180 chars each):
  - `"🎉 Английская вкладка снова жива! AnimePahe ожил через новый stealth-Chromium резолвер — Frieren смотрится на ура, 28 эпизодов и ни одного 403."`
  - `"🔧 Под капотом — новый микросервис animepahe-resolver: один тёплый Chromium с puppeteer-extra stealth разруливает DDoS-Guard, парсер пошёл через него вместо прямого фетча."`
  - `"⚡ animepahe убран из SCRAPER_DEGRADED_PROVIDERS по умолчанию — теперь оркестратор реально пробует его, а не пропускает."`
Choose one or two short entries; the exact wording is at the skill's discretion within those constraints. The entry MUST mention animepahe and the sidecar (so users + operators reading the changelog know what changed).
(5) Commit all changes — including the docker-compose.yml flip from Task 1 (if not yet committed by `feat(compose): remove animepahe from SCRAPER_DEGRADED_PROVIDERS default` above) — with the co-authors footer per MEMORY.md:
```
Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>
```
(6) Push to remote.

Note: the after-update skill should NOT add a `services/animepahe-resolver/` to any place that doesn't already reference it (CLAUDE.md services-ports table, etc.); the sidecar is internal-only and not part of the user-facing service registry. The changelog is the only user-facing artifact.
</action>

<verify>
  <manual>(a) `bun run build` (frontend) succeeds; (b) `make redeploy-animepahe-resolver` + `make redeploy-scraper` both succeed; (c) `make health` shows all services healthy; (d) `frontend/web/public/changelog.json` HEAD has a new entry mentioning animepahe + animepahe-resolver; (e) `git log -1` shows co-authors trailer per MEMORY.md; (f) `git push` succeeds.</manual>
</verify>

<done>
After-update skill complete: changelog entry committed, all changes pushed with co-authors per MEMORY.md.
</done>

**Commit:** Handled by the `/animeenigma-after-update` skill itself (per skill workflow). The skill will produce one or more commits — the final one MUST include the co-authors trailer.

---

## Plan Exit Criteria

```bash
# Compose default flipped
grep -q "SCRAPER_DEGRADED_PROVIDERS:-gogoanime}" /data/animeenigma/docker/docker-compose.yml
! grep -q "SCRAPER_DEGRADED_PROVIDERS:-gogoanime,animepahe}" /data/animeenigma/docker/docker-compose.yml
# animepahe is REGISTERED at boot
docker compose -f /data/animeenigma/docker/docker-compose.yml logs --tail=80 scraper | grep -q "registered provider.*animepahe"
! docker compose -f /data/animeenigma/docker/docker-compose.yml logs --tail=80 scraper | grep -q "SKIPPED.*animepahe"
# Changelog has an animepahe entry
grep -q "animepahe" /data/animeenigma/frontend/web/public/changelog.json
# git history has the co-authored commit
git -C /data/animeenigma log -3 --pretty=format:%B | grep -q "Co-Authored-By: Claude"
git -C /data/animeenigma log -3 --pretty=format:%B | grep -q "Co-Authored-By: 0neymik0"
# Phase 27 is complete
```

## Cross-References

- **Backward:** Plan 27-04 (PASS verdict in `docs/issues/scraper-provider-verification-2026-05-19.md` is the precondition for the compose flip per D7).
- **Forward:** Phase 27 ships. The next phase touching this surface is the maintenance-bot's Pattern 7 auto-pin-refresh pipeline (out of scope for Phase 27 per CONTEXT.md "Deferred Ideas — Auto-refreshing the stealth plugin pin on DDoS-Guard rotation").
- **Threat ownership:** none new.
