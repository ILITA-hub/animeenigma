---
phase: 28-provider-expansion-r2
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md
  - services/scraper/internal/providers/miruro/obfuscation.go
  - services/scraper/internal/providers/miruro/obfuscation_test.go
  - services/scraper/internal/providers/miruro/testdata/env2.js
  - services/scraper/internal/providers/miruro/testdata/transform_vectors.json
autonomous: true
requirements:
  - SCRAPER-HEAL-34
user_setup: []

must_haves:
  truths:
    - "A verdict file SPIKE-MIRURO.md exists in the phase directory with a clear `converged` or `killed` line."
    - "If converged: a pure-Go obfuscation transform (HMAC/AES/base64 stdlib only) is committed as `obfuscation.go` + table-driven test asserting at least 3 known-input → known-output vectors."
    - "If converged: a live call from the production server to `pro.ultracloud.cc/<transformed-url>` for Frieren AniList 154587 returned HTTP 200 with parseable HLS or JSON (response excerpt logged in SPIKE-MIRURO.md)."
    - "If killed: SPIKE-MIRURO.md names which D3 convergence gate failed and which residual artifact (if any) ships."
    - "Plan 28-04 (Miruro lift) is conditionally enabled by the verdict line — `converged` ⇒ proceed; `killed` ⇒ skip."
  artifacts:
    - path: ".planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md"
      provides: "Verdict + 4-gate evaluation + Go-port reference impl path or kill rationale"
      contains: "Verdict: converged"
    - path: "services/scraper/internal/providers/miruro/obfuscation.go"
      provides: "Pure-Go port of VITE_PROXY_OBF_KEY transform (CONDITIONAL: only if converged)"
      contains: "func TransformProxyURL"
    - path: "services/scraper/internal/providers/miruro/obfuscation_test.go"
      provides: "Table-driven test with known input/output vectors (CONDITIONAL: only if converged)"
      contains: "TestTransformProxyURL"
    - path: "services/scraper/internal/providers/miruro/testdata/env2.js"
      provides: "Snapshot of upstream env2.js with OBF keys (stability evidence)"
  key_links:
    - from: ".planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md"
      to: "28-04-PLAN.md"
      via: "verdict line — `converged` enables Wave 2; `killed` skips Plan 28-04 to v3.2"
      pattern: "^Verdict:\\s+(converged|killed)$"
    - from: "services/scraper/internal/providers/miruro/obfuscation.go"
      to: "pro.ultracloud.cc upstream"
      via: "TransformProxyURL(endpoint, VITE_PROXY_OBF_KEY) → obfuscated URL"
      pattern: "TransformProxyURL"
---

<objective>
Reverse-engineer Miruro's frontend obfuscation transform (governed by `VITE_PROXY_OBF_KEY=a54d389c18527d9fd3e7f0643e27edbe`) and port it to pure Go using only stdlib `crypto/hmac`, `crypto/sha256`, `crypto/aes`, `crypto/cipher`, `encoding/base64`. This is a Wave 0 spike with a HARD 4-agent-session kill-switch — if any of CONTEXT.md D3's four convergence gates fails, this plan emits `Verdict: killed`, SCRAPER-HEAL-37 rolls to v3.2, and Wave 2 (Plan 28-04) is skipped without blocking the rest of Phase 28.

Purpose: Phase 28's middle failover slot (slot 5: Miruro) depends on whether the obfuscation can be ported cleanly to Go without `chromedp`/`utls`/`tls-client`. Spike-first protects the phase from absorbing unbounded R&D effort into the Miruro lift plan (28-04).

Output: `.planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md` verdict file. If converged, also: `services/scraper/internal/providers/miruro/obfuscation.go` + `obfuscation_test.go` + captured testdata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-provider-expansion-r2/28-CONTEXT.md
@.planning/phases/28-provider-expansion-r2/28-RESEARCH.md

<!-- Per D3, the spike is bounded by 4 agent-sessions of focused effort. -->
<!-- The four convergence gates are non-negotiable verbatim from CONTEXT.md. -->

<interfaces>
<!-- The spike does NOT consume any in-repo Go interfaces; it produces one. -->
<!-- The shape of the produced function (if converged): -->

If converged, the function signature is:

```go
// TransformProxyURL applies the VITE_PROXY_OBF_KEY transform to a plaintext
// upstream endpoint path (e.g. "/api/anime/154587/episodes") and returns
// the obfuscated path that pro.ultracloud.cc accepts. Returns an error if
// the transform inputs are malformed (zero-length endpoint, key shape
// mismatch).
//
// Stdlib only: crypto/hmac, crypto/sha256, crypto/aes, crypto/cipher,
// encoding/base64. NO third-party deps.
func TransformProxyURL(endpoint string, obfKey []byte) (string, error)
```

The corresponding test vectors come from observing the upstream's actual
request shape (DevTools network panel + curl reproduction).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe upstream, capture OBF key stability evidence, identify transform shape</name>
  <files>
    - .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md
    - services/scraper/internal/providers/miruro/testdata/env2.js
    - services/scraper/internal/providers/miruro/testdata/transform_vectors.json
  </files>
  <action>
    Create `.planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md` with the convergence-gate evaluation header (per D3). Then probe upstream from the production server:

    (a) Fetch `https://www.miruro.tv/env2.js` THREE times spaced ≥30s apart and confirm `VITE_PROXY_OBF_KEY` is stable across all three. Capture the first response verbatim to `services/scraper/internal/providers/miruro/testdata/env2.js`. Record the SHA-256 of each of the three responses in SPIKE-MIRURO.md under "Gate 3: Key Stability" — if any differ, mark Gate 3 FAILED and proceed to verdict=killed.

    (b) Fetch the minified frontend bundle from `https://www.miruro.tv/` (look for `<script type="module" src="/assets/index-*.js">`). Check for a source map (`curl -I https://www.miruro.tv/assets/index-*.js.map`). If a source map exists, download it for offline analysis. If absent, use the minified bundle with manual de-minification.

    (c) Trigger one full request flow against the Frieren AniList ID 154587 with browser DevTools network tab open (or via headless reproduction). Capture: (i) the plaintext endpoint the SPA calls upstream (e.g. `/api/anime/154587`), (ii) the obfuscated request URL hitting `pro.ultracloud.cc`. Record at least 3 such (plaintext, obfuscated) pairs in `services/scraper/internal/providers/miruro/testdata/transform_vectors.json` (one per known endpoint family — info, episodes, sources).

    (d) Statically inspect the JS bundle to identify the transform construction. Look for: `crypto.subtle.importKey` / `HMAC` / `AES-CTR` / `AES-GCM` / `base64`. Record the construction family in SPIKE-MIRURO.md under "Transform Shape Identified". If the construction is exotic (e.g., Salsa20, ChaCha, custom xor-mixer not implementable via stdlib primitives) — mark Gate 1 FAILED and proceed to verdict=killed.

    DO NOT execute upstream JS — operate purely statically on the bundle + observed (plaintext, obfuscated) pairs.

    Per CONTEXT.md D3, if EITHER Gate 1 (stdlib-only construction) OR Gate 3 (key stability across 3 fetches) fails — STOP NOW and emit `Verdict: killed` in SPIKE-MIRURO.md. Do not attempt the Go port. Plan 28-04 will be skipped.
  </action>
  <verify>
    <automated>test -s .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md &amp;&amp; test -s services/scraper/internal/providers/miruro/testdata/env2.js &amp;&amp; grep -E '^(Verdict:|Gate [0-9]:)' .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md | grep -v '^#' | head</automated>
  </verify>
  <done>SPIKE-MIRURO.md exists with all 4 gates evaluated; env2.js captured 3x stably; transform construction family identified or marked exotic-fail. If verdict=killed at this task, Tasks 2-3 are skipped and Task 4 finalizes the kill rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Port the transform to pure Go (CONDITIONAL on Task 1 not killing)</name>
  <files>
    - services/scraper/internal/providers/miruro/obfuscation.go
    - services/scraper/internal/providers/miruro/obfuscation_test.go
  </files>
  <action>
    Skip this task entirely if Task 1 set `Verdict: killed`. Otherwise, port the identified transform to pure Go:

    (a) Create `services/scraper/internal/providers/miruro/obfuscation.go` exporting `TransformProxyURL(endpoint string, obfKey []byte) (string, error)`. Implementation uses ONLY stdlib `crypto/hmac`, `crypto/sha256`, `crypto/aes`, `crypto/cipher`, `encoding/base64`, and helpers from `encoding/hex` and `strings` as needed. NO third-party imports. NO calls to `goja`, `chromedp`, `utls`. Document the construction at the top with a `[VERIFIED: testdata/transform_vectors.json]` reference. Validate inputs eagerly: empty endpoint → error; obfKey length mismatch (must match observed shape; standard hex-32 decodes to 16 bytes for AES-128 / HMAC seed) → error.

    (b) Create `services/scraper/internal/providers/miruro/obfuscation_test.go` with a table-driven test `TestTransformProxyURL` using the 3+ vectors captured in Task 1's `transform_vectors.json`. Each row: `{name, endpoint, expected}`. Test loads `testdata/transform_vectors.json` once and iterates. Add at least one negative test (empty endpoint, malformed key length) asserting error returned.

    (c) Run `go test ./services/scraper/internal/providers/miruro/... -race -count=2 -run TestTransformProxyURL` and confirm green.

    Per CONTEXT.md D3 + RESEARCH.md "Don't Hand-Roll" — use stdlib primitives, do not invent the construction. The transform shape was identified in Task 1; this task implements it. If the implementation diverges from observed vectors after 4 agent-sessions of focused work, mark Gate 1 FAILED in SPIKE-MIRURO.md and proceed to verdict=killed via Task 4.
  </action>
  <verify>
    <automated>cd /data/animeenigma &amp;&amp; go test ./services/scraper/internal/providers/miruro/... -race -count=2 -run TestTransformProxyURL 2>&amp;1 | tail -10</automated>
  </verify>
  <done>obfuscation.go compiles, exports TransformProxyURL, uses stdlib only (grep proves no chromedp/utls/goja imports); obfuscation_test.go passes 3+ vectors green; no test skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Live integration probe against pro.ultracloud.cc (CONDITIONAL on Task 2 green)</name>
  <files>
    - .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md
  </files>
  <action>
    Skip this task entirely if Task 1 or Task 2 set `Verdict: killed`. Otherwise, write a small one-shot Go test or `go run` driver in `services/scraper/internal/providers/miruro/obfuscation_test.go` that:

    (a) Calls `TransformProxyURL("/api/anime/154587", obfKey)` for the Frieren AniList ID 154587. (The endpoint shape is captured from Task 1's recon.)
    (b) Makes an actual HTTPS request to `https://pro.ultracloud.cc/<transformed>` from the production server (use `go test -tags=integration` if you want the test gated, or run as a one-shot via `go run ./services/scraper/internal/providers/miruro/cmd/probe/` — pick whichever fits cleanly without polluting unit-test runs).
    (c) Records: HTTP status code, first 500 bytes of response body, parses as either HLS m3u8 or JSON. Append the result to SPIKE-MIRURO.md under "Gate 2: Live Integration" with the request URL, response status, response excerpt, and a verdict line.
    (d) Repeats the call against `/api/anime/154587/episodes` and records the episode count. Per D3 gate 4: "spot-check against Frieren AniList ID returns a non-error episode listing." If count ≥ 1 → Gate 4 PASSED. If 404/403/empty → Gate 4 FAILED.

    Per CONTEXT.md Pitfall 8 — if `pro.ultracloud.cc` returns 403 / HTML Cloudflare challenge from the production IP, mark Gate 2 FAILED. DO NOT add `utls` or `chromedp` to circumvent (forbidden by v3.1-REQUIREMENTS.md non-goals + RESEARCH.md "Don't Hand-Roll"). The 403 IS the kill signal.

    After this task: write the FINAL verdict line at the top of SPIKE-MIRURO.md: `Verdict: converged` (if all 4 gates passed) or `Verdict: killed` (if any failed). Include a one-paragraph rationale.
  </action>
  <verify>
    <automated>grep -E '^Verdict:\s+(converged|killed)$' .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md</automated>
  </verify>
  <done>SPIKE-MIRURO.md has a final `Verdict:` line; if `converged`, all 4 gates evaluated PASSED with evidence; if `killed`, the failing gate(s) are documented with the response/symptom that triggered the kill.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize verdict, update REQUIREMENTS.md traceability, signal downstream</name>
  <files>
    - .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    (a) Confirm SPIKE-MIRURO.md has a final `Verdict:` line on the first non-blank content line so downstream tooling can grep it deterministically.

    (b) Update `.planning/REQUIREMENTS.md` traceability table: add row `| SCRAPER-HEAL-34 | Phase 28 | Complete (Spike — verdict: <converged|killed>) |`. If verdict=killed, also add `| SCRAPER-HEAL-37 | Phase 28 → v3.2 | Carry — Miruro spike killed |` and document the rollover briefly in SPIKE-MIRURO.md.

    (c) In SPIKE-MIRURO.md, add a "Downstream Effect" section spelling out what happens next:
      - If converged: "Plan 28-04 (Miruro lift) proceeds in Wave 2. Reuses `obfuscation.go` from this plan."
      - If killed: "Plan 28-04 (Miruro lift) is SKIPPED. SCRAPER-HEAL-37 rolls to v3.2. Wave 2 of Phase 28 emits no artifacts. Wave 3 (Plans 28-05, 28-06) is unaffected."

    Per CONTEXT.md `<plan_sketch>` Wave gates: this verdict is the gate input to Wave 2's start condition.
  </action>
  <verify>
    <automated>head -5 .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md | grep -E '^Verdict:\s+(converged|killed)$' &amp;&amp; grep -E 'SCRAPER-HEAL-34\s*\|\s*Phase 28' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Verdict line is on a top-of-file line; REQUIREMENTS.md row added for SCRAPER-HEAL-34 (and SCRAPER-HEAL-37 if killed); Downstream Effect section names the conditional behavior of Plan 28-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-host → miruro.tv | Production server fetches public JS bundle + env2.js. No auth crossed. |
| spike-host → pro.ultracloud.cc | Production server makes obfuscated HTTPS request. Treat all upstream response bytes as untrusted. |
| Go transform code → input endpoint | Endpoint string treated as opaque path; transform produces a path, never executes upstream code locally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-00-01 | Tampering | env2.js fetch (Task 1) | mitigate | TLS verification preserved (default Go http.Client). SHA-256 captured to detect tamper-in-transit. |
| T-28-00-02 | Information Disclosure | obfuscation key handling | accept | The key is ALREADY public — every browser visiting miruro.tv downloads it via env2.js. Treating it as a constant is correct. |
| T-28-00-03 | Denial of Service | upstream response size (Task 3) | mitigate | `io.LimitReader(resp.Body, 1<<20)` (1 MiB) for any read in the spike driver. |
| T-28-00-04 | Spoofing | TLS fingerprint scrape by Cloudflare on pro.ultracloud.cc | accept | If Cloudflare returns challenge HTML from prod IP → spike `killed` per D3 Gate 2 (controlled outcome, not a vulnerability). |
| T-28-00-05 | Malicious Code | adding chromedp/utls/tls-client to circumvent | mitigate | v3.1-REQUIREMENTS.md SCRAPER-FOUND-09 CI lint rejects these deps. Spike STAYS on stdlib only per D3 Gate 1. |
| T-28-00-06 | Tampering | malformed transform vectors in testdata/transform_vectors.json | mitigate | Vectors captured manually from upstream observed behavior; test asserts implementation reproduces them byte-exact. Divergence = bug. |
</threat_model>

<verification>
- SPIKE-MIRURO.md exists and contains a `Verdict: converged` or `Verdict: killed` line on a top-of-file line.
- If converged: `go test ./services/scraper/internal/providers/miruro/... -race -count=2 -run TestTransformProxyURL` passes.
- If converged: `grep -E "chromedp|utls|tls-client|flaresolverr|cloudscraper" services/scraper/internal/providers/miruro/obfuscation.go` returns nothing.
- If converged: a live curl from production against `pro.ultracloud.cc/<transformed>` returned HTTP 200 with parseable HLS or JSON (excerpt logged).
- REQUIREMENTS.md traceability table has the SCRAPER-HEAL-34 row updated.
- If killed: REQUIREMENTS.md row for SCRAPER-HEAL-37 marked as v3.2 carryover.
</verification>

<success_criteria>
- SPIKE-MIRURO.md committed with a final verdict line and 4-gate evaluation.
- If converged: `obfuscation.go` + `obfuscation_test.go` + testdata committed; tests green; live probe documented.
- If killed: kill rationale documented; SCRAPER-HEAL-37 rolled to v3.2 in REQUIREMENTS.md; downstream signal (Plan 28-04 skipped) is unambiguous.
- 4-agent-session cap respected — if Task 2/3 still hasn't converged by then, mark killed per D3 (workflow time-box, not effort estimate).
</success_criteria>

<output>
After completion, create `.planning/phases/28-provider-expansion-r2/28-00-SUMMARY.md` capturing:
1. Final verdict (converged | killed)
2. Evidence per D3 gate (1: stdlib-only construction; 2: live HTTP 200; 3: key stability across 3 fetches; 4: Frieren episodes returned)
3. Downstream effect on Plan 28-04 (proceed or skip)
4. If killed: residual artifacts (env2.js snapshot kept for v3.2 retry)
</output>
