---
phase: 28-provider-expansion-r2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md
  - services/scraper/internal/providers/animefever/testdata/search_frieren.html
  - services/scraper/internal/providers/animefever/testdata/info_frieren.html
  - services/scraper/internal/providers/animefever/testdata/watch_ep28.html
  - services/scraper/internal/providers/animefever/testdata/ajax_load_ep28.json
  - services/scraper/internal/providers/animefever/testdata/embed_vidstream_vip.html
autonomous: true
requirements:
  - SCRAPER-HEAL-35
user_setup: []

must_haves:
  truths:
    - "A verdict file SPIKE-ANIMEFEVER.md exists with an ordered list of embed-host(s) AnimeFever proxies to for Frieren."
    - "Each embed host is classified as `existing-registry` (already covered by kwik/megacloud/streamhg/earnvids/vibeplayer) or `needs-new-extractor` (with proposed file name + extraction approach)."
    - "Captured testdata fixtures exist for AnimeFever's search/info/watch/ajax/embed pages so Plan 28-02 can run unit tests offline."
    - "Recon includes the `ctk` token shape (regex pattern), the `sources: [...]` literal shape on the embed page, and the upstream allowlist hosts to add to libs/videoutils/proxy.go."
  artifacts:
    - path: ".planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md"
      provides: "Embed-host classification + recommended extractor names + allowlist host list"
      contains: "Embed Hosts"
    - path: "services/scraper/internal/providers/animefever/testdata/search_frieren.html"
      provides: "Captured search HTML for offline unit tests"
    - path: "services/scraper/internal/providers/animefever/testdata/info_frieren.html"
      provides: "Captured info page with episode listing"
    - path: "services/scraper/internal/providers/animefever/testdata/watch_ep28.html"
      provides: "Captured watch page exposing `var ctk` token + server `<select>`"
    - path: "services/scraper/internal/providers/animefever/testdata/ajax_load_ep28.json"
      provides: "Captured AJAX response with iframe URL"
    - path: "services/scraper/internal/providers/animefever/testdata/embed_vidstream_vip.html"
      provides: "Captured vidstream.vip embed page with inline `sources: [...]` literal"
  key_links:
    - from: ".planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md"
      to: "28-02-PLAN.md and 28-03-PLAN.md"
      via: "Recon output drives both the provider-lift impl (28-02) and the embed-extractor write-list (28-03)"
      pattern: "Recommended Extractors:"
---

<objective>
Recon AnimeFever's data path end-to-end for Frieren MAL 52991 / AniList 154587 and produce a written `SPIKE-ANIMEFEVER.md` artifact that names: (a) the embed hosts AnimeFever proxies to (per D4 — likely `am.vidstream.vip` confirmed in RESEARCH.md), (b) for each host: `existing-registry` (kwik/megacloud/streamhg/earnvids/vibeplayer cover it) or `needs-new-extractor` (proposed file + extraction shape), (c) HLS proxy allowlist hosts to add. Capture testdata fixtures for offline unit tests in Plan 28-02.

Purpose: Plan 28-02 (AnimeFever lift) and Plan 28-03 (new embed extractors) BOTH depend on this recon output. Per CONTEXT.md D4: "We do not pre-implement embed extractors." Recon-driven only.

Output: `.planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md` + 5 captured testdata fixtures committed under `services/scraper/internal/providers/animefever/testdata/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-provider-expansion-r2/28-CONTEXT.md
@.planning/phases/28-provider-expansion-r2/28-RESEARCH.md

<!-- Live recon 2026-05-20 already proved: search/<term> → /info/<slug> → -->
<!-- /watch/<slug>?ep=<id> → POST /ajax/anime/load_episodes_v2?s=tserver -->
<!-- → iframe to am.vidstream.vip → JWPlayer page with inline sources: [...] -->

<interfaces>
<!-- The embed extractors that already exist in the registry: -->

From services/scraper/internal/embeds/:
```
kwik.go          → Matches: kwik.cx, kwik.si                    (AnimePahe-style packed HLS)
megacloud.go     → Matches: megacloud.* (any subdomain)         (HiAnime-era megacloud)
streamhg.go      → Matches: otakuhg.site                        (Dean-Edwards packed)
earnvids.go      → Matches: dramiyos-cdn.com                    (Dean-Edwards packed)
vibeplayer.go    → Matches: vibeplayer.site                     (regex-only, no goja)
```

If AnimeFever's `am.vidstream.vip` is not covered above → `needs-new-extractor`.
The proposed shape (per RESEARCH.md Pattern 2): plain regex against inline `sources: [{"file":"https://...m3u8"}]` literal — NO Dean-Edwards-packer unwrapping needed (verified during research).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the 5-stage AnimeFever data path testdata fixtures for Frieren</name>
  <files>
    - services/scraper/internal/providers/animefever/testdata/search_frieren.html
    - services/scraper/internal/providers/animefever/testdata/info_frieren.html
    - services/scraper/internal/providers/animefever/testdata/watch_ep28.html
    - services/scraper/internal/providers/animefever/testdata/ajax_load_ep28.json
    - services/scraper/internal/providers/animefever/testdata/embed_vidstream_vip.html
  </files>
  <action>
    Reproduce the live recon flow from RESEARCH.md ("Sources / Primary / Live recon 2026-05-20") from the production server and capture each response body to testdata. Use a fresh cookie jar (Go's `cookiejar.New(&cookiejar.Options{PublicSuffixList: publicsuffix.List})`) so PHPSESSID propagation matches what Plan 28-02 will rely on.

    (a) `GET https://animefever.cc/search/frieren` → write response HTML to `testdata/search_frieren.html`. Confirm body contains `<div class="card-block">` cards with `<h3 a href="https://animefever.cc/info/<slug>">` — the search-card shape from RESEARCH.md `Code Examples / AnimeFever search + episode list`. If body shape doesn't match → spike fails this step; document deviation in SPIKE-ANIMEFEVER.md.

    (b) Extract the best-matching slug from search HTML (visually + matched against "Frieren - Beyond Journey's End"). `GET https://animefever.cc/info/<slug>` → write to `testdata/info_frieren.html`. Confirm body contains `<a href="...watch/<slug>?ep=<id>">` links — at least 28 for Frieren S1 per CONTEXT.md D6.

    (c) Pick episode 28 (the highest-numbered). `GET https://animefever.cc/watch/<slug>?ep=<eid>` → write to `testdata/watch_ep28.html`. Confirm body contains `var ctk = '<32hex>';` and a server `<select>` (or equivalent) listing `tserver` + `hserver` (per Pitfall 3). Record the actual `ctk` value into SPIKE-ANIMEFEVER.md "Token Shape" section.

    (d) Extract the episode_id from the watch page (per Pitfall 2). `POST https://animefever.cc/ajax/anime/load_episodes_v2?s=tserver` with form `episode_id=<eid>&ctk=<token>` + headers `Content-Type: application/x-www-form-urlencoded`, `X-Requested-With: XMLHttpRequest`, `Referer: https://animefever.cc/watch/<slug>?ep=<eid>` → write to `testdata/ajax_load_ep28.json`. Confirm body is `{"status":true,"value":"<iframe ...>","embed":true,...}`. If `status:false` returned → ctk capture failed; re-run Task 1c.

    (e) Extract the iframe `src` from the AJAX value (regex `<iframe[^>]+src=['"]([^'"]+)['"]`). `GET <iframe-src>` → write to `testdata/embed_vidstream_vip.html`. Confirm body contains inline `sources: [{"file":"https://...m3u8","type":"...","label":"..."}]` literal (per RESEARCH.md `Pattern 2`). Record the exact regex anchor (the `sources\s*:\s*\[` prefix) and the JSON shape into SPIKE-ANIMEFEVER.md "Embed Page Shape" section.

    Mask sensitive fields (PHPSESSID values, request IDs) with `<masked>` in any committed text excerpts within SPIKE-ANIMEFEVER.md, but commit raw HTML/JSON bytes to testdata (no secrets — server pages are publicly cacheable). All 5 testdata files must exist after this task.
  </action>
  <verify>
    <automated>for f in services/scraper/internal/providers/animefever/testdata/{search_frieren.html,info_frieren.html,watch_ep28.html,ajax_load_ep28.json,embed_vidstream_vip.html}; do test -s "$f" || { echo "MISSING: $f"; exit 1; }; done &amp;&amp; grep -c 'var ctk' services/scraper/internal/providers/animefever/testdata/watch_ep28.html | grep -v '^0$' &amp;&amp; grep -c '"status"' services/scraper/internal/providers/animefever/testdata/ajax_load_ep28.json | grep -v '^0$'</automated>
  </verify>
  <done>All 5 testdata files committed with non-empty content; watch_ep28.html contains a `var ctk` declaration; ajax_load_ep28.json has the expected status/value/embed shape; embed_vidstream_vip.html contains the inline sources literal.</done>
</task>

<task type="auto">
  <name>Task 2: Classify embed hosts and write SPIKE-ANIMEFEVER.md verdict</name>
  <files>
    - .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md
  </files>
  <action>
    Create `.planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md` with the following sections (per D4):

    (a) **Embed Hosts**: enumerate every host AnimeFever proxies to for Frieren ep28 (across BOTH `tserver` AND `hserver`). For each: hostname, observed during which step (ajax/value iframe src), and the URL shape.

    Repeat Task 1d with `s=hserver` so the recon covers both servers (per Pitfall 3) — capture the alternate iframe URL but do NOT overwrite `ajax_load_ep28.json`; instead append a second `ajax_load_ep28_hserver.json` if needed and reference both in SPIKE-ANIMEFEVER.md.

    (b) **Classification per host**: for each host, mark one of:
      - `existing-registry: <extractor-name>` — Matches() check confirmed against `services/scraper/internal/embeds/{kwik,megacloud,streamhg,earnvids,vibeplayer}.go` host lists.
      - `needs-new-extractor: <proposed-filename>` — propose a name (per D4 + RESEARCH.md Pattern 2 the expected new file is `embeds/vidstream_vip.go`). Note the extraction approach (regex / packed-unpack / API).

    Expected result per RESEARCH.md: `am.vidstream.vip` → `needs-new-extractor: vidstream_vip.go` with regex extraction of inline `sources: [{"file":"...m3u8"}]`. The CDN-host (e.g. `static-cdn-ca1.mofl.pro`) is NOT an extractor target — it's where the HLS m3u8 actually lives, and only needs `HLSProxyAllowedDomains` entry.

    (c) **Recommended Extractors for Plan 28-03**: an ordered numbered list of `embeds/<name>.go` files to write. Each item names:
      - file path
      - input host(s) it matches
      - upstream Referer to set
      - extraction approach (plain regex / packed-unpack / API call)
      - one captured test fixture path (which testdata file proves the shape)
      - approximate line count estimate

    (d) **HLS Proxy Allowlist Hosts**: list the upstream hosts to add to `libs/videoutils/proxy.go::HLSProxyAllowedDomains` in Plan 28-02's main.go-change commit (per D7). Expected (from RESEARCH.md): `am.vidstream.vip`, `static-cdn-ca1.mofl.pro`. Verify the actual CDN host by inspecting the m3u8 URL in `embed_vidstream_vip.html`'s sources literal.

    (e) **Token Shape**: record the regex pattern Plan 28-02 will use for `ctk`. Default expectation per RESEARCH.md Pitfall 2 + A8: `var\s+ctk\s*=\s*'([0-9a-fA-F]{32,64})'`. If the observed value violates this (e.g. UUID format with hyphens) → adjust the regex and document the deviation.

    (f) **Per-Server Coverage**: note whether `tserver` carries Frieren ep28 AND whether `hserver` does. If only one carries → CONTEXT.md A2 risk surfaces; document fallback strategy for Plan 28-02's GetStream.

    (g) **Verdict line** at the top of the file: `Verdict: ready — Plan 28-02 may proceed with N existing-registry extractors + M new extractors`.
  </action>
  <verify>
    <automated>test -s .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md &amp;&amp; grep -E '^Verdict:\s+ready' .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md &amp;&amp; grep -E 'Recommended Extractors' .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md &amp;&amp; grep -E 'HLS Proxy Allowlist' .planning/phases/28-provider-expansion-r2/SPIKE-ANIMEFEVER.md</automated>
  </verify>
  <done>SPIKE-ANIMEFEVER.md has all 7 sections; Verdict line is at top; recon classifies every observed embed host; allowlist list names specific hosts; Plan 28-03's write-list is unambiguous.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md traceability for SCRAPER-HEAL-35</name>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    Add a row to the traceability table in `.planning/REQUIREMENTS.md`:
    `| SCRAPER-HEAL-35 | Phase 28 | Complete (Spike — recon classified embeds; see SPIKE-ANIMEFEVER.md) |`

    If REQUIREMENTS.md does not have a v3.1 traceability section, add a "v3.1 SCRAPER-HEAL traceability" section at the end mirroring the v3.0 table shape, with rows for SCRAPER-HEAL-34..39 (status pending for those not yet started).
  </action>
  <verify>
    <automated>grep -E 'SCRAPER-HEAL-35\s*\|\s*Phase 28' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md has a SCRAPER-HEAL-35 row referencing this plan and the SPIKE-ANIMEFEVER.md artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike-host → animefever.cc | Production server scrapes public HTML; PHPSESSID cookie acquired and used in same request chain only. No auth credentials transmitted. |
| spike-host → am.vidstream.vip (embed) | Production server fetches embed page (untrusted HTML), extracts inline JSON literal only. No JS executed. |
| testdata files → repo | Captured HTML/JSON committed. Mask request-specific cookie values in any SPIKE-ANIMEFEVER.md prose excerpts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Information Disclosure | PHPSESSID cookie leaked into committed testdata HTML | mitigate | Sanitize HTML excerpts in SPIKE-ANIMEFEVER.md prose; commit raw HTML bytes only if they contain no per-session secrets (verified — public watch pages are session-agnostic). |
| T-28-01-02 | Denial of Service | upstream returns multi-MB HTML | mitigate | `io.LimitReader(resp.Body, 4<<20)` (4 MiB) in any Go-based recon driver. Manual `curl` capture naturally caps at upstream response size; verify each testdata file ≤ 4 MiB before commit. |
| T-28-01-03 | Tampering | upstream HTML shape changes between capture and Plan 28-02 ship | accept | RESEARCH.md "Valid until: 2026-06-19" — recon valid for 30 days. Plan 28-02 runs within window; if HTML drifts, daily canary catches per Phase 23. |
| T-28-01-04 | Malicious Code | naively running upstream JS as part of recon | mitigate | Recon is STATIC: capture bytes, extract via regex/goquery. No headless browser. No goja execution of upstream JS. |
</threat_model>

<verification>
- 5 testdata files exist with non-empty content matching expected shapes.
- SPIKE-ANIMEFEVER.md exists with `Verdict: ready` line at top.
- Every embed host observed is classified `existing-registry: <name>` or `needs-new-extractor: <file>`.
- HLS proxy allowlist hosts enumerated for Plan 28-02's allowlist commit.
- `ctk` regex pattern recorded for Plan 28-02's `ctkRegex` constant.
- Per-server coverage (tserver vs hserver) documented.
- REQUIREMENTS.md row added for SCRAPER-HEAL-35.
</verification>

<success_criteria>
- Plan 28-02 (AnimeFever lift) can execute against the captured fixtures offline without needing network access.
- Plan 28-03 (new embed extractors) has an unambiguous write-list — zero speculative writes per D4.
- The allowlist host list is explicit (no "TBD"); Plan 28-02's `libs/videoutils/proxy.go` diff is determined.
</success_criteria>

<output>
After completion, create `.planning/phases/28-provider-expansion-r2/28-01-SUMMARY.md` capturing:
1. List of embed hosts AnimeFever proxies to + classification each
2. Recommended new extractors (file paths + extraction approach)
3. HLS proxy allowlist hosts to add in Plan 28-02
4. Token shape (ctk regex)
5. Per-server (tserver vs hserver) coverage observations
</output>
