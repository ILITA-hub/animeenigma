---
phase: 28-provider-expansion-r2
plan: 04
type: execute
wave: 2
depends_on:
  - 28-00
files_modified:
  - services/scraper/internal/providers/miruro/doc.go
  - services/scraper/internal/providers/miruro/client.go
  - services/scraper/internal/providers/miruro/cache.go
  - services/scraper/internal/providers/miruro/dto.go
  - services/scraper/internal/providers/miruro/client_test.go
  - services/scraper/internal/providers/miruro/testdata/env2.js
  - services/scraper/internal/providers/miruro/testdata/info_154587.json
  - services/scraper/internal/providers/miruro/testdata/episodes_154587.json
  - services/scraper/internal/providers/miruro/testdata/sources_154587_ep1.json
  - services/scraper/internal/config/config.go
  - services/scraper/internal/config/config_test.go
  - services/scraper/cmd/scraper-api/main.go
  - libs/videoutils/proxy.go
autonomous: false
requirements:
  - SCRAPER-HEAL-37
user_setup: []

must_haves:
  truths:
    - "CONDITIONAL: This plan executes ONLY if Plan 28-00's SPIKE-MIRURO.md ends with `Verdict: converged`. If verdict=killed, this plan is SKIPPED — no artifacts emitted, SCRAPER-HEAL-37 carries to v3.2, Wave 3 proceeds unaffected."
    - "On converged path: Package `services/scraper/internal/providers/miruro/` exists; `miruro.Provider` satisfies `domain.Provider`; FindID resolves via ARM (MAL → AniList → /anime/<anilist_id>); all upstream calls route through deobfuscated `pro.ultracloud.cc` using the pure-Go transform from Plan 28-00's `obfuscation.go`."
    - "On converged path: Provider registered in `services/scraper/cmd/scraper-api/main.go` AFTER AnimeFever in failover-chain order (slot 5 per D5)."
    - "On converged path: HLS proxy allowlist adds `pro.ultracloud.cc` + `pru.ultracloud.cc` per D7."
    - "On converged path: Frieren AniList 154587 E2E gate returns ≥28 episodes via `?provider=miruro`."
    - "Frontmatter, behavior, and test assertions reference the conditional skip path explicitly so a fresh executor doesn't accidentally proceed against a `killed` verdict."
  artifacts:
    - path: "services/scraper/internal/providers/miruro/client.go"
      provides: "Provider struct + 6 domain.Provider methods; uses obfuscation.go from Plan 28-00 for upstream URL transforms"
      min_lines: 300
      contains: "func (p *Provider) FindID"
    - path: "services/scraper/internal/providers/miruro/cache.go"
      provides: "4 key families: showID/episodes/servers/stream with miruro: prefix"
      min_lines: 70
    - path: "services/scraper/internal/providers/miruro/dto.go"
      provides: "JSON shapes for /api/anime/<id> info/episodes/sources endpoints"
      min_lines: 40
    - path: "services/scraper/internal/providers/miruro/client_test.go"
      provides: "Table-driven tests against captured testdata"
      min_lines: 150
      contains: "var _ domain.Provider = (*Provider)(nil)"
  key_links:
    - from: "services/scraper/internal/providers/miruro/client.go"
      to: "services/scraper/internal/providers/miruro/obfuscation.go (from Plan 28-00)"
      via: "TransformProxyURL(endpoint, obfKey) returns the path component sent to pro.ultracloud.cc"
      pattern: "TransformProxyURL"
    - from: "libs/idmapping (ARM client)"
      to: "miruro.FindID"
      via: "MAL ID → AniList ID resolution; Miruro URL shape is /anime/<anilist_id>"
      pattern: "idmapping"
---

<objective>
**This plan is CONDITIONAL on Plan 28-00's verdict.** Before doing ANY implementation work, the executor MUST read `.planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md` and find the `Verdict:` line:
- `Verdict: converged` → proceed with all tasks below.
- `Verdict: killed` → SKIP THIS PLAN ENTIRELY. Emit a one-line `28-04-SUMMARY.md` documenting the skip and move on. SCRAPER-HEAL-37 carries to v3.2 per D3 kill-switch.

On the converged path, implement the Miruro provider as a new self-contained package under `services/scraper/internal/providers/miruro/`. FindID resolves via ARM (`libs/idmapping/`) translating MAL ID → AniList ID (Miruro's URL shape is `/anime/<anilist_id>`). All upstream calls route through the deobfuscated `pro.ultracloud.cc` proxy using `TransformProxyURL` from Plan 28-00's `obfuscation.go`. Registered in main.go as failover slot 5. HLS proxy allowlist updated per D7.

Purpose: Add the secondary fallback (slot 5) between AnimeFever (slot 4) and 9anime (slot 6). Slot 5 is reached when both allanime AND animefever are unhealthy.

Output (CONDITIONAL on converged): New `miruro` package + config block + main.go registration + 2 new allowlist entries.
Output (CONDITIONAL on killed): A 28-04-SUMMARY.md skip notice. NO code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-provider-expansion-r2/28-CONTEXT.md
@.planning/phases/28-provider-expansion-r2/28-RESEARCH.md
@.planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md

@services/scraper/internal/providers/allanime/client.go
@services/scraper/internal/providers/allanime/cache.go
@services/scraper/internal/providers/animepahe/client.go
@services/scraper/internal/domain/provider.go
@services/scraper/internal/domain/embed.go
@services/scraper/internal/domain/errors.go
@services/scraper/internal/health/stage.go
@libs/idmapping

<interfaces>
<!-- The transform function provided by Plan 28-00 (only exists if converged): -->

From services/scraper/internal/providers/miruro/obfuscation.go (Plan 28-00):
```go
func TransformProxyURL(endpoint string, obfKey []byte) (string, error)
```

ARM client (libs/idmapping/):
```go
// Looks up cross-source ID mapping. ARM source=myanimelist returns AniList ID.
type Client interface {
    LookupByMAL(ctx context.Context, malID string) (*Mapping, error)
}
type Mapping struct {
    AniList string
    MAL     string
    // ... other sources
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Read SPIKE-MIRURO.md verdict; gate Tasks 1-4 on converged</name>
  <files>(checkpoint — no files modified; verification only)</files>
  <action>
  <decision>Proceed with Miruro lift, OR skip this plan?</decision>
  <context>
    Plan 28-00 emitted a verdict. Per CONTEXT.md D3, this plan is CONDITIONAL on that verdict:
    - converged: implement the provider as designed; ship in failover slot 5.
    - killed: skip the plan entirely; SCRAPER-HEAL-37 carries to v3.2.
  </context>
  <options>
    <option id="proceed">
      <name>Verdict converged — proceed with Tasks 1-4</name>
      <pros>Phase 28 ships with the full 3-provider expansion (animefever + miruro + nineanime). UXΔ ceiling +2.</pros>
      <cons>Implementation effort absorbed; integration risk on the obfuscation path.</cons>
    </option>
    <option id="skip">
      <name>Verdict killed — skip Tasks 1-4; emit a skip-only 28-04-SUMMARY.md</name>
      <pros>Honors the 4-agent-session kill-switch; SCRAPER-HEAL-37 cleanly rolls to v3.2; Wave 3 continues unaffected.</pros>
      <cons>Phase 28 ships with 2 providers instead of 3. UXΔ ceiling +1.</cons>
    </option>
  </options>
  </action>
  <verify>
    <automated>echo "Human-verification checkpoint — operator manually runs the gates above and returns approval signal."</automated>
  </verify>
  <done>Operator confirmed all gates passed via the resume signal.</done>

  <resume-signal>Auto-resolved by reading `head -5 .planning/phases/28-provider-expansion-r2/SPIKE-MIRURO.md | grep -E '^Verdict:'`. The executor agent acts on the matched value: `converged` → execute Tasks 1-4; `killed` → skip to Task 5 (skip-summary).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold miruro package + config block (CONDITIONAL on converged)</name>
  <files>
    - services/scraper/internal/providers/miruro/doc.go
    - services/scraper/internal/providers/miruro/dto.go
    - services/scraper/internal/providers/miruro/cache.go
    - services/scraper/internal/config/config.go
    - services/scraper/internal/config/config_test.go
  </files>
  <behavior>
    - Test 1: `config.Load()` reads `SCRAPER_MIRURO_BASE_URL` env var with default `https://www.miruro.tv` and `SCRAPER_MIRURO_PROXY_A` with default `https://pro.ultracloud.cc`; invalid URL fails Load().
    - Test 2: `cache.go` newCacheLayer constructs without panic; keys are prefixed `miruro:`.
  </behavior>
  <action>
    SKIP this task if Task 0 chose `skip`. Otherwise:

    (a) Create `doc.go` documenting the upstream path (Vite SPA → env2.js OBF keys → pro.ultracloud.cc proxy), the obfuscation port from Plan 28-00, and the AniList-ID-as-providerID convention.

    (b) Create `dto.go` with JSON shapes matching the observed `/api/anime/<id>` response. The exact shapes come from Plan 28-00's recon — the executor must inspect the captured `transform_vectors.json` plus do a live spot-check to confirm the response JSON shape for episodes + sources. Document each field's source.

    (c) Create `cache.go` modeled after `allanime/cache.go` — 4 key families (`miruro:show:`, `miruro:episodes:`, `miruro:servers:`, `miruro:stream:`) with TTLs matching allanime/animepahe.

    (d) Extend `services/scraper/internal/config/config.go` with `MiruroConfig{BaseURL string, ProxyURL string, ProxyURLAlt string}` and bind env vars `SCRAPER_MIRURO_BASE_URL` (default `https://www.miruro.tv`), `SCRAPER_MIRURO_PROXY_A` (default `https://pro.ultracloud.cc`), `SCRAPER_MIRURO_PROXY_B` (default `https://pru.ultracloud.cc`). Add URL parse validation matching Gogoanime block. Add tests in `config_test.go`.

    Per D3 gate 3: The obfuscation keys are public AND stable; we fetch from env2.js at startup OR pin in code per spike outcome. Default: pin in code as constants, document the SPIKE-MIRURO.md observed values. If SPIKE-MIRURO.md noted key-rotation risk, prefer runtime fetch with 24h cache.
  </action>
  <verify>
    <automated>cd /data/animeenigma; go build ./services/scraper/internal/providers/miruro/...; go test ./services/scraper/internal/config/... -race -count=2 -run Miruro</automated>
  </verify>
  <done>4 files committed; package compiles; config_test for Miruro passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement client.go (Provider struct + 6 methods) with tests (CONDITIONAL on converged)</name>
  <files>
    - services/scraper/internal/providers/miruro/client.go
    - services/scraper/internal/providers/miruro/client_test.go
    - services/scraper/internal/providers/miruro/testdata/info_154587.json
    - services/scraper/internal/providers/miruro/testdata/episodes_154587.json
    - services/scraper/internal/providers/miruro/testdata/sources_154587_ep1.json
  </files>
  <behavior>
    - Test 1 (FindID via ARM): given a stub ARM client returning AniList="154587" for MAL="52991", FindID(ctx, {ShikimoriID:"52991"}) returns "154587".
    - Test 2 (FindID empty MAL): FindID with empty ShikimoriID + non-empty Title falls back to title search OR returns ErrNotFound (pick the simpler path; document choice).
    - Test 3 (ListEpisodes): given httptest serving `testdata/episodes_154587.json` for the deobfuscated path, ListEpisodes(ctx, "154587") returns ≥28 episodes for Frieren S1.
    - Test 4 (ListServers): given a captured servers fixture, returns a non-empty slice of Server entries.
    - Test 5 (GetStream): returns a Stream with at least one Source — either an HLS m3u8 URL pointing to a CDN host (one that the calling site's HLS proxy can serve) or delegates to an embed extractor if Miruro proxies through a separate player host.
    - Test 6 (markStage success/failure): mirror allanime's tests.
    - Test 7 (compile-time): `var _ domain.Provider = (*Provider)(nil)`.
  </behavior>
  <action>
    SKIP this task if Task 0 chose `skip`. Otherwise:

    (a) Create `client.go` modeled after `allanime/client.go` with the following adaptations:
      - `Deps{BaseURL, ProxyURL, ProxyURLAlt, ObfKey []byte, HTTP *domain.BaseHTTPClient, Embeds *domain.Registry, IDMapping idmapping.Client, Cache cache.Cache, Log *logger.Logger}` — `IDMapping` is new (vs allanime/animefever).
      - `New(d Deps)` validates all required deps; default ProxyURLAlt to the alt host from env2.js (pru.ultracloud.cc).
      - `FindID(ctx, ref)`:
        - Cache key uses ShikimoriID. On cache hit return.
        - If `ref.ShikimoriID != ""` use ARM client to look up MAL→AniList ID. The cached/returned providerID is the AniList ID as a string.
        - On ARM error or missing mapping → WrapNotFound.
      - `ListEpisodes(ctx, aniListID)`:
        - Build endpoint `/api/anime/<aniListID>/episodes` (exact path per Task 1 recon).
        - Apply `TransformProxyURL(endpoint, obfKey)` → get obfuscated path.
        - GET `<ProxyURL>/<obf-path>` via BaseHTTPClient.
        - On 5xx/network → WrapProviderDown; on 4xx → WrapExtractFailed. On parsing failure → WrapExtractFailed.
        - On success → unmarshal episodes, sort by Number ascending, materialize as `[]domain.Episode`.
        - Cache; markStage.
      - `ListServers(ctx, aniListID, episodeID)` + `GetStream(ctx, aniListID, episodeID, serverID, category)`:
        - Same shape as ListEpisodes but for the servers/sources endpoints; deobfuscate, fetch via ProxyURL, parse.
        - If Miruro's sources response embeds an iframe to a separate player host: pass to `p.embeds.Find(iframeURL).Extract(...)` — but per RESEARCH.md it's more likely that pro.ultracloud.cc directly returns the m3u8 URL; in that case build the Stream inline.
        - On primary `ProxyURL` failure with 5xx/timeout, retry once against `ProxyURLAlt` (the alt proxy from env2.js).
      - `HealthCheck(ctx)` mirrors allanime.

    (b) Capture testdata fixtures from live (using the converged transform) for: info, episodes, sources. Mask any per-request secrets. Save under `testdata/`.

    (c) Tests use `httptest.NewServer` for `pro.ultracloud.cc` mock, an in-memory stub ARM client, and a fake embed Registry. Cover the 7 behaviors above.

    Per RESEARCH.md "Don't Hand-Roll": use `libs/idmapping/` ARM client; don't write a new ID resolver. Use BaseHTTPClient; don't make a new http.Client.
  </action>
  <verify>
    <automated>cd /data/animeenigma; go test ./services/scraper/internal/providers/miruro/... -race -count=2</automated>
  </verify>
  <done>client.go compiles; tests green; `var _ domain.Provider = (*Provider)(nil)` present; no chromedp/utls; client.go uses TransformProxyURL from obfuscation.go.</done>
</task>

<task type="auto">
  <name>Task 3: Register provider in main.go (failover slot 5) + HLS proxy allowlist (CONDITIONAL on converged)</name>
  <files>
    - services/scraper/cmd/scraper-api/main.go
    - libs/videoutils/proxy.go
  </files>
  <action>
    SKIP this task if Task 0 chose `skip`. Otherwise:

    (a) Edit `services/scraper/cmd/scraper-api/main.go` to register Miruro AFTER the AnimeFever registration from Plan 28-02 and BEFORE the AnimeKai gated block. Mirror the AllAnime/AnimeFever registration shape verbatim. Include the ARM ID-mapping client construction (per `libs/idmapping/` package; mirror existing usages in catalog if any).

    (b) Add `"miruro"` to the `candidateProviders` slice in failover-chain order (after `"animefever"`, before `"animekai"` if Enabled).

    (c) Edit `libs/videoutils/proxy.go::HLSProxyAllowedDomains` to add the Miruro-required hosts (per D7):
    ```
    // Phase 28 (SCRAPER-HEAL-37) — Miruro proxy + alt proxy hosts.
    "pro.ultracloud.cc",
    "pru.ultracloud.cc",
    ```
    Also add any CDN hosts the actual m3u8s sit on (these surface during testdata capture in Task 2 — append per the SPIKE-MIRURO.md "Downstream Hosts Observed" section).

    (d) Add `import` for `"github.com/ILITA-hub/animeenigma/services/scraper/internal/providers/miruro"` to main.go.
  </action>
  <verify>
    <automated>cd /data/animeenigma; go build ./services/scraper/cmd/scraper-api/...</automated>
  </verify>
  <done>main.go imports + registers miruro after animefever; candidateProviders slice updated; allowlist updated; `go build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Frieren AniList 154587 E2E gate (CONDITIONAL on converged)</name>
  <files>(checkpoint — no files modified; verification only)</files>
  <action>
  <what-built>
    Miruro provider lift complete (only if Plan 28-00 converged). Failover slot 5 active. Allowlist updated.
  </what-built>
  <how-to-verify>
    SKIP this task if Task 0 chose `skip`. Otherwise:

    1. `make redeploy-scraper && make redeploy-streaming`
    2. `curl -s http://localhost:8088/scraper/health | jq '.providers.miruro.stages'` — expect 5 stages, all up:true after probe activity.
    3. **Frieren E2E gate (per CONTEXT.md D6):**
       ```
       curl -s 'http://localhost:8000/api/anime/f0b40660-6627-4a59-8dcf-7ec8596b3623/scraper/episodes?provider=miruro' | jq '. | length'
       ```
       Expected: ≥ 28.
    4. **Stream resolution:**
       ```
       curl -s 'http://localhost:8000/api/anime/f0b40660-6627-4a59-8dcf-7ec8596b3623/scraper/stream?provider=miruro&episode=<eid>' | jq '.data.sources[0]'
       ```
       Expected: `{url: "https://...", type: "hls" or "mp4", ...}`.
    5. **HLS proxy gate:** confirm the m3u8 URL host resolves through `pro.ultracloud.cc` allowlist entry (200, not 502).
  </how-to-verify>
  </action>
  <verify>
    <automated>echo "Human-verification checkpoint — operator manually runs the gates above and returns approval signal."</automated>
  </verify>
  <done>Operator confirmed all gates passed via the resume signal.</done>

  <resume-signal>Type "approved" if Frieren gate passes, or "killed-post-impl" if implementation revealed the spike was over-optimistic (then update SPIKE-MIRURO.md verdict to killed-post-impl + roll SCRAPER-HEAL-37 to v3.2).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Skip-path summary (CONDITIONAL on killed)</name>
  <files>
    - .planning/phases/28-provider-expansion-r2/28-04-SUMMARY.md
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    EXECUTE THIS TASK ONLY IF Task 0 chose `skip` (SPIKE-MIRURO.md verdict=killed). Otherwise SKIP.

    (a) Create `.planning/phases/28-provider-expansion-r2/28-04-SUMMARY.md` with a single section:
    ```
    # 28-04 SUMMARY — SKIPPED

    Status: SKIPPED per CONTEXT.md D3 kill-switch.
    Reason: Plan 28-00's SPIKE-MIRURO.md ended with `Verdict: killed`.
    SCRAPER-HEAL-37 carries to v3.2.
    No code changes shipped. Wave 3 (Plans 28-05, 28-06) proceeds unaffected.
    ```

    (b) Update `.planning/REQUIREMENTS.md` traceability: ensure SCRAPER-HEAL-37 is marked `| Phase 28 → v3.2 | Carry — Miruro spike killed |`.

    (c) No git changes beyond these two .md files.
  </action>
  <verify>
    <automated>test -s .planning/phases/28-provider-expansion-r2/28-04-SUMMARY.md</automated>
  </verify>
  <done>Skip-summary exists OR Task 5 was correctly skipped because Task 0 chose `proceed`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper service → pro.ultracloud.cc | Outbound HTTPS via deobfuscated path. Response untrusted. |
| scraper service → ARM (arm.haglund.dev) | Outbound HTTPS for ID mapping. Already used by libs/idmapping; no new boundary. |
| frontend → HLS proxy for m3u8 from CDN behind pro.ultracloud.cc | Allowlist gates host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Spoofing | Cloudflare challenge on pro.ultracloud.cc | accept | If observed during impl, mark spike killed-post-impl and roll SCRAPER-HEAL-37 to v3.2 (no utls workaround per D3 gate 2 + non-goals). |
| T-28-04-02 | Cryptography | obfuscation key handling | accept | Key is public (env2.js published to all browsers). Pinned-vs-runtime-fetch choice in Task 1 documented. |
| T-28-04-03 | Tampering | obfuscation transform produces wrong path | mitigate | obfuscation_test.go from Plan 28-00 asserts byte-exact reproduction against captured vectors. CI catches drift. |
| T-28-04-04 | SSRF | TransformProxyURL takes untrusted endpoint input | mitigate | All endpoint strings constructed internally from validated providerID + path fragments; no user-supplied raw input crosses transform. |
| T-28-04-05 | DoS | upstream multi-MB response | mitigate | BaseHTTPClient 4 MiB cap. |
| T-28-04-06 | Information Disclosure | obfuscated URLs logged | mitigate | Log at DEBUG only; per-request URL not in INFO logs. Mirror existing provider log shape. |
| T-28-04-07 | DoS | rate-limit abuse against pro.ultracloud.cc | mitigate | `WithPerHostRPS("pro.ultracloud.cc", 0.5, 2)` — half of standard since this is a Cloudflare-fronted host with stricter pacing expectations. |
</threat_model>

<verification>
- **Conditional gate**: Task 0 outcome documented before any code change.
- On converged: tests green; Frieren E2E gate returns ≥28; HLS proxy gate green; allowlist updated.
- On killed: 28-04-SUMMARY.md exists with skip rationale; REQUIREMENTS.md row reflects v3.2 carryover; no code changes shipped.
- No new external Go deps in either path.
</verification>

<success_criteria>
- The plan's outcome (proceed or skip) is unambiguous and traceable to SPIKE-MIRURO.md's verdict line.
- On converged: Miruro is in failover slot 5; orchestrator falls through allanime → animefever → miruro on health failures.
- On killed: Phase 28 ships with 2 of 3 planned new providers; v3.2 absorbs Miruro lift; no orphaned code in repo.
</success_criteria>

<output>
After completion, create `.planning/phases/28-provider-expansion-r2/28-04-SUMMARY.md`:
- If proceed: capture package shape, test fixtures, registration position, Frieren E2E evidence, deviations from RESEARCH.md.
- If skip: short skip notice referencing SPIKE-MIRURO.md verdict.
</output>
