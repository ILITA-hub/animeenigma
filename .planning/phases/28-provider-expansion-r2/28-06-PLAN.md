---
phase: 28-provider-expansion-r2
plan: 06
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-05
files_modified:
  - frontend/web/src/components/player/EnglishPlayer.vue
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/ja.json
  - frontend/web/e2e/english-player-sources.spec.ts
  - frontend/web/public/changelog.json
autonomous: false
requirements:
  - SCRAPER-HEAL-39
user_setup: []

must_haves:
  truths:
    - "`frontend/web/src/components/player/EnglishPlayer.vue` (when present per Phase 24 dependency) has `capitalizeProvider` branches for `animefever` → \"AnimeFever\", `miruro` → \"Miruro\" (only if Plan 28-04 proceeded), `nineanime` → \"9anime\"."
    - "Provider dropdown ordering matches the failover-chain order (CONTEXT.md Discretion #3): allanime → animefever → miruro → nineanime → animepahe → gogoanime → animekai."
    - "i18n labels for the new providers exist in all three locales (`en.json`, `ru.json`, `ja.json`) — `anime.scraper.providers.{animefever,miruro,nineanime}`."
    - "A Playwright e2e spec exercises source-switching mid-episode and asserts the MP4 path works for `nineanime`."
    - "`frontend/web/public/changelog.json` gains a user-facing Phase 28 entry (informative + enthusiastic tone with emojis per CLAUDE.md `After-Update Skill`)."
    - "`/animeenigma-after-update` skill is invoked at the end of this plan."
    - "Soft-dependency handling per CONTEXT.md A6: if `EnglishPlayer.vue` does NOT exist at the time of execution (Phase 24 not yet shipped), this plan ships the i18n + changelog + e2e-scaffold-only changes; the `capitalizeProvider` diff becomes a pending follow-up annotated in 28-06-SUMMARY.md."
  artifacts:
    - path: "frontend/web/src/locales/en.json"
      provides: "i18n keys for new provider labels + dropdown copy"
      contains: "animefever"
    - path: "frontend/web/src/locales/ru.json"
      provides: "Russian translations"
      contains: "animefever"
    - path: "frontend/web/src/locales/ja.json"
      provides: "Japanese translations"
      contains: "animefever"
    - path: "frontend/web/e2e/english-player-sources.spec.ts"
      provides: "Playwright e2e for source-switching mid-episode"
      min_lines: 40
      contains: "source-dropdown"
    - path: "frontend/web/public/changelog.json"
      provides: "User-facing Phase 28 changelog entry"
  key_links:
    - from: "frontend/web/src/components/player/EnglishPlayer.vue capitalizeProvider"
      to: "/api/anime/<id>/scraper/episodes?provider=animefever|miruro|nineanime"
      via: "Source dropdown selection → scraperApi query string"
      pattern: "capitalizeProvider"
    - from: "frontend/web/e2e/english-player-sources.spec.ts"
      to: "production EnglishPlayer + multi-provider scraper backend"
      via: "Playwright test exercises Frieren source switch"
      pattern: "source-dropdown"
---

<objective>
Light up the EnglishPlayer source dropdown for the new providers from Phase 28 (animefever, miruro, nineanime), add i18n labels in all three locales, write a Playwright e2e spec exercising source-switching mid-episode, update the changelog, and invoke `/animeenigma-after-update` to deploy/document/commit.

Per CONTEXT.md A6 (soft dependency): `EnglishPlayer.vue` is restored by Phase 24 — it MAY not exist at Phase 28 execution time. The plan ships the backend-independent parts (i18n + e2e scaffold + changelog) unconditionally, and gates the `capitalizeProvider` + dropdown ordering changes on file existence. If `EnglishPlayer.vue` is absent, the dropdown polish is annotated as a pending follow-up in `28-06-SUMMARY.md` (does NOT block phase closure).

Output: 3 locale JSON updates + 1 Playwright spec + 1 changelog entry + (conditional) 1 EnglishPlayer.vue diff + after-update invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-provider-expansion-r2/28-CONTEXT.md
@.planning/phases/28-provider-expansion-r2/28-RESEARCH.md
@CLAUDE.md
@frontend/web/src/locales/en.json
@frontend/web/src/locales/ru.json
@frontend/web/src/locales/ja.json

<interfaces>
<!-- The EnglishPlayer.vue (when restored by Phase 24) exposes a source dropdown -->
<!-- via `capitalizeProvider(name: string): string` switch. Phase 28 adds branches: -->

```ts
function capitalizeProvider(name: string): string {
  switch (name) {
    case 'allanime':   return 'AllAnime'
    case 'animepahe':  return 'AnimePahe'
    case 'gogoanime':  return 'Anitaku'
    case 'animekai':   return 'AnimeKai'
    case 'animefever': return 'AnimeFever'   // NEW Phase 28
    case 'miruro':     return 'Miruro'       // NEW Phase 28 (conditional on 28-04)
    case 'nineanime':  return '9anime'       // NEW Phase 28
    default:           return name.charAt(0).toUpperCase() + name.slice(1)
  }
}

// Dropdown order = failover-chain (CONTEXT.md Discretion #3):
const availableProviders = computed(() => {
  const order = ['allanime', 'animefever', 'miruro', 'nineanime', 'animepahe', 'gogoanime', 'animekai']
  return order.filter(name => healthSnapshot.value?.[name]?.stages?.search?.up)
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n labels in en/ru/ja for new providers</name>
  <files>
    - frontend/web/src/locales/en.json
    - frontend/web/src/locales/ru.json
    - frontend/web/src/locales/ja.json
  </files>
  <action>
    Add nested keys for the new provider labels + source-dropdown copy. Place under a coherent existing section (e.g. `anime.scraper.providers` if such a key path already exists for other providers; otherwise create `anime.player.scraperProviders` matching the existing locale structure pattern).

    Inspect each locale file's top-level shape before deciding the path. The KEY PATHS chosen must match each other across all three locales.

    Per RESEARCH.md "i18n keys (locales)" example, add (use one path consistently):

    **en.json**:
    ```json
    "scraperProviders": {
      "allanime": "AllAnime",
      "animefever": "AnimeFever",
      "miruro": "Miruro",
      "nineanime": "9anime"
    },
    "sourceLabel": "Source",
    "sourceUnavailable": "This source isn't responding right now. Try another."
    ```

    **ru.json**:
    ```json
    "scraperProviders": {
      "allanime": "AllAnime",
      "animefever": "AnimeFever",
      "miruro": "Miruro",
      "nineanime": "9anime"
    },
    "sourceLabel": "Источник",
    "sourceUnavailable": "Этот источник сейчас не отвечает. Попробуйте другой."
    ```

    **ja.json**:
    ```json
    "scraperProviders": {
      "allanime": "AllAnime",
      "animefever": "AnimeFever",
      "miruro": "Miruro",
      "nineanime": "9anime"
    },
    "sourceLabel": "ソース",
    "sourceUnavailable": "このソースは現在応答していません。別のソースを試してください。"
    ```

    If `EnglishPlayer.vue` is absent (Phase 24 not shipped), the i18n keys still land — they wait for the Vue component to consume them when Phase 24 ships.

    If Plan 28-04 ended with `Verdict: killed`, the `miruro` key STILL ships in i18n (so the future v3.2 lift just needs the Vue code) — but mark in 28-06-SUMMARY.md as "miruro key pre-staged for v3.2 lift."

    Run `bun run lint:i18n` (per `frontend/web/package.json`) if such a target exists; if not, use `node` to JSON.parse each locale to confirm no syntax errors. Run `cd frontend/web; bunx eslint src/` to confirm no lint regressions.
  </action>
  <verify>
    <automated>cd /data/animeenigma; for f in frontend/web/src/locales/en.json frontend/web/src/locales/ru.json frontend/web/src/locales/ja.json; do node -e "JSON.parse(require('fs').readFileSync('$f', 'utf8'))" || exit 1; grep -q 'animefever' "$f" || exit 1; grep -q 'nineanime' "$f" || exit 1; done</automated>
  </verify>
  <done>All 3 locales contain `animefever` + `miruro` + `nineanime` keys at the same path; each JSON parses; `bun run lint:i18n` (or equivalent JSON validation) green.</done>
</task>

<task type="auto">
  <name>Task 2: Update EnglishPlayer.vue capitalizeProvider + dropdown order (CONDITIONAL on file existence)</name>
  <files>
    - frontend/web/src/components/player/EnglishPlayer.vue
  </files>
  <action>
    Check existence: `test -f frontend/web/src/components/player/EnglishPlayer.vue`.

    **If absent (Phase 24 not yet shipped):**
      - Skip this task body. Annotate in 28-06-SUMMARY.md that the `capitalizeProvider` diff is pending Phase 24 restore.
      - Do NOT create the file de novo — that's Phase 24's plan, not Phase 28's. Per CONTEXT.md A6 the plan ships backend-independent parts and lets Phase 24 catch up.

    **If present:**
      - Add `case 'animefever': return 'AnimeFever'`, `case 'miruro': return 'Miruro'` (only if Plan 28-04 proceeded), `case 'nineanime': return '9anime'` to the `capitalizeProvider` switch (per the interfaces block).
      - Update or add the `availableProviders` computed (or whatever the dropdown's data source is called) to enumerate providers in failover-chain order: `['allanime', 'animefever', 'miruro', 'nineanime', 'animepahe', 'gogoanime', 'animekai']`. Filter by health snapshot's `search.up` (or whatever the existing filter uses).
      - Use the i18n key from Task 1 for the `<select>` label and the "source unavailable" copy.
      - Confirm dropdown is visible whenever `availableProviders.value.length > 1` (matches Phase 24/26 SCRAPER-HEAL-28 contract).
      - Run `cd frontend/web; bunx tsc --noEmit` to confirm TypeScript shape.
      - Run `cd frontend/web; bunx eslint src/components/player/EnglishPlayer.vue` to confirm no lint regressions.

    Per CONTEXT.md Discretion #3 — dropdown order = failover-chain order (NOT alphabetical, NOT last-used-first).

    Do NOT modify any other component (e.g. KodikPlayer, AnimeLibPlayer). Phase 28 only touches the EN scraper-source surface.
  </action>
  <verify>
    <automated>if test -f frontend/web/src/components/player/EnglishPlayer.vue; then cd /data/animeenigma; grep -q "animefever" frontend/web/src/components/player/EnglishPlayer.vue; grep -q "nineanime" frontend/web/src/components/player/EnglishPlayer.vue; cd frontend/web; bunx tsc --noEmit 2>&amp;1 | tail -5; else echo "SOFT-SKIP: EnglishPlayer.vue absent — Phase 24 dependency unfulfilled"; fi</automated>
  </verify>
  <done>If EnglishPlayer.vue exists: capitalizeProvider has branches for new providers; dropdown order matches failover chain; tsc passes. If absent: skip path annotated in 28-06-SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: Write Playwright e2e spec for source-switching mid-episode</name>
  <files>
    - frontend/web/e2e/english-player-sources.spec.ts
  </files>
  <action>
    Create `frontend/web/e2e/english-player-sources.spec.ts` modeled after existing `e2e/raw-player.spec.ts` (the closest analog — has login flow + player interaction). Use the spec sketch from RESEARCH.md "Playwright e2e (source-switching mid-episode)" as the starting point.

    Key behaviors to cover:
    (a) Login as `ui_audit_bot` via the `/api/auth/login` POST + localStorage seeding pattern documented in CLAUDE.md "UI Audit Test User" + RESEARCH.md spec sketch.
    (b) Navigate to a Frieren anime page (use the Shikimori ID 52991 or the production UUID `f0b40660-6627-4a59-8dcf-7ec8596b3623`).
    (c) Click the EN tab.
    (d) **Verify dropdown** shows at least 2 providers (allanime + at least one Phase 28 provider). If Plan 28-04 proceeded, expect at least 3.
    (e) Start with `allanime`; assert `<video>` element becomes visible.
    (f) Switch to `animefever`; assert `<video>` still visible within 15s timeout.
    (g) (CONDITIONAL) Switch to `miruro` if 28-04 proceeded; assert video.
    (h) Switch to `nineanime` (Marriagetoxin or any anime present in upstream); assert `<video src*=".mp4">` visible.
    (i) Add a smoke test asserting source dropdown labels match expected `capitalizeProvider` outputs ("AllAnime", "AnimeFever", "Miruro", "9anime").

    **Gating logic:** If `EnglishPlayer.vue` is absent (Phase 24 not shipped), the spec must `test.skip(true, "EnglishPlayer.vue not yet restored — Phase 24 dependency")` at the top with a clear skip reason so CI doesn't fail. Use the existing `test.beforeAll` or `test.describe.skip` pattern in raw-player.spec.ts for the equivalent guard.

    Spec should be runnable via `cd frontend/web && bunx playwright test english-player-sources --reporter=list`.

    Per CLAUDE.md "UI Audit Test User" — DO NOT recreate the `ui_audit_bot` account; reuse the existing one. Use `audit_bot_test_password_2026` for the password login flow that sets the refresh cookie.
  </action>
  <verify>
    <automated>test -s frontend/web/e2e/english-player-sources.spec.ts; cd /data/animeenigma/frontend/web; bunx tsc --noEmit e2e/english-player-sources.spec.ts 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Spec file exists; type-checks (no `any`-ridden hacks); has clear skip-gate for the Phase 24 dependency; covers source-switching with at least 3 providers (or 4 if 28-04 proceeded).</done>
</task>

<task type="auto">
  <name>Task 4: Update changelog.json with Phase 28 entry</name>
  <files>
    - frontend/web/public/changelog.json
  </files>
  <action>
    Per CLAUDE.md "After-Update Skill" #4: add a user-facing entry to `frontend/web/public/changelog.json` with informative + enthusiastic tone and emojis. The entry must mention the new sources by their user-facing names (per Task 1 i18n): AnimeFever, Miruro (if 28-04 proceeded), 9anime.

    Read the existing changelog.json structure first (`grep -E '^\s*"date"' frontend/web/public/changelog.json | head -5` to inspect the entry shape). Match that shape verbatim.

    Suggested entry content (adapt to the existing schema):
    - Date: today (2026-05-19 to 2026-05-20 window).
    - Title: "More English sources" / "Больше английских источников" / "英語ソース追加".
    - Body for each locale:
      - EN: "Three new fallback sources joined the English tab: AnimeFever, Miruro, and 9anime. When one source is down, the player falls through to the next automatically. Pick a specific one from the new Source dropdown."
      - RU: "Три новых резервных источника появились во вкладке English: AnimeFever, Miruro и 9anime. Если один источник недоступен, плеер автоматически переключится на следующий. Выберите конкретный в новом меню Источник."
      - JA: "Englishタブに3つの新しいフォールバックソースが追加されました: AnimeFever, Miruro, 9anime。1つのソースが利用できない場合、プレーヤーは自動的に次のソースに切り替わります。新しいソースメニューから特定のソースを選択できます。"
    - Conditional on Plan 28-04 — if killed, drop "Miruro" from the entry (the i18n key stays pre-staged silently).
    - Conditional on Phase 24 — if EnglishPlayer.vue absent, change phrasing to "Three new English sources are ready in the backend; the player UI exposes them once Phase 24's English tab restore lands."
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('frontend/web/public/changelog.json', 'utf8'))"; grep -E "AnimeFever|9anime" frontend/web/public/changelog.json | head -3</automated>
  </verify>
  <done>changelog.json parses as valid JSON; has Phase 28 entry referencing AnimeFever + 9anime (and Miruro if 28-04 proceeded); LastUpdates.vue consumers will display the new card.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Invoke /animeenigma-after-update skill</name>
  <files>(checkpoint — no files modified; verification only)</files>
  <action>
  <what-built>
    Phase 28 backend + frontend polish complete. All Phase 28 deliverables landed:
    - AnimeFever provider live (Plan 28-02 + 28-03).
    - Miruro provider live OR cleanly skipped (Plan 28-04 outcome).
    - 9anime provider live (Plan 28-05).
    - Frontend i18n + dropdown polish + e2e + changelog (this plan).

    Per CLAUDE.md "After-Update Skill (MUST USE)": invoke `/animeenigma-after-update` to:
    1. Lint and build the affected code (Go scraper + frontend Vue).
    2. Redeploy `make redeploy-scraper`, `make redeploy-streaming`, frontend rebuild via the post-build pipeline.
    3. Run health checks (`make health`).
    4. Update `frontend/web/public/changelog.json` — already done in Task 4; the skill confirms it's coherent.
    5. Commit all changes with the 3 co-authors documented in MEMORY.md ("Co-Authored-By: Claude Opus 4.6/4.7", "Co-Authored-By: 0neymik0", "Co-Authored-By: NANDIorg") and push to remote.
  </what-built>
  <how-to-verify>
    Operator runs `/animeenigma-after-update` in the working session. The skill:
    1. Confirms compile + test green.
    2. Runs `make redeploy-scraper` and `make redeploy-streaming`; confirms `make health` returns all green.
    3. Confirms changelog entry renders correctly in the LastUpdates.vue Changelog tab.
    4. Creates a commit per the 3-co-author convention from MEMORY.md (HEREDOC format per CLAUDE.md).
    5. Pushes to `main` (after confirming user wants the push).
  </how-to-verify>
  </action>
  <verify>
    <automated>echo "Human-verification checkpoint — operator manually runs the gates above and returns approval signal."</automated>
  </verify>
  <done>Operator confirmed all gates passed via the resume signal.</done>

  <resume-signal>Type "shipped" when after-update completes; describe any failure during deploy/health/lint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → /api/anime/.../scraper/* | Already-existing boundary; no new threat surface. Phase 28 just lights up new provider names in the query string. |
| Playwright spec → production EnglishPlayer | Authentication via ui_audit_bot; reuses CLAUDE.md-documented test user; no new auth path. |
| i18n JSON → frontend bundle | Static strings; no XSS surface (Vue auto-escapes interpolation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Information Disclosure | Playwright spec embeds test credentials | accept | `audit_bot_test_password_2026` is a documented automation password per CLAUDE.md; not a production user credential. Spec uses it identically to existing raw-player.spec.ts. |
| T-28-06-02 | Tampering | i18n string injection (XSS via locale value) | mitigate | Vue v-text / interpolation auto-escapes; no v-html usage for new keys. Confirm during code review. |
| T-28-06-03 | DoS | Playwright e2e regression run-time bloat | mitigate | Spec uses `test.skip` guard for missing EnglishPlayer.vue; total spec ≤4 sub-tests; per-test timeout ≤30s. |
</threat_model>

<verification>
- All 3 locales contain the new keys at matching paths; JSON parses; lint clean.
- Playwright spec exists, type-checks, has Phase 24 skip-guard.
- changelog.json has Phase 28 entry; JSON parses.
- (Conditional) EnglishPlayer.vue updated with new switch cases + dropdown order.
- /animeenigma-after-update invoked at the end; commit pushed (or local commit if user defers push).
- Phase 28 closure: all 7 plans (28-00 through 28-06) have SUMMARY.md files.
</verification>

<success_criteria>
- Source dropdown displays 4 providers (or 3 if Miruro killed) with correctly-capitalized labels.
- Source-switching mid-episode works end-to-end (per Playwright spec).
- 9anime MP4 path renders via native `<video>` element (no HLS.js confusion per Pitfall 6).
- Changelog entry visible to users in LastUpdates.vue.
- Phase 28 ships per CLAUDE.md After-Update Skill conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/28-provider-expansion-r2/28-06-SUMMARY.md` capturing:
1. Locale keys added (paths + values per locale)
2. EnglishPlayer.vue diff (or skip rationale if file absent)
3. Playwright spec coverage
4. Changelog entry text
5. After-update invocation result (commit hash, push status)
6. Pending follow-ups (e.g. if EnglishPlayer.vue absent, the dropdown polish becomes Phase 24-aftercare)
</output>
