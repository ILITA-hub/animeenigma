---
milestone: v4.0
audited: 2026-06-08T02:36:22Z
status: gaps_found
scores:
  requirements: 11/16 satisfied (2 partial, 3 unsatisfied)
  phases: 4/6 verified passed (2 human_needed deferred; 1 missing VERIFICATION — Phase 4)
  integration: 17/19 wired (3 blockers in the FE→BE trace_id join, 1 warning)
  flows: 2/4 E2E complete (2 broken — FE register trace_id/source, FE RUM source)
gaps:
  requirements:
    - id: "AR-FE-01"
      status: "unsatisfied"
      phase: "04-fe-causation-rum"
      verification_status: "missing"
      evidence: "frontend/web/src/analytics/index.ts:76 — track() wraps {source,trace_id,operation,target,target_kind} into the `properties` blob. services/analytics/internal/handler/collect.go wireEvent reads these as TOP-LEVEL JSON fields only, so fe.call rows land in ClickHouse with source='' and trace_id=''."
    - id: "AR-FE-02"
      status: "unsatisfied"
      phase: "04-fe-causation-rum"
      verification_status: "missing"
      evidence: "Two breaks: (a) fe.call trace_id='' (AR-FE-01); (b) libs/tracing/effect.go Effect struct has NO TraceID field, libs/tracing/producer.go wireProducerEffect drops it, services/analytics/internal/handler/effects.go never sets domain.Event.TraceID, and recordingTransport never extracts trace.SpanFromContext().TraceID(). BE effect rows always have trace_id=''. The FE↔BE join yields 0 rows."
    - id: "AR-FE-03"
      status: "unsatisfied"
      phase: "04-fe-causation-rum"
      verification_status: "missing"
      evidence: "Same properties-wrapping mechanism: rum.ts sends source='fe_rum' inside properties; collect.go defaultStr(we.Source,'be') returns 'be'. RUM rows land as source='be', accuracy='' instead of source='fe_rum'/accuracy='approximate'. The byte-discipline guard holds only by accident (RUM bytes are 0 due to cross-origin TAO)."
  integration:
    - "FE action → BE effects share trace_id (central milestone E2E claim): BROKEN at FE serialization (track→properties) AND BE Effect struct (no TraceID). 0 rows match on join."
    - "AR-EGRESS-05 (WARNING): services/scraper/internal/embeds/megaplay extractor uses an unwrapped http.Client — megaplay CDN egress bytes are not recorded. Other scraper providers are wired."
  flows:
    - "FE register row source/trace_id: broken at frontend/web/src/analytics/index.ts:76"
    - "FE RUM source classification: broken via collect.go defaultStr fallback"
tech_debt:
  - phase: 01-clickhouse-foundation-eventstore-swap
    items:
      - "VERIFICATION human_needed — deferred human/visual checks (automated passed)."
  - phase: 04-fe-causation-rum
    items:
      - "No VERIFICATION.md — phase status deferred-human-verify; live FE→BE join never verified because parallel-workstream useProviderResolver.ts tsc errors blocked make redeploy-web."
  - phase: 06-consolidation-topology-a
    items:
      - "VERIFICATION human_needed — 2 visual Grafana checks deferred by user (dashboard render + logs→traces click-through); all automated checks passed. Tracked in 06-HUMAN-UAT.md."
      - "IN-02 (code review, deliberately skipped): span-metrics emitted as calls_total, not Tempo legacy traces_spanmetrics_*; no namespace set (doc-consistency only)."
      - "Optional: MinIO `tempo` bucket not reclaimed (harmless orphan; volumes were reclaimed)."
---

# v4.0 Activity Register — Milestone Audit

**Status: GAPS FOUND.** The platform-observability spine (Phases 1–3, 5, 6) is built, verified, and live. The milestone's **central E2E claim — "a frontend action and its resulting backend effects share one `trace_id`" — is broken in code** (Phase 4), so the FE→BE join that the whole register is meant to enable returns zero matches.

## Phase Verification Summary

| Phase | Name | VERIFICATION status | Note |
|-------|------|---------------------|------|
| 1 | ClickHouse Foundation + EventStore Swap | human_needed | automated passed; visual deferred |
| 2 | BE Egress Recorder | passed | — |
| 3 | DB/Cache Effects + Auto Operation Discovery | passed | — |
| 4 | FE Causation + RUM | **MISSING** | code-done, deferred-human-verify; 3 structural blockers below |
| 5 | Reports & Dashboards | passed | data-plane proven; visual deferred |
| 6 | Consolidation → Topology A | human_needed | automated 5/5 passed; 2 visual checks deferred by user |

## Requirements Coverage (16 total)

- **Satisfied (11):** AR-STORE-01, -02, -04, -05; AR-EGRESS-01, -02, -03, -04; AR-CONS-01, -02, -03.
- **Partial (2):** AR-STORE-03 (BE effect `trace_id` always empty — tied to AR-FE-02); AR-EGRESS-05 (megaplay extractor egress unrecorded — WARNING).
- **Unsatisfied (3):** AR-FE-01, AR-FE-02, AR-FE-03 — the FE causation/RUM wiring (see gaps above).

## Root Cause (single mechanism, two layers)

1. **FE serialization:** `frontend/web/src/analytics/index.ts:76` — `track(name, props)` always nests `props` under `properties`. The collector (`collect.go` `wireEvent`) reads `source`/`trace_id`/`operation`/`target`/`target_kind` only as top-level fields → they arrive empty. Fix: promote those keys to top-level on the wire (FE side) OR have `collect.go` read them out of `properties` (BE side).
2. **BE trace_id propagation:** the `libs/tracing` Effect chain has no `TraceID` field and never extracts the OTel span's trace id. Fix: add `TraceID` to `Effect` + `wireProducerEffect`, populate it from `trace.SpanFromContext(req.Context()).SpanContext().TraceID()` in `recordingTransport`, and set `domain.Event.TraceID` in `effects.go`.

Unit tests pass because each layer is tested in isolation with mocks across the serialization boundary; the live gate that would have caught it (Phase 04-04) was deferred.

## Phase 6 Attestation

Phase 6 (this run) is clean: Tempo/Loki/Promtail retired, traces+logs in ClickHouse with populated TraceId, span-metrics regenerated to Prometheus, 15 alert rules healthy, 0 code-review Criticals (3 Warnings fixed, live-verified). Phase 6 did **not** introduce the FE→BE gaps — they pre-date it.

## Recommendation

Close AR-FE-01/02/03 with a small, well-scoped fix (exact files/lines above), then re-verify Phase 4 live (requires the parallel `useProviderResolver.ts` tsc errors to be resolved so `make redeploy-web` passes). The fix is ~2 layers, low structural disruption.
