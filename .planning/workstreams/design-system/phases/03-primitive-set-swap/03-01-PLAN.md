---
phase: 03-primitive-set-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/components/ui/badge-variants.ts
  - frontend/web/src/components/ui/Badge.vue
  - frontend/web/src/components/ui/Badge.spec.ts
  - frontend/web/src/components/ui/Input.vue
  - frontend/web/src/components/ui/Input.spec.ts
  - frontend/web/src/components/ui/Tabs.vue
  - frontend/web/src/components/ui/Tabs.spec.ts
  - frontend/web/src/components/ui/index.ts
autonomous: true
requirements: [DS-LIB-05, DS-NF-04]
must_haves:
  truths:
    - "Badge renders the same 8 variant colors + 3 sizes it does today"
    - "Input renders identically (label/hint/error/clearable/prefix/suffix) with the same props/emits/slots"
    - "Tabs renders identically (3 variants, dynamic panel slots, a11y wiring) with the same props/emits/slots"
    - "All ~10 Badge/Input/Tabs consumers still compile under vue-tsc --noEmit"
  artifacts:
    - path: "frontend/web/src/components/ui/badge-variants.ts"
      provides: "cva badge variant map + BadgeVariants type"
      contains: "badgeVariants"
    - path: "frontend/web/src/components/ui/Badge.vue"
      provides: "Badge re-skinned on cva + cn()"
      contains: "badgeVariants"
    - path: "frontend/web/src/components/ui/Badge.spec.ts"
      provides: "variant→class assertions mirroring Button.spec.ts"
    - path: "frontend/web/src/components/ui/Input.spec.ts"
      provides: "props/slots/clearable mount assertions"
    - path: "frontend/web/src/components/ui/Tabs.spec.ts"
      provides: "variant class + slot switch assertions"
  key_links:
    - from: "frontend/web/src/components/ui/Badge.vue"
      to: "frontend/web/src/components/ui/badge-variants.ts"
      via: "import { badgeVariants }"
      pattern: "badgeVariants"
    - from: "frontend/web/src/components/ui/index.ts"
      to: "./badge-variants"
      via: "export badgeVariants + BadgeVariants type"
      pattern: "badge-variants"
---

<objective>
Swap the three LOWEST-risk existing primitives — Badge, Input, Tabs — onto the Phase-2
shadcn-vue toolchain (`cva` for Badge, `cn()` for Input/Tabs) BEHIND THEIR EXISTING IMPORT
PATHS, with byte-for-byte-identical rendered output. None of these three use a Reka
primitive (Reka has no Input/Tabs primitive and Badge is a pure `<span>`), so this is a
pattern re-skin only — mirror exactly how Phase 2 did Button (`cva`+`cn()`) and Card (`cn()`).

Purpose: De-risk the phase by shipping the three zero-portal, zero-focus-trap primitives
first. Each is independently committable + provable via `vue-tsc --noEmit` (compile gate
that proves all ~10 consumers still typecheck against the preserved API).

Output: `badge-variants.ts` (new), re-skinned `Badge.vue`/`Input.vue`/`Tabs.vue`, three
co-located `.spec.ts`, barrel updated to export `badgeVariants` + `BadgeVariants`.

Requirements: DS-LIB-05 (Badge/Input/Tabs swap behind same paths), DS-NF-04 (preserve API).
Effort note (no days/hours): UXΔ = 0 (Ambiguous — zero intended rendered change) | CDI = 0.03 * 5 | MVQ = Sprite 90%/85%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/phases/03-primitive-set-swap/RESEARCH.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/REQUIREMENTS.md

# Phase-2 patterns to MIRROR EXACTLY (cva + cn + back-compat aliases + spec style):
@frontend/web/src/components/ui/button-variants.ts
@frontend/web/src/components/ui/Button.vue
@frontend/web/src/components/ui/Button.spec.ts
@frontend/web/src/lib/utils.ts
@frontend/web/src/components/ui/index.ts

# Current components being swapped (preserve EXACT classes/props/emits/slots):
@frontend/web/src/components/ui/Badge.vue
@frontend/web/src/components/ui/Input.vue
@frontend/web/src/components/ui/Tabs.vue

<interfaces>
<!-- cn() helper (Phase 2). Executor uses this directly — no exploration needed. -->
From frontend/web/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string  // clsx + tailwind-merge
```

<!-- cva pattern (Phase 2 Button). Mirror this file's shape for badge-variants.ts. -->
From frontend/web/src/components/ui/button-variants.ts:
```typescript
import { cva, type VariantProps } from 'class-variance-authority'
export const buttonVariants = cva('<base classes>', { variants: {...}, defaultVariants: {...} })
export type ButtonVariants = VariantProps<typeof buttonVariants>
```

<!-- Badge PUBLIC API to preserve (props only; no events/v-model). -->
Badge props: variant ('default'|'primary'|'secondary'|'success'|'warning'|'rating'|'info'|'destructive'), size ('sm'|'md'|'lg'). Slots: default, #icon.

<!-- Input PUBLIC API to preserve. -->
Input props: modelValue(string), type, placeholder, label, hint, error, disabled, readonly, clearable, size('sm'|'md'|'lg'), variant('default'|'filled'). Emits: update:modelValue. v-model: default(string). Slots: #prefix, #suffix. MUST keep defineOptions({inheritAttrs:false}) + v-bind="$attrs" on the <input>. MUST keep the scoped <style> hiding webkit search-clear.

<!-- Tabs PUBLIC API to preserve. -->
Tabs props: modelValue(string), tabs(Tab[] = {value,label,icon?,count?,disabled?}), variant('default'|'pills'|'underline'), fullWidth. Emits: update:modelValue. v-model: default(string). Slots: dynamic #[value] (one panel slot per tab). MUST keep role=tablist/tab/tabpanel + aria-selected/controls/labelledby wiring (UA-069).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge → cva re-skin (badge-variants.ts + Badge.vue + spec)</name>
  <files>frontend/web/src/components/ui/badge-variants.ts, frontend/web/src/components/ui/Badge.vue, frontend/web/src/components/ui/Badge.spec.ts</files>
  <behavior>
    - badgeVariants({ variant: 'primary' }) contains 'bg-cyan-500/20' and 'text-cyan-400'
    - badgeVariants({ variant: 'default' }) contains 'bg-white/10' and 'text-white/80'
    - badgeVariants({ variant: 'destructive' }) contains 'bg-red-500/20' and 'text-red-400'
    - badgeVariants({ variant: 'info' }) contains 'bg-purple-500/20' (NOT tokenized — literal)
    - badgeVariants({ size: 'sm' }) contains 'px-2 py-0.5 text-xs rounded'
    - badgeVariants({ size: 'md' }) contains 'px-2.5 py-1 text-sm rounded-md'
    - badgeVariants({ size: 'lg' }) contains 'px-3 py-1.5 text-base rounded-lg'
    - default variants resolve to variant=default + size=md
    - mount(Badge) renders a <span>; #icon slot renders inside an 'mr-1' wrapper span when provided
    - assert NO accidental tokenization: badgeVariants({variant:'primary'}) does NOT contain 'bg-primary'
  </behavior>
  <action>
    Create `badge-variants.ts` mirroring `button-variants.ts` shape: `cva('inline-flex items-center font-medium', { variants: { variant: {...}, size: {...} }, defaultVariants: { variant: 'default', size: 'md' } })` then `export type BadgeVariants = VariantProps<typeof badgeVariants>`. Copy the EXACT literal class strings from the current `Badge.vue` lines 26-43 into the variant/size maps — do NOT tokenize them (these are intentional literal Tailwind colors, same rule Phase 2 followed: no `bg-primary`/`bg-accent` substitution). Variant keys: default `bg-white/10 text-white/80`, primary `bg-cyan-500/20 text-cyan-400`, secondary `bg-pink-500/20 text-pink-400`, success `bg-emerald-500/20 text-emerald-400`, warning `bg-amber-500/20 text-amber-400`, rating `bg-black/60 text-amber-400 backdrop-blur-sm`, info `bg-purple-500/20 text-purple-400`, destructive `bg-red-500/20 text-red-400`. Size keys: sm `px-2 py-0.5 text-xs rounded`, md `px-2.5 py-1 text-sm rounded-md`, lg `px-3 py-1.5 text-base rounded-lg`.

    Rewrite `Badge.vue` to use `cn(badgeVariants({ variant, size }), props.class)` for the `<span>` class (add an optional `class?: HTMLAttributes['class']` prop mirroring Button — additive, back-compatible). Keep the template `<span>` + `#icon` slot wrapper (`<span v-if="$slots.icon" class="mr-1">`) + default slot EXACTLY as today. Keep `variant`/`size` prop names + default values identical. Import `cn` from `@/lib/utils` and `badgeVariants, type BadgeVariants` from `./badge-variants`. Type the `variant`/`size` props as `NonNullable<BadgeVariants['variant']>` / `NonNullable<BadgeVariants['size']>` (mirror Button.vue).

    Create `Badge.spec.ts` mirroring `Button.spec.ts`: a `describe('badgeVariants')` asserting the variant/size class strings per the behavior block, plus a `describe('Badge.vue')` mounting with `@vue/test-utils` to assert the `<span>` element + `#icon` slot wrapper. Use `import { describe, it, expect } from 'vitest'` and `import { mount } from '@vue/test-utils'`.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/Badge.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>Badge.spec.ts green; vue-tsc clean (proves the 7 Badge consumers still compile); badge-variants.ts exports badgeVariants + BadgeVariants; Badge.vue uses cn(badgeVariants(...)) with identical literal classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input + Tabs → cn() re-skin (no Reka) + specs</name>
  <files>frontend/web/src/components/ui/Input.vue, frontend/web/src/components/ui/Input.spec.ts, frontend/web/src/components/ui/Tabs.vue, frontend/web/src/components/ui/Tabs.spec.ts</files>
  <behavior>
    Input:
    - mount(Input) renders one <input>; v-model round-trips (set value → emits update:modelValue with the string)
    - clearable+value renders the clear <button>; clicking it emits update:modelValue with '' 
    - error prop renders the pink-400 error <p>; hint (without error) renders the white/50 hint <p>
    - #prefix / #suffix slots render in their absolute-positioned spans; input gets pl-10 / pr-10 respectively
    - size='md' input classes contain 'px-4 py-3 text-base rounded-xl'
    - error state input classes contain 'border-pink-500'; non-error contains 'border-white/10'
    Tabs:
    - mount with tabs=[{value:'a',label:'A'},{value:'b',label:'B'}], modelValue='a' → 2 role=tab buttons; tab 'a' has aria-selected='true'
    - clicking tab 'b' emits update:modelValue 'b'
    - the active tab button (default variant) classes contain 'bg-white/10' and 'text-white'; inactive contains 'text-white/60'
    - variant='pills' active classes contain 'bg-cyan-500/20'; variant='underline' active contains 'border-cyan-400'
    - tabpanel has id `tabpanel-a` + aria-labelledby `tab-a`; the #a slot content renders
    - tab.count renders the count pill; tab.disabled adds 'cursor-not-allowed'
  </behavior>
  <action>
    Input.vue: Convert the two `[...].join(' ')` class builders (`inputClasses`, and the inline `:class` array on the `<input>`) to `cn(...)` so tailwind-merge dedups, but keep EVERY literal class string identical to the current lines 85-102 (base `w-full bg-white/5 border text-white placeholder-white/30 transition-all duration-200 focus:outline-none`; sizes sm/md/lg as today; error vs non-error states as today; the `pl-10`/`pr-10` prefix/suffix conditionals). Preserve `defineOptions({ inheritAttrs: false })`, the `v-bind="$attrs"` on `<input>`, all props/emits/the computed `model` getter/setter, `focused` ref, `inputId`, and the scoped `<style>` block (webkit search-clear hide) UNCHANGED. Import `cn` from `@/lib/utils`. Do NOT add a Reka primitive.

    Tabs.vue: Convert `tabListClasses` and `getTabClasses` from `[...].join(' ')` to `cn(...)`, keeping every literal variant/size/active/inactive class string identical to current lines 65-111. Preserve the `tabs` array prop, dynamic `#[value]` panel slot (`<slot :name="modelValue" />`), all a11y attributes (role, id `tab-${value}`/`tabpanel-${value}`, aria-selected/controls/labelledby), the count pill, and the disabled handling. Import `cn` from `@/lib/utils`. Do NOT add a Reka primitive.

    Create `Input.spec.ts` and `Tabs.spec.ts` (vitest + @vue/test-utils) asserting the behavior block above. For Input v-model, use `await wrapper.find('input').setValue('x')` then assert `wrapper.emitted('update:modelValue')`. For slot tests pass `slots: { prefix: '<i/>' }`. For Tabs slot test pass `slots: { a: '<p>panel-a</p>' }` with `props: { modelValue: 'a', tabs: [...] }`.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/Input.spec.ts src/components/ui/Tabs.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>Both specs green; vue-tsc clean (proves Game.vue/SearchAutocomplete internal Input consumer + Profile/Anime Tabs consumers still compile); Input keeps inheritAttrs:false + scoped style; Tabs keeps a11y + dynamic slots; both use cn() with identical literal classes; neither imports a Reka primitive.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel export badgeVariants + commit wave-1 swaps</name>
  <files>frontend/web/src/components/ui/index.ts</files>
  <action>
    Edit `frontend/web/src/components/ui/index.ts`: directly after the existing `export { default as Badge } from './Badge.vue'` line, add `export { badgeVariants, type BadgeVariants } from './badge-variants'` (mirrors how Button's line is followed by `export { buttonVariants, type ButtonVariants } from './button-variants'`). Leave the existing `Badge`/`Input`/`Tabs` default exports and every other line UNCHANGED — import paths must not move.

    Then run the full wave-1 verification (specs + the three previously-touched specs + compile gate) before committing. Commit each logical unit with `git add <explicit paths>` (NEVER `git add -A` — parallel sessions have unrelated dirty files like `.claude/worktrees/`, `tmp/`, `services/scraper/scraper-api` on main). COMMIT ONLY — do NOT push. Every commit carries the 3 co-authors:

    Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
    Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
    Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>

    Suggested commit grouping (atomic per primitive):
    - `git add frontend/web/src/components/ui/badge-variants.ts frontend/web/src/components/ui/Badge.vue frontend/web/src/components/ui/Badge.spec.ts frontend/web/src/components/ui/index.ts` → commit `feat(design-system/03): Badge on cva + badgeVariants barrel export (DS-LIB-05)`
    - `git add frontend/web/src/components/ui/Input.vue frontend/web/src/components/ui/Input.spec.ts` → commit `feat(design-system/03): Input on cn() SFC, API preserved (DS-LIB-05)`
    - `git add frontend/web/src/components/ui/Tabs.vue frontend/web/src/components/ui/Tabs.spec.ts` → commit `feat(design-system/03): Tabs on cn() SFC, a11y + slots preserved (DS-LIB-05)`
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/Badge.spec.ts src/components/ui/Input.spec.ts src/components/ui/Tabs.spec.ts && bunx vue-tsc --noEmit && git log --oneline -3 | grep -c "design-system/03"</automated>
  </verify>
  <done>Barrel exports badgeVariants + BadgeVariants; all 3 specs green; vue-tsc clean; 3 atomic commits exist with 3 co-authors each; nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational SFC re-skin; no new untrusted input crosses any boundary. Components render existing typed props only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Badge/Input/Tabs class output | accept | Classes are static literals/cva maps, not user-controlled; no injection surface. Spec-tested for exact output. |
| T-03-02 | Information disclosure | Input `$attrs` passthrough | accept | Pre-existing behavior (inheritAttrs:false + v-bind="$attrs"); unchanged by this plan. |
</threat_model>

<verification>
- `cd frontend/web && bunx vue-tsc --noEmit` → zero new errors (proves all Badge/Input/Tabs consumers still compile against preserved API — DS-NF-04).
- `cd frontend/web && bunx vitest run src/components/ui/` → Badge/Input/Tabs specs green alongside existing Button/Card specs.
- No Reka primitive imported in Input.vue or Tabs.vue (grep: `grep -L "reka-ui" Input.vue Tabs.vue` should list both as NOT containing it).
- main.css NOT touched (grep: `git diff --name-only` must not include `src/styles/main.css`).
</verification>

<success_criteria>
1. Badge/Input/Tabs render identically to today (literal classes preserved; cva/cn used only for merge mechanics).
2. Import paths + prop/event/v-model/slot surfaces unchanged; `vue-tsc --noEmit` proves consumers compile.
3. Three co-located specs green; barrel exports `badgeVariants` + `BadgeVariants`.
4. 3 atomic commits, explicit-path staging, 3 co-authors, NO push.
</success_criteria>

<orchestrator_browser_gate>
NONE required for this plan. Badge/Input/Tabs are zero-portal, zero-focus-trap, literal-class re-skins — "identical rendering" is high-confidence (RESEARCH render risk = LOW for all three). The standing 5-surface smoke at phase end will incidentally cover Badge (cards) anyway.
</orchestrator_browser_gate>

<output>
After completion, create `.planning/workstreams/design-system/phases/03-primitive-set-swap/03-01-SUMMARY.md`
</output>
