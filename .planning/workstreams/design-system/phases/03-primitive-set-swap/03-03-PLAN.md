---
phase: 03-primitive-set-swap
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/web/src/components/ui/DropdownMenu.vue
  - frontend/web/src/components/ui/DropdownMenu.spec.ts
  - frontend/web/src/components/ui/Tooltip.vue
  - frontend/web/src/components/ui/Tooltip.spec.ts
  - frontend/web/src/components/ui/Popover.vue
  - frontend/web/src/components/ui/Popover.spec.ts
  - frontend/web/src/components/ui/Switch.vue
  - frontend/web/src/components/ui/Switch.spec.ts
  - frontend/web/src/components/ui/Checkbox.vue
  - frontend/web/src/components/ui/Checkbox.spec.ts
  - frontend/web/src/components/ui/index.ts
  - frontend/web/src/App.vue
autonomous: true
requirements: [DS-LIB-06, DS-LIB-08, DS-NF-04]
must_haves:
  truths:
    - "A Reka-based DropdownMenu is exported from @/components/ui (trigger-anchored, token-driven) — the foundation Plan 04 rebuilds AnimeContextMenu on"
    - "Tooltip, Popover, Switch, Checkbox exist as token-driven shadcn-vue primitives, each with a Vitest mount test"
    - "Switch and Checkbox use plain boolean v-model (not :checked)"
    - "A single TooltipProvider is mounted at app root in App.vue so Tooltip has its required ancestor"
    - "vue-tsc --noEmit stays clean (new primitives + App.vue edit compile)"
  artifacts:
    - path: "frontend/web/src/components/ui/DropdownMenu.vue"
      provides: "Reka DropdownMenu (Root/Trigger/Portal/Content/Item/Separator/Label) token-driven"
      contains: "DropdownMenuRoot"
    - path: "frontend/web/src/components/ui/Tooltip.vue"
      provides: "Reka Tooltip with trigger + content slots"
      contains: "TooltipRoot"
    - path: "frontend/web/src/components/ui/Popover.vue"
      provides: "Reka Popover with v-model:open"
      contains: "PopoverRoot"
    - path: "frontend/web/src/components/ui/Switch.vue"
      provides: "Reka Switch, boolean v-model"
      contains: "SwitchRoot"
    - path: "frontend/web/src/components/ui/Checkbox.vue"
      provides: "Reka Checkbox, boolean|'indeterminate' v-model"
      contains: "CheckboxRoot"
  key_links:
    - from: "frontend/web/src/App.vue"
      to: "reka-ui TooltipProvider"
      via: "wraps the app shell so Tooltip works app-wide"
      pattern: "TooltipProvider"
    - from: "frontend/web/src/components/ui/index.ts"
      to: "./DropdownMenu.vue ./Tooltip.vue ./Popover.vue ./Switch.vue ./Checkbox.vue"
      via: "5 new default exports"
      pattern: "DropdownMenu"
---

<objective>
Add the five NEW primitives this phase introduces: the trigger-anchored Reka `DropdownMenu`
(which Plan 04 rebuilds the anime kebab action-menu on — so it MUST land before Plan 04),
plus the four greenfield primitives `Tooltip`, `Popover`, `Switch`, `Checkbox`. All are
standard shadcn-vue compositions on Reka, token-driven, each with a Vitest mount test. Mount
ONE `TooltipProvider` at app root in `App.vue` (standard shadcn-vue pattern, RESEARCH note +
assumption A5) so `Tooltip` has its required provider ancestor.

Purpose: Complete DS-LIB-06 (the four hand-rolled-today primitives) and stand up the
DropdownMenu foundation DS-LIB-08 needs, without touching any existing consumer (these are
greenfield additions — zero call-site risk).

Output: 5 new SFCs + 5 specs, barrel updated with 5 exports, TooltipProvider in App.vue.

Requirements: DS-LIB-06 (Tooltip/Popover/Switch/Checkbox), DS-LIB-08 part (b) (add Reka DropdownMenu to barrel), DS-NF-04 (no consumer breakage; additive).
Effort note (no days/hours): UXΔ = +1 (Better — new affordances available) | CDI = 0.04 * 8 | MVQ = Sprite 85%/85%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/phases/03-primitive-set-swap/RESEARCH.md
@.planning/workstreams/design-system/REQUIREMENTS.md

@frontend/web/src/lib/utils.ts
@frontend/web/src/components/ui/index.ts
@frontend/web/src/App.vue

<interfaces>
<!-- cn() helper. -->
From frontend/web/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

<!-- Reka parts (all verified present in reka-ui@2.9.8): -->
DropdownMenu: DropdownMenuRoot, DropdownMenuTrigger, DropdownMenuPortal, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel.
Tooltip: TooltipProvider (app root), TooltipRoot, TooltipTrigger, TooltipPortal, TooltipContent, TooltipArrow.
Popover: PopoverRoot (v-model:open), PopoverTrigger, PopoverPortal, PopoverContent, PopoverClose, PopoverArrow.
Switch: SwitchRoot (v-model: boolean — Reka 2.x uses plain v-model, NOT v-model:checked), SwitchThumb.
Checkbox: CheckboxRoot (v-model: boolean | 'indeterminate'), CheckboxIndicator.

<!-- Tokens available (Phase 1, per CLAUDE.md / RESEARCH assumption A2): -->
bg-popover, text-popover-foreground, bg-primary, border-input. Use these for Tooltip/Popover/Switch/Checkbox surfaces. Use literal bg-white/* for hovers + literal radii (no --radius util, no bg-accent — same rule as Phase 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DropdownMenu.vue (Reka, trigger-anchored, slotted) + spec</name>
  <files>frontend/web/src/components/ui/DropdownMenu.vue, frontend/web/src/components/ui/DropdownMenu.spec.ts</files>
  <behavior>
    - mount(DropdownMenu) renders a DropdownMenuRoot; a #trigger slot renders inside DropdownMenuTrigger
    - supports controlled open via v-model:open (boolean) — emitting Reka open-change emits update:open
    - content slot renders the menu body (assert via document.body when open + attachTo, OR assert the trigger slot + that open emits work — jsdom portal caveat, RESEARCH Pitfall 6)
    - content classes use token surface: contains 'bg-popover' OR the documented dropdown surface; rounded + border present
    - exposes a way to anchor content to an arbitrary element for Plan 04's kebab use: accept an optional `reference` prop forwarded to DropdownMenuContent's `:reference` (Reka PopperContent virtual-element anchor). If `reference` is provided, the trigger may be omitted (anchored mode). Document this in a code comment for Plan 04.
  </behavior>
  <action>
    Create `DropdownMenu.vue` composing Reka `DropdownMenuRoot`+`DropdownMenuTrigger`+`DropdownMenuPortal`+`DropdownMenuContent` with optional `DropdownMenuItem`/`DropdownMenuSeparator`/`DropdownMenuLabel` exposed via slots. Design the API for reuse:
    - Props: `open?: boolean` (controlled), `reference?` (virtual element / HTMLElement bounding-rect source forwarded to `DropdownMenuContent :reference` for anchored mode — this is what lets Plan 04 anchor the menu to the kebab without a literal trigger child). Emits: `update:open`.
    - Slots: `#trigger` (rendered inside `DropdownMenuTrigger` — optional when `reference` is set), default slot for the menu items (rendered inside `DropdownMenuContent`).
    - Content classes (token-driven): `cn('z-[9999] min-w-[220px] max-w-[320px] bg-popover text-popover-foreground border border-white/10 rounded-xl shadow-xl shadow-black/30 p-1 overflow-hidden', props.class)`. Use `cn` from `@/lib/utils`. Add `data-[state=open]:` / `data-[state=closed]:` fade+scale transition utilities for parity with the bespoke ContextMenu's transition (Plan 04 reuses this).
    - Forward `align`/`side`/`sideOffset` props to DropdownMenuContent with sensible defaults (align 'start', side 'bottom', sideOffset 4) so Plan 04 can anchor to the kebab's right/top.
    Create `DropdownMenu.spec.ts` per the behavior block. For portaled content use `attachTo: document.body` + query document, or assert trigger slot + open-emit. Add a comment in the spec noting the anchored-mode `reference` prop is exercised live in Plan 04 (kebab).
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/DropdownMenu.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>DropdownMenu.vue on Reka with #trigger + items slots + optional `reference` anchored mode + v-model:open; token-driven bg-popover content; spec green; vue-tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tooltip + Popover (portal primitives) + TooltipProvider in App.vue + specs</name>
  <files>frontend/web/src/components/ui/Tooltip.vue, frontend/web/src/components/ui/Tooltip.spec.ts, frontend/web/src/components/ui/Popover.vue, frontend/web/src/components/ui/Popover.spec.ts, frontend/web/src/App.vue</files>
  <behavior>
    Tooltip:
    - mount renders TooltipRoot; #trigger slot renders inside TooltipTrigger; default/#content slot is the tooltip body
    - content classes contain 'bg-popover' + 'text-popover-foreground' + 'rounded-md px-3 py-1.5 text-xs'
    - accepts a `delayDuration` prop (forwarded to TooltipRoot)
    Popover:
    - mount renders PopoverRoot; v-model:open round-trips (emitting open-change emits update:open)
    - #trigger slot inside PopoverTrigger; default/#content slot inside PopoverContent
    - content classes contain 'bg-popover' + 'border' + 'rounded-xl p-4'
    App.vue:
    - App still mounts (existing App.spec or a smoke mount does not throw); the app shell is wrapped in a single TooltipProvider
  </behavior>
  <action>
    Tooltip.vue: compose `TooltipRoot`+`TooltipTrigger`+`TooltipPortal`+`TooltipContent`(+`TooltipArrow`). Props: `delayDuration?` (forward to TooltipRoot), `side?`/`sideOffset?` (forward to content). Slots: `#trigger`, default (content). Content class `cn('z-[9999] bg-popover text-popover-foreground rounded-md px-3 py-1.5 text-xs shadow-md select-none', props.class)`. Note: Tooltip relies on a `TooltipProvider` ancestor (see App.vue change below).

    Popover.vue: compose `PopoverRoot`(v-model:open)`+PopoverTrigger`+`PopoverPortal`+`PopoverContent`(+`PopoverArrow`). Props: `open?` (controlled), `side?`/`align?`/`sideOffset?`. Emits: `update:open`. Slots: `#trigger`, default (content). Content class `cn('z-[9999] bg-popover text-popover-foreground border border-white/10 rounded-xl p-4 shadow-xl', props.class)`.

    App.vue: import `TooltipProvider` from `'reka-ui'` and wrap the app shell with it (the standard shadcn-vue app-root pattern, RESEARCH note + assumption A5). MINIMAL change: wrap the existing root `<div id="app">...</div>` content in `<TooltipProvider :delay-duration="300"> ... </TooltipProvider>` (or wrap inside the div — keep the `id="app"` element + all existing children/Navbar/Toaster/Footer/NotificationToast intact). Do NOT alter any existing App.vue logic, watchers, or error handling. Confirm the TooltipProvider does not introduce an extra DOM wrapper that breaks layout (it renders as a Fragment / Slot — verify in the orchestrator gate's standing 5-surface smoke).

    Create Tooltip.spec.ts + Popover.spec.ts per the behavior block. Tooltip spec must wrap the mount in a TooltipProvider (global stub or real) so TooltipRoot has its ancestor. Use attachTo/document.body for portaled content or assert trigger slot + emits.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/Tooltip.spec.ts src/components/ui/Popover.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>Tooltip + Popover on Reka, token-driven; specs green; TooltipProvider mounted once in App.vue without altering existing logic; vue-tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch + Checkbox (boolean v-model) + specs</name>
  <files>frontend/web/src/components/ui/Switch.vue, frontend/web/src/components/ui/Switch.spec.ts, frontend/web/src/components/ui/Checkbox.vue, frontend/web/src/components/ui/Checkbox.spec.ts</files>
  <behavior>
    Switch:
    - mount(Switch, { props: { modelValue: false } }) renders a SwitchRoot button (role=switch)
    - toggling emits 'update:modelValue' with true (boolean v-model, NOT :checked)
    - on-state root classes contain 'bg-primary'; off-state contains 'bg-white/10'; thumb has 'bg-white rounded-full' + a data-[state=checked]:translate-x utility
    - supports `disabled` prop
    Checkbox:
    - mount(Checkbox, { props: { modelValue: false } }) renders a CheckboxRoot
    - toggling emits 'update:modelValue' with true (boolean v-model)
    - checked-state root classes contain 'data-[state=checked]:bg-primary' + 'border-input'; CheckboxIndicator renders a lucide check icon when checked
    - accepts 'indeterminate' as a modelValue value (type is boolean | 'indeterminate')
  </behavior>
  <action>
    Switch.vue: compose `SwitchRoot`+`SwitchThumb`. v-model is PLAIN boolean (Reka 2.x — `v-model="model"` on SwitchRoot, NOT `v-model:checked`; RESEARCH State-of-the-Art row). Props: `modelValue: boolean`, `disabled?`. Emits: `update:modelValue: [boolean]`. Bridge via a computed get/set like the existing Input/Select pattern. Root class `cn('relative inline-flex h-6 w-11 shrink-0 items-center rounded-full transition-colors data-[state=checked]:bg-primary data-[state=unchecked]:bg-white/10 disabled:opacity-50 disabled:cursor-not-allowed', props.class)`; thumb class `'pointer-events-none block h-5 w-5 rounded-full bg-white shadow-lg transition-transform data-[state=checked]:translate-x-5 data-[state=unchecked]:translate-x-0.5'`.

    Checkbox.vue: compose `CheckboxRoot`+`CheckboxIndicator`. v-model `boolean | 'indeterminate'`. Props: `modelValue: boolean | 'indeterminate'`, `disabled?`. Emits: `update:modelValue`. Root class `cn('h-5 w-5 shrink-0 rounded-md border border-input data-[state=checked]:bg-primary data-[state=checked]:border-primary data-[state=indeterminate]:bg-primary transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center', props.class)`. Inside `CheckboxIndicator` render a lucide `Check` icon (`import { Check } from 'lucide-vue-next'` — iconLibrary is lucide per components.json) sized `h-3.5 w-3.5 text-primary-foreground` (and a `Minus` for indeterminate if trivially available, else just Check).

    Create Switch.spec.ts + Checkbox.spec.ts per the behavior block: mount, assert root role/classes, trigger the toggle (`await wrapper.trigger('click')` on the root or call the model setter) and assert `wrapper.emitted('update:modelValue')` carries the boolean.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/Switch.spec.ts src/components/ui/Checkbox.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>Switch + Checkbox on Reka with boolean v-model (Checkbox also accepts 'indeterminate'); token-driven (bg-primary/border-input); specs assert v-model emit; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Barrel exports (5 new primitives) + wave-3 verification + commits</name>
  <files>frontend/web/src/components/ui/index.ts</files>
  <action>
    Edit `frontend/web/src/components/ui/index.ts` to add 5 new default exports (alphabetical-ish, anywhere consistent with the existing style): `Checkbox`, `DropdownMenu`, `Popover`, `Switch`, `Tooltip` — e.g. `export { default as Checkbox } from './Checkbox.vue'` etc. Leave every existing line unchanged.

    Run full wave-3 verification, then commit atomically with explicit-path staging (NEVER `git add -A`). COMMIT ONLY — do NOT push. 3 co-authors on every commit:

    Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
    Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
    Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>

    Commit grouping:
    - `git add frontend/web/src/components/ui/DropdownMenu.vue frontend/web/src/components/ui/DropdownMenu.spec.ts` → `feat(design-system/03): add Reka DropdownMenu primitive (DS-LIB-08)`
    - `git add frontend/web/src/components/ui/Tooltip.vue frontend/web/src/components/ui/Tooltip.spec.ts frontend/web/src/App.vue` → `feat(design-system/03): add Tooltip + mount TooltipProvider in App.vue (DS-LIB-06)`
    - `git add frontend/web/src/components/ui/Popover.vue frontend/web/src/components/ui/Popover.spec.ts` → `feat(design-system/03): add Popover primitive (DS-LIB-06)`
    - `git add frontend/web/src/components/ui/Switch.vue frontend/web/src/components/ui/Switch.spec.ts frontend/web/src/components/ui/Checkbox.vue frontend/web/src/components/ui/Checkbox.spec.ts frontend/web/src/components/ui/index.ts` → `feat(design-system/03): add Switch + Checkbox primitives + barrel exports (DS-LIB-06)`
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/ui/ && bunx vue-tsc --noEmit && [ "$(cd /data/animeenigma && git diff HEAD~4 --name-only | grep -c 'main.css')" = "0" ]</automated>
  </verify>
  <done>All 5 new primitives exported from barrel; full ui/ suite green; vue-tsc clean; main.css untouched; 4 atomic commits with 3 co-authors; nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Tooltip/Popover/DropdownMenu content → body portal | Portaled DOM; Reka manages focus/dismiss. No untrusted data — slot content is app-authored. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | App.vue TooltipProvider wrap | mitigate | Wrap is additive (Fragment/Slot render); orchestrator standing-5-surface smoke confirms no layout shift from the wrapper. |
| T-03-07 | Elevation of privilege | Switch/Checkbox v-model | accept | Pure UI state toggle; no auth/permission semantics. Booleans only. |
</threat_model>

<verification>
- `bunx vue-tsc --noEmit` clean (new primitives + App.vue edit compile).
- `bunx vitest run src/components/ui/` all green.
- `grep -n "TooltipProvider" src/App.vue` → present and wraps the shell.
- `grep -c "DropdownMenu\|Tooltip\|Popover\|Switch\|Checkbox" src/components/ui/index.ts` → ≥5 new exports.
- main.css NOT in commit diff.
</verification>

<success_criteria>
1. DropdownMenu + Tooltip + Popover + Switch + Checkbox exist, token-driven, each with a Vitest mount test (DS-LIB-06 + DS-LIB-08 part b).
2. Switch/Checkbox use boolean v-model; Checkbox accepts 'indeterminate'.
3. TooltipProvider mounted once in App.vue; existing App logic untouched.
4. Barrel exports all 5; vue-tsc clean; specs green; 4 atomic commits, 3 co-authors, NO push.
</success_criteria>

<orchestrator_browser_gate>
LIGHT — these are greenfield additions with no current consumers, so there is no "identical rendering" risk to gate. The only live check needed:
1. **App.vue TooltipProvider wrap did not shift layout** — confirm during the standing 5-surface smoke (Home/Browse/Anime/player/404) that the app shell renders exactly as before (TooltipProvider should be a transparent Fragment). If any layout shift appears, the wrap introduced a stray DOM node — file back to planner.
DropdownMenu's live behavior is gated in Plan 04 (the kebab rebuild) where it gets a real consumer.
</orchestrator_browser_gate>

<output>
After completion, create `.planning/workstreams/design-system/phases/03-primitive-set-swap/03-03-SUMMARY.md`
</output>
