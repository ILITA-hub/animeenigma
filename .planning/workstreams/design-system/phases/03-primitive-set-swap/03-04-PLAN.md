---
phase: 03-primitive-set-swap
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - frontend/web/src/composables/useContextMenu.ts
  - frontend/web/src/components/anime/AnimeContextMenu.vue
  - frontend/web/src/components/anime/AnimeContextMenu.spec.ts
  - frontend/web/src/components/ui/ContextMenu.vue
  - frontend/web/src/views/Home.vue
  - frontend/web/src/views/Browse.vue
  - frontend/web/src/views/Schedule.vue
  - frontend/web/src/views/Profile.vue
  - frontend/web/src/views/Anime.vue
autonomous: false
requirements: [DS-LIB-08, DS-NF-04]
must_haves:
  truths:
    - "Right-clicking an anime card shows the NATIVE browser context menu (no preventDefault, no custom cursor menu)"
    - "Clicking the anime-card kebab opens a Reka DropdownMenu anchored to the kebab, with the EXACT same action set + auth-gating as the old AnimeContextMenu (status set / remove / mark-next / go-to-page / open-new-tab + poster/info header + login-to-manage notice)"
    - "The bespoke cursor-positioned components/ui/ContextMenu.vue is retired (deleted), and the cursor open(event)/preventDefault/clientX-Y path is removed from useContextMenu.ts"
    - "All 5 consumer views (Home/Browse/Schedule/Profile/Anime) still compile + their kebab menus work"
    - "AnimeContextMenu's emitted events (statusChange/removeFromList/episodesChange) + optimistic store calls are preserved"
  artifacts:
    - path: "frontend/web/src/components/anime/AnimeContextMenu.vue"
      provides: "Action menu rebuilt on Reka DropdownMenu, anchored to the kebab"
      contains: "DropdownMenu"
    - path: "frontend/web/src/composables/useContextMenu.ts"
      provides: "Trigger-anchored state only; cursor open(event) path removed"
    - path: "frontend/web/src/components/anime/AnimeContextMenu.spec.ts"
      provides: "action-set + auth-gating + emit assertions"
  key_links:
    - from: "frontend/web/src/components/anime/AnimeContextMenu.vue"
      to: "frontend/web/src/components/ui/DropdownMenu.vue"
      via: "Reka DropdownMenu anchored via :reference to the kebab element"
      pattern: "DropdownMenu"
    - from: "frontend/web/src/components/anime/AnimeContextMenu.vue"
      to: "@/stores/watchlist"
      via: "setStatusOptimistic / removeEntryOptimistic preserved"
      pattern: "Optimistic"
    - from: "frontend/web/src/views/Home.vue"
      to: "AnimeContextMenu kebab flow"
      via: "openAtElement still wires the kebab; no @contextmenu binding"
      pattern: "openAtElement|openHomeCtx"
---

<objective>
Execute DS-LIB-08 — the intentional UX change (NOT zero-diff) locked by the 2026-06-02 user
decision. Restore NATIVE browser right-click on anime cards by removing the custom right-click
interception, retire the bespoke cursor-positioned `components/ui/ContextMenu.vue`, and rebuild
the anime-card action menu (`AnimeContextMenu.vue`) on the trigger-anchored Reka `DropdownMenu`
added in Plan 03 — anchored to the existing `AnimeKebab` trigger, preserving the EXACT action
set + auth-gating. Wire it across the kebab consumers (Home/Browse/Schedule/Profile/Anime).

IMPORTANT — this OVERRIDES the RESEARCH "carve-out" recommendation (Decision #2 / assumption
A1). The brief, ROADMAP Phase 3 goal+success-criterion 4, and REQUIREMENTS DS-LIB-08 are the
LOCKED user decision and take precedence: ContextMenu.vue IS retired, the cursor path IS
removed, and AnimeContextMenu IS rebuilt on Reka DropdownMenu. Do NOT leave ContextMenu.vue
in place.

Verified during planning: NO consumer currently binds `@contextmenu` and NO consumer
destructures the cursor `open()` function — the kebab flow uses `openAtElement` (click) +
`onTouchstart` (mobile long-press). So removing the cursor `open(event)` path from the
composable is safe; the kebab `openAtElement` path is what must be re-anchored onto Reka.

Acceptance (ROADMAP criterion 4, verified LIVE): right-click an anime card → BROWSER's native
menu appears; click the kebab → the Reka DropdownMenu with the same actions.

Output: rewritten AnimeContextMenu on DropdownMenu + spec; slimmed useContextMenu; deleted
ContextMenu.vue; consumer views updated; final full-phase verification gate.

Requirements: DS-LIB-08 (right-click rework), DS-NF-04 (consumers still compile / kebab UX preserved in behavior).
Effort note (no days/hours): UXΔ = +2 (Better — native right-click restored, actions behind a discoverable trigger) | CDI = 0.07 * 21 | MVQ = Griffin 85%/80%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/phases/03-primitive-set-swap/RESEARCH.md
@.planning/workstreams/design-system/REQUIREMENTS.md
@.planning/workstreams/design-system/ROADMAP.md

# The DropdownMenu primitive built in Plan 03 (this plan's foundation):
@frontend/web/src/components/ui/DropdownMenu.vue

# Right-click machinery being reworked (read EXACT action set + auth-gating + emits):
@frontend/web/src/composables/useContextMenu.ts
@frontend/web/src/components/anime/AnimeContextMenu.vue
@frontend/web/src/components/anime/AnimeKebab.vue
@frontend/web/src/components/ui/ContextMenu.vue

<interfaces>
<!-- DropdownMenu API from Plan 03 (use these directly): -->
DropdownMenu props: open?(controlled boolean), reference?(HTMLElement/virtual element to anchor content — ANCHORED MODE, lets us position the menu at the kebab without a literal trigger child), align?/side?/sideOffset?. Emits: update:open. Slots: #trigger (optional in anchored mode), default (menu items). Content is token-driven (bg-popover) + has data-[state] transitions.

<!-- AnimeContextMenu PUBLIC surface to PRESERVE (consumers depend on these): -->
Props: visible(bool), x(number), y(number), anime(Anime|null), listStatus(string|null), siteRating?, episodesWatched?, episodesTotal?.
Emits: update:visible, statusChange[animeId,status], removeFromList[animeId], episodesChange[animeId,episodes].
Action set (EXACT — preserve all, with auth-gating):
  - if authenticated: 5 status options (watching/plan_to_watch/completed/on_hold/dropped) w/ current-check icon; if listStatus set → remove (danger); if listStatus==='watching' && (total===0 || ep<total) → mark-next.
  - always: go-to-page, open-in-new-tab.
  - if NOT authenticated: a 'login to manage' notice (anime.loginToManage), then go-to-page + open-in-new-tab only.
  - header: poster img + title + year/episodes + rating + siteRating.
Optimistic store calls to PRESERVE: watchlistStore.setStatusOptimistic, watchlistStore.removeEntryOptimistic, userApi.updateWatchlistEntry + watchlistStore.invalidate/fetchWatchlist (mark-next). Emit BEFORE await (optimistic mirror sync), same as today.

<!-- useContextMenu state model to PRESERVE (consumers read contextMenu.{visible,anime,listStatus,siteRating,episodesWatched,episodesTotal}): -->
Keep: reactive contextMenu state, openAtElement(el, anime, opts), close(), onTouchstart/onTouchmove/onTouchend (mobile long-press still anchors at touch point — keep as a menu open, not a cursor menu).
REMOVE: the cursor open(event, anime, opts) function (the event.preventDefault() + clientX/clientY path) — DS-LIB-08 part (a).
NOTE: openAtElement currently stores x/y from el.getBoundingClientRect(); for the Reka anchored mode, store the ELEMENT (or its rect) so AnimeContextMenu can pass it as DropdownMenu :reference. Add an `anchorEl: HTMLElement | null` (or a rect) to the state, set by openAtElement + onTouchstart.

<!-- AnimeKebab emits @open(el: HTMLElement) on click/enter/space — the consumer calls openAtElement(el, ...). Keep this. -->

<!-- Consumer wiring (5 views), all identical shape — keep, just ensure they pass the anchor element through: -->
Home.vue: openHomeCtx = openAtElement; <AnimeContextMenu :visible :x :y :anime :list-status :site-rating @update:visible @status-change @remove-from-list>
Browse.vue: openContextMenuAt = openAtElement; AnimeCardNew @open-menu → openContextMenuAt(el, anime, opts); @touchstart→onTouchstart
Schedule.vue: openScheduleCtx = openAtElement; AnimeKebab @open → openScheduleMenuAt(el, anime)
Profile.vue: openProfileCtxAt = openAtElement; AnimeKebab @open → openProfileMenuAt(el, anime); passes episodesWatched/episodesTotal
Anime.vue: openContextMenuAt = openAtElement (related-anime cards)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slim useContextMenu (drop cursor path, add anchor element) + rebuild AnimeContextMenu on Reka DropdownMenu + spec</name>
  <files>frontend/web/src/composables/useContextMenu.ts, frontend/web/src/components/anime/AnimeContextMenu.vue, frontend/web/src/components/anime/AnimeContextMenu.spec.ts</files>
  <behavior>
    AnimeContextMenu (spec — assert logic, not portaled DOM):
    - authenticated + listStatus='watching' + episodesWatched=2,episodesTotal=12 → actions include all 5 status options, a remove action, AND a mark-next action; goto + newtab present
    - authenticated + listStatus=null → 5 status options, NO remove, NO mark-next; goto+newtab present
    - NOT authenticated → NO status/remove/mark-next; the loginToManage notice is shown; goto+newtab present
    - activating a status action emits 'statusChange' with (animeId, status) BEFORE the optimistic store await; activating remove emits 'removeFromList'; mark-next emits 'episodesChange'
    - closing the menu emits 'update:visible' false
    useContextMenu (spec or assertion):
    - the composable no longer exports a cursor `open` function (only openAtElement/close/onTouchstart/onTouchmove/onTouchend + contextMenu state)
    - openAtElement(el, anime, opts) sets contextMenu.visible=true, contextMenu.anime, and the new anchor element/rect
  </behavior>
  <action>
    useContextMenu.ts:
    - DELETE the `open(event, anime, opts)` function entirely (the `event.preventDefault()` + `contextMenu.x = event.clientX` path) and remove it from the returned object — DS-LIB-08 part (a).
    - Add an anchor to the reactive state for Reka anchored positioning: `anchorEl: HTMLElement | null` (set by `openAtElement` to the passed `el`, and by `onTouchstart` to a virtual element built from the touch point — Reka accepts a virtual element with `getBoundingClientRect`). Keep `x`/`y` in state for back-compat with the consumer prop bindings (`:x`/`:y`) so views need no edit, but the menu positioning now uses `anchorEl`. Keep `openAtElement`, `close`, `onTouchstart`/`onTouchmove`/`onTouchend`, and the scroll-dismiss listener.
    - For `onTouchstart` (mobile long-press): instead of a cursor menu, build a virtual reference element at the touch coordinates (`{ getBoundingClientRect: () => ({ x, y, top: y, left: x, right: x, bottom: y, width: 0, height: 0 }) as DOMRect }`) and store it as `anchorEl`. Keeps long-press working as a trigger-anchored menu (no preventDefault on the native menu — long-press on touch doesn't conflict with desktop right-click).

    AnimeContextMenu.vue:
    - REPLACE the `<ContextMenu :visible :x :y>` wrapper with the Plan-03 `<DropdownMenu>` in ANCHORED mode: `<DropdownMenu :open="visible" :reference="anchorEl" @update:open="$emit('update:visible', $event)" align="start" side="right" :side-offset="4">`. The `anchorEl` comes through as a new prop (add `anchorEl?: HTMLElement | { getBoundingClientRect(): DOMRect } | null` to props) OR read it from the composable state passed by the view — preferred: add `anchorEl` to the props the views already spread. (Views currently pass `:x`/`:y`; add `:anchor-el="contextMenu.anchorEl"` to each of the 5 consumer bindings in Task 2.)
    - Keep the ENTIRE inner content identical in spirit: poster/info header, the auth-gated `loginToManage` notice, and the action list. Render actions as `<DropdownMenuItem>` (from the DropdownMenu default slot) preserving the `actions` computed, the icons per kind (status-check/remove/mark-next/goto/newtab), `itemClasses` styling (cyan current, pink danger, default), and ALL handlers (`setStatus`/`removeFromList`/`markNextWatched`/`goToPage`/`openInNewTab`) with their optimistic store calls + emit-before-await ordering UNCHANGED. Reka DropdownMenuItem handles roving focus/keyboard natively — you MAY drop the hand-rolled roving-tabindex (`itemEls`/`moveFocus`/`onItemKeydown`) since Reka provides it; if you do, verify keyboard nav in the live gate. Keep the `closeWithReason('item')` semantics by closing the menu (emit update:visible false) after each action.
    - Keep all emits (`update:visible`, `statusChange`, `removeFromList`, `episodesChange`) and the `inject('ctxMenuClose')` replacement (now just close the DropdownMenu). Import `DropdownMenu` from `@/components/ui`.

    Create AnimeContextMenu.spec.ts asserting the action-set + auth-gating + emit behavior above. Mock auth store (authenticated true/false), watchlist store (stub setStatusOptimistic/removeEntryOptimistic), and i18n (global stub returning the key). Assert on the `actions` computed output (action keys/kinds) + emitted events when an action's `onActivate` runs — not on portaled DOM.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/anime/AnimeContextMenu.spec.ts && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>useContextMenu has NO cursor open() (grep confirms); openAtElement/onTouchstart set anchorEl; AnimeContextMenu renders on Reka DropdownMenu in anchored mode with the EXACT action set + auth-gating + emits + optimistic store calls preserved; spec green; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire anchorEl through the 5 consumer views + retire ContextMenu.vue</name>
  <files>frontend/web/src/views/Home.vue, frontend/web/src/views/Browse.vue, frontend/web/src/views/Schedule.vue, frontend/web/src/views/Profile.vue, frontend/web/src/views/Anime.vue, frontend/web/src/components/ui/ContextMenu.vue</files>
  <action>
    In EACH of the 5 views' `<AnimeContextMenu ...>` binding, add `:anchor-el="contextMenu.anchorEl"` (Profile uses `profileContextMenu.anchorEl`). The existing `:visible`/`:x`/`:y`/`:anime`/`:list-status`/`:site-rating`/`:episodes-*`/`@update:visible`/`@status-change`/`@remove-from-list`/`@episodes-change` bindings stay UNCHANGED. Do NOT add any `@contextmenu` binding (native right-click must stay native — DS-LIB-08 part a). Confirm none of the views import or reference the cursor `open` function (verified during planning that none do — but re-check after editing).

    DELETE `frontend/web/src/components/ui/ContextMenu.vue` (the bespoke cursor menu — DS-LIB-08 retires it). Confirm it is NOT exported from the barrel (verified: it never was) and its ONLY importer was AnimeContextMenu.vue (now reworked in Task 1). Use `git rm frontend/web/src/components/ui/ContextMenu.vue`.

    Verify no dangling references: `grep -rn "ui/ContextMenu" frontend/web/src` returns nothing; `grep -rn "\.open(" frontend/web/src/views frontend/web/src/composables/useContextMenu.ts | grep -i contextmenu` returns nothing (cursor path gone).
  </action>
  <verify>
    <automated>cd frontend/web && ! grep -rq "ui/ContextMenu" src && [ ! -f src/components/ui/ContextMenu.vue ] && bunx vue-tsc --noEmit</automated>
  </verify>
  <done>All 5 views pass anchorEl through; no @contextmenu bindings added; ContextMenu.vue deleted with no dangling imports; vue-tsc clean (all 5 views + AnimeContextMenu compile).</done>
</task>

<task type="auto">
  <name>Task 3: Final full-phase verification gate + build + commits</name>
  <files>frontend/web/src/components/anime/AnimeContextMenu.vue</files>
  <action>
    Run the FULL phase verification gate (this is the phase's terminal automated gate — it must pass before the orchestrator runs the in-browser checks):
    - `cd frontend/web && bunx vue-tsc --noEmit` (zero errors — proves ALL ~18 swap consumers + 5 kebab views compile against preserved APIs; DS-NF-04)
    - `cd frontend/web && bunx vitest run` (FULL suite green — all ui/ specs + AnimeContextMenu spec + the rest of the app's specs)
    - `cd frontend/web && bun run build` (production build clean — no new tsc/build errors)

    Then commit atomically with explicit-path staging (NEVER `git add -A` — parallel sessions have dirty `.claude/worktrees/`, `tmp/`, `services/scraper/scraper-api`). COMMIT ONLY — do NOT push (push happens later via /animeenigma-after-update or explicit user OK). 3 co-authors on every commit:

    Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
    Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
    Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>

    Commit grouping:
    - `git add frontend/web/src/composables/useContextMenu.ts frontend/web/src/components/anime/AnimeContextMenu.vue frontend/web/src/components/anime/AnimeContextMenu.spec.ts` → `feat(design-system/03): rebuild anime action-menu on Reka DropdownMenu, drop cursor right-click path (DS-LIB-08)`
    - `git add frontend/web/src/views/Home.vue frontend/web/src/views/Browse.vue frontend/web/src/views/Schedule.vue frontend/web/src/views/Profile.vue frontend/web/src/views/Anime.vue` → `feat(design-system/03): wire kebab anchor through 5 views, native right-click restored (DS-LIB-08)`
    - `git rm frontend/web/src/components/ui/ContextMenu.vue` (stage the deletion) → `chore(design-system/03): retire bespoke cursor ContextMenu.vue (DS-LIB-08)`

    Do NOT run `/animeenigma-after-update` here — the orchestrator coordinates deploy + the batch after-update at phase end (per the project's batch rule). Leave the working tree committed-but-not-pushed and report the in-browser gate checklist to the orchestrator.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vue-tsc --noEmit && bunx vitest run && bun run build && [ "$(cd /data/animeenigma && git diff HEAD~3 --name-only | grep -c 'main.css')" = "0" ]</automated>
  </verify>
  <done>vue-tsc clean; full vitest green; bun run build clean; main.css untouched across the phase; 3 atomic commits (incl. the git rm) with 3 co-authors; nothing pushed; in-browser gate checklist reported to orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kebab click / long-press → DropdownMenu (body portal) | User-triggered menu open; Reka manages focus/dismiss. Action handlers call watchlist API (authenticated) — same surface as today. |
| Native right-click | Now passes through to the browser (no interception) — DS-LIB-08 intended behavior. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of privilege | AnimeContextMenu auth-gated actions | mitigate | Preserve the EXACT `authStore.isAuthenticated` gating: status/remove/mark-next only when authenticated; spec asserts unauthenticated users get only goto/newtab + the login notice. |
| T-03-09 | Tampering | Optimistic watchlist mutations | mitigate | Keep setStatusOptimistic/removeEntryOptimistic + emit-before-await ordering unchanged; server is source of truth on failure (existing rollback toasts retained). |
| T-03-10 | Repudiation | mark-next episode update | accept | userApi.updateWatchlistEntry already authenticated server-side; client rework does not change the API contract. |
</threat_model>

<verification>
- `grep -n "function open(" frontend/web/src/composables/useContextMenu.ts` → returns nothing (cursor path removed).
- `grep -rn "ui/ContextMenu" frontend/web/src` → nothing; `[ ! -f src/components/ui/ContextMenu.vue ]`.
- `grep -rn "@contextmenu" frontend/web/src/views` → nothing (native right-click preserved).
- `grep -n "DropdownMenu" frontend/web/src/components/anime/AnimeContextMenu.vue` → present.
- `bunx vue-tsc --noEmit` + full `bunx vitest run` + `bun run build` all clean.
- main.css untouched across the entire phase.
</verification>

<success_criteria>
1. Native browser right-click works on anime cards (no preventDefault, no custom cursor menu) — ROADMAP criterion 4 part a.
2. Kebab opens a Reka DropdownMenu with the same list-management actions + auth-gating — ROADMAP criterion 4 part b.
3. ContextMenu.vue retired; cursor open() path removed from useContextMenu — DS-LIB-08 part a.
4. All 5 views + AnimeContextMenu compile (vue-tsc); full vitest + build green; main.css untouched.
5. 3 atomic commits (incl. git rm), explicit-path staging, 3 co-authors, NO push.
</success_criteria>

<orchestrator_browser_gate>
REQUIRED — DS-LIB-08 is an INTENTIONAL UX change; ROADMAP criterion 4 + 6 mandate live verification of the NEW behavior (not the old). The orchestrator must run AFTER deploy:

1. **Native right-click restored** (DS-LIB-08 acceptance): right-click an anime card on Home, Browse, and a Profile watchlist card → the BROWSER's native context menu appears (Inspect / Open in new tab / etc.). NO custom cursor menu, no preventDefault.
2. **Kebab → Reka DropdownMenu** (DS-LIB-08 acceptance): hover/focus a card → kebab appears → click it → the Reka DropdownMenu opens ANCHORED to the kebab with the EXACT actions:
   - Authenticated: 5 status options (current one checked/highlighted) + Remove (when listed) + Mark-next (when watching with a next episode) + Go-to-page + Open-in-new-tab. Verify a status change + remove + mark-next actually mutate the watchlist (optimistic + persisted), and the menu closes after.
   - Unauthenticated: only the login-to-manage notice + Go-to-page + Open-in-new-tab.
   - Header shows poster + title + year/episodes + ratings.
   - Keyboard: Arrow/Home/End/Enter/Esc navigate + activate + dismiss (Reka native roving focus).
3. **Mobile long-press** (touch): long-press a card → the action menu opens anchored at the touch point (still works as a menu, not a cursor menu); native long-press selection is acceptable per the intended change.
4. **All 5 views**: spot-check the kebab menu on Home columns, Browse grid, Schedule list, Profile watchlist (with episodesWatched/Total → mark-next), and Anime-detail related cards.
5. **Standing 5-surface smoke** — confirm Home/Browse/Anime-detail/player/404 otherwise unchanged. Per ROADMAP criterion 6, the right-click/kebab surface is verified against the NEW intended behavior; everything else must be zero-diff.
6. **6 composites untouched** (ROADMAP criterion 5): ButtonGroup/GenreFilterPopup/PaginationBar/SearchAutocomplete/Skeleton/Toaster still render normally (e.g. GenreFilterPopup popover on Browse, PaginationBar, the search box) — confirm no incidental breakage.

This is the phase's final gate. If any check fails, file back to the planner with the specific surface + observed-vs-expected.
</orchestrator_browser_gate>

<output>
After completion, create `.planning/workstreams/design-system/phases/03-primitive-set-swap/03-04-SUMMARY.md`
</output>
