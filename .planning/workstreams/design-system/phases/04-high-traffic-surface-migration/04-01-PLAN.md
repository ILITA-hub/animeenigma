---
phase: 04-high-traffic-surface-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/views/Home.vue
  - frontend/web/src/views/Browse.vue
  - frontend/web/src/components/anime/AnimeCardNew.vue
  - frontend/web/src/components/anime/AnimeCard.vue
  - frontend/web/src/components/anime/EpisodeCard.vue
  - frontend/web/src/components/anime/GenreChip.vue
  - frontend/web/src/components/anime/AnimeContextMenu.vue
  - frontend/web/src/components/anime/AnimeKebab.vue
  - frontend/web/src/components/anime/AnimeQuickNav.vue
autonomous: false
requirements: [DS-MIGRATE-01, DS-MIGRATE-02, DS-MIGRATE-03, DS-MIGRATE-04, DS-MIGRATE-06]

must_haves:
  truths:
    - "Home and Browse catalog surfaces contain zero off-palette color classes and zero hardcoded hex"
    - "Status badges on anime cards (ongoing/announced/released, episode-available, age-rating) render from semantic tokens"
    - "Home spotlight carousel (stats card + RandomTail purple cta-hero) renders pixel-identically at desktop + mobile"
    - "Browse filter sidebar, star badges, cards, and pagination render pixel-identically"
  artifacts:
    - path: "frontend/web/src/views/Home.vue"
      provides: "Token-only home view; --ink-3/--accent usages repointed to canonical"
      contains: "var(--muted-foreground)"
    - path: "frontend/web/src/components/anime/AnimeCardNew.vue"
      provides: "Card status/badge colors from semantic tokens"
    - path: "frontend/web/src/components/anime/AnimeCard.vue"
      provides: "Legacy card hex replaced with tokens"
  key_links:
    - from: "frontend/web/src/views/Home.vue"
      to: "main.css @theme inline tokens"
      via: "bg-/text- semantic utilities + var(--token) references"
      pattern: "text-muted-foreground|var\\(--muted-foreground\\)"
    - from: "frontend/web/src/components/anime/AnimeCardNew.vue"
      to: "--success/--warning/--brand-violet/--destructive"
      via: "semantic Tailwind utilities"
      pattern: "bg-success|bg-warning|bg-brand-violet|bg-destructive"
---

<objective>
Migrate the catalog surfaces — Home, Browse, and the anime-card family — off off-palette
Tailwind color classes and hardcoded hex onto the canonical token system, and repoint the
deprecated `--ink-3`/`--accent` alias usages in Home.vue to canonical token names.

This is a VALUE-PRESERVING migration. Every touched surface must render pixel-identically
to today at desktop + mobile widths (smoke surfaces 1 Home + 2 Browse). No new UX, no
layout change, no copy change. Color/token/primitive edits only.

Purpose: deliver the most user-visible consistency first (Home/Browse), proving tokens on
the busiest screens (DS-MIGRATE-01).
Output: 9 token-clean catalog SFCs + a clean acceptance grep + a clean in-browser smoke of
Home + Browse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-UI-SPEC.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<locked_mapping>
Off-palette → semantic token (DS-MIGRATE-02, apply per-occurrence with role judgment):
- red → destructive · amber/yellow → warning · emerald/green → success
- blue/sky → info · purple/violet → brand-violet · gray/slate/zinc → muted/card/border (judge per role)
Opacity modifiers (/20, /80): prefer the documented `-soft` token when intent is a soft
background; keep the modifier on the base token only when no `-soft` matches the alpha.
`text-gray-400` → `text-muted-foreground`.

Alias → canonical (DS-MIGRATE-04, repoint USAGES only — do NOT flip the --accent definition):
- --ink → --foreground · --ink-3 → --muted-foreground · --accent → --brand-cyan (per role)
- --pink → --brand-pink · --pink-soft → --brand-pink-soft
- LITERAL (no canonical equivalent — leave as-is unless a clear token matches):
  --ink-2, --ink-4, --accent-soft, --accent-line, --accent-glow

Verified existing tokens: bg-success/-soft/-foreground, bg-warning/-soft/-foreground,
bg-info/-soft/-foreground, bg-destructive/-soft/-foreground, bg-brand-violet (NO -soft),
bg-muted, bg-card, border-border, text-muted-foreground all generate from @theme inline.
</locked_mapping>

<cascade_warning>
DS-NF-02 (load-bearing): `.cta-*` MUST stay inside `@layer components`; `.spotlight-frame`/
`.shuffle-deck`/`.glass-card` MUST stay UNLAYERED. Home.vue is the spotlight cascade-footgun
zone. Do NOT move these classes between layered/unlayered. The spotlight `cta-hero[data-accent]`
family lives in components/home/spotlight/ (Phase 5) and is OUT of scope — `data-accent="purple"`
attribute values are NOT off-palette classes (no -NNN suffix), do NOT touch them.
</cascade_warning>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Home.vue + Browse.vue to tokens + repoint aliases</name>
  <files>frontend/web/src/views/Home.vue, frontend/web/src/views/Browse.vue</files>
  <read_first>
    - frontend/web/src/views/Home.vue (file being modified)
    - frontend/web/src/views/Browse.vue (file being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-file line map: Home off-palette lines 76/102/126 `text-gray-400`; Home alias lines 313/352 `--ink-3`, 321/365 `--accent-line`, 364 `--accent-soft`, 367 `--accent`; Browse already grep-clean of off-palette per inventory)
  </read_first>
  <action>
    In Home.vue: replace the three `text-gray-400` occurrences (research lines 76/102/126)
    with `text-muted-foreground`. In the `<style scoped>` block, repoint `var(--ink-3)`
    (lines 313/352) → `var(--muted-foreground)` and `var(--accent)` (line 367) →
    `var(--brand-cyan)` per role. LEAVE `--accent-line` (321/365) and `--accent-soft` (364)
    untouched — they are literal aliases with no canonical equivalent (per DS-MIGRATE-04 literal
    list). Do NOT move or relayer any `.cta-*`/`.spotlight-frame`/`.glass-card`/`.shuffle-deck`
    rules — only change the color value inside existing declarations. In Browse.vue: re-run the
    acceptance grep; if any off-palette class or hex literal is present, map per the locked table.
    Inventory shows Browse grep-clean — if confirmed clean, no edit needed beyond verifying.
    DO NOT touch copy, i18n keys, spacing, or font utilities.
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/views/Home.vue src/views/Browse.vue | grep -v '^#'; grep -rnE 'var\(--(ink|accent|pink)\b' src/views/Home.vue src/views/Browse.vue | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow'</automated>
  </verify>
  <acceptance_criteria>
    - The off-palette grep above returns ZERO hits on Home.vue and Browse.vue.
    - The alias grep returns ZERO repointable `var(--ink|--accent|--pink)` hits (only literal `--accent-line`/`--accent-soft` may remain).
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
    - No `.cta-*`/`.spotlight-frame`/`.glass-card`/`.shuffle-deck` rule moved between layered and unlayered.
  </acceptance_criteria>
  <done>Home.vue and Browse.vue contain zero off-palette classes; repointable aliases repointed; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate anime-card family to semantic tokens + hex→token</name>
  <files>frontend/web/src/components/anime/AnimeCardNew.vue, frontend/web/src/components/anime/AnimeCard.vue, frontend/web/src/components/anime/EpisodeCard.vue, frontend/web/src/components/anime/GenreChip.vue, frontend/web/src/components/anime/AnimeContextMenu.vue, frontend/web/src/components/anime/AnimeKebab.vue, frontend/web/src/components/anime/AnimeQuickNav.vue</files>
  <read_first>
    - All 7 anime/ SFCs listed in files (files being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-file line map: AnimeCardNew lines 41 `bg-amber-500/90`, 89 `bg-purple-500/80`, 218 `bg-emerald-500/80`, 219 `bg-amber-500/80`, 220 `bg-red-500/80`; AnimeContextMenu line 28 `text-amber-400`; EpisodeCard line 28 `bg-emerald-500`; AnimeCard hex lines 49 `#1a1a1a`/68 `#111`/104+160 `#ff6b6b`/126 `#ffd700` star-gold/139 `#fff`/155+177 `#999`+`#aaa`/174 `#333`; AnimeCard is barrel-only with no live non-barrel consumer per A1, migrate regardless; novel-hex note: `#ffd700` star-gold judge warning vs star token)
  </read_first>
  <action>
    AnimeCardNew.vue: map per locked table — `bg-amber-500/90`→`bg-warning/90` (or `bg-warning-soft`
    if it's a soft fill), `bg-purple-500/80`→`bg-brand-violet/80`, `bg-emerald-500/80`→`bg-success/80`,
    `bg-amber-500/80`→`bg-warning/80`, `bg-red-500/80`→`bg-destructive/80`. These are status pill
    backgrounds (ongoing/announced/released, episode-available, age-rating) — keep them as
    token-only color repoints (Badge primitive swap deferred per DS-MIGRATE-06 partial; a swap
    would shift pixels). EpisodeCard line 28 `bg-emerald-500`→`bg-success`. AnimeContextMenu line 28
    `text-amber-400`→`text-warning`. AnimeKebab/AnimeQuickNav/GenreChip: grep-clean per inventory —
    re-grep and map only if a hit appears. AnimeCard.vue hex: `#1a1a1a`/`#111`→`var(--card)` (surface),
    `#ff6b6b`→`var(--destructive)`, `#fff`→`var(--foreground)`, `#999`/`#aaa`→`var(--muted-foreground)`,
    `#333`→`var(--border)`. For `#ffd700` star-gold: snap to `var(--warning)` (#ffd600, 1-unit hue
    delta, within tolerance per A3) — document the judgment in the commit message. Do NOT touch copy/
    i18n/spacing/fonts. Keep existing AnimeContextMenu.spec.ts assertions valid (color class change
    only, no structural change).
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/components/anime/AnimeCardNew.vue src/components/anime/AnimeCard.vue src/components/anime/EpisodeCard.vue src/components/anime/GenreChip.vue src/components/anime/AnimeContextMenu.vue src/components/anime/AnimeKebab.vue src/components/anime/AnimeQuickNav.vue | grep -v '^#'; grep -rnE '#[0-9a-fA-F]{3,8}\b' src/components/anime/AnimeCard.vue | grep -v '^#'; bunx vitest run src/components/anime/__tests__/AnimeContextMenu.spec.ts 2>/dev/null || bunx vitest run src/components/anime/AnimeContextMenu.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across all 7 anime/ files.
    - Hex grep returns ZERO hits on AnimeCard.vue (no documented novel-hex allowlist entries for this file).
    - AnimeContextMenu.spec.ts passes (structural — color-only change must not break it).
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All 7 anime/ SFCs token-clean; AnimeCard hex replaced; status pills on semantic tokens; spec green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-browser smoke — Home + Browse (desktop + mobile)</name>
  <what-built>
    Home.vue, Browse.vue, and the anime-card family migrated to semantic tokens + canonical
    var() references; deprecated alias usages in Home repointed; AnimeCard hex replaced.
    Full Vitest + vue-tsc + build run automatically before this gate.
  </what-built>
  <how-to-verify>
    1. Run `cd frontend/web && bunx vitest run && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit && bun run build` — all green.
    2. Deploy (`make redeploy-web`) and open the live site.
    3. Smoke surface 1 — Home, at desktop (~1440px) AND mobile (~390px): the spotlight
       carousel (stats joke card + RandomTail purple `cta-hero` — the cascade-footgun zone),
       Онгоинги + Топ rails, and anime-card status badges all render exactly as before. Confirm
       NO layout shift, NO color shift on the purple cta-hero, NO `md:hidden` regression.
    4. Smoke surface 2 — Browse/catalog, desktop AND mobile: filter sidebar, star badges,
       anime cards (status pills: ongoing/announced/released colors), pagination render identically.
    5. Confirm anime-card ongoing/announced/released/age-rating pills kept their hues (warning/
       brand-violet/success/destructive must resolve to the prior computed colors).
  </how-to-verify>
  <resume-signal>Type "approved" if Home + Browse render pixel-identically at both widths, or describe any rendered diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | — | catalog SFCs | accept | No new attack surface — pure presentational token/class migration, zero behavioral change. No new code paths, inputs, network calls, or auth changes. |
</threat_model>

<verification>
- Acceptance grep (off-palette + hex) returns zero hits across all 9 files in this plan.
- Alias grep returns zero repointable `var(--ink|--accent|--pink)` hits in Home.vue.
- `bunx vitest run` (incl. AnimeContextMenu.spec.ts) + `rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` + `bun run build` all green.
- In-browser smoke of Home + Browse at desktop + mobile shows zero rendered diff.
</verification>

<success_criteria>
1. Zero off-palette color classes + zero hardcoded hex in Home.vue, Browse.vue, and the 7 anime/ SFCs.
2. Repointable `--ink-3`/`--accent` usages in Home.vue repointed to canonical names (no `--accent` flip).
3. Status colors on anime cards come from `--success`/`--warning`/`--info`/`--brand-violet`/`--destructive`.
4. Full Vitest + vue-tsc + build green.
5. Home + Browse smoke (desktop + mobile) shows zero rendered regression.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-01-SUMMARY.md`
</output>
