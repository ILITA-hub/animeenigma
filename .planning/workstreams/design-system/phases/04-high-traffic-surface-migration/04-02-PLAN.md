---
phase: 04-high-traffic-surface-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/components/layout/Navbar.vue
  - frontend/web/src/components/layout/BrandMark.vue
  - frontend/web/src/components/layout/FeedbackButton.vue
  - frontend/web/src/views/NotFound.vue
autonomous: false
requirements: [DS-MIGRATE-01, DS-MIGRATE-02, DS-MIGRATE-03, DS-MIGRATE-04, DS-MIGRATE-06]

must_haves:
  truths:
    - "Navbar, BrandMark, and FeedbackButton contain zero off-palette classes and zero raw hex (alias-fallback hex repointed; documented novel hex allowlisted)"
    - "NotFound (404) hex replaced with tokens; the «На главную» CTA renders pixel-identically"
    - "BrandMark glow + Navbar teal gradient kept verbatim (documented allowlist) with no hue shift"
  artifacts:
    - path: "frontend/web/src/components/layout/Navbar.vue"
      provides: "Alias-fallback hex repointed to canonical; novel teal gradient documented in allowlist"
    - path: "frontend/web/src/views/NotFound.vue"
      provides: "404 CTA + text colors on tokens"
  key_links:
    - from: "frontend/web/src/components/layout/BrandMark.vue"
      to: "--brand-cyan / --brand-pink canonical tokens"
      via: "var(--token, #fallback) fallbacks repointed to canonical alias names"
      pattern: "var\\(--brand-cyan|var\\(--brand-pink"
    - from: "frontend/web/src/views/NotFound.vue"
      to: "--destructive / --foreground / --muted-foreground"
      via: "var(--token) references replacing #hex"
      pattern: "var\\(--destructive\\)|var\\(--foreground\\)|var\\(--muted-foreground\\)"
---

<objective>
Migrate the nav/layout chrome — Navbar, BrandMark, FeedbackButton — and the 404 view off
off-palette classes and hardcoded hex, repointing the `var(--token, #fallback)` alias
fallbacks to canonical names and documenting the novel hex (BrandMark glow, Navbar teal
gradient) in the allowlist per-case instead of blind-snapping.

This is a VALUE-PRESERVING migration. The nav and 404 must render pixel-identically at
desktop + mobile (smoke surface 5 = 404; nav appears on every surface). No new UX, no copy
change. Color/token/primitive edits only.

Purpose: clean the global chrome (nav on every page) + the 404 smoke surface (DS-MIGRATE-01).
Output: 4 token-clean layout/404 SFCs + a clean acceptance grep (modulo the documented
novel-hex allowlist) + a clean in-browser smoke of nav + 404.

NOTE: This plan does NOT touch `main.css` (kept out of `files_modified` so all 4 Phase-4
plans run parallel in Wave 1). Novel hex is documented in the allowlist and kept verbatim
in the SFC, NOT promoted to a `main.css` token in this phase (token promotion → Phase 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-UI-SPEC.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<locked_mapping>
Off-palette → semantic token (DS-MIGRATE-02): red→destructive · amber/yellow→warning ·
emerald/green→success · blue/sky→info · purple/violet→brand-violet · gray/slate/zinc→
muted/card/border (judge per role).

Alias → canonical (DS-MIGRATE-04, repoint USAGES only — do NOT flip --accent definition):
- --ink → --foreground · --ink-3 → --muted-foreground · --accent → --brand-cyan (per role)
- --pink → --brand-pink · --pink-soft → --brand-pink-soft
- LITERAL (no canonical equivalent — leave as-is): --ink-2, --ink-4, --accent-soft,
  --accent-line, --accent-glow

NOVEL-HEX (human-judgment-per-case — do NOT blind-snap, do NOT add a main.css token this phase):
- BrandMark `--accent-glow` shadow value — literal in main.css, no canonical alias. Keep
  via the --accent-glow var; DO NOT inline a raw hex.
- Navbar `#1a3a4a→#0e2030` teal gradient (line ~650) — novel, no token within tolerance.
  Keep VERBATIM and ADD to the documented novel-hex allowlist (record in SUMMARY + commit).
  Do NOT snap to an existing token (would shift the gradient hue). Token promotion deferred
  to Phase 5 to avoid a same-wave main.css write conflict with the players plan (04-04).
</locked_mapping>

<hex_classification>
From RESEARCH per-file hex map:
- BrandMark.vue: lines 13 `#00d4ff`/`#ff2d7c`, 25 `#08080f`, 38 `#00d4ff` are ALL
  `var(--token, #fallback)` fallbacks → repoint the var NAME: `var(--accent,#00d4ff)`→
  `var(--brand-cyan)`, `var(--pink,#ff2d7c)`→`var(--brand-pink)`. `var(--color-base,#08080f)`
  is canonical-fine. `--accent-glow` (line 14) is literal — keep the var, no raw hex.
- Navbar.vue: 12 hex lines (550–682) — many `#00d4ff`/`#ffffff` are `var(--accent/--ink,
  #fallback)` fallbacks → repoint var names (accent→brand-cyan, ink→foreground, ink-3→
  muted-foreground); line ~681 `#00ff9d` is `var(--color-success,#00ff9d)` fallback (already
  canonical-fine); line ~650 `#1a3a4a→#0e2030` is the NOVEL teal gradient (keep verbatim + allowlist).
- FeedbackButton.vue: off-palette only (line 17 `text-green-400`→`text-success`; line 34
  `bg-purple-500/20`→`bg-brand-violet/20`, `border-purple-400/50`→`border-brand-violet/50`;
  line 76 `bg-purple-500/20`/`bg-purple-500/30`→`bg-brand-violet/20`,`/30`, `text-purple-300`→
  `text-brand-violet`). No hex.
- NotFound.vue: lines 29/49 `#ff6b6b`, 58 `#ff5252` (hand-rolled `.btn` red)→`var(--destructive)`;
  36 `#fff`→`var(--foreground)`; 42 `#999`→`var(--muted-foreground)`.
</hex_classification>

<primitive_note>
NotFound `.btn` "На главную" HAS a Button `default`/`brand` analog (DS-MIGRATE-06). BUT
"zero rendered change" overrides "use the primitive" when a swap would shift pixels. Prefer
token-only color repoint of the existing `.btn` (safe, zero-diff). Swap to `@/components/ui`
Button ONLY if you verify it renders pixel-identical in-browser; if you swap, add a minimal
mount `.spec.ts` (Wave 0 note). Default to token-only repoint — keep this plan zero-diff.
</primitive_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FeedbackButton + NotFound (off-palette + hex → tokens)</name>
  <files>frontend/web/src/components/layout/FeedbackButton.vue, frontend/web/src/views/NotFound.vue</files>
  <read_first>
    - frontend/web/src/components/layout/FeedbackButton.vue (file being modified)
    - frontend/web/src/views/NotFound.vue (file being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-file line map: FeedbackButton lines 17/34/76 off-palette green+purple; NotFound hex lines 29/49 `#ff6b6b`, 58 `#ff5252`, 36 `#fff`, 42 `#999`)
  </read_first>
  <action>
    FeedbackButton.vue: `text-green-400`→`text-success` (line 17); `bg-purple-500/20`→
    `bg-brand-violet/20`, `border-purple-400/50`→`border-brand-violet/50` (line 34);
    `bg-purple-500/20`/`bg-purple-500/30`→`bg-brand-violet/20`,`/30`, `text-purple-300`→
    `text-brand-violet` (line 76). NotFound.vue `<style>`: the hand-rolled `.btn` reds
    `#ff6b6b` (lines 29/49) and `#ff5252` (line 58)→`var(--destructive)`; `#fff` (line 36)→
    `var(--foreground)`; `#999` (line 42)→`var(--muted-foreground)`. Keep the `.btn` structure
    (token-only repoint, NOT a Button-primitive swap — see primitive_note; default zero-diff).
    Do NOT touch the "На главную" copy or any i18n key. Do NOT change spacing/fonts.
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/components/layout/FeedbackButton.vue src/views/NotFound.vue | grep -v '^#'; grep -rnE '#[0-9a-fA-F]{3,8}\b' src/views/NotFound.vue | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits on FeedbackButton.vue and NotFound.vue.
    - Hex grep returns ZERO hits on NotFound.vue (no novel-hex allowlist entries for 404).
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
    - "На главную" copy and i18n keys unchanged.
  </acceptance_criteria>
  <done>FeedbackButton off-palette mapped; NotFound `.btn`/text hex on tokens; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint Navbar + BrandMark alias-fallback hex + allowlist novel teal/glow</name>
  <files>frontend/web/src/components/layout/Navbar.vue, frontend/web/src/components/layout/BrandMark.vue</files>
  <read_first>
    - frontend/web/src/components/layout/Navbar.vue (file being modified)
    - frontend/web/src/components/layout/BrandMark.vue (file being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference + deprecated-alias map)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (hex + alias map: BrandMark 13/25/38 + `--accent-glow` line 14; Navbar 12 hex lines 550–682 incl. line ~650 novel teal `#1a3a4a→#0e2030`, alias usages 550–669 `--accent`/`--ink`/`--ink-2`/`--ink-3`/`--accent-line`)
  </read_first>
  <action>
    BrandMark.vue: repoint the `var(--token, #fallback)` fallbacks by NAME — `var(--accent,
    #00d4ff)`→`var(--brand-cyan)` (lines 13/38), `var(--pink,#ff2d7c)`→`var(--brand-pink)`
    (line 13). Keep `var(--color-base,#08080f)` (line 25, canonical-fine). Keep
    `var(--accent-glow)` (line 14) — literal, no canonical alias; do NOT inline a raw hex.
    Navbar.vue: repoint alias usages (550–669) — `var(--accent)`→`var(--brand-cyan)`,
    `var(--ink)`→`var(--foreground)`, `var(--ink-3)`→`var(--muted-foreground)`; leave literal
    `--ink-2`/`--accent-line` as-is (no canonical equivalent). Repoint `var(--accent,#fallback)`/
    `var(--ink,#fallback)` fallback hex by name; the `var(--color-success,#00ff9d)` (line ~681)
    is canonical-fine. NOVEL teal gradient (line ~650 `#1a3a4a`→`#0e2030`): KEEP VERBATIM and
    record it in the documented novel-hex allowlist (SUMMARY + commit message) — do NOT snap to
    a token (would shift the gradient hue) and do NOT add a main.css token this phase (deferred
    to Phase 5 to avoid a same-wave main.css conflict with 04-04). Do NOT flip `--accent`.
    Do NOT touch nav copy/i18n/spacing.
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE 'var\(--(ink|accent|pink)\b' src/components/layout/Navbar.vue src/components/layout/BrandMark.vue | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow'; grep -rnE '#[0-9a-fA-F]{3,8}\b' src/components/layout/Navbar.vue src/components/layout/BrandMark.vue | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - The alias grep returns ZERO repointable `var(--ink|--accent|--pink)` hits on Navbar.vue and BrandMark.vue (only literal `--ink-2`/`--accent-line`/`--accent-glow` may remain).
    - The hex grep returns ONLY the documented novel-hex allowlist entries (`#1a3a4a`/`#0e2030` teal gradient on Navbar). Every remaining hex is named in the commit message + SUMMARY allowlist note. Zero undocumented hex.
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
    - `--accent` definition in main.css unchanged (not flipped — this plan does not edit main.css).
  </acceptance_criteria>
  <done>Navbar + BrandMark alias fallbacks repointed; novel teal/glow kept verbatim + allowlisted; no undocumented raw hex; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-browser smoke — nav (global) + 404 (desktop + mobile)</name>
  <what-built>
    Navbar, BrandMark, FeedbackButton, and NotFound migrated to canonical tokens; alias
    fallbacks repointed; novel teal gradient + brand glow kept verbatim + allowlisted.
    Full Vitest + vue-tsc + build run automatically before this gate.
  </what-built>
  <how-to-verify>
    1. Run `cd frontend/web && bunx vitest run && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit && bun run build` — all green.
    2. Deploy (`make redeploy-web`) and open the live site.
    3. Nav (appears on every surface), desktop (~1440px) AND mobile (~390px): the BrandMark
       logo (cyan/pink + glow), active nav indicator (cyan), the teal gradient region, and the
       FeedbackButton (violet) all render exactly as before. Confirm the brand glow and teal
       gradient hues did NOT shift.
    4. Smoke surface 5 — 404 (`/some-nonexistent-route`), desktop AND mobile: muted styling +
       the «На главную» button (its red/destructive hue + hover) render identically.
    5. Confirm no `md:hidden`/mobile-menu cascade regression on the navbar at mobile width.
  </how-to-verify>
  <resume-signal>Type "approved" if nav + 404 render pixel-identically at both widths (esp. brand glow + teal gradient unchanged), or describe any rendered diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | — | nav/layout + 404 SFCs | accept | No new attack surface — pure presentational token/class migration, zero behavioral change. No new code paths, inputs, network calls, or auth changes. |
</threat_model>

<verification>
- Acceptance grep (off-palette) returns zero hits across all 4 files.
- Hex grep returns zero hits on NotFound + FeedbackButton; only documented novel-hex (teal gradient) remains on Navbar.
- Alias grep returns zero repointable `var(--ink|--accent|--pink)` hits on Navbar + BrandMark.
- `bunx vitest run` + `rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` + `bun run build` all green.
- In-browser smoke of nav + 404 at desktop + mobile shows zero rendered diff (brand glow + teal gradient unchanged).
</verification>

<success_criteria>
1. Zero off-palette classes in Navbar, BrandMark, FeedbackButton, NotFound.
2. Zero raw hex except documented novel-hex allowlist entries; alias fallbacks repointed to canonical names.
3. BrandMark glow + Navbar teal gradient preserved verbatim (allowlisted, no hue shift).
4. `--accent` definition NOT flipped (Phase 5).
5. Full Vitest + vue-tsc + build green; nav + 404 smoke (desktop + mobile) zero rendered regression.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-02-SUMMARY.md`
</output>
