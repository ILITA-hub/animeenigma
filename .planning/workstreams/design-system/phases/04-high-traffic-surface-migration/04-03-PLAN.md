---
phase: 04-high-traffic-surface-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/views/Anime.vue
autonomous: false
requirements: [DS-MIGRATE-01, DS-MIGRATE-02, DS-MIGRATE-06]

must_haves:
  truths:
    - "views/Anime.vue contains zero off-palette color classes and zero hardcoded hex"
    - "Anime-detail status pills (schedule/ongoing/announced) + language pills + green OurEnglish button render from semantic tokens"
    - "The cyan «Смотреть» .btn-primary renders pixel-identically (token-driven from Phase 1; not re-skinned)"
  artifacts:
    - path: "frontend/web/src/views/Anime.vue"
      provides: "Token-only anime-detail view; status/language colors from --warning/--success/--brand-violet"
      contains: "text-warning"
  key_links:
    - from: "frontend/web/src/views/Anime.vue"
      to: "--warning / --success / --brand-violet semantic tokens"
      via: "semantic Tailwind utilities replacing amber/emerald/purple classes"
      pattern: "text-warning|bg-success|text-brand-violet"
---

<objective>
Migrate the anime-detail view (`views/Anime.vue`) — the heaviest single file in this phase
(13 off-palette occurrences) — off off-palette Tailwind color classes onto the canonical
semantic tokens. This view hosts the players at route `/anime/:id`; its status pills,
language pills, and the green OurEnglish button are the most user-visible status-color surface.

This is a VALUE-PRESERVING migration. The view must render pixel-identically at desktop +
mobile (smoke surface 3 = anime detail). No new UX, no copy change, no layout change.
Color/token edits only. The cyan `.btn-primary` "Смотреть" is already token-driven (Phase 1
DS-FOUND-05) and cascade-sensitive — keep it as-is, do NOT re-skin or swap it.

Purpose: prove tokens on the anime-detail screen (DS-MIGRATE-01).
Output: a token-clean `views/Anime.vue` + a clean acceptance grep + a clean in-browser smoke
of anime detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-UI-SPEC.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<locked_mapping>
Off-palette → semantic token (DS-MIGRATE-02, per-occurrence role judgment):
- red→destructive · amber/yellow→warning · emerald/green→success · blue/sky→info
- purple/violet→brand-violet · gray/slate/zinc→muted/card/border
Opacity modifiers (/20, /30): prefer the documented `-soft` token when the intent is a soft
background; keep the modifier on the base token only when no `-soft` matches the exact alpha.
Verified soft tokens exist: bg-warning-soft, bg-success-soft, bg-info-soft, bg-destructive-soft.
`bg-brand-violet` has NO -soft variant — keep the opacity modifier on the base for violet.
</locked_mapping>

<inventory>
From RESEARCH per-file map — `views/Anime.vue`, 13 off-palette occurrences across these lines:
- lines 82/628/704/709 `text-amber-400` -> `text-warning`
- line 249 `bg-amber-500/20`, `bg-amber-500/30`, `border-amber-500/30` ->
  `bg-warning-soft` / `bg-warning/30` / `border-warning/30` (judge soft vs explicit alpha per occurrence)
- line 442 `bg-emerald-500/20`, `border-emerald-500/50`, `text-emerald-400` ->
  `bg-success-soft` / `border-success/50` / `text-success`
- lines 696/805 `text-purple-400` -> `text-brand-violet`
No hex literals in Anime.vue per the grep inventory (off-palette only).
</inventory>

<primitive_note>
The cyan «Смотреть» `.btn-primary` HAS a Button `default` analog (DS-MIGRATE-06) BUT it is a
LAYERED `.btn-primary` already token-repointed in Phase 1 and is cascade-sensitive. "Zero
rendered change" overrides "use the primitive" — KEEP `.btn-primary` as-is (do NOT swap to
Button, do NOT change its classes). Status/language pills MAY have Badge analogs but a swap
risks a pixel shift — prefer token-only color repoint (safer, zero-diff); a Badge swap is
allowed ONLY if verified pixel-identical in-browser.
</primitive_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate views/Anime.vue off-palette classes to semantic tokens</name>
  <files>frontend/web/src/views/Anime.vue</files>
  <read_first>
    - frontend/web/src/views/Anime.vue (file being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-line inventory: lines 82/628/704/709 amber text; 249 amber bg/border; 442 emerald; 696/805 purple)
  </read_first>
  <action>
    Apply the locked mapping per-occurrence: the four `text-amber-400` (lines 82/628/704/709)
    -> `text-warning`. Line 249 amber pill group: `bg-amber-500/20`->`bg-warning-soft` (soft fill
    intent), `bg-amber-500/30`->`bg-warning/30` (keep alpha if not a -soft match — judge),
    `border-amber-500/30`->`border-warning/30`. Line 442 OurEnglish/language group:
    `bg-emerald-500/20`->`bg-success-soft`, `border-emerald-500/50`->`border-success/50`,
    `text-emerald-400`->`text-success`. Lines 696/805 `text-purple-400`->`text-brand-violet`.
    KEEP the cyan `.btn-primary` "Смотреть" exactly as-is (do NOT touch — see primitive_note).
    Do NOT swap status/language pills to Badge (token-only repoint, zero-diff). Do NOT touch
    copy, i18n keys, spacing, or font utilities. Do NOT alter any `.cta-*`/`.glass-card` cascade.
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/views/Anime.vue | grep -v '^#'; grep -rnE '#[0-9a-fA-F]{3,8}\b' src/views/Anime.vue | grep -v '^#'; rm -f *.tsbuildinfo && bunx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits on views/Anime.vue.
    - Hex grep returns ZERO hits on views/Anime.vue.
    - `bunx vue-tsc --noEmit` exits 0 (after deleting *.tsbuildinfo).
    - The `.btn-primary` "Смотреть" markup/classes are byte-unchanged.
  </acceptance_criteria>
  <done>views/Anime.vue token-clean; status/language pills on semantic tokens; .btn-primary untouched; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-browser smoke — anime detail (desktop + mobile)</name>
  <what-built>
    views/Anime.vue migrated to semantic tokens (amber->warning, emerald->success,
    purple->brand-violet); cyan `.btn-primary` left untouched. Full Vitest + vue-tsc + build
    run automatically before this gate.
  </what-built>
  <how-to-verify>
    1. Run `cd frontend/web && bunx vitest run && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit && bun run build` — all green.
    2. Deploy (`make redeploy-web`) and open an anime detail page (`/anime/:id`).
    3. Smoke surface 3 — Anime detail, desktop (~1440px) AND mobile (~390px): the cyan
       «Смотреть» `.btn-primary`, the status badge, the schedule pill (warning hue), the
       language pills + the green OurEnglish button (success hue), and any violet highlights
       all render exactly as before.
    4. Confirm the warning/success/violet hues resolve to the prior computed colors (no shift),
       and the `.btn-primary` cyan + hover is identical.
    5. Confirm a player loads on this route and its controls still render (player chrome is
       migrated separately in 04-04 — here just confirm Anime.vue's own chrome is unchanged).
  </how-to-verify>
  <resume-signal>Type "approved" if anime detail renders pixel-identically at both widths (status/language pills + «Смотреть» unchanged), or describe any rendered diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | — | views/Anime.vue | accept | No new attack surface — pure presentational token/class migration, zero behavioral change. No new code paths, inputs, network calls, or auth changes. |
</threat_model>

<verification>
- Acceptance grep (off-palette + hex) returns zero hits on views/Anime.vue.
- `bunx vitest run` + `rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` + `bun run build` all green.
- In-browser smoke of anime detail at desktop + mobile shows zero rendered diff.
</verification>

<success_criteria>
1. Zero off-palette color classes + zero hardcoded hex in views/Anime.vue.
2. Status/schedule/language pills + green OurEnglish button come from `--warning`/`--success`/`--brand-violet`.
3. The cyan `.btn-primary` "Смотреть" is byte-unchanged (Phase 1 token-driven, cascade-safe).
4. Full Vitest + vue-tsc + build green; anime-detail smoke (desktop + mobile) zero rendered regression.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-03-SUMMARY.md`
</output>
