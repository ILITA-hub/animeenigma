---
phase: 04-high-traffic-surface-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/components/player/KodikPlayer.vue
  - frontend/web/src/components/player/AnimeLibPlayer.vue
  - frontend/web/src/components/player/HanimePlayer.vue
  - frontend/web/src/components/player/OurEnglishPlayer.vue
  - frontend/web/src/components/player/RawPlayer.vue
  - frontend/web/src/components/player/OtherSubsPanel.vue
  - frontend/web/src/components/player/ResumePill.vue
  - frontend/web/src/components/player/SubtitleSettingsMenu.vue
  - frontend/web/src/components/player/SubtitleOverlay.vue
autonomous: false
requirements: [DS-MIGRATE-01, DS-MIGRATE-02, DS-MIGRATE-03, DS-MIGRATE-06]

must_haves:
  truths:
    - "All 5 players + OtherSubsPanel/ResumePill/SubtitleSettingsMenu contain zero off-palette color classes"
    - "Player hex is either token-replaced or documented in the novel-hex allowlist (per-player --player-accent hues + SubtitleOverlay render defaults kept verbatim, no hue shift)"
    - "One player surface loads with styled controls rendering pixel-identically"
  artifacts:
    - path: "frontend/web/src/components/player/KodikPlayer.vue"
      provides: "19 off-palette occurrences mapped to semantic tokens"
    - path: "frontend/web/src/components/player/SubtitleOverlay.vue"
      provides: "Novel subtitle-render hex kept verbatim + allowlisted"
  key_links:
    - from: "frontend/web/src/components/player/KodikPlayer.vue"
      to: "--warning / --success / --info semantic tokens"
      via: "semantic Tailwind utilities replacing yellow/green/blue classes"
      pattern: "bg-warning|text-success|bg-info"
    - from: "frontend/web/src/components/player/OtherSubsPanel.vue"
      to: "--destructive / --warning semantic tokens"
      via: "semantic utilities replacing red/amber classes"
      pattern: "text-destructive|text-warning"
---

<objective>
Migrate the 5 player components + their chrome helpers (OtherSubsPanel, ResumePill,
SubtitleSettingsMenu, SubtitleOverlay) off off-palette Tailwind color classes onto the
canonical semantic tokens, and handle each player's hex per-case: snap canonical-equivalent
values to tokens, but KEEP the deliberate novel hues (per-player `--player-accent` orange/
pink/cyan, SubtitleOverlay user-overridable render defaults) verbatim and record them in the
documented novel-hex allowlist — NEVER blind-snap a brand/render hue (Pitfall 2).

This is a VALUE-PRESERVING migration. A player surface must render pixel-identically at
desktop + mobile (smoke surface 4 = a player). No new UX, no copy change. Color/token edits only.

Purpose: clean the highest-density off-palette files (KodikPlayer alone = 19 occurrences) and
prove tokens on a real player surface (DS-MIGRATE-01).
Output: 9 token-clean player SFCs + a clean acceptance grep (modulo the documented novel-hex
allowlist) + a clean in-browser smoke of a player.

NOTE: This plan does NOT touch `main.css` — novel `--player-accent` hex is kept verbatim in
the SFC and allowlisted (token promotion deferred to Phase 5 to avoid a same-wave main.css
conflict with 04-02). The existing per-SFC specs (OtherSubsPanel, OurEnglishPlayer,
SubtitleOverlay, SubtitleSettingsMenu) MUST stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-UI-SPEC.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<locked_mapping>
Off-palette → semantic token (DS-MIGRATE-02, per-occurrence role judgment):
- red->destructive · amber/yellow->warning · emerald/green->success · blue/sky->info
- purple/violet->brand-violet · gray/slate/zinc->muted/card/border
Opacity modifiers (/20, /30, /70, /80, /90): prefer the documented `-soft` token for soft
backgrounds; keep the modifier on the base token when no `-soft` matches the alpha (e.g.
`text-amber-400/70`->`text-warning/70`). Verified soft tokens: bg-warning-soft, bg-success-soft,
bg-info-soft, bg-destructive-soft. `bg-zinc-900/95`->`bg-card/95` or `bg-popover/95` (judge
the surface role — elevated menu => popover).

NOVEL-HEX allowlist (human-judgment-per-case — do NOT blind-snap, do NOT add a main.css token):
- Per-player `--player-accent`: AnimeLibPlayer `#f97316` orange (line 869), HanimePlayer
  `#ec4899` pink (line 479), KodikPlayer `#06b6d4` cyan (line 1077) — deliberate per-player
  identity hues. `#f97316` has NO token. `#ec4899`/`#06b6d4` are NEAR but NOT equal to
  brand-pink/brand-cyan — snapping shifts the hue. KEEP all three verbatim + allowlist them.
- SubtitleOverlay `#ffffff` (line 202, default JP-subtitle text) + `#ffcccc` (line 336,
  subtitle render) — user-overridable render defaults, legitimately novel. KEEP verbatim + allowlist.
</locked_mapping>

<inventory>
From RESEARCH per-file map:
OFF-PALETTE:
- KodikPlayer.vue (19): 37 `bg-yellow-500/90`->`bg-warning/90`; 132 `bg-green-500/20`/
  `border-green-500/50`/`text-green-400`->success; 145/169 `bg-blue-500/20`/`border-blue-500/50`/
  `text-blue-400`->info; 169 green group->success; 171 `ring-amber-500/30`->`ring-warning/30`;
  180/208 `bg-amber-500/20`,`bg-amber-500/30`,`text-amber-400`->warning; 194 `bg-blue-500`->
  `bg-info`, `bg-green-500`->`bg-success`.
- OtherSubsPanel.vue (2): 13 `text-red-400`->`text-destructive`; 115 `text-amber-400/80`->`text-warning/80`.
- ResumePill.vue (2): 17 `text-emerald-400/70`->`text-success/70`; 52 `text-amber-400/70`->`text-warning/70`.
- AnimeLibPlayer.vue (1 off-palette): 283 `text-red-400/70`->`text-destructive/70`.
- OurEnglishPlayer.vue (1): 52 `text-amber-400/80`->`text-warning/80`.
- SubtitleSettingsMenu.vue (1): 24 `bg-zinc-900/95`->`bg-popover/95` (elevated menu surface).
- RawPlayer.vue: grep-clean of off-palette (re-grep; map only if a hit appears).
HEX:
- AnimeLibPlayer.vue 869 `#f97316` (player-accent orange) -> NOVEL, allowlist verbatim.
- HanimePlayer.vue 479 `#ec4899` (player-accent pink) -> NOVEL, allowlist verbatim.
- KodikPlayer.vue 1077 `#06b6d4` (player-accent cyan) -> NOVEL, allowlist verbatim.
- SubtitleOverlay.vue 202 `#ffffff` + 336 `#ffcccc` -> NOVEL render defaults, allowlist verbatim.
</inventory>

<primitive_note>
Player chrome control buttons have NO bespoke `@/components/ui` analog (DS-MIGRATE-06) — keep
the structure, token-only colors. Do NOT swap player controls to Button. The `--player-accent`
hues are deliberate per-player identity — keep verbatim per the novel-hex rule.
</primitive_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate KodikPlayer + chrome helpers off-palette to semantic tokens</name>
  <files>frontend/web/src/components/player/KodikPlayer.vue, frontend/web/src/components/player/OtherSubsPanel.vue, frontend/web/src/components/player/ResumePill.vue, frontend/web/src/components/player/SubtitleSettingsMenu.vue, frontend/web/src/components/player/OurEnglishPlayer.vue</files>
  <read_first>
    - All 5 SFCs listed in files (files being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-line inventory in this plan's <inventory> block; KodikPlayer lines 37/132/145/169/171/180/194/208)
  </read_first>
  <action>
    KodikPlayer.vue: map all 19 off-palette occurrences per <inventory> — yellow/amber->warning
    (keep alpha modifier or use -soft per role), green->success, blue->info, including the solid
    `bg-blue-500`->`bg-info` and `bg-green-500`->`bg-success` (line 194) and `ring-amber-500/30`->
    `ring-warning/30` (line 171). KodikPlayer line 1077 `#06b6d4` player-accent: KEEP verbatim +
    allowlist (NOVEL, near-but-not-equal to brand-cyan — Pitfall 2). OtherSubsPanel.vue: line 13
    `text-red-400`->`text-destructive`, line 115 `text-amber-400/80`->`text-warning/80`. ResumePill.vue:
    line 17 `text-emerald-400/70`->`text-success/70`, line 52 `text-amber-400/70`->`text-warning/70`.
    SubtitleSettingsMenu.vue: line 24 `bg-zinc-900/95`->`bg-popover/95` (elevated menu surface).
    OurEnglishPlayer.vue: line 52 `text-amber-400/80`->`text-warning/80`. Do NOT touch copy/i18n/
    spacing/fonts. Keep the existing OtherSubsPanel/OurEnglishPlayer/SubtitleSettingsMenu specs green
    (color-class-only change must not break structural assertions).
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/components/player/KodikPlayer.vue src/components/player/OtherSubsPanel.vue src/components/player/ResumePill.vue src/components/player/SubtitleSettingsMenu.vue src/components/player/OurEnglishPlayer.vue | grep -v '^#'; bunx vitest run src/components/player 2>/dev/null | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across all 5 files in this task.
    - The only hex remaining in KodikPlayer.vue is the allowlisted `#06b6d4` player-accent (named in commit + SUMMARY).
    - Existing player specs (OtherSubsPanel, OurEnglishPlayer, SubtitleSettingsMenu) pass.
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>KodikPlayer + 4 chrome helpers token-clean of off-palette; Kodik player-accent allowlisted; specs green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AnimeLib/Hanime/Raw players + SubtitleOverlay; allowlist novel hex</name>
  <files>frontend/web/src/components/player/AnimeLibPlayer.vue, frontend/web/src/components/player/HanimePlayer.vue, frontend/web/src/components/player/RawPlayer.vue, frontend/web/src/components/player/SubtitleOverlay.vue</files>
  <read_first>
    - All 4 SFCs listed in files (files being modified)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (canonical token reference)
    - .planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-RESEARCH.md (per-line inventory: AnimeLib 283 off-palette + 869 hex; Hanime 479 hex; SubtitleOverlay 202/336 hex; novel-hex allowlist candidates)
  </read_first>
  <action>
    AnimeLibPlayer.vue: line 283 `text-red-400/70`->`text-destructive/70`; line 869 `#f97316`
    player-accent orange: KEEP verbatim + allowlist (NOVEL, no token within tolerance).
    HanimePlayer.vue: line 479 `#ec4899` player-accent pink: KEEP verbatim + allowlist (NOVEL,
    near-but-not-equal to brand-pink — snapping shifts hue, Pitfall 2). RawPlayer.vue: grep-clean
    per inventory — re-grep, map only if a hit appears. SubtitleOverlay.vue: lines 202 `#ffffff`
    (default JP-subtitle text) + 336 `#ffcccc` (subtitle render): KEEP verbatim + allowlist
    (legitimately novel user-overridable render defaults — CLAUDE.md flags these; do NOT snap).
    Keep the existing SubtitleOverlay spec green. Do NOT touch copy/i18n/spacing/fonts.
    Record every kept-verbatim hex in the SUMMARY novel-hex allowlist + commit message.
  </action>
  <verify>
    <automated>cd frontend/web && grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/components/player/AnimeLibPlayer.vue src/components/player/HanimePlayer.vue src/components/player/RawPlayer.vue src/components/player/SubtitleOverlay.vue | grep -v '^#'; grep -rnE '#[0-9a-fA-F]{3,8}\b' src/components/player/AnimeLibPlayer.vue src/components/player/HanimePlayer.vue src/components/player/RawPlayer.vue src/components/player/SubtitleOverlay.vue | grep -v '^#'; bunx vitest run src/components/player/SubtitleOverlay 2>/dev/null | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across all 4 files.
    - Hex grep returns ONLY the documented novel-hex allowlist entries: AnimeLib `#f97316`, Hanime `#ec4899`, SubtitleOverlay `#ffffff`/`#ffcccc`. Zero undocumented hex. Each is named in commit + SUMMARY.
    - SubtitleOverlay spec passes.
    - `cd frontend/web && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AnimeLib/Hanime/Raw + SubtitleOverlay token-clean of off-palette; all novel player/render hex allowlisted verbatim; spec green; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-browser smoke — a player surface (desktop + mobile)</name>
  <what-built>
    All 5 players + chrome helpers (OtherSubsPanel, ResumePill, SubtitleSettingsMenu,
    SubtitleOverlay) migrated to semantic tokens; per-player `--player-accent` hues + subtitle
    render defaults kept verbatim + allowlisted. Full Vitest + vue-tsc + build + e2e player spec
    run automatically before this gate.
  </what-built>
  <how-to-verify>
    1. Run `cd frontend/web && bunx vitest run && rm -f *.tsbuildinfo && bunx vue-tsc --noEmit && bun run build` — all green.
    2. Run the e2e player spec: `cd frontend/web && bunx playwright test player` — passes.
    3. Deploy (`make redeploy-web`) and open an anime with a player.
    4. Smoke surface 4 — a player surface, desktop (~1440px) AND mobile (~390px): the player
       loads, styled controls render, the player-accent color (e.g. Kodik cyan / AnimeLib orange /
       Hanime pink) is UNCHANGED, status banners (warning/success/info — e.g. Kodik's quality/
       translation badges) render with the prior hues, ResumePill (success/warning) + ReportButton
       (destructive) look identical.
    5. If JP subtitles are available (Raw/OurEnglish), confirm SubtitleOverlay text color
       (`#ffffff`/`#ffcccc` defaults) is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if a player renders pixel-identically at both widths (player-accent + status banners + subtitle color unchanged), or describe any rendered diff.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | — | 5 players + chrome helper SFCs | accept | No new attack surface — pure presentational token/class migration, zero behavioral change. No new code paths, inputs, network calls, or auth changes. |
</threat_model>

<verification>
- Acceptance grep (off-palette) returns zero hits across all 9 player files.
- Hex grep returns only the documented novel-hex allowlist (player-accent ×3 + subtitle render ×2).
- Existing player specs (OtherSubsPanel, OurEnglishPlayer, SubtitleOverlay, SubtitleSettingsMenu) stay green.
- `bunx vitest run` + `rm -f *.tsbuildinfo && bunx vue-tsc --noEmit` + `bun run build` + `bunx playwright test player` all green.
- In-browser smoke of a player at desktop + mobile shows zero rendered diff (player-accent + status + subtitle color unchanged).
</verification>

<success_criteria>
1. Zero off-palette color classes across all 5 players + OtherSubsPanel/ResumePill/SubtitleSettingsMenu/SubtitleOverlay.
2. Player hex is token-replaced OR documented in the novel-hex allowlist (per-player accents + subtitle render defaults verbatim, no hue shift).
3. Player chrome controls kept structurally intact (no Button swap — no analog).
4. Full Vitest + vue-tsc + build + player e2e green; player smoke (desktop + mobile) zero rendered regression.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-04-SUMMARY.md`
</output>
