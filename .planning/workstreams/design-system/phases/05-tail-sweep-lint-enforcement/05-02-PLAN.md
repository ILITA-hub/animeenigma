---
phase: 05-tail-sweep-lint-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/views/admin/AdminCollectionEdit.vue
  - frontend/web/src/views/admin/AdminCollections.vue
  - frontend/web/src/views/admin/AdminRecs.vue
  - frontend/web/src/views/admin/RawLibrary.vue
  - frontend/web/src/views/Game.vue
  - frontend/web/src/views/Profile.vue
  - frontend/web/src/views/StatusPage.vue
  - frontend/web/src/views/Themes.vue
autonomous: true
requirements: [DS-MIGRATE-02, DS-MIGRATE-03, DS-MIGRATE-04]

must_haves:
  truths:
    - "Admin views (Collections, Recs, RawLibrary) render identically; status colors come from semantic tokens"
    - "Profile, Game, StatusPage, Themes views render identically (stat numbers, status pills, banners)"
    - "No admin/view file references a deprecated var(--ink|--accent|--pink) brand alias"
  artifacts:
    - path: "frontend/web/src/views/Profile.vue"
      provides: "Token-clean Profile (22 off-palette hits cleared: stat numbers, list-status pills)"
    - path: "frontend/web/src/views/StatusPage.vue"
      provides: "Token-clean status page (13 off-palette hits cleared)"
  key_links:
    - from: "admin + standalone view SFCs"
      to: "canonical semantic tokens (--success/--warning/--info/--destructive/--brand-violet/--muted-foreground)"
      via: "off-palette class → token-class swap"
      pattern: "(text|bg|border)-(success|warning|info|destructive|brand-violet|muted)"
---

<objective>
Migrate the 4 admin views + 4 standalone user-facing views (Profile, Game, StatusPage, Themes) off
off-palette Tailwind color classes and deprecated brand-alias `var()` usages onto canonical semantic
tokens. Value-preserving color/token-only migration — zero behavioral change.

Purpose: These are the highest-hit-count tail files (Profile 22, StatusPage 13, AdminRecs 12,
RawLibrary 10). Clearing them removes the bulk of remaining off-palette occurrences.
Output: 8 token-clean view SFCs; any genuinely-novel hex recorded for the Wave-3 allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-CONTEXT.md
@frontend/web/src/styles/DESIGN-SYSTEM.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-01-SUMMARY.md

<mapping_table>
The LOCKED DS-MIGRATE-02 off-palette → semantic-token mapping (Phase 4, verbatim):
- red → destructive · amber/yellow → warning · emerald/green → success · blue/sky → info ·
  purple/violet → brand-violet · gray/slate/zinc → muted/card/border/popover (flat surface→card,
  elevated menu→popover, hairline→border, secondary text→muted-foreground).
Opacity modifiers (`/10../90`) KEPT on the base semantic token when no `-soft` matches the alpha.
DS-MIGRATE-04 alias repoints (repoint NAME only, never flip the definition):
  var(--ink)→var(--foreground) · var(--ink-3)→var(--muted-foreground) ·
  var(--accent)→var(--brand-cyan) · var(--pink)→var(--brand-pink).
LEAVE literal aliases untouched: --ink-2, --ink-4, --accent-soft, --accent-line, --accent-glow, --pink-soft.
LEAVE cyan/pink/orange/rose brand/provider classes as-is — they are NOT off-palette.
  (AdminRecs has `bg-orange-500/20 text-orange-300` status pills — orange is a non-brand status hue here;
   migrate it to `warning` ONLY if it reads as a status color, else leave it. Use semantic judgment and
   document the call in the commit message. BrowseSidebar's `text-orange-500` ring is NOT in this plan.)
</mapping_table>

<staging_discipline>
Stage ONLY this plan's 8 files by EXPLICIT path. NEVER `git add -A`. The repo has pre-existing unrelated
uncommitted changes (analytics workstream + scraper Go) that MUST NOT be swept into any commit.
One atomic commit per task.
</staging_discipline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 4 admin views to semantic tokens</name>
  <files>frontend/web/src/views/admin/AdminCollectionEdit.vue, frontend/web/src/views/admin/AdminCollections.vue, frontend/web/src/views/admin/AdminRecs.vue, frontend/web/src/views/admin/RawLibrary.vue</files>
  <read_first>
  Per file run:
    cd frontend/web
    grep -nE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' <file>
    grep -nE 'var\(--(ink|accent|pink)\b' <file> | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow'
  Read @mapping_table. AdminRecs line ~258 has `bg-orange-500/20 text-orange-300` (status pill — judge:
  warning vs leave-as-provider-orange) and `text-orange-300`; treat as status → `warning` unless it
  encodes a provider identity.
  </read_first>
  <action>
  Swap every off-palette class to its semantic-token equivalent per the LOCKED @mapping_table, keeping
  `/NN` opacity on the base token. Repoint any deprecated `var(--ink|--accent|--pink)` to canonical names.
  Apply semantic judgment to AdminRecs orange status pills (→ warning if status-meaning) and document the
  judgment in the commit message. No template/logic changes. Commit staging ONLY these 4 files.
  Commit: `refactor(05): migrate admin views to semantic tokens`.
  </action>
  <verify>
    <automated>cd frontend/web && test $(grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/views/admin/AdminCollectionEdit.vue src/views/admin/AdminCollections.vue src/views/admin/AdminRecs.vue src/views/admin/RawLibrary.vue | grep -v '\.spec\.' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across the 4 admin views.
    - Repointable-alias grep returns ZERO hits across the 4 files.
    - `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean.
  </acceptance_criteria>
  <done>All 4 admin views off-palette-clean + alias-clean; one atomic commit staging only these files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Profile, Game, StatusPage, Themes views to semantic tokens</name>
  <files>frontend/web/src/views/Game.vue, frontend/web/src/views/Profile.vue, frontend/web/src/views/StatusPage.vue, frontend/web/src/views/Themes.vue</files>
  <read_first>
  Per file run the off-palette + alias greps (see Task 1 read_first). Profile is the heaviest (≈22 hits):
  stat numbers (`text-yellow-400`/`text-green-400`/`text-blue-400` → warning/success/info), list-status
  hover pills (`hover:bg-red-500/20 text-red-400` → destructive), the score badge `text-yellow-400`
  (→ warning), MAL/Shiki sync `text-green-400` (→ success). Keep `border-cyan-500/50`/`focus:ring-cyan-500`
  (brand). StatusPage (13) is service up/down status colors (green→success, red→destructive, amber→warning).
  </read_first>
  <action>
  Swap every off-palette class to its semantic-token equivalent per the LOCKED @mapping_table, preserving
  `/NN` opacity on the base token. Status numbers/pills map by meaning: success/warning/info/destructive.
  Repoint any deprecated `var(--ink|--accent|--pink)` to canonical names. Leave cyan/pink brand classes.
  No logic changes. Commit staging ONLY these 4 files.
  Commit: `refactor(05): migrate Profile/Game/StatusPage/Themes to semantic tokens`.
  </action>
  <verify>
    <automated>cd frontend/web && test $(grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/views/Game.vue src/views/Profile.vue src/views/StatusPage.vue src/views/Themes.vue | grep -v '\.spec\.' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across Profile/Game/StatusPage/Themes.
    - Repointable-alias grep returns ZERO hits across the 4 files.
    - `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean; `bunx vitest run` green (excluding documented pre-existing failures).
  </acceptance_criteria>
  <done>All 4 standalone views off-palette-clean + alias-clean; one atomic commit staging only these files.</done>
</task>

</tasks>

<verification>
- Off-palette grep across all 8 files → ZERO hits.
- Repointable-alias grep across all 8 files → ZERO hits.
- `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean.
- `git status --porcelain` shows ONLY this plan's 8 files committed (no analytics/scraper files swept in).
</verification>

<success_criteria>
All 8 admin + standalone view SFCs contain zero off-palette Tailwind color classes and zero repointable
brand-alias `var()` usages, with render unchanged (status colors now resolve from semantic tokens). Commits
are atomic per task, staging only this plan's files.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-02-SUMMARY.md`.
Include any kept novel hex as `path:hex:reason` for the Wave-3 allowlist seed.
</output>
