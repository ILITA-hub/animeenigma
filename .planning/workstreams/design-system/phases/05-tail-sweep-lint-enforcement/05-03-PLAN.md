---
phase: 05-tail-sweep-lint-enforcement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/App.vue
  - frontend/web/src/components/LastUpdates.vue
  - frontend/web/src/components/ui/GenreFilterPopup.vue
  - frontend/web/src/components/ui/Select.vue
  - frontend/web/src/components/ui/Toaster.vue
  - frontend/web/src/components/themes/RatingStars.vue
  - frontend/web/src/components/themes/ThemeCard.vue
  - frontend/web/src/components/browse/BrowseSidebar.vue
  - frontend/web/src/components/profile/ActiveSessionsCard.vue
  - frontend/web/src/components/watch-together/RoomSidebar.vue
  - frontend/web/src/components/ActivityFeed.vue
autonomous: true
requirements: [DS-MIGRATE-02, DS-MIGRATE-03, DS-MIGRATE-04]

must_haves:
  truths:
    - "ui/ composites (GenreFilterPopup, Select, Toaster) render identically; popovers use --popover surface"
    - "Theme cards/stars, browse sidebar, active-sessions card, WT room sidebar render identically"
    - "LastUpdates changelog colors come from semantic tokens; no repointable brand alias remains in these files"
    - "ActivityFeed.vue's 2 var(--accent) + var(--ink|--ink-3) usages are repointed to canonical tokens, committed in isolation from the pending analytics changes (stash-isolation), leaving zero repointable brand-alias usages anywhere in src/**/*.vue"
  artifacts:
    - path: "frontend/web/src/components/ui/Toaster.vue"
      provides: "Token-clean toast variants (success/destructive from semantic tokens)"
    - path: "frontend/web/src/components/ui/Select.vue"
      provides: "Token-clean select popper (slate-900 elevated surface → popover)"
    - path: "frontend/web/src/components/ActivityFeed.vue"
      provides: "Color lines repointed (--accent→--brand-cyan, --ink/--ink-3→canonical) via stash-isolated commit; zero remaining brand var(--accent) in the file"
      contains: "var(--brand-cyan)"
  key_links:
    - from: "ui composites + theme/browse/profile/WT/ActivityFeed SFCs"
      to: "canonical semantic tokens (--popover/--success/--warning/--info/--destructive/--brand-violet/--brand-cyan/--foreground/--muted-foreground)"
      via: "off-palette class → token-class swap; var(--alias) → var(--canonical) repoint"
      pattern: "var\\(--brand-cyan\\)|bg-(popover|success|destructive|warning|info|brand-violet|muted)"
---

<objective>
Migrate the remaining tail SFCs — the 3 ui/ composites (GenreFilterPopup, Select, Toaster), themes
(RatingStars, ThemeCard), browse sidebar, profile active-sessions card, watch-together room sidebar,
plus App.vue and LastUpdates.vue — off off-palette Tailwind color classes and deprecated brand-alias
`var()` usages onto canonical semantic tokens. THEN repoint ActivityFeed.vue's color lines in isolation
from its pending analytics changes (Task 3, stash-isolation recipe). Value-preserving, zero behavioral
change.

Purpose: Closes out EVERY remaining off-palette/alias usage in `src/**/*.vue`, INCLUDING ActivityFeed.vue.
After this plan + 05-01 + 05-02 land, the tree is off-palette-clean and alias-clean with NO survivors —
which the Wave-2 `--accent` flip-gate and Wave-3 lint gate both require to self-complete in autonomous mode.
Output: 11 token-clean SFCs (10 standard + ActivityFeed via stash-isolation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-CONTEXT.md
@frontend/web/src/styles/DESIGN-SYSTEM.md
@.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/04-01-SUMMARY.md

<mapping_table>
The LOCKED DS-MIGRATE-02 mapping (Phase 4, verbatim):
- red→destructive · amber/yellow→warning · emerald/green→success · blue/sky→info ·
  purple/violet→brand-violet · gray/slate/zinc→muted/card/border/popover.
  ELEVATED menu/popover surfaces (`bg-slate-900/95`, `bg-zinc-900/95`) → `bg-popover/95` (Phase-4 precedent).
Opacity modifiers KEPT on base token when no `-soft` matches the alpha.
DS-MIGRATE-04 alias repoints (NAME only, never flip definition):
  var(--ink)→var(--foreground) · var(--ink-3)→var(--muted-foreground) ·
  var(--accent)→var(--brand-cyan) · var(--pink)→var(--brand-pink).
LEAVE literal aliases (--ink-2/-4, --accent-soft/-line/-glow, --pink-soft) + cyan/pink brand classes.
LastUpdates has changelog hex (`#fbbf24`,`#9ca3af`,`#4c1d95`,`#38bdf8`,`#34d399`,`#0e7490`): map each to
the nearest semantic token (#fbbf24→warning, #9ca3af→muted-foreground, #38bdf8→info, #34d399→success,
#4c1d95→brand-violet) where within tolerance; record any genuinely-novel kept hex for the allowlist.
BrowseSidebar has `text-orange-500 focus:ring-orange-500` (accent ring) — treat as warning ONLY if it
reads as a status/accent token; if it is a deliberate non-token decorative hue, leave it and document.
</mapping_table>

<staging_discipline>
Stage ONLY this plan's files by EXPLICIT path. NEVER `git add -A`. Pre-existing unrelated uncommitted
changes (analytics workstream + scraper Go) MUST NOT be swept in. One atomic commit per task. ActivityFeed.vue
carries pre-existing UNCOMMITTED analytics-workstream changes — Task 3 uses stash-isolation (see its
<action>) so ONLY the color repoint is committed, never the analytics hunks.
</staging_discipline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 3 ui/ composites + App.vue to semantic tokens</name>
  <files>frontend/web/src/App.vue, frontend/web/src/components/ui/GenreFilterPopup.vue, frontend/web/src/components/ui/Select.vue, frontend/web/src/components/ui/Toaster.vue</files>
  <read_first>
  Per file run the off-palette + alias greps (see other plans' read_first). App.vue: `bg-purple-500
  hover:bg-purple-600` (PWA-update button → brand-violet) + `bg-red-500/90` (error banner → destructive).
  Select.vue + GenreFilterPopup.vue: `bg-slate-900/95` elevated popper → `bg-popover/95`. Toaster.vue:
  `bg-red-500/90`→destructive, `bg-emerald-500/90`→success.
  </read_first>
  <action>
  Swap every off-palette class to its semantic-token equivalent per the LOCKED @mapping_table, preserving
  `/NN` opacity on the base token; elevated poppers (`slate-900/95`) → `popover/95`. Repoint any deprecated
  `var(--ink|--accent|--pink)`. Leave cyan/pink brand classes and cascade rules. No logic changes. Commit
  staging ONLY these 4 files. Commit: `refactor(05): migrate ui composites + App to semantic tokens`.
  </action>
  <verify>
    <automated>cd frontend/web && test $(grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/App.vue src/components/ui/GenreFilterPopup.vue src/components/ui/Select.vue src/components/ui/Toaster.vue | grep -v '\.spec\.' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across the 4 files.
    - Repointable-alias grep returns ZERO hits across the 4 files.
    - `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean; `bunx vitest run src/components/ui/` green.
  </acceptance_criteria>
  <done>App.vue + 3 ui composites off-palette-clean + alias-clean; one atomic commit staging only these files.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate themes, browse, profile, WT, LastUpdates to semantic tokens</name>
  <files>frontend/web/src/components/LastUpdates.vue, frontend/web/src/components/themes/RatingStars.vue, frontend/web/src/components/themes/ThemeCard.vue, frontend/web/src/components/browse/BrowseSidebar.vue, frontend/web/src/components/profile/ActiveSessionsCard.vue, frontend/web/src/components/watch-together/RoomSidebar.vue</files>
  <read_first>
  Per file run the off-palette + alias + hex greps. LastUpdates carries the changelog-category hex set
  (see @mapping_table mapping). RatingStars/ThemeCard: rating-star + theme-status hues. BrowseSidebar:
  the `text-orange-500 focus:ring-orange-500` accent ring (semantic judgment). RoomSidebar: WT member
  presence/status colors. ActiveSessionsCard: session-status pills.
  </read_first>
  <action>
  Swap every off-palette class + map the LastUpdates changelog hex to nearest semantic tokens per the
  LOCKED @mapping_table, preserving opacity on base tokens. Apply semantic judgment to BrowseSidebar's
  orange ring and document the call. Repoint any deprecated `var(--ink|--accent|--pink)`. Record any
  genuinely-novel kept hex for the allowlist. Leave cyan/pink brand classes. No logic changes. Commit
  staging ONLY these 6 files. Commit: `refactor(05): migrate themes/browse/profile/WT/changelog to semantic tokens`.
  </action>
  <verify>
    <automated>cd frontend/web && test $(grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src/components/LastUpdates.vue src/components/themes/RatingStars.vue src/components/themes/ThemeCard.vue src/components/browse/BrowseSidebar.vue src/components/profile/ActiveSessionsCard.vue src/components/watch-together/RoomSidebar.vue | grep -v '\.spec\.' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Off-palette grep returns ZERO hits across the 6 files.
    - Repointable-alias grep returns ZERO hits across the 6 files.
    - Novel kept hex recorded in SUMMARY for the allowlist.
    - `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean.
  </acceptance_criteria>
  <done>All 6 SFCs off-palette-clean + alias-clean; one atomic commit staging only these files.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint ActivityFeed.vue color lines via stash-isolation (no analytics sweep)</name>
  <files>frontend/web/src/components/ActivityFeed.vue</files>
  <read_first>
  cd /data/animeenigma
  # Confirm ActivityFeed has pre-existing UNCOMMITTED analytics-workstream changes:
  git status --porcelain frontend/web/src/components/ActivityFeed.vue   # expect ' M'
  # The repointable brand-alias usages in <style> (lines ~343/352 are var(--accent), plus var(--ink|--ink-3)):
  grep -nE 'var\(--(ink|accent|pink)\b' frontend/web/src/components/ActivityFeed.vue | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow|pink-soft'
  # The 2 var(--accent) lines (343/352) are PRE-EXISTING code (present at HEAD), NOT analytics edits.
  </read_first>
  <action>
  ActivityFeed.vue carries pre-existing UNCOMMITTED analytics-workstream changes (staging-discipline
  forbids sweeping them in), but its `var(--accent)`/`var(--ink|--ink-3)` color lines are pre-existing code
  at HEAD. Repoint them in ISOLATION from the analytics hunks using this EXACT stash-isolation recipe —
  it is executor-safe because the color lines are disjoint from the analytics hunks:

  1. `git stash push -- frontend/web/src/components/ActivityFeed.vue`
     (stashes ONLY the uncommitted analytics changes; the file reverts to HEAD, where lines 343/352
     `var(--accent)` are pre-existing code).
  2. Edit ActivityFeed.vue: repoint the 2 `var(--accent)` → `var(--brand-cyan)`, and every
     repointable `var(--ink)` → `var(--foreground)` and `var(--ink-3)` → `var(--muted-foreground)` per the
     same DS mapping (@mapping_table) used elsewhere. LEAVE literal `var(--ink-2)` / `var(--ink-4)` /
     `--accent-soft|-line|-glow` untouched. No logic changes.
  3. `git add frontend/web/src/components/ActivityFeed.vue && git commit -m "refactor(05): repoint ActivityFeed color lines to canonical tokens [DS-MIGRATE-04]"`
     (commits ONLY the color repoint — clean, no analytics changes).
  4. `git stash pop` (re-applies the analytics changes on top; they don't touch 343/352 so it merges
     cleanly). If `git stash pop` CONFLICTS, STOP and surface — but it will not, since the edited lines are
     disjoint from the analytics hunks.

  Record in the SUMMARY: the committed diff touched ONLY ActivityFeed's color lines, and `git stash list`
  is back to its prior state after the pop.
  </action>
  <verify>
    <automated>cd /data/animeenigma && test $(grep -rnE 'var\(--accent\b' frontend/web/src/components/ActivityFeed.vue | grep -vE 'accent-soft|accent-line|accent-glow' | wc -l) -eq 0 && test $(grep -rnE 'var\(--(ink|accent|pink)\b' frontend/web/src/components/ActivityFeed.vue | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow|pink-soft' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'var\(--accent\b' frontend/web/src/components/ActivityFeed.vue` (excluding accent-soft/line/glow) returns ZERO.
    - No repointable `var(--ink|--ink-3|--accent|--pink)` remains in ActivityFeed.vue (literal --ink-2/-4 + -soft/-line/-glow may stay).
    - The committed diff touches ONLY ActivityFeed's color lines — NO analytics hunks were committed.
    - `git stash list` is back to its prior state (analytics changes re-applied, working tree shows ` M` ActivityFeed again).
  </acceptance_criteria>
  <done>ActivityFeed.vue's repointable color lines committed in isolation; zero brand var(--accent) remains in the file; analytics changes preserved uncommitted in the working tree.</done>
</task>

</tasks>

<verification>
- Off-palette grep across all migrated files → ZERO hits.
- Repointable-alias grep across all migrated files (INCLUDING ActivityFeed.vue) → ZERO hits.
- Repo-wide off-palette grep (`grep -rnE '(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|...|900)' src --include='*.vue' | grep -v '\.spec\.'`) → ZERO hits AFTER 05-01 + 05-02 + 05-03 all land (validate at end).
- Repo-wide repointable-alias grep (`grep -rnE 'var\(--(ink|accent|pink)\b' src --include='*.vue' | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow|pink-soft'`) → ZERO hits with NO survivors (ActivityFeed now handled in Task 3).
- `git status --porcelain` shows ONLY this plan's committed files; ActivityFeed.vue still shows ` M` (its uncommitted analytics changes preserved); no analytics/scraper files swept into any commit.
</verification>

<success_criteria>
All SFCs in this plan are off-palette-clean and alias-clean (render unchanged), INCLUDING ActivityFeed.vue
whose color lines were repointed in isolation from its pending analytics changes via stash-isolation.
Together with 05-01 and 05-02, the `src/**/*.vue` tree is off-palette-clean and brand-alias-clean with
ZERO survivors, so the Wave-2 `--accent` flip-gate finds zero brand `var(--accent)` usages and the phase
self-completes in autonomous mode.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-03-SUMMARY.md`.
Include the kept novel hex (`path:hex:reason`) AND a confirmation that ActivityFeed.vue was repointed via
stash-isolation (committed color-only diff; analytics changes preserved uncommitted; `git stash list` clean).

UXΔ = +1 (Better) — tail surfaces now read from canonical tokens; only ActivityFeed's intended hover/title
hues preserved exactly via brand-cyan repoint (no rendered change).
CDI = 0.03 * 13 — 11 files, mechanical token swaps + one stash-isolated commit; low spread, low shift.
MVQ = Sprite 88%/82% — small, surgical, value-preserving cleanup with a clever stash-isolation trick.
</output>
