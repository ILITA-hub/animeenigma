---
phase: 05-tail-sweep-lint-enforcement
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - frontend/web/src/styles/main.css
autonomous: false
requirements: [DS-MIGRATE-05]

must_haves:
  truths:
    - "Zero remaining brand var(--accent) usages exist in src/**/*.vue before the flip (hard precondition — ActivityFeed handled in 05-03 Task 3)"
    - "--accent now resolves to the shadcn hover surface (not brand-cyan)"
    - "The temporary brand-cyan alias note + deprecated --accent→brand-cyan comment are removed"
    - "No visual regression on the standing 5-surface smoke set except the intended hover-surface correction"
  artifacts:
    - path: "frontend/web/src/styles/main.css"
      provides: "--accent redefined to the shadcn hover surface; temporary brand-cyan alias deleted"
      contains: "--accent"
  key_links:
    - from: "main.css :root --accent"
      to: "shadcn hover surface token"
      via: "single-commit redefinition after all brand usages repointed"
      pattern: "--accent:\\s*"
---

<objective>
Flip the deprecated `--accent` token in main.css from its temporary brand-cyan alias to the shadcn
hover-surface meaning, and delete the temporary alias scaffolding — in ONE atomic commit, AFTER
grep-confirming (as a HARD PRECONDITION) zero remaining brand `var(--accent)` usages across `src/`. This
is the milestone's ONLY intentional rendered change (a deliberate, documented hover-surface correction),
so it carries a human-verify in-browser smoke gate.

Purpose: DS-MIGRATE-05. Completes the alias retirement and aligns `--accent` with its canonical
shadcn-vue meaning (a subtle hover/active surface), unblocking the Wave-3 lint gate rule (c) which will
then correctly treat any `var(--accent)` brand usage as a violation.
Output: main.css with `--accent` = shadcn hover surface; temporary brand-cyan alias removed; HUMAN-UAT
record of the in-browser smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-CONTEXT.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<current_state>
main.css today (verified at plan time):
- line ~405: `--accent: var(--brand-cyan); /* deprecated → --brand-cyan (P2 flips to shadcn hover surface) */`
- lines ~406-408: `--accent-soft`, `--accent-line`, `--accent-glow` (literal — these STAY; they have no
  canonical equivalent and are NOT the `--accent` brand token).
- line ~436-437: comment block "NOTE: shadcn --accent (hover surface) is deferred to P2 — --accent stays
  brand-cyan below for back-compat."
- line ~440: `--brand-cyan: var(--color-cyan-400);` (the canonical brand-cyan definition — KEEP; only the
  temporary `--accent → brand-cyan` ALIAS is being removed, not brand-cyan itself).

The shadcn-vue canonical meaning of `--accent` is a subtle hover/active SURFACE (neutral, low-contrast),
distinct from a brand accent color. Choose the hover-surface value consistent with the existing neutral
elevated tokens (e.g. an elevated/surface-2 neutral such as `var(--elevated)` or `var(--surface-2)`),
matching how shadcn-vue components expect `bg-accent` to read as a hover background. Confirm the exact
value against DESIGN-SYSTEM.md Tier-2 notes during execution; document the chosen value in the commit.
</current_state>

<precondition>
**HARD PRECONDITION (no longer conditional on ActivityFeed):** 05-03 Task 3 repoints ActivityFeed.vue's
`var(--accent)` lines via stash-isolation, so after Wave-1 the flip-gate grep
`grep -rnE 'var\(--accent\b' src --include='*.vue' | grep -vE 'accent-soft|accent-line|accent-glow'`
MUST return ZERO hits across ALL of `src/` with NO survivors. There is no "if ActivityFeed still shows
var(--accent)..." carve-out anymore. If the gate grep returns ANY hit, that is a REAL BLOCKER — STOP and
surface it (a Wave-1 plan, most likely 05-03 Task 3, did not land). Do NOT flip while any brand
`var(--accent)` usage remains; force-flipping would silently regress that component cyan→neutral-surface
(violates SC#2).
</precondition>

<cascade_footgun>
reference_tailwind_v4_css_cascade + DS-NF-06: Tailwind v4 unlayered custom classes beat utilities, and
jsdom cannot catch cascade regressions. The `--accent` flip MUST be verified in a REAL browser, not via
unit tests. Do NOT reorder/relayer any rule; only change the `--accent` value + delete the temp-alias
comment scaffolding.
</cascade_footgun>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert zero brand --accent usages (hard precondition), then flip --accent + delete temp alias</name>
  <files>frontend/web/src/styles/main.css</files>
  <read_first>
  cd frontend/web
  # HARD PRECONDITION GATE: there MUST be ZERO repointable brand var(--accent) usages left in components.
  # ActivityFeed.vue was repointed in 05-03 Task 3 (stash-isolation), so there are NO survivors.
  grep -rnE 'var\(--accent\b' src --include='*.vue' | grep -vE 'accent-soft|accent-line|accent-glow'
  # ^ MUST be empty. ANY hit is a real blocker (a Wave-1 plan did not land) — STOP and surface, do NOT flip.
  grep -nE '\-\-accent|brand-cyan' src/styles/main.css
  </read_first>
  <action>
  FIRST assert the gate grep returns ZERO brand `var(--accent)` usages across `src/**/*.vue` (excluding
  the literal `--accent-soft|-line|-glow`). This is a HARD precondition — if ANY hit remains it is a real
  blocker (ActivityFeed and all other usages were already repointed in Wave-1, including 05-03 Task 3's
  stash-isolated ActivityFeed commit); STOP and surface them, do NOT flip. Once confirmed empty:
  in main.css, redefine `--accent` from `var(--brand-cyan)` to the shadcn hover-surface value (a neutral
  elevated surface per @current_state; document the exact chosen value in the commit message), update the
  inline comment from "deprecated → --brand-cyan" to reflect the new shadcn-hover-surface meaning, and
  remove the "deferred to P2 / stays brand-cyan for back-compat" NOTE comment block. KEEP `--brand-cyan`,
  `--accent-soft`, `--accent-line`, `--accent-glow` untouched. Do NOT relayer or reorder any rule. One
  atomic commit staging ONLY main.css. Commit: `refactor(05): flip --accent to shadcn hover surface, drop temp brand-cyan alias [DS-MIGRATE-05]`.
  </action>
  <verify>
    <automated>cd frontend/web && test $(grep -rnE 'var\(--accent\b' src --include='*.vue' | grep -vE 'accent-soft|accent-line|accent-glow' | wc -l) -eq 0 && grep -qE '^\s*--accent:\s*' src/styles/main.css && ! grep -qE '\-\-accent:\s*var\(--brand-cyan\)' src/styles/main.css && bunx vue-tsc --noEmit && bunx vite build >/dev/null 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - HARD PRECONDITION: brand `var(--accent)` usage grep across `src/**/*.vue` (minus literal aliases) returns ZERO hits; if not, the task STOPS (real blocker) and does NOT flip.
    - `--accent` in main.css is NO LONGER `var(--brand-cyan)` (the temp alias is gone).
    - `--brand-cyan`, `--accent-soft`, `--accent-line`, `--accent-glow` are unchanged.
    - `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean.
    - Commit stages ONLY main.css.
  </acceptance_criteria>
  <done>Hard precondition confirmed empty; main.css `--accent` resolves to the shadcn hover surface; temp brand-cyan alias deleted; build green; single-file commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-browser smoke of the --accent flip (the milestone's only intended render change)</name>
  <action>
  Human (or auto-approve in autonomous mode) verifies the flipped `--accent` hover surface in a REAL
  browser across the standing 5-surface set, then records the outcome as a HUMAN-UAT artifact. No code is
  written in this task — it is the blocking visual-verification gate for the single intentional render
  change. See <what-built>/<how-to-verify>/<resume-signal> below for the exact procedure.
  </action>
  <what-built>
  main.css `--accent` flipped from brand-cyan to the shadcn hover surface; temporary brand-cyan alias
  deleted. This is the ONLY intentional rendered change in the whole design-system milestone.
  </what-built>
  <how-to-verify>
  1. `make redeploy-web` (runs i18n-lint + type-check + build). Wait for healthy.
  2. In a REAL browser (NOT jsdom — Tailwind-v4 cascade footgun), smoke the standing 5-surface set at
     desktop AND mobile widths:
     a. Home — spotlight carousel (RandomTail purple cta-hero zone), Онгоинги/Топ rails.
     b. Browse / catalog — filter sidebar, star badges, cards, pagination.
     c. Anime detail — cyan `.btn-primary` ("Смотреть"), status badge, schedule pill, language pills.
     d. A watch/player surface — one player loads + styled controls.
     e. 404 — muted styling + "На главную" button.
  3. Specifically confirm: anywhere that previously used `bg-accent`/`var(--accent)` as a HOVER surface
     now shows the intended neutral hover (not the old cyan), and NO brand-cyan element accidentally went
     neutral (those should now read from `--brand-cyan` directly, repointed in Wave-1 — INCLUDING
     ActivityFeed's hover/title links repointed in 05-03 Task 3). Hover over interactive elements that use
     `bg-accent` to confirm the corrected hover surface.
  4. Confirm zero unexpected color regressions on all 5 surfaces.
  </how-to-verify>
  <verify>
    <automated>MANUAL — in-browser visual smoke (DS-NF-06: jsdom cannot catch cascade regressions). Auto-approved in autonomous mode and persisted as 05-04-HUMAN-UAT.md.</automated>
  </verify>
  <acceptance_criteria>
    - 5-surface in-browser smoke shows only the intended hover-surface correction; no other color regression.
    - HUMAN-UAT record (05-04-HUMAN-UAT.md) persisted with the outcome.
  </acceptance_criteria>
  <done>In-browser smoke confirms only the intended hover-surface change; HUMAN-UAT recorded.</done>
  <resume-signal>Type "approved" if the 5-surface smoke is clean (only the intended hover-surface correction visible), or describe any regression. In autonomous mode this auto-approves and is persisted as a HUMAN-UAT record.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rnE 'var\(--accent\b' src --include='*.vue' | grep -vE 'accent-soft|accent-line|accent-glow'` → ZERO hits (hard precondition; no ActivityFeed carve-out).
- main.css `--accent` is no longer `var(--brand-cyan)`; `--brand-cyan` + literal `--accent-*` intact.
- `make redeploy-web` succeeds; 5-surface in-browser smoke clean (only the intended hover-surface change).
- HUMAN-UAT record persisted.
</verification>

<success_criteria>
`--accent` resolves to the shadcn hover surface; the temporary brand-cyan alias is deleted; no visual
regression beyond the intended, documented hover-surface correction (verified in a real browser). The tree
has zero brand `var(--accent)` usages (a HARD precondition met by Wave-1 including 05-03 Task 3), satisfying
the precondition for the Wave-3 lint gate rule (c).
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-04-SUMMARY.md`
and a `05-04-HUMAN-UAT.md` recording the in-browser smoke outcome.

UXΔ = +1 (Better) — corrects --accent to its intended shadcn hover-surface meaning; the single deliberate render change.
CDI = 0.01 * 5 — one token redefinition in one file; minimal spread, single intentional shift.
MVQ = Sprite 86%/84% — tiny surgical flip behind a hard precondition + in-browser smoke gate.
</output>
