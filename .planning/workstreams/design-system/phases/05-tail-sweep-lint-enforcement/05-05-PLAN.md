---
phase: 05-tail-sweep-lint-enforcement
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - frontend/web/scripts/design-system-lint.sh
  - frontend/web/scripts/design-system-allowlist.txt
  - Makefile
  - frontend/web/src/styles/DESIGN-SYSTEM.md
autonomous: true
requirements: [DS-GOV-01, DS-GOV-02]

must_haves:
  truths:
    - "The lint gate exits 1 on a deliberately-introduced bg-red-500 and exits 0 on the clean tree"
    - "The gate enforces exactly 3 color/token rules: off-palette classes, non-allowlisted hex, deprecated brand-alias var() usages"
    - "The gate runs on the same path redeploy-web/CI use (make lint-frontend + redeploy-web prereq)"
    - "The allowlist + gate are documented in DESIGN-SYSTEM.md"
    - "Empirically-discovered hex in files NOT touched by 05-01/02/03 is ADJUDICATED with a recorded reason (Auth.vue Telegram blue, Collections.vue gradient), never blanket-allowlisted"
    - "The full vitest suite + vue-tsc + the home/spotlight e2e canary run green (no NEW failure beyond the documented AnimeContextMenu.spec.ts:227 pre-existing one)"
  artifacts:
    - path: "frontend/web/scripts/design-system-lint.sh"
      provides: "Custom bash gate (set -euo pipefail, colored ERRORS/WARNINGS, exit 1 on errors, --selftest mode)"
      min_lines: 80
    - path: "frontend/web/scripts/design-system-allowlist.txt"
      provides: "path:hex:reason allowlist seeded from the documented novel-hex inventory; each line a justified+adjudicated hex"
    - path: "Makefile"
      provides: "lint-design target wired into lint-frontend AND redeploy-web prerequisites"
      contains: "lint-design"
  key_links:
    - from: "make lint-frontend + make redeploy-web"
      to: "scripts/design-system-lint.sh"
      via: "new lint-design make target invoked on the deploy/CI path"
      pattern: "design-system-lint\\.sh"
    - from: "design-system-lint.sh"
      to: "scripts/design-system-allowlist.txt"
      via: "allowlist read at hex-rule evaluation"
      pattern: "design-system-allowlist\\.txt"
---

<objective>
Build the build-failing color/token lint gate that locks the migration shut: a custom bash script
`design-system-lint.sh` (mirroring `i18n-lint.sh`) enforcing exactly the 3 color/token-discipline rules,
a seeded + ADJUDICATED allowlist file, the Make wiring into `lint-frontend` + the `redeploy-web` deploy
gate, a provable fail-path (`--selftest`), DESIGN-SYSTEM.md documentation, AND a final full-suite green
verification (vitest + tsc + home/spotlight e2e canary).

Purpose: DS-GOV-01 (build-failing lint on hardcoded hex / off-palette classes, wired into the deploy gate)
+ DS-GOV-02 (documented allowlist/escape-hatch). This is the step that makes consolidation STAY, and it
owns SC#5 (full test/type/e2e green).
Output: a passing gate on the clean tree, a failing gate on `bg-red-500`, wired into the same path CI/deploy
runs, fully documented, with the full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/STATE.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-CONTEXT.md
@frontend/web/src/styles/DESIGN-SYSTEM.md
@frontend/web/scripts/i18n-lint.sh

<gate_rules>
The gate enforces EXACTLY these 3 rules over `frontend/web/src/**/*.vue` (excluding `*.spec.ts`/`__tests__`).
These ARE the field-proven Phase-4 greps — reuse them verbatim:

RULE 1 — Zero off-palette Tailwind color classes (ERROR). Pattern (Phase-4 verbatim):
  (text|bg|border|ring|from|to|via|fill|stroke|placeholder|divide|outline|decoration|shadow)-(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)
  NOTE: `cyan`, `pink`, `orange`, `rose`, `indigo`, `teal`, `lime` are NOT in this set — cyan/pink are
  the Neon-Tokyo BRAND primitives and orange/rose are per-provider identity hues (Kodik cyan, AniLib
  orange, Hanime pink, Raw rose). Including them would fail the clean tree (Anime.vue, players, Navbar all
  legitimately use them). This brand-exemption rationale is documented in DESIGN-SYSTEM.md (DS-GOV-02).

RULE 2 — Zero hardcoded hex outside the allowlist (ERROR). Pattern:
  raw `#[0-9a-fA-F]{3,8}` literals in `.vue` (both `<style>` raw hex and Tailwind arbitrary `*-[#...]`),
  EXCLUDING any `path:hex` pair present in `scripts/design-system-allowlist.txt`. The allowlist match is
  per-(file,hex): a hex is allowed only in the file(s) that list it. (Implementation: for each offending
  `file:hex`, skip if a non-comment allowlist line contains both that file path and that hex.)

RULE 3 — Zero deprecated brand-alias usages (ERROR). Pattern:
  var(--ink) | var(--accent) | var(--pink)  (word-boundary), EXCLUDING the literal-alias survivors
  `--ink-2`, `--ink-4`, `--accent-soft`, `--accent-line`, `--accent-glow`, `--pink-soft`.
  After Wave-2, `var(--accent)` brand usage is itself a violation (the flip made --accent a hover surface).

OUT OF LINT SCOPE (Phase-6 governance docs only — do NOT add these rules): use-the-primitives, font-weight
scale, padding scale, cva variant usage, hex-in-`<style>`-beyond-allowlist niceties, arbitrary `text-[#...]`
beyond rule 2. The user chose the narrower roadmap scope; deferred items stay deferred.
</gate_rules>

<allowlist_seed>
Seed `scripts/design-system-allowlist.txt` (format: `path:hex:reason`, `#`-prefixed comments allowed)
from the documented novel-hex inventory. Authoritative sources, in priority order:
1. The "Novel hex for Wave-3 allowlist" sections in 05-01/05-02/05-03 SUMMARYs (kept hex from the sweep).
2. Phase-4 deferred-items + Phase-4 player/chrome allowlist decisions:
   - per-player accent hues: KodikPlayer.vue:#06b6d4, AnimeLibPlayer.vue:#f97316, HanimePlayer.vue:#ec4899
   - SubtitleOverlay render defaults: SubtitleOverlay.vue:#ffffff, SubtitleOverlay.vue:#ffcccc
   - Navbar avatar teal gradient: Navbar.vue:#1a3a4a, Navbar.vue:#0e2030 (+ #08080f, #00ff9d if present)
   - BrandMark brand hex: BrandMark.vue:#08080f
   - ActivityFeed.vue teal gradient: #1a3a4a, #0e2030 (analytics-workstream file; color lines repointed in
     05-03 Task 3 — any kept gradient hex still needs an allowlist line with reason)
   - SVG `<stop stop-color>` gradient hex (raw SVG markup is not token-addressable), e.g.
     ContinueWatchingRow.vue:#00d4ff and any others recorded by 05-01.
   - `var(--token,#fallback)` canonical-fine fallbacks if rule 2 flags them.
3. CRITICAL — derive the allowlist EMPIRICALLY: after the script is written, run rule 2 over the
   post-Wave-1/2 tree and add a `path:hex:reason` entry for EVERY remaining legitimate hex so the gate
   passes on the clean tree. Every added entry MUST have a real reason (provider hue / subtitle default /
   gradient stop / brand glow / SVG stop). Do NOT blanket-allowlist; each line is one justified hex.
</allowlist_seed>

<adjudication>
**ADJUDICATION RULE (FIX 2 — mandatory):** Any empirically-discovered hex in a file NOT touched by
05-01/05-02/05-03 (i.e. outside the Wave-1 migration file set) MUST be ADJUDICATED — decide migrate-to-token
vs justified-allowlist — and the chosen disposition recorded with a real reason in the allowlist file (or
migrated out). NEVER blanket-allowlist an out-of-scope hex just to force the gate green. Specifically known
out-of-scope hex to adjudicate by NAME:

- `frontend/web/src/views/Auth.vue` — `bg-[#54a9eb] hover:bg-[#4a96d2]` (Telegram-brand blue on the
  "log in with Telegram" button). LIKELY a JUSTIFIED provider-brand allowlist (Telegram's official brand
  blue is a third-party identity color, analogous to per-provider player hues). Allowlist BOTH hex WITH the
  reason "Telegram provider brand blue (official) — provider-identity, not a token". Confirm it truly is the
  Telegram button before allowlisting; if it's decorative chrome, migrate instead.
- `frontend/web/src/views/Collections.vue` — `linear-gradient(135deg, #0e7490 0%, #6b21a8 100%)` (a
  cyan→purple gradient). ADJUDICATE migrate-vs-allowlist: `#0e7490` is near `brand-cyan`/`info` and
  `#6b21a8` is near `brand-violet` — if a token-built gradient (`var(--brand-cyan)`→`var(--brand-violet)`
  or the equivalent semantic tokens) reproduces it within tolerance, MIGRATE Collections.vue (preferred,
  since these map cleanly to existing tokens). If a faithful token gradient is not achievable, allowlist
  BOTH hex WITH the reason "Collections hero gradient — no exact token pair; adjudicated keep". Record the
  decision (migrated vs allowlisted-with-reason) in the SUMMARY either way.

If Collections.vue is migrated, add it to this plan's `files_modified` and stage it in Task 1's commit.
</adjudication>

<i18n_lint_pattern>
Mirror `frontend/web/scripts/i18n-lint.sh` structure: `#!/usr/bin/env bash`, `set -euo pipefail`,
`SCRIPT_DIR`, RED/YELLOW/GREEN/NC colors, `ERRORS`/`WARNINGS` counters, per-rule `=== ... ===` section
headers, `OK`/`ERROR` lines, a `=== Summary ===` block, and `exit 1` when `ERRORS > 0` else `exit 0`.
Avoid the SIGPIPE-under-pipefail footgun the i18n script documents (don't pipe `grep -q` off a feeding
`echo`/`cat` under pipefail; collect into temp files / use `grep -c ... || true`).
</i18n_lint_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write design-system-lint.sh + seed/adjudicate the allowlist, prove pass-on-clean</name>
  <files>frontend/web/scripts/design-system-lint.sh, frontend/web/scripts/design-system-allowlist.txt</files>
  <read_first>
  Read `frontend/web/scripts/i18n-lint.sh` fully (the pattern to mirror — see @i18n_lint_pattern).
  Read the 05-01/05-02/05-03 SUMMARYs' "Novel hex for Wave-3 allowlist" sections (@allowlist_seed source 1).
  Run the live greps to know the current state of each rule:
    cd frontend/web
    grep -rnE '(text|bg|border|ring|from|to|via|fill|stroke|placeholder|divide|outline|decoration|shadow)-(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50|100|200|300|400|500|600|700|800|900)' src --include='*.vue' | grep -v '\.spec\.'   # RULE 1: expect ZERO after Wave-1
    grep -rnoE '#[0-9a-fA-F]{3,8}\b' src --include='*.vue' | grep -vE 'rgba?\('   # RULE 2: the hex universe to allowlist
    grep -rnE 'var\(--(ink|accent|pink)\b' src --include='*.vue' | grep -vE 'ink-2|ink-4|accent-soft|accent-line|accent-glow'   # RULE 3: expect ZERO (ActivityFeed handled in 05-03 Task 3)
  # Out-of-scope hex to ADJUDICATE (see @adjudication): Auth.vue #54a9eb/#4a96d2, Collections.vue #0e7490/#6b21a8.
  </read_first>
  <action>
  Create `scripts/design-system-lint.sh` mirroring i18n-lint.sh (@i18n_lint_pattern), enforcing exactly the
  3 rules in @gate_rules over `src/**/*.vue` (exclude `*.spec.*` and `__tests__`). RULE 1 + RULE 3 are
  simple greps that increment ERRORS per hit and print `ERROR <relpath>:<line>: <snippet>`. RULE 2 collects
  every `file:hex` and skips any pair whose `path` + `hex` both appear on a non-comment allowlist line read
  from `scripts/design-system-allowlist.txt`; remaining hits increment ERRORS. Print a `=== Summary ===`
  block with per-rule counts; `exit 1` if `ERRORS>0` else print PASS + `exit 0`. Reserve a `--selftest`
  branch (full implementation in Task 2). Then create `scripts/design-system-allowlist.txt`, seeding it per
  @allowlist_seed and EMPIRICALLY adding a justified `path:hex:reason` line for every legitimate remaining
  hex until the gate passes on the clean tree.

  ADJUDICATE (per @adjudication, FIX 2) every empirically-discovered hex that lives in a file NOT touched by
  05-01/02/03 — decide migrate-to-token vs justified-allowlist with a recorded reason; NEVER blanket-allowlist
  an out-of-scope hex to force green. In particular adjudicate Auth.vue's Telegram-brand blue (`#54a9eb`/
  `#4a96d2` → likely justified provider-brand allowlist WITH reason) and Collections.vue's `#0e7490`/`#6b21a8`
  gradient (migrate to a token gradient if reproducible within tolerance, else allowlist WITH reason). If
  Collections.vue is migrated, stage it in this task's commit and note it in files_modified.

  Run the gate; iterate the allowlist until `bash scripts/design-system-lint.sh` exits 0. Commit staging
  ONLY this task's files (the 2 script/allowlist files + Collections.vue IF migrated).
  Commit: `feat(05): add design-system color/token lint gate + adjudicated allowlist [DS-GOV-01,DS-GOV-02]`.
  </action>
  <verify>
    <automated>cd frontend/web && bash scripts/design-system-lint.sh; echo "clean-tree exit=$?"; test -f scripts/design-system-allowlist.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/design-system-lint.sh` exits 0 on the clean (post-Wave-1/2) tree.
    - The script enforces exactly the 3 @gate_rules (no out-of-scope rules added).
    - `scripts/design-system-allowlist.txt` exists; every entry is `path:hex:reason` with a real reason; no blanket wildcard allowlist.
    - Auth.vue's Telegram blue + Collections.vue's gradient are ADJUDICATED (migrated OR allowlisted-with-reason), NOT blanket-allowlisted; the disposition is recorded.
    - Script has `set -euo pipefail`, colored ERRORS/WARNINGS, a Summary block, `exit 1` on errors.
  </acceptance_criteria>
  <done>Gate script + adjudicated allowlist exist; gate passes on the clean tree; out-of-scope hex adjudicated; one atomic commit staging only this task's files.</done>
</task>

<task type="auto">
  <name>Task 2: Prove fail-path (--selftest), wire into make, document, run full suite + e2e green</name>
  <files>frontend/web/scripts/design-system-lint.sh, Makefile, frontend/web/src/styles/DESIGN-SYSTEM.md</files>
  <read_first>
  Read Makefile lines around `lint-frontend` (≈149-150), `i18n-lint` (≈261-262), `redeploy-web` (≈267) and
  the `.PHONY` block (≈1-6). `redeploy-web: i18n-lint type-check` today — the gate must join that prereq
  chain (the deploy path) AND `lint-frontend` (the `make lint`/`all`/CI path). Read DESIGN-SYSTEM.md
  "Usage rules (governance)" + "Deprecated aliases" sections — that is where the allowlist + gate docs go.
  Read `.planning/workstreams/design-system/phases/04-high-traffic-surface-migration/` deferred-items.md
  for the documented pre-existing AnimeContextMenu.spec.ts:227 failure (the ONE acceptable test failure).
  </read_first>
  <action>
  (a) SELF-TEST: implement `--selftest` in design-system-lint.sh — it writes a throwaway scratch `.vue`
  under `src/` containing a `bg-red-500` marker, runs RULE 1 against it, asserts the gate DETECTS it (an
  ERROR is counted / would exit 1), then deletes the scratch file and asserts the clean tree exits 0; print
  `SELFTEST PASS`/`SELFTEST FAIL` and exit accordingly. Use a `trap` to guarantee the scratch file is
  removed even on failure (never leave the tree dirty). Document a one-liner fallback in the script header
  comment (inject `bg-red-500` into a real file → run gate → revert) for manual proof.
  (b) MAKE WIRING: add a `lint-design` target (`@cd frontend/web && bash scripts/design-system-lint.sh`),
  add it to the `.PHONY` list, make `lint-frontend` depend on it (so `make lint`/`all`/CI run it), AND add
  `lint-design` to `redeploy-web`'s prerequisites (`redeploy-web: i18n-lint lint-design type-check`) so the
  deploy gate enforces it on the SAME path CI/deploy already use.
  (c) DOCS: add to DESIGN-SYSTEM.md a "Lint gate (enforced)" subsection documenting the 3 enforced rules,
  the brand-exemption rationale (why cyan/pink/orange/rose are NOT off-palette), the allowlist file path +
  `path:hex:reason` format + how to add an exception, the adjudication rule for out-of-scope hex, and the
  `--selftest` proof command. Keep docs matching the enforced rules exactly (no documented-but-unenforced
  rule, the Phase-6 DS-GOV-03 precondition).
  (d) FULL-SUITE VERIFICATION (FIX 3 — SC#5 owner): run the full frontend suite and assert green —
      `cd frontend/web && bunx vitest run`        (FULL vitest suite, not a subset)
      `cd frontend/web && bunx vue-tsc --noEmit`   (type-check)
      `cd frontend/web && bunx playwright test e2e/spotlight.spec.ts`  (home/spotlight e2e canary — the
        canonical full-stack regression; add the home spec if a separate one exists).
    "GREEN" means NO NEW failures beyond the ONE documented pre-existing `AnimeContextMenu.spec.ts:227`
    failure (logged in phase-04 deferred-items.md) — that single failure is acceptable/expected. If ANY
    OTHER test/type/e2e failure appears, STOP and surface it (a real regression from the migration). Record
    the full-suite result (pass counts + the expected AnimeContextMenu failure) in the SUMMARY.
  Commit staging ONLY these 3 files (the suite run produces no source changes).
  Commit: `feat(05): wire design-system gate into make + selftest + docs; full-suite green [DS-GOV-01,DS-GOV-02]`.
  </action>
  <verify>
    <automated>cd /data/animeenigma && bash frontend/web/scripts/design-system-lint.sh --selftest; echo "selftest exit=$?"; grep -q 'lint-design' Makefile && grep -qE 'redeploy-web:.*lint-design' Makefile && grep -qiE 'lint gate|design-system-allowlist' frontend/web/src/styles/DESIGN-SYSTEM.md; git -C /data/animeenigma diff --quiet -- frontend/web/src; echo "tree-clean-after-selftest=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash design-system-lint.sh --selftest` exits 0 (proves it DETECTS bg-red-500 then PASSES on the reverted clean tree) and leaves `git diff --quiet -- frontend/web/src` clean (no scratch file left behind).
    - `make lint-frontend` includes `lint-design`; `redeploy-web` prerequisites include `lint-design`; `lint-design` is in `.PHONY`.
    - DESIGN-SYSTEM.md documents the 3 enforced rules, the brand-exemption rationale, the allowlist format/path, the adjudication rule, and the selftest command — matching the enforced rules exactly.
    - SC#5: `bunx vitest run` (full) + `bunx vue-tsc --noEmit` + the home/spotlight e2e canary all GREEN, with the SOLE exception of the documented pre-existing AnimeContextMenu.spec.ts:227 failure; any other failure STOPS the plan. Result recorded in the SUMMARY.
    - Commit stages ONLY these 3 files.
  </acceptance_criteria>
  <done>Self-test proves fail-then-pass with a clean tree afterward; gate wired into lint-frontend + redeploy-web; docs match the enforced rules; full vitest + tsc + home/spotlight e2e green (modulo the documented AnimeContextMenu failure); one atomic commit staging only these 3 files.</done>
</task>

</tasks>

<verification>
- `bash frontend/web/scripts/design-system-lint.sh` exits 0 on the clean tree.
- `bash frontend/web/scripts/design-system-lint.sh --selftest` exits 0 and leaves no scratch file (tree clean).
- `make lint-frontend` and `make redeploy-web` both run the gate (greps confirm `lint-design` in both).
- DESIGN-SYSTEM.md documents exactly the 3 enforced rules + allowlist + adjudication + selftest (no documented-but-unenforced rule).
- Out-of-scope hex (Auth.vue Telegram blue, Collections.vue gradient) ADJUDICATED with recorded reasons, not blanket-allowlisted.
- SC#5: full `bunx vitest run` + `bunx vue-tsc --noEmit` + home/spotlight e2e canary GREEN (modulo the documented AnimeContextMenu.spec.ts:227 pre-existing failure).
- `git status --porcelain` shows ONLY this plan's files committed (no analytics/scraper files swept in).
</verification>

<success_criteria>
A custom bash lint gate enforces exactly the 3 color/token rules, passes on the clean tree, fails on a
deliberate `bg-red-500` (provable via `--selftest`), reads a documented + ADJUDICATED `path:hex:reason`
allowlist (out-of-scope hex like Auth.vue's Telegram blue and Collections.vue's gradient adjudicated, not
blanket-allowlisted), and is wired into both `make lint-frontend` (CI/`all`) and `redeploy-web` (deploy
gate). The full vitest suite + vue-tsc + home/spotlight e2e canary are GREEN (modulo the single documented
pre-existing AnimeContextMenu failure), satisfying SC#5. The allowlist + gate are documented in
DESIGN-SYSTEM.md, matching the enforced rules exactly. DS-GOV-01 + DS-GOV-02 satisfied.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/05-tail-sweep-lint-enforcement/05-05-SUMMARY.md`.
Record the final allowlist contents (each `path:hex:reason`), the adjudication decisions (Auth.vue + Collections.vue:
migrated vs allowlisted-with-reason), the selftest result, the full-suite result (vitest/tsc/e2e pass counts +
the expected AnimeContextMenu.spec.ts:227 failure), and confirm ActivityFeed.vue's disposition (its color lines
were repointed in 05-03 Task 3; any kept gradient hex allowlisted with reason).

UXΔ = 0 (Ambiguous) — pure governance/tooling; no user-facing render change, locks the migration shut.
CDI = 0.04 * 21 — script + allowlist + make wiring + docs + adjudication + full-suite gate; moderate spread, mechanical.
MVQ = Griffin 87%/85% — durable build-failing guardrail with a provable fail-path and adjudicated escape hatch.
</output>
