---
phase: 06-governance-into-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md
  - /root/.claude/projects/-data-animeenigma/memory/MEMORY.md
autonomous: true
requirements: [DS-GOV-03, DS-NF-05, DS-NF-06]

must_haves:
  truths:
    - "A project-memory governance file exists capturing the 4 rule themes (use tokens / never hardcode, reuse ui/ primitives before building new, verify visual changes in-browser, the gate is real + allowlist escape-hatch) with a pointer to DESIGN-SYSTEM.md"
    - "CLAUDE.md has a 'Design System' subsection pointing at DESIGN-SYSTEM.md + the lint-gate command"
    - "The governance text matches the enforced lint rule: the 3 lint-enforced rules are labeled enforced; the structural rules are labeled governance-only"
    - "MEMORY.md has a one-line pointer to the new governance memory file"
    - "DS-NF-06 (verify-in-browser) is a governance bullet; DS-NF-05 (per-phase shippable) is acknowledged in one line"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Design System governance subsection (enforced vs governance-only labeled), DESIGN-SYSTEM.md pointer, lint-gate command"
      contains: "design-system-lint.sh"
    - path: "/root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md"
      provides: "Durable design-system governance memory entry with frontmatter + 4 rule themes + DESIGN-SYSTEM.md pointer"
      contains: "DESIGN-SYSTEM.md"
    - path: "/root/.claude/projects/-data-animeenigma/memory/MEMORY.md"
      provides: "One-line index pointer to the governance memory file"
      contains: "project_design_system_governance"
  key_links:
    - from: "CLAUDE.md Design System subsection"
      to: "frontend/web/src/styles/DESIGN-SYSTEM.md"
      via: "explicit path reference"
      pattern: "DESIGN-SYSTEM\\.md"
    - from: "CLAUDE.md Design System subsection"
      to: "frontend/web/scripts/design-system-lint.sh"
      via: "lint-gate command (make lint-frontend / design-system-lint.sh)"
      pattern: "design-system-lint\\.sh|lint-frontend"
    - from: "MEMORY.md"
      to: "project_design_system_governance.md"
      via: "one-line index pointer"
      pattern: "project_design_system_governance"
---

<objective>
Write the design-system rules where every future session (human or AI) will see them, so the now-enforced reality (Phases 1–5) doesn't silently erode. Pure documentation/governance — no code, no rendered change.

Two surfaces:
1. **Project memory** — a durable governance entry the assistant loads each session, plus a `MEMORY.md` index pointer.
2. **`CLAUDE.md`** (repo root) — a concise "Design System" subsection pointing at the canonical `frontend/web/src/styles/DESIGN-SYSTEM.md` + the enforced lint gate.

The load-bearing constraint (SC#3): the governance text MUST match the ENFORCED lint rule exactly — label the 3 lint-enforced color/token rules as enforced, and the structural rules (reuse primitives / weights / padding) as governance-only. Do NOT duplicate the full DESIGN-SYSTEM.md; point at it.

Purpose: lock in the consolidation so it stays consolidated — close out the v1.0 milestone (DS-GOV-03, DS-NF-05, DS-NF-06).
Output: an edited `CLAUDE.md`, a new project-memory governance file, and a `MEMORY.md` pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/phases/06-governance-into-memory/06-CONTEXT.md
@.planning/workstreams/design-system/ROADMAP.md
@.planning/workstreams/design-system/REQUIREMENTS.md

<interfaces>
<!-- The EXACT enforced rules the governance text must mirror. Extracted from -->
<!-- frontend/web/scripts/design-system-lint.sh + DESIGN-SYSTEM.md "Lint gate (enforced)". -->
<!-- Executor must match these labels — no enforced rule undocumented; no governance-only -->
<!-- rule mislabeled as enforced. -->

LINT-ENFORCED (build fails on these — `make lint-frontend` / `make redeploy-web`, via
`scripts/design-system-lint.sh`, ERRORS>0 ⇒ exit 1):
  RULE 1 — No off-palette Tailwind color classes:
    (text|bg|border|ring|from|to|via|fill|stroke|placeholder|divide|outline|decoration|shadow)
    -(red|amber|yellow|emerald|green|blue|sky|purple|violet|gray|slate|zinc)-(50…900).
    EXEMPT (brand/provider identity hues, deliberately absent from the set):
    cyan, pink, orange, rose, indigo, teal, lime.
  RULE 2 — No hardcoded hex outside scripts/design-system-allowlist.txt
    (per-(file,hex), `path:hex:reason` lines).
  RULE 3 — No deprecated brand-alias var(--ink|--accent|--pink) usages
    (survivors --ink-2, --ink-4, --accent-soft, --accent-line, --accent-glow, --pink-soft kept).

GOVERNANCE-ONLY (human/AI-followed, NOT build-enforced — a grep can't AST-distinguish them):
  - Reuse @/components/ui primitives before building new
    (Button/Card/Badge/Input/Select/Dialog/Tabs/DropdownMenu/Tooltip/Popover/Switch/Checkbox).
  - Only font-medium / font-semibold weights.
  - Padding scale (card p-4 md:p-6 lg:p-8).
  - cva variants.

STANDING PROCESS RULES:
  - DS-NF-06 — verify visual changes in a real browser (desktop + mobile); jsdom can't catch
    Tailwind-v4 cascade bugs (unlayered custom classes beat utilities). → a governance bullet.
  - DS-NF-05 — every phase independently shippable; satisfied by each phase's green build —
    one-line acknowledgement, no new machinery.

Escape-hatch: add a justified `path:hex:reason` line to scripts/design-system-allowlist.txt;
don't disable the gate. `--accent` is the shadcn hover surface since 05-04 (use `--brand-cyan`
for brand cyan).

Project-memory format (from the assistant's system prompt): kebab-named file with frontmatter
(name / description / metadata.type: project|reference); a one-line `MEMORY.md` index pointer;
`[[name]]` cross-links. NOTE: `/root/.claude/projects/-data-animeenigma/memory/` is OUTSIDE the
git repo (the assistant's persistent memory store) — acceptance is file-existence + content, NOT
a git commit.

MEMORY.md ALREADY HAS a "## Design System (Neon Tokyo, shadcn-vue)" section (~line 194) — add the
new pointer there as a bullet; do NOT create a duplicate section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Design System" subsection to repo-root CLAUDE.md (DS-GOV-03 + DS-NF-06)</name>
  <read_first>
    - CLAUDE.md (the file being modified — read the "Code Conventions" area ~lines 70–181 to find the slot near frontend/conventions guidance; the new "## Design System" subsection goes near there, NOT duplicated)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (the canonical doc to point at — read the "Lint gate (enforced)" section so labels MATCH)
    - frontend/web/scripts/design-system-lint.sh (the 3 enforced rules verbatim)
  </read_first>
  <action>
    Add a concise "## Design System" subsection to repo-root CLAUDE.md, placed near the existing frontend/conventions guidance in the Code Conventions area. Content per the 06-CONTEXT.md decisions and the `<interfaces>` block — BRIEF, pointing at `frontend/web/src/styles/DESIGN-SYSTEM.md` as the canonical reference; do NOT duplicate the full DESIGN-SYSTEM.md.

    The subsection MUST (per SC#3, the load-bearing consistency rule):
    - State the canonical reference path `frontend/web/src/styles/DESIGN-SYSTEM.md` and the lint-gate command (`make lint-frontend` / `make redeploy-web`, via `frontend/web/scripts/design-system-lint.sh`; `--selftest` proves the fail-path; allowlist `scripts/design-system-allowlist.txt`).
    - LABEL AS LINT-ENFORCED the 3 color/token rules verbatim from the gate: (1) no off-palette Tailwind color classes — and name the EXEMPT brand/provider hues cyan/pink/orange/rose/indigo/teal/lime; (2) no non-allowlisted hardcoded hex; (3) no deprecated `var(--ink|--accent|--pink)` brand-alias usages. State that these FAIL THE BUILD.
    - LABEL AS GOVERNANCE-ONLY (human/AI-followed, NOT build-enforced) the structural rules: reuse `@/components/ui` primitives before building new; only `font-medium`/`font-semibold` weights; padding scale; `cva` variants.
    - Include the in-browser-verify STANDING rule (DS-NF-06): verify any rendered change in a real browser at desktop + mobile — jsdom/vitest cannot catch Tailwind-v4 cascade bugs.
    - Note the escape-hatch (add a justified `path:hex:reason` allowlist line; don't disable the gate) and that `--accent` is the shadcn hover surface since 05-04 (use `--brand-cyan` for brand cyan).

    Per the project's no-days convention, keep prose terse/imperative; no time-effort units.
  </action>
  <verify>
    <automated>cd /data/animeenigma && grep -q "## Design System" CLAUDE.md && grep -q "DESIGN-SYSTEM.md" CLAUDE.md && grep -q "design-system-lint.sh" CLAUDE.md && grep -qE "lint-frontend|redeploy-web" CLAUDE.md && grep -qiE "enforce" CLAUDE.md && grep -qiE "governance-only|not (build-)?enforced" CLAUDE.md && grep -qiE "cyan|brand" CLAUDE.md && grep -qiE "browser" CLAUDE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains a "## Design System" heading.
    - It references `frontend/web/src/styles/DESIGN-SYSTEM.md` and the lint-gate command (`design-system-lint.sh` + `make lint-frontend`/`make redeploy-web`).
    - The 3 enforced rules are present and labeled enforced; the structural rules are present and labeled governance-only (string-detectable: an "enforced" marker AND a "governance-only"/"not enforced" marker both appear).
    - The exempt brand/provider hues (cyan/pink/orange/rose/indigo/teal/lime) are named under Rule 1.
    - The in-browser-verify (DS-NF-06) standing rule appears.
    - Does NOT duplicate the full DESIGN-SYSTEM.md (no token-tier tables copied in).
  </acceptance_criteria>
  <done>CLAUDE.md has a concise, correctly-labeled Design System subsection pointing at the canonical doc + lint gate; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write the project-memory governance entry + MEMORY.md pointer (DS-GOV-03)</name>
  <read_first>
    - /root/.claude/projects/-data-animeenigma/memory/MEMORY.md (the index — read the existing "## Design System (Neon Tokyo, shadcn-vue)" section ~line 194 where the new bullet goes; do NOT create a duplicate section)
    - frontend/web/src/styles/DESIGN-SYSTEM.md (the doc to point at)
    - frontend/web/scripts/design-system-lint.sh (the enforced rules to mirror in the memory body)
  </read_first>
  <action>
    Write ONE focused project-memory file at `/root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md` (this path is OUTSIDE the git repo — the assistant's persistent memory store; acceptance is file-existence + content, NOT a git commit).

    Follow the project-memory format from the `<interfaces>` block:
    - Frontmatter with `name`, a recall-friendly `description`, and `metadata.type: project` (or `reference`).
    - Body capturing the 4 rule themes: (a) use semantic tokens, never hardcode hex/off-palette color classes — brand/provider hues cyan/pink/orange/rose/indigo/teal/lime are the documented lint-exempt exception, genuinely-novel hex goes in `scripts/design-system-allowlist.txt` as `path:hex:reason`; (b) reuse `@/components/ui` primitives before building new (governance-only); (c) verify visual changes in a real browser at desktop + mobile (DS-NF-06; jsdom can't catch Tailwind-v4 cascade bugs); (d) the gate is real — `make lint-frontend` / `make redeploy-web` fail the build on the 3 color/token rules; `--selftest` proves the fail-path; don't disable it, add a justified allowlist entry instead.
    - The ENFORCED-vs-GOVERNANCE-ONLY distinction must be explicit in the body (the 3 lint-enforced rules vs the structural governance-only rules), so a reader never mistakes one for the other.
    - A pointer to `frontend/web/src/styles/DESIGN-SYSTEM.md` as the canonical reference.
    - `[[...]]` cross-links to related memories where natural (e.g. `[[reference_tailwind_v4_css_cascade]]` for the cascade footgun, `[[feedback_native_rightclick_dropdown_triggers]]` for the primitives-over-custom rule).

    Then add a ONE-LINE pointer bullet to MEMORY.md under the EXISTING "## Design System (Neon Tokyo, shadcn-vue)" section (~line 194) referencing `project_design_system_governance.md`. Do NOT create a duplicate section.

    Keep terse; no time-effort units (no-days convention).
  </action>
  <verify>
    <automated>test -f /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -qiE "^name:|^description:|type:" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -q "DESIGN-SYSTEM.md" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -qiE "enforce" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -qiE "governance-only|not (build-)?enforced" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -qiE "browser" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -q "design-system-allowlist" /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md && grep -q "project_design_system_governance" /root/.claude/projects/-data-animeenigma/memory/MEMORY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `/root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md` exists with frontmatter (name + description + metadata.type).
    - Body covers all 4 rule themes (tokens-not-hardcode / reuse-primitives / verify-in-browser / the gate-is-real + allowlist escape-hatch).
    - Body makes the enforced-vs-governance-only distinction explicit.
    - Body points at `frontend/web/src/styles/DESIGN-SYSTEM.md`.
    - MEMORY.md has a one-line pointer to `project_design_system_governance.md` under the existing Design System section (no duplicate section created).
  </acceptance_criteria>
  <done>Governance memory file exists with correct frontmatter + 4 rule themes + enforced/governance-only distinction + DESIGN-SYSTEM.md pointer; MEMORY.md indexes it; grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Consistency verification (SC#3) + milestone-shippable ack (DS-NF-05)</name>
  <read_first>
    - frontend/web/scripts/design-system-lint.sh (source of truth — grep the enforced RULE 1/2/3 out of it)
    - CLAUDE.md (the "## Design System" subsection from Task 1)
    - /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md (from Task 2)
  </read_first>
  <action>
    Verify the consistency invariant SC#3: every lint-ENFORCED rule is documented as enforced on both governance surfaces, and no GOVERNANCE-ONLY structural rule is mislabeled as enforced.

    Concretely:
    - Confirm the 3 enforced rule families from `design-system-lint.sh` are each represented in CLAUDE.md's Design System section, labeled enforced: (1) off-palette Tailwind color classes; (2) hardcoded hex outside the allowlist; (3) deprecated `var(--ink|--accent|--pink)` brand-alias usages.
    - Confirm the structural rules (reuse `@/components/ui` primitives; `font-medium`/`font-semibold` weights; padding scale; `cva` variants) are present and labeled governance-only — NOT presented as build-enforced.
    - Confirm the brand/provider exempt hues (cyan/pink/orange/rose/indigo/teal/lime) are named so a reader doesn't think off-palette is merely advisory or that brand hues are forbidden.

    Then acknowledge DS-NF-05 (every phase independently shippable): add a one-line note — in this plan's SUMMARY and/or the workstream ROADMAP Phase 6 record — that DS-NF-05 was satisfied by each phase's green build (tsc + vitest + 5-surface in-browser smoke), needing no new machinery. No code or gate changes for DS-NF-05.

    This is a verification/audit task — make the assertions concrete (grep/string-contains), never subjective. If any enforced rule is undocumented or any governance-only rule is mislabeled as enforced, fix the offending surface (Task 1 / Task 2 output) until the assertions pass.
  </action>
  <verify>
    <automated>cd /data/animeenigma && \
      grep -qE 'off-palette' frontend/web/scripts/design-system-lint.sh && \
      grep -qiE 'off-palette' CLAUDE.md && \
      grep -qiE 'hex' CLAUDE.md && \
      grep -qE 'var\(--ink|--accent|--pink|brand-alias' CLAUDE.md && \
      grep -qiE 'cyan|exempt|brand' CLAUDE.md && \
      grep -qiE 'governance-only|not (build-)?enforced' CLAUDE.md && \
      grep -qiE 'primitive' CLAUDE.md && \
      echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The 3 enforced rule names (off-palette classes, hardcoded hex, deprecated brand-alias) each appear in CLAUDE.md's Design System section, labeled enforced.
    - The structural rules (reuse primitives, weights, padding, cva) appear labeled governance-only — a "governance-only"/"not enforced" marker is string-detectable.
    - The exempt brand/provider hues are named.
    - DS-NF-05 is acknowledged in one line (SUMMARY and/or ROADMAP Phase 6 record) as satisfied by per-phase green builds.
    - No enforced rule is undocumented; no governance-only rule is mislabeled as enforced.
  </acceptance_criteria>
  <done>SC#3 consistency holds (enforced labeled enforced; governance-only labeled governance-only; exempt hues named); DS-NF-05 acknowledged in one line; grep gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase touches only repo documentation (`CLAUDE.md`) and the assistant's local project-memory store. No runtime code, no user input, no network surface, no new dependency. There is no new trust boundary crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Governance text drifts from the enforced lint rule (docs say a rule is enforced when it isn't, or omits one that is) | mitigate | Task 3 greps the enforced rules out of `design-system-lint.sh` and asserts the CLAUDE.md + memory text matches (SC#3); concrete string-contains assertions, not subjective. |
| T-06-02 | Information disclosure | Memory file written outside the git repo (`/root/.claude/.../memory/`) | accept | This is the assistant's own persistent store by design; no secrets are written (only public design-system rules + repo paths already in CLAUDE.md). |
</threat_model>

<verification>
- `grep "## Design System" CLAUDE.md` matches.
- `test -f /root/.claude/projects/-data-animeenigma/memory/project_design_system_governance.md` succeeds.
- `grep project_design_system_governance /root/.claude/projects/-data-animeenigma/memory/MEMORY.md` matches.
- CLAUDE.md labels the 3 lint rules enforced and the structural rules governance-only (SC#3).
- DS-NF-05 acknowledged in one line; DS-NF-06 present as a governance bullet on both surfaces.
- No full DESIGN-SYSTEM.md duplication on either surface — both point at the canonical doc.
</verification>

<success_criteria>
1. A memory entry captures the 4 rule themes (use tokens / never hardcode / reuse `ui/` primitives before building new / verify visual changes in-browser) with a pointer to `DESIGN-SYSTEM.md`. (SC#1)
2. `CLAUDE.md` has a Design System subsection. (SC#2)
3. The governance text matches the enforced lint rule — the 3 color/token rules labeled enforced; the structural rules labeled governance-only; exempt brand/provider hues named. (SC#3)
4. DS-NF-06 is a governance bullet on both surfaces; DS-NF-05 is acknowledged in one line.
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/06-governance-into-memory/06-01-SUMMARY.md`.
Note: the project-memory file lives OUTSIDE the git repo — record it in the SUMMARY as a created artifact (file-existence + content), not a committed file.
</output>
