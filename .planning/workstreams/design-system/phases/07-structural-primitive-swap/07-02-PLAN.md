---
phase: 07-structural-primitive-swap
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/web/src/components/player/SubtitleSettingsMenu.vue
  - frontend/web/src/components/home/spotlight/CarouselDots.vue
autonomous: false
requirements: [DS-MIGRATE-06, DS-MIGRATE-01]

must_haves:
  truths:
    - "Each raw <button> in SubtitleSettingsMenu.vue is either swapped to <Button> (variant+size, all behavior + data-test attrs preserved) or kept bespoke with a recorded reason"
    - "CarouselDots dot pills are adjudicated; the expected outcome is KEEP bespoke (the pill geometry/scoped-CSS/spec contract does not fit the Button variant API) with a recorded reason"
    - "Phase-close verification passes: full vitest (no NEW failures), vue-tsc exit 0, vite build clean, make lint-design exit 0"
    - "Affected-surface in-browser smoke is captured as a HUMAN-UAT checkpoint (DS-NF-06)"
  artifacts:
    - path: "frontend/web/src/components/player/SubtitleSettingsMenu.vue"
      provides: "Subtitle timing menu with the gear + 4 nudge + reset buttons adjudicated against <Button>"
    - path: "frontend/web/src/components/home/spotlight/CarouselDots.vue"
      provides: "Carousel dot indicators, adjudicated (expected: kept bespoke + documented)"
  key_links:
    - from: "frontend/web/src/components/player/SubtitleSettingsMenu.vue"
      to: "@/components/ui Button"
      via: "import { Button } from '@/components/ui' (only if any swap is made)"
      pattern: "data-test=\"sub-timing-gear\""
---

<objective>
Adjudicate and (where the API fits) swap the raw `<button>` elements in the player subtitle-timing
menu and the spotlight carousel dots, then run the PHASE-CLOSE verification (full vitest + vue-tsc +
vite build + lint-design) and gate on a HUMAN-UAT in-browser smoke of the affected surfaces.

This plan is the structural close-out of DS-MIGRATE-06 + DS-MIGRATE-01. It runs in Wave 2 (after
07-01) so the full-suite verification sees ALL of the phase's swaps at once.

CarouselDots is expected to be a **keep + document** case: its dot pills are tiny geometry-overridden
controls whose entire look comes from scoped CSS (26×4 / 36×4 pill, `!important` glass backgrounds,
cyan glow) and whose spec asserts on raw `bg-white/10` / `bg-purple-*` / `scale-110` classes — a
`<Button>` swap would break both the visuals and the spec. Adjudicate honestly and document the keep.

Purpose: finish the structural migration; prove zero behavioral/visual regression.
Output: both files adjudicated; full phase-close verification green; HUMAN-UAT smoke gated.

Metrics (no-days convention): **UXΔ = +1 (Better)** | **CDI = 0.05 * 8** | **MVQ = Sprite 80%/85%**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/design-system/phases/07-structural-primitive-swap/07-CONTEXT.md
@frontend/web/src/styles/DESIGN-SYSTEM.md

<interfaces>
<!-- Button API (same as 07-01). Use directly. -->
From frontend/web/src/components/ui/button-variants.ts:
```
variant: default(cyan) | brand(pink) | ghost(bg-white/5,border,rounded-lg) | outline | destructive
size:    sm(px-3 py-1.5 text-sm) | md(px-6 py-3) | lg | icon(h-10 w-10 p-0)
```
From frontend/web/src/components/ui/Button.vue:
```
Props: variant, size, type(default 'button'), href, disabled, loading, fullWidth, class(merged via cn()).
Forwards data-*, aria-*, :aria-expanded, :title, @click onto the rendered <button> root.
```

SubtitleSettingsMenu spec contract (frontend/web/src/components/player/SubtitleSettingsMenu.spec.ts):
asserts via `data-test` selectors ONLY (sub-timing-gear, nudge-minus-1, nudge-minus-01,
nudge-plus-01, nudge-plus-1, reset, readout) + the gear's `disabled` attribute. Button forwards
`data-test` + `:disabled`, so swaps that keep those attributes keep the spec green. The spec does
NOT assert on class names — so visual fidelity is a manual/read concern, not a spec concern here.

CarouselDots spec contract (frontend/web/src/components/home/spotlight/CarouselDots.spec.ts):
asserts on `[data-testid="spotlight-dots"] button`, `aria-current`, `aria-label`, `title`, AND raw
classes `bg-white/10`, `bg-purple-*`, `scale-110`. Plus scoped CSS in CarouselDots.vue overrides
button geometry to a 26×4 pill with `!important` glass backgrounds. A `<Button>` swap would change
the rendered element's class set and break these assertions → strong signal to KEEP bespoke.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjudicate + swap the SubtitleSettingsMenu raw buttons; adjudicate CarouselDots (expect keep)</name>
  <files>frontend/web/src/components/player/SubtitleSettingsMenu.vue, frontend/web/src/components/home/spotlight/CarouselDots.vue</files>
  <read_first>
    - frontend/web/src/components/player/SubtitleSettingsMenu.vue (5 raw `<button>`: the gear toggle L3 with `:disabled`/`:title`/`:aria-label`/`:aria-expanded`/`aria-haspopup`/`@click` + inline svg + label; four nudge buttons L29-33 `data-test=nudge-*` `@click="nudge(±n)"`; reset L35 `data-test=reset` underline link `@click="reset()"`)
    - frontend/web/src/components/player/SubtitleSettingsMenu.spec.ts (the data-test-based contract — must stay green)
    - frontend/web/src/components/home/spotlight/CarouselDots.vue (the dot-pill `<button v-for>` L39 + its scoped CSS geometry overrides)
    - frontend/web/src/components/home/spotlight/CarouselDots.spec.ts (asserts raw classes bg-white/10, bg-purple-*, scale-110)
    - frontend/web/src/components/ui/Button.vue + button-variants.ts (the API)
    - frontend/web/src/components/layout/Navbar.vue (existing `<Button>` consumer idiom)
  </read_first>
  <action>
    SubtitleSettingsMenu.vue — adjudicate EACH of the 5 buttons per 07-CONTEXT:

    Gear toggle (L3): genuine button — `inline-flex … px-4 py-2 rounded-md bg-white/10 hover:bg-white/15
    border`, `:disabled="!hasActiveSub"`, `:title`, `:aria-label`, `:aria-expanded`, `aria-haspopup`,
    `@click`, with an inline svg + label. Close to `ghost` (bg-white/5 vs bg-white/10). ADJUDICATE: if
    `<Button variant="ghost" size="sm">` (+ a `class` to nudge bg-white/10 + rounded-md if needed)
    reproduces the look with zero visible diff AND forwards `data-test="sub-timing-gear"`, `:disabled`,
    `:title`, `:aria-label`, `:aria-expanded`, `aria-haspopup`, `@click`, swap. The spec asserts the
    `disabled` attribute renders when `!hasActiveSub` — verify Button forwards `:disabled` (it does:
    `disabled || loading`). If the bg/radius can't match without a visible diff, KEEP + document.

    Four nudge buttons (L29-33): tiny stepper buttons `px-2 py-1 rounded bg-white/10 hover:bg-white/20
    text-sm focus-visible:ring-cyan-400/60`, each with a `data-test` + `@click="nudge(±n)"`. These are
    very small chrome steppers. ADJUDICATE: Button's smallest size `sm` is `px-3 py-1.5` (larger than
    `px-2 py-1`), so a swap would enlarge them unless overridden via `class`. If `<Button variant="ghost"
    size="sm" class="px-2 py-1 …">` reproduces the exact compact look with zero diff AND keeps each
    `data-test` + `@click`, swap; otherwise KEEP + document (compact player-chrome steppers sized below
    the Button size scale — a documented, expected keep).

    Reset (L35): underlined text link `text-xs text-white/50 hover:text-white/80 underline`,
    `data-test="reset"`, `@click="reset()"`. Link-styled — no Button text-only variant. KEEP + document.

    For EVERY swap in this file: import `{ Button }` from `@/components/ui`; PRESERVE every `data-test`,
    `@click`, `:disabled`, `:title`, `:aria-*`, `aria-haspopup`. Do NOT change the popover `<div>` or
    the readout `<span>`. After editing, the spec (data-test-based) MUST stay green.

    CarouselDots.vue — adjudicate the dot-pill `<button v-for>` (L39):
    The dot is a 26×4 / 36×4 pill whose geometry is forced by scoped CSS (`.dot-pill { width:26px;
    height:4px; border-radius:999px; padding:0; … }` + `!important` glass backgrounds + cyan glow),
    carries `:aria-label`/`:aria-current`/`:title`/`@click="$emit('goto', i)"`, and its spec asserts on
    the raw Tailwind classes `bg-white/10`, `bg-purple-*`, `scale-110`. A `<Button>` (min size `icon` =
    40×40, `rounded-xl`, variant bg classes) cannot express a 4px pill and would (a) change the rendered
    class set → break the spec, (b) change the visuals. This is the canonical "specialized control the
    Button API doesn't model" case. EXPECTED OUTCOME: KEEP bespoke — add a one-line inline comment near
    the `<button>` (e.g. "Kept bespoke: 4px pill geometry + scoped-CSS look + spec class-contract don't
    fit the Button variant/size API") and record it in the SUMMARY. Do NOT force a swap. (If, against
    expectation, you find a zero-diff spec-safe swap, document the reasoning — but keeping is correct.)

    No new off-palette color, hex, or `var(--ink|--accent|--pink)` alias in either file.
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx vitest run src/components/player/SubtitleSettingsMenu.spec.ts src/components/home/spotlight/CarouselDots.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run` on both co-located specs is GREEN (data-test + class-contract assertions intact).
    - `bunx vue-tsc --noEmit` reports zero errors in both files.
    - Every swapped button preserves its `data-test`, `@click`, `:disabled`, `:aria-*`, `aria-haspopup`/`aria-current`, `:title`.
    - CarouselDots dots: kept bespoke with an inline one-line reason comment (or, if swapped against expectation, the zero-diff + spec-green reasoning is documented).
    - `cd frontend/web && bash scripts/design-system-lint.sh` exits 0.
  </acceptance_criteria>
  <done>Both files adjudicated; SubtitleSettingsMenu swaps preserve data-test/behavior; CarouselDots kept+documented; both specs green; per-file tsc clean; lint-design clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-close verification — full vitest + vue-tsc + vite build + lint-design</name>
  <files>(no source edits — verification only; fix any regression the swaps introduced)</files>
  <read_first>
    - .planning/workstreams/design-system/phases/07-structural-primitive-swap/07-CONTEXT.md (the phase-close verification list)
  </read_first>
  <action>
    Run the full phase-close gate over the WHOLE phase's swaps (07-01 + 07-02 are both landed by now):

    1. `cd frontend/web && bunx vue-tsc --noEmit` — MUST exit 0.
    2. `cd frontend/web && bunx vitest run` — MUST be green with NO NEW failures. The ONLY accepted
       pre-existing failure is AnimeContextMenu.spec.ts:227 (known, documented in 07-CONTEXT). Any
       other red is a regression introduced by a swap → fix it (re-adjudicate the offending button:
       if a swap broke a behavior/spec and can't be made zero-diff, revert that one swap to bespoke +
       document the reason). Re-run until green.
    3. `cd frontend/web && bunx vite build` — MUST complete clean (no type/import/build errors).
    4. `cd frontend/web && bash scripts/design-system-lint.sh` — MUST exit 0 (no off-palette/hex/alias
       reintroduced by any swap across all 5 phase files).

    Record each command's pass/fail + the vitest summary line (e.g. "N passed, 1 known pre-existing
    failure") in the SUMMARY. Do NOT redeploy here (HUMAN-UAT checkpoint follows).
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx vue-tsc --noEmit && bunx vitest run 2>&1 | tail -15 && bunx vite build 2>&1 | tail -8 && bash scripts/design-system-lint.sh && echo "PHASE-CLOSE-GATE-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vue-tsc --noEmit` exit 0.
    - `bunx vitest run` green — no NEW failures beyond the known AnimeContextMenu.spec.ts:227.
    - `bunx vite build` completes clean.
    - `bash scripts/design-system-lint.sh` exit 0.
    - The terminal prints `PHASE-CLOSE-GATE-PASS`.
  </acceptance_criteria>
  <done>All four phase-close commands pass; the verifier prints PHASE-CLOSE-GATE-PASS; results recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: HUMAN-UAT in-browser smoke of affected surfaces (DS-NF-06)</name>
  <action>Deploy via `make redeploy-web` and in-browser-smoke the affected surfaces (Browse, Themes, Home spotlight dots, a JP-subtitle player) at desktop + mobile widths; confirm zero rendered/behavioral regression on the swapped/kept buttons. See <how-to-verify> for the step-by-step.</action>
  <what-built>
    Structural `<button>`→`<Button>` adjudication landed across 5 files (Themes.vue, Browse.vue,
    AnimeCard.vue in 07-01; SubtitleSettingsMenu.vue, CarouselDots.vue here). Each raw button was
    either swapped to the `@/components/ui` `<Button>` primitive (variant/size, behavior + a11y
    preserved) or kept bespoke + documented. Full vitest + vue-tsc + vite build + lint-design are
    green. Per DS-NF-06, the in-browser smoke of the affected surfaces is deferred to this HUMAN-UAT
    gate (jsdom can't catch cascade bugs).
  </what-built>
  <how-to-verify>
    Deploy the web frontend (`make redeploy-web` — runs i18n-lint + lint-design + type-check first),
    then in a real browser at desktop + mobile widths confirm NO rendered regression on the affected
    surfaces:
    1. Browse/catalog — the mobile filter toggle (open the drawer + close it via the X), recent-search
       chips, the "search/refresh on Shikimori" link buttons, the two existing outline retry/search
       buttons. All click + focus correctly, look unchanged.
    2. Themes view — the admin sync button (as admin: idle cyan-soft vs syncing warning state), the
       segmented type-filter row (active vs inactive), the Retry button on an error state.
    3. Home spotlight carousel — the dot indicators below the card: active dot is the wider glowing
       pill, inactive dots are the thin glass pills, clicking a dot navigates. (Expected unchanged —
       kept bespoke.)
    4. A player surface with JP subtitles — open the subtitle-timing gear, nudge ±0.1 / ±1, read the
       offset readout, reset. Gear is disabled when no active sub.
    Confirm focus rings, hover states, and disabled states all behave as before.
  </how-to-verify>
  <resume-signal>Type "approved" (or "HUMAN-UAT" in auto mode) once the affected surfaces render + behave with no regression; otherwise describe the visual/behavioral diff so the offending swap can be reverted to bespoke.</resume-signal>
</task>

</tasks>

<verification>
- Phase-close gate (Task 2) green: vue-tsc exit 0, vitest no-new-failures, vite build clean, lint-design exit 0.
- Co-located specs (SubtitleSettingsMenu, CarouselDots) green.
- HUMAN-UAT in-browser smoke of the affected surfaces approved (DS-NF-06).
- Staging discipline: stage ONLY `components/player/SubtitleSettingsMenu.vue` + `components/home/spotlight/CarouselDots.vue` by explicit path. Do NOT `git add -A` (pre-existing uncommitted changes — ActivityFeed.vue analytics, scraper Go files, etc. — must stay untouched).
</verification>

<success_criteria>
- Each remaining hand-rolled `<button>` in the player + spotlight files is EITHER swapped to `<Button>` (correct variant/size, events + a11y preserved, visually identical) OR documented bespoke with a recorded reason — CarouselDots kept + documented as expected. (SC#1)
- No behavioral/visual regression: `bunx vue-tsc --noEmit` + `bunx vite build` clean; `bunx vitest run` green (no NEW failures beyond AnimeContextMenu.spec.ts:227); affected-surface in-browser smoke → HUMAN-UAT approved. (SC#2)
- `make lint-design` exit 0 — no off-palette/hex/alias reintroduced. (SC#3)
- DS-MIGRATE-06 + DS-MIGRATE-01 closable: residual bespoke controls (CarouselDots dots, link-styled buttons, compact steppers if kept) explicitly accepted as governance-only with recorded reasons. (SC#4)
</success_criteria>

<output>
After completion, create `.planning/workstreams/design-system/phases/07-structural-primitive-swap/07-02-SUMMARY.md`.
The SUMMARY MUST:
- Include a per-button adjudication table (file:line → swapped/kept → variant or reason) for SubtitleSettingsMenu + CarouselDots.
- Record the phase-close verification results (the four commands + the vitest summary line).
- State the DS-MIGRATE-06 + DS-MIGRATE-01 closure verdict (✅ or residual-bespoke-accepted-as-governance-only) consolidating both 07-01 + 07-02 adjudications.
</output>
