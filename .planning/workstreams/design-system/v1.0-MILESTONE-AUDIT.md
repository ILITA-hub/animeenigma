---
milestone: v1.0
workstream: design-system
audited: 2026-06-03
status: passed
scores:
  requirements: 32/32
  phases: 6/6
  integration: pass
  flows: pass (build + lint gate + 831/1 tests green)
gaps:
  requirements:
    - id: "DS-MIGRATE-06"
      status: "partial"
      phase: "04-05"
      verification_status: "not-swept"
      evidence: "Hand-rolled buttons/cards/badges replaced with ui/ primitives WHERE THEY EXIST: 21 <Button> primitive usages across 11 files, but 10 raw <button> elements remain. Phases 4-5 were deliberately COLOR/TOKEN-ONLY (value-preserving, zero behavioral change); no structural primitive-swap pass was planned or executed. Phase 6 reclassified 'reuse ui/ primitives before building new' as GOVERNANCE-ONLY (not lint-enforced), effectively descoping a full mechanical swap to an ongoing guideline."
    - id: "DS-MIGRATE-01"
      status: "partial"
      phase: "04"
      verification_status: "color-passed / primitive-partial"
      evidence: "High-traffic surfaces (Home, Browse, Watch/player, nav, anime detail): the 'ONLY tokens, no off-palette, no hardcoded hex' half is FULLY done + verified (Phase 4 VERIFICATION 5/5, repo-wide off-palette grep zero). The 'use ui/ primitives' half is partial — same root as DS-MIGRATE-06."
tech_debt:
  - phase: 04-high-traffic-surface-migration
    items:
      - "HUMAN-UAT: 5-surface in-browser visual smoke (desktop+mobile) deferred — surfaces at end-of-milestone redeploy-web deploy."
  - phase: 05-tail-sweep-lint-enforcement
    items:
      - "HUMAN-UAT (05-04): --accent flip in-browser smoke + spotlight e2e canary deferred (canary needs live dev server; couldn't run headless)."
      - "Pre-existing (NOT this milestone): AnimeContextMenu.spec.ts:227 fails (Phase-3 Reka DropdownMenu anchored-mode reference-prop); logged in 04-deferred-items.md."
      - "Pre-existing (other workstream): ActivityFeed.vue carries uncommitted analytics-workstream changes; its DS color repoint was committed via stash-isolation, analytics changes left untouched in the working tree."
---

# Milestone v1.0 — Design System Consolidation — Audit

**Status:** passed (32/32 — the 2 partial structural requirements were closed by the post-audit Phase 7: 1 swap + 9 documented justified keeps, user-accepted 2026-06-03)
**Requirements:** 30/32 satisfied · **Phases:** 6/6 complete · **Build/lint/tests:** green

## Verdict in one line

The milestone fully delivered its **core thesis** — one consolidated token vocabulary, shadcn-vue
primitives available behind stable import paths, every surface migrated to tokens-only, a
**build-failing lint gate** locking it in, and governance written into memory + CLAUDE.md. The only
shortfall is the **structural** "swap every hand-rolled `<button>` for `<Button>`" sweep
(DS-MIGRATE-06 + the primitive half of DS-MIGRATE-01), which Phases 4-5 deliberately excluded
(they were color/token-only) and which Phase 6 reclassified as a **governance-only** guideline.

## Requirements coverage (3-source: codebase + SUMMARY + traceability)

| Group | Status |
|-------|--------|
| DS-FOUND-01..07, DS-NF-01/02 (Phase 1 tokens) | ✅ all |
| DS-LIB-01..08, DS-NF-03/04 (Phases 2-3 shadcn-vue adoption) | ✅ all — codebase-confirmed (cn() + reka-ui/cva/clsx/tailwind-merge; Button/Card/Badge/Input/Select/Modal-as-Dialog/Tabs/DropdownMenu/Tooltip/Popover/Switch/Checkbox; ContextMenu retired; no extra deps; vue-tsc clean). **Traceability checkboxes were stale from prior sessions — corrected during this audit.** |
| DS-MIGRATE-02/03/04/05 (color/token migration + --accent flip) | ✅ all — repo-wide off-palette/hex/alias grep zero; `--accent: var(--elevated)` |
| DS-MIGRATE-01 (hi-traffic tokens+primitives) | ⚠ partial — tokens ✅, primitives partial |
| DS-MIGRATE-06 (hand-rolled → primitives) | ⚠ partial — 21 `<Button>` usages / 10 raw `<button>` remain |
| DS-GOV-01/02/03, DS-NF-05/06 (lint gate + governance) | ✅ all — gate live + wired to redeploy-web; `--selftest` proves fail-path; governance in CLAUDE.md + memory |

## Phases

| Phase | Status | Verification |
|-------|--------|--------------|
| 1 Token Foundation | ✅ | (pre-workstream; SUMMARY) |
| 2 shadcn-vue Install + Button/Card | ✅ | (pre-workstream; SUMMARY) |
| 3 Primitive Set Swap | ✅ | (pre-workstream; SUMMARY) |
| 4 High-Traffic Surface Migration | ✅ | 5/5 must-haves; human_needed (visual smoke deferred) |
| 5 Tail Sweep + Lint Enforcement | ✅ | 5/5 must-haves; human_needed (visual smoke + e2e deferred) |
| 6 Governance into Memory | ✅ | passed (3/3) |

## Integration / E2E

- Tokens → primitives → migrated surfaces → lint gate → governance all wire together: `make lint-design` exits 0 on the clean tree, `--selftest` proves the `bg-red-500` fail-path, gate is a prerequisite of `make lint-frontend` AND `make redeploy-web`.
- `bunx vue-tsc --noEmit` exit 0; `bunx vite build` clean; `bunx vitest run` 831 pass / 1 pre-existing fail (AnimeContextMenu.spec.ts:227, not this milestone).

## The decision the gap needs

DS-MIGRATE-06 / DS-MIGRATE-01-primitive can be **accepted as a deliberate descope** (consistent with
Phase 6 making primitive-reuse governance-only — the de-facto project decision), or **closed** with a
follow-up structural-swap phase. This is a user call.
