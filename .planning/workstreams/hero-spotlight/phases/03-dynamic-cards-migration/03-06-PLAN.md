---
phase: 03-dynamic-cards-migration
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified:
  - frontend/web/src/views/Home.vue
  - CLAUDE.md
autonomous: true
requirements:
  - HSB-MIG-01
  - HSB-NF-05
tags: [hero-spotlight, migration, docs, home, phase-3]
must_haves:
  truths:
    - "Home.vue has zero references to trendingRecs / trendingProgress / trendingLoading / rowLabelKey / reasonI18nKey / onRecClick / dominantSignalKey / trendingIds"
    - "Home.vue template no longer contains the legacy trending row markup or its skeleton"
    - "Home.vue still imports + mounts <HeroSpotlightBlock />"
    - "All unused imports removed (useRecs, useAnimeProgress, emitRecClick, PinSource, RecItem if not referenced elsewhere in Home.vue)"
    - "CLAUDE.md has a new 'Adding a Spotlight Card Type' section under Common Tasks"
    - "Feature flags SPOTLIGHT_ENABLED + VITE_HERO_SPOTLIGHT_ENABLED are NOT removed (HSB-MIG-02 — they stay)"
  artifacts:
    - path: "frontend/web/src/views/Home.vue"
      provides: "Home view with trendingRecs row removed; spotlight block now directly above 3-column grid"
    - path: "CLAUDE.md"
      provides: "New 'Adding a Spotlight Card Type' section under Common Tasks"
      contains: "Adding a Spotlight Card Type"
  key_links:
    - from: "Home.vue"
      to: "HeroSpotlightBlock.vue"
      via: "<HeroSpotlightBlock />"
      pattern: "<HeroSpotlightBlock"
---

<objective>
Remove the legacy `trendingRecs` row from `Home.vue` (HSB-MIG-01), now that `personal_pick` inside the spotlight aggregator surfaces the same trending recommendations. Add the "Adding a Spotlight Card Type" reference section to `CLAUDE.md` under "Common Tasks" (HSB-NF-05).

Critical constraints:
- The backend `/api/users/recs` endpoint STAYS — it now powers `personal_pick`.
- Feature flags `SPOTLIGHT_ENABLED` + `VITE_HERO_SPOTLIGHT_ENABLED` STAY — kill switches for one release (HSB-MIG-02 explicitly defers removal).
- Other Home.vue regions (search, 3-column grid, schedule link) are UNTOUCHED.

Purpose: This is the migration cut-over. After this plan ships and is verified by Plan 07, the rotating spotlight FULLY REPLACES the static trending row. CLAUDE.md gains the cross-reference contributors need to add a 10th card without re-reading Phase 1-3 plans.

Output:
- `frontend/web/src/views/Home.vue` with the legacy block removed (template + script identifiers).
- `CLAUDE.md` with a new ~30-line "Adding a Spotlight Card Type" subsection under "Common Tasks".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/hero-spotlight/REQUIREMENTS.md
@.planning/workstreams/hero-spotlight/phases/03-dynamic-cards-migration/03-CONTEXT.md
@.planning/workstreams/hero-spotlight/phases/03-dynamic-cards-migration/03-RESEARCH.md

# Files we modify
@frontend/web/src/views/Home.vue
@CLAUDE.md

# Reference — what Plan 05 produced (so CLAUDE.md docs reference real paths)
@frontend/web/src/components/home/spotlight/HeroSpotlightBlock.vue
@frontend/web/src/components/home/spotlight/cards/AnimeOfDayCard.vue
@frontend/web/src/types/spotlight.ts
@services/catalog/internal/service/spotlight/cards/anime_of_day.go

<interfaces>
Current Home.vue trendingRecs region lives between the SystemStatusBanner / HeroSpotlightBlock mount (top of the view) and the 3-column Ongoing/Top/Announced grid. Identifiers verified by grep on the live file:

Template block to DELETE (anchored by the comment `<!-- Trending Now Row (Phase 10` near line ~40) — runs through the closing `</div>` of the v-else-if skeleton (around line ~138).

Setup-script identifiers to DELETE (all are exclusively scoped to the trending row per Research finding):
- `const { recs: rawRecs, isLoading: trendingLoading, rowLabelKey } = useRecs()`
- `const trendingRecs = computed(() => rawRecs.value.slice(0, 20))`
- `const trendingIds = computed(() => trendingRecs.value.map((r) => String(r.anime.id)))`
- `const { progressMap: trendingProgress } = useAnimeProgress(trendingIds)`
- `function onRecClick(item: RecItem): void { ... }`
- `const reasonI18nKey = computed(() => ...)`
- `const dominantSignalKey = computed(() => ...)` (if present)

Imports to remove (only if no other consumer in Home.vue references them):
- `useRecs` composable
- `useAnimeProgress` composable
- `emitRecClick` (used by `onRecClick` impl)
- Types `RecItem`, `PinSource` (if only used inside the trending block)

DOM landmarks BEFORE the legacy row (PRESERVE): `<SystemStatusBanner />`, `<HeroSpotlightBlock />`, `<h1 class="sr-only">AnimeEnigma</h1>`, search bar block, schedule link.

DOM landmarks AFTER the legacy row (PRESERVE): the 3-column Ongoing / Top / Announced grid (`<!-- Three Columns Layout -->`).

The `t` import + `useI18n` setup MUST stay (used by other parts of Home.vue). `getLocalizedTitle` import MUST stay (used by the 3-column grid).

CLAUDE.md current "Common Tasks" section contains: Adding a new anime parser, Adding a new video source, Pinning anime to the top of listings (sort_priority), Database migrations. We append a NEW `### Adding a Spotlight Card Type` subsection between "Database migrations" and the next `##` header.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove trendingRecs markup + setup-script state from Home.vue</name>
  <files>
    frontend/web/src/views/Home.vue
  </files>
  <read_first>
    - frontend/web/src/views/Home.vue (ENTIRE file — we need to know every reference to trendingRecs et al. before deleting)
    - Pre-edit grep (run before editing) to capture every line that mentions any of the legacy identifiers
    - frontend/web/src/components/home/spotlight/HeroSpotlightBlock.vue (confirm the spotlight mount is reachable from Home.vue — preserved)
  </read_first>
  <behavior>
    - Home.vue's `<template>` no longer contains the trending row block or its skeleton.
    - Home.vue's `<script setup>` no longer references any of the legacy identifiers.
    - Imports cleaned: if `useRecs`, `useAnimeProgress`, `emitRecClick`, `RecItem`, `PinSource` were ONLY used in the trending block, they are removed. If any is still used elsewhere, it stays.
    - All other Home.vue functionality (search, schedule, spotlight mount, 3-column grid, system banner) UNCHANGED.
    - `bunx tsc --noEmit -p tsconfig.app.json` exits 0.
    - ESLint reports zero new warnings on the file.
  </behavior>
  <action>
    1. **Phase A — Grep audit (MANDATORY before editing):**
       Run from the repo root: `grep -n 'trendingRecs\|trendingProgress\|trendingLoading\|rowLabelKey\|reasonI18nKey\|onRecClick\|dominantSignalKey\|trendingIds\|useRecs\|useAnimeProgress\|emitRecClick\|RecItem\|PinSource' frontend/web/src/views/Home.vue`. Capture the line numbers. EVERY hit must be accounted for in step 2.

    2. **Phase B — Template removal:**
       In `<template>`, find the comment line that begins `<!-- Trending Now Row (Phase 10` (around line 40-44). Delete from THAT comment through the closing `</div>` of the v-else-if skeleton fallback block (around line 138). The block to delete consists of:
       - The leading comment block (3-5 lines)
       - The `<div v-if="trendingRecs.length > 0" ...>` block (~85 lines including nested elements)
       - The `<div v-else-if="trendingLoading" ...>` skeleton block (~10 lines)
       Leave NOTHING behind from this region. The next remaining markup MUST be the `<!-- Three Columns Layout -->` comment.

    3. **Phase C — Script removal:**
       In `<script setup>`, delete EVERY identifier from the grep output:
       - `function onRecClick(item: RecItem): void { ... }` — delete the whole function (multi-line; identify by `function onRecClick(`)
       - `const { recs: rawRecs, isLoading: trendingLoading, rowLabelKey } = useRecs()` — delete this destructuring statement
       - `const trendingRecs = computed(() => rawRecs.value.slice(0, 20))`
       - `const trendingIds = computed(...)`
       - `const { progressMap: trendingProgress } = useAnimeProgress(trendingIds)`
       - `const reasonI18nKey = computed(...)` (multi-line — delete the whole computed)
       - `const dominantSignalKey = computed(...)` (if present)
       Any other line containing these identifiers — leave the other function alone but verify the identifier isn't used elsewhere. If used elsewhere, the identifier stays; surface the unexpected reference in the SUMMARY for follow-up.

    4. **Phase D — Import cleanup:**
       In the import block at the top of `<script setup>`:
       - Remove `import { useRecs } from '@/composables/useRecs'` IF no remaining code references `useRecs`.
       - Remove `import { useAnimeProgress } from '@/composables/useAnimeProgress'` IF no remaining code references it.
       - Remove `import { emitRecClick } from '@/...'` IF no remaining code references it.
       - Remove `import type { RecItem, PinSource } from '@/types/recs'` IF no remaining code references these types.
       Use a follow-up grep AFTER the script removal to confirm before deleting each import.

    5. **Phase E — Verification:**
       Run a post-edit grep — output MUST be empty. Run `bunx tsc --noEmit -p tsconfig.app.json`, `bunx eslint src/views/Home.vue`, and grep for `<HeroSpotlightBlock` to confirm at least 1 hit. All four checks must pass.

    6. **Phase F — Existing Home tests (if any) must still pass:**
       If `src/views/__tests__/Home.spec.ts` exists, run it via Vitest. If absent, skip — Plan 07's Playwright e2e covers the migration check.

    Per HSB-MIG-01 + Research finding 1 (`/api/users/recs` stays — only the frontend row is removed).
  </action>
  <verify>
    <automated>cd /data/animeenigma && test "$(grep -c 'trendingRecs\|trendingProgress\|trendingLoading\|rowLabelKey\|reasonI18nKey\|onRecClick\|dominantSignalKey\|trendingIds' frontend/web/src/views/Home.vue)" = "0" && cd frontend/web && bunx tsc --noEmit -p tsconfig.app.json && bunx eslint src/views/Home.vue</automated>
  </verify>
  <done>
    - Post-edit grep for the 8 legacy identifiers returns 0 matches.
    - `grep -c '<HeroSpotlightBlock' frontend/web/src/views/Home.vue` returns at least 1 (spotlight mount preserved).
    - `bunx tsc --noEmit -p tsconfig.app.json` clean.
    - `bunx eslint src/views/Home.vue` clean.
    - Any unused imports removed (verified by tsc unused-var detection if `noUnusedLocals` is enabled, else by manual review).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 'Adding a Spotlight Card Type' section to CLAUDE.md under Common Tasks</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    - CLAUDE.md (locate the "## Common Tasks" header and the existing subsections so we know where to insert)
    - .planning/workstreams/hero-spotlight/phases/03-dynamic-cards-migration/03-CONTEXT.md (the `<decisions>` CLAUDE.md documentation section provides the structural outline to follow)
    - services/catalog/internal/service/spotlight/cards/anime_of_day.go (the canonical resolver to reference as example)
    - frontend/web/src/components/home/spotlight/cards/AnimeOfDayCard.vue (canonical Vue card to reference)
    - frontend/web/src/types/spotlight.ts (discriminated union to reference)
    - frontend/web/src/locales/en.json (i18n key location to reference)
  </read_first>
  <behavior>
    - CLAUDE.md gains a new `### Adding a Spotlight Card Type` subsection under `## Common Tasks`.
    - The section is concise (one screen, ~30-40 lines), mirroring the depth of "Adding a new anime parser".
    - Each numbered step references a REAL file path (verified via cross-reference to Plan 03 + Plan 05 artifacts).
    - The section is placed between "Database migrations" and the next `## Local Development Commands` header (or wherever Common Tasks ends).
  </behavior>
  <action>
    1. Open `CLAUDE.md`. Locate the `### Database migrations` subsection (under `## Common Tasks`). After its content ends (and before the next `##` top-level header), insert the new subsection.

    2. Insert the following section verbatim (adjust path references if any of the cited files renamed during prior tasks — they should be stable per Plan 03/05 outputs):

       ```markdown
       ### Adding a Spotlight Card Type

       The home page's `HeroSpotlightBlock` (workstream `hero-spotlight`) is a 9-card rotating carousel. To add a 10th card type, touch the following 5 anchors (all from the same package boundaries, ~50 lines total):

       1. **Backend resolver** — Create `services/catalog/internal/service/spotlight/cards/{new_type}.go` implementing the `spotlight.Resolver` interface (`Type()` + `Resolve(ctx, userID *string) (*spotlight.Card, error)`). Mirror `anime_of_day.go`'s pattern: manual `cache.Get`/`cache.Set` with `errors.Is(err, cache.ErrNotFound)`, return `(nil, nil)` for ineligible, `(nil, err)` for failure, `(*Card, nil)` for success. Multi-item resolvers MUST apply `spotlight.AdaptiveSlice` (the 1-2-3 layout rule). Login-only resolvers return `(nil, nil)` when `userID == nil`. Always carry the `spotlight:` Redis key prefix for new keys (HSB-NF-03). Add a co-located `_test.go` with handwritten fakes — no testify/mock.

       2. **Backend Data type** — Add the JSON-shaped `{NewType}Data` struct to `services/catalog/internal/service/spotlight/types.go` extending the Card union. Update `types_test.go` with a round-trip marshal/unmarshal test.

       3. **Backend DI** — Add a `cards.New{NewType}Resolver(...)` call to the `spotlightResolvers` slice in `services/catalog/cmd/catalog-api/main.go`. Stable order matters for tie-breaking display order.

       4. **Frontend SFC** — Create `frontend/web/src/components/home/spotlight/cards/{NewType}Card.vue` accepting a typed `data` prop (use the new TypeScript variant from step 5). Honor the UI-SPEC contract: ONLY `font-medium` / `font-semibold` weights, `p-4 md:p-6 lg:p-8` padding, Tailwind utility-only, `min-h-[400px] md:min-h-[340px] lg:min-h-[320px]` height. Add `target="_blank"` + `rel="noopener noreferrer"` on any external anchor. Co-locate a `.spec.ts` with at least 5 Vitest assertions.

       5. **Frontend dispatch + i18n + types** —
          - Extend the `SpotlightCard` discriminated union in `frontend/web/src/types/spotlight.ts` with the new `{ type: '{new_type}', data: {NewType}Data }` variant.
          - Add a `v-else-if="active.type === '{new_type}'"` branch to the dispatch chain in `frontend/web/src/components/home/spotlight/HeroSpotlightBlock.vue` (DO NOT switch to `<component :is>` — keep the typed chain so vue-tsc narrows props).
          - Add a new `spotlight.{newType}.*` sub-namespace to BOTH `frontend/web/src/locales/en.json` and `frontend/web/src/locales/ru.json`. The parity test at `frontend/web/src/locales/__tests__/spotlight-keys.spec.ts` will fail if any key is added to one file but not the other.

       Verify the full stack with: `cd services/catalog && go test ./internal/service/spotlight/... -count=1 -race` and `cd frontend/web && bunx vitest run src/components/home/spotlight/ src/locales/__tests__/spotlight-keys.spec.ts && bunx tsc --noEmit`. The Phase 3 end-to-end Playwright spec `frontend/web/e2e/spotlight-full.spec.ts` is the canonical regression test for the full carousel.
       ```

    3. Save CLAUDE.md.

    Per HSB-NF-05.
  </action>
  <verify>
    <automated>grep -q '### Adding a Spotlight Card Type' /data/animeenigma/CLAUDE.md && grep -q 'spotlight.Resolver' /data/animeenigma/CLAUDE.md && grep -q 'spotlight-keys.spec.ts' /data/animeenigma/CLAUDE.md</automated>
  </verify>
  <done>
    - `grep -c 'Adding a Spotlight Card Type' CLAUDE.md` returns at least 1.
    - The new section appears BETWEEN `### Database migrations` and any `## Local Development Commands` header.
    - The section references all 5 anchors with verifiable file paths.
    - CLAUDE.md remains syntactically valid markdown (no broken code fences).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home.vue → user-facing rendering | Removing the legacy trendingRecs row does not introduce new trust boundaries. The remaining markup (spotlight, search, schedule, 3-column grid) is identical to its pre-Plan-3 form. |
| CLAUDE.md → future contributors | Documentation; no runtime impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | I (Info disclosure) | Removed trendingRecs region | accept | Frontend-only removal; backend `/api/users/recs` continues to serve the same data (now consumed by `personal_pick` resolver). No data path changes. |
| T-03-22 | T (Tampering) | Accidental deletion of unrelated identifiers | mitigate | Mandatory pre-edit grep audit (Phase A) + post-edit grep verification (Phase E) + tsc clean as gate. If any identifier had a second consumer, the unused-var detection or the tsc compile error catches it. |
</threat_model>

<verification>
After both tasks:

1. **Legacy identifiers gone:** `grep -c "trendingRecs\|trendingProgress\|trendingLoading\|rowLabelKey\|reasonI18nKey\|onRecClick\|dominantSignalKey\|trendingIds" frontend/web/src/views/Home.vue` returns `0`.
2. **Spotlight mount preserved:** `grep -c "<HeroSpotlightBlock" frontend/web/src/views/Home.vue` returns at least `1`.
3. **TypeScript clean:** `cd frontend/web && bunx tsc --noEmit -p tsconfig.app.json` exits 0.
4. **ESLint clean:** `bunx eslint src/views/Home.vue` exits 0.
5. **Feature flags STILL present:** `grep -q "SPOTLIGHT_ENABLED" docker/.env.example` succeeds AND `grep -q "VITE_HERO_SPOTLIGHT_ENABLED" frontend/web/.env.example` succeeds (or `.env.development` — wherever Phase 2 placed it). HSB-MIG-02 gate.
6. **CLAUDE.md section added:** `grep -q "### Adding a Spotlight Card Type" CLAUDE.md` succeeds.
7. **CLAUDE.md references real files:** `grep -q "services/catalog/internal/service/spotlight/cards" CLAUDE.md` AND `grep -q "frontend/web/src/components/home/spotlight" CLAUDE.md` AND `grep -q "spotlight-keys.spec.ts" CLAUDE.md` all succeed.
8. **Build does not regress:** `cd frontend/web && bun run build` succeeds (full frontend production build). Note: this is a longer-running gate — verify via Playwright in Plan 07 too.
</verification>

<success_criteria>
- `grep -c "trendingRecs" frontend/web/src/views/Home.vue` returns `0` (the canonical HSB-MIG-01 gate).
- `Home.vue` still mounts `<HeroSpotlightBlock />`.
- Both feature flags STILL present in the project's env example files (HSB-MIG-02).
- `CLAUDE.md` has a new "Adding a Spotlight Card Type" section under "Common Tasks" with all 5 anchors referencing real file paths (HSB-NF-05).
- TypeScript + ESLint + production build all green.
- No unrelated Home.vue functionality is altered.

**Metrics (per CONVENTIONS.md):**
- UXΔ = +1 (Better) — page is less cluttered; rotating spotlight replaces a static row
- CDI = 0.03 × 5 (2 files, surgical removal + doc append; small surface but coordination-heavy because of the grep audit)
- MVQ = Basilisk 90%/92% (Removal + docs — slop risk is collateral damage from over-aggressive deletion; defused by the pre-edit grep audit + tsc gate)
</success_criteria>

<output>
After completion, create `.planning/workstreams/hero-spotlight/phases/03-dynamic-cards-migration/03-06-SUMMARY.md` per the standard template. MUST include:
- The list of identifiers that WERE successfully removed (vs the original grep)
- Confirmation that no unexpected identifier had a second consumer (or, if it did, the surface for follow-up)
- The exact line-anchor for the inserted CLAUDE.md section (e.g. "between ### Database migrations and ## Local Development Commands")
- Confirmation that both feature flags are still in place (HSB-MIG-02)
- Reference to Plan 07 as the end-to-end verifier
</output>
