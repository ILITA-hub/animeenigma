---
status: passed
workstream: hero-spotlight
milestone: v1.0
milestone_name: HeroSpotlightBlock
phases: 3
phases_passed: 3
phases_with_gaps: 0
audited: 2026-05-21
auditor: gsd-audit-milestone (autonomous)
---

# Milestone Audit — hero-spotlight v1.0 HeroSpotlightBlock

## Verdict: ✅ PASSED

All 3 phases verified individually + cross-phase integration verified.
Workstream is ready for `gsd-complete-milestone v1.0`.

## Per-phase scores

| Phase | Name | Verdict | Score |
|-------|------|---------|-------|
| 01 | Backend Aggregator + Static Cards | passed | 7/7 ROADMAP success criteria; 13/13 requirements |
| 02 | Frontend HeroSpotlightBlock + Carousel | passed | 10/10 success criteria; 14/14 requirements |
| 03 | Dynamic Cards + Migration | passed | 10/10 success criteria; 18/18 requirements |

**Total requirements implemented:** 45/45 (HSB-BE-01..07, HSB-BE-10..13,
HSB-BE-20..26, HSB-BE-30, HSB-FE-01..09, HSB-FE-20..28, HSB-FE-40,
HSB-MIG-01..02, HSB-NF-01..05).

## Cross-phase integration (11/11 wired)

Verified by `gsd-integration-checker` against the running deploy:

- A. Phase 1 → Phase 2 bare-envelope contract WIRED (live curl shape verified)
- B. Phase 1 → Phase 3 aggregator extensibility WIRED (variadic resolver slice; 4→9 without signature change)
- C. Phase 3's `latest_news` AdaptiveSlice retrofit WIRED (no Phase 1 regression)
- D. Phase 2 forward-declared 9-variant union consumed correctly by Phase 3's 5 new card components
- E. HeroSpotlightBlock v-if dispatch chain extends cleanly from 4 → 9 branches
- F. i18n key-parity gate (Vitest 72/72) green after Phase 3 added 5 sub-namespaces
- G. `useSpotlight()` composable handles 9-card responses generically
- H. A11y attributes preserved after dispatch extension (axe-core 0 violations)
- I. Phase 3's OptionalAuthMiddleware NEVER 401s on anon (verified live anon=6 cards)
- J. CLAUDE.md docs section at line 504 (HSB-NF-05)
- K. Feature flags retained as kill switches (HSB-MIG-02)

## Live deploy confirmation

- `https://animeenigma.ru/` shows HeroSpotlightBlock at top of Home.vue
- Anon: `curl /api/home/spotlight | jq '.cards | length'` → **6 cards** (4 static + telegram_news + personal_pick anon-trending)
- Authenticated (`ui_audit_bot`): 7 cards (anon set + `continue_watching_new`)
- `now_watching` + `not_time_yet` correctly eligibility-filtered out when data unavailable
- Cached p95 latency: 3-4ms (target ≤400ms — 100×+ margin)
- Cold p95 target: ≤1500ms (small infrastructure; not stress-tested)
- `idx_watch_progress_updated_at` live in Postgres
- Legacy `trendingRecs` row provably gone: `grep -c trendingRecs frontend/web/src/views/Home.vue` returns 0
- Defense-in-depth: `curl http://localhost:8000/internal/users/u1/list?status=watching` returns 404

## Tech debt / follow-ups (informational, not blocking)

These are NOT blockers — they are deliberate deferrals captured during execution:

1. **HSB-MIG-02 feature-flag cleanup** — both `SPOTLIGHT_ENABLED` and `VITE_HERO_SPOTLIGHT_ENABLED` env flags remain as one-release kill switches. Per HSB-MIG-02, removal is deferred to a follow-up cleanup commit (Phase 4 if scheduled, or manual cleanup after one stable week).
2. **Cold-latency stress test (HSB-NF-01 lower-bound)** — only cached latency was validated under load (50-curl p95 = 2.9ms in Phase 1; 14ms full-suite Playwright). Cold (cache-miss) p95 ≤1500ms is implied by single-shot timing but not stress-tested under concurrency. Acceptable for a small self-hosted deployment per CLAUDE.md "Don't add CDN-related code".
3. **`users.show_in_now_watching` opt-out** (HSB-NF-04) deferred to v1.1+ pending user feedback. Phase 3 default include-all is documented.
4. **WebSocket-driven `now_watching` updates** deferred to v1.1 (Phase 3 polls Redis every 10s).
5. **Slide-order personalization** deferred to v1.1.
6. **Editorial admin-curated card type** deferred to v1.1.

## Recommendation

Proceed to `/gsd-complete-milestone v1.0 --ws hero-spotlight` then `/gsd-cleanup --ws hero-spotlight`.
