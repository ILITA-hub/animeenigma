---
milestone: v0.2
milestone_name: Self-Hosted Library
workstream: raw-jp
audited: 2026-05-18
status: passed
scores:
  requirements: 15/15
  phases: 6/6
  integration: 4/4
  flows: 5/5
---

# Milestone v0.2 — Self-Hosted Library Audit

**Workstream:** raw-jp
**Verdict:** ✓ Passed
**Date:** 2026-05-18

## Summary

All 6 phases shipped and verified live. All 15 milestone requirements (LIB-01, LIB-02, LIB-03, LIB-04, LIB-04b, LIB-05, LIB-06, LIB-07, LIB-08, LIB-09, LIB-10 + LIB-NF-01 through LIB-NF-04) satisfied with evidence in phase SUMMARY.md files. Cross-phase integration verified: search → enqueue → download → encode → upload → episodes endpoint → hybrid resolver → MinIO HLS playback path is whole.

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| LIB-01 — services/library/ scaffold + DB + health + metrics | 01 | ✓ | `services/library/` exists; `curl :8089/health` → 200; `curl :8089/metrics` → Prometheus; `library` Postgres DB auto-created |
| LIB-02 — gateway routing + Makefile + admin gate scaffold | 01,02 | ✓ | `/api/library/*` routes via gateway; `make health` shows library:8089; admin gate mirrors `/streaming/admin/*` (Phase 2) |
| LIB-03 — Nyaa.si RSS client | 02 | ✓ | `services/library/internal/parser/nyaa/` + 5 unit tests |
| LIB-04 — AnimeTosho JSON client | 02 | ✓ | `services/library/internal/parser/animetosho/` + 7 unit tests |
| LIB-04b — Merger + `GET /search` | 02 | ✓ | `services/library/internal/service/search.go`; `curl /api/library/search?q=&mal_id=` returns merged ranked results |
| LIB-05 — Embedded torrent client | 03 | ✓ | `services/library/internal/torrent/`; live smoke downloaded Big Buck Bunny |
| LIB-06 — Postgres job queue + endpoints | 03 | ✓ | `library_jobs` table; FOR UPDATE SKIP LOCKED claim; POST/GET/DELETE endpoints live |
| LIB-07 — ffmpeg HLS transcoder | 04 | ✓ | `services/library/internal/ffmpeg/`; synthetic encode → playlist + segments |
| LIB-08 — MinIO writer + episode detection + episodes table | 04 | ✓ | `services/library/internal/minio/`; `library_episodes` + `library_filename_patterns` tables; `raw-library/57466/1/` populated in smoke |
| LIB-09 — RawLibrary.vue admin UI | 05 | ✓ | `/admin/raw-library` route; 3-section view; PATCH/POST/health/extended endpoints; e2e tests |
| LIB-10 — Hybrid resolver | 06 | ✓ | `raw_resolver.go` library-first branch; cache invalidation webhook; `source` field; fallback within 16ms when library down |
| LIB-NF-01 — Disk-free guard | 03 | ✓ | `LIBRARY_DISK_FREE_MIN_PCT=99` reproduced 507 in smoke |
| LIB-NF-02 — Stall detection | 03 | ✓ | `LIBRARY_TORRENT_STALL_TIMEOUT` env wired; worker tracks `lastNonZeroPeerAt`; failed state w/ error_text |
| LIB-NF-03 — Prometheus metrics + Grafana | 03,04 | ✓ | All 8 metrics defined; `library` dashboard auto-loaded in Grafana |
| LIB-NF-04 — Operator docs | 01 | ✓ | `CLAUDE.md` Service Ports + Gateway Routing updated; `docker/.env.example` documents all `LIBRARY_*` knobs |

## Phase Status

| Phase | Name | Status | SUMMARY |
|-------|------|--------|---------|
| 01 | Library Service Scaffold | ✓ | `phases/01-library-service-scaffold/01-SUMMARY.md` |
| 02 | Nyaa + AnimeTosho Search Clients | ✓ | `phases/02-nyaa-animetosho-search-clients/02-SUMMARY.md` |
| 03 | Torrent Client + Job Queue + Metrics | ✓ | `phases/03-torrent-client-job-queue-metrics/03-SUMMARY.md` |
| 04 | ffmpeg HLS Transcoder + MinIO Writer | ✓ | `phases/04-ffmpeg-hls-transcoder-minio-writer/04-SUMMARY.md` |
| 05 | RawLibrary.vue Admin UI | ✓ | `phases/05-rawlibrary-vue-admin-ui/05-SUMMARY.md` |
| 06 | Hybrid Resolver | ✓ | `phases/06-hybrid-resolver/06-SUMMARY.md` |

## Cross-Phase Integration

| Integration | From | To | Status |
|-------------|------|-----|--------|
| Search → Enqueue | Phase 2 (`GET /search`) | Phase 3 (`POST /jobs`) | ✓ — admin UI in Phase 5 wires both via `adminLibraryApi` |
| Job lifecycle | Phase 3 download_worker | Phase 4 encoder_worker | ✓ — download_worker transitions `encoding` on done; encoder_worker claims `encoding` jobs |
| Episode persistence | Phase 4 encoder_worker | Phase 5 episodes endpoint + Phase 6 resolver | ✓ — `library_episodes` written by Phase 4; consumed by `/api/library/episodes/{shikimori_id}/{ep}` (Phase 4) and catalog hybrid resolver (Phase 6) |
| Cache invalidation | Phase 4 encoder_worker → Phase 6 catalog endpoint | ✓ — webhook fires post-`done`; live smoke verified Redis bust |

## E2E Flows

| Flow | Steps | Status |
|------|-------|--------|
| Admin queue → encode → playback | Search → Queue → Download → Encode → Upload → Episode row → `/raw/stream` returns `source:library` | ✓ (synthetic encode verified Phase 4; Phase 6 smoke shows library-first selection) |
| Library down → fallback | Stop library service → `/raw/stream?episode=N` returns `source:allanime` within 2.5s | ✓ — Phase 6 live smoke: fallback in 16ms |
| Pending link | Job done without shikimori_id → admin links via PATCH → MinIO Move + episode insert | ✓ — handler + writer.Move + repo.UpdateShikimoriID with side effects all unit-tested; Phase 5 verified the wiring |
| Cancel running download | DELETE /jobs/{id} → status `cancelled` within 2s | ✓ — Phase 3 live smoke verified |
| Disk-full enqueue | `LIBRARY_DISK_FREE_MIN_PCT=99` → POST returns 507 | ✓ — Phase 3 live smoke verified |

## Deferred / Tech Debt (Non-blocking)

| Item | Phase | Disposition |
|------|-------|-------------|
| `ui_audit_bot` lacks admin role | 02, 05 | Smoke testing used temp admin keys minted/revoked per-run. Future improvement: dedicated admin test account or grant ui_audit_bot admin for e2e suites. |
| `library_jobs_total`, `library_enqueue_rejected_total`, `library_cache_invalidation_total`, `library_filename_detect_fallback_total`, `library_encode_failures_total` counters only appear on first event | 03, 04, 06 | Normal Prometheus counter semantics; alerting can use `rate()` over scrape windows. |
| Per-anime quality profiles | 04 | Explicit v0.2 out-of-scope; 1080p-cap encode is fine for v0.2. |
| Bulk-queue UI | 05 | Explicit v0.2.1 followup. |
| Storage cleanup / retention | 04, 05 | Explicit v0.2.1 followup. |
| User-facing `source` chip on player | 06 | Explicit v0.3+ followup. |
| Port 8087 → 8089 deviation across upstream docs | 01 | Phase 1 documented in SUMMARY; upstream design doc + REQUIREMENTS still mention 8087. Recommended docs sweep before declaring v0.3 prep. |
| `go.work` Go-version bump to 1.24.0 | 02 | Required by anacrolix/torrent v1.61.0; affects all services. |

## Out of Scope (Per Milestone Requirements)

- Auto-download for ongoing series — explicit v0.3 milestone.
- User-facing library listing / request-to-add — v0.3+.
- Subtitle extraction from torrent files — out of scope all v0.x; subs aggregator (v0.1 Phase 2) owns subtitles.
- Multi-season pack auto-split — v0.2.1 if needed.
- Kage Project / fansubs.ru — Cloudflare-protected, deferred.
- Subtitle quality scoring — v0.4+.
- Library historical cleanup — v0.2.1.
- Cloudflare WARP egress for library service — not needed (torrent peers, not CF CDN).

## Conclusion

Milestone v0.2 Self-Hosted Library shipped end-to-end. Backend pipeline (search → enqueue → torrent → encode → MinIO → episodes) is live; admin UI is built and routed; hybrid resolver prefers library and falls back to AllAnime gracefully. All services healthy post-redeploy. Ready for `gsd-complete-milestone`.
