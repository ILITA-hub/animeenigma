---
milestone: v0.1
workstream: social
audited: 2026-05-13
status: passed
scores:
  requirements: 8/8
  phases: 1/1
  integration: 14/14
  flows: 8/8
gaps: []
tech_debt:
  - phase: 01-social-reviews-comments
    items:
      - "WR-02: rate-limiter NTP-clock-correction — deferred, requires Redis-backed bucket; in-process time.Now() is acceptable for v0.1 single-replica deploy"
      - "SOCIAL-NF-01: golden-file pre-image was never captured before migration deployed; functionally satisfied by 3 unit-level shape-assertion tests (`TestReviewHandler_*ShapeIsExactly7Fields`) + live key-set inspection. Letter of the requirement (golden-file diff) cannot be retroactively obtained."
      - "Pre-existing: API key (`ak_`) auth bug on `/anime/{animeId}/*` route family — gateway 401s API keys for these routes even though they work on `/api/users/*`. Out of scope for this milestone; affects only automated tests, real users use JWT cookies."
      - "UI review (advisory, 18/24): 2 blockers + 9 warnings noted in `01-UI-REVIEW.md` — loadMoreFailed unreachable, Tabs.vue base class bakes in font-medium violating 2-weight rule, Save edit reuses 'Posting…' copy. Worth a small follow-up patch."
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 0
  overall: partial
  notes: "VALIDATION.md exists with full per-task verification table; `nyquist_compliant: false` in frontmatter because the audit asked the executor to flip it to true only after Wave-0 completion is mechanically confirmed. Functionally all VALIDATION.md tasks are GREEN (verified by the executor agents during execution + the gsd-verifier's 8/8 pass), but the frontmatter flag was not flipped. Cosmetic — re-run `/gsd-validate-phase 1 --ws social` to formalize compliance, or accept as-is."
---

# v0.1 Social — Milestone Audit

**Workstream:** `social`
**Milestone:** v0.1 Social: Reviews + Comments
**Audited:** 2026-05-13
**Status:** ✓ passed

## Requirements Coverage (8/8)

All 8 requirements (SOCIAL-01..06, SOCIAL-NF-01, SOCIAL-NF-02) verified satisfied via 3-source cross-reference: (a) phase VERIFICATION.md status `passed`, 8/8 must_haves verified; (b) phase SUMMARY.md frontmatter lists each requirement under its plan's `requirements_completed`; (c) REQUIREMENTS.md traceability table now shows `Done` for all 8 entries.

| REQ-ID        | Description                                                | Phase | Status     | Evidence                                                                            |
|---------------|------------------------------------------------------------|-------|------------|-------------------------------------------------------------------------------------|
| SOCIAL-01     | Drop reviews table; add review_text + username to anime_list | 1     | satisfied  | `\d anime_list` shows new columns; `\d reviews` says "Did not find any relation"     |
| SOCIAL-02     | One-shot idempotent migration                              | 1     | satisfied  | `TestSocialMigration_Idempotent` passes; 3rd redeploy logs zero migration lines     |
| SOCIAL-03     | 6 review endpoints read/write anime_list, shape preserved   | 1     | satisfied  | `reviewResponse` 7-field projection + 3 shape tests; live curl returns expected keys |
| SOCIAL-04     | Comments table + 4 CRUD endpoints                          | 1     | satisfied  | 8/8 unit tests pass + live smoke (POST 201, PATCH 403 non-owner, DELETE 204 soft, 429 on 11th) |
| SOCIAL-05     | Comment create emits `type='comment'` activity event       | 1     | satisfied  | `TestCommentService_EmitsActivity` + live `SELECT FROM activity_events`              |
| SOCIAL-06     | Anime.vue Reviews/Comments tabs + URL persistence + anon UX  | 1     | satisfied  | 4/4 Playwright e2e tests pass in 5.5s; zero `[intlify]` warnings                    |
| SOCIAL-NF-01  | Frontend unchanged for reviews schema swap                  | 1     | satisfied* | Shape contract enforced by unit tests; *golden-file pre-image was never captured (see tech debt) |
| SOCIAL-NF-02  | GORM AutoMigrate + bootstrap migration idempotent           | 1     | satisfied  | `HasTable("reviews")` guard; 3rd redeploy emits zero migration lines                |

## Phase Status (1/1)

| Phase | Name                          | Status   | Verification           |
|-------|-------------------------------|----------|------------------------|
| 1     | Reviews + Ratings + Comments  | complete | `01-VERIFICATION.md` — passed, 8/8 must_haves verified |

## Cross-Phase Integration (14/14 wired)

The integration checker traced 14 distinct integration paths end-to-end:

1. Gateway `/anime/{id}/comments*` routes registered BEFORE the `/anime/*` catch-all to catalog (RESEARCH.md Pitfall 1 mitigation verified).
2. Player main.go → CommentHandler → chi router (4 routes mounted: 1 public GET, 3 auth-protected POST/PATCH/DELETE).
3. Frontend `commentApi` (in `client.ts`) → gateway → player (template-literal paths match gateway routes verbatim).
4. ActivityFeed.vue `if event.type === 'comment'` branch → `activity.comment.posted` locale key resolved in en/ja/ru.
5. ReviewService refactored to consume `ListRepository` (no `reviewRepo` parameter); `domain.Review` + `ReviewRepository` deleted from codebase.
6. AnimeListEntry gained `ReviewText` + `Username` via GORM AutoMigrate; DB confirmed.
7. `runSocialMigration` idempotency guard via `db.Migrator().HasTable("reviews")`.
8. Gateway JWT defense-in-depth applied on POST/PATCH/DELETE comments (CR-04 fix).
9. CommentRepository.Update filters `deleted_at IS NULL` (WR-01 fix).
10. Soft-delete excluded from list responses (verified live: 16 soft-deleted rows present, omitted from GET).
11. Cursor pagination via `libs/pagination/cursor.go` — round-trip verified.
12. Rate limit 10/hr/user/anime → 429 verified live.
13. 24 `anime.ugc.*` keys + 1 `activity.comment.posted` key present in all 3 locale files (verified via `jq | keys | length` = 24 across en/ja/ru).
14. Anime.vue Tabs.vue reuse — URL state via `router.replace`, anon login prompt at `v-else` branch.

## E2E Flows (8/8 complete)

- F1 Anonymous Comments tab → list + login CTA: VERIFIED (Playwright)
- F2 Logged-in comment → DB row + activity event: VERIFIED (live)
- F3 Edit own → 200; non-owner → 403: VERIFIED (unit + live)
- F4 Soft delete (owner / admin): VERIFIED (unit + live)
- F5 11th comment → 429: VERIFIED (unit + live)
- F6 MAL-imported score-only rows in Reviews list: VERIFIED (live DB)
- F7 Cursor pagination Load More: VERIFIED (both ends)
- F8 Migration idempotency: VERIFIED (3rd-redeploy logs)

## Tech Debt

Non-blocking. Documented for follow-up; does NOT block milestone completion.

### Phase 1 (social-reviews-comments)

- **WR-02 (deferred):** Rate-limiter NTP clock-correction risk. Current `time.Now()` approach acceptable for single-replica deploy; real fix is a Redis-backed bucket (out of scope for v0.1).
- **SOCIAL-NF-01 letter vs spirit:** Golden-file pre-image was never captured before the migration deployed. Functional contract enforced by `TestReviewHandler_*ShapeIsExactly7Fields` unit tests + live key-set verification. For future migrations: run capture script BEFORE deploying.
- **Pre-existing API-key auth bug:** `JWTValidationMiddleware` 401s `ak_` bearer tokens on `/anime/{animeId}/*` routes. Affects automated tests; real users use JWT cookies. Out of scope for this milestone.
- **UI review (advisory, 18/24):** 2 blockers + 9 warnings in `01-UI-REVIEW.md`. Worth a small follow-up patch: `loadMoreFailed` unreachable error message, `Tabs.vue` font-medium baked into base class violating the 2-weight contract, "Save edit" button reuses "Posting…" copy. Non-blocking — review is advisory.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 1     | exists        | partial   | `/gsd-validate-phase 1 --ws social` to formalize. Functionally all VALIDATION.md tasks are green (per gsd-verifier 8/8 + executor reports); only the frontmatter `nyquist_compliant: false → true` flip is pending. Cosmetic. |

## Conclusion

The `social` v0.1 milestone is integration-sound and ready for completion. All 8 requirements satisfied, zero broken flows, zero unsatisfied requirements, zero unprotected mutating routes. Tech debt is documented and non-blocking. Recommend proceeding to `/gsd-complete-milestone v0.1 --ws social`.
