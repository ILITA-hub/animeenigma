---
id: 02.1
phase: 02-frontend-shell
workstream: watch-together
title: TypeScript types + API client (createRoom/getRoom/deleteRoom)
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/types/watch-together.ts
  - frontend/web/src/api/watch-together.ts
  - frontend/web/src/api/__tests__/watch-together.spec.ts
requirements: [WT-SHELL-03]
autonomous: true
UXΔ: 0 (Ambiguous)
CDI: 0.02 × 5
MVQ: Sprite 80%/85%
must_haves:
  - frontend/web/src/types/watch-together.ts exports `RoomSnapshot`, `Room`, `Member`, `MemberMeta`, `ChatMessage`, `CreateRoomRequest`, `CreateRoomResponse`, plus the 20 wire message constants + payload interfaces matching `services/watch-together/internal/domain/ws_message.go`
  - frontend/web/src/api/watch-together.ts exports `createRoom`, `getRoom`, `deleteRoom` returning typed responses, throwing `RoomGoneError` on 410 and `RoomForbiddenError` on 403
---

<objective>
Ship the leaf TypeScript type module + API client that the rest of Phase 2 depends on. Mirrors the Go domain shapes from `services/watch-together/internal/domain/{room,message,ws_message}.go` 1:1 so the rest of the frontend gets compile-time guarantees against the locked Phase 1 wire protocol.

Purpose: Single source of truth for WS message shapes + REST envelopes. Every downstream plan (composable, components, view) imports from these two files. No backend changes required — protocol is locked.

Output: Two new source files + one spec file. Pure leaf — no Vue, no DOM, no composables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md
@.planning/workstreams/watch-together/phases/01-backend-foundation/01-PHASE-SUMMARY.md
@services/watch-together/internal/domain/ws_message.go
@services/watch-together/internal/domain/room.go
@services/watch-together/internal/domain/message.go
@services/watch-together/internal/handler/rooms.go
@frontend/web/src/api/notifications.ts
@frontend/web/src/api/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author `frontend/web/src/types/watch-together.ts` mirroring the Go domain</name>
  <read_first>
    - services/watch-together/internal/domain/ws_message.go (every constant + every `*Data` struct — these are the wire shapes)
    - services/watch-together/internal/domain/room.go (Room / Member / MemberMeta / RoomSnapshot)
    - services/watch-together/internal/domain/message.go (ChatMessage)
    - frontend/web/src/types/spotlight.ts (exemplar for a discriminated-union TypeScript module)
    - frontend/web/src/types/notification.ts (exemplar for paired-with-API-client types)
  </read_first>
  <behavior>
    - `MsgInbound` union of the 10 inbound type strings exactly matching Go constants (`'playback:play' | 'playback:pause' | 'playback:seek' | 'playback:time_tick' | 'state:change_episode' | 'state:change_player' | 'state:change_translation' | 'chat:message' | 'chat:reaction' | 'presence:heartbeat'`)
    - `MsgOutbound` union of the 10 outbound type strings (`'room:snapshot' | 'room:state_changed' | 'playback:event' | 'playback:correction' | 'member:joined' | 'member:left' | 'chat:message' | 'chat:reaction' | 'room:closed' | 'error'`)
    - `ErrorCode` union of 7 strings: `CAPACITY_FULL | ROOM_NOT_FOUND | RATE_LIMITED | CHAT_TOO_LONG | PERSISTENT_DRIFT | AUTH_EXPIRED | EPISODE_UNAVAILABLE`
    - `PlayerKind` union of 5 strings: `kodik | animelib | ourenglish | hanime | raw`
    - `PlaybackState` union: `playing | paused`
    - `PROTOCOL_VERSION` constant = `'1.0'`
    - Interfaces (snake_case JSON fields — matching Go json tags exactly): `Room`, `MemberMeta`, `Member`, `ChatMessage`, `RoomSnapshot`
    - Inbound payload interfaces: `PlaybackPlayData`, `PlaybackPauseData`, `PlaybackSeekData`, `PlaybackTimeTickData`, `StateChangeEpisodeData`, `StateChangePlayerData`, `StateChangeTranslationData`, `ChatMessageInData`, `ChatReactionInData`, `PresenceHeartbeatData` (latter is `Record<string, never>`)
    - Outbound payload interfaces: `RoomStateChangedData`, `PlaybackEventData`, `PlaybackCorrectionData`, `MemberJoinedData`, `MemberLeftData`, `ChatMessageOutData`, `ChatReactionOutData`, `RoomClosedData`, `ErrorData`
    - `Envelope<T>` generic: `{ type: string; data: T }`
    - REST shapes: `CreateRoomRequest = { anime_id; episode_id; player: PlayerKind; translation_id }`, `CreateRoomResponse = { room_id; invite_url; ws_url }`
    - `RoomGoneError` and `RoomForbiddenError` custom Error subclasses (used by the API client)
    - Spec file: vitest tests asserting (a) constant values match Go strings character-for-character via inline string literals, (b) the 24-emoji whitelist constant exported (see Task 2)
  </behavior>
  <action>
    Create `frontend/web/src/types/watch-together.ts` exporting every constant + interface listed in <behavior>. Use snake_case for JSON field names (matching Go `json` tags) — DO NOT convert to camelCase. Add JSDoc comments on each exported symbol pointing at the corresponding Go file/line (e.g. `// Mirrors services/watch-together/internal/domain/ws_message.go MsgPlaybackPlay`).

    Also export the locked 24-emoji `REACTION_WHITELIST` const tuple matching the Go `reactionWhitelist` map in `services/watch-together/internal/service/inbound.go:92-117` EXACTLY: `['🔥', '❤️', '😂', '😭', '👀', '🙏', '🎉', '✨', '💀', '🥺', '😍', '🤔', '👏', '🙌', '😱', '😎', '🌸', '⚡', '💯', '🎌', '🍣', '🌟', '💢', '🤯']` (preserve `❤️` as U+2764 U+FE0F and `⚡` as U+26A1 per the inline Go comments). Length MUST be 24.

    Create `frontend/web/src/types/__tests__/watch-together.spec.ts` with vitest cases:
    1. Asserts every inbound/outbound type string literal value (e.g. `expect(MSG_PLAYBACK_PLAY).toBe('playback:play')`).
    2. Asserts `PROTOCOL_VERSION === '1.0'`.
    3. Asserts `REACTION_WHITELIST.length === 24` AND `REACTION_WHITELIST.includes('🔥')` AND `REACTION_WHITELIST.includes('❤️')` AND `REACTION_WHITELIST.includes('⚡')`.
    4. Asserts the 5 `PlayerKind` literal values via a runtime tuple `['kodik', 'animelib', 'ourenglish', 'hanime', 'raw'] satisfies readonly PlayerKind[]`.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/types/__tests__/watch-together.spec.ts && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - File `frontend/web/src/types/watch-together.ts` exists and exports every symbol listed in <behavior>
    - All vitest assertions pass
    - `bunx tsc --noEmit` passes with zero errors anywhere in the codebase
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author `frontend/web/src/api/watch-together.ts` API client</name>
  <read_first>
    - frontend/web/src/api/notifications.ts (exemplar — envelope-unwrap helper, JSDoc style, gateway-relative URLs)
    - frontend/web/src/api/client.ts (apiClient instance — DO NOT instantiate a second axios)
    - services/watch-together/internal/handler/rooms.go (CreateRoomBody / CreateRoomResponse — wire shapes)
    - frontend/web/src/types/watch-together.ts (the file created in Task 1)
  </read_first>
  <behavior>
    - `createRoom(payload: CreateRoomRequest): Promise<CreateRoomResponse>` — POSTs `/watch-together/rooms`. Unwraps the `{success, data}` envelope. Returns the data.
    - `getRoom(id: string): Promise<RoomSnapshot>` — GETs `/watch-together/rooms/{id}`. On 410 → throws `RoomGoneError`. Other errors propagate.
    - `deleteRoom(id: string): Promise<void>` — DELETEs `/watch-together/rooms/{id}`. On 403 → throws `RoomForbiddenError`. On 410 → throws `RoomGoneError`. Returns void on 204.
    - URL-encodes the room ID via `encodeURIComponent` (matches `notifications.ts`).
    - Re-exports the type symbols callers need (so consumers can `import { createRoom, type RoomSnapshot } from '@/api/watch-together'`).
  </behavior>
  <action>
    Create `frontend/web/src/api/watch-together.ts`:
    - Use the existing `apiClient` from `@/api/client` (DO NOT create a new axios instance).
    - Use the same `unwrap<T>(raw)` envelope helper pattern from `notifications.ts:34-42` (copy-paste — keeping it co-located is the project pattern; do NOT extract to a shared util).
    - Wrap each call in try/catch only where status-code branching is required (`getRoom` 410, `deleteRoom` 403/410). For other errors, let axios propagate.
    - Detect axios errors via `axios.isAxiosError(err)` and read `err.response?.status`.
    - Throw the custom `RoomGoneError` / `RoomForbiddenError` classes from `@/types/watch-together` (defined in Task 1).

    Create `frontend/web/src/api/__tests__/watch-together.spec.ts` with vitest cases using `vi.mock('@/api/client')`:
    1. `createRoom` POSTs `/watch-together/rooms` with the body and returns unwrapped `data`.
    2. `getRoom` GETs `/watch-together/rooms/{id}` and returns the RoomSnapshot from `data`.
    3. `getRoom` on 410 throws `RoomGoneError` (mock `apiClient.get` to reject with `{ isAxiosError: true, response: { status: 410 } }`).
    4. `deleteRoom` on 403 throws `RoomForbiddenError`.
    5. `deleteRoom` on 204 resolves to undefined.
    6. Room IDs containing `/` are URL-encoded (call `getRoom('abc/def')` → assert mock called with `/watch-together/rooms/abc%2Fdef`).
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/api/__tests__/watch-together.spec.ts && bunx tsc --noEmit && bunx eslint src/api/watch-together.ts src/types/watch-together.ts</automated>
  </verify>
  <done>
    - `frontend/web/src/api/watch-together.ts` exists with createRoom/getRoom/deleteRoom signatures matching <behavior>
    - All 6 vitest cases pass
    - `bunx tsc --noEmit` clean, `bunx eslint` clean
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run src/types/__tests__/watch-together.spec.ts src/api/__tests__/watch-together.spec.ts` → all green
- `bunx tsc --noEmit` → zero errors
- `bunx eslint src/api/watch-together.ts src/types/watch-together.ts` → zero errors
- Manual grep gate: `grep -v '^//\|^ *\*' frontend/web/src/types/watch-together.ts | grep -c "❤️\|🔥\|⚡"` ≥ 3 (the whitelist is present, not just commented)
</verification>

<success_criteria>
- Phase 2 downstream plans can `import { createRoom, getRoom, type RoomSnapshot, type Envelope, type Member, MSG_PLAYBACK_PLAY, REACTION_WHITELIST } from '@/api/watch-together'` (and re-exports) without runtime errors.
- Constants character-match Go source.
- 24-emoji whitelist is locked from this plan forward; ReactionPalette (02.5) and the Phase 1 backend agree.
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/02-frontend-shell/02.1-SUMMARY.md` documenting:
- Final list of constants exported and their wire string values
- Reaction whitelist (paste the 24 emoji from the source file)
- Any deviations from the Go source (should be none)
</output>
