---
id: 02.2
phase: 02-frontend-shell
workstream: watch-together
title: i18n watch_together.* namespace (en/ru) + parity test
wave: 1
depends_on: []
files_modified:
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/__tests__/watch-together-keys.spec.ts
requirements: [WT-SHELL-07]
autonomous: true
UXΔ: +1 (Better)
CDI: 0.02 × 3
MVQ: Sprite 75%/90%
must_haves:
  - frontend/web/src/locales/en.json AND ru.json both have a top-level `watch_together` object with identical leaf-key sets
  - Locale parity test at frontend/web/src/locales/__tests__/watch-together-keys.spec.ts is green
  - Every required key from CONTEXT.md §i18n is present in BOTH locales as a non-empty string
---

<objective>
Add the `watch_together.*` i18n namespace in both English and Russian, with a parity test that fails if either locale drifts. Mirrors the spotlight pattern exactly so a future Phase 5 polish key addition is fenced by the same guardrail.

Purpose: i18n key bindings are string-typed and not type-checked — wrong namespace passes tsc/eslint/tests and only blows up at runtime as raw key strings (see MEMORY: feedback_smoke_verify_i18n). Lock the contract here in Wave 1 so every downstream component can `$t('watch_together.foo')` with confidence.

Output: Two locale-file updates + one new parity-test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md
@frontend/web/src/locales/__tests__/spotlight-keys.spec.ts
@frontend/web/src/locales/en.json
@frontend/web/src/locales/ru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `watch_together.*` keys to en.json and ru.json</name>
  <read_first>
    - .planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md §i18n (line 70-72 — exact required key list)
    - frontend/web/src/locales/en.json (find the existing top-level alphabetical position — `watch_together` slots between `updates` and others; check jq keys output)
    - frontend/web/src/locales/ru.json (mirror)
    - frontend/web/src/locales/__tests__/spotlight-keys.spec.ts (parity test pattern to mirror in Task 2)
  </read_first>
  <action>
    Add a top-level `"watch_together"` object to BOTH locales. The exact required keys (per CONTEXT.md §i18n + cross-checked against the component-needs list from Wave 3) are:

    English (`en.json`):
    ```
    "watch_together": {
      "title": "Watch Together",
      "subtitle": "Watching with friends",
      "members_heading": "Members",
      "host_badge": "host",
      "you_badge": "you",
      "empty_chat": "No messages yet — say hi!",
      "chat_input_placeholder": "Send a message…",
      "chat_input_aria": "Chat message",
      "chat_char_count": "{n}/500",
      "send_button": "Send",
      "reaction_palette_title": "Reactions",
      "reaction_palette_aria": "Send an emoji reaction",
      "invite_button_label": "Invite to Watch Together",
      "invite_copied_toast": "Invite link copied — share it with friends",
      "invite_copy_manual": "Copy this link to share:",
      "room_ended_title": "This Watch Together room has ended",
      "room_ended_back_button": "Back to anime",
      "reconnecting_indicator": "Reconnecting…",
      "reconnect_failed_title": "Unable to reconnect",
      "reconnect_failed_button": "Refresh",
      "capacity_full_title": "Room is full (10/10)",
      "capacity_full_back_button": "Back to anime",
      "auth_expired_error": "Your session expired — sign in again",
      "loading": "Loading room…",
      "member_joined": "{username} joined",
      "member_left": "{username} left",
      "room_id_label": "Room ID"
    }
    ```

    Russian (`ru.json`) — same keys, Russian translations:
    ```
    "watch_together": {
      "title": "Смотреть вместе",
      "subtitle": "Совместный просмотр",
      "members_heading": "Участники",
      "host_badge": "хост",
      "you_badge": "вы",
      "empty_chat": "Сообщений пока нет — поприветствуй друзей!",
      "chat_input_placeholder": "Написать сообщение…",
      "chat_input_aria": "Сообщение чата",
      "chat_char_count": "{n}/500",
      "send_button": "Отправить",
      "reaction_palette_title": "Реакции",
      "reaction_palette_aria": "Отправить эмодзи-реакцию",
      "invite_button_label": "Смотреть вместе",
      "invite_copied_toast": "Ссылка скопирована — поделись с друзьями",
      "invite_copy_manual": "Скопируй эту ссылку, чтобы поделиться:",
      "room_ended_title": "Эта комната Watch Together завершена",
      "room_ended_back_button": "Назад к аниме",
      "reconnecting_indicator": "Переподключение…",
      "reconnect_failed_title": "Не удалось переподключиться",
      "reconnect_failed_button": "Обновить",
      "capacity_full_title": "Комната заполнена (10/10)",
      "capacity_full_back_button": "Назад к аниме",
      "auth_expired_error": "Сессия истекла — войдите снова",
      "loading": "Загрузка комнаты…",
      "member_joined": "{username} присоединился",
      "member_left": "{username} вышел",
      "room_id_label": "ID комнаты"
    }
    ```

    Insert the new `watch_together` object preserving the existing JSON structure (do NOT reorder other top-level keys). Use the Edit tool to add the block — do not rewrite the whole file.

    Note: `{n}` and `{username}` interpolations are vue-i18n templates and MUST be preserved verbatim in BOTH locales.
  </action>
  <verify>
    <automated>cd frontend/web && jq -e '.watch_together | length' src/locales/en.json && jq -e '.watch_together | length' src/locales/ru.json && python3 -c "import json; en=set(json.load(open('src/locales/en.json'))['watch_together'].keys()); ru=set(json.load(open('src/locales/ru.json'))['watch_together'].keys()); assert en==ru, f'drift: en-ru={en-ru} ru-en={ru-en}'; print('OK', len(en), 'keys')"</automated>
  </verify>
  <done>
    - Both en.json and ru.json contain a `watch_together` top-level object
    - Both objects have exactly the same key set (verified by the python3 oneliner)
    - At least 27 leaf keys present (matches the required list above)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create locale parity test `watch-together-keys.spec.ts`</name>
  <read_first>
    - frontend/web/src/locales/__tests__/spotlight-keys.spec.ts (the exemplar — mirror its structure exactly: leafPaths helper, get helper, parity assertions, interpolation preservation)
    - The en.json / ru.json `watch_together` objects from Task 1
  </read_first>
  <behavior>
    - Test 1: `en.json` has a top-level `watch_together` object
    - Test 2: `ru.json` has a top-level `watch_together` object
    - Test 3: en and ru `watch_together` leaf-key sets are identical (sort + deep-equal)
    - Test 4: every en leaf value is a non-empty trimmed string
    - Test 5: every ru leaf value is a non-empty trimmed string
    - Test 6: `it.each` over the required key list (title, subtitle, members_heading, host_badge, you_badge, empty_chat, chat_input_placeholder, chat_input_aria, chat_char_count, send_button, reaction_palette_title, reaction_palette_aria, invite_button_label, invite_copied_toast, invite_copy_manual, room_ended_title, room_ended_back_button, reconnecting_indicator, reconnect_failed_title, reconnect_failed_button, capacity_full_title, capacity_full_back_button, auth_expired_error, loading, member_joined, member_left, room_id_label) — each is `'string'` in both locales
    - Test 7: `chat_char_count` preserves `{n}` interpolation in both locales
    - Test 8: `member_joined` preserves `{username}` interpolation in both locales
    - Test 9: `member_left` preserves `{username}` interpolation in both locales
  </behavior>
  <action>
    Create `frontend/web/src/locales/__tests__/watch-together-keys.spec.ts` mirroring the structure of `spotlight-keys.spec.ts`:
    - Import `en` from `../en.json` and `ru` from `../ru.json`. (DO NOT import `ja.json` — the watch-together namespace is en+ru only for v1.0; ja will land in v1.1.)
    - Reuse the same `leafPaths` and `get` helpers from spotlight-keys.spec.ts (copy-paste — keeping them local is the project pattern).
    - Implement the 9 test groups in <behavior>.

    Key interpolation preservation tests use:
    ```
    expect((enWT as Record<string, string>).chat_char_count).toContain('{n}')
    expect((ruWT as Record<string, string>).chat_char_count).toContain('{n}')
    ```
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/locales/__tests__/watch-together-keys.spec.ts</automated>
  </verify>
  <done>
    - All 9 test groups pass
    - Test file follows the exact structure of `spotlight-keys.spec.ts`
    - Test fails loudly if any key is added to one locale but not the other (verify by temporarily removing one key from ru.json and re-running — then restore)
  </done>
</task>

</tasks>

<verification>
- `cd frontend/web && bunx vitest run src/locales/__tests__/watch-together-keys.spec.ts` → all green
- `python3 -c "..."` parity oneliner in Task 1 → "OK 27 keys" (or more if Task 1 needed extra)
- Grep gate: `jq -r '.watch_together | keys | length' frontend/web/src/locales/en.json` and `... ru.json` both return ≥ 27 and the same number
</verification>

<success_criteria>
- Every key required by Phase 2 components (Wave 3+) is present in both locales.
- Parity test guards future plans (Phase 5 polish, v1.1) against drift.
- No raw-key-string renders possible in either locale on the room view (will be smoke-verified in Plan 02.10).
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/02-frontend-shell/02.2-SUMMARY.md` documenting:
- Final key set (count and list)
- Interpolation tokens used (`{n}`, `{username}`)
- Any keys added beyond the CONTEXT.md required list and why
</output>
