---
id: 02.5
phase: 02-frontend-shell
workstream: watch-together
title: ReactionPalette.vue + ReactionBurstOverlay.vue components
wave: 3
depends_on: [02.1, 02.2, 02.3]
files_modified:
  - frontend/web/src/components/watch-together/ReactionPalette.vue
  - frontend/web/src/components/watch-together/ReactionPalette.spec.ts
  - frontend/web/src/components/watch-together/ReactionBurstOverlay.vue
  - frontend/web/src/components/watch-together/ReactionBurstOverlay.spec.ts
requirements: [WT-SHELL-04]
autonomous: true
UXΔ: +2 (Better)
CDI: 0.03 × 5
MVQ: Sprite 80%/80%
must_haves:
  - ReactionPalette renders exactly the 24-emoji REACTION_WHITELIST as clickable chips; click calls `sendReaction(emoji)` prop
  - ReactionBurstOverlay is `pointer-events: none` absolute-positioned, renders floating emoji that fade after 3s using CSS-only animation (no JS spring lib)
---

<objective>
The "reactions" half of the sidebar plus the floating-bursts overlay that sits on top of the player. Both consume the composable's reactive state and locked whitelist from `@/types/watch-together`.

Purpose: Emoji reactions are the second demo moment ("two browsers see floating bursts on each other's player"). Burst polish animations land in Phase 5 — Phase 2 ships a simple CSS keyframe-fade that proves the wire path.

Output: Two Vue SFCs + two specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md
@frontend/web/src/types/watch-together.ts
@frontend/web/src/composables/useWatchTogetherRoom.ts
@frontend/web/src/components/watch-together/MemberList.vue
@CLAUDE.md
</context>

<interfaces>
From `@/types/watch-together`:
```typescript
export const REACTION_WHITELIST: readonly string[]  // 24 emoji, locked in 02.1
```

From `@/composables/useWatchTogetherRoom`:
```typescript
reactions: Ref<ReactionEvent[]>  // local ring buffer, last 20, aged out at 5s
sendReaction(emoji: string): void
```

`ReactionEvent` shape (defined in the composable in 02.3):
```typescript
interface ReactionEvent { id: number; emoji: string; user_id: string; ts: number }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ReactionPalette.vue` — 24-emoji chip grid</name>
  <read_first>
    - frontend/web/src/components/watch-together/MemberList.vue (sibling component — same conventions)
    - frontend/web/src/types/watch-together.ts (REACTION_WHITELIST source)
    - CLAUDE.md UI conventions (font-medium/semibold only, p-4 md:p-6)
  </read_first>
  <behavior>
    Props:
    - `sendReaction: (emoji: string) => void`

    Renders:
    - `<section aria-label="$t('watch_together.reaction_palette_aria')">` with `<h3 class="font-semibold">{{ $t('watch_together.reaction_palette_title') }}</h3>`
    - A grid `<div class="grid grid-cols-6 gap-2 md:grid-cols-8">` containing one `<button>` per emoji in `REACTION_WHITELIST` (must render all 24).
    - Each button: `<button type="button" class="text-2xl p-2 rounded hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-cyan-400" :aria-label="emoji" @click="onClick(emoji)">{{ emoji }}</button>`
    - `onClick(emoji)`: calls `props.sendReaction(emoji)`; locally applies a `disabled` flag for 200ms to throttle accidental double-clicks (visual feedback only — server already rate-limits at 5/sec)

    Behaviors validated in spec:
    - Test 1: Renders exactly 24 buttons
    - Test 2: Each button's text content matches the corresponding REACTION_WHITELIST entry in order
    - Test 3: Clicking the first button calls `sendReaction('🔥')` exactly once
    - Test 4: Clicking the same button twice within 200ms calls `sendReaction` once (throttled)
    - Test 5: After 200ms, the throttle releases and a second click fires `sendReaction`
    - Test 6: Each button has `aria-label` matching its emoji
    - Test 7: No `font-bold` in rendered HTML
  </behavior>
  <action>
    Create `frontend/web/src/components/watch-together/ReactionPalette.vue`:
    - `<script setup lang="ts">` with `defineProps<{ sendReaction: (emoji: string) => void }>()`
    - Import `REACTION_WHITELIST` from `@/types/watch-together`
    - Throttle state: `const lastClickAt = ref(0)`; `onClick` checks `Date.now() - lastClickAt.value > 200` before calling sendReaction; updates `lastClickAt` on each successful call.
    - Use `useI18n` for the two i18n keys: `watch_together.reaction_palette_title`, `watch_together.reaction_palette_aria`.
    - Tailwind utility-only; `p-4 md:p-6` on outer section.

    Create `frontend/web/src/components/watch-together/ReactionPalette.spec.ts` with the 7 tests. Use `vi.useFakeTimers()` for the throttle test; advance with `vi.advanceTimersByTime(250)`.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/watch-together/ReactionPalette.spec.ts && bunx tsc --noEmit && bunx eslint src/components/watch-together/ReactionPalette.vue</automated>
  </verify>
  <done>
    - All 7 tests pass
    - 24 buttons rendered in REACTION_WHITELIST order
    - tsc/eslint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ReactionBurstOverlay.vue` — floating emoji that fade after 3s</name>
  <read_first>
    - frontend/web/src/components/watch-together/ReactionPalette.vue (sibling)
    - frontend/web/src/composables/useWatchTogetherRoom.ts (the reactions ring buffer shape — ReactionEvent type)
    - CLAUDE.md UI conventions (Tailwind utility-only)
  </read_first>
  <behavior>
    Props:
    - `reactions: ReactionEvent[]` (from composable's `reactions.value`)

    Renders:
    - Outer `<div class="pointer-events-none absolute inset-0 overflow-hidden">` — meant to be a child of the player wrapper so it overlays the video.
    - For each reaction in `reactions`, one `<span>` positioned via inline style with a random `left: NN%` (computed once per reaction id and memoized in a local Map) and a CSS class that triggers a 3s `@keyframes` animation: starts at `bottom: 0; opacity: 1; transform: translateY(0)` and ends at `bottom: 0; opacity: 0; transform: translateY(-200px)`.
    - The CSS keyframes are defined ONCE inside a `<style scoped>` block at the bottom of the SFC (the only place custom CSS is allowed in this plan — keyframes can't be expressed as utilities).
    - Use `text-4xl` Tailwind class for emoji size.
    - The composable already prunes reactions after 5s; we only need to render whatever's in the ref. The 3s CSS animation finishes before pruning, so opacity is 0 by render time anyway.

    Behaviors validated in spec:
    - Test 1: Empty reactions → no `<span>` rendered in the overlay
    - Test 2: 3 reactions in props → 3 `<span>` rendered, each containing the correct emoji
    - Test 3: Each rendered `<span>` has `style="left:..."` with a value between 0% and 100%
    - Test 4: The same reaction (same id) rendered twice (e.g. via re-render) keeps the same `left` value (memoized per id)
    - Test 5: Outer wrapper has `pointer-events-none` class
    - Test 6: `<style scoped>` block contains `@keyframes` (smoke-grep the SFC source)
  </behavior>
  <action>
    Create `frontend/web/src/components/watch-together/ReactionBurstOverlay.vue`:
    - `<script setup lang="ts">` with `defineProps<{ reactions: ReactionEvent[] }>()`
    - Import `ReactionEvent` type from `@/composables/useWatchTogetherRoom` (or re-export it through the composable for clean import)
    - `const positions = new Map<number, number>()`; `function leftFor(id: number): number { if (!positions.has(id)) positions.set(id, Math.random() * 90 + 5); return positions.get(id)! }` — memoizes the `left:` position per reaction id so re-renders don't jitter.
    - Template: `<div class="pointer-events-none absolute inset-0 overflow-hidden"><span v-for="r in reactions" :key="r.id" class="absolute text-4xl burst" :style="{ left: leftFor(r.id) + '%' }">{{ r.emoji }}</span></div>`
    - Scoped CSS:
      ```css
      .burst {
        bottom: 10%;
        animation: burst-rise 3s ease-out forwards;
      }
      @keyframes burst-rise {
        from { opacity: 1; transform: translateY(0); }
        to { opacity: 0; transform: translateY(-200px); }
      }
      ```

    Create `frontend/web/src/components/watch-together/ReactionBurstOverlay.spec.ts` with the 6 tests:
    - Test 6 uses `import.meta.glob` or `fs.readFileSync` to read the SFC source and assert `@keyframes` appears. Alternatively, mount with `attachTo: document.body` and read `wrapper.element.querySelector('style')?.textContent`. Prefer the fs read for determinism.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/components/watch-together/ReactionBurstOverlay.spec.ts && bunx tsc --noEmit && bunx eslint src/components/watch-together/ReactionBurstOverlay.vue</automated>
  </verify>
  <done>
    - All 6 tests pass
    - Overlay is `pointer-events: none` (so click-through to player works)
    - `@keyframes` defined in a scoped style block
    - tsc/eslint clean
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run src/components/watch-together/ReactionPalette.spec.ts src/components/watch-together/ReactionBurstOverlay.spec.ts` → all green
- `bunx tsc --noEmit` → clean
- `bunx eslint src/components/watch-together/Reaction*.vue` → clean
- Grep gate: `grep -c "REACTION_WHITELIST" frontend/web/src/components/watch-together/ReactionPalette.vue` ≥ 1 (palette consumes the whitelist directly, not a hardcoded array)
- Grep gate: `grep -E "font-bold|font-black|font-extrabold" frontend/web/src/components/watch-together/Reaction*.vue` → no matches
</verification>

<success_criteria>
- Palette is the locked 24-emoji set that matches the Phase 1 backend filter.
- Burst overlay overlays player without intercepting clicks (pointer-events: none verified in spec).
- CSS-only animation, no JS spring lib added (WT-NF-05 dependency hygiene).
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/02-frontend-shell/02.5-SUMMARY.md` documenting:
- Confirmed 24-emoji palette order
- Throttle ms (200) and rationale
- Burst animation duration (3s) and the keyframe shape
</output>
