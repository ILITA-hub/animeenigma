---
id: 02.7
phase: 02-frontend-shell
workstream: watch-together
title: Add `room?` prop to all 5 player components (accepted but ignored)
wave: 3
depends_on: [02.1]
files_modified:
  - frontend/web/src/components/player/KodikPlayer.vue
  - frontend/web/src/components/player/AnimeLibPlayer.vue
  - frontend/web/src/components/player/OurEnglishPlayer.vue
  - frontend/web/src/components/player/HanimePlayer.vue
  - frontend/web/src/components/player/RawPlayer.vue
requirements: [WT-SHELL-06]
autonomous: true
UXΔ: 0 (Ambiguous)
CDI: 0.01 × 3
MVQ: Sprite 70%/90%
must_haves:
  - All 5 player components accept an optional `room?: WatchTogetherRoomHandle | null` prop
  - The prop is REFERENCED at least once (so `vue-tsc` and `eslint`'s no-unused-vars stay happy) but does NOT change player behavior — purely additive
  - Existing player tests still pass (zero regression)
---

<objective>
Wire a typed-but-ignored `room?` prop into all 5 player components. Phase 3 will read this prop and bridge it to `usePlayerSyncBridge`; Phase 2 just locks the API surface so 02.8's `WatchTogetherView` can pass `:room="roomHandle"` without any player file changes in Phase 3.

Purpose: The prop ships now so Phase 3 can be pure additive sync wiring without revisiting player signatures. Zero behavior change in Phase 2.

Output: Five player files lightly edited (additive prop only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md
@frontend/web/src/types/watch-together.ts
@frontend/web/src/composables/useWatchTogetherRoom.ts
@frontend/web/src/components/player/KodikPlayer.vue
@frontend/web/src/components/player/AnimeLibPlayer.vue
@frontend/web/src/components/player/OurEnglishPlayer.vue
@frontend/web/src/components/player/HanimePlayer.vue
@frontend/web/src/components/player/RawPlayer.vue
</context>

<interfaces>
The composable `useWatchTogetherRoom` (created in 02.3) returns a `UseWatchTogetherRoomReturn` shape. The `room?` prop on players will be the FULL returned object (not just the `room: Ref<Room | null>` ref inside it) — Phase 3's `usePlayerSyncBridge` needs access to `emitPlay`, `onPlaybackEvent`, etc.

Export from `@/composables/useWatchTogetherRoom`:
```typescript
export type WatchTogetherRoomHandle = UseWatchTogetherRoomReturn
```

This plan's player prop will be:
```typescript
room?: WatchTogetherRoomHandle | null
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Export `WatchTogetherRoomHandle` type alias from the composable</name>
  <read_first>
    - frontend/web/src/composables/useWatchTogetherRoom.ts (the file from Plan 02.3 — find the `UseWatchTogetherRoomReturn` interface)
  </read_first>
  <action>
    Edit `frontend/web/src/composables/useWatchTogetherRoom.ts` to add a type-alias export:
    ```typescript
    export type WatchTogetherRoomHandle = UseWatchTogetherRoomReturn
    ```
    Place it directly above the `export function useWatchTogetherRoom(...)` line. Brief JSDoc: `/** Stable consumer-facing type alias. The `room?` prop on every player accepts this. */`
  </action>
  <verify>
    <automated>cd frontend/web && bunx tsc --noEmit && grep -c "export type WatchTogetherRoomHandle" frontend/web/src/composables/useWatchTogetherRoom.ts</automated>
  </verify>
  <done>
    - `WatchTogetherRoomHandle` type alias exported
    - tsc clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `room?: WatchTogetherRoomHandle | null` to all 5 player defineProps blocks</name>
  <read_first>
    - frontend/web/src/components/player/KodikPlayer.vue (lines 343-349 — the current defineProps block)
    - frontend/web/src/components/player/AnimeLibPlayer.vue (locate its defineProps via grep)
    - frontend/web/src/components/player/OurEnglishPlayer.vue (lines around 205 — defineProps block)
    - frontend/web/src/components/player/HanimePlayer.vue (defineProps)
    - frontend/web/src/components/player/RawPlayer.vue (defineProps)
  </read_first>
  <action>
    For EACH of the 5 player SFCs, do exactly two edits:

    **Edit 1 (top of `<script setup lang="ts">`):** Add the import directly under the existing imports section, but DO NOT collide with existing imports — if a `WatchTogetherRoomHandle` import already exists for any reason, skip.
    ```typescript
    import type { WatchTogetherRoomHandle } from '@/composables/useWatchTogetherRoom'
    ```

    **Edit 2 (inside the `defineProps<{ … }>()` generic):** Append a new line BEFORE the closing `}>()`:
    ```typescript
    room?: WatchTogetherRoomHandle | null
    ```

    Example for KodikPlayer.vue (the current block is lines 343-349):
    ```typescript
    const props = defineProps<{
      animeId: string
      animeName?: string
      totalEpisodes?: number
      initialEpisode?: number
      preferredCombo?: WatchCombo | null
      room?: WatchTogetherRoomHandle | null   // Phase 2 (02.7): accepted but ignored; Phase 3 wires sync
    }>()
    ```

    **Reference the prop once (eslint no-unused-vars + vue-tsc happy):** In each player, add one line of dead-code reference INSIDE an `onMounted` (or near top of `<script setup>`) like:
    ```typescript
    // Phase 2 (02.7) — room prop accepted, sync wiring lands in Phase 3.
    void props.room
    ```

    Place this dead-reference line near other top-of-script comments. The `void` operator pattern is idiomatic TypeScript for "I'm intentionally not using this".

    Do NOT modify any other code in these files. Do NOT change any existing behavior, prop, watcher, or render path. The diff per file should be ≤ 5 lines added.

    Player files to edit (all 5):
    1. `frontend/web/src/components/player/KodikPlayer.vue`
    2. `frontend/web/src/components/player/AnimeLibPlayer.vue`
    3. `frontend/web/src/components/player/OurEnglishPlayer.vue`
    4. `frontend/web/src/components/player/HanimePlayer.vue`
    5. `frontend/web/src/components/player/RawPlayer.vue`
  </action>
  <verify>
    <automated>cd frontend/web && bunx tsc --noEmit && bunx eslint src/components/player/KodikPlayer.vue src/components/player/AnimeLibPlayer.vue src/components/player/OurEnglishPlayer.vue src/components/player/HanimePlayer.vue src/components/player/RawPlayer.vue</automated>
  </verify>
  <done>
    - Each of the 5 SFCs has `room?: WatchTogetherRoomHandle | null` in its defineProps
    - Each of the 5 SFCs imports `WatchTogetherRoomHandle` type-only
    - Each has a `void props.room` (or equivalent) reference to satisfy lint
    - `bunx tsc --noEmit` clean
    - `bunx eslint` clean
    - Diff per file is ≤ 5 added lines (no other changes)
  </done>
</task>

<task type="auto">
  <name>Task 3: Sanity-check existing player tests still pass</name>
  <read_first>
    - Any existing player tests — locate via `find frontend/web/src/components/player -name "*.spec.ts" -o -name "*.test.ts"`. If none exist, skip the run but still do the integration check.
  </read_first>
  <action>
    Run the existing player-related vitest suite (if any):
    ```bash
    cd frontend/web && bunx vitest run src/components/player/ 2>&1 | tail -50
    ```
    Also run a broader compile check to ensure no consumer of these players broke (Anime.vue passes 5 specific props to each — none collide with `room`):
    ```bash
    cd frontend/web && bunx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>cd frontend/web && bunx tsc --noEmit && (test -z "$(find src/components/player -name '*.spec.ts' -o -name '*.test.ts' 2>/dev/null)" || bunx vitest run src/components/player/)</automated>
  </verify>
  <done>
    - No existing test regresses
    - Anime.vue's player mounts (currently passing 5 props) still compile cleanly because `room` is optional
  </done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` → clean across the whole project
- `bunx eslint src/components/player/` → clean
- Grep gate: `grep -lE "room\?: WatchTogetherRoomHandle" frontend/web/src/components/player/{KodikPlayer,AnimeLibPlayer,OurEnglishPlayer,HanimePlayer,RawPlayer}.vue | wc -l` == 5 (all 5 players have the prop)
- Diff size sanity: `git diff frontend/web/src/components/player/ | grep -c "^+"` < 30 (additive only, no big rewrites)
</verification>

<success_criteria>
- All 5 players accept `:room` from `WatchTogetherView` (Plan 02.8) without complaint.
- Phase 3 can extract `props.room` and call `room.emitPlay(...)` / `room.onPlaybackEvent(...)` without changing the prop signature.
- Zero regression in existing player behavior (verified by existing tests + tsc).
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/02-frontend-shell/02.7-SUMMARY.md` documenting:
- Confirmed list of 5 players touched
- Exact lines added per player (paste a 3-line diff from each)
- Confirmation that `void props.room` (or equivalent) sits in each so Phase 3 can replace it with real wiring
</output>
