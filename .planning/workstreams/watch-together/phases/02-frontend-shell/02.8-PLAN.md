---
id: 02.8
phase: 02-frontend-shell
workstream: watch-together
title: WatchTogetherView.vue + router entry + lazy-load chunk + room-ended/capacity states
wave: 5
depends_on: [02.1, 02.2, 02.3, 02.6, 02.7]
files_modified:
  - frontend/web/src/views/WatchTogetherView.vue
  - frontend/web/src/views/WatchTogetherView.spec.ts
  - frontend/web/src/router/index.ts
requirements: [WT-SHELL-01, WT-SHELL-06, WT-NF-04]
autonomous: true
UXΔ: +3 (Better)
CDI: 0.04 × 13
MVQ: Griffin 85%/75%
must_haves:
  - Route `/watch/room/:roomId` with `requiresAuth: true` lazy-loads `WatchTogetherView.vue` as its own chunk
  - On mount: getRoom(:roomId) → on success, connect composable + render player + RoomSidebar; on 410 → render "Room ended" empty state with "Back to anime" button
  - The 5-way player switch mounts the correct `<*Player>` based on `room.player` and passes `:room="roomHandle"` to it
  - Bundle audit shows the WatchTogetherView lazy chunk is < 30KB gz (or summary documents why not)
---

<objective>
Glue every Wave 1-3 output together: the view that loads on `/watch/room/:roomId`, the router entry, the 5-way player mount, the empty/error states, the bundle audit. After this plan, a user can navigate to a room URL and see the assembled UI (chat + reactions + player). Plan 02.9 adds the InviteButton mount in Anime.vue; Plan 02.10 smoke-tests two-browser end-to-end.

Purpose: First end-to-end-renderable view. Chunk-isolated via lazy import so the bundle impact (WT-NF-04) is opt-in only on the room route.

Output: One new view + one spec + one router edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-CONTEXT.md
@frontend/web/src/composables/useWatchTogetherRoom.ts
@frontend/web/src/api/watch-together.ts
@frontend/web/src/components/watch-together/RoomSidebar.vue
@frontend/web/src/types/watch-together.ts
@frontend/web/src/router/index.ts
@frontend/web/src/views/Anime.vue
@CLAUDE.md
</context>

<interfaces>
From `@/composables/useWatchTogetherRoom`:
```typescript
export function useWatchTogetherRoom(roomId: Ref<string> | string): UseWatchTogetherRoomReturn
```

From `@/api/watch-together`:
```typescript
export function getRoom(id: string): Promise<RoomSnapshot>
export class RoomGoneError extends Error {}
```

Player components (Plan 02.7 added `:room` prop, accepted-but-ignored in Phase 2):
- `<KodikPlayer :anime-id :anime-name :total-episodes :initial-episode :room>`
- `<AnimeLibPlayer ...>`
- `<OurEnglishPlayer :anime-id :initial-episode :room>`
- `<HanimePlayer :anime-id :anime-name :total-episodes :initial-episode :room>`
- `<RawPlayer :anime-id :room>`

The locked router pattern (see existing routes in `frontend/web/src/router/index.ts`):
```typescript
{
  path: '/watch/room/:roomId',
  name: 'watch-together-room',
  component: () => import('@/views/WatchTogetherView.vue'),
  meta: { titleKey: 'watch_together.title', requiresAuth: true }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author `WatchTogetherView.vue` — snapshot fetch + state branches + player mount + sidebar</name>
  <read_first>
    - frontend/web/src/views/Anime.vue (lines 470-530 — the 5-way player switch with v-if/v-else-if chain — mirror this exact pattern; remember MEMORY: never place non-conditional elements between v-if/v-else-if)
    - frontend/web/src/composables/useWatchTogetherRoom.ts (the API contract from Plan 02.3)
    - frontend/web/src/api/watch-together.ts (getRoom signature)
    - frontend/web/src/components/watch-together/RoomSidebar.vue (prop signature)
    - frontend/web/src/types/watch-together.ts (RoomSnapshot, PlayerKind)
    - CLAUDE.md UI conventions
  </read_first>
  <behavior>
    View structure (top-level):
    - `<script setup lang="ts">`:
      - `useRoute()` → `const roomId = computed(() => route.params.roomId as string)`
      - `const loading = ref(true)`, `const errorState = ref<'gone' | 'capacity' | 'auth_expired' | 'failed' | null>(null)`
      - On mount: `await getRoom(roomId.value)` → on `RoomGoneError` set `errorState='gone'`; on success store the snapshot and proceed.
      - `const roomHandle = useWatchTogetherRoom(roomId)` → call `await roomHandle.connect()` after snapshot fetches OK.
      - Subscribe to `roomHandle.onError((e) => { if (e.code === 'CAPACITY_FULL') errorState='capacity'; if (e.code === 'AUTH_EXPIRED') { errorState='auth_expired'; router.push({ path: '/auth', query: { next: route.fullPath } }) } })`
      - Subscribe to `roomHandle.onRoomClosed(() => errorState='gone')`
      - `onUnmounted(() => roomHandle.disconnect())` (composable also auto-disconnects but explicit is safer here)
    - `<template>`:
      - `<div v-if="loading">{{ $t('watch_together.loading') }}</div>`
      - `<div v-else-if="errorState === 'gone'">`: room-ended state. Heading `{{ $t('watch_together.room_ended_title') }}` + `<button @click="goBackToAnime">{{ $t('watch_together.room_ended_back_button') }}</button>`. `goBackToAnime()` reads `roomHandle.room.value?.anime_id` (if snapshot loaded) and routes to `/anime/${id}`; if no anime id known, routes to `/`.
      - `<div v-else-if="errorState === 'capacity'">`: full-room state. `{{ $t('watch_together.capacity_full_title') }}` + back-to-anime button (same as above).
      - `<div v-else-if="connectionStatus === 'failed' && errorState !== 'auth_expired'">`: reconnect-failed state. `{{ $t('watch_together.reconnect_failed_title') }}` + `<button @click="reload">{{ $t('watch_together.reconnect_failed_button') }}</button>` where reload does `window.location.reload()`.
      - `<div v-else>`: the live room. Layout:
        - Desktop (`lg:grid lg:grid-cols-[1fr_360px] lg:gap-4`):
          - Player column: the 5-way switch (v-if `roomHandle.room.value?.player === 'kodik'` → `<KodikPlayer …>`, etc.). Pass props: `:anime-id`, `:initial-episode` (parse from `roomHandle.room.value?.episode_id` — for Phase 2 just pass it as-is since players already handle the string), and crucially `:room="roomHandle"`.
          - Sidebar column: `<RoomSidebar :room="roomHandle.room.value" :members="roomHandle.members.value" :messages="roomHandle.messages.value" :send-chat="roomHandle.sendChat" :send-reaction="roomHandle.sendReaction" :connection-status="roomHandle.connectionStatus.value" />`
          - Burst overlay sits as a sibling positioned absolute over the player wrapper: `<ReactionBurstOverlay :reactions="roomHandle.reactions.value" />` inside the player column's relative-positioned wrapper.
        - Mobile (`<lg`): single column, player on top, sidebar below. Tabbed mobile layout is DEFERRED to Phase 5.
      - Tailwind utility-only; font-medium / font-semibold only; padding `p-4 md:p-6 lg:p-8` on the outer wrapper.

    Defensive behaviors:
    - If `roomHandle.room.value === null` AFTER snapshot fetch succeeded but BEFORE the WS room:snapshot frame arrives → render the player + sidebar with empty defaults; the WS snapshot will populate within ~100ms.
    - If `roomHandle.room.value?.player` is somehow not one of the 5 kinds → render an empty state "Unsupported player" with a back button. Defensive guard for protocol forward-compat.

    Spec tests (uses `vue-router/memory` or `@vue/test-utils` + `router.push` setup):
    - Test 1: On mount, calls `getRoom(roomId)` exactly once
    - Test 2: On `getRoom` → `RoomGoneError`, renders `room_ended_title` text
    - Test 3: On `getRoom` success, calls `roomHandle.connect()` exactly once
    - Test 4: When `room.player === 'kodik'`, mounts `<KodikPlayer>` and NOT the other 4
    - Test 5: When `room.player === 'animelib'`, mounts `<AnimeLibPlayer>`
    - Test 6: When `room.player === 'ourenglish'`, mounts `<OurEnglishPlayer>`
    - Test 7: When `room.player === 'hanime'`, mounts `<HanimePlayer>`
    - Test 8: When `room.player === 'raw'`, mounts `<RawPlayer>`
    - Test 9: Pass-through — RoomSidebar receives `room`, `members`, `messages`, `sendChat`, `sendReaction`, `connectionStatus` props from the composable
    - Test 10: On `CAPACITY_FULL` error received, renders capacity-full state
    - Test 11: On `AUTH_EXPIRED` error, calls `router.push({ path: '/auth', query: { next: '/watch/room/<id>' } })`
    - Test 12: On unmount, calls `roomHandle.disconnect()`
    - Test 13: No `font-bold` in rendered HTML
  </behavior>
  <action>
    Create `frontend/web/src/views/WatchTogetherView.vue` implementing the structure in <behavior>.

    Player imports MUST use `defineAsyncComponent` to keep the WT view chunk under the 30KB target (WT-NF-04). Mirror Anime.vue:928-937 exactly:
    ```typescript
    const KodikPlayer = defineAsyncComponent(() => import('@/components/player/KodikPlayer.vue'))
    const AnimeLibPlayer = defineAsyncComponent(() => import('@/components/player/AnimeLibPlayer.vue'))
    const OurEnglishPlayer = defineAsyncComponent(() => import('@/components/player/OurEnglishPlayer.vue'))
    const HanimePlayer = defineAsyncComponent(() => import('@/components/player/HanimePlayer.vue'))
    const RawPlayer = defineAsyncComponent(() => import('@/components/player/RawPlayer.vue'))
    ```
    This way the player implementations don't inflate the WT view's chunk — each player loads only when its branch is rendered.

    Create `frontend/web/src/views/WatchTogetherView.spec.ts`:
    - `vi.mock('@/api/watch-together', () => ({ getRoom: vi.fn(), RoomGoneError: class extends Error {} }))`
    - `vi.mock('@/composables/useWatchTogetherRoom', () => ({ useWatchTogetherRoom: vi.fn(() => fakeHandle) }))`
    - Stub the player components and RoomSidebar via `global.stubs`
    - `vi.mock('vue-router', () => ({ useRoute: () => ({ params: { roomId: 'abc' }, fullPath: '/watch/room/abc' }), useRouter: () => ({ push: vi.fn() }) }))`
    - The 13 tests in <behavior>
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/views/WatchTogetherView.spec.ts && bunx tsc --noEmit && bunx eslint src/views/WatchTogetherView.vue</automated>
  </verify>
  <done>
    - All 13 tests pass
    - 5-way player switch follows the v-if/v-else-if chain pattern (no non-conditional elements between branches — MEMORY rule)
    - `defineAsyncComponent` used for all 5 player imports
    - tsc/eslint clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Register the `/watch/room/:roomId` route with `requiresAuth: true` + lazy load</name>
  <read_first>
    - frontend/web/src/router/index.ts (find the right insertion point — alphabetical-ish; place near other `/watch/*` routes; the `/anime/:id/watch` redirect exists at line 36-39, so a sensible neighbor is after `/themes` block or after `/game/:roomId`)
  </read_first>
  <action>
    Edit `frontend/web/src/router/index.ts` to add ONE new route entry inside the `routes` array. Place after the `/game/:roomId` block (line 78-83) for thematic grouping with other room-style routes:

    ```typescript
    {
      // Workstream watch-together / Phase 2 (WT-SHELL-01) — synchronized
      // watch-with-friends room. requireAuth via meta; redirects to /auth?next=…
      // through the existing global beforeEach guard.
      path: '/watch/room/:roomId',
      name: 'watch-together-room',
      component: () => import('@/views/WatchTogetherView.vue'),
      meta: { titleKey: 'watch_together.title', requiresAuth: true }
    },
    ```

    Verify the lazy-load syntax matches the router's existing pattern (`() => import('@/views/X.vue')`) — this triggers Vite's code-splitting → separate chunk → contributes to the WT-NF-04 budget.

    DO NOT modify any other routes. DO NOT change the beforeEach guard — it already handles `requiresAuth` via `sessionStorage.setItem('returnUrl', to.fullPath)` then redirects to `/auth` (lines 195-199 of router/index.ts). When a user later navigates from `/auth` back via `sessionStorage.getItem('returnUrl')`, they'll land on `/watch/room/:roomId` directly. This satisfies WT-SHELL-01's "redirect to `/login?next=/watch/room/:roomId`" requirement using the project's existing convention (`returnUrl` in sessionStorage, not `?next=` query) — note this deviation in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend/web && bunx tsc --noEmit && grep -c "watch-together-room\|/watch/room/:roomId" src/router/index.ts</automated>
  </verify>
  <done>
    - Route present in `router/index.ts` exactly as specified
    - `name: 'watch-together-room'` (not duplicating any other route name)
    - `requiresAuth: true` set
    - tsc clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Bundle size audit for the WT view chunk</name>
  <read_first>
    - frontend/web/vite.config.ts (to understand the chunk output naming if available)
    - WT-NF-04 in REQUIREMENTS.md (<30KB gz budget)
  </read_first>
  <action>
    Run a production build and measure the WT chunk size:
    ```bash
    cd frontend/web && bun run build 2>&1 | tail -100
    ```

    From the build output, find the chunk whose name contains `WatchTogetherView` (Vite typically names lazy chunks after the source file). The line will look like `dist/assets/WatchTogetherView-abc123.js   12.34 kB │ gzip:  4.56 kB`.

    Record the gzipped size. If `gzip` column < 30KB → PASS the budget. If ≥ 30KB → document the over-budget in 02.8-SUMMARY.md with a top-5 import audit (run `bunx vite-bundle-visualizer` or `bunx source-map-explorer dist/assets/WatchTogetherView-*.js` to identify the culprit). Phase 2 STILL ships even if over-budget; the requirement says "audit imports" not "must ship under". The follow-up audit lives in 02.10 phase summary.

    Cite the exact bytes in the plan summary.

    NOTE: The 5 lazy-loaded players (KodikPlayer etc.) live in their OWN chunks already (Anime.vue uses the same defineAsyncComponent pattern → shared chunk → NOT counted in the WT view's chunk). So the WT view chunk should contain only: WatchTogetherView itself, RoomSidebar + MemberList + ChatPanel + ReactionPalette + ReactionBurstOverlay + InviteButton (if eagerly imported anywhere — it's NOT in this view, only in Anime.vue), useWatchTogetherRoom composable, the types module, and the API client. Expected size: comfortably under 30KB.
  </action>
  <verify>
    <automated>cd frontend/web && bun run build 2>&1 | grep -E "WatchTogetherView.*kB" | tee /tmp/wt-chunk-size.txt && test -s /tmp/wt-chunk-size.txt</automated>
  </verify>
  <done>
    - `bun run build` exits 0
    - WatchTogetherView chunk found in build output
    - Gzipped size measured and recorded
    - If > 30KB: top-5 imports identified and noted for follow-up; phase still ships
  </done>
</task>

</tasks>

<verification>
- `bunx vitest run src/views/WatchTogetherView.spec.ts` → all 13 tests green
- `bunx tsc --noEmit` → clean
- `bunx eslint src/views/WatchTogetherView.vue src/router/index.ts` → clean
- `bun run build` exits 0
- Grep gate: `grep -c "watch-together-room" frontend/web/src/router/index.ts` == 1 (route registered exactly once)
- Bundle gate: WatchTogetherView chunk found in `dist/assets/` (verified by build output)
</verification>

<success_criteria>
- A logged-in user can navigate to `/watch/room/<id>` and see the assembled view: the correct `<*Player>` mounted with `:room` prop, RoomSidebar to the right (desktop) / below (mobile), ReactionBurstOverlay layered over the player.
- 410 → "room ended" state with back button works.
- CAPACITY_FULL → "room full" state works.
- AUTH_EXPIRED → router pushes to `/auth?next=...`.
- WT view chunk size measured (under 30KB target or noted).
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/02-frontend-shell/02.8-SUMMARY.md` documenting:
- The route entry (paste the literal block)
- WT view chunk size (gzipped bytes from build output)
- Any deviations from CONTEXT.md (e.g. the `returnUrl` sessionStorage convention vs `?next=` query param)
- The 5-way player switch's v-if/v-else-if chain (paste the template chunk)
</output>
