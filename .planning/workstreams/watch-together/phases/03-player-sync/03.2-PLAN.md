---
id: 03.2
phase: 03-player-sync
workstream: watch-together
title: Wire usePlayerSyncBridge into AnimeLibPlayer + OurEnglishPlayer
wave: 2
depends_on: [03.1]
files_modified:
  - frontend/web/src/components/player/AnimeLibPlayer.vue
  - frontend/web/src/components/player/OurEnglishPlayer.vue
requirements:
  - WT-SYNC-02
  - WT-SYNC-05
  - WT-SYNC-06
autonomous: true
type: execute
UXΔ: "+0 (Ambiguous)"
CDI: "0.01 * 3"
MVQ: "Sprite 80%/85%"
must_haves:
  truths:
    - "AnimeLibPlayer with props.room set synchronizes play/pause/seek to the room"
    - "OurEnglishPlayer with props.room set synchronizes play/pause/seek to the room"
    - "Both players with props.room null/undefined behave identically to pre-Phase-3 (zero regression)"
  artifacts:
    - path: frontend/web/src/components/player/AnimeLibPlayer.vue
      provides: "Bridge wired via single call replacing the `void props.room` anchor"
      contains: "usePlayerSyncBridge"
    - path: frontend/web/src/components/player/OurEnglishPlayer.vue
      provides: "Bridge wired same"
      contains: "usePlayerSyncBridge"
  key_links:
    - from: "AnimeLibPlayer.vue"
      to: "usePlayerSyncBridge"
      via: "if (props.room) call in setup, passing videoRef"
      pattern: "if \\(props\\.room\\) usePlayerSyncBridge"
    - from: "OurEnglishPlayer.vue"
      to: "usePlayerSyncBridge"
      via: "same pattern"
      pattern: "if \\(props\\.room\\) usePlayerSyncBridge"
---

<objective>
Replace the Phase 2 anchor `void props.room` in two HTML5 players with the real bridge call from plan 03.1. AnimeLib + OurEnglish picked together because both use the `<video ref="videoRef">` + `:video-element="videoRef"` pattern with backend-proxy MP4/HLS — they share enough mechanism that touching them together amortizes context.

Purpose: WT-SYNC-02 (HTML5 wiring) for 2 of 4 HTML5 players. The other 2 (Hanime, Raw) land in 03.3 (parallel-safe, no file overlap).

Output: Both player files contain the bridge call; both still type-check, lint clean, and existing tests pass. No new tests in this plan — the bridge is exhaustively tested in 03.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/03-player-sync/03-CONTEXT.md
@.planning/workstreams/watch-together/phases/03-player-sync/03.1-PLAN.md
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
@frontend/web/src/composables/useWatchTogetherRoom.ts
@CLAUDE.md

<interfaces>
<!-- The exact anchor each player file has from Phase 2 plan 02.7. -->

`AnimeLibPlayer.vue` (line 333-344):
```typescript
const props = defineProps<{
  // ... existing props ...
  room?: WatchTogetherRoomHandle | null
}>()
// Phase 2 (02.7) — reference `props.room` so eslint/no-unused-vars + vue-tsc stay happy.
// Phase 3 replaces this with real WatchTogether sync wiring.
void props.room
```
`videoRef` declared at line 374 as `ref<HTMLVideoElement | null>(null)`.

`OurEnglishPlayer.vue` (line 206-214): identical structure.
`videoRef` declared at line 217 as `ref<HTMLVideoElement | null>(null)`.

Bridge signature (from plan 03.1):
```typescript
import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'
usePlayerSyncBridge(videoRef: Ref<HTMLVideoElement | null>, room: WatchTogetherRoomHandle): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire bridge into AnimeLibPlayer.vue</name>
  <files>frontend/web/src/components/player/AnimeLibPlayer.vue</files>
  <read_first>
    - /data/animeenigma/frontend/web/src/components/player/AnimeLibPlayer.vue (full file — need to confirm the `videoRef` exists at the location grep reported, line 374, and the anchor at line 344)
    - /data/animeenigma/frontend/web/src/composables/usePlayerSyncBridge.ts (the file shipped by 03.1 — read its JSDoc + signature)
  </read_first>
  <action>
1. Add the import. Place it alphabetically among other `@/composables/...` imports OR right after the existing `WatchTogetherRoomHandle` type import:
   ```typescript
   import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'
   ```

2. Replace the line `void props.room` (around line 344) with:
   ```typescript
   // Phase 3 (03.2): wire real sync when a room is provided. When room is
   // null/undefined the bridge is never instantiated and the player behaves
   // exactly as it did pre-Phase-3 (zero regression).
   if (props.room) {
     usePlayerSyncBridge(videoRef, props.room)
   }
   ```

3. Confirm no other reference to `void props.room` remains in the file.

4. Do NOT touch any other code paths in the file: existing progress saving, subtitle rendering, HLS attachment — all unchanged. The bridge attaches its own native `play`/`pause`/`seeked` listeners and is orthogonal to the player's existing logic.

5. Do NOT add new props or change the `room` prop signature. The prop signature is locked per Phase 2 SUMMARY §"Locked decisions".
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx tsc --noEmit 2>&1 | head -20 && bunx eslint src/components/player/AnimeLibPlayer.vue && test $(grep -c "void props.room" src/components/player/AnimeLibPlayer.vue) -eq 0 && test $(grep -c "usePlayerSyncBridge(videoRef, props.room)" src/components/player/AnimeLibPlayer.vue) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` clean
    - `bunx eslint src/components/player/AnimeLibPlayer.vue` clean
    - `grep -c "void props.room" src/components/player/AnimeLibPlayer.vue` returns 0 (anchor removed)
    - `grep -c "usePlayerSyncBridge(videoRef, props.room)" src/components/player/AnimeLibPlayer.vue` returns 1
    - `grep -c "import { usePlayerSyncBridge }" src/components/player/AnimeLibPlayer.vue` returns 1
  </acceptance_criteria>
  <done>Bridge wired in AnimeLibPlayer; type + lint clean; Phase 2 anchor fully replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bridge into OurEnglishPlayer.vue</name>
  <files>frontend/web/src/components/player/OurEnglishPlayer.vue</files>
  <read_first>
    - /data/animeenigma/frontend/web/src/components/player/OurEnglishPlayer.vue (full file)
    - /data/animeenigma/frontend/web/src/composables/usePlayerSyncBridge.ts
  </read_first>
  <action>
Apply the IDENTICAL transformation as Task 1, but in OurEnglishPlayer.vue:

1. Add import `import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'`
2. Replace the `void props.room` line (around line 214) with the 4-line `if (props.room) { usePlayerSyncBridge(videoRef, props.room) }` block, plus the same explanatory comment.
3. No other changes.
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx tsc --noEmit 2>&1 | head -20 && bunx eslint src/components/player/OurEnglishPlayer.vue && test $(grep -c "void props.room" src/components/player/OurEnglishPlayer.vue) -eq 0 && test $(grep -c "usePlayerSyncBridge(videoRef, props.room)" src/components/player/OurEnglishPlayer.vue) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` clean
    - `bunx eslint src/components/player/OurEnglishPlayer.vue` clean
    - `grep -c "void props.room"` returns 0
    - `grep -c "usePlayerSyncBridge(videoRef, props.room)"` returns 1
    - `grep -c "import { usePlayerSyncBridge }"` returns 1
  </acceptance_criteria>
  <done>Bridge wired in OurEnglishPlayer; type + lint clean; anchor fully replaced.</done>
</task>

</tasks>

<verification>
- Both player files import `usePlayerSyncBridge` and call it inside an `if (props.room)` guard.
- Neither file contains `void props.room` anymore.
- Full project `bunx tsc --noEmit` exits 0.
- `bunx vitest run` for any AnimeLib or OurEnglish player spec still passes (existing tests must continue to work — the bridge is a no-op when room is null).
</verification>

<success_criteria>
AnimeLibPlayer and OurEnglishPlayer, when mounted in WatchTogetherView with a live `room` handle, sync play/pause/seek to the room. When mounted in WatchView (no room prop), they behave exactly as today. Zero existing tests broken.
</success_criteria>

<output>
Create `.planning/workstreams/watch-together/phases/03-player-sync/03.2-SUMMARY.md` documenting the two file changes (3-4 lines added per file), the grep evidence, and zero broken tests.
</output>
