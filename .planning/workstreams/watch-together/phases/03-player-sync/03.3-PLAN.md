---
id: 03.3
phase: 03-player-sync
workstream: watch-together
title: Wire usePlayerSyncBridge into HanimePlayer + RawPlayer
wave: 2
depends_on: [03.1]
files_modified:
  - frontend/web/src/components/player/HanimePlayer.vue
  - frontend/web/src/components/player/RawPlayer.vue
requirements:
  - WT-SYNC-02
  - WT-SYNC-05
  - WT-SYNC-06
autonomous: true
type: execute
UXΔ: "+0 (Ambiguous)"
CDI: "0.01 * 3"
MVQ: "Sprite 80%/85%"
must_haves:
  truths:
    - "HanimePlayer with props.room set synchronizes play/pause/seek to the room"
    - "RawPlayer with props.room set synchronizes play/pause/seek to the room"
    - "Both players with props.room null/undefined behave identically to pre-Phase-3 (zero regression)"
  artifacts:
    - path: frontend/web/src/components/player/HanimePlayer.vue
      provides: "Bridge wired via single call replacing the `void props.room` anchor"
      contains: "usePlayerSyncBridge"
    - path: frontend/web/src/components/player/RawPlayer.vue
      provides: "Bridge wired same"
      contains: "usePlayerSyncBridge"
  key_links:
    - from: "HanimePlayer.vue"
      to: "usePlayerSyncBridge"
      via: "if (props.room) call in setup, passing videoRef"
      pattern: "if \\(props\\.room\\) usePlayerSyncBridge"
    - from: "RawPlayer.vue"
      to: "usePlayerSyncBridge"
      via: "same pattern"
      pattern: "if \\(props\\.room\\) usePlayerSyncBridge"
---

<objective>
Replace the Phase 2 anchor `void props.room` in the remaining two HTML5 players (Hanime + Raw) with the bridge call. Parallel to 03.2 — disjoint files, no merge conflicts.

Purpose: Complete WT-SYNC-02 (HTML5 wiring) across all 4 HTML5 players.

Output: Both player files contain the bridge call; both still type-check and lint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/03-player-sync/03-CONTEXT.md
@.planning/workstreams/watch-together/phases/03-player-sync/03.1-PLAN.md
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
@frontend/web/src/composables/useWatchTogetherRoom.ts
@CLAUDE.md

<interfaces>
`HanimePlayer.vue` (line 160-170):
```typescript
const props = defineProps<{
  // ... existing props ...
  room?: WatchTogetherRoomHandle | null
}>()
void props.room
```
`videoRef` declared at line 186.

`RawPlayer.vue` (line 161-168): identical structure.
`videoRef` declared at line 173.

Bridge signature (from plan 03.1):
```typescript
import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'
usePlayerSyncBridge(videoRef: Ref<HTMLVideoElement | null>, room: WatchTogetherRoomHandle): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire bridge into HanimePlayer.vue</name>
  <files>frontend/web/src/components/player/HanimePlayer.vue</files>
  <read_first>
    - /data/animeenigma/frontend/web/src/components/player/HanimePlayer.vue (full file — note the existing `watch(videoRef, ...)` pattern at line 417 — the bridge attaches listeners via its own watcher, no conflict)
    - /data/animeenigma/frontend/web/src/composables/usePlayerSyncBridge.ts (file shipped by 03.1)
  </read_first>
  <action>
1. Add `import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'` to the imports block.

2. Replace the line `void props.room` (around line 170) with:
   ```typescript
   // Phase 3 (03.3): wire real sync when a room is provided. Zero behavior
   // change when room is null/undefined.
   if (props.room) {
     usePlayerSyncBridge(videoRef, props.room)
   }
   ```

3. Do NOT touch the existing `watch(videoRef, (el) => { ... })` block at line 417 — it manages stream attachment after videoRef mounts, orthogonal to the bridge's own videoRef watcher.

4. No other changes.
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx tsc --noEmit 2>&1 | head -20 && bunx eslint src/components/player/HanimePlayer.vue && test $(grep -c "void props.room" src/components/player/HanimePlayer.vue) -eq 0 && test $(grep -c "usePlayerSyncBridge(videoRef, props.room)" src/components/player/HanimePlayer.vue) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` clean
    - `bunx eslint src/components/player/HanimePlayer.vue` clean
    - `grep -c "void props.room"` returns 0
    - `grep -c "usePlayerSyncBridge(videoRef, props.room)"` returns 1
    - `grep -c "import { usePlayerSyncBridge }"` returns 1
    - Existing `watch(videoRef, (el) => {` block at ~line 417 still present (grep -c "watch(videoRef" returns ≥1)
  </acceptance_criteria>
  <done>Bridge wired in HanimePlayer; type + lint clean; existing stream-attach watcher untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bridge into RawPlayer.vue</name>
  <files>frontend/web/src/components/player/RawPlayer.vue</files>
  <read_first>
    - /data/animeenigma/frontend/web/src/components/player/RawPlayer.vue (full file)
    - /data/animeenigma/frontend/web/src/composables/usePlayerSyncBridge.ts
  </read_first>
  <action>
1. Add `import { usePlayerSyncBridge } from '@/composables/usePlayerSyncBridge'`.

2. Replace the `void props.room` line (around line 168) with the same 4-line `if (props.room) { usePlayerSyncBridge(videoRef, props.room) }` block + comment.

3. No other changes.
  </action>
  <verify>
    <automated>cd /data/animeenigma/frontend/web && bunx tsc --noEmit 2>&1 | head -20 && bunx eslint src/components/player/RawPlayer.vue && test $(grep -c "void props.room" src/components/player/RawPlayer.vue) -eq 0 && test $(grep -c "usePlayerSyncBridge(videoRef, props.room)" src/components/player/RawPlayer.vue) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` clean
    - `bunx eslint src/components/player/RawPlayer.vue` clean
    - `grep -c "void props.room"` returns 0
    - `grep -c "usePlayerSyncBridge(videoRef, props.room)"` returns 1
    - `grep -c "import { usePlayerSyncBridge }"` returns 1
  </acceptance_criteria>
  <done>Bridge wired in RawPlayer; type + lint clean; anchor fully replaced.</done>
</task>

</tasks>

<verification>
- After 03.2 + 03.3 both ship, `grep -rn "void props.room" frontend/web/src/components/player/` should return 0 hits across the 4 HTML5 players (Kodik's anchor is replaced in plan 03.4).
- `bunx tsc --noEmit` clean.
- All existing player tests still pass.
</verification>

<success_criteria>
HanimePlayer and RawPlayer, mounted with a live room handle, sync play/pause/seek. Mounted without a room (existing WatchView), behave exactly as today.
</success_criteria>

<output>
Create `.planning/workstreams/watch-together/phases/03-player-sync/03.3-SUMMARY.md` documenting the two file changes, grep evidence, and zero broken tests.
</output>
