---
workstream: watch-together
phase: 04-state-switching
plan: 6
type: execute
wave: 4
depends_on: [4, 5]
files_modified:
  - frontend/web/e2e/watch-together-state-switching.spec.ts
  - .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md
  - .planning/workstreams/watch-together/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - WT-STATE-05
tags: [watch-together, e2e, phase-close]

must_haves:
  truths:
    - "frontend/web/e2e/watch-together-state-switching.spec.ts exists; two browsers join the same room; state changes propagate per WT-STATE-05"
    - "The spec follows the Phase 2 + Phase 3 e2e pattern: test.beforeAll probes live stack via /api/anime/_/scraper/health and test.skip()s if down; uses ui_audit_bot login + ephemeral second user"
    - "At least 4 e2e scenarios pass: (1) episode switch propagates, (2) player switch propagates, (3) translation switch propagates, (4) invalid episode yields sender-only EPISODE_UNAVAILABLE toast (no second-browser change)"
    - "04-PHASE-SUMMARY.md exists with >= 8 H2 sections per the workstream summary convention; cross-references all per-plan SUMMARYs (04.1-04.5)"
    - "Phase summary documents all locked decisions, deviations from CONTEXT.md, what Phase 5 inherits, and a live smoke transcript"
    - "ROADMAP.md Phase 4 entry is updated with status: complete, plans_shipped: [04.1..04.6], requirements_covered: [WT-STATE-01..05]"
    - "STATE.md is updated to reflect Phase 4 complete + Phase 5 next"
  artifacts:
    - path: frontend/web/e2e/watch-together-state-switching.spec.ts
      provides: "Two-browser state-switching e2e — episode/player/translation propagation + sender-only error UX"
      contains: "watch-together-state-switching"
      min_lines: 250
    - path: .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md
      provides: "Phase 4 close-out summary"
      contains: "Phase 4 (State Switching)"
      min_lines: 150
  key_links:
    - from: 04-PHASE-SUMMARY.md
      to: 04.1-SUMMARY.md, 04.2-SUMMARY.md, 04.3-SUMMARY.md, 04.4-SUMMARY.md, 04.5-SUMMARY.md
      via: "Cross-references table"
      pattern: "04.\\d-SUMMARY"
---

<objective>
Close out Phase 4 with the two-browser state-switching Playwright e2e spec, the Phase 4 close-out summary, and the ROADMAP + STATE updates. This is the canonical regression check for WT-STATE-05 and the artifact Phase 5 (Polish + Production-Ship) inherits as its starting point.

Purpose: produce the deliverable that Phase 5 inherits cleanly. After this plan completes, `make redeploy-watch-together && make redeploy-gateway` plus a fresh browser session should be able to run through episode/player/translation switching in a shared room with full validation + propagation + error UX.

Output: 1 new e2e spec, 1 new PHASE-SUMMARY, 2 documentation files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/04-state-switching/04-CONTEXT.md
@.planning/workstreams/watch-together/REQUIREMENTS.md
@.planning/workstreams/watch-together/ROADMAP.md
@.planning/workstreams/watch-together/phases/01-backend-foundation/01-PHASE-SUMMARY.md
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
@.planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md
@frontend/web/e2e/watch-together-shell.spec.ts
@frontend/web/e2e/watch-together-sync.spec.ts

<interfaces>
The reference e2e specs already exist:
- frontend/web/e2e/watch-together-shell.spec.ts (Phase 2): two-browser join + chat + reactions + leave; test.beforeAll skip-on-stack-down pattern; ui_audit_bot login via real POST /api/auth/login; ephemeral second user via POST /api/auth/register; seeded anime UUID resolved from /api/users/me/anime-list?status=watching.
- frontend/web/e2e/watch-together-sync.spec.ts (Phase 3): same skeleton but exercises play/pause/seek + drift. 6 tests × 3 projects = 18 listings.

Mirror the structure: test.describe('watch-together state switching', () => { ... }) with test.beforeAll, helpers for login + room creation, then per-scenario test() blocks. Use the Playwright `Page` API for both browsers (open one context, request a second context via `browser.newContext()`).

The Playwright project config (frontend/web/playwright.config.ts) lists 3 projects: chromium, firefox, mobile-chrome. The new spec inherits all 3 automatically.

For asserting backend state propagation: the simplest probe is the `room:state_changed` reactive DOM update. E.g. after host clicks a player tab, follower's PlayerTabBar shows the new active tab; after host's selectEpisode click, follower's player chunk re-mounts to the new episode (verify via component finder or a data-testid attribute).

A data-testid attribute is the cleanest hook. If PlayerTabBar.vue from 04.4 doesn't already have one, the e2e author can add `data-testid="player-tab-${kind}"` to each button in 04.4's component — coordinate via the e2e spec's locators. (Pre-coordinate: include the data-testid in 04.4's PlayerTabBar; the spec uses it directly.)

For the error-toast assertion: the project's Toaster (mounted in App.vue) renders toasts with a stable DOM structure. Grep for the existing Toaster component to confirm the locator pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-browser state-switching e2e spec</name>
  <files>frontend/web/e2e/watch-together-state-switching.spec.ts</files>
  <read_first>
    - frontend/web/e2e/watch-together-shell.spec.ts (full file — copy the test.beforeAll skip-on-stack-down, login helper, room creation helper, ephemeral second user helper)
    - frontend/web/e2e/watch-together-sync.spec.ts (full file — similar skeleton with two-browser scaffolding)
    - frontend/web/src/components/watch-together/PlayerTabBar.vue (locator: data-testid="player-tab-{kind}" — coordinate with 04.4 if missing)
    - frontend/web/playwright.config.ts (just to confirm the 3-project setup is inherited)
  </read_first>
  <behavior>
    Spec produces >= 4 test() blocks (× 3 Playwright projects = 12+ listings):

    Test 1 — episode_switch_propagates:
      - Both browsers join the same room (using the helper from watch-together-shell.spec.ts).
      - Host (browser A) clicks the in-player "next episode" button OR triggers the visible episode picker. (For AnimeLib: the episode is in the side panel inside the player; pick by data-testid or visible label.)
      - Wait up to 2000ms for follower (browser B) to observe room.episode_id change. Assertion vehicle: the follower's URL or DOM shows the new episode_id. Best path is exposing a data-testid="active-episode" on the player surface (small UI change, defer to executor judgment).
      - Pass condition: follower's active episode equals the host's new episode within 2s.

    Test 2 — player_switch_propagates:
      - Both browsers join.
      - Host clicks PlayerTabBar [data-testid="player-tab-animelib"] (assuming room starts on kodik).
      - Within 2000ms, follower's active tab visibly switches AND a new player component is mounted (assert via a player-specific data-testid like "animelib-player").
      - Pass condition: follower's mounted player matches host's new player.

    Test 3 — translation_switch_propagates:
      - Both browsers join; ensure player is AnimeLib (the most-likely-to-have-multiple-translations player for the seeded UI audit bot's anime). If the seeded anime has only one translation, skip the test with a console message.
      - Host clicks the second translation in the AnimeLib translation list.
      - Within 2000ms, follower's selected translation in the same list changes to match.

    Test 4 — invalid_episode_yields_sender_only_error:
      - Both browsers join.
      - Host directly emits state:change_episode with an obviously invalid episode_id (e.g. "99999") via the in-page composable handle — accessible via window.__wtTestHook if 04.4/04.5 wires one; otherwise via page.evaluate() that calls the composable through Vue devtools mock. If no test hook is available, this scenario can manipulate the WebSocket directly via the established connection (page.evaluate() that sends a frame on the existing WS).
      - Assert: within 2000ms, host's UI shows a toast with the localized message text (or matching data-testid="toast-state_change_episode_unavailable" if Toaster renders test ids).
      - Assert: follower's room.episode_id is UNCHANGED (still on whatever it was before).

      If the test hook plumbing is too complex for this plan, this test may be marked test.fixme with a TODO referencing v1.1; the other 3 tests must pass. Document the decision in 04.6-SUMMARY.md.

    Skeleton requirements (mirror Phase 2 + 3 e2e):
    - test.beforeAll: probe gateway /api/anime/_/scraper/health; if not 200, call test.skip() with reason "stack not up".
    - test.beforeAll: login as ui_audit_bot (using the existing helper or repeating the pattern: POST /api/auth/login, set token in localStorage, set user object in localStorage).
    - test.beforeAll: register an ephemeral second user with Date.now() suffix; capture both auth tokens.
    - For each test: createContextWithToken helper produces a Playwright BrowserContext + Page with the right auth state.
    - Helper: createRoom(token, animeUuid, initialEpisode, initialPlayer, initialTranslation) returns roomId + inviteUrl; host's first context creates, follower's context navigates.
    - tests run sequentially within the describe (test.describe.configure({ mode: 'serial' }) acceptable to keep the room state isolated per test).
  </behavior>
  <action>
    Create frontend/web/e2e/watch-together-state-switching.spec.ts (>= 250 lines including the skeleton helpers). Mirror the structure of frontend/web/e2e/watch-together-shell.spec.ts and frontend/web/e2e/watch-together-sync.spec.ts.

    Key implementation notes:
    - Use test.describe.configure({ mode: 'serial' }) so per-test room setup doesn't race.
    - Use a stable seed anime UUID resolved at runtime from /api/users/me/anime-list?status=watching (same pattern as the existing spec). If the bot's list is empty (rare in CI), test.skip the suite with a hint to run scripts/seed-ui-audit-user.sh.
    - For PlayerTabBar locators: page.locator('[data-testid="player-tab-animelib"]'). If 04.4 didn't add data-testid attributes, add them as part of this plan's edit to PlayerTabBar.vue (1-line per button). Coordinate via an inline comment.
    - For player-mounted assertions: each player SFC's root element should have a data-testid distinguishing it (e.g. data-testid="player-root-kodik"). If absent, add it as part of THIS plan (one root-element data-testid per player file).
    - For toast assertions: the existing Toaster renders each toast with a class like .toast or data-testid="toast"; locate by text match OR by adding a data-testid attribute if absent.
    - 2000ms tolerance per assertion (matches Phase 3 e2e's "feels-instant" budget).
    - Document the spec's "designed to test.skip when stack is down" behavior in the docblock.
    - At the bottom: a cleanup hook that DELETEs the room as host (best-effort try/catch).

    For the invalid-episode test (Test 4): the cleanest approach is to expose a window.__wtTestRoom = roomHandle hook in WatchTogetherView.vue ONLY in test mode (gate on import.meta.env.MODE === 'test' or a VITE_TEST_HOOK flag). If introducing this is risky for prod build, mark test 4 as test.fixme with a TODO and ship the other 3 tests — the unit-test coverage in 04.3 already validates the sender-only error path for the backend; the e2e is gravy here.

    Skip behavior for missing-translation scenarios: console.warn + test.skip with a hint to seed the bot's anime list with a multi-translation entry.
  </action>
  <acceptance_criteria>
    - cd frontend/web && bunx playwright test e2e/watch-together-state-switching.spec.ts --list parses cleanly (>= 12 listings: >= 4 tests × 3 projects, or >= 9 if test 4 is fixme'd)
    - cd frontend/web && bunx tsc --noEmit clean (spec compiles under strict TS)
    - cd frontend/web && bunx eslint e2e/watch-together-state-switching.spec.ts clean
    - The spec file is >= 250 lines (includes helpers + 4 test bodies)
    - grep -c "test.beforeAll" frontend/web/e2e/watch-together-state-switching.spec.ts is >= 1
    - grep -c "test.skip" frontend/web/e2e/watch-together-state-switching.spec.ts is >= 1 (skip-on-stack-down)
    - grep -c "WT-STATE-05" frontend/web/e2e/watch-together-state-switching.spec.ts is >= 1 (docblock anchor)
    - If data-testid was added to PlayerTabBar.vue or to player root elements, those changes are documented in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd frontend/web && bunx playwright test e2e/watch-together-state-switching.spec.ts --list && bunx tsc --noEmit</automated>
  </verify>
  <done>e2e spec exists, lists cleanly across 3 Playwright projects, compiles + lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 4 close-out summary + ROADMAP + STATE updates + live smoke transcript</name>
  <files>.planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md, .planning/workstreams/watch-together/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/workstreams/watch-together/phases/01-backend-foundation/01-PHASE-SUMMARY.md (template for the workstream's phase-summary structure)
    - .planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
    - .planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md
    - .planning/workstreams/watch-together/ROADMAP.md (Phase 4 section)
    - .planning/STATE.md (current state)
    - all 04.1-SUMMARY.md through 04.5-SUMMARY.md as they land (they're produced by their respective plan executors)
  </read_first>
  <behavior>
    PHASE-SUMMARY structure (mirror Phase 3's 14+ H2 sections):
    - Frontmatter (workstream, milestone, phase, status: complete, closed: <date>, plans_shipped: [04.1..04.6], requirements_covered: [WT-STATE-01..05], acceptance_criteria_pass: "5/5", smoke_spec: frontend/web/e2e/watch-together-state-switching.spec.ts, unit_test_count: <fill in from per-plan summaries>, e2e_test_listings: ">= 12")
    - H1: Phase 4: State Switching — Summary
    - H2 sections (>= 14):
        1. Outcome (3-5 paragraphs — what the user can now do; what's invariant; what carries forward unchanged)
        2. Locked decisions (table — at least 15 rows for Phase 5 to inherit)
        3. Files created/modified (grouped per source area)
        4. Acceptance criteria from ROADMAP.md Phase 4 (5/5, table)
        5. Test coverage (per-spec breakdown; unit + component + e2e)
        6. Build artifact (WatchTogetherView chunk size; comparison to Phase 3 baseline)
        7. Smoke transcript (curl + bunx playwright list outputs)
        8. Deviations from CONTEXT.md (zero or per-deviation analysis)
        9. Threat flags (most likely none)
        10. What Phase 5 inherits
        11. Cross-references (table linking 04.1-04.6 SUMMARY files)
        12. Live infrastructure verified
        13. Self-check (checklist table)
        14. Next: Phase 5 — Polish + Production-Ship

    ROADMAP.md update: edit the Phase 4 entry to mark status: complete and add the requirements_covered list. Match the format used for Phases 1-3 (which are already complete).

    STATE.md update: set the watch-together workstream's current phase to 5 (or to "ready for phase 5"). Match existing STATE.md format.

    Live smoke transcript: execute the following commands sequentially against the live stack and paste the actual output:

      cd services/watch-together && go test ./... -count=1 -race | tail -5
      cd services/catalog && go test ./... -count=1 -race | tail -5
      cd frontend/web && bunx vitest run src/views/WatchTogetherView.spec.ts src/components/watch-together/PlayerTabBar.spec.ts | tail -10
      cd frontend/web && bunx playwright test e2e/watch-together-state-switching.spec.ts --list | head -20
      curl -s http://localhost:8081/internal/anime/57466/episodes/validate?player=animelib&episode_id=1&translation_id=42
      curl -s http://localhost:8000/api/watch-together/_/health 2>&1 | head -5 || echo "(gateway health probe placeholder — replace with actual healthcheck URL)"

    The transcript section MUST contain real command output (not placeholders). Use the make redeploy targets if needed: make redeploy-watch-together; make redeploy-catalog; make redeploy-gateway.
  </behavior>
  <action>
    1. Read all 5 per-plan SUMMARYs (04.1-SUMMARY through 04.5-SUMMARY) and extract: file lists, test counts, decisions, deviations.

    2. Write `.planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md` per the structure above. Aggregate per-plan summaries into the unified phase-level view. Mirror the depth and tone of 01/02/03-PHASE-SUMMARY.md.

    3. Live-execute the smoke commands. If any service is down, redeploy with the appropriate `make redeploy-*` target. Paste the actual output verbatim in the Smoke transcript section.

    4. Edit `.planning/workstreams/watch-together/ROADMAP.md`:
       - Find the Phase 4 section
       - Mark status as complete
       - Update the Plans line to enumerate plans 04.1 through 04.6
       - Add a line confirming all 5 requirements (WT-STATE-01..05) are covered
       - Match the format used by Phases 1-3

    5. Edit `.planning/STATE.md`:
       - Find the watch-together workstream entry
       - Update the phase position to phase 5
       - Match the existing format

    6. Self-check: re-read 04-PHASE-SUMMARY.md after writing to confirm:
       - >= 14 H2 sections present
       - All 5 plan SUMMARYs referenced in the cross-references table
       - The live smoke transcript contains real (not placeholder) output
       - Deviations enumerated honestly (or "None" if none)
       - Phase 5 inheritance section reads cleanly into a `/gsd-plan-phase --ws watch-together 05-polish` invocation
  </action>
  <acceptance_criteria>
    - .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md exists, >= 150 lines, >= 14 H2 sections
    - grep -c "04.1-SUMMARY\|04.2-SUMMARY\|04.3-SUMMARY\|04.4-SUMMARY\|04.5-SUMMARY" on 04-PHASE-SUMMARY.md is >= 5 (each per-plan SUMMARY cross-referenced)
    - grep -c "## " 04-PHASE-SUMMARY.md is >= 14 (H2 section count)
    - ROADMAP.md Phase 4 entry shows complete status
    - STATE.md reflects Phase 4 complete + Phase 5 next
    - Live smoke transcript has real go test / bunx vitest / bunx playwright output (not placeholders)
    - Self-check checklist at the bottom of 04-PHASE-SUMMARY.md is all ✅
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md && wc -l .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md | awk '$1 >= 150 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Phase 4 closed; documentation complete; live smoke transcript verified; Phase 5 ready to plan.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Full workstream test sweep
cd services/watch-together && go test ./... -count=1 -race
cd services/catalog && go test ./... -count=1 -race
cd frontend/web && bunx vitest run
cd frontend/web && bunx playwright test e2e/watch-together-state-switching.spec.ts --list

# Documentation sanity
test -f .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md
grep -c '## ' .planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md
```

All exit 0; H2 count >= 14.
</verification>

<success_criteria>
- e2e spec exists, lists cleanly across 3 Playwright projects, exercises WT-STATE-05 scenarios
- PHASE-SUMMARY documents Phase 4 outcomes, decisions, deviations, what Phase 5 inherits
- Live smoke transcript shows real green output from go test + vitest + playwright list
- ROADMAP + STATE updated to reflect Phase 4 complete
- The 5 ROADMAP Phase 4 success criteria are addressed (host clicks next-episode propagates, host switches player propagates, host switches translation propagates, invalid episode yields sender-only error, drift correction continues working post-state-change)
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/04-state-switching/04.6-SUMMARY.md` AND `.planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md`.

Commit changes with the co-authors:
  Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
  Co-Authored-By: 0neymik0 <0neymik0@gmail.com>
  Co-Authored-By: NANDIorg <super.egor.mamonov@yandex.ru>
</output>
