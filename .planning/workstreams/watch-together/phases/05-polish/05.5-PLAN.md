---
workstream: watch-together
phase: 05-polish
plan: 5
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/web/src/views/WatchTogetherView.vue
  - frontend/web/src/views/WatchTogetherView.spec.ts
  - frontend/web/src/composables/useWatchTogetherRoom.ts
  - frontend/web/src/composables/__tests__/useWatchTogetherRoom.spec.ts
  - frontend/web/src/locales/en.json
  - frontend/web/src/locales/ru.json
  - frontend/web/src/locales/__tests__/watch-together-keys.spec.ts
autonomous: true
requirements:
  - WT-POLISH-04
  - WT-POLISH-05
  - WT-POLISH-06
  - WT-POLISH-07

must_haves:
  truths:
    - "Joining a room that returns CAPACITY_FULL renders a dedicated 10/10 page with copy 'Room is full (10/10)' and a Return-to-anime button (NOT auto-retry, NOT generic 'room ended')"
    - "Visiting a URL for an expired room (REST 410) redirects to /anime/{animeId}/watch with toast 'This Watch Together room has ended'; falls back to '/' if animeId is unknown"
    - "On 410 returned MID-SESSION via room:closed event, the redirect + toast also fires (not just on initial REST pre-fetch)"
    - "Receiving AUTH_EXPIRED error envelope on the WS triggers a modal 'Your session has expired. Log in again to rejoin the room.' with a Login button; clicking pushes to /auth?next=/watch/room/{roomId} preserving the resume URL"
    - "After the user re-logs and returns to the room URL, the existing auth guard handles the redirect; the room rejoin is automatic via the existing composable connect path"
    - "All new user-facing strings are keyed via t() — zero raw key strings in the rendered DOM in both en and ru"
    - "The auth-expired modal blocks interaction (modal overlay with backdrop) — no way for the user to dismiss without choosing Login or actively navigating away"
  artifacts:
    - path: "frontend/web/src/views/WatchTogetherView.vue"
      provides: "Polished errorState branches: 'capacity' (existing), 'gone' (existing — now with router.push redirect + toast), 'auth-expired' (NEW modal); lastAnimeId sessionStorage-cached so redirect works after WS-only delivery of room:closed"
      contains: "errorState ===.*auth"
    - path: "frontend/web/src/composables/useWatchTogetherRoom.ts"
      provides: "onAuthExpired subscribe method (new); the existing onError already fires for AUTH_EXPIRED but the view will use the dedicated subscription for clarity"
      contains: "onAuthExpired"
    - path: "frontend/web/src/locales/en.json"
      provides: "+4 new keys: room_ended_redirect_toast, auth_expired_modal_title, auth_expired_modal_body, auth_expired_modal_login_button"
      contains: "auth_expired_modal_title"
    - path: "frontend/web/src/locales/ru.json"
      provides: "+4 matching Russian keys (parity-locked)"
      contains: "auth_expired_modal_title"
  key_links:
    - from: "WatchTogetherView.vue onError handler (existing)"
      to: "router.push + toast.push + modal display"
      via: "ERR_AUTH_EXPIRED branch now shows modal instead of immediate router.push (delegates click to user); ERR_CAPACITY_FULL still sets errorState; room:closed via onRoomClosed → router.push + toast (was just errorState='gone')"
      pattern: "ERR_AUTH_EXPIRED.*=>"
    - from: "WatchTogetherView.vue onRoomClosed handler"
      to: "router.push('/anime/{lastAnimeId}/watch') + toast"
      via: "lastAnimeId is captured from REST pre-fetch + persisted to sessionStorage on mount so the redirect target survives a mid-session WS-only room:closed"
      pattern: "sessionStorage.setItem.*lastAnimeId"
---

<objective>
Polish the three exceptional WatchTogetherView states (capacity-full, room-expired redirect, auth-expired modal) per WT-POLISH-04/05/06, plus complete the i18n smoke pass per WT-POLISH-07. Phase 2 shipped stub branches for capacity + room-ended; Phase 5 makes them feel intentional and adds the missing modal + redirect flows.

Purpose: WT-POLISH-04 → "Joining at-capacity room shows 10/10 page with clear CTA." WT-POLISH-05 → "Expired room URL redirects to anime watch page with toast." WT-POLISH-06 → "JWT expires mid-session prompts re-login; on return rejoin the same room." Each was deferred from Phase 2 explicitly (see 02-PHASE-SUMMARY.md Deviation 3). WT-POLISH-07 → "No raw key strings render in en or ru."

Output: WatchTogetherView with polished branches + new auth-expired modal + room-closed redirect; composable gains a dedicated onAuthExpired subscriber for code clarity; 4 new i18n keys in en + ru; smoke-verified in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/05-polish/05-CONTEXT.md
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
@frontend/web/src/views/WatchTogetherView.vue
@frontend/web/src/views/WatchTogetherView.spec.ts
@frontend/web/src/composables/useWatchTogetherRoom.ts
@frontend/web/src/composables/__tests__/useWatchTogetherRoom.spec.ts
@frontend/web/src/composables/useToast.ts
@frontend/web/src/router/index.ts
@frontend/web/src/locales/en.json
@frontend/web/src/locales/ru.json
@frontend/web/src/locales/__tests__/watch-together-keys.spec.ts

<interfaces>
<!-- Composable surface (Phase 2 locked) — see useWatchTogetherRoom.ts: -->
roomHandle.onError((e: { code: string; message?: string }) => void)
roomHandle.onRoomClosed(() => void)
// We will ADD: roomHandle.onAuthExpired(() => void) — sugar over onError for ERR_AUTH_EXPIRED

<!-- ERR codes (already exported from @/api/watch-together): -->
ERR_CAPACITY_FULL, ERR_AUTH_EXPIRED, ERR_EPISODE_UNAVAILABLE, ERR_PLAYER_UNAVAILABLE, ERR_TRANSLATION_UNAVAILABLE

<!-- Toast composable (existing): -->
const toast = useToast()
toast.push(message: string, level: 'error' | 'success' | 'info')

<!-- Router (existing): -->
router.push({ path: '/auth', query: { next: route.fullPath } })

<!-- Existing errorState type (line 104 in WatchTogetherView.vue): -->
type ErrorState = 'gone' | 'capacity' | null
// EXTEND to: 'gone' | 'capacity' | 'auth-expired' | null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n keys + parity test scaffolding</name>
  <files>frontend/web/src/locales/en.json, frontend/web/src/locales/ru.json, frontend/web/src/locales/__tests__/watch-together-keys.spec.ts</files>
  <read_first>
    - frontend/web/src/locales/en.json (watch_together namespace)
    - frontend/web/src/locales/ru.json (watch_together namespace)
    - frontend/web/src/locales/__tests__/watch-together-keys.spec.ts (expectedKeys array; possibly interpolation tests for keys with {animeName} placeholders)
  </read_first>
  <behavior>
    Add exactly 4 new i18n keys under `watch_together.*`:

    en.json:
    ```
    "room_ended_redirect_toast": "This Watch Together room has ended.",
    "auth_expired_modal_title": "Your session has expired",
    "auth_expired_modal_body": "Log in again to rejoin the room.",
    "auth_expired_modal_login_button": "Log in"
    ```

    ru.json (parity-locked):
    ```
    "room_ended_redirect_toast": "Эта комната Watch Together закрыта.",
    "auth_expired_modal_title": "Сессия истекла",
    "auth_expired_modal_body": "Войдите снова, чтобы вернуться в комнату.",
    "auth_expired_modal_login_button": "Войти"
    ```

    Update `watch-together-keys.spec.ts` expectedKeys array.

    Audit pass: also smoke-verify every existing watch_together.* key resolves in both locales by extending the parity test (if not already). The Phase 2 spec already enforces key-set parity; this plan just adds 4 more. The smoke-verification of "no raw key strings in DOM" happens in Plan 05.9's end-to-end test.

    Commit: `i18n(05-polish/05.5): 4 polish keys (room_ended_redirect, auth_expired_modal_*) en + ru`.
  </behavior>
  <action>
    Add the 4 keys to both locale files in alphabetical order within the watch_together namespace (preserve existing convention). Update spec expectedKeys. Run `cd frontend/web && bunx vitest run src/locales/__tests__/watch-together-keys.spec.ts` — green.

    Self-check:
    - `grep -c "room_ended_redirect_toast" frontend/web/src/locales/en.json` and `... ru.json` both return 1.
    - `grep -c "auth_expired_modal" frontend/web/src/locales/en.json` and `... ru.json` both return 3.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/locales/__tests__/watch-together-keys.spec.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>i18n parity test green; both locales contain the 4 new keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composable onAuthExpired subscriber + auth-expired branch separation</name>
  <files>frontend/web/src/composables/useWatchTogetherRoom.ts, frontend/web/src/composables/__tests__/useWatchTogetherRoom.spec.ts</files>
  <read_first>
    - frontend/web/src/composables/useWatchTogetherRoom.ts full file — find the existing onError pub-sub + the ERR_AUTH_EXPIRED branch
    - frontend/web/src/composables/__tests__/useWatchTogetherRoom.spec.ts — current 22 tests; find the existing onError test for AUTH_EXPIRED
  </read_first>
  <behavior>
    Add a dedicated `onAuthExpired(handler: () => void): () => void` subscribe method to the composable. It's sugar — internally it wraps onError and filters for ERR_AUTH_EXPIRED. Two reasons to add it:

    1. The view code becomes more readable: `roomHandle.onAuthExpired(showModal)` vs an `if (e.code === ERR_AUTH_EXPIRED)` branch inside the catch-all onError. The 4 state-switching error codes (EPISODE/PLAYER/TRANSLATION_UNAVAILABLE) still flow through onError; the terminal auth case gets its own channel.

    2. Tests can mock onAuthExpired independently in Plan 05.5's view spec without coupling to the broader onError surface.

    Implementation:

    - In useWatchTogetherRoom.ts, near the existing onError implementation, add:
      ```
      function onAuthExpired(handler: () => void): () => void {
        return onError((e) => {
          if (e.code === ERR_AUTH_EXPIRED) handler()
        })
      }
      ```
    - Add it to the return object alongside the existing 9 subscribe methods.
    - Export the type: extend `UseWatchTogetherRoomReturn` to include `onAuthExpired: (handler: () => void) => () => void`.

    Tests (extend the composable spec):
    - `onAuthExpired fires when an ERR_AUTH_EXPIRED frame arrives`
    - `onAuthExpired does NOT fire for other error codes`
    - `onAuthExpired returns an unsubscriber that prevents further calls`

    Commits:
    1. `test(05-polish/05.5): RED — onAuthExpired subscriber semantics`
    2. `feat(05-polish/05.5): GREEN — onAuthExpired composable method`
  </behavior>
  <action>
    TDD: add 3 tests for onAuthExpired, run, expect FAIL. Add the method, run, expect GREEN. Commit each step.

    Self-check:
    - `grep -c "onAuthExpired" frontend/web/src/composables/useWatchTogetherRoom.ts` returns >= 2 (declaration + export).
    - `bunx tsc --noEmit` clean — WatchTogetherRoomHandle / UseWatchTogetherRoomReturn types updated.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/composables/__tests__/useWatchTogetherRoom.spec.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>onAuthExpired exists in composable + tests pass + type exported; no break to existing 22 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WatchTogetherView polish — capacity copy + room-closed redirect + auth-expired modal</name>
  <files>frontend/web/src/views/WatchTogetherView.vue, frontend/web/src/views/WatchTogetherView.spec.ts</files>
  <read_first>
    - frontend/web/src/views/WatchTogetherView.vue full file (current 371 lines)
    - frontend/web/src/views/WatchTogetherView.spec.ts (current 19 tests from Phase 4)
    - frontend/web/src/composables/useToast.ts (push signature)
    - frontend/web/src/router/index.ts (look for an existing Modal component or pattern; if none, we build an inline modal in the view)
    - frontend/web/src/components/watch-together/MemberList.vue (any existing modal we could reuse — usually no; check)
  </read_first>
  <behavior>
    Three behavior changes, all confined to WatchTogetherView.vue (no new component files):

    A) Extend errorState type:
       - `type ErrorState = 'gone' | 'capacity' | 'auth-expired' | null`
       - errorState is now read in 4 template branches (loading | gone | capacity | auth-expired | live).

    B) Capacity-full branch — polish copy + cta:
       - Already exists from Phase 2 (around lines 279-291). No structural change needed; just verify the i18n keys used are `watch_together.capacity_full_title` + `capacity_full_back_button` (these EXIST in en + ru from Phase 2 per 02.2 + 02.8 SUMMARIES).
       - Per 05-CONTEXT.md the copy should be "Room is full (10/10)"; if the existing en.json value is just "Room is full", update it to include the (10/10) suffix. (Edit en.json + ru.json accordingly OR add an interpolation pattern with {capacity}.)
       - Pick the simpler path: hard-code "(10/10)" in the i18n value. en.json `capacity_full_title: "This room is full (10/10)"`. ru.json `capacity_full_title: "Эта комната заполнена (10/10)"`. This means Task 1 also potentially touches existing keys — add to its scope retroactively if needed.

    C) Room-closed redirect — replace `errorState='gone'` with router.push + toast on the WS-side close event:
       - Currently `roomHandle.onRoomClosed(() => { errorState.value = 'gone' })` just shows the empty page.
       - WT-POLISH-05 wants: redirect to /anime/{animeId}/watch + toast.
       - Two cases distinguished:
         1. Initial REST pre-fetch returns 410 → user typed a stale URL → just show the "ended" page with back button (Phase 2 behavior; this is friendlier than an immediate redirect because the user may not realize where they are).
         2. Mid-session room:closed event arrives via WS → user was watching, the room just ended → redirect immediately to the anime watch page with toast.
       - Implementation:
         - In `roomHandle.onRoomClosed(...)`:
           ```
           // WT-POLISH-05 — mid-session room:closed → redirect to anime watch page + toast
           toast.push(t('watch_together.room_ended_redirect_toast'), 'info')
           if (lastAnimeId.value) {
             router.push(`/anime/${lastAnimeId.value}/watch`)
           } else {
             router.push('/')
           }
           ```
         - The REST 410 branch (bootstrap catch block) still sets `errorState.value = 'gone'` so we get the friendlier landing page.
       - Persist lastAnimeId to sessionStorage on bootstrap success so it survives a page reload or the composable resetting state on disconnect.
         - On bootstrap success: `sessionStorage.setItem('wt-last-anime-id-' + roomId, snap.room.anime_id)`.
         - On WS room:state_changed for anime_id (shouldn't happen in v1.0 but defensively): update the cache.
         - On router.push: clear the cache: `sessionStorage.removeItem(...)`.
         - Wrap all sessionStorage in try/catch (privacy modes throw).

    D) Auth-expired modal — new template branch:
       - Replace the existing `roomHandle.onError((e) => { ... ERR_AUTH_EXPIRED branch })` immediate router.push with:
         ```
         roomHandle.onAuthExpired(() => { errorState.value = 'auth-expired' })
         ```
       - New template branch (between 'capacity' and the live layout):
         ```
         <div
           v-else-if="errorState === 'auth-expired'"
           class="fixed inset-0 z-50 flex items-center justify-center bg-background/80 backdrop-blur-sm"
           role="dialog"
           aria-modal="true"
           :aria-labelledby="modalTitleId"
         >
           <div class="max-w-md w-full mx-4 p-6 rounded-lg bg-background border border-foreground/20 flex flex-col gap-4">
             <h2 :id="modalTitleId" class="text-xl font-semibold">
               {{ t('watch_together.auth_expired_modal_title') }}
             </h2>
             <p class="text-foreground/80 font-medium">
               {{ t('watch_together.auth_expired_modal_body') }}
             </p>
             <button
               type="button"
               class="px-4 py-2 rounded-md bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-colors self-end"
               @click="onAuthExpiredLoginClick"
             >
               {{ t('watch_together.auth_expired_modal_login_button') }}
             </button>
           </div>
         </div>
         ```
       - `const modalTitleId = useId()` (Vue 3.5+) OR a static `'wt-auth-expired-modal-title'`. Pick static for compat: less stuff to import.
       - `function onAuthExpiredLoginClick()`:
         ```
         try { sessionStorage.setItem('returnUrl', route.fullPath) } catch {}
         router.push({ path: '/auth', query: { next: route.fullPath } })
         ```
         (Mirrors the existing Phase 2 belt-and-suspenders pattern.)

    E) Remove the immediate router.push in the ERR_AUTH_EXPIRED branch of onError:
       - The existing handler at lines 146-156 redirects immediately to /auth. With the new modal, this is replaced by setting errorState. The user clicks Login → THEN redirect.
       - Net behavior change: the user gets a moment to read the modal title before redirect, AND if they navigate away (router.back, close tab) without clicking Login, they don't end up at the auth page they didn't ask for.

    F) Update tests (extend Phase 4's 19 cases):
       - `capacity_full title includes (10/10) literal`:
         - Mount with errorState='capacity', assert rendered title text includes "(10/10)".
       - `room-closed via onRoomClosed triggers router.push + toast`:
         - Mock useToast + useRouter; mount; simulate onRoomClosed firing; assert toast.push called with the redirect key OR i18n-resolved string; assert router.push called with `/anime/{animeId}/watch`.
       - `room-closed redirects to / when animeId is unknown`:
         - Same setup but with lastAnimeId.value = null; assert router.push('/').
       - `REST 410 initial bootstrap still shows 'gone' empty state (NOT redirect)`:
         - Mock getRoom to throw RoomGoneError; assert errorState='gone' branch renders; assert NO router.push fires.
       - `AUTH_EXPIRED via onAuthExpired sets errorState='auth-expired' (NOT immediate router.push)`:
         - Simulate the FakeHandle's onAuthExpired callback firing; assert errorState='auth-expired'; assert router.push NOT yet called.
       - `auth-expired modal renders title + body + login button with i18n labels`:
         - Force errorState='auth-expired'; assert role="dialog" and aria-modal="true" present; assert button label resolves from i18n stub (not raw key).
       - `clicking Login in modal pushes to /auth?next=/watch/room/{roomId}`:
         - Force errorState='auth-expired'; find button; click; assert router.push called with { path: '/auth', query: { next: route.fullPath } }; assert sessionStorage.setItem called with 'returnUrl'.
       - `sessionStorage.setItem for last-anime-id is called on bootstrap success`:
         - Spy on sessionStorage.setItem; bootstrap returns a valid snap; assert setItem called with the correct key + value.

    G) Update WatchTogetherView.spec.ts FakeHandle:
       - The FakeHandle pattern from Phase 4 already has emitChange* + onPlaybackEvent + onError + onRoomClosed. Add `onAuthExpired: vi.fn(() => () => {})`.

    Commits:
    1. `test(05-polish/05.5): RED — view polish branches (capacity copy, redirect, modal)`
    2. `feat(05-polish/05.5): GREEN — capacity (10/10) + room-closed redirect + auth-expired modal`
  </behavior>
  <action>
    Strict TDD per the behavior contract.

    1. RED commit with the 8 new tests + updated FakeHandle. Existing 19 tests must stay PASS unless their assertions changed (e.g. the AUTH_EXPIRED immediate-redirect test from Phase 2 will now expect modal display instead; update or replace it).

    2. GREEN commit:
       - Extend ErrorState type.
       - Update capacity_full_title in en.json + ru.json to include "(10/10)" suffix (touch is small; can fold into Task 1's commit OR add here).
       - Replace the existing roomHandle.onError ERR_AUTH_EXPIRED branch with errorState assignment.
       - Subscribe to roomHandle.onAuthExpired separately.
       - Update onRoomClosed callback to do toast + redirect (with REST-path distinction via the existing bootstrap catch block which sets errorState='gone').
       - Add the auth-expired modal template branch.
       - Add lastAnimeId sessionStorage cache on bootstrap + on router.push clear.

    3. Build sanity:
       - `bunx tsc --noEmit` clean.
       - `bunx eslint src/views/WatchTogetherView.vue src/views/WatchTogetherView.spec.ts src/composables/useWatchTogetherRoom.ts` clean.
       - `bun run build` — chunk size remains under WT-NF-04 30KB budget; report delta.

    4. Manual browser smoke (document in summary):
       - Capacity test: send 11th user via WS → 11th sees capacity page with (10/10) title.
       - Room-closed test: host clicks DELETE (or use admin API to delete); follower sees toast + redirect to /anime/{id}/watch.
       - Auth-expired test: in DevTools, expire the JWT (modify localStorage.token to be expired); send any action; the WS will close with AUTH_EXPIRED; modal appears; click Login; ensure /auth?next= URL is correct.
  </action>
  <verify>
    <automated>cd frontend/web && bunx vitest run src/views/WatchTogetherView.spec.ts src/composables/__tests__/useWatchTogetherRoom.spec.ts src/locales/__tests__/watch-together-keys.spec.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All view spec cases pass (Phase 4's 19 + new 8 = 27+)
    - bunx tsc --noEmit clean
    - bunx eslint clean
    - WT-POLISH-04: capacity_full title now reads "Room is full (10/10)" in both locales
    - WT-POLISH-05: mid-session room:closed → router.push + toast, REST 410 initial → empty state (distinct paths)
    - WT-POLISH-06: auth-expired modal renders, login button preserves next= URL
    - WT-POLISH-07: zero raw key strings in any rendered branch (verified by i18n parity test + view spec assertions)
    - lastAnimeId persisted to sessionStorage so WS-only close events have a redirect target
    - Bundle size remains under 30KB gz
  </done>
</task>

</tasks>

<verification>
- `cd frontend/web && bunx vitest run src/views/WatchTogetherView.spec.ts src/composables/__tests__/useWatchTogetherRoom.spec.ts src/locales/__tests__/watch-together-keys.spec.ts` all pass
- `grep -c "room_ended_redirect_toast" frontend/web/src/locales/en.json` returns 1
- `grep -c "auth_expired_modal_" frontend/web/src/locales/en.json` returns 3
- `grep -c "auth-expired" frontend/web/src/views/WatchTogetherView.vue` returns at least 2 (errorState type + template branch)
- `grep -c "onAuthExpired" frontend/web/src/composables/useWatchTogetherRoom.ts` returns at least 2
- `grep -c "router.push.*anime.*watch\|router.push.*/anime/" frontend/web/src/views/WatchTogetherView.vue` returns at least 1 (room-closed redirect)
</verification>

<success_criteria>
- WT-POLISH-04: 10/10 page with friendly copy + return-to-anime button.
- WT-POLISH-05: mid-session room:closed → user lands on /anime/{id}/watch with toast.
- WT-POLISH-06: AUTH_EXPIRED → modal with title + body + Login button → /auth?next=…
- WT-POLISH-07: 6 new keys (2 from 05.4 + 4 here) parity-locked en + ru; smoke-verification deferred to Plan 05.9 e2e.
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/05-polish/05.5-SUMMARY.md`. Record:
- All 4 new keys + 1 modified existing key (capacity_full_title) with the new "(10/10)" suffix
- Test count delta (3 composable + 8 view + 4 locale parity = 15+)
- Bundle size before/after
- Manual browser smoke notes (capacity / room-closed / auth-expired)
- Deviations (e.g. if Phase 2 already had a different capacity title, document the override)
</output>
