---
workstream: watch-together
phase: 05-polish
plan: 7
type: execute
wave: 3
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - WT-NF-07
  - WT-NF-05

must_haves:
  truths:
    - "CLAUDE.md §Watch Together section is comprehensive: architecture overview, REST + WS surface summary, link to design doc, link to v1.0 phase summaries, env vars table, key locked decisions table"
    - "CLAUDE.md Service Ports table still lists watch-together at port 8091 (no regression from Phase 1's row addition)"
    - "CLAUDE.md Gateway Routing section still lists /api/watch-together/* (no regression)"
    - "WT-NF-05 dependency audit attestation appears: confirms no new heavyweight libs were added in Phases 1-5 beyond go-redis, gorilla/websocket, libs/jwt, libs/logger, libs/metrics on backend and zero new npm deps on frontend"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Expanded §Watch Together section with architecture, surfaces, decisions, env vars, deps audit"
      contains: "Watch Together"
---

<objective>
Finalize CLAUDE.md per WT-NF-07. Phase 1 added a 3-line stub; Phase 5 expands it into the canonical project-level reference an AI agent or human dev can consult to understand watch-together without reading 5 phase summaries.

Purpose: CLAUDE.md is THE entry point for any AI agent working on this codebase. A 3-line stub for a 5-phase feature is insufficient. The expanded section must let a fresh agent answer: "what is it, what are the surfaces, what are the env vars, what should I NOT touch without a plan?".

Output: One file edit (CLAUDE.md). No new dependencies, no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/05-polish/05-CONTEXT.md
@.planning/workstreams/watch-together/phases/01-backend-foundation/01-PHASE-SUMMARY.md
@.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md
@.planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md
@.planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CLAUDE.md §Watch Together section + add WT-NF-05 dependency audit attestation</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md current §Watch Together section (lines 626-632)
    - CLAUDE.md surrounding context (Service Ports table, Gateway Routing section)
    - All 4 prior PHASE-SUMMARY.md files for the workstream — extract the locked decisions worth surfacing at CLAUDE.md level
    - .planning/workstreams/watch-together/REQUIREMENTS.md (for the env var list — see WT-FOUND-09)
    - go.mod files at services/watch-together/go.mod (for the dependency audit — what's actually required)
    - frontend/web/package.json (for the frontend dep audit — confirm zero new npm deps for watch-together)
  </read_first>
  <behavior>
    Replace the existing §Watch Together stub (3 lines + 3 bullets) with a comprehensive section. Structure:

    ```
    ### Watch Together

    Ephemeral private friend rooms (2-10 members) for synchronized anime watching across all 5 players. v1.0 shipped 2026-05 across 5 phases (backend foundation → frontend shell → player sync → state switching → polish). State is Redis-only with sliding 15min TTL + 5min last-disconnect grace.

    **Architecture:**
    - Single Go microservice `services/watch-together/` (port 8091) — no Postgres, no migrations, Redis-only state under the `wt:` key prefix.
    - REST for room lifecycle (POST/GET/DELETE `/rooms`), WebSocket at `/ws?token=&room=` for real-time sync/chat/reactions/state-changes.
    - All 10 inbound + 10 outbound message types defined in `internal/domain/ws_message.go` (protocol_version "1.0", forward-compat field on every snapshot).
    - Drift detection engine with soft (>1.5s) / hard (>5s) / persistent (5 consecutive) thresholds; per-recipient `playback:correction` envelopes.
    - In-process per-user rate limits (1 seek/s, 5 chat/s) via `golang.org/x/time/rate` token buckets. v2 horizontal-scale will need a Redis-backed limiter (deferred).
    - State validation (episode/player/translation switches): synchronous call to catalog's `/internal/anime/{id}/episodes/validate` with 3s timeout + 5s positive-result cache. Permissive for ourenglish/hanime/raw (v1.1 will tighten).
    - 5min grace timer (`internal/service/grace.go`): last-member-disconnect starts a `time.AfterFunc`; returning member cancels; timer fire broadcasts `room:closed` + deletes Redis keys.

    **HTTP + WS surface (gateway-routed):**
    - `POST   /api/watch-together/rooms` — create room (JWT required)
    - `GET    /api/watch-together/rooms/{id}` — full RoomSnapshot or 410 Gone (JWT required)
    - `DELETE /api/watch-together/rooms/{id}` — host-only force-close (broadcasts `room:closed` then deletes)
    - `WS     /api/watch-together/ws?token=<jwt>&room=<id>` — bidirectional sync channel

    **Frontend:**
    - Route `/watch/room/:roomId` → `WatchTogetherView.vue` (chunk ~7 kB gz, lazy)
    - Composable `useWatchTogetherRoom(roomId)` owns WS lifecycle + reconnect backoff + snapshot replay + 9 emit methods + 10 subscribe methods (onPlaybackEvent, onStateChanged, onRoomClosed, onAuthExpired, onError, …)
    - Player sync via `usePlayerSyncBridge(videoRef, room)` for HTML5 players (AnimeLib/OurEnglish/Hanime/Raw); Kodik via `kodik_player_api` postMessage RPC adapter with boot-time smoke probe + daily Playwright canary at `frontend/web/e2e/kodik-rpc-probe.spec.ts`
    - Reaction whitelist: 24 emoji declared in both backend (`internal/service/inbound.go:reactionWhitelist`) AND frontend (`@/types/watch-together.REACTION_WHITELIST`); MUST update both sides in lock-step

    **Env vars (set in `docker/.env`):**

    | Var | Default | Purpose |
    |-----|---------|---------|
    | `WATCH_TOGETHER_PORT` | 8091 | Service listen port |
    | `WATCH_TOGETHER_REDIS_ADDR` | redis:6379 | Redis backend |
    | `WATCH_TOGETHER_JWT_SECRET` | `${JWT_SECRET}` | JWT validation; same secret as auth service |
    | `WATCH_TOGETHER_MAX_MEMBERS` | 10 | Per-room capacity cap |
    | `WATCH_TOGETHER_ROOM_TTL` | 15m | Sliding TTL on wt:room:* keys |
    | `WATCH_TOGETHER_GRACE_PERIOD` | 5m | Last-disconnect grace before delete |
    | `WATCH_TOGETHER_PUBLIC_BASE_URL` | https://animeenigma.ru | Used to construct invite URLs |
    | `WATCH_TOGETHER_ALLOW_ALL_ORIGINS` | false | Dev override for WS origin allowlist |
    | `WATCH_TOGETHER_CATALOG_URL` | http://catalog:8081 | Catalog HTTP back-channel for state validation |

    The gateway side reads `WATCH_TOGETHER_SERVICE_URL` (default `http://watch-together:8091`) — separate var so the gateway can be redeployed without touching the watch-together service's own env.

    **Locked decisions (across all 5 phases):**
    - Port 8091, Redis-only, `wt:` key prefix, 15min sliding TTL, 5min grace, capacity 10
    - WS auth: `?token=` query param (browsers can't set `Authorization: Bearer` on WS upgrade)
    - Pre-upgrade rejections use HTTP 401/400/404 (NOT close frames) for debuggability
    - DELETE /rooms broadcasts `room:closed` BEFORE deleting Redis keys (Plan 05.1 closed the original 01.4 TODO)
    - 24-emoji reaction whitelist — must be reconciled across backend `internal/service/inbound.go:reactionWhitelist` and frontend `@/types/watch-together.REACTION_WHITELIST`
    - Permissive episode validation for ourenglish/hanime/raw (v1.1 will tighten via scraper round-trip; v1.0 trusts user selection)
    - Window test hook `__wtTestRoom` exposed via VITE_TEST_HOOK in dev/test only — NEVER ship in production builds

    **References:**
    - Design doc: [`docs/superpowers/specs/2026-05-25-watch-together-design.md`](docs/superpowers/specs/2026-05-25-watch-together-design.md)
    - Workstream: [`.planning/workstreams/watch-together/`](.planning/workstreams/watch-together/)
    - Phase summaries: [Phase 1](.planning/workstreams/watch-together/phases/01-backend-foundation/01-PHASE-SUMMARY.md) · [Phase 2](.planning/workstreams/watch-together/phases/02-frontend-shell/02-PHASE-SUMMARY.md) · [Phase 3](.planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md) · [Phase 4](.planning/workstreams/watch-together/phases/04-state-switching/04-PHASE-SUMMARY.md) · [Phase 5](.planning/workstreams/watch-together/phases/05-polish/05-PHASE-SUMMARY.md)
    - Grafana dashboard: `infra/grafana/dashboards/watch-together.json` (auto-provisioned; UID `watch-together`)
    - Kodik RPC reference: `reference_kodik_inbound_postmessage_api.md` (user memory)

    **Dependency audit (WT-NF-05):**

    Backend (`services/watch-together/go.mod`): only project-default + minimal additions —
    - `github.com/gorilla/websocket` (WS lib)
    - `github.com/redis/go-redis/v9` (Redis client)
    - `golang.org/x/time/rate` (token bucket)
    - `github.com/google/uuid` (instance/room ids)
    - `github.com/prometheus/client_golang` (metrics — already used project-wide)
    - All `libs/*` modules used: logger, metrics, jwt, errors

    Frontend: ZERO new npm dependencies introduced by watch-together across all 5 phases. All UI uses vue 3 + pinia + vue-i18n + vue-router + tailwind (all pre-existing).

    Audited 2026-05-25 (close of v1.0). No license-incompatible deps; all licenses are MIT/BSD/Apache-compatible with project (MIT).

    **Daily Kodik canary CI:**

    The Kodik postMessage RPC is undocumented; the bundle could change at any time. A Playwright spec at `frontend/web/e2e/kodik-rpc-probe.spec.ts` runs daily via GitHub Actions (`.github/workflows/watch-together-kodik-canary.yml`) and alerts via Telegram on failure. If you see this alert: confirm `kodik_player_api` still works in browser DevTools by sending `{key:'kodik_player_api',value:{method:'get_time'}}` to a Kodik iframe; if the dispatcher really has changed, all rooms using Kodik will fall back to "Kodik sync unavailable" banner mode — single-user playback still works.
    ```

    Other CLAUDE.md touch-ups (only if these are inconsistent — verify by grep before editing):
    - Service Ports table row for `watch-together` already exists (line 605); leave it alone.
    - Gateway Routing bullet for `/api/watch-together/*` already exists (line 624); leave it alone.

    Constraints:
    - DO NOT touch any other section of CLAUDE.md.
    - DO NOT rename existing tables/sections.
    - DO NOT introduce emoji into the section unless the rest of CLAUDE.md uses them in the same role (per project convention).
    - Mention the design doc by full path AND the workstream directory.
    - The "Locked decisions" sub-section is intentionally short — comprehensive locked decisions live in PHASE-SUMMARY files; CLAUDE.md surfaces only the dozen-or-so a fresh agent needs to avoid breaking things.
  </behavior>
  <action>
    1. Read CLAUDE.md lines 620-640 to confirm exact bounds of the current §Watch Together section.

    2. Use Edit (NOT Write) to replace the section. Preserve the surrounding context exactly.

    3. After editing, validate:
       - The §Service Ports table row still shows watch-together (grep).
       - The §Gateway Routing bullet still shows /api/watch-together/* (grep).
       - The new §Watch Together section is 80+ lines (substantial expansion from the 7-line stub).
       - No raw markdown syntax errors (use `bunx markdownlint CLAUDE.md` if markdownlint is configured; else eyeball).

    4. Verify dep audit accuracy:
       - `grep -E '^\s+github\.com|^\s+golang\.org' services/watch-together/go.mod | sort` — confirm the list in CLAUDE.md matches reality. Add/remove dep names accordingly.
       - `git diff frontend/web/package.json` from Phase 1 baseline (now over a week ago) — if zero changes to dependencies/devDependencies sections, the "ZERO new npm deps" claim holds. If there ARE changes, document them.

    5. Self-check after edit:
       - `grep -c "Watch Together" CLAUDE.md` returns at least 3 (Service Ports row + Gateway Routing + the new H3 + body references).
       - `grep -c "WATCH_TOGETHER_GRACE_PERIOD" CLAUDE.md` returns at least 1.
       - `grep -c "kodik_player_api" CLAUDE.md` returns at least 1.

    Commit: `docs(05-polish/05.7): finalize CLAUDE.md §Watch Together section (WT-NF-07 + WT-NF-05 audit)`.
  </action>
  <verify>
    <automated>grep -A 80 "^### Watch Together$" /data/animeenigma/CLAUDE.md | wc -l</automated>
  </verify>
  <done>
    - §Watch Together is 80+ lines, comprehensive
    - All env vars documented in a table
    - Design doc + workstream + 5 phase summary links discoverable
    - Locked decisions surfaced
    - WT-NF-05 dependency audit attestation present
    - Kodik canary explanation includes a debugging recipe
    - No regression to Service Ports table or Gateway Routing section
  </done>
</task>

</tasks>

<verification>
- `grep -c "watch-together" CLAUDE.md` returns >= 5 (multiple section references)
- `grep -c "WATCH_TOGETHER_" CLAUDE.md` returns >= 8 (env var table)
- `grep -c "/api/watch-together" CLAUDE.md` returns >= 2 (HTTP + WS surface mentions)
- `grep -c "05-PHASE-SUMMARY" CLAUDE.md` returns 1 (Phase 5 summary link)
- `grep -c "kodik_player_api" CLAUDE.md` returns >= 1
</verification>

<success_criteria>
- WT-NF-07 documentation requirement satisfied
- WT-NF-05 dependency audit attestation in writing
- Fresh AI agent reading CLAUDE.md can describe watch-together's architecture, surfaces, env vars, and known constraints without reading any other file
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/05-polish/05.7-SUMMARY.md`. Record:
- Final line count of §Watch Together section
- The dep list verified against go.mod and package.json (timestamped)
- Any deviations from this plan (e.g. if a critical decision listed here turned out NOT to be canonical and needed removal)
</output>
