---
workstream: watch-together
phase: 05-polish
plan: 8
type: execute
wave: 3
depends_on: []
files_modified:
  - .github/workflows/watch-together-kodik-canary.yml
autonomous: true
requirements:
  - WT-SYNC-10

must_haves:
  truths:
    - "A new GitHub Actions workflow file exists at .github/workflows/watch-together-kodik-canary.yml"
    - "The workflow is scheduled daily via cron: trigger (matches the player-health.yml cadence pattern but at a different time to avoid fleet contention)"
    - "The workflow runs `bunx playwright test e2e/kodik-rpc-probe.spec.ts --grep '[canary]'` (or equivalent invocation that targets only the canary)"
    - "On failure, a Telegram notification is sent to the admin chat using the existing TELEGRAM_BOT_TOKEN + TELEGRAM_ADMIN_CHAT_ID secrets (same pattern as player-health.yml)"
    - "On success, NO alert is sent (silent success — Telegram noise is bad for a daily canary)"
    - "Manual workflow_dispatch is also wired so operators can trigger the canary on demand without waiting for the cron"
    - "Test artifacts (playwright report + results.json) are uploaded for 7 days for postmortem"
  artifacts:
    - path: ".github/workflows/watch-together-kodik-canary.yml"
      provides: "Daily-cron workflow, single-job, Playwright canary execution + Telegram alert on failure"
      min_lines: 70
---

<objective>
Wire the daily Kodik RPC canary cron + alerting per WT-SYNC-10 (deferred from Phase 3 to Phase 5). The spec already exists; this plan ONLY adds the CI plumbing. No new test code, no new infra.

Purpose: The undocumented `kodik_player_api` postMessage RPC is the single point of failure for RU watch-together sync. If Kodik changes their bundle and removes the dispatcher, every RU room silently degrades. The daily canary is the early-warning signal — fail loud, fail to one human (admin Telegram), let the human investigate before users complain.

Output: ONE new file (a GitHub Actions workflow YAML). Mirrors the existing `player-health.yml` pattern exactly to minimize maintenance surface. No test code changes — Plan 03.4 / 03.6 already shipped the spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/watch-together/phases/05-polish/05-CONTEXT.md
@.planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md
@.github/workflows/player-health.yml
@frontend/web/e2e/kodik-rpc-probe.spec.ts

<interfaces>
<!-- Existing pattern (player-health.yml — copy this almost verbatim, change schedule + paths): -->
//   - cron: '7 6 * * *'   (daily 6:07 UTC — off-the-hour to avoid fleet contention)
//   workflow_dispatch trigger for manual runs
//   matrix.project: [chrome, firefox, webkit]  — we can simplify since canary only needs one browser
//   working-directory: frontend/web
//   bun install + bunx playwright install --with-deps
//   Upload artifacts on always() with retention-days: 7
//   notify job: needs: health-check; if: failure(); curl -X POST telegram

<!-- Existing Playwright spec (Plan 03.4): -->
//   frontend/web/e2e/kodik-rpc-probe.spec.ts (354 lines, `[canary]`-tagged)
//   Invocation: `bunx playwright test e2e/kodik-rpc-probe.spec.ts` (the spec file ALREADY has the canary tagging built in; --grep is not strictly required if the file is dedicated)

<!-- The spec runs against https://animeenigma.ru by default; verify by re-reading the spec for BASE_URL behavior. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/watch-together-kodik-canary.yml</name>
  <files>.github/workflows/watch-together-kodik-canary.yml</files>
  <read_first>
    - .github/workflows/player-health.yml (full file — the canonical template to mirror)
    - frontend/web/e2e/kodik-rpc-probe.spec.ts (first 50 lines + the test() definitions to understand exact invocation requirements — does it need BASE_URL, JWT, etc.?)
    - .planning/workstreams/watch-together/phases/03-player-sync/03-PHASE-SUMMARY.md §Files created (canary spec details)
  </read_first>
  <behavior>
    Author one YAML file. Structure mirrors player-health.yml with the following adaptations:

    1. Name: `Watch Together — Kodik RPC Canary`

    2. Triggers:
       - `schedule: - cron: '13 7 * * *'` (daily 7:13 UTC — offset from player-health.yml's 6:07 so the two workflows don't compete for runner capacity)
       - `workflow_dispatch:` (manual trigger; empty input set)

    3. Jobs:
       a) `canary` job:
          - runs-on: ubuntu-latest
          - timeout-minutes: 10
          - working-directory: frontend/web
          - Steps:
            1. `uses: actions/checkout@v4`
            2. `uses: oven-sh/setup-bun@v1` with bun-version: latest
            3. Install dependencies: `bun install`
            4. Install Playwright browsers (chromium only — the canary doesn't need to verify across browsers; if Chromium fails, the admin should investigate; firefox/webkit add CI minutes without proportional signal):
               `bunx playwright install --with-deps chromium`
            5. Run the canary:
               `bunx playwright test e2e/kodik-rpc-probe.spec.ts --project=chromium`
               env: BASE_URL: https://animeenigma.ru
            6. Upload artifacts (always(), retention-days: 7):
               - `frontend/web/playwright-report/` (HTML report)
               - `frontend/web/test-results/` (raw artifacts including screenshots/videos)

       b) `notify` job:
          - runs-on: ubuntu-latest
          - needs: canary
          - if: failure()
          - Single step: curl Telegram with a message:
            ```
            MESSAGE="🚨 *Watch Together Kodik Canary FAILED*

            The undocumented kodik_player_api postMessage RPC may have changed. RU watch-together sync could be degraded.

            🔍 Debug recipe:
            1. Open https://animeenigma.ru in a browser, navigate to any anime with Kodik player.
            2. In DevTools console:
               window.frames[0].postMessage({key:'kodik_player_api',value:{method:'get_time'}}, '*')
            3. If no \`kodik_player_time\` response arrives within 2s, the RPC is gone.

            🔗 [View Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})

            _Daily canary — $(date -u '+%Y-%m-%d %H:%M UTC')_"
            ```
          - Telegram secrets: TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}; TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_ADMIN_CHAT_ID }} (same names as player-health.yml uses).

    4. Documentation comment at top of the file:
       ```
       # Watch Together — Kodik RPC Canary (WT-SYNC-10)
       #
       # Daily check that the undocumented kodik_player_api postMessage RPC
       # (https://reference_kodik_inbound_postmessage_api.md) still works.
       # Hard-fails only when the dispatcher is genuinely gone (test.skip otherwise).
       #
       # Spec: frontend/web/e2e/kodik-rpc-probe.spec.ts (shipped Plan 03.4)
       # Workflow plumbing: Plan 05.8 (this file)
       #
       # On failure, alerts admin via Telegram using TELEGRAM_BOT_TOKEN +
       # TELEGRAM_ADMIN_CHAT_ID secrets (same secrets as .github/workflows/player-health.yml).
       ```

    Constraints:
    - Use single-quote strings for cron values (YAML quirk).
    - Use `secrets.TELEGRAM_BOT_TOKEN` and `secrets.TELEGRAM_ADMIN_CHAT_ID` — verify these secrets exist by checking whether player-health.yml uses them (it does — see lines 65-66 of that file).
    - Do NOT add a job that auto-creates a GitHub issue; over-engineering and noisy. Telegram alone is the v1.0 channel.
    - Do NOT skip the artifact upload — the playwright HTML report is the primary postmortem signal when the canary fires.
    - DO NOT change the spec file (kodik-rpc-probe.spec.ts) — it ships from Plan 03.4 with the right skip/fail logic.
  </behavior>
  <action>
    1. Read player-health.yml in full to confirm the exact YAML structure (especially the if: failure() and curl invocation).

    2. Read kodik-rpc-probe.spec.ts opening to confirm:
       - What `BASE_URL` it expects (does it have a default?).
       - Whether it needs a JWT/login (probably not — Kodik is an iframe; the canary only needs to load a public anime page).
       - The exact spec file path relative to `frontend/web`.

    3. Create `.github/workflows/watch-together-kodik-canary.yml` per the behavior contract. Pay attention to YAML indentation (2-space, consistent).

    4. Validate:
       - `python3 -c "import yaml; yaml.safe_load(open('/data/animeenigma/.github/workflows/watch-together-kodik-canary.yml'))"` exits 0.
       - `cat .github/workflows/watch-together-kodik-canary.yml | grep -E '^(name|on|jobs):'` shows the three top-level keys.
       - Manual `gh workflow view watch-together-kodik-canary.yml` (if `gh` is configured and PR-merged) would confirm registration — defer this validation to post-merge.

    5. Trigger a manual test run (document in summary):
       - After merge to main: `gh workflow run watch-together-kodik-canary.yml` and verify the run logs show:
         - `bun install` succeeds
         - playwright install succeeds
         - the canary test executes against https://animeenigma.ru
         - If the test passes: no telegram message; the canary job succeeds.
         - If the test fails: notify job runs; admin receives Telegram message with the debug recipe.

    Commit: `feat(05-polish/05.8): daily Kodik RPC canary workflow (WT-SYNC-10)`.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('/data/animeenigma/.github/workflows/watch-together-kodik-canary.yml')); print('VALID')"</automated>
  </verify>
  <done>
    - .github/workflows/watch-together-kodik-canary.yml exists, valid YAML
    - schedule cron set to '13 7 * * *' (or another off-the-hour value documented in the file's header comment)
    - workflow_dispatch trigger present
    - chromium-only matrix (or no matrix; single-project run)
    - Playwright canary invocation targets e2e/kodik-rpc-probe.spec.ts
    - On failure: Telegram alert with debug recipe + run URL
    - Artifacts uploaded with 7-day retention
    - Header comment documents the workflow's purpose + cross-references
    - No regression to player-health.yml or other workflows
  </done>
</task>

</tasks>

<verification>
- `ls /data/animeenigma/.github/workflows/watch-together-kodik-canary.yml` exists
- `python3 -c "import yaml; yaml.safe_load(open('/data/animeenigma/.github/workflows/watch-together-kodik-canary.yml'))"` exits 0
- `grep -c "cron:" .github/workflows/watch-together-kodik-canary.yml` returns 1
- `grep -c "workflow_dispatch" .github/workflows/watch-together-kodik-canary.yml` returns 1
- `grep -c "TELEGRAM_BOT_TOKEN\|TELEGRAM_ADMIN_CHAT_ID" .github/workflows/watch-together-kodik-canary.yml` returns 2
- `grep -c "kodik-rpc-probe" .github/workflows/watch-together-kodik-canary.yml` returns 1
</verification>

<success_criteria>
- WT-SYNC-10 final CI wiring complete (spec ships from Phase 3, cron ships from Phase 5)
- Daily silent success in production; Telegram alert on first real failure
- 7-day artifact retention enables postmortem
</success_criteria>

<output>
After completion, create `.planning/workstreams/watch-together/phases/05-polish/05.8-SUMMARY.md`. Record:
- Final cron time chosen (and why — fleet-contention rationale)
- Confirmation of secrets availability (`gh secret list` if the agent has gh credentials, else note "secrets assumed present per player-health.yml pattern")
- Manual workflow_dispatch test result (success/fail) — should be invoked at least once post-merge to confirm wiring
</output>
