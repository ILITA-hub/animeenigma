apiVersion: v1
kind: Secret
metadata:
  name: admin-basic-auth
  namespace: monitoring
type: Opaque
stringData:
  # Generate with: htpasswd -nb admin <password>
  # Default: admin:admin (CHANGE IN OVERLAY)
  auth: admin:$apr1$8n5j2k8x$QYvC0F1HH8EXAMPLE
