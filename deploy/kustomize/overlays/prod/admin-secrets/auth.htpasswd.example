# Copy to auth.htpasswd (git-ignored). Generate a real line with:
#   htpasswd -nbB admin "$(openssl rand -base64 24)"
# (record the password in your password manager — it gates /admin/*)
admin:CHANGE_ME
